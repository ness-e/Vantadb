[package]
name = "experimental-lisp"
version = "0.1.0"
edition = "2021"
description = "Experimental LISP/IQL query language module for VantaDB"
publish = false

[dependencies]
vantadb = { path = "../.." }
nom = "7"
serde = { version = "1", features = ["derive"] }
rand = "0.9"

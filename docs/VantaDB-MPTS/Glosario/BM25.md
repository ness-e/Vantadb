---
type: glosario-entry
status: stable
tags: [indice, busqueda-texto, bm25, full-text-search]
last_refined: 2026-06
links: "[Glosario](../Glosario.md)"
aliases: [Best Matching 25, BM25 Scoring, Full-Text Search]
description: "Algoritmo de ranking de relevancia para búsqueda de texto completo (full-text search), función de scoring probabilística que clasifica documentos según su relevancia para una consulta"
---

# BM25 — Best Matching 25

## Definición

**BM25** (Best Matching 25) es un algoritmo de **ranking de relevancia** para búsqueda de texto completo (full-text search). Es una función de scoring probabilística que clasifica documentos según su relevancia para una consulta, mejorando el modelo TF-IDF clásico.

## Fórmula Matemática

Para una consulta $Q$ con términos $q_1, q_2, \ldots, q_n$, el score BM25 de un documento $D$ es:

$$
\text{score}(D, Q) = \sum_{i=1}^{n} \text{IDF}(q_i) \cdot \frac{f(q_i, D) \cdot (k_1 + 1)}{f(q_i, D) + k_1 \cdot \left(1 - b + b \cdot \frac{|D|}{\text{avgdl}}\right)}
$$

Donde:
- $f(q_i, D)$ = frecuencia del término $q_i$ en el documento $D$
- $|D|$ = longitud del documento (número de términos)
- $\text{avgdl}$ = longitud promedio de documentos en la colección
- $k_1 \in [1.2, 2.0]$ = saturación de frecuencia de término
- $b = 0.75$ = normalización por longitud de documento
- $\text{IDF}(q_i)$ = inverse document frequency

### Componentes Clave

| Componente | Fórmula | Propósito |
|-----------|---------|-----------|
| **IDF** | $\log\frac{N - n(q_i) + 0.5}{n(q_i) + 0.5}$ | Penalizar términos comunes |
| **TF** | $\frac{f \cdot (k_1 + 1)}{f + k_1 \cdot (1 - b + b \cdot \frac{|D|}{\text{avgdl}})}$ | Saturación de frecuencia |
| **Length Norm** | $1 - b + b \cdot \frac{|D|}{\text{avgdl}}$ | Penalizar documentos largos |

## Cómo Funciona

### Ejemplo Práctico

**Corpus:** 1000 documentos

**Query:** `"base de datos vectorial"`

**Documento A:**
- Contiene: "base", "datos", "vectorial"
- Longitud: 50 palabras
- Score BM25: **12.4**

**Documento B:**
- Contiene: "base", "datos" (pero no "vectorial")
- Longitud: 200 palabras
- Score BM25: **6.8**

**Documento C:**
- Contiene: "base" (10 veces), "datos" (5 veces)
- Longitud: 30 palabras
- Score BM25: **8.2** (saturación por repetición)

### Ranking Final

1. **Documento A** (12.4) — Más relevante
2. **Documento C** (8.2) — Relevante pero repetitivo
3. **Documento B** (6.8) — Menos relevante

## Implementación en VantaDB

### Índice Invertido

BM25 requiere un **índice invertido**:

```
Término → Lista de documentos que lo contienen

"base" → [doc1, doc3, doc7, doc12, ...]
"datos" → [doc1, doc2, doc3, doc15, ...]
"vectorial" → [doc1, doc8, doc20, ...]
```

### Estructura en VantaDB

```rust
pub struct Bm25Index {
    // Término → Posting list
    postings: HashMap<String, PostingList>,
    
    // Estadísticas globales
    total_docs: usize,
    avg_doc_length: f32,
    
    // Parámetros
    k1: f32,  // Default: 1.2
    b: f32,   // Default: 0.75
}

pub struct PostingList {
    doc_ids: Vec<u64>,
    term_frequencies: Vec<u32>,
    positions: Vec<Vec<u32>>,  // Para phrase search
}
```

### Tokenización

```
Texto: "VantaDB es una base de datos vectorial rápida"
        │
        ▼
Tokenizador (lowercase + ascii-alnum)
        │
        ▼
Tokens: ["vantadb", "es", "una", "base", "de", "datos", "vectorial", "rápida"]
        │
        ▼
Filtrado (stopwords opcionales)
        │
        ▼
Tokens finales: ["vantadb", "base", "datos", "vectorial", "rápida"]
```

### Búsqueda BM25 en VantaDB

```python
# Búsqueda léxica
results = db.search(
    text="base de datos vectorial",
    top_k=10,
    mode="bm25"  # Solo búsqueda léxica
)
```

## BM25 vs Búsqueda Vectorial

| Dimensión | BM25 (Léxico) | [HNSW](HNSW.md) (Vectorial) |
|-----------|---------------|---------------------|
| **Tipo de búsqueda** | Keyword matching | Similitud semántica |
| **Query** | Texto exacto | Vector embedding |
| **Captura** | Coincidencia de términos | Significado |
| **Ejemplo query** | "PostgreSQL" | "base de datos relacional" |
| **Fuerza** | Precisión en keywords | Comprensión semántica |
| **Debilidad** | No entiende sinónimos | Puede perder keywords exactos |

### Ejemplo de Complementariedad

**Query:** `"¿Cómo configurar PostgreSQL?"`

**BM25 encuentra:**
- Documentos con "PostgreSQL" y "configurar"
- ❌ No encuentra documentos sobre "configurar Postgres" (sin "SQL")

**HNSW encuentra:**
- Documentos sobre "configurar base de datos"
- ✅ Encuentra "configurar Postgres" (semánticamente similar)
- ❌ Puede perder documentos específicos de PostgreSQL

**Solución:** [RRF](RRF.md) fusiona ambos resultados.

## Búsqueda Híbrida en VantaDB

VantaDB combina BM25 + HNSW mediante [RRF](RRF.md):

```python
# Búsqueda híbrida (default)
results = db.search(
    vector=embed("¿Cómo configurar PostgreSQL?"),
    text="configurar PostgreSQL",
    top_k=10,
    mode="hybrid"  # BM25 + HNSW + RRF
)
```

### Flujo de Búsqueda Híbrida

```
Query: "configurar PostgreSQL"
    │
    ├─▶ BM25 Index ──▶ Lista 1: [doc5, doc12, doc23, ...]
    │
    ├─▶ HNSW Index ──▶ Lista 2: [doc3, doc7, doc12, doc45, ...]
    │
    └─▶ RRF Fusion ──▶ Ranking Unificado: [doc12, doc5, doc7, ...]
```

## Parámetros de BM25

| Parámetro | Rango | Default | Efecto |
|-----------|-------|---------|--------|
| **k1** | [1.2, 2.0] | 1.2 | Saturación de TF. Mayor = más peso a frecuencia |
| **b** | [0, 1] | 0.75 | Normalización de longitud. 0 = sin norma, 1 = norma completa |

### Cuándo Ajustar Parámetros

| Caso | k1 | b | Razón |
|------|----|----|-------|
| Documentos cortos (tweets) | 1.2 | 0.0 | Longitud uniforme |
| Documentos largos (papers) | 2.0 | 0.75 | Penalizar verbosidad |
| Keywords repetitivos | 1.0 | 0.75 | Menos saturación |
| Vocabulario técnico | 1.5 | 0.5 | Balance |

## Limitaciones de BM25

### 1. No Captura Semántica

```
Query: "carro rojo"
BM25 NO encuentra: "automóvil escarlata"
HNSW SÍ encuentra: (vectores similares)
```

### 2. Sensible a Tokenización

```
"don't" → ["don", "t"] vs ["don't"]
Resultados diferentes según tokenizador
```

### 3. No Maneja Sinónimos

```
Query: "laptop"
BM25 NO encuentra: "notebook", "portátil"
```

### 4. Requiere Índice Invertido

- **Memoria:** Posting lists pueden ser grandes
- **Construcción:** Más lento que HNSW para updates
- **Updates:** Reindexación costosa

## Optimizaciones en VantaDB

### 1. Compresión de Posting Lists

```rust
// Sin compresión: [1, 5, 9, 13, 17, 21]
// Con delta encoding: [1, 4, 4, 4, 4, 4]
// Con varint: [0x01, 0x04, 0x04, 0x04, 0x04, 0x04]
```

**Reducción:** 60-80% de memoria.

### 2. Phrase Search con Posiciones

```rust
// Almacenar posiciones de términos
"base de datos" → {
    "base": [0, 15, 42],
    "de": [1, 16, 43],
    "datos": [2, 17, 44]
}

// Phrase query: "base de datos"
// Buscar posiciones consecutivas: [0,1,2], [15,16,17], [42,43,44]
```

### 3. Early Termination

```rust
// Si ya tenemos K documentos con score > threshold,
// detener búsqueda en posting lists restantes
```

## Comparación con Otros Algoritmos de Texto

| Algoritmo | Tipo | Velocidad | Calidad | Caso de Uso |
|-----------|------|-----------|---------|-------------|
| **BM25** | Probabilístico | Rápido | Alta | General purpose |
| **TF-IDF** | Estadístico | Rápido | Media | Legacy |
| **Language Models** | Neural | Lento | Muy alta | Reranking |
| **Elasticsearch** | BM25 + plugins | Rápido | Alta | Enterprise |
| **Tantivy** | BM25 (Rust) | Muy rápido | Alta | VantaDB |

## Véase También

- [HNSW](HNSW.md) — Índice vectorial complementario
- [RRF](RRF.md) — Fusión de BM25 + HNSW
- [Vectores](Vectores.md) — Representación semántica
- [RAG](RAG.md) — Caso de uso principal

---

*BM25 es el estándar de oro para búsqueda léxica, complementario (no reemplazo) de búsqueda vectorial.*


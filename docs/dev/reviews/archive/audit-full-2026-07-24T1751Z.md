---
title: "Audit Report: full"
type: review
status: archived
tags: [vantadb, review]
last_reviewed: 2026-09-15
aliases: []
related: []
---

# Audit Report: full

**Date:** 2026-07-24T17:51:24Z
**Duration:** ~20 min (abortado en Phase 1)
**Mode:** full
**Branch:** develop
**Veredict:** ❌ FAIL — pre-existing test failure blocked pipeline

## Summary

- Phases completed: 1/9
- Blocking issues: 1 (pre-existing test failure)
- Recommendations: 3
- Bugs fixed during audit: 2 (fmt + WASM type error)

## Per-Phase Results

| Phase | Status | Details |
|-------|--------|---------|
| 0. Pre-check | ✅ | 5 modified files, branch develop |
| 1. CLI | ❌ | fmt ✅ clippy ✅ test ❌ deny ✅ audit ✅ machete ✅ |
| 2. Security | ⏭ | Abortado — test failure en Phase 1 |
| 3. Performance | ⏭ | — |
| 4. Code Review | ⏭ | — |
| 5. Root Cause | ⏭ | — |
| 6. Deep Module | ⏭ | — |
| 7. Full ISO | ⏭ | — |
| 8. Certify | ⏭ | — |

## Findings

### Critical

1. **`src/metrics/core/registry.rs:777` — test failure preexistente**
   - `test_metrics_registry_created` asume que `METRICS_REGISTRY.gather()` devuelve métricas
   - Pero `METRICS_REGISTRY` es un `LazyLock<Registry>` sin registro automático de métricas
   - Las métricas individuales también son lazy (`LazyLock<Option<M>>`) y no se inicializan hasta su primer acceso
   - **Fix:** `init_metric(&QUERY_LATENCY)` antes de gather, o eliminar el test
   - **No relacionado a cambios actuales.** Es un bug preexistente que bloquea todo CI/CD.

### Important

2. **`vantadb-wasm/src/worker.rs:210` — type mismatch en `js_sys::Reflect::apply`** (✅ FIXED)
   - `set_timeout` era `JsValue` pero `Reflect::apply` espera `&Function`
   - Se agregó `dyn_into::<js_sys::Function>()` para downcast seguro
   - Bug preexistente feature-gated (solo fallaba con `--all-features`)

3. **`src/sdk/api.rs:944` — formato rustfmt** (✅ FIXED)
   - `assert!()` multiline mal formateado
   - Corregido con `cargo fmt`

### Warnings

4. **`deny.toml` — 7 duplicados en Cargo.lock**
   - `r-efi`, `rand`, `rand_core`, `syn`, `thiserror`, `thiserror-impl`, `windows-sys`
   - Todos warnings, no errores. Típico de transiciones entre major versions de dependencias.

## FODA (Deducción Estratégica)

| Dimensión | Hallazgos |
|-----------|-----------|
| **Fortalezas** | Clippy ✅ sin warnings con `-D warnings`. Deny ✅, Machete ✅, Audit ✅. Sin dependencias no usadas, sin vulnerabilidades conocidas. |
| **Oportunidades** | Arreglar test `test_metrics_registry_created` destrabaría todo CI/CD. Simplificar: o eliminar el test (YAGNI — la registry se verifica en otros tests) o agregar un init_metric. |
| **Debilidades** | Test de metrics con diseño defectuoso: asume lazy statics auto-registran. Bug preexistente en WASM con `--all-features` (✅ fixed). |
| **Amenazaz** | Duplicados en Cargo.lock (thiserror 1.x y 2.x, syn 2.x y 3.x) pueden causar conflictos en el futuro. |

## Veredicto
❌ FAIL — pre-existing test failure `test_metrics_registry_created` bloqueó pipeline en Phase 1. No relacionado a cambios actuales. Arreglar el test destrabaría full pipeline.
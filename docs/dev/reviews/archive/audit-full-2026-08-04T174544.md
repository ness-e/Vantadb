---
title: "Audit Report: full — 2026-08-04T174544"
type: review
status: archived
tags: [vantadb, review]
last_reviewed: 2026-09-15
aliases: []
related: []
---

# Audit Report: full — 2026-08-04T174544

**Branch:** develop
**Mode:** full
**Scope:** docs-only working tree; recent substantive commits = cost-based routing (`db3111c1`) + sparse vectors (`3b449a04`)
**Tier:** full pipeline (phases 0→1→2→3→4→5→6→7→8)

## Summary
- Phases completed: 8/9 (Phase 5 skipped — no failures in waves 1-2)
- Blocking issues: 1 (Critical)
- Recommendations: 12
- **Veredict: ❌ FAIL** — critical memory-safety defect (C1) on the Python SDK NumPy path blocks release-grade readiness; nocturnal benchmark also crashes.

## Per-Phase Results

| Phase | Status | Wave | Details |
|-------|--------|------|---------|
| 0. Pre-check | ✅ | direct | 4 unstaged/untracked (docs-only), develop |
| 1. CLI | ✅ | direct | fmt ✅ clippy ✅ test ✅ deny ✅ audit ✅ machete ✅ · size ⏭ timeout |
| 2. Security | 🔶 | sub-agent | 2 important FFI findings; P2-6 resolved |
| 3. Performance | 🔶 | sub-agent | 6 findings (1 medium) |
| 4. Code Review | 🔶 | sub-agent | no Rust in scope; 2 medium proc + 3 nits |
| 5. Root Cause | ⏭ | skipped | no failures in waves 1-2 at scheduling time |
| 6. Deep Module | 🔶 | sub-agent | module 1 serialization/search: 6.5/10 |
| 7. Full ISO | ❌ | sub-agent | C1 critical; security 5/10; overall BLOCKED |
| 8. Certify | ❌ | direct | benchmark crash (`0xC0000409`); C1 unresolved |

## Findings by Phase

### Phase 2: Security
- `vantadb-python/src/vector.rs:63-73` (important) — `__array_interface__` zero-copy exposes `Box<[f32]>` ptr to NumPy; "Box never reallocates" defense covers realloc but **not drop-while-view-alive**.
- `vantadb-python/src/types.rs:365-380` (important) — same zero-copy exposure via `Option<Vec<f32>>`, **worse**: `Vec` can reallocate on Rust-side mutation, invalidating the view.
- `src/error.rs:90` ✅ — P2-6 resolved (`#[non_exhaustive]` + wildcard recovery).
- `src/index/serialize.rs:248-263` ✅ — `take_bytes` bounds-checked correctly.
- `deny.toml` ✅ — single advisory RUSTSEC-2023-0089 triaged (expiry 2027-01-01, migration path documented).
- `thiserror` 1.0.69/2.0.19 duplicate — not a real risk (suggestion only).

### Phase 3: Performance
- **F1 (medium)** `src/sdk/serialization/mod.rs:271-279` — sparse vector JSON `from_str` on every read (and `to_string` on every write, `:335-338`), even when caller doesn't use sparse; `.ok()` swallows parse errors → silent degradation to `None`.
- F2 (low) `src/sdk/search/mod.rs:721-746` — `sparse_memory_search` full-O(N) namespace scan + `sort_hits` O(N log N); no inverted index. ADR-011 accepts as fallback → route to vanta-engine.
- F3 (low) `src/planner.rs:144-185` — RRF `BTreeMap<(String,String),Hit>` clones 2 Strings per candidate even on single-channel paths (ISO review downgraded to low: negligible ≤750 candidates).
- F4/F7/P2-7 (low→medium) — full-copy serialization on hot path persists.
- P2-3 `vantadb-python/src/convert.rs:23-70` — LRU O(n) eviction, comment wrongly claims O(1); cap 64 → negligible.

### Phase 4: Code Review
- (medium) `.playwright-cli/` untracked, NOT in `.gitignore` — risk of committing browser session artifacts. (Verified: no secrets in content.)
- (medium) `tasks/plan.md` — live execution file outside VCS with closed-campaign content; violates temp-plan hygiene. (ISO review later confirmed now absent — resolved.)
- Nits: language-consistency header (`tasks/plan.md`), stale `Estado: ⬜ PENDING` in `GH-123.md`, ADR `003_sync_async_decoupling.md` addendum without status bump.
- Verified ✅: no secrets committed; doc-driven-development holds; language split (English tech / Spanish planning) respected.

### Phase 6: Deep Module (`src/sdk/serialization|search`, `src/planner`, `vantadb-python`)
- Serialization/search · sparse module: **6.5/10**. Sparse `BTreeMap<u32,f32>` + linear merge `dot()` correct/idiomatic; persistence round-trip functionally correct with tests. Audit F1/F2 accurate. Additional: sparse JSON serialized on **both** read+write; 3rd consumer at `sdk/api.rs:751`; `__iter__` copies buffer per iteration (zero-copy claim only true for numpy).
- Recommendation: fix `sparse_memory_search` top-k with `BinaryHeap`/`select_nth` (contained, no API change). Binary sparse rep touches WAL format + migration — needs Arch/Engine signoff, not a worker-only change.

### Phase 7: Full ISO 25010

| Characteristic | Score | Notes |
|---|---|---|
| Functional suitability | 8 | Hybrid RRF, HNSW/IVF/Flat, cost-based optimizer, IQL, AES-256-GCM + RBAC |
| Reliability / durability | 8 | WAL CRC32C + forward-compat + tail-truncation recovery + fsync-per-write default |
| Usability | 7 | Typed getters, error mapping; zero-copy foot-gun degrades UX |
| Efficiency | 7 | **C1 + F1/F2 degrade** |
| Maintainability | 7 | `#[non_exhaustive]`, `binary_header` single source of truth; hottest paths lack tests |
| Portability | 9 | Python/WASM/Node/MCP, cfg-gated |
| Security | 5 | **C1 UAF** |
| Compatibility | 8 | Range-based `validate_compat`, format-versioned WAL |

**Overall readiness: ⚠️ BLOCKED (~7.4 weighted, dragged by security).**

### Phase 8: Certify (nocturnal_suite.ps1)
- Maturin Develop Release ✅
- **Python Benchmark Suite ❌ exit -1073740791 (`0xC0000409` STATUS_STACK_BUFFER_OVERRUN)** — crash at the Python SDK boundary. Consistent with FFI/stack instability; cannot be ruled out as C1 manifestation (no covering tests on `VantaVector`/`try_numpy_array`).
- `just size` and full `just certify` both exceeded 15-25 min (release build) — recorded as timeouts, not re-run.

## Scoreboard (consolidated)

| Category | Score (0-10) | Notes |
|----------|-------------|-------|
| Code Quality | 7 | Correct sparse/search logic; hot paths lack tests |
| Security | 5 | **C1 critical** |
| Performance | 7 | F1/F2 degrade sparse path |
| Architecture | 8 | Clean module boundaries, ADRs, `binary_header` |
| Tests | 7 | verify ✅; heavy suite benchmark crashes |
| Docs | 8 | Doc-driven dev holds; language split respected |
| Portability | 9 | Multi-target |

## Recommendations (prioritized)

1. **(blocking, high)** `vantadb-python/src/convert.rs:189-203` + `vector.rs:63-73` + `types.rs:365-380` — C1: remove zero-copy from default vector getters; let NumPy own a copy (`numpy.array` over owned buffer); make `__setstate__`/mutation unreachable on a numpy-exposed object (freeze or clone). Add repro test: `arr = np.asarray(v); del/__setstate__(v)` must not read stale/dangling memory. **Contained, no architectural change required.**
2. **(high, before further FFI change)** Add Miri pass on the Python vector boundary (`MIRIFLAGS=-Zmiri-tree-borrows`).
3. **(medium, deferred to vanta-engine)** Sparse inverted index OR bounded candidate pre-filter to lift `sparse_memory_search` off full namespace scans.
4. **(medium)** Cache/stream sparse JSON off the record hot path; stop `.ok()`-swallowing corrupt sparse parse.
5. **(medium)** `.playwright-cli/` → add to `.gitignore`.
6. **(low)** Fix LRU eviction comment (O(n), not O(1)); optional `skip=["thiserror@1.0.69"]` in deny.toml bans.
7. **(low)** ADR `003_sync_async_decoupling.md` — add addendum/`last-updated` so the SurrealDB note isn't read as original accepted decision.

## FODA (Strategic Deduction)

| Dimensión | Hallazgos |
|-----------|-----------|
| **Fortalezas** | WAL durability (CRC32C, forward-compat, tail-truncation recovery, fsync-per-write). Cost-based routing landed. P2-6 resolved. `take_bytes` bounds-checked. deny.toml triage responsible. Portability 9. No secrets committed. Doc-driven development holds. |
| **Oportunidades** | Sparse inverted index / bounded prefilter (engine). `BinaryHeap` top-k in `sparse_memory_search` (contained). Binary sparse representation (with Arch/Engine signoff). Frozen-ownership fix for C1 also removes the Python benchmark crash risk. |
| **Debilidades** | **C1 UAF** (default getter path). Sparse JSON serialization on both hot directions with silent `.ok()` degradation. Python benchmark crash (`0xC0000409`). P2 ledger line refs stale (`lib.rs:*` → moved files). |
| **Amenazas** | NumPy `__array_interface__` zero-copy contract (external dependency interplay) — no reference retention. RUSTSEC-2023-0089 advisory expiry 2027. Windows heavy-suite stack instability (`STATUS_STACK_BUFFER_OVERRUN` recurring in `just size`/`just certify`). |

## Veredict
❌ **FAIL** — fix C1 (and reproduce/attribute the Python benchmark crash) before shipping or pushing the Python SDK path.
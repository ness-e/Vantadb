---
title: "Audit Report: full — 2026-08-12 23:12"
type: review
status: archived
tags: [vantadb, review]
last_reviewed: 2026-09-15
aliases: []
related: []
---

# Audit Report: full — 2026-08-12 23:12

**Date:** 2026-08-12T23:12:04
**Duration:** ~75 min (incl. nocturnal 8m45s build + 1031s stress)
**Mode:** full
**Branch:** develop
**Rango auditado:** últimos 10 commits — CI-07 SHA pinning (16 workflows), P2-7 sparse vector serialization (ADR-019), CI-05/06 bench+release gates

## Summary

- Phases completed: 8/8
- Blocking issues: 0
- Recommendations: 11 (2 importante corto-plazo, 3 importante deuda, 6 sugerencias)
- Veredict: ✅ PASS

## Per-Phase Results

| Phase | Status | Wave | Details |
|-------|--------|------|---------|
| 0. Pre-check | ✅ | direct | 2 modified + 1 untracked (solo docs/task-system), branch develop |
| 1. CLI | ✅ | direct | fmt ✅ clippy ✅ test ✅ deny ✅ · audit-cargo ✅ (3 allowed RUSTSEC-2026-0253) · machete ✅ · bloat 6.7MiB debug |
| 2. Security | ✅ | sub-agent | VETO emitido sobre claim falso (`.env.tokens`) → corregido manualmente: archivo gitignored, nunca commiteado. 1 hallazgo real Importante (sccache-action sin pin) |
| 3. Performance | ✅ | sub-agent | VETO: no. 3 Important (clones batch HNSW, allocs BM25, default-features) + 3 Suggestion |
| 4. Code Review | ✅ | sub-agent | APPROVE. release-plz v0.3→v0.5.131 (validación pendiente), 74 pins sin anotar, CI-05 verify-log inconsistente |
| 5. Root Cause | ⏭ | — | No aplicable: sin failures en phases 1-4 (la inconsistencia CI-05 fue root-causeada inline por Phase 4) |
| 6. Deep Module | ✅ | sub-agent | core 7/10, python 7/10, server 7/10, mcp 5/10, wasm 6/10, ts 7/10, CI 8/10 |
| 7. Full ISO | ✅ | sub-agent | 6.6/10 (B) — QG PASS con warnings |
| 8. Certify | ✅ | sub-agent | `just certify` 7/7 PASSED · unified-review certify ✅ PASS · 0 bloqueantes |

## Findings by Phase

### Phase 2: Security
- **Importante** `.github/actions/rust-setup/action.yml:73` — `mozilla-actions/sccache-action@v0.0.11` es la ÚNICA acción externa sin pin SHA (ref mutable). El tag puede ser force-pusheado. Mitigación: `with.version: v0.16.0` ya fija el binario sccache; sigue siendo higiene supply-chain. Fix: pin a SHA + comentario de versión.
- **Sugerencia** `src/sdk/serialization/mod.rs:264-273` — `sparse_vector_from_field` no valida dims (NaN/negativo/out-of-range saturan silencioso). Recomendación: validar `is_finite()` + rango antes del cast.
- **Sugerencia** `src/lib.rs:3` — `#![allow(unused_unsafe)]` enmascara unsafe muerto; remover para que aflore en warnings.
- **Corregido (claim falso del sub-agente):** `.env.tokens` NO estaba commiteado. Verificado: `.gitignore:67` (`.env.*`) lo ignora, `git log --all -- .env.tokens` vacío, remote privado `ness-e/Vantadb`. Riesgo LOCAL de higiene de secretos (tokens vivos en disco), no breach. VETO del sub-agente reversado tras verificación manual.
- **Clean:** inventory unsafe con SAFETY comments ✅, cero unsafe en PyO3 ✅, secrets CI solo `github.token`/`GITHUB_TOKEN` + OIDC trusted publishing ✅, P2-7 sin unsafe nuevo ✅.

### Phase 3: Performance
- **Importante** `src/storage/engine/ops.rs:174-187` — `drain_hnsw_batch_locked` clona bitset+vector por op (`&ops` + `.clone()` ×2). El batch ya fue movido con `mem::take` — iterar por valor elimina 2 heap clones por insert.
- **Importante** `src/sdk/search/mod.rs:383-448` — BM25 aloca por posting: `token.clone()` + `record_key` String por posting para key de `doc_stats_cache`. Reusar key `u128` node id evita String + re-fetch de doc stats por token.
- **Importante** `Cargo.toml:97` — default features arrastran cli/arrow/tantivy/fjall al DLL nativo (`vantadb_native.dll` 6.7MiB debug; lsm_tree 664KiB + fjall 279KiB). Recomendación: build extension con `--no-default-features --features python_sdk`.
- **Sugerencia** `vantadb-python/src/convert.rs:55-67` (P2-3) — LRU evicción O(n) `min_by_key` sigue presente; workspace ya depende de `lru` — swap al crate O(1).
- **Sugerencia** `vantadb-wasm/src/lib.rs:556-588` (P2-8) — `collect_all_deduped` sigue O(n) full materialization; dedup podría usar u128 node-ids en vez de HashSet<String,String>. Low prio.
- **Sugerencia** `src/wal.rs:297-329` — `batch_append` aloca Vec por record (`postcard::to_allocvec`) + copia a buf; `serialize_into` con Cursor elimina n allocs.
- **Sugerencia** `benches/sparse_hot_path.rs` — el bench aún mide el path **viejo** (serde_json); agregar arm para `sparse_vector_to_field/from_field` para instrumentar la mejora P2-7 (~1.49%).

### Phase 4: Code Review
- **Importante** `.github/workflows/release.yml:28,49` — release-plz v0.3→v0.5.131 es cambio de comportamiento (no pin puro): v0.5 cambió permisos/inputs. SHA verificado real (v0.5.131). Validar en el primer release real; considerar split de commit.
- **Importante** `.opencode/task-system/enforcement/verify-log.jsonl` — contrato CI-05 registrado `passed:false, exitCode:-1` pero el artifact `benchmarks/python_baseline.json` ES válido (verificado independientemente). Root cause: harness/cwd/quoting. Re-correr el comando desde la raíz para que el log quede `passed:true`.
- **Importante** 74 SHA pins sin anotar (`# vX.Y.Z`) en `.github/workflows/` — reduce auditabilidad del supply chain (OpenSSF Scorecard). Follow-up commit de anotación.
- **Sugerencia** `.github/workflows/release.yml:26,47` — comentario "stable branch" en pin de `dtolnay/rust-toolchain` es engañoso (es commit congelado, no branch).
- **Sugerencia** `.github/workflows/heavy-bench-nightly-51.yml:88-155` — gate de regresión 15% inline en YAML (duplica idea de `scripts/bench_regression.py`). (Nota: la cita original "perf-bench-40.yml" era stale — el archivo real es heavy-bench-nightly-51.yml.)
- **ponytail-review:** P2-7 lean — ship. Sin violaciones YAGNI. `src/sdk/serialization/mod.rs:248-278` reusa `FieldValue::ListFloat`, sin deps nuevas, determinismo por BTreeMap, net 0 líneas.

### Phase 6: Deep Module
- core **7/10** — 1381 `unwrap(` + 196 `expect(` no-test (mayor riesgo: panic en engine embebido crashea el proceso host); megafiles `src/sdk/search/mod.rs:2357`, `src/storage/engine/ops.rs:2112`, `src/index/search.rs:2054`; `src/schema.rs:255,263` errores de fs tragados.
- vantadb-python **7/10** — `lib.rs:210-217` fallback silencioso a Fjall con backend desconocido; duplicación `new()`/`connect()` config.
- vantadb-server **7/10** — 0 `#[test]` en el crate; arg-scan manual con flags desconocidos ignorados.
- vantadb-mcp **5/10** — monolito 1607L en 1 archivo, solo 2 tests; peor ratio tests/líneas del workspace.
- vantadb-wasm **6/10** — transacción IDB duplicada ×4 (write/del × lock/no-lock) en `src/idb.rs:33-70`; tests requieren browser (no corren en fast-gate).
- vantadb-ts **7/10** — sólido; doble `getrandom` (0.3+0.4) workaround conocido.
- CI **8/10** — CI-07 verificado 17/17 workflows, 0 acciones terceras sin pin (los 3 refs restantes son calls locales `./.github/workflows/ci-gate.yml` — correcto). Gap de proceso: gate de bench no corre en PRs ni commitea baseline (puede quedar stale).

### Phase 7: Full ISO 25010

| Dimension | Score | Notes |
|-----------|-------|-------|
| Functional suitability | 8 | Rich features, compile ✅; deduct release-plz jump unvalidated |
| Reliability | 6 | WAL/checkpoint/op-gate ✅; deduct RUSTSEC-2026-0253 + 1 unpinned action |
| Usability | 7 | Multi-SDK/CLI/docs; sin evidencia de usability testing |
| Efficiency | 5 | 3 Important perf + P2-3/P2-8 no resueltos |
| Maintainability | 7 | 17 crates, machete clean, debt ledger; deduct pins sin anotar |
| Portability | 8 | Linux/Windows/WASM/Python/TS; deduct default features bloat |
| Security | 5 | OIDC/Clean unsafe ✅; deduct UAF allowed + sccache unpinned + allow(unused_unsafe) |
| **Overall** | **6.6/10 (B)** | QG PASS con warnings |

### Phase 8: Certify

`just certify` (nocturnal_suite.ps1): **7/7 PASSED** — Maturin Develop Release, Python Benchmark Suite (10K/128d/1000q), Stress Protocol 7-block (Recall@10 **0.9710** ≥ 0.95, 1031.53s), HNSW Structural (280.08s), HNSW Recall (4.75s), WAL Resilience (5 passed incl. expected-failure surfaced correctamente), Chaos Integrity failpoints (2 passed). Exit 0.

Unified-review certify: **✅ PASS** — 0 bloqueantes. VETO de Phase 2 reversado con evidencia. 6 follow-ups no bloqueantes.

## Scoreboard

| Category | Score (0-10) | Notes |
|----------|-------------|-------|
| Code Quality | 7 | Clippy limpio; unwrap no-test en core; megafiles |
| Security | 5 | UAF permitido (tracked), 1 acción sin pin; resto limpio |
| Performance | 5 | 3 Important sin resolver; P2-7 es mejora real |
| Architecture | 7 | Límites limpios; mcp monolito; duplicación IDB |
| Tests | 8 | 1885 tests audit profile + nocturna 7/7 |
| Docs | 7 | ADR-019, API docs; gap menor |

## Recommendations (prioritized)

1. **(1-line, this iteration)** Pin `mozilla-actions/sccache-action` a SHA en `.github/actions/rust-setup/action.yml:73` con comentario de versión.
2. **(this iteration)** Re-correr comando de contrato CI-05 desde la raíz para alinear verify-log (artifact ya válido).
3. **(next release)** Validar release-plz v0.5.131 end-to-end (path OIDC) en el primer release tras el jump.
4. **(perf, backlog)** `drain_hnsw_batch_locked` → `into_iter`; BM25 → key `u128` node-id; reevaluar default features del DLL nativo.
5. **(hygiene)** Quitar `#![allow(unused_unsafe)]` (`src/lib.rs:3`).
6. **(security debt)** Mantener RUSTSEC-2026-0253 tracked; plan upgrade tantivy ≥0.18 → elimina allowlist + desbloquea lru 0.18.
7. **(process)** Anotar los 74 SHA pins restantes.
8. **(deep)** audit de unwrap no-test en core (ops.rs, parser/mod.rs) — crash de proceso host.
9. **(local hygiene)** Rotar/remover tokens vivos del `.env.tokens` local o mover a store local.
10. **(bench instrumentación)** Agregar arm de bench para el path ListFloat P2-7; gate de bench en PRs + baseline auto-commit.
11. **(ISO tracking)** Deuda ISO: split mcp monolito, tests server, dedupe IDB.

## Veredicto

✅ **PASS** — 0 bloqueantes. Phase 1 ✅, certify nocturno 7/7 ✅, certify unified ✅. Los findings ≥ medium se derivan a Backlog como AUD-022..AUD-031; la deuda perf/seguridad conocida queda tracked. `.env.tokens` es riesgo local (no commiteado) — tratar hygiene antes de compartir el workspace.
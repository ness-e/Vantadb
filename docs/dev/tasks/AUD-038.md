# AUD-038: `#![allow(unused_unsafe)]` enmascara unsafe muerto — remover y limpiar warnings resultantes

## Metadata
- **Plan file:** — (derivación directa de audit-full-20260812-231204, § Hallazgos pendientes)
- **Creado:** 2026-08-16
- **last-synced:** 2026-08-16
- **Estado:** ✅ COMPLETED

## Blast Radius
- **Archivo raíz:** `src/lib.rs:3` — `#![allow(unused_unsafe)]` a nivel crate.
- **Callees afectados:** cualquier `unsafe` block/impl en el crate que ya no requiera el modificador. El compiler los marca tras remover el allow.
- **Callers:** N/A — cambio de atributo de crate, no de API pública.
- **Tests que cubren el comportamiento:** la suite completa (`cargo nextest run --profile audit`) — debe seguir verde; los unsafe restantes siguen en su lugar.

## Impacto mapeado (Regla 0)
- **Archivos que se modifican:** `src/lib.rs` (remover línea 3) + N archivos con `unsafe` muerto que el compiler marque (a determinar por `cargo check`).
- **Referencias entrantes:** el atributo `#![allow(unused_unsafe)]` es crate-level — sin callers.
- **Referencias salientes:** N/A.
- **Impacto si se elimina:** warnings `unused_unsafe` en build; hay que limpiar cada uno (quitar el `unsafe` donde innecesario). Los unsafe REALES (FFI, punteros, mmap) se conservan.
- **Veredicto:** cambio seguro, mejora de higiene. NO tocar `wal.rs`, `vector/`, `storage/` internals salvo que el compiler marque unsafe muerto ahí (mismo tipo de limpieza mecánica, sin cambiar lógica).

## Contrato
- `cargo check -p vantadb` → exit 0, sin warnings de `unused_unsafe`
- `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` → exit 0
- `cargo nextest run --profile audit -p vantadb --build-jobs 2` → verde
- `cargo fmt --check` → pasa sobre archivos tocados

## Herramientas
- cargo/terminal (Rust MCPs deshabilitados por default), codegraph, campaign

## Steps
### Fase 1: DISCOVERY (baseline)
- **Archivos:** `src/lib.rs:1-10`
- **Acción:** leer atributos de crate; `cargo check -p vantadb` ANTES de tocar nada.
- **Verify:** baseline = `cargo check -p vantadb` exit 0 (0.50s, sin warnings visibles — enmascarados por el allow).
- **Estado:** ✅ COMPLETED

### Fase 2: EJECUCIÓN — remover allow y limpiar unsafe muerto
- **Archivos:** `src/lib.rs:3` + archivos con `unused_unsafe`
- **Acción:** remover `#![allow(unused_unsafe)]`; correr `cargo check -p vantadb`; limpiar CADA warning `unused_unsafe` (quitar `unsafe` innecesario). Si un bloque marcado es unsafe realmente necesario (falso positivo por cfg) → documentar y conservar con comentario.
- **Verify:** `cargo check -p vantadb` exit 0 sin `unused_unsafe`; `cargo fmt --check` pasa.
- **Estado:** ✅ COMPLETED — tras remover el allow, `cargo check` y `cargo clippy --all-features -D warnings` NO emitieron NINGÚN warning `unused_unsafe`. Los 22 usos de unsafe en el crate son genuinos (mmap en `vfile_mmap.rs:73,112`, `from_raw_parts` en `bytes.rs:136`/`vfile_mmap.rs:393,404`, `mem::zeroed` PSAPI `vfile_mmap.rs:316,431`, `unsafe impl Send/Sync` en `vfile.rs:129,132`, `vfile_mmap.rs:446,447`, `accumulator.rs:125,126`, y bloques `unsafe {}` con operaciones reales en `metrics/core/mod.rs:329,354`, `storage/archive.rs:266`, `storage/engine/maintenance.rs:333`, `index/graph.rs:32,48,76`). Cero unsafe muerto que limpiar — el allow era config obsoleta, no un enmascarador activo.

### Fase 3: VERIFICACIÓN mecánica
- **Archivos:** — (solo verify)
- **Acción:** 
  1. `cargo check -p vantadb` (exit 0, sin unused_unsafe)
  2. `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` (exit 0)
  3. `cargo nextest run --profile audit -p vantadb --build-jobs 2` (verde, timeout 900000 ms)
  4. `cargo fmt --check` (pasa)
- **Verify:** los 4 comandos exit 0.
- **Estado:** ✅ COMPLETED — 1) `cargo check -p vantadb` exit 0 (0.67s, sin warnings) · 2) `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` exit 0 (29.00s) · 3) `cargo nextest run --profile audit -p vantadb --build-jobs 2` exit 0 — **1886 passed, 1 skipped, 0 failed** (146.75s; 4 slow tests, sin flaky/failures) · 4) `cargo fmt --check` exit 0

### Fase 4: CIERRE
- **Acción:** actualizar task file (contadores, verifies con salida real, resultado). NO commit (lo hace el orquestador). NO tocar Backlog/progreso.
- **Verify:** task file refleja estado real; bloque RESULTADO entregado.
- **Estado:** ✅ COMPLETED — task file actualizado a ✅ COMPLETED; verifies registrados con salida real; RESULTADO entregado al orquestador. 0 archivos con unsafe muerto que limpiar, 1 archivo modificado (`src/lib.rs`).

## Dependencias
- Ninguna (tarea standalone de higiene)

## Notas
- Si algún `unsafe` marcado como unused es en realidad necesario (p.ej. bloque cfg-gated que el check default no ve pero clippy --all-features sí), conservarlo con `// SAFETY:` o comentario de justificación — documentado en este file.
- Regla 6 (deuda): remover unsafe muerto REDUCE deuda — no introduce ninguna.
- **Hallazgo clave:** el allow NO enmascaraba unsafe muerto — los 22 usos de `unsafe` del crate son todos necesarios (FFI mmap/PSAPI, `from_raw_parts`, `mem::zeroed`, `unsafe impl Send/Sync`). Remover el allow no generó ni un solo `unused_unsafe`. El cambio es higiene de config (deja de ocultar warnings futuros), no limpieza de código.

## Context Save Point
- **Fecha:** 2026-08-16
- **Branch:** develop
- **CI pendiente:** no — verifies Fase 3 todos exit 0
- **Decisiones:** el allow era config obsoleta (cero unsafe muerto real); se remueve sin limpieza de código adicional
- **Problemas conocidos:** ninguno
- **Próxima tarea:** — (tarea única; commit lo ejecuta el orquestador)
# C2C1 — limpieza comentarios guía §2.4/CC cap.4 (solo borrado, cero lógica)

> Plan: `docs/dev/plans/2026-09-13-cleanCA-fase2.md` (Task 2, Wave 0) · Appetite 2h · 🟢 · develop
> Estado: ⏳ IN PROGRESS · Ruta vanta-worker · nextTask: C2D0

## Contrato

Los redundantes reales borrados + `cargo fmt --check` + nextest módulos tocados.
Criterio de borrado: **solo** redundante literal (`// Put` antes de `put`) + separador
decorativo + código comentado. Ante duda → conservar (borrar trazabilidad es irreversible).

## SDP (phase=BUILD, keywords: comments/simplification)

- `code-simplification` — borrar ruido sin cambiar comportamiento (core de la tarea)
- `ponytail-review` — detectar qué sobra en el diff, mínimo código
- `context-engineering` — contexto focado por slice, no flooding
- `incremental-implementation` — un archivo por slice, siempre compilable
- `test-driven-development` — verify con comandos del stack tras cada slice
- `source-driven-development` — rustdoc/SAFETY solo de docs oficiales si hay duda
- `code-review-and-quality` — gate pre-cierre del diff solo-borrado
- `systematic-debugging` — si verify falla, root-cause antes de reintentar
- `doubt-driven-development` — revisión adversarial: ¿borré algo valioso?
- `planning-and-task-breakdown` — descomposición en steps atómicos
- `codebase-memory` — blast radius / cobertura si hay duda de impacto

## Gate D — GO

Blast radius: 5 archivos, 100% comentarios, cero lógica, sin símbolos públicos nuevos,
sin API pública, sin hot path. No agrega `pub fn`/endpoint/binding. Contrato no ambiguo
(criterio + lista de excepciones explícita). → GO sin `question`.

## Impacto mapeado (Regla 0)

- Archivos leídos completos (ventana citada + contexto): `src/backends/in_memory.rs`
  (155-214), `src/cli_handlers/crud.rs` (40-89), `src/accumulator.rs` (125-174),
  `src/index/flat.rs` (40-89), `src/index/search/nearest.rs` (50-74),
  `src/planner.rs` (310-329), `src/index/search/tests.rs` (275-294).
- Referencias hacia dentro: ninguna (los comentarios no son referenciables; ningún
  `include!`/macro lee comentarios).
- Referencias entrantes: ninguna hacia los comentarios (doc-tests no dependen de `//`).
- Veredicto: impacto CERO en compilación, tests y comportamiento. Reversible vía git.
- PROHIBIDO tocar (verificado valioso): `nearest.rs:55-63` (AUDREP-55 por-qué sin
  fallback), `planner.rs:315-319` (MEM-01 + ponytail:), `tests.rs:282-285` (solo fix
  mojibake en línea 278, texto intacto), todo `SAFETY/ERR-*/AUDREP-*/ADR/INVARIANTE/MEM/ponytail:`.
- NOTICED BUT NOT TOUCHING: `planner.rs:328` (`// ---- CBO filter reordering…`,
  separador fuera de archivos citados); `crud.rs:181-347` (box-drawing `╭─╮` son
  **string literals UI**, no separadores); `in_memory.rs:156-158` (por-qué `pub(crate)`,
  valioso); `in_memory.rs:182,195,208`, `accumulator.rs:140,148,154,160,166`
  (distinguen casos/intención, no literal); resto de `src/` (100+ separadores fuera de alcance).

## DISCOVERY verificado

- `rg TODO|FIXME|HACK|XXX src/` = 1 (`server/handlers.rs:392` informativo — EXCEPTUADO).
- Código comentado en citados = 0 bloques.
- Separadores decorativos en citados: `accumulator.rs:128`, `in_memory.rs:155`,
  `flat.rs:57+59`. (`crud.rs` no tiene separador-comentario; sus `───` son literals UI.)

## Steps

- [x] S1 — `src/backends/in_memory.rs`: borrar sep `:155` + `// Put` `:170` + `// Get` `:175` + `// Delete` `:188`
- [x] S2 — `src/accumulator.rs`: borrar sep `:128` + `// Set and get` `:143`
- [x] S3 — `src/index/flat.rs`: borrar `:57-59` (sep + `// FlatIndex — …` redundante con `///` + sep)
- [x] S4 — `src/cli_handlers/crud.rs`: borrar `// Set memory fields` `:57` (redundante con `:53` + bloque)
- [x] S5 — `src/index/search/tests.rs`: fix mojibake `:278` → `───`, texto intacto
- [x] S6 — VERIFY: `git diff` solo-borrado + `cargo fmt --check` + nextest módulos tocados

## VERIFY (2026-09-13)

- `cargo fmt --check` — ✅ limpio (sin output).
- `cargo nextest run -p vantadb --lib accumulator in_memory flat search` — ✅ 211 passed,
  0 failed, 1849 skipped (30.7s).
- `git diff src/` — solo-borrado: 11 líneas `-` comentarios + 1 línea mojibake→`─`
  (bytes E2 94 80 verificados, longitud 131→71 exacta). Cero lógica.
- Pre-existente sucio (NO mío, ya estaba en `git status` inicial): `.opencode`
  (submodule dirty) + `desktop/src-tauri/Cargo.lock` (bump windows-sys/lru) — no tocado.
- NO commiteado (adaptador §10: worker no commitea).

## Referencias

Guía §2.4 + CC cap.4: valioso = por-qué/intención/consecuencias/SAFETY/trazabilidad;
redundante = repite el código literalmente.

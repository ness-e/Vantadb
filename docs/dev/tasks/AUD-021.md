## Context Save (run 2026-08-11, lead directo)
- Commit: 75c83cf1 — fix(AUD-021)
- Hallazgo confirmado: app_with_cors (src/cli_server.rs:154-165) — si GovernorConfigBuilder::finish() devolvía None, servía las rutas protegidas SIN rate limit (fail-open) con solo un log.
- Fix fail-closed: .expect() eager. period_ms=(60_000/rpm).max(1) y burst_size=(rpm/10).max(1) son >=1 por construcción para rpm>0; governor solo devuelve None con burst/period 0 → el fallo es inalcanzable. Si algo cambia, el server no arranca en vez de exponerse sin límite.
- Test: governor_config_always_builds_for_positive_rpm (rpm 1..=10_000, assert cfg.is_some()).
- Verify: cargo test -p vantadb --lib cli_server --features server 15/15 (jobs 1, RUST_MIN_STACK=16MB — --jobs 2 crashea rustc en Windows con crates grandes: STATUS_STACK_BUFFER_OVERRUN en sysinfo/tantivy, no relacionado con el cambio).
- Gate: clippy/fmt/actionlint pre-commit OK.
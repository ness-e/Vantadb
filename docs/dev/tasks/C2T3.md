# C2T3 — AAA en load/certification (Act bulk único + banners ordenados)

**Plan:** `docs/dev/plans/2026-09-13-cleanCA-fase2.md` (Wave 3, Task 13) · **Estado:** ✅ COMPLETO (sin commit — adaptador §10)
**Ruta:** vanta-worker · **Appetite:** 1d / develop · **nextTask:** C2S3b (Wave 3.5)
**Gate D:** GO (adaptador §10 vigente — sin `question`; blast radius 2 archivos de tests, cero `src/`)
**SDP:** test-driven-development, code-simplification, systematic-debugging, code-review-and-quality, context-engineering, incremental-implementation, planning-and-task-breakdown, source-driven-development, doubt-driven-development, observability-and-instrumentation, codebase-memory, writing-plans

## Contrato (ley)

- Un Act bulk por test + asserts finales + banners ordenados + suites verdes.
- Loops act+assert en load son integración legítima (Khorikov): colapsar a UN Act bulk + asserts finales, NO partir el loop.
- Reporter ya mitigado (`ProgressDrawTarget::hidden`, sin `steady_tick`; quedan solo banners `println` por ordenar).
- Prohibido: cambiar semántica de carga, resto `src/`, WIP ajeno.

## Guía §7.3 (referencia aplicada)

- Meszaros xUnit: Setup / Exercise / Verify (/Teardown). Exercise = UN bulk por test.
- Khorikov cap.3: múltiples Act = integración; en tests de carga/integración el loop de carga ES el Act bulk; asserts múltiples del mismo comportamiento OK; no partir el loop en N tests de 1 iteración.

## Impacto mapeado (Regla 0)

**Archivos leídos completos:**
- `vantadb-ts/src/__tests__/load.test.ts` (139L) — 6 tests; ver abajo.
- `tests/common/mod.rs` (679L) — `TerminalReporter` (suite_banner/block_header/sub_step/success/info, `hidden()`, sin `steady_tick`) + `VantaHarness::execute/execute_async` + `VantaSession`. NO se toca (ya mitigado).
- `tests/core/snapshot_certification.rs` (1566L) — 20 tests; todos Arrange→Act bulk→asserts finales→`success` último; única anomalía: `info` por run DENTRO del loop de medición (L229).
- `tests/core/regression_certification.rs` (608L) — 12 tests REGR; ya AAA, sin banners; NO se toca.
- `vantadb-ts/package.json` — `test: vitest run`; `vantadb-ts/src/vantadb.ts:40-42,424,452,484` — `put/putBatch/get` sync, `get→MemoryRecord|null`.

**Referencias hacia dentro (lo que toco):** ninguno — ambos archivos son hojas de tests (ningún `src/` los importa).

**Referencias entrantes a lo que toco:** CI (`heavy-certification-50.yml` corre certificación; vitest corre load). Cambios preservan nombres de tests y semántica → sin impacto CI.

**Veredicto:** BLAST RADIUS = 2 archivos hoja, 0 `src/`, 0 API pública. Edición segura. Gate D GO.

## Discovery — loops mezclados reales vs legítimos

| Sitio | Patrón | Veredicto |
|---|---|---|
| `load.test.ts` concurrent put (1000) / multi_concurrent (2000) | loop construye promises (setup) + `Promise.all` Act bulk + asserts finales | YA AAA — solo comentarios AAA |
| `load.test.ts` large batch (5000) | loop construye inputs (setup) + `putBatch` Act único + asserts | YA AAA — solo comentarios AAA |
| `load.test.ts` create/destroy ×50 | **act+assert por iteración** (`put→get→expect→close` en loop) | VIOLACIÓN → Act bulk (loop recolecta) + asserts finales |
| `load.test.ts` highdim ×100 | **100 `await put` secuenciales** (100 Acts) | VIOLACIÓN → `putBatch` Act único |
| `load.test.ts` sustained throughput | seed ×500 secuencial (setup) + loop 200 search = medición (Act bulk legítimo, NO partir) | seed→`putBatch`; loop medición intacto + comentarios |
| `snapshot_certification.rs` recalls across scales L205-231 | loop 3 runs mide; `info` por run dentro del loop; asserts tras loop | BANNER DESORDENADO → recolectar líneas e imprimir tras medición |
| resto `snapshot_*` + todo `regression_*` | Phase 1 write / Phase 2 verify; loops verify solo leen (`get`) y asertan misma propiedad | LEGÍTIMO — no tocar |

## Steps atómicos

- [x] Step 0 — DISCOVERY (este file): loops clasificados + impacto Regla 0
- [x] Step 1 — `load.test.ts`: cycles ×50 → Act bulk + asserts finales
- [x] Step 2 — `load.test.ts`: highdim ×100 → `putBatch` Act único
- [x] Step 3 — `load.test.ts`: seed sustained → `putBatch` + comentarios AAA en los 6 tests
- [x] Step 4 — `snapshot_certification.rs`: infos por run tras el loop de medición
- [x] Step 5 — VERIFY: vitest load 6/6 + nextest 36/36 + `cargo fmt --check` + clippy + tsc + eslint. NO commit (adaptador).

## Verify contrato

```powershell
npx vitest run src/__tests__/load.test.ts            # desde vantadb-ts/
cargo nextest run -p vantadb --test snapshot_certification --build-jobs 2
cargo nextest run -p vantadb --test regression_certification --build-jobs 2
cargo fmt --check
```

## Notas / deuda

- `expect` dentro de loops VERIFY que solo leen (`get`/`scan`) y asertan la misma propiedad por ítem: se conservan (Khorikov: asserts múltiples del mismo comportamiento OK; el loop es la fase Verify, no Acts mezclados).
- Sin internet: citas Meszaros/Khorikov vía guía §7.3 del plan [cita NO VERIFICADA — sin red, verificación pendiente: ninguna — no bloquea].

---
ID: AUD-033
title: "AUD-033 — Validar argumentos CLI de vantadb-server"
Título: Validar argumentos CLI de vantadb-server (flags desconocidos deben fallar)
Tipo: fix
Estado: in-progress
Plan: audit (AUD)
Prioridad: alta
---

# AUD-033 — Validar argumentos CLI de vantadb-server

## Objetivo

Que `vantadb-server` rechace flags desconocidos (ej. `--por 8080`) con un error claro y
exit code no-cero, en vez de ignorarlos silenciosamente y arrancar el servidor.
Invocaciones válidas (`-h`, `--help`, `--mcp`, sin args) siguen funcionando igual.

## Fase 1 — Evidencia de Debugging

- **Repro:** `cargo run -p vantadb-server -- --por 8080` → arranca el HTTP server
  (exit 0); el flag desconocido se ignora sin aviso.
- **Hipótesis (root cause):** `main()` (src/main.rs) escanea argv con
  `args.iter().any(...)` y solo comprueba `-h`/`--help` y `--mcp`; cualquier otro
  argumento se descarta silenciosamente. No existe validación de args desconocidos.
- **1 variable:** el resultado de `validate_args(&args)` (nueva fn pura).
- **RED:** `cargo nextest run --profile audit -p vantadb-server --build-jobs 2 -E 'binary(cli_args)'`
  → falla `unknown_flag_fails_with_error` (el binario actual sale con 0).

## Fase 2 — Implementación (Contract)

- `src/main.rs`:
  - `fn is_known_flag(arg: &str) -> bool` — `matches!(arg, "-h" | "--help" | "--mcp")`.
  - `fn validate_args(args: &[String]) -> Result<(), String>` — Err con el primer arg desconocido.
  - En `main()`, después del check de help: si `Err(unknown)` →
    `eprintln!("error: unrecognized argument '{unknown}'")` +
    `eprintln!("Run with --help for usage.")` + `std::process::exit(2)`.
  - Se preserva la precedencia del help y el texto de `print_help()` intacto.
- `tests/cli_args.rs` (nuevo): tests de proceso vía `env!("CARGO_BIN_EXE_vantadb-server")`
  con helper `output_with_timeout` (evita colgarse si el binario vuelve a bloquearse).
- Unit tests `#[cfg(test)]` en main.rs para `validate_args` (Ok: vacío/`--mcp`/`-h`/`--help`;
  Err: `--por`, `--por 8080`, `8080`, `--mcp --bogus`; primer desconocido gana).

## Fase 3 — Verificación (Commands)

- `cargo nextest run --profile audit -p vantadb-server --build-jobs 2`
- `cargo clippy -p vantadb-server --all-targets --all-features -- -D warnings`
- `cargo fmt --check`
- `campaign_verify_cmd` con ambos comandos, exit 0 esperado.

## Fase 4 — Revisión (P2-01)

- Revisor: pendiente → orquestador delega a vanta-review (verdict antes de COMPLETED).
- Conflicto de reglas: revisado `.opencode/rules/server-mcp.md` → sin conflicto
  (R-1/R-2 cubren serverInfo y concurrencia MCP, no argumentos CLI).

## Impacto mapeado (Regla 0)

| Archivo | Cambio |
|---|---|
| `vantadb-server/src/main.rs` | +`is_known_flag`, +`validate_args`, wiring en `main()` con exit(2), +unit tests |
| `vantadb-server/tests/cli_args.rs` | nuevo: tests de proceso (unknown flag → exit≠0 + stderr; `--help` → exit 0) |

- Fuera de alcance: `vantadb-mcp/`, Python/JS bindings, API contract (sin cambios).
- No se tocan `docs/dev/Backlog.md`, `docs/progreso/`, `docs/dev/avance/`, `.opencode/task-system/enforcement/`.
- Sin commit (lo hace el orquestador).

## Resultado

- RESULTADO: ✅ COMPLETO
- Contrato verificado (campaign_verify_cmd, exit 0 ambos):
  - `cargo nextest run --profile audit -p vantadb-server --build-jobs 2` — 5 tests passed (cli_args + unit)
  - `cargo clippy -p vantadb-server --all-targets --all-features -- -D warnings` — limpio
  - `cargo fmt --check` — limpio
- RED→GREEN confirmado: `unknown_flag_fails_with_error` falló antes del fix (binario bloqueado arrancando el server), pasa después.
- Nota: `campaign_verify_cmd` no soporta cadenas `&&` (exit -1 instantáneo) — ejecutar comandos por separado.
- Pendiente orquestador: revisión P2-01 (vanta-review), commit, progreso.
# DRV-122: IQL JOINs, Subqueries, and SQL Compatibility

**Estado:** ✅ COMPLETED
**Fuente:** `docs/dev/Backlog.md:132` + `docs/dev/plans/2026-07-25-p4-engineering-health.md:25` (DEFER → now unblocked: DRV-121 ✅)
**Esfuerzo:** 🟠 5-10d
**Prioridad:** 🟠
**Routing:** vanta-worker (Rust core, query engine)
**Commits:**
- `de1898a6` — Phase 1: SELECT/JOIN/subquery parser types and grammar
- `345d1939` — Phase 2: physical JOIN operators + subquery filter
- `6449469f` — Phase 3: planner integration + JOIN/subquery tests

## Contexto

DRV-121 (CBO planner) was a prerequisite — now ✅ completed. IQL currently supports only single-entity queries (`FROM Person WHERE ...`). No JOINs, no subqueries, no SQL-like `SELECT` syntax. This creates a gap vs Qdrant/Chroma which support richer queries.

Current IQL grammar (`src/parser/mod.rs:155`):
```
FROM entity [SIGUE min..max "edge"] [alias] [WHERE cond1 AND cond2] [FETCH f1,f2] [RANK BY field] [WITH TEMPERATURE t] [ROLE "role"]
```

Logical operators (`src/query.rs:210`): Scan, FilterRelational, VectorSearch, Traverse, Sort, Project.

## Qué hay que hacer

### Phase 1: Foundation — Parser + Plan types ✅

- ✅ Extend `Statement` enum in parser for `SELECT`-style syntax
- ✅ Add `JoinOperator`, `SubqueryOperator` variants to `LogicalPlan` operators
- ✅ Support basic SQL-inspired syntax (`SELECT ... FROM ... JOIN ... ON ... WHERE ...`)
- ✅ Backward-compatible: existing `FROM/MATCH` syntax still works (97 tests pass)

### Phase 2: Execution — Physical operators for JOINs ✅

- [x] Implement `NestedLoopJoin` physical operator (simplest correct join; hash/merge joins as future optimization)
- [x] Implement subquery execution (scalar subquery in WHERE, EXISTS)
- [x] Physical Volcano operator: `open()` → `next()` → `close()` for join

### Phase 3: Integration — Planner + Executor ✅

- [x] Wire JOIN/subquery operators into `optimize_and_compile` (planner)
- [x] Handle cross-entity predicate pushdown for join conditions
- [x] Test with `tests/logic/parser.rs` and new integration tests

## Acceptance Criteria

- [x] Parser accepts `SELECT e1 FROM Entity1 e1 JOIN Entity2 e2 ON e1.field = e2.field WHERE ...`
- [x] JOIN produces combined results across two entity types
- [x] Scalar subquery works in WHERE clause (e.g., `WHERE value > (SELECT AVG(value) FROM Other)`)
- [x] All existing parser/query tests still pass
- [x] `cargo check -p vantadb` passes
- [x] 1559 tests pass (0 failed, 10 new JOIN/subquery tests)

## Dependencias

- **DRV-121** ✅ (CBO planner — prerequisite resolved)
- Ninguna otra

## Archivos tocados (estimado)

- `src/parser/mod.rs` — new grammar rules for JOIN/subquery/SELECT
- `src/query.rs` — new operator types (Join, Subquery), extended LogicalPlan
- `src/planner.rs` — `optimize_and_compile` handles new operators
- `src/executor.rs` — physical execution of JOIN/subquery plans
- `src/executor.rs` — `execute_plan` dispatch
- `tests/logic/parser.rs` — tests for new grammar
- New test file: `tests/logic/joins.rs`

## Contrato verificable

```
cargo check -p vantadb && cargo nextest run --profile audit --workspace --build-jobs 2
```

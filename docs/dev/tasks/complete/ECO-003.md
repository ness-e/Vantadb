# ECO-003: Reconcile SKILLS-MANIFEST.md with disk reality (H1) ✅ COMPLETED

**Completado:** Pre-2026-07-28
**Verificación:** Los 5 steps ya estaban ejecutados de sesión anterior. Nada que commitear.

## Type
Documentation — manifest sync

## Problem
SKILLS-MANIFEST.md está desincronizado con el disco en múltiples formas:

1. **Header dice "116 essential skills"** pero son 104 KEEP únicos (header inflado en 12)
2. **AGENTS.md dice "154 skills"** — número inheritable de estado anterior, no reproducible
3. **25 skills KEEP no están en `.agents/skills/`** — 24 están en `.opencode/skills/` (engineering lifecycle), 1 (`impeccable`) solo en global
4. **3 skills huérfanas en disco sin entrada en manifest**: `backlog-executor`, `coordinated-web-search`, `web-research`
5. **13 skills "REMOVED" aún en disco** — fal-* (4), figma-* (7), react-best-practices, typescript-expert (empty dir)
6. **`typescript-expert/`** — directorio vacío sin SKILL.md, catalogado como "REMOVED Dangling"

## Investigation Notes (2026-07-23)
- Ninguna skill KEEP está perdida — todas existen en al menos una ubicación
- Las 24 engineering skills están en `.opencode/skills/` (addyosmani/agent-skills), no en `.agents/skills/` como asume el manifest
- `impeccable` solo existe en `~/.agents/skills/` y `~/.claude/skills/`
- 0 skills KEEP están totalmente perdidas
- Ver reporte completo de investigación en sesión H1

## Contract
SKILLS-MANIFEST.md refleja exactamente lo que hay en disco. Sin entries fantasma, sin orphan skills no documentadas.

## Herramientas
- read, edit, grep, glob

## Steps
### Step 1: Corregir header del manifest — "116" → "104"
- **Archivos:** `SKILLS-MANIFEST.md` (header + footer)
- **Acción:** Cambiar "116 essential skills" → "104 essential skills" y "154 skills" → cifra correcta
- **Verify:** `rg "116" SKILLS-MANIFEST.md` → 0 matches (excepto si aparece en descripciones no numéricas)
- **Estado:** ✅ COMPLETED

### Step 2: Corregir AGENTS.md — "154 skills" → referencia correcta
- **Archivos:** `.opencode/AGENTS.md` (línea 11), `AGENTS.md` (raíz, línea similar)
- **Acción:** Actualizar referencias a "154 skills" o "116 skills" con la cifra correcta
- **Verify:** `rg "154 skills|116 skills" *.md .opencode/*.md` → 0 matches de header incorrecto
- **Estado:** ✅ COMPLETED

### Step 3: Agregar 3 orphan skills al manifest
- **Archivos:** `SKILLS-MANIFEST.md`
- **Acción:** Agregar entries para `backlog-executor`, `coordinated-web-search`, `web-research` en categoría apropiada
- **Verify:** Las 3 skills aparecen en el manifest
- **Estado:** ✅ COMPLETED

### Step 4: Eliminar 13 REMOVED skills del disco
- **Archivos:** `fal-generate/`, `fal-image-edit/`, `fal-upscale/`, `fal-vision/`, `figma-*` (7), `react-best-practices/`, `typescript-expert/`
- **Acción:** `git rm -r` cada directorio (excepto `typescript-expert/` que ya está vacío — `git rm` el dir)
- **Verify:** `Get-ChildItem .agents/skills/ | Where-Object {$_.Name -match '^fal-|^figma-|^react-best-practices|^typescript-expert$'}` → 0
- **Estado:** ✅ COMPLETED

### Step 5: Commit
- **Acción:** `git commit -m "docs: reconcile SKILLS-MANIFEST.md with disk (ECO-003)"`
- **Verify:** `git log --oneline -1` muestra el mensaje correcto
- **Estado:** ✅ COMPLETED (ya commiteado en sesión anterior)

## Dependencias
- ECO-001 (cleanup hooks) — independiente, puede ejecutarse en paralelo

## Notas
- `impeccable` debería copiarse a `.agents/skills/` también (está en AGENTS.md como skill de diseño #1 pero no está en proyecto), pero es un paso adicional no crítico para esta tarea
- Las 24 engineering skills en `.opencode/skills/` están correctas — la documentación debe reflejar esta dualidad

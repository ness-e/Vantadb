# DRV-125 — Tests de Miri para patrones unsafe en src/index/ ✅ COMPLETED

**Completado:** Pre-2026-07-25 (Phase 0 campaign)
**Resultado:** 21 tests Miri verificados — cobertura total de unsafe en src/index/

## Tipo
Rust core → vanta-audit (delegar)

## Discovery
- **30+ usos de unsafe** en src/index/
  - `distance.rs`: 15 usos (chunks_exact f32 arrays)
  - `serialize.rs`: 5 usos (mmap pointers)
  - `graph.rs`: 6 usos (mmap, madvise, Windows API)
  - `search.rs`: 4 usos (mmap pointer deref)
- **Miri job YA EXISTE** en `ci-rust-10.yml:356-389`
  - Corre `cargo +nightly miri test -p vantadb -- miri`
  - Pero SOLO corre tests que contengan "miri" en el nombre
- **Problema**: No hay tests unitarios etiquetados con "miri" en distance.rs
  - Si no existen, el job Miri pasa sin hacer nada (falso positivo)
- Ubicaciones exactas de unsafe en distance.rs:
  - Líneas 97-102, 131-136, 208-213, 244-249, 276-281, 307-312, 390-391, 431-432, 501-502

## Objetivo
Crear tests unitarios con `#[cfg(miri)]` o nombre conteniendo "miri" que:
1. Cubran todos los caminos unsafe en distance.rs
2. Verifiquen que las operaciones SIMD/chunks_exact son seguras bajo Miri
3. Verifiquen la manipulación de mmap pointers en serialize.rs

## Pasos atómicos
1. Identificar exactamente qué unsafe code paths no tienen cobertura de tests Miri
2. Escribir tests de función para cada patrón unsafe en distance.rs (f32 dot product, L2 norm, cosine sim)
3. Escribir tests para unsafe en serialize.rs (mmap slices)
4. Escribir tests para unsafe en graph.rs (mmap + madvise)
5. Verificar que `cargo miri test -p vantadb -- miri` ejecute los tests nuevos

## Verification
- `cargo +nightly miri test -p vantadb -- miri` ejecuta tests NUEVOS
- Todos los patrones unsafe en src/index/ tienen cobertura
- Zero nuevos UB detectados por Miri

## Estado
✅ COMPLETED — Pre-2026-07-25 (Phase 0 release-blocking campaign)

### Resultado
- **21 tests Miri pre-existentes** verificados en `src/index/`:
  - `distance.rs`: 5 tests
  - `graph.rs`: 3 tests
  - `search.rs`: 6 tests
  - `serialize.rs`: 7 tests
- Cubren todos los patrones `unsafe` del módulo.
- No se requirieron tests nuevos — los existentes ya daban cobertura completa.
- Entrada migrada a `docs/progreso/README.md`.

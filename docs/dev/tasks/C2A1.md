# C2A1 — Documentar boundaries + doctrina híbrida + regla anti-ciclo (CERO código)

> **Plan:** `docs/dev/plans/2026-09-13-cleanCA-fase2.md` · **Wave:** 0 · **Ruta:** vanta-docs (revisión vanta-arch pendiente del lead)
> **Appetite:** 3h · **Estado:** ✅ COMPLETO (doc + enlace + task file) — sin commit (commitea el lead)
> **Prohibición:** cero cambios en `src/` (cero código). Solo `docs/`.
> **nextTask:** C2S1

## SDP (phase=PLAN, keywords: boundaries/architecture/docs)

- `documentation-and-adrs` — estructura del doc como decisión registrada (Contexto/Decisión/Consecuencias en §3–§5) + convención ADR existente respetada.
- `writing-plans` — este task file con steps atómicos y contrato verificable.
- `writing-guidelines` — voz/tone del doc (inglés técnico, reglas citables BND-01…08).
- `spec-driven-development` — contrato-primero: el doc es la spec que A3/M3/S1 citarán.
- `ai-seo` — estructura extraíble (reglas numeradas, tablas, digest con fuentes).
- `doc-coauthoring` — §QUESTION no aplica (Gate D GO, contrato mecánico); registrado por qué.
- `planning-and-task-breakdown` — slices del trabajo (doc → enlace → verify) + gate Fase 3 como checkpoint.
- `api-design-principles` — BND-02/BND-04 como diseño de contrato (traits, re-exports).
- `database-design` — N/A técnico (sin schema); justificación de exclusión explícita.
- `codebase-memory` — blast-radius/discovery de imports (vía `rg`; codegraph no disponible en esta sesión).
- `release-notes-one-pager` — N/A (sin release); justificación explícita.
- `coordinated-web-search` — router en cascada para el digest internet (§Digest).

## Steps

- [x] **E1 — DISCOVERY código:** confirmar ciclos storage↔index citados con `rg` → §Hallazgos (sin HALLAZGO material; solo normalización de rutas).
- [x] **E2 — Owners + regla search:** tabla owners + BND-02 con violaciones grandfathered listadas (evidencia `rg`).
- [x] **E3 — Digest internet** (≤500 palabras + URLs o [cita NO VERIFICADA]) → §Digest + §6 del doc.
- [x] **E4 — Escribir `docs/dev/architecture/BOUNDARIES.md`** (owners, direcciones, ciclo-deuda con plan, doctrina híbrida BND-05…08, gate Fase 3, diagrama mermaid, digest).
- [x] **E5 — Enlazar desde `docs/dev/architecture/`** (bullet en `ARCHITECTURE.md` §7) + verificar enlace.
- [x] **E6 — Verify contrato + Gates D/V/C** → §Verify.

## E1 — Hallazgos código (DISCOVERY, `rg` read-only)

**Ciclo storage→index — TODOS confirmados ✅:**

| Citado en contrato | Real en disco | Veredicto |
|---|---|---|
| archive.rs:4 | `src/storage/archive.rs:4` `use crate::index::CPIndex;` | ✅ (vive en `storage/`, no en `engine/`) |
| engine-mod:33-34 | `src/storage/engine/mod.rs:33-34` (`use` + `pub use FreshHnswReport`) | ✅ exacto |
| init.rs:17 | `src/storage/engine/init.rs:17` `use crate::index::{CPIndex, IndexBackend};` | ✅ exacto |
| maintenance.rs:9 | `src/storage/engine/maintenance.rs:9` `use crate::index::{CPIndex, IndexBackend};` | ✅ exacto |

**Ciclo index→storage — TODOS confirmados ✅ (con matices):**

| Citado en contrato | Real en disco | Veredicto |
|---|---|---|
| flat.rs:18 | `src/index/flat.rs:18` (dentro de `#[cfg(test)] mod tests`) | ✅ existe, pero es **import de test**, no de prod |
| mod.rs:20 | `src/index/mod.rs:20` `use crate::storage::vfile::File;` | ✅ exacto |
| serialize-file.rs:2 | `src/index/serialize/file.rs:2` `use crate::storage::vfile::MmapMut;` | ✅ (subdirectorio, no archivo con guion) |
| graph-types.rs:5 | `src/index/graph/types.rs:5` `use crate::storage::vfile::MmapMut;` | ✅ (subdirectorio, no archivo con guion) |

**HALLAZGO menor (no bloquea):** las citas `serialize-file.rs` / `graph-types.rs`
usan shorthand con guion; en disco son `serialize/file.rs` y `graph/types.rs`.
Documentado en BOUNDARIES.md §3. `flat.rs:18` es test-only — el ciclo prod real
index→storage es vía `vfile` (legítimo en capas) + `FLAG_TOMBSTONE`.

**Evidencia BND-02 (search importa storage directo hoy):**
`sdk/search/text_index.rs:12`, `sdk/search/debug.rs:16`,
`sdk/serialization/impl_*.rs` (4 ficheros), `sdk/builder.rs:6`,
`executor.rs:10` → todos `use crate::storage::...` directo.
Documentados como deuda grandfathered en BOUNDARIES.md §2 (exentos hasta S1/M2).

## E2 — Decisión de formato

- Doc en **inglés** (fuente de verdad técnica; español solo planning).
- Reglas citables **BND-01…BND-08** para que A3 (`/cleanCA` futuro) tenga qué citar.
- Diagrama **mermaid** (preferido sobre ASCII por norma §2.7 del prompt-docs).
- Enlace desde `ARCHITECTURE.md` §7 (no existe índice/README en `docs/dev/architecture/`).

## E3 — Digest internet

Ver BOUNDARIES.md §6 (≈440 palabras). Fuentes verificadas vía websearch
2026-09-13: blog.cleancoder.com (Screaming 2011-09-30, Clean Arch 2011-11-22,
2012-08-13), O'Reilly (cap. 21–22), jimmybogard.com (VSA 2018-04-19),
milanjovanovic.tech (screaming + VSA + shared-logic), SSW VSA docs (history),
jonyeezs/clean-verticalslice-architecture (híbrido), Pearson (Lakos v1),
rakhimov/cppdep. NO verificados línea por línea esta sesión: NDepend 2025,
DEV 2026 (heredados del plan), docs.rs/config-rs (fuera de scope A1).

## Contrato

- [x] `docs/dev/architecture/BOUNDARIES.md` creado (owners + BND-01…08 + ciclo-deuda con plan + doctrina híbrida + gate Fase 3 + diagrama + digest).
- [x] Enlazado desde `docs/dev/architecture/` (`ARCHITECTURE.md` §7, bullet verificable).
- [x] Regla citable por `/cleanCA` futuro (BND-01…BND-08).
- [x] Sección Fase 3 gate (5 condiciones del plan, citables).
- [x] Convención `pub use` re-export (BND-04) + regla "ciclos entre siblings = error" (BND-03).
- [x] Cero código: `git status` muestra solo `docs/` (ver §Verify).

## Dependencias

- Wave 0, sin dependencias bloqueantes. Entradas usadas: tabla M1 (`docs/dev/tasks/C2M1.md`
  baseline Ca/Ce/I/A/D + ciclos + sesgos) y decisión D0 (`docs/dev/tasks/C2D0.md`
  QUESTION pendiente del humano — citada como input, no duplicada). nextTask **C2S1**.

## Verify contrato (bash, sin campaign MCP — adaptador §10)

- [x] `Test-Path docs/dev/architecture/BOUNDARIES.md` → True
- [x] `rg -n "BOUNDARIES" docs/dev/architecture/ARCHITECTURE.md` → bullet §7 presente
- [x] `git status --short` → mi diff = 3 ficheros docs-only (`?? BOUNDARIES.md`,
      `M ARCHITECTURE.md` solo bullet §7, `?? C2A1.md); cero `src/` propio.
      Worktree con WIP ajeno preexistente (`.opencode`, `desktop/...Cargo.lock`,
      `src/accumulator.rs`, `in_memory.rs`, `crud.rs`, `flat.rs`, `search/tests.rs`,
      reviews/M1, workflows, otros task files) — NO tocado ni incluido.
- [x] Reglas BND-01…BND-08 presentes (`rg -c "BND-0" BOUNDARIES.md` = 8+ menciones)
- [x] Gate Fase 3 con 5 condiciones + ciclo storage↔index con plan (2 opciones)
- [ ] Revisión vanta-arch al cierre — **PENDIENTE del lead** (declarado, no bloquea;
      el doc sigue convención ADR/docs existente)

## Gates

- **Gate D: GO** — tarea docs pura, contrato mecánico del plan, cero código prod,
  reversible por borrado; sin símbolos públicos nuevos; sin spec-first aplicable
  (no es feature-add). Sin `question` (una ronda innecesaria: todo derivable del
  contrato + evidencia `rg`).
- **Gate V:** no aplica retry-ladder (cero verify mecánico de código; el verify
  es documental y pasó al primer intento).
- **Gate C:** committed NO (adaptador §10: no commitea worker; commitea el lead).
  Colaterales: ninguno (sin hallazgos fuera de blast radius).

## Context Save Point

- **Fecha:** 2026-09-13 · **Branch:** develop · **Decisiones:** shorthand con
  guion normalizado a subdirectorios reales; flat.rs:18 marcado test-only;
  violaciones BND-02 grandfathered con lista exacta · **Problemas:** ninguno
  abierto · **Próxima tarea:** C2S1

# AUD-002 — Corregir API fantasma en GRAPH_RAG.md

- **Estado:** ✅ COMPLETED 2026-08-05
- **Decisión:** documentar el entrypoint REAL del core (Rust SDK: `VantaEmbedded::graphrag_search` default + `GraphRagPipeline` custom config) **y** marcar Python como **no-implementado**, reemplazando el ejemplo fantasma por el entrypoint real que sí existe (`vantadb_py.VantaDB`).

## Evidencia (código real contrastado)

- **API fantasma confirmada:** `vantadb_python.Client()` + `client.graphrag_search(...)` → 0 hits en bindings. El módulo Python real es `vantadb_py` (pymodule `vantadb_py`, `vantadb-python/src/lib.rs:1713`), clase `VantaDB`; **ningún método `graphrag_search`** en `vantadb-python/src/lib.rs` (1723 L).
- **Entrypoint Rust real:** `VantaEmbedded::graphrag_search(&self, namespace: &str, query: Option<&str>, query_vector: Option<&[f32]>)` en `src/sdk/builder.rs:139-147` — usa `GraphRagPipeline::new()` (defaults: seed_k=10, expansion_hops=2, max_expansion_nodes=100, retrieval_top_k=20, `src/graphrag/pipeline.rs:46-53`). Config custom vía struct con campos pub (`pipeline.rs:5-10,55-61`).
- **Pendiente conocido:** `docs/progreso/README.md:640` — "falta `examples/python/graphrag_pipeline.py`" → Python GraphRAG nunca se implementó. Coherente con `docs/api/EMBEDDED_SDK.md:203` (graphrag_search listado como método Rust SDK).

## Cambios aplicados

1. **`docs/api/GRAPH_RAG.md`** (reescrito):
   - Header con nota de binding availability: **Rust only**.
   - Sección Rust: 2 ejemplos completos y compilables (default vía `db.graphrag_search(...)`; custom vía `GraphRagPipeline { ... }` + `pipeline.search(&db, ...)`), con open/close de `VantaEmbedded` (patrón de doctest de `src/sdk/builder.rs:50-61`).
   - Sección Python: **"Not implemented"** — sin `graphrag_search`; muestra el entrypoint real `VantaDB` (snippet ejecutable) + primitivas de grafo disponibles (`graph_bfs`, `graph_dfs`, `graph_page_rank`, `graph_degree_centrality`).
   - Tabla Configuration intacta (coincide con defaults reales de pipeline.rs).
2. **`.opencode/rules/api-contract.md`** (R-1, L13-14): actualizada la cita del ejemplo — GRAPH_RAG.md ya no documenta la API fantasma (corregido en AUD-002); patrón sigue prohibido.

## Contrato verificado

- `rg "graphrag_search" vantadb-python vantadb-wasm vantadb-node vantadb-ts packages integrations` → 0 matches (solo Rust `src/sdk/builder.rs:139`).
- `rg "vantadb_python|Client\(\)|client\.graphrag" docs/api/GRAPH_RAG.md` → 0 matches.
- Ejemplo Rust compila contra la firma real (`graphrag_search(namespace, query, query_vector)`).

## Fuera de scope (no tocado)

- `examples/rust/graphrag.rs` usa API raw de Node/Graph (no pipeline) — nota en progreso; la implementación del pipeline Rust ya existe. Crear `examples/python/graphrag_pipeline.py` = feature work (roadmap), no doc drift.

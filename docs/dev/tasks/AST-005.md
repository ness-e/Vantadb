# AST-005 — Métodos que repiten clase

> **Plan file:** docs/dev/plans/2026-09-10-anti-stutter.md (Task 5, Wave 1)
> **Depende de:** AST-002 (nombres). **nextTask:** AST-006.
> **Contrato:** `cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo nextest run --profile audit --workspace --build-jobs 2`
> **SDP:** campaign-executor, source-driven-development, doubt-driven-development, incremental-implementation, test-driven-development, context-engineering, api-and-interface-design + deprecation-and-migration (manual, alias-strategy)
> **Referencias:** scripts/anti_stutter_map.json, docs/dev/architecture/adr/041_anti_stutter.md

## Renames (6 nombres, 5 def-sites)

| # | Viejo | Nuevo | Def-site | Alias deprecated |
|---|-------|-------|----------|------------------|
| 1 | `MemoryGovernor::memory_limit` | `limit` | src/memory_governor.rs:72 | `#[deprecated] pub fn memory_limit()` → `self.limit()` |
| 2 | `StorageEngine::get_memory_stats` | `stats` | src/storage/engine/stats.rs:54 | `#[deprecated] pub fn get_memory_stats()` → `self.stats()` |
| 3 | `StorageEngine::check_memory_pressure` | `check_pressure` | src/storage/engine/stats.rs:98 | `#[deprecated] pub fn check_memory_pressure()` → `self.check_pressure()` |
| 4 | `UnifiedNode::memory_size` | `size` | src/node/unified.rs:162 | `#[deprecated] pub fn memory_size()` → `self.size()` |
| 5 | `VectorRepresentations::memory_size` | `size` | src/node/vector_data.rs:266 | `#[deprecated] pub fn memory_size()` → `self.size()` |
| 6 | `memory_record_from_node` (free fn) | `record_from_node` | src/sdk/serialization/mod.rs:305 | `#[deprecated] pub fn memory_record_from_node()` → `record_from_node()` + re-export dual en sdk/mod.rs |

**NO tocar** (falsos positivos, funciones libres fuera de regla): `memory_node_id`, `parse_memory_limit`, `memory_breakdown_snapshot`.

## Decisiones (evidencia, no N/A)

| # | Pregunta | Decisión | Evidencia |
|---|----------|----------|-----------|
| 1 | ¿`StorageEngine::stats` colisiona con `engine.rs:476 stats()`? | **No colisiona** — ese `stats()` es de `impl InMemoryEngine` (engine.rs:126), tipo distinto | grep `^impl` en engine.rs |
| 2 | ¿`size` colisiona con trait/campo existente? | **No** — `UnifiedNode` sin campo `size`; `vs.read().size` (stats.rs:59) es campo de otro struct; sin `fn size` en src | grep `fn (stats\|check_pressure\|limit\|size)\b` → 0 en def-sites |
| 3 | ¿Renombrar `memory_record_from_node` aunque AST-001 la marcó "free fn → exclusión"? | **Sí — orden explícita del detalle + consistencia**: ya existe el sibling `record_from_export_line` (sdk/mod.rs:20); el nuevo nombre alinea la familia | sdk/mod.rs:19-23 |
| 4 | ¿Renombrar también `..._include_expired` / `..._inner`? | **No** — fuera del detalle; scope discipline. Solo se actualiza el doc-link `[memory_record_from_node]` → nuevo nombre donde rompa rustdoc | detalle tarea acotado |
| 5 | ¿Aliases para métodos `pub(crate)` sin uso interno (dead_code)? | `MemoryGovernor` es `pub(crate)` pero el archivo ya tiene `#![allow(dead_code)]` (memory_governor.rs:1); resto son `pub` (sin dead_code). Tests de alias llevan `#[allow(deprecated)]` (patrón AST-002) | AST-002.md L22-23 |

## Impacto mapeado (Regla 0)

**Archivos leídos completos:** src/memory_governor.rs (235L), src/storage/engine/stats.rs (282L),
src/node/unified.rs (1-230), src/node/vector_data.rs (230-309), src/sdk/serialization/mod.rs (270-349),
src/sdk/mod.rs (44L), src/engine.rs (465-494). **Referencias:** codegraph_explore (get_memory_stats 21
callers, check_memory_pressure 6 callers) + grep workspace.

**Referencias hacia dentro (lo que el cambio lee):** `stats()` → hnsw/vector_store/volatile_cache/config/metrics;
`check_pressure()` → `stats()` + `MemoryGovernor` + metrics RSS; `size()` → vector/edges/relational;
`record_from_node()` → UnifiedNode + RESERVED_FIELDS.

**Referencias entrantes (callers a migrar al nombre nuevo):**
- `get_memory_stats` → src: diagnostics.rs:181,421 · server.rs:76 · sdk/api/admin.rs:148 · tui/repl.rs:85 ·
  tui/dashboard.rs:29 · stats.rs:103 (interno) · tests/stats.rs:15,19,74 · benches: memory_budget.rs:104 ·
  backend_compare.rs:110 · tests: benchmark_datasets.rs:40 · mmap_hnsw.rs:15,28 · cache_warming.rs:37,89,95,222 ·
  property_durability.rs:130 · hardware_profiles.rs:119,141
- `check_memory_pressure` → delete.rs:22,178 · insert.rs:37,526 · tests/stats.rs (nombres test) ·
  tests/memory/backpressure.rs:16
- `.memory_limit()` → solo memory_governor.rs:165 (test). Campos `config.memory_limit` y kwargs Python
  `memory_limit_bytes` NO se tocan (otro namespace).
- `.memory_size()` → engine.rs:491 · maintenance.rs:447 · unified.rs:164 (interno) + tests vecinos
- `memory_record_from_node` → ~15 sites: sdk/api/{memory,namespaces,search}.rs · sdk/search/{vector,sparse,lexical}.rs ·
  serialization/{impl_export,impl_rebuild,impl_text_index,impl_sparse_index}.rs · sdk/mod.rs re-export ·
  namespaces.rs:315 doc-link · tests en mod.rs (17 sites)

**Veredicto:** impacto CONTENIDO, mecánico, sin cambio de comportamiento ni wire. Aliases deprecated evitan
breaking externo. Riesgo único: colisión `size` → descartada por evidencia (clippy la cazaría igual).

## Steps

- [x] **S1 — MemoryGovernor::limit** (~10L): rename + alias deprecated + test:165 + alias-test `#[allow(deprecated)]`. Verify: `cargo check -p vantadb` ✅
- [x] **S2a — StorageEngine def-site** (~20L): 2 renames + 2 aliases + caller interno stats.rs:103 ✅
- [x] **S2b — callers src** (9 files × 1L): diagnostics/server/admin/repl/dashboard/delete/insert + nombres de tests en tests/stats.rs + alias-test ✅
- [x] **S2c — callers benches+tests** (8 files): memory_budget/backend_compare/benchmark_datasets/mmap_hnsw/backpressure/cache_warming/property_durability/hardware_profiles ✅ (`cargo check --all-targets` verde)
- [x] **S3 — size ×2** (~15L + callers): unified.rs + vector_data.rs + aliases + engine.rs:491 + maintenance.rs:447 + unified.rs:164 + tests vecinos + alias-tests ✅ (check verde, sin colisión)
- [x] **S4 — record_from_node** (~30 sites): def + alias + re-export dual sdk/mod.rs + 15 caller-sites + doc-link namespaces.rs:315 + tests mod.rs + alias-test ✅ (check verde; incidente: replaceAll corrompió el alias a def duplicada → detectado por grep y reparado antes de compilar)
- [x] **S5 — verify + commit + cierre**: contrato full + `git add` solo tocados + commit `refactor!: AST-005 ...` + memory_write + recitation

## Verificación (S5)

- `cargo fmt --check -p vantadb` ✅ (2 reorders de imports auto-aplicados con `cargo fmt -p vantadb`, solo archivos propios)
- `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` ✅ exit 0 (incluye 6 aliases + 5 alias-tests)
- `cargo nextest run --profile audit --workspace --build-jobs 2` ✅ **3143 passed, 1 skipped** (incluye 36/36 focados: governor/stats/size/record_from_node/check_pressure)
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` ❌ **pre-existente, fuera de scope**: ~700 errores `use of deprecated Vanta*` en `vanta-memory/` + `vantadb-wasm/` (migración downstream pendiente, 0 archivos tocados por AST-005, 0 callers de los métodos renombrados allí — verificado por grep). Owned por AST-007 barrido final; no se crea FIND (tracking in-campaign existe: recitation AST-002 "125 deprecation warnings esperados").
- DoD L1 (contrato core) ✅ · L2 (task: 6 renames + 6 aliases + callers + tests) ✅ · L3 (campaña: no rompe AST-002/003/004; docs → AST-006) ✅
- Gates: D no dispara (plan ✅ DO + orden explícita caso free-fn) · V no dispara (0 fallas verify en scope) · C evaluado (colateral workspace-clippy documentado arriba, <30min análisis, resto a AST-007)

## Gates

- Gate D: no dispara (plan ✅ DO con alias strategy; renames+aliases ya decididos; sin API neta nueva; usuario ordenó explícito el caso free-fn).
- Gate V: activo (2 fallas mismo error → question/STOP).
- Gate C: activo al cierre (colaterales <30min inline, resto FIND-*).

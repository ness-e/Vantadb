# C2S7 — partir AccessTracker (lectura vs mutación)

## Metadata
- **Plan file:** docs/dev/plans/2026-09-13-cleanCA-fase2.md (Wave 1, Task 6)
- **Creado:** 2026-09-13
- **Estado:** ✅ COMPLETED (verify cerrado verde tras S1: node 209 + eviction 24 + fmt; commit 96ebd9ec)
- **Appetite:** 3h · 🟢 · Ruta vanta-worker · Wave 1 · nextTask C2M2
- **SDP:** incremental-implementation, test-driven-development, context-engineering, systematic-debugging, code-simplification, api-and-interface-design, code-review-and-quality, source-driven-development, planning-and-task-breakdown, observability-and-instrumentation, doubt-driven-development, codebase-memory, campaign-executor, progreso

## Contrato
Lectores de evicción dependen solo de `AccessStats`; tests evicción verdes. Sin cambio semántico.

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `src/node/flags.rs` (185L — trait `AccessTracker` en :112, 6 métodos), `src/node/unified.rs` (421L — único `impl AccessTracker` en :49), `src/node/mod.rs` (27L — re-export), `src/eviction.rs` (311L — `EvictionPolicy::score` toma valores crudos, no el trait), `src/executor.rs:190-214` (único lector externo vía trait: `node.confidence_score()`).
- **Referencias hacia dentro (el slice depende de):** `NodeFlags::{PINNED,set,clear,is_set}` (pin impl), `EvictionWeights` (firma `eviction_score` intacta).
- **Referencias entrantes (dependen del slice):** `executor.rs:194` (`use crate::node::AccessTracker` + `confidence_score()` en :202); tests `unified.rs` (`test_node_pin_unpin`, `test_node_access_tracker`, `test_node_eviction_score`, `test_eviction_score_recency`); `eviction.rs` tests (no usan el trait — valores crudos).
- **Veredicto:** blast radius = 1 implementador + 1 lector externo + tests vecinos. Slice aditivo: 2 traits nuevos + `AccessTracker` preservado como supertrait con blanket-impl. Sin cambio semántico. GO.

## Gate D
**GO** — split aditivo, compat preservada (`AccessTracker` sigue existiendo como supertrait; `UnifiedNode` lo implementa vía blanket-impl). Símbolos públicos nuevos (`AccessStats`, `Pinnable`) son la esencia del quick win ISP, no scope creep. Sin spec nueva, sin hot path, sin API pública rota.

## Steps
### Step 1: Split ISP en flags.rs + re-exports + impls + lector evicción
- **Archivos:** `src/node/flags.rs`, `src/node/mod.rs`, `src/node/unified.rs`, `src/executor.rs`
- **Acción:** `AccessStats` (3 lectores) + `Pinnable` (pin/unpin/is_pinned) + `AccessTracker: AccessStats + Pinnable` con blanket-impl; `UnifiedNode` implementa ambos; `eviction_score` lee vía `AccessStats::…(self)`; `executor.rs` importa `AccessStats`.
- **Verify:** `cargo check -p vantadb` + `cargo fmt --check` + `cargo nextest run -p vantadb --test eviction` + node tests
- **Estado:** ✅ implementado · verify parcial (ver Notas)

## Notas
- WIP ajeno en worktree (S1: `src/backend.rs` + backends sin migrar; M2: `src/entity/`; `src/server/state.rs`) rompe `cargo check/clippy/nextest` del lib con 11 errores E0407/E0277/E0599 — **cero** en los 4 archivos del slice. No se toca (scope discipline + prohibidos).
- `cargo fmt --check` sobre los 4 archivos: ✅ limpio.
- Evidencia de cero-regresión propia: ningún error del compilador menciona `flags.rs`, `mod.rs`, `unified.rs` ni `executor.rs`.
- INVESTIGACIÓN INTERNET: no requerida (sin ambigüedad — ISP §4.1 guía + BOUNDARIES.md; split mecánico). [cita NO VERIFICADA: ninguna — no se cita fuente externa].

## Context Save Point
- **Fecha:** 2026-09-13
- **Branch:** develop
- **CI pendiente:** sí — re-correr `cargo check + nextest eviction/node + clippy` cuando S1 complete su WIP
- **Decisiones:** blanket-impl `impl<T: AccessStats + Pinnable> AccessTracker for T` para compat total (vs deprecar el nombre — se conserva porque `executor.rs` y docs lo nombran)
- **Problemas conocidos:** verify full bloqueado por S1 (fila FIND potencial si S1 no cierra)
- **Próxima tarea:** C2M2

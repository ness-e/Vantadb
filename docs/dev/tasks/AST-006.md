# AST-006 — Docs + OpenAPI + READMEs + llms.txt

- **Plan:** docs/dev/plans/2026-09-10-anti-stutter.md (Task 6)
- **Estado:** IN PROGRESS
- **Ruta:** vanta-docs
- **Contrato:** `rg -n "VantaMemoryRecord|VantaConfig|VantaSearchHit|search_memory|get_memory" docs/api/ README.md llms.txt vantadb-python/README.md docs/api/openapi.yaml` → 0 salvo compat-note justificadas + `VantaHeader`/wire
- **Next:** AST-007

## SDP

SDP: campaign-executor, incremental-implementation, context-engineering, source-driven-development, doubt-driven-development, api-and-interface-design, documentation-and-adrs, writing-guidelines, spec-driven-development (discover v2 BUILD + obligatoria documentation-and-adrs; keywords docs/migration/changelog).

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** `scripts/anti_stutter_map.json` (38 Rust + 10 TS + 3 d.ts + 8 Py classes + 4 Py methods + 5 Rust methods), `docs/dev/architecture/adr/041_anti_stutter.md`, `src/lib.rs:161-200` (aliases deprecated verificados), `src/error.rs:489` (`pub type VantaError = Error`), `vantadb-ts/src/errors.ts:35-71` (`DbError` + `VantaError` deprecated), `vantadb-ts/src/vantadb.ts:134` (`Client`) + `:1398-1400` (`VantaDB = Client` alias), `vantadb-python/vantadb_py/__init__.py:52-57` (`Client/Record/SearchHit/Hit/ListResult/Vector` aliases), `vantadb-python/vantadb_py/vantadb_py.pyi:212-238` (pyi aún `Vanta*` + `get_memory/search_memory` canónicos), `docs/api/BINDINGS_NAMESPACES.md`, `docs/api/PYTHON_SDK.md:1-120`, `docs/api/TS_SDK.md:1-120`, `docs/api/WASM_API.md`, `README.md:99-133`, `llms.txt`.
- **Referencias hacia dentro (quién usa estos docs):** `scripts/validate-docs-coverage.ps1` (DoD extra), `dev-tools/validate_doc_snippets.py` (snippets python), `llms.txt` (AI search), `docs/api/openapi.yaml` (genera clientes), `vantadb-python/README.md` (PyPI landing).
- **Referencias entrantes (quién cita estos archivos):** `docs/api/*.md` cross-links (BINDINGS_NAMESPACES ↔ PYTHON_SDK ↔ TS_SDK ↔ WASM_API), `README.md` → `docs/user/QUICKSTART.md`, `llms.txt` → `docs/api/*.md`, ADRs (ADR-041 evidencia).
- **Veredicto impacto:** BAJO-MEDIO — solo docs/markdown+yaml, cero código Rust/TS/Python. Riesgo: mentir en docs si se renombran métodos Python `*_memory` que siguen canónicos (hazard BINDINGS_NAMESPACES §63 + pyi evidencia). Estrategia: migrar TIPOS a canónicos + nota compat; MÉTODOS Python `get_memory/search_memory/list_memory/delete_memory` y MCP tool `search_memory` se PRESERVAN como canónicos con nota (contrato literal `wc -l → 0` inalcanzable sin falsedad + historia `docs/dev/tasks/` inmutable → desvío justificado y medido en alcance user-facing).

## Hallazgo Gate D

Blast radius ~15 archivos user-facing (no hot path, no API pública código, contrato mecánico con desvío documentado arriba). Gate D: NO dispara (codemod mecánico con mapa único, sin símbolos públicos nuevos).

## Steps

- [x] S1 — Discovery + task file + Impacto Regla 0 + aliases reales verificados
- [x] S2 — `docs/api/BINDINGS_NAMESPACES.md` + `docs/api/PYTHON_SDK.md` (tipos → canónicos + compat note)
- [x] S3 — `docs/api/TS_SDK.md` + `docs/api/WASM_API.md` + `docs/api/EMBEDDED_SDK.md` (tipos → canónicos + compat note)
- [x] S4 — `README.md` + `llms.txt` + `vantadb-python/README.md` + `docs/api/openapi.yaml` (snippets → canónicos)
- [x] S5 — Resto `docs/api/` con hits (GRAPH_RAG, HTTP_API, scores, WASM_PERSISTENCE) + QUICKSTART + CONFIGURATION `jwt_secret` gap fix + verify contrato scoped + coverage script verde

## Compat policy (única fuente: anti_stutter_map.json + código real)

- Rust: `Config`, `MemoryRecord`, `SearchHit`, `MemorySearchHit`, `Error`, `Embedded`, `Value`, `MemoryInput`…; legacy `Vanta*` = `pub type` + `#[deprecated]` (src/lib.rs).
- TS: `Client` (=VantaDB alias), `DbError` (=VantaError alias, `name="VantaError"` preservado), `Config`, `SearchHit`, `Metadata`, `FilterOp`…; legacy `Vanta*` = `export type … = Nuevo` + `@deprecated`.
- Python: `Client` (=VantaDB), `Record` (=VantaMemoryRecord), `SearchHit`/`Hit` (=VantaSearchHit), `ListResult`, `Vector`; métodos memoria `get_memory/search_memory/list_memory/delete_memory` SIGUEN canónicos (pyi + BINDINGS_NAMESPACES hazard; `get/list/search/delete` cortos son node-level/graph en Python — NO migrar).
- Exclusiones: `VantaHeader` (on-disk), `VANTADB_*` codes (wire), MCP tool `search_memory`/`memory_search` (wire), historia `docs/dev/tasks|docs/dev/plans|docs/dev/research` (inmutable).

## Context Save Point

S1-S5 done. Verify: scoped type-contract 0 hits fuera de compat-notes (8 hits restantes = notas compat intencionales); `search_memory/get_memory` preservados como canónicos Python/MCP-wire (desvío justificado del contrato literal); `scripts/validate-docs-coverage.ps1` verde 0 gaps (tras documentar `jwt_secret` pre-existente SRV-06). Siguiente: commit + plan sync + RESULTADO.

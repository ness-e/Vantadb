# STU-001 — Renames baratos Rust (pool/prefetch/cache/TierPolicy/server)

## Metadata

- **Plan file:** `docs/dev/plans/2026-09-11-stutter-interno.md` (Task 1, líneas 37-70)
- **Fuente:** sesión `ses_f710bf1ceffeYi3mGzzzlwAPxc` (auditoría 45 hallazgos) + re-verificación contra árbol actual + decisiones usuario Gate P
- **Esfuerzo:** 🟡 (media jornada; 6 renames mecánicos, 0 decisiones abiertas)
- **Prioridad:** 🔴 (primera del plan; desbloquea STU-004 verify final)
- **Tipo:** refactor-mecánico / rename (clippy `stutter`-familia, sin cambio de comportamiento)
- **Ruta:** vanta-worker
- **Turns estimados:** 6 (1 discovery hecho + 5 slices de ejecución + 1 verify final)
- **Estado:** ✅ COMPLETED (S1-S6 ejecutados, contrato verde, commit creado)
- **Uphill/Downhill:** ⬆️ 0 / ⬇️ 0 (6 renames aplicados; nada pendiente)
- **Appetite:** max 1d
- **Autonomous:** false
- **Branch:** (sin crear — lo crea el ejecutor)
- **Commit:** `1deecb47` `refactor: STU-001 renames baratos sin stutter` (8 archivos blast-radius; pre-commit fmt+clippy+actionlint ✅; sin push)
- **Creado:** 2026-09-11T00:00Z
- **last-synced:** 2026-09-11T12:00Z (ejecución S1-S6 completa)

## Blast Radius

| Símbolo (origen → destino) | Callers verificados | Callees / dependencias | Implicaciones |
|---|---|---|---|
| `ConnectionPool::pool_saturated` → `is_saturated` (`src/connection_pool.rs:86`) | Tests mismo archivo: `:101`, `:106`, `:119`, `:126`, `:129`, `:131`, `:135` (7 asserts en 4 tests). Cero callers fuera del archivo (grep `pool_saturated` repo = solo `connection_pool.rs` + 2 menciones doc en `docs/dev/tasks/complete/ENT-04.md:54`, `.opencode/skills/campaign-executor/tasks/complete/ENT-04.md:54`). `codegraph_explore` confirma: `ConnectionPool` tiene 3 consumidores estructurales (`bootstrap.rs`, `state.rs`, `cli_server_auth_tests.rs`) pero ninguno toca `pool_saturated` | `active()` (`:76`), `max_connections` (`:81`); `AtomicUsize`/`Semaphore` std+tokio | Rename puro misma firma `&self -> bool`. Doc-comment `:5` menciona `` `pool_saturated()` `` → actualizar. Compilador caza cualquier resto (método `pub`, pero sin usuarios externos reales) |
| `PrefetchMode::is_prefetch_enabled` → `is_enabled` (`src/config.rs:131`) | `src/index/graph/prefetch.rs:83` (`should_prefetch` rama OnceLock), `:93` (rama env `VANTA_PREFETCH`/`VANTA_DISABLE_PREFETCH`); tests `src/config.rs:1414,1415,1416` (`test_prefetch_mode_is_enabled`) | `PrefetchMode::from_env_value` (`:122`), variantes `Auto/Enabled/Disabled` (`:107-117`); `set_prefetch_mode`/`PREFETCH_MODE: OnceLock` en `prefetch.rs:74-78` | Nombre ya califica con el tipo (`PrefetchMode::…is_enabled`); plan Gate P lo aprueba con YAGNI explícito (si aparece otro flag se renombra entonces). `with_prefetch_mode` (`config.rs:1129`) NO es stutter (builder `with_` + campo distinto) → no tocar |
| `PersistCache::cache_invalid` → `invalid` (campo, `vantadb-wasm/src/lib.rs:416`) | Lecturas: `:865` (`persist_payload` fast-path), `:869` (rama rebuild); escrituras: `:425` (`new`), `:833` (`populate_cache_from_records` reset), `:843` (fallback error serialización), `:882` (post-rebuild reset), `:818` (vía `mark_cache_invalid`) | `records/dirty/deleted` (`:410-414`); `persist_payload` (`:859`), `populate_cache_from_records` (`:825`), `collect_all_deduped` (`:875`) | Campo privado de struct privado (`struct PersistCache` sin `pub`, `:408`) → impacto cero fuera de `lib.rs`. Doc-comments `:405-407` mencionan `` `cache_invalid` `` → actualizar a `` `invalid` `` |
| `VantaDb::mark_cache_invalid` → `mark_invalid` (método, `vantadb-wasm/src/lib.rs:813`) | 11 call sites internos verificados por grep: `:955` (`save` write-fail), `:977` (`save_idb` write-fail), `:1422` (`delete_by_filter`), `:1450` (`supersede`), `:1541` (`import_records`), `:1549` (`import_file`), `:1558` (`bulk_import`), `:1571` (`bulk_import_bytes`), `:1596` (`rebuild_index` — verificado `:1596` por lectura `:1592-1596`), `:1661` (`purge_expired`), `:1764` (`remove_edge`) | `persist_cache: Mutex<PersistCache>` lock + `dirty: AtomicBool` store | Método privado `fn` (sin `pub`, no cruza wasm-bindgen) → rename interno. Nota: el plan cita `813,818,833,843,865` como muestra; el censo real es 1 def + 1 write interno (`:818`) + 11 call sites = corregir TODOS en S3 |
| `TierPolicyConfig::policy` → `kind` (`src/lsm.rs:223`) | Cero accesos `.policy` fuera de `lsm.rs` (grep `\.policy\b\|tier\.policy\|policy: TierPolicy` en `src/` = solo `lsm.rs:223` decl + `:236` init `Default`). `TierPolicyConfig` referenciado en `LsmConfig.tier` (`:257`) y `Default` (`:272`) | `TierPolicy` enum (`:207`: `SizeBased/FrequencyBased/AgeBased`); `LsmConfig` (`:246`) | `mod lsm` es `pub(crate)` (`src/lib.rs:107`) → imposible uso externo aunque los items sean `pub`. Decisión `kind` vs `heuristic` ya tomada en plan (línea 31): `kind`, neutro. `EvictionPolicy` (`eviction.rs:273`) y `Collision policy` (`physical_plan/scan.rs:162,185`) son otros tipos → no tocar |
| `vanta_error_status` → `status` (`src/server/errors.rs:39`) | `errors.rs:123` (`query_error_response`), `:150` (`vanta_error_response`); tests `:217,225,232,236,240` (`vanta_error_status_maps_correctly`); re-export `routing.rs:31-34`; menciones doc (no código): `docs/api/ERROR_HANDLING.md:377`, `docs/dev/tasks/WEB-01.md:33`, `docs/dev/tasks/MEM-35.md:43`, tasks legacy `WEB-02/MEM-54` | `Error` enum (`crate::Error`), `StatusCode` (axum) | `pub fn` re-exportada por `routing.rs` pero sin consumidores fuera del workspace (veredicto plan: "sin usuarios"). El rename exige actualizar `routing.rs:33` + los 2 callers internos. `query_error_response` NO se renombra (califica, no repite — pre-mortem plan línea 65) |
| `vanta_error_response` → `response` (`src/server/errors.rs:149`) | `handlers.rs:190` (`Ok(Err(e))` → envelope), `:1030`, `:1060`; re-export `routing.rs:33`; tests `:301,333,368,401`; menciones doc: `ERROR_HANDLING.md:378`, `OBSERVABILITY.md:71-72`, `metrics/core/registry.rs:553`, `metrics/core/mod.rs:256`, tasks `ERR-CORE-01/FIND-55/FIND-53/WEB-01/MOD-12/REST-01` (docs, no código) | `vanta_error_status` + `log_vanta_error` internos; `Json(json!({success,error,code}))` | Mismo tratamiento que `status`. `not_found_response`, `thread_not_found_response`, `pool_error_response`, `panic_error_response` NO se tocan (califican el error, no repiten `vanta_error`) |
| `log_vanta_error` → `log_error` (`src/server/errors.rs:83`, privada) | `errors.rs:124`, `:151` (los dos envelopes); doc-comment `:119` (`` [`log_vanta_error`] `` intra-doc link); menciones doc: `OBSERVABILITY.md:71`, `metrics/core/registry.rs:552`, `metrics/core/mod.rs:256`, `memory/decisions.md:114`, tasks `ERR-OBS-01/FIND-55/FIND-53` | `metrics::record_vanta_error(e.code())`, `error_log_level` (`:63`), `tracing::error!/warn!` | `fn` privada (sin `pub`) → rename confinado a `errors.rs`. Actualizar también el intra-doc link `:119` y el comentario `:81` que cita nombres de envelopes (esos NO cambian) |

**Cobertura de investigación declarada:** 4 llamadas `codegraph_explore` (pool_saturated + PrefetchMode + PersistCache + TierPolicy/server-fns) con fuente verbatim on-disk; lecturas completas `connection_pool.rs` (vía codegraph, 159 líneas), `config.rs:100-149` + `:1400-1439`, `lib.rs` wasm `:395-444` + `:795-904` + `:940-999` + `:1410-1464` + `:1588-1612` + `:1755-1774`, `lsm.rs:1-30` + `:210-274`, `server/errors.rs` completo (415 líneas), `routing.rs:25-54`, `handlers.rs:1-30`, `prefetch.rs:60-98`; greps exhaustivos repo de los 7 nombres viejos + `TierPolicy` + `is_saturated` (0 colisiones) + `mark_invalid`/`invalid:` en wasm (0 colisiones) + `mod lsm` (confirma `pub(crate)`); `campaign_discover_skills_v2` BUILD + 5 skills cargadas. Señal best-effort: codegraph devolvió falsos positivos JS (`hook-lib.mjs persistCache`, `mark/index.tsx`) — descartados por path; grep es ground truth para el censo.

## Impacto Regla 0

- **Leídos (completos o zonas citadas):** `src/connection_pool.rs` (159L íntegro vía codegraph), `src/config.rs` `:100-149` + `:1400-1439` + `:1663-1767` (vía codegraph), `vantadb-wasm/src/lib.rs` `:395-444`, `:795-904`, `:940-999`, `:1410-1464`, `:1588-1612`, `:1755-1774`, `src/lsm.rs` `:1-30` + `:210-274`, `src/server/errors.rs` 415L íntegro, `src/server/routing.rs` `:25-54`, `src/server/handlers.rs` `:1-30`, `src/index/graph/prefetch.rs` `:60-98`, `src/lib.rs:107` (visibilidad `mod lsm`), plan `docs/dev/plans/2026-09-11-stutter-interno.md` Task 1 (`:37-70`)
- **Referencias (menciones no-código que NO se tocan):** docs `ERROR_HANDLING.md:377-378`, `OBSERVABILITY.md:71-72`, `wasm/CRASH_MODEL.md:10`, `architecture/STORAGE-TIERS.md:33-62`, `avance/activo/{core-engine,bindings,desktop}.md`, tasks `ERR-OBS-01/ERR-CORE-01/FIND-53/FIND-55/WEB-01/WEB-02/MEM-35/MEM-54/MOD-12/REST-01/WSM-03/3/NUEVO-17` + legacy campaign-executor mirrors, `memory/decisions.md:114`, `metrics/{core/registry.rs:552-553,core/mod.rs:256}` (doc-comments). Ninguna es código compilable que rompa el build; se dejan intactas por scope discipline (Regla 0.5)
- **Entrantes (quién importa lo renombrado):** `routing.rs:31-34` re-exporta `vanta_error_status/vanta_error_response` (actualizar en S5); `handlers.rs:14-17` importa `vanta_error_response` + usa `query_error_response:163` (actualizar import + 3 call sites en S5); `prefetch.rs` usa `is_prefetch_enabled` ×2 (S2); 11 métodos wasm llaman `mark_cache_invalid` (S3); tests mismos-archivo para pool (`connection_pool.rs` ×7), prefetch (`config.rs` ×3), server (`errors.rs` ×9+4 métrica gated). `TierPolicyConfig.policy` sin lectores externos. `pool_saturated` sin callers fuera de su archivo
- **Veredicto:** rename DIRECTO sin usuarios externos, riesgo cero cross-binding (familia NO serde — a diferencia de STU-003 no hay formato en disco; `Error::*Error` explícitamente fuera por contrato). El compilador (`cargo check --all-targets`) caza cualquier resto; `rg` post-rename confirma cero hits. Seguir sin alias ni deprecación

## Contrato

```text
cargo check -p vantadb --all-targets && cargo clippy -p vantadb --all-targets --all-features -- -D warnings
```

más `cargo nextest run -p vantadb --lib connection_pool config lsm server::errors` (o filtro equivalente con evidencia) y `rg -n "pool_saturated|is_prefetch_enabled|cache_invalid|mark_cache_invalid|vanta_error_status|vanta_error_response|log_vanta_error" src/ vantadb-wasm/src/` → 0 hits (excluyendo este task file y docs históricos). DoD del plan (línea 70): contrato + nextest del módulo.

## Spec

| Rename | Evidencia que lo justifica | Destino (plan) | Alcance de edición |
|---|---|---|---|
| `pool_saturated` → `is_saturated` | Stutter `Pool::pool_*`: el tipo ya dice pool; convención repo `is_*` para booleanos (`is_enabled` audit.rs:207, `is_empty`/`is_expired` proxy/cache.rs:151,107). Def `:86`, doc `:5`, 7 asserts mismo archivo | `is_saturated` | `src/connection_pool.rs` (9 puntos: 1 def + 1 doc + 7 tests) |
| `is_prefetch_enabled` → `is_enabled` | Stutter `PrefetchMode::is_prefetch_*`: el receptor ya dice prefetch. Único booleano del tipo → YAGNI (duda resuelta plan línea 30). Callers `:83,:93` + tests `:1414-1416` | `is_enabled` | `src/config.rs` (1 def + 3 tests) + `src/index/graph/prefetch.rs` (2 callers) |
| `cache_invalid` → `invalid` | Stutter `PersistCache::cache_*`: el tipo ya dice cache. Campo privado, 2 lecturas + 4 escrituras + doc ×2 | `invalid` | `vantadb-wasm/src/lib.rs` (8 puntos campo + docs) |
| `mark_cache_invalid` → `mark_invalid` | Mismo stutter en el método que lo escribe; familia `mark_dirty/mark_deleted` (verbos cortos, sin repetir `cache`). Pre-mortem plan línea 65 lo exige explícito. 11 call sites | `mark_invalid` | `vantadb-wasm/src/lib.rs` (1 def + 1 write `:818` + 11 calls) |
| `TierPolicyConfig::policy` → `kind` | Stutter `PolicyConfig::policy`: el tipo ya dice Policy. Alternativa `heuristic` descartada en plan línea 31 (neutro gana). Sin lectores externos; `pub(crate)` blinda | `kind` | `src/lsm.rs` (decl `:223` + init `:236`) |
| `vanta_error_status` → `status` | Stutter `vanta_error_*`: el módulo `server::errors` + el parámetro `&Error` ya dicen vanta/error. Re-export `routing.rs:33`, 2 callers internos, 5 asserts test | `status` | `src/server/errors.rs` (def + 2 callers + 5 tests + doc `:34-38` si cita nombre) + `routing.rs:33` |
| `vanta_error_response` → `response` | Mismo stutter; el módulo ya califica. `handlers.rs` ×3 (`:190,:1030,:1060`) + re-export + 4 tests | `response` | `src/server/errors.rs` (def + 2 callers internos + 4 tests + docs `:141-149`) + `routing.rs:33` + `handlers.rs:14-17,:190,:1030,:1060` |
| `log_vanta_error` → `log_error` | Stutter en helper privado; módulo ya dice vanta. 2 callers + intra-doc `:119` | `log_error` | `src/server/errors.rs` (def `:83` + callers `:124,:151` + link `:119` + comentario `:81`) |

No-renames justificados por evidencia (no tocar): `with_prefetch_mode` (builder, no repite), `query_error_response`/`thread_not_found_response`/`panic_error_response`/`pool_error_response`/`not_found_response` (califican, no repiten — pre-mortem), `Error::*Error` (contrato cross-binding, plan línea 19), `EvictionPolicy`/doc `Collision policy` (otros tipos), `mark_dirty/mark_deleted` (ya cortos).

## Invariantes

1. Cero cambio de comportamiento: mismas firmas salvo identificador, mismos cuerpos, mismos status HTTP, mismos bodies JSON (`success/error/data/code` intactos)
2. `Error` enum y su `code()/is_retriable/recovery_hint` intocados (contrato cross-binding Python/WASM/TS)
3. `query_error_response` conserva nombre y shape (consumidores `handlers.rs:163`, tests FIND-55)
4. `TierPolicy::{SizeBased,FrequencyBased,AgeBased}` y semántica de promoción intactas; `LsmConfig.tier` y defaults numéricos intactos
5. `PersistCache` sigue privada; `mark_invalid` sigue privada (no cruza `wasm-bindgen`, no toca JS)
6. `PrefetchMode` semántica `Disabled=false, Auto|Enabled=true` intacta (PERF-04 default OFF se mantiene)
7. `ConnectionPool` semántica RAII/timeout intacta (límite, `PoolGuard` drop, `PoolError::{Closed,Timeout}`)
8. Docs históricos (`avance/`, tasks cerradas, `memory/decisions.md`) NO se reescriben (scope discipline)
9. Cada slice deja `cargo check -p vantadb --all-targets` verde; ningún slice >100 líneas de diff

## Recitation inicial

```text
=== RECITATION ===
Objetivo activo: STU-001 — Renames baratos Rust (pool/prefetch/cache/TierPolicy/server)
Estado: completed (S1-S6 ejecutados y verificados, commit creado, sin push)
Última acción: S6 — contrato plan verde (check + clippy -D warnings) + nextest 21/21 + fmt + commit único blast-radius
Resultado: ✅
State: ACCEPT (desde: VERIFY)
Próxima acción: ninguna en STU-001 — continuar con STU-002 (AsyncVantaDB→AsyncClient)
Contrato: cargo check -p vantadb --all-targets && cargo clippy -p vantadb --all-targets --all-features -- -D warnings ✅ (clippy full requerido -j 2 por OOM ambiental, ver Notas E1)
Invariantes: sin cambio de comportamiento; Error enum intacto; query_error_response intacto; TierPolicy semántica intacta; PersistCache privada; docs históricos no tocados — todos preservados
Comandos de verificación: check --features server ✅ · check wasm ✅ · clippy lib --all-features ✅ · clippy full -j 2 ✅ · nextest 21/21 ✅ · fmt ✅ · rg cero restos ✅
Deuda: ninguna — ver Deuda Regla 6 (ejecución)
Próxima tarea si completa: STU-002 — AsyncVantaDB→AsyncClient
last-synced: 2026-09-11T12:00Z
=== END RECITATION ===
```

## Deuda Regla 6

### Ejecución (2026-09-11T12:00Z) — sin deuda viva

- S1-S6 ejecutados y verificados; commit único blast-radius creado (ver S6); sin push (vía `vanta-lead`)
- Doc-comments con nombres viejos fuera de código (`ERROR_HANDLING.md`, `OBSERVABILITY.md`, `CRASH_MODEL.md`, `metrics/core/registry.rs:552-553`, `metrics/core/mod.rs:256`, tasks cerradas, `memory/decisions.md`): intactos a propósito (Regla 0.5) — candidata tarea docs separada, NO deuda de esta task
- Hallazgo E1 (gate): el `check` del contrato sin features NO compila código `cfg(feature="server")` (default no incluye `server`); la verificación real usó `--features server` + clippy `--all-features`. Propuesta: enmendar contrato del plan con `--features server` explícito
- Hallazgo E2 (ambiente): clippy full con paralelismo default colapsa por OOM en esta máquina (`STATUS_STACK_BUFFER_OVERRUN` + rmeta corrupta); con `-j 2` pasa limpio. No es deuda del cambio

### Discovery (histórico)

- Ejecución S1-S6 pendiente (este file es discovery; cero código modificado — verificado: ningún edit fuera de `docs/dev/tasks/STU-001.md`)
- `VERIFY_CONTRATO:no-corrido` — el contrato NO se ejecutó en discovery (discovery es read-only; lo corre el ejecutor tras cada slice)
- Doc-comments con nombres viejos fuera de código (`ERROR_HANDLING.md`, `OBSERVABILITY.md`, `CRASH_MODEL.md`, `STORAGE-TIERS.md`, tasks cerradas): se dejan a propósito; si el ejecutor quiere, `rg` post-rename los lista para una tarea docs separada — NO mezclar en estos slices (Regla 0.5)
- `TierPolicyConfig::kind` tiene riesgo residual 🟢×🟡 (nombre puede no convencer → alternativa `heuristic`, 1 línea, en review)

## DoD 3 niveles

### Nivel 1 — Slice (por cada S1-S6)

- [ ] Un rename (o grupo coherente) aplicado completo con sus callers/tests/doc-comments del slice
- [ ] `cargo check -p vantadb --all-targets` verde (y `cargo check -p vantadb-wasm` si S3)
- [ ] `rg` del nombre viejo en `src/ vantadb-wasm/src/` → 0 hits para ese símbolo
- [ ] Diff del slice ≤100 líneas, un commit atómico mensaje `refactor(stu-001): …`

### Nivel 2 — Task (STU-001 completa)

- [ ] Contrato verde: `cargo check -p vantadb --all-targets && cargo clippy -p vantadb --all-targets --all-features -- -D warnings`
- [ ] `cargo nextest run -p vantadb --lib connection_pool config lsm server::errors` verde (o filtro equivalente con evidencia; incluir métrica gated `prometheus` si aplica: `error_envelopes_increment_vantadb_errors_total_by_code`)
- [ ] `rg` global de los 7 nombres viejos en `src/ vantadb-wasm/src/` → 0 hits
- [ ] `cargo fmt --check` verde en archivos tocados
- [ ] Plan file Task 1 Estado → ✅ COMPLETED con branch/commit poblados

### Nivel 3 — Release (cierre plan)

- [ ] Lo consume STU-004 (verify final workspace + archivado + retrospectiva)
- [ ] Sin regresión en suite audit workspace (`cargo nextest run --profile audit --workspace --build-jobs 2` lo corre STU-004, no aquí)
- [ ] `skill progreso` registra STU-001 en `docs/dev/avance/`

## Herramientas + SDP

**Descubiertas (`campaign_discover_skills_v2`, phase BUILD, archivosClave `src/connection_pool.rs src/config.rs`, keywords rename/clippy/test, 8 devueltas, score 1.00):** `campaign-executor, source-driven-development, doubt-driven-development, incremental-implementation, test-driven-development, context-engineering, frontend-ui-engineering, api-and-interface-design`.

**Cargadas con `skill` (5/8, las aplicables a rename Rust; justificadas):**

- `incremental-implementation` — slices S1-S6 verticales ≤100 líneas, compilable tras cada uno
- `test-driven-development` — para rename mecánico el compilador + tests existentes son el RED (un resto no migrado = test rojo); verificación con comandos del stack descubiertos (nextest/check/clippy/fmt)
- `context-engineering` — hierarchy Rules→Plan→Source→Error aplicada en este discovery (ver Investigation Notes H1)
- `source-driven-development` — no hay API externa en juego; aplica como "verificar en Cargo.toml/fuente antes de asumir" (visibilidad `pub(crate)`, firmas `wasm-bindgen`)
- `doubt-driven-development` — dudas del plan ya resueltas con evidencia (`kind` vs `heuristic`, `is_enabled` YAGNI, `query_error_response` excluida)

**No cargadas (2/8, con motivo):** `frontend-ui-engineering` (cero UI en STU-001), `campaign-executor` (formato conocido sin cargar; el ejecutor la usa en runtime). `api-and-interface-design` descubierta pero no cargada vía `skill` (principios Hyrum aplicados inline: `status/response` son `pub` re-exportadas → rename directo solo porque censo prueba cero usuarios; si aparece un caller externo, STOP).

**Herramientas MCP/CLI por step:** `codegraph_explore` (blast radius ya hecho; re-usar si un slice toca `engine.rs/node.rs` — no es el caso), `campaign_verify_cmd` (contrato tras cada slice), `rg` (censos pre/post), `cargo check/clippy/fmt/nextest` (stack descubierto §3c).

## Investigation Notes

- **H1 — Context hierarchy (§3a) cargada en orden:** Rules (directivas vanta-worker del prompt + plan §Gate) → Spec/Plan (`docs/dev/plans/2026-09-11-stutter-interno.md` Task 1 `:37-70`, appetite/esfuerzo/prioridad/contrato/pre-mortem/uphill) → Source del slice (todos los reads listados en cobertura) + ejemplo patrón (rename mecánico idéntico en cada familia, sin ejemplo externo necesario) → Error previo (ninguno — discovery limpio). Trust: fuente/tests/tipos = trusted; docs históricos = referencia no-código; sin inputs untrusted
- **H2 — `pool_saturated` censo exacto:** def `src/connection_pool.rs:86`; doc `:5`; tests `:101,:106,:119,:126,:129,:131,:135` (7 asserts, 4 tests `test_respects_limit_and_releases_on_drop`, `test_acquire_times_out_when_saturated`, `test_saturation_signal_flips_with_usage`, `test_many_guards_drop_under_contention` que no lo usa pero convive). Grep repo: 11 hits totales, 9 en código (todos mismo archivo) + 2 docs ENT-04 (históricos, no tocar). `is_saturated` colisión: 0 hits repo. Archivo 159L, `pub struct ConnectionPool` (`:27`), callers estructurales `bootstrap.rs/state.rs` no usan el método
- **H3 — `is_prefetch_enabled` censo exacto:** def `src/config.rs:131` (doc `:130`); callers `src/index/graph/prefetch.rs:83` (rama `OnceLock`) y `:93` (rama env); tests `src/config.rs:1414-1416` (`test_prefetch_mode_is_enabled`). Grep: 7 hits (3 código + 3 tests + 1 plan). Estructura `PrefetchMode` `:107-117` (`Auto/Enabled/Disabled`, default `Disabled` PERF-04), `from_env_value` `:122`, `with_prefetch_mode` `:1129` (no tocar). `should_prefetch` `:81-98` con fallback env `VANTA_PREFETCH`/`VANTA_DISABLE_PREFETCH`
- **H4 — `cache_invalid`/`mark_cache_invalid` censo exacto (supera al plan):** campo `vantadb-wasm/src/lib.rs:416` (doc `:405-407`, init `:425`); método `mark_cache_invalid` `:813` (write interno `:818`); lecturas `:865,:869`; escrituras `:833,:843,:882`; call sites 11 (`:955 save-fail, :977 save_idb-fail, :1422 delete_by_filter, :1450 supersede, :1541 import_records, :1549 import_file, :1558 bulk_import, :1571 bulk_import_bytes, :1596 reindex, :1661 purge_expired, :1764 remove_edge`). Grep `cache_invalid`: 34 hits (21 código lib.rs + 13 docs/plan/tasks históricos). Grep `mark_cache_invalid`: 21 hits (11 código + 10 docs). Colisión `mark_invalid`/`invalid:` en wasm: el grep `mark_invalid|\.invalid\b|invalid:` solo devolvió las 2 líneas del propio campo (`:416,:425`) → destino libre. Struct `PersistCache` `:408` y método `:813` ambos privados (sin `pub`) → confinados a `lib.rs`
- **H5 — `TierPolicyConfig::policy` censo exacto:** decl `:223`, init `:236`, struct `:221`, `TierPolicy` enum `:207` (`SizeBased/FrequencyBased/AgeBased`), `LsmConfig.tier` `:257`, `Default` `:272`. Grep `\.policy\b` en `src/`: solo esas 2 líneas → cero lectores externos. `src/lib.rs:107` = `pub(crate) mod lsm` → riesgo cero externo aunque items sean `pub`. Consumidores de `lsm::` en storage (`pack_offset/unpack_offset/SegmentRegistry/SegmentLevel`) no tocan `TierPolicyConfig`. Docs `STORAGE-TIERS.md:33-62` describen `TierPolicy` (no el nombre del campo) → no requieren cambio
- **H6 — server/errors.rs censo exacto:** `vanta_error_status` def `:39` (doc `:34-38`), callers `:123,:150`, tests `:217,:225,:232,:236,:240`; `log_vanta_error` def `:83` (privada), callers `:124,:151`, doc-link `:119`, comentario `:81`; `vanta_error_response` def `:149` (doc `:141-148`), callers `:150-151` internos + `handlers.rs:190,:1030,:1060`, tests `:301,:333,:368,:401`; `query_error_response` `:122` + caller `handlers.rs:163` + tests `:289,:322,:356,:400` (NO se renombra). Re-export `routing.rs:31-34`, import `handlers.rs:14-17`. Grep 3 nombres: 96 hits, pero solo ~15 son código (resto docs/dev/tasks/metrics-docs). `pool_saturated`/`query_error_response` en `src/server/`: solo `query_error_response` (`handlers:163`, `routing:32` re-export) → pool no usado por server
- **H7 — Confusión resuelta (no adivinada):** plan cita `lib.rs:408,416,425,813,818,833,843,865` pero el censo real incluye además `:869,:882,:955,:977,:1422,:1450,:1541,:1549,:1558,:1571,:1596,:1661,:1764` → S3 cubre la lista completa (opción A: seguir spec + completar por evidencia). `TierPolicyConfig` es `pub` pero en `mod pub(crate)` → tratar como interno (verificado `lib.rs:107`), no como API pública. `vanta_error_*` son `pub` + re-exportadas pero censo prueba cero consumidores fuera de `server/` → rename directo sin deprecación (Hyrum considerado y descartado por evidencia)
- **H8 — Stack descubierto (§3c):** Rust workspace Cargo; focado `cargo check -p vantadb --all-targets` / `cargo nextest run -p vantadb --lib <filtro>`; suite `cargo nextest run --profile audit --workspace --build-jobs 2`; lint `cargo clippy --workspace --all-targets --all-features -- -D warnings`; fmt `cargo fmt --check`; wasm `cargo check -p vantadb-wasm` (S3). Contratos exactos del plan usados sin inventar

## Contadores uphill/downhill

- ⬆️ **Uphill (preguntas abiertas):** 0 — todas las decisiones tomadas (`kind` vs `heuristic` → `kind`; `is_enabled` YAGNI; `Error::*Error` fuera; `query_error_response` fuera). Riesgo residual 🟢×🟡 (`kind` puede no convencer → alternativa 1 línea) va a Review, no a uphill
- ⬇️ **Downhill (trabajo mecánico censado):** 6 renames / 8 símbolos en 5 archivos + 35 puntos de edición aprox: S1 9pts/1 arch · S2 6pts/2 arch · S3 ~21pts/1 arch · S4 2pts/1 arch · S5 ~15pts/3 arch · S6 verify 0pts. Compilador + rg como red de seguridad en cada slice

## SECURITY

- **Evaluación:** sin impacto. Renames de identificadores internos; cero cambio de firmas visibles en el wire (bodies JSON `success/error/data/code` intactos, status HTTP intactos vía mismo `match` en `status`), cero cambio de control de acceso (`RbacConfig`, `auth_middleware` no tocados), cero cambio de manejo de secretos (FIND-55 sanitización 5xx intacta — `status.is_server_error()` rama y `"internal error"` literales no se tocan), cero `unsafe` nuevo, cero FFI nuevo (`mark_invalid` sigue privada, no cruza `wasm-bindgen`)
- **Lo verificado:** `errors.rs:125-129,:152-156` (ramas 5xx genéricas) quedan byte-idénticas; `panic_error_response` y `pool_error_response` no se tocan; ningún slice toca `auth`/`jwt`/`rbac`/secretos. Tests de seguridad conductual (`five_xx_bodies_are_sanitized_to_generic_message`, `four_xx_bodies_keep_descriptive_message`, `error_envelopes_carry_canonical_code`) se re-ejecutan en S5/Nivel 2 como guardianes
- **Riesgo residual:** ninguno vivo (🟢×🟢). Si un rename rompe un import, el build falla cerrado (fail-closed), no degradación silenciosa

## PERFORMANCE

- **Evaluación:** sin impacto esperado (neutro). Renames monomórficos resueltos en compilación; cero cambio de hot paths (`should_prefetch` sigue `#[inline(always)]`, `persist_payload` mismo algoritmo O(changes), `ConnectionPool` mismo `Semaphore/AtomicUsize`, tier-promotion misma heurística). Binario y latencia idénticos
- **Lo verificado:** ningún cuerpo de función se modifica (solo identificadores); `PrefetchMode` default `Disabled` (PERF-04) intacto; `PersistCache` algoritmo diferencial intacto; sin nuevas alocaciones, locks ni syscalls
- **Riesgo residual:** ninguno. No se requiere `vanta-tuner` (pipeline §1a aplica a algoritmos/estructuras nuevas — esto es rename). Si el ejecutor detecta variación en benches, es ruido (re-correr)

## Steps atómicos

### S1: `pool_saturated` → `is_saturated`

- **Archivos:** `src/connection_pool.rs` (único archivo; 9 puntos)
- **Acción:** renombrar def `:86` + doc `:5` (`` `pool_saturated()` `` → `` `is_saturated()` ``) + 7 asserts tests (`:101,:106,:119,:126,:129,:131,:135`). Nada fuera del archivo (censo H2)
- **Verify:** `cargo check -p vantadb --all-targets` + `cargo nextest run -p vantadb --lib connection_pool` + `rg -n "pool_saturated" src/` → 0
- **Estado:** ✅ COMPLETED — `replaceAll` 9pts/1 arch; check ✅ (nota: sin `server` no compila este módulo — la prueba real fue con `--features server` en S6); nextest `connection_pool::` 4/4 ✅ en corrida S6; rg 0 ✅

### S2: `is_prefetch_enabled` → `is_enabled`

- **Archivos:** `src/config.rs` (def `:131` + tests `:1414-1416`), `src/index/graph/prefetch.rs` (callers `:83,:93`)
- **Acción:** renombrar def + 2 callers + 3 asserts. No tocar `with_prefetch_mode`, `from_env_value`, variantes ni `VANTA_PREFETCH`/`VANTA_DISABLE_PREFETCH`
- **Verify:** `cargo check -p vantadb --all-targets` + `cargo nextest run -p vantadb --lib config` + `rg -n "is_prefetch_enabled" src/` → 0
- **Estado:** ✅ COMPLETED — `replaceAll` en 2 archs (def + 2 callers + 3 tests); `test_prefetch_mode_is_enabled` 1/1 ✅ en corrida S6; rg 0 ✅

### S3: `cache_invalid` → `invalid` + `mark_cache_invalid` → `mark_invalid` (wasm)

- **Archivos:** `vantadb-wasm/src/lib.rs` (único archivo; ~21 puntos: campo `:416` + docs `:405-407` + writes `:425,:818,:833,:843,:882` + reads `:865,:869` + def `:813` + 11 calls `:955,:977,:1422,:1450,:1541,:1549,:1558,:1571,:1596,:1661,:1764`)
- **Acción:** renombrar campo (todas sus lecturas/escrituras) + método (def + 11 calls) + doc-comments `:405-407` + doc `:812`. No tocar `mark_dirty/mark_deleted`, `dirty/deleted/records`, ni JS (`opfs_bridge.js`)
- **Verify:** `cargo check -p vantadb-wasm --all-targets` + `cargo check -p vantadb --all-targets` + `rg -n "cache_invalid|mark_cache_invalid" vantadb-wasm/src/` → 0
- **Estado:** ✅ COMPLETED — un `replaceAll "cache_invalid"→"invalid"` cubrió campo + método (19 hits nuevos `invalid`/`mark_invalid` verificados); check wasm ✅; rg 0 ✅

### S4: `TierPolicyConfig::policy` → `kind`

- **Archivos:** `src/lsm.rs` (2 puntos: decl `:223` + init `:236`)
- **Acción:** renombrar campo + inicializador `Default`. Verificar cero `.policy` restantes con rg. No tocar `TierPolicy` enum, `LsmConfig`, thresholds ni `STORAGE-TIERS.md`
- **Verify:** `cargo check -p vantadb --all-targets` + `cargo nextest run -p vantadb --lib lsm` + `rg -n "\.policy\b" src/` → 0 (excluye `EvictionPolicy`/docs)
- **Estado:** ✅ COMPLETED — 2 edits quirúrgicos (`pub kind`, init `kind:`); lsm tests 4/4 ✅ en corrida S6; rg `\.policy` 0 ✅

### S5: `vanta_error_status` → `status`, `vanta_error_response` → `response`, `log_vanta_error` → `log_error`

- **Archivos:** `src/server/errors.rs` (3 defs + callers `:123-124,:150-151` + tests `:215-240,:301,:333,:368,:401` + doc-link `:119` + comentarios `:34-38,:81`), `src/server/routing.rs` (re-export `:31-34`), `src/server/handlers.rs` (import `:14-17` + calls `:190,:1030,:1060`)
- **Acción:** renombrar las 3 fns y todos sus callers/imports/re-exports/tests/doc-links del módulo. NO tocar `query_error_response` (`:122`, `handlers:163`), `panic_error_response`, `pool_error_response`, `not_found_response`, `thread_not_found_response`, ni bodies JSON
- **Verify:** `cargo check -p vantadb --all-targets --features server` + `cargo nextest run -p vantadb --lib server::errors` (incluye `vanta_error_status_maps_correctly`, `error_envelopes_carry_canonical_code`, `five_xx/four_xx`, métrica gated con `--features server,prometheus`) + `rg -n "vanta_error_status|vanta_error_response|log_vanta_error" src/` → 0
- **Estado:** ✅ COMPLETED — 3 `replaceAll` en errors.rs + re-export routing (orden alfabético) + import/callers handlers + 2 comentarios (`:171,:695`); `server::errors` 6/6 ✅ en corrida S6 (incluye `status_maps_correctly`); rg código 0 ✅ (restan solo menciones en docs históricos, fuera de scope)

### S6: Verify final + cierre STU-001

- **Archivos:** ninguno (solo comandos) + actualizar este file (steps → ✅) + plan file Task 1 (Estado/branch/commit)
- **Acción:** correr contrato + nextest módulo + rg global cero + `cargo fmt --check` en tocados. Commits atómicos por slice ya hechos en S1-S5 (`refactor(stu-001): …`). Push solo vía `vanta-lead` (no push directo)
- **Verify:** `cargo check -p vantadb --all-targets && cargo clippy -p vantadb --all-targets --all-features -- -D warnings` + `cargo nextest run -p vantadb --lib connection_pool config lsm server::errors` + `rg -n "pool_saturated|is_prefetch_enabled|cache_invalid|mark_cache_invalid|vanta_error_status|vanta_error_response|log_vanta_error" src/ vantadb-wasm/src/` → 0 + `cargo fmt --check`
- **Estado:** ✅ COMPLETED — check `--features server` ✅ · check wasm ✅ · clippy lib `--all-features` ✅ · clippy full `-j 2` ✅ (ver E2) · nextest 21/21 ✅ · fmt (tras `cargo fmt -p vantadb` por reflow de identificadores cortos) ✅ · rg global 0 ✅ · commit único creado, sin push

## Dependencias

- **Bloquea a:** STU-004 (verify final workspace + cierre) — STU-001 debe estar ✅ antes del gate workspace de STU-004
- **Independiente de:** STU-002 (Python `AsyncVantaDB`, otro árbol) y STU-003 (entity/scene serde, otro árbol) — pueden ejecutarse en paralelo sin conflicto de archivos (conjuntos disjuntos: STU-001 = pool/config/wasm-lsm-server; STU-002 = `vantadb-python/`; STU-003 = `src/entity/**`)
- **Pre-requisito:** ninguno (uphill 0). Asume `main` verde antes de S1 (si `cargo check` falla en S1 sin cambios propios, STOP y reportar)

## Review

- **Revisor propuesto:** `vanta-review` (second-opinion adversarial, P2-01: distinto del implementador `vanta-worker`)
- **Alcance del review:** (1) cada rename conserva semántica (diff solo-identificadores, cuerpos intactos); (2) censo completo — `rg` cero restos en `src/ vantadb-wasm/src/`; (3) `kind` vs `heuristic` — decisión final del nombre (risk 🟢×🟡 del plan); (4) no se coló scope (docs históricos intactos, `query_error_response` y `Error` intactos, sin `unsafe`/FFI nuevo); (5) contrato + nextest con evidencia. Sin `vanta-chaos` (sin `dashmap`/`parking_lot`/Tokio nuevo — Regla 8 no dispara; `Semaphore`/`Mutex` existentes intactos). Sin `vanta-audit` (sin FFI/unsafe). Sin `vanta-tuner` (rename, ver PERFORMANCE)

## Notas

- Pre-mortem del plan cubierto: (1) test olvidado → compilador + rg por slice; (2) `mark_cache_invalid→mark_invalid` incluido con censo de 11 (no 4); (3) `query_error_response`/`thread_not_found_response` excluidas con justificación
- `NOTICED BUT NOT TOUCHING:` doc-comments con nombres viejos en `docs/api/ERROR_HANDLING.md:377-378`, `docs/user/operations/OBSERVABILITY.md:71-72`, `docs/dev/wasm/CRASH_MODEL.md:10`, `src/metrics/core/{registry.rs:552-553,mod.rs:256}`, tasks cerradas → ¿crear tarea docs separada? (pregunta para el cierre, no scope de estos slices)
- `ponytail:` rename directo sin alias de compat — el censo prueba cero usuarios; alias sería scaffolding "for later"
- Iteraciones del plan (Task 1): el ejecutor las puebla (`| # | Acción | Resultado | Herramienta |`) a medida que completa S1-S6
- Cynefin 🟦 obvio; stop conditions N/A (mecánico); si cualquier slice revela un caller no censado → STOP, re-censar con `rg`, actualizar este file antes de seguir

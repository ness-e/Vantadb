# Task GH-141 — Webhook GitHub→Discord

## Contexto
`docs/discord/server-config.md` sección Integrations ya tenía la fila `GitHub Webhook | push, pull_request, issues, release | #📢announcements` pero sin tipos detallados ni cómo agregar ni dónde se configura. Issue #141 pedía documentar.

## Acción ejecutada (2026-08-05)
1. Leído `docs/discord/server-config.md` (Integrations ~L98-102).
2. Expandida la sección Integrations:
   - Tabla de 4 tipos de evento con trigger GitHub y canal destino:
     - `push` → pushes a cualquier rama → #announcements
     - `pull_request` → PR opened/closed/merged/reopened → #announcements
     - `issues` → issue opened/closed/reopened/labeled → #announcements
     - `release` → release published → #announcements
   - Procedimiento para añadir eventos: repo → Settings → Webhooks → Edit/Add webhook → "Let me select individual events" → check evento → Update webhook.
   - Dónde se configura: GitHub webhook **Payload URL = Discord channel webhook (`https://discord.com/api/webhooks/<id>/<token`); Discord side channel → Edit Channel → Integrations → Webhooks (Send Messages).
3. Verifiqué que NO existe `.github/workflows/` — el mecanismo es webhook nativo de repo, no una GitHub Action. Documenté el mecanismo real (no inventé workflow).
4. Cerrado issue #141 (`gh issue close 141 -r completed` con comentario de evidencia).

## Contrato cumplido
- Sección Integrations completa con los 4 tipos ✅
- `gh issue close 141` ejecutado ✅ (closed with reason completed)
- Staging selectivo solo `docs/discord/server-config.md` (más este task file)
- Commit: `docs(GH-141): documentar webhook GitHub→Discord (push, PR, issues, release)`

## Commit
- hash: ver `git log -1`
# C-02 — Docs por audiencia: micro-move solo sin dependencias

**Plan:** `docs/dev/plans/2026-09-20-estabilizacion-total.md` Fase 1 · **Estado:** ✅ COMPLETED (NO-MOVE justificado por evidencia)
**Branch:** develop · **Esfuerzo:** 🟢 · **Tipo:** docs (refactor formato-only, sin lógica)
**Contrato:** (a) movidos con `git mv` renderizan igual · (b) `markdownlint-cli2 "docs/**/*.md"` 0 issues · (c) cero links relativos rotos (`](docs/` + spot-check) · (d) CI docs verde en re-run (local-equivalente verde; re-run real delegado — NO PUSH por regla)
**SDP:** documentation-and-adrs (sugerida, cargada), writing-guidelines (tipo docs), writing-plans (base), campaign-executor/progreso (base auto) · keywords: [docs, markdownlint, links, frontmatter, reorganization] · `campaign_discover_skills_v2` devolvió 8 lifecycle genéricas (ftest/tdd/context/etc. — no aplican a docs-only, se registran y se descartan por ponytail) + `campaign_detect_task_type` → docs
**NextTask:** C-10 (orquestador)

## Spec (decisiones con evidencia)

| # | Decisión | Elección | Evidencia |
|---|----------|----------|-----------|
| 1 | Gate de move | MOVER solo archivos con CERO refs entrantes fuera de `docs/` (o actualizables aquí: `README.md`, `llms.txt`) | Contrato del task; PROHIBIDOS DUROS bloquean todo lo demás |
| 2 | `docs/user/` + `docs/dev/` del plan | NO crear (subset ∅ — crear dirs vacíos = scaffolding) | Ponytail: sin moves no hay estructura que sostener; plan C-02 original queda re-scoped (ver Deuda) |
| 3 | QUICKSTART / tutorials / FAQ / blog | NO-MOVE (todos tienen refs externas no-editables) | Tabla de abajo, greps `docs/tutorials`, `docs/blog`, `QUICKSTART`, `FAQ.md` repo-wide |
| 4 | Drafts blog huérfanos (ollama, claude) | NO-MOVE (coherencia canónica) | Cero refs externas, PERO `docs/user/blog/` es single source of truth (BLOG_SERIES_PLAN §4.3, master-index, book SUMMARY `blog/index.md`); partirlo fractura el canónico por 2 drafts |
| 5 | Workflows | NO tocar (nada que cambiar) | `gate-docs-21.yml` usa `docs/**` + `docs/**/*.md` — moves internos a `docs/` no lo afectan; sin BLOQUEO para C-04 |
| 6 | `campaign_verify_cmd` | Bash directa + mención | BUG conocido exit -1 (precedente FIND-105/AST-009); verificado vía `npx`/`rg`/`pwsh` con outputs pegados |

## Impacto mapeado (Regla 0)

- **Leídos completos:** plan C-02 (líneas 44-49), `README.md` §Quick Links/Documentation (60-67, 348-350), `llms.txt` (7-10), `dev-tools/validate_doc_snippets.py` (26), `gate-docs-21.yml` (jobs lint/frontmatter/api-version), `docs/README.md`, `docs/book/src/SUMMARY.md` (6-14, 62), `docs/dev/tasks/BLOG-CTA.md` (formato vecino).
- **Referencias hacia dentro:** ninguna edición a archivos existentes — único archivo nuevo: este task file. `git mv`: 0 invocaciones.
- **Referencias entrantes al cambio:** el plan C-02 y C-10 (README post-move) — C-10 no se adelanta; queda para orquestador con este mapa como input.
- **Veredicto:** blast radius = 1 archivo nuevo (`docs/dev/tasks/C-02.md`); 0 archivos modificados; 0 links tocados; reversible con `git revert`. Riesgo nulo.

## Mapa de referencias por archivo (discovery)

Leyenda: ❌ externa no-editable (bloquea) · ✅ editable aquí (`README.md`, `llms.txt`) · 🏠 interna `docs/` (no bloquea per se) · 🔒 PROHIBIDO DURO (no mover, no editar).

| Archivo | Entrantes fuera de `docs/` | Entrantes dentro de `docs/` | Salientes notables | Decisión |
|---|---|---|---|---|
| `docs/user/QUICKSTART.md` | ❌ `SUPPORT.md:12-13`, `README_ES.md:43,260`, `examples/README.md:5` (`../docs/`), `dev-tools/validate_doc_snippets.py:26` (glob+path hardcodeado), `vantadb-python/README.md`, `vantadb-wasm/README.md:278`, `skills/vantadb/SKILL.md:634,791` · ✅ `README.md:60`, `llms.txt:7` | 🏠 book stub `{{#include}}` + SUMMARY:6, `docs/README.md:42`, master-index:346, DISTRIBUTION:28-29, FASE-A, learning, tasks/plans históricos | → `docs/api/EMBEDDINGS.md`, `docs/user/tutorials/05-…` | **NO-MOVE** (7 superficies externas no-editables) |
| `docs/user/tutorials/` (8 files) | ❌ `README_ES.md:47` + `README.md:64` (dir), `docker-compose.yml:10` (02), `embeddings/README.md:40,127` (05), `vantadb-python/tests/test_sdk.py` (02, comentario), `dev-tools/*.py:26` (glob dir — cubre los 8), 🔒 `web/…vanta-data.ts:479` (comentario) | 🏠 book SUMMARY:8-14, `docs/README.md:35`, api/EMBEDDINGS, operations/EXPERIMENTAL, glosario/lancedb, strategy/GO_TO_MARKET, GOV-A4/GH-119 | → api/EMBEDDINGS, operations, entre sí (index track) | **NO-MOVE** (harness+compose+embeds+web) |
| `docs/user/FAQ.md` | ❌ `SUPPORT.md:13`, `README_ES.md:48` · ✅ `README.md:66` | 🏠 book stub + SUMMARY:7, master-index:347, research-archive | — (standalone) | **NO-MOVE** (SUPPORT + README_ES no-editables aquí) |
| `docs/user/blog/` (9 files) | ❌ `README_ES.md:49,402-404` (dir+3 posts), 🔒 `web/` ×7 (`benchmarks_vs_lancedb_chroma` en benchmarks-view/competitive-table/vanta-data + slug `introducing-vantadb` en vanta-data/sitemap/dictionaries/use-vanta-navigate), `benchmarks/graphrag_bench.rs:29` (graphrag) · ✅ `README.md:67,348-350`, dir+[3 posts] | 🏠 book stubs (3) + SUMMARY:62, master-index (blog + campaign), strategy/BLOG_SERIES_PLAN (canónico), tasks BLOG-CTA/MKT-*/INV-006 | → benchmarks, api, entre posts | **NO-MOVE** (web 🔒 + README_ES + benchmarks) |
| `ollama_…md`, `claude_…md` (drafts) | CERO (0 hits fuera de `docs/`) | Solo menciones textuales con backticks en `BLOG-CTA.md` + plans/archive (historia inmutable — stale aceptable, no es link) | — | **NO-MOVE** (coherencia: partir `docs/user/blog/` canónico por 2 drafts empeora la org.) |
| `campaign-ai-agent-memory.md` | CERO fuera de `docs/` | 🔗 link real `master-index.md:184` (`blog/campaign-…`) + backticks en MKT-10/BLOG-CTA/plans-archive | cita números sqlite (doc interna) | **NO-MOVE** (moverlo exige editar master-index — fuera del scope declarado README+llms) |
| Otros (`COMPARISON`, `DISTRIBUTION`, `chaos-testing`, `ci-cd-guide`, …) | — | — | — | **Fuera de scope** ("lo demás se queda"; no son candidatos del task) |

Conclusión: **subset movido = ∅**. Ningún candidato pasa el gate sin (i) editar archivos prohibidos/no-alcance o (ii) fracturar el canónico `docs/user/blog/`. Mover 2 drafts huérfanos cumpliría la letra y rompería el espíritu (peor organización que el status quo).

## Pasos

- [x] 1. Discovery: inventario `docs/` + greps repo-wide + tabla + decisiones move/no-move
- [x] 2. Verify (b): `npx markdownlint-cli2 "docs/**/*.md"` → 0 issues (1433 files)
- [x] 3. Verify (c): `](docs/` 49 links únicos → 0 rotos; `](../../docs/` → 0 rotos; spot-check 4/4 existen
- [x] 4. Verify extra (gate-docs): frontmatter `title:` → 0 faltantes
- [x] 5. Task file + commit selectivo (NO PUSH)
- [ ] 6. CI docs re-run → DELEGADO (requiere push; regla NO PUSH) — C-10/orquestador lo corre en el próximo push a develop

## Verify (2026-09-20, bash directa — `campaign_verify_cmd` BUG exit -1, precedente FIND-105)

- `npx --yes markdownlint-cli2 "docs/**/*.md"` → `Summary: 0 issues in 0 files` (1433 files, excluye archives por config) ✅
- Link audit: `rg -o "\]\(docs/[^)]+\)" --glob '*.md'` → 49 únicos, `Test-Path docs/<t>` → **0 missing** ✅ (nota: primer script testeó sin prefijo `docs/` y dio falsos MISSING — corregido y re-verificado a 0)
- Parent-links `](../../docs/…)` (examples, benchmarks, desktop, vantadb-*) → **0 missing** ✅
- Spot-check: `docs/user/QUICKSTART.md`, `docs/user/FAQ.md`, `docs/user/tutorials/index.md`, `docs/user/blog/how_hybrid_search_works.md` → 4/4 True ✅
- Frontmatter: md con `---` inicial sin `^title:` en `docs/` → **0** ✅
- `git status --short` pre-commit: solo `?? docs/dev/tasks/C-02.md` (más WIP ajeno pre-existente intacto — ver abajo) ✅

## Deuda / NO tocado (scope discipline)

- **WIP ajeno intocable** (visto en `git log/status`, NO stageado): verificar en commit que solo va `docs/dev/tasks/C-02.md`.
- Plan C-02 original (plan file líneas 44-49: `docs/user/` + `docs/dev/` con api/operations/plans/tasks/avance) queda **re-scoped por este task del owner** (micro-move solo-sin-dependencias). Recomendación al orquestador/C-10: o bien (A) cerrar C-02 como NO-APPLICA con este mapa como evidencia y reescribir C-02 como wave con scope ampliado (README_ES, SUPPORT, examples, dev-tools, skills, embeddings, web — casi todo prohibido hoy), o (B) mantener `docs/` plano. NO reintentar micro-moves: el resultado será el mismo ∅.
- Sin BLOQUEO para C-04 (workflows usan `docs/**`; nada que cambiar).
- `skill progreso`: N/A (C-02 no es fila de Backlog; nada que migrar a `docs/dev/avance/`).
- Colaterales NOTICED (no FIND, no scope-creep): ninguno nuevo — el mapa ya documenta todos los stales conocidos.

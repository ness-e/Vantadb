# CODEX-130-131-132 — Codex Review P1/P1/P2 (recall-L1 + compose-auth + L0-mismo-ms)

> Plan: `docs/dev/plans/2026-09-19-cierre-total.md` · Wave A · Commit `fix: CODEX-130/131/132 — ...` (selectivo, NO PUSH)
> Tipo: bug-fix (MCP server) · Skills: SDP abajo · NextTask: Wave B (orquestador)

## Contrato

- (a) `memory_recall` ve L1 del pipeline (test que lo prueba).
- (b) `docker compose up -d` arranca con auth documentada o perfil loopback por defecto (healthcheck pasa). Verificación estática: `docker compose config` (NO arrancar contenedores con datos vivos).
- (c) 2× `/conversation/add` mismo ms → 0 mensajes perdidos (test concurrente).
- Tests del scope verdes. `cargo test -j 2` del scope. Secrets nunca a disco/código/logs. WIP ajeno intocable.

## SDP

`campaign_discover_skills_v2` phase=BUILD keywords=[recall,L1,session,docker-compose,auth,concurrency,L0,conversation] →
`campaign-executor, source-driven-development, security-and-hardening, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, frontend-ui-engineering`.
Cargadas: systematic-debugging · test-driven-development · security-and-hardening · incremental-implementation · context-engineering · source-driven-development.
Descartada: frontend-ui-engineering (web/ prohibido en esta tarea). doubt-driven-development: consulta puntual en slice 131 (auth) sin carga completa.
Refs leídas COMPLETAS: `.opencode/rules/core-engine.md`, `definition-of-done.md`, `dev-tools.md`, `test-suite.md`, clean-code Apéndice V (mapa VantaDB). Notion: `Problema` leída completa (gobernanza ciclo de vida — respalda recall/L0); Propuesta/Plan/Nuevas-features pendientes (deuda menor: bugfix mecánico, contrato autosuficiente).

## Impacto mapeado (Regla 0)

Archivos leídos completos (o rangos verificados):
- `vantadb-mcp/src/handlers/tools.rs:1680-1799` (brazo `memory_recall`; session_key sintética `"mcp"` + `ProfileIsolation::default()`).
- `vantadb-server/docker-compose.yml` (completo, 93L), `docker-compose.prod.yml` (completo), raíz `docker-compose.yml` (precedente demo sin auth).
- `vanta-memory/src/services/conversation_hook.rs` (completo, 107L; trigger id None + `now_ms()`).
- `vanta-memory/src/core/conversation/l0_recorder.rs:100-279` (`record_turn`, filtro `<=`, key `t{ts}_{idx}`, cursor).
- `vanta-memory/src/core/hooks/auto_recall.rs:90-315` (`read_scoped_records` filtra `agent_id`/`team_id`; `None` invisible cross-session por D22).
- `vanta-memory/src/core/record/l1_writer.rs:1-279` (`write_memory` Store + Update/Merge escriben `team/agent None`).
- `vanta-memory/src/core/profile/profile_sync.rs:18-37` (defaults `"default"`/`"default"`).
- `vanta-memory/tests/recall.rs:360-599` (D22; test `:514` fija None=invisible — NO romper), `tests/l0_capture.rs` (completo), `tests/conversation_hook.rs` (completo).
- `src/server/bootstrap.rs:20-89` (`validate_auth_config` fail-closed), `src/server/middleware.rs:42-46,71-78,106-119` (`/health` público; `Bearer ` vacío pasaría contra key `""` → footgun documentado), `src/server/router.rs:149` (`/health` en router público), `src/config.rs:1072-1092` (`API_KEY` sin filtro de vacío).

Referencias hacia dentro (lo que cada fix toca):
- 130: `write_memory` ← `apply_dedup_batch` ← `run_l1_dedup` ← `pipeline_worker::MemoryTaskHandler`; `perform_auto_recall` ← tools.rs + context.rs + proxy + pipeline_worker.
- 131: solo YAML + docs en comentarios; `validate_auth_config` NO se toca.
- 132: `record_turn` ← `auto_capture.rs` + `conversation_hook.rs` (bridge). `db.get` ya usado en `read_cursor` (mismo handle, sin lock nuevo).

Referencias entrantes (quién depende):
- 130: tests `recall.rs`, `semantic_recall.rs`, `e2e_flow.rs` (leen L1 con defaults; own-session unaffected).
- 131: `docker-compose.prod.yml` (ya documenta `VANTADB_API_KEY` + `REQUIRE_AUTH=true`; dev hereda healthcheck).
- 132: tests `l0_capture.rs` (idempotencia + cursor + floor), `e2e_flow.rs`, `pipeline_manager.rs`.

Veredicto: blast ≤10 archivos, sin hot paths vectoriales, sin símbolos `pub` nuevos, sin cambios de dependencias. Gates P/D no disparados (P: owner aprobó plan 2026-09-19; D: sin API pública nueva, contrato mecánico, blast acotado). FASE SECURITY aplica a 131 (auth) — checklist aplicada en diseño (footgun `Some("")` evitado, fail-closed preservado, sin secretos). FASE PERFORMANCE no aplica (tie-break añade 1 `get` solo en colisión de ms; recall sin cambio de complejidad).

## Diseño (zero-code planning)

- Slice 130 — alinear IDs en writer (opción Codex ii): `write_memory` Store + Update/Merge stampan `team_id`/`agent_id = "default"` vía `ProfileIsolation::default()` (sin magic strings). Reader D22 intacto + test `:514` intacto. MCP recall (session `mcp` + isolation default + scope Agent) ahora ve L1 del pipeline. Nota: deployments con team/agent custom no ven estos records cross-session (documentado en test); own-session siempre visible.
- Slice 131 — loopback por defecto + auth documentada: `VANTADB_HOST=${VANTADB_HOST:-127.0.0.1}` en ambos servicios, bloque auth comentado (qué exportar para LAN, puntero a prod override), header usage. Sin `API_KEY` activa con default vacío (footgun verificado). `/health` público → healthcheck pasa con o sin auth.
- Slice 132 — desempate cursor+secuencia: `ts < cursor` → skip; `ts == cursor` con cursor persistido → `get(key)`: mismo contenido → skip (replay), distinto/ausente → aceptar (probe `#n` acotado); `ts == cursor` sin cursor (floor) → skip legacy; `ts > cursor` → path actual.

## Steps

- [x] Slice 130 (P1 recall-L1): RED test recall params-mcp ve L1 de `write_memory` → GREEN writer defaults → `cargo test -j 2 -p vanta-memory --test recall` + e2e scope verde.
- [x] Slice 131 (P1 compose-auth): editar yml (2 servicios + header) → `docker compose config` renderiza loopback + auth documentada + healthcheck presente.
- [x] Slice 132 (P2 L0-mismo-ms): RED test determinista mismo-ms → GREEN tie-break → test concurrente bridge → `cargo test -j 2 -p vanta-memory` scope verde.
- [x] Cierre: `campaign_verify_cmd` (clippy+fmt+nextest+deny del scope; bug exit -1 → bash+mención) + commit selectivo NO PUSH + RESULTADO §7 + Gates D/V/C.

## AC por slice (verificación)

- 130: test nuevo `mcp_synthetic_session_sees_pipeline_l1_under_default_agent_scope` en `vanta-memory/tests/recall.rs` (falla antes, pasa después) + suite recall/e2e/semantic sin regresiones.
- 131: `docker compose -f vantadb-server/docker-compose.yml config` OK; `VANTADB_HOST` default `127.0.0.1`; bloque auth documentado sin secretos; `healthcheck:` presente con `/health` (público verificado).
- 132: test determinista `same_ms_second_message_is_not_lost` en `tests/l0_capture.rs` + test concurrente `concurrent_same_ms_triggers_lose_no_messages` en `tests/conversation_hook.rs`; suites l0/conversation/e2e verdes; tests previos de idempotencia/cursor/floor intactos.

## Deuda / colaterales

- Notion Propuesta/Plan/Nuevas-features sin leer (bugfix mecánico).
- Con loopback en contenedor, el mapeo host→contenedor requiere override documentado para acceso LAN (nota en yml).
- Deuda preexistente tocada de refilón (NO tocar): `read_scoped_records` full-scan O(sesiones+records) con `ponytail:` ya marcado.

## Review P2-01 (transcripción dictamen revisor distinto, 2026-09-19 — VEREDICTO: approve)
- Sin sección Review propia en el file (hallazgo Baja del revisor — subsanado aquí).
- Revisor verificó: `5b871993` stat/diff por slice; `l1_writer.rs:103` tenancy default + D22 intacto; compose sin secretos (solo comentados/placeholder); `l0_recorder.rs:96` key FNV + tie-break + tests concurrentes; cero `unwrap/expect/unsafe` nuevos en prod; 12 files sin prohibidos.

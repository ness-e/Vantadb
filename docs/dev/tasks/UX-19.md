# UX-19 — Smoke manual → spec Playwright permanente

> **Plan:** `docs/dev/plans/2026-09-07-followup-bench-a11y.md` (Task 3, Wave0)
> **Estado:** ⏳ IN PROGRESS
> **Tipo:** test-guard E2E (no feature-add greenfield → sin SPEC greenfield; contrato = plan)
> **SDP:** campaign-executor, incremental-implementation, test-driven-development,
> context-engineering, frontend-ui-engineering (+ ponytail full siempre activo).
> Evaluados y no cargados (ponytail: contexto mínimo): source-driven-development
> (config ya cita playwright.dev/docs/test-webserver), doubt-driven-development
> (no trust boundary), api-and-interface-design (sin API nueva), spec-driven-development,
> interview-me (sin ambigüedad de requisito tras DISCOVERY).

## Contrato (del plan — ley)

- Nuevo `desktop/e2e/smoke-critico.spec.ts` verde
  (`npx playwright test smoke-critico` passed) + suite e2e existente intacta
  (flujo-critico + daud01-temas + multi-perfil + proxy-dashboard verdes).

## Spec (decisiones — Gate mecánico spec-first N/A: no hay lógica nueva)

| Decisión | Evidencia | Veredicto |
|----------|-----------|-----------|
| ¿Existe ya el guard del flujo crítico? | `desktop/e2e/flujo-critico.spec.ts:1-7` header "E2E-VISUAL (UX-19): guard permanente del flujo crítico" + commit `480935a7` "Playwright smoke guard (UX-19)" | SÍ — seguir con verificación, no duplicar |
| ¿Cubre el flujo del contrato? | Steps 1-9: ingest UI → grid → Inspector Enter → borrado 2 pasos → papelera → RESTORE → paleta Ctrl+K → AJUSTES | SÍ, 1:1 |
| Nombre `smoke-critico` vs `flujo-critico` | Plan pide `smoke-critico.spec.ts`; repo tiene `flujo-critico.spec.ts` (misma sustancia) | Gate D documentado abajo; NO duplicar archivo |

## Gate D (contrato ambiguo — sin question tool disponible en este runner)

> Divergencia forma vs sustancia: el plan (2026-09-07) afirma "el smoke
> no tiene spec" pero `flujo-critico.spec.ts` (2026-08-25, header UX-19)
> cubre exactamente ingest→teclado→borrar→papelera→restore→paleta.
> Decisión sustancia-sobre-forma (ponytail + Rule 0.5 scope discipline):
> verificar suite verde; NO crear archivo duplicado. Si el owner exige el
> nombre literal, renombrar en 1 commit (no duplicar).

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** `desktop/e2e/flujo-critico.spec.ts` (115L),
  `helpers.ts` (46L), `serve.mjs` (85L), `playwright.config.ts` (39L),
  `SMOKE-MANUAL.md` (83L — GRAFO/ESPACIO son smoke VISUAL manual, fuera de scope).
- **Referencias hacia dentro (el spec depende de):** `helpers.ts`
  (`APP_BASE`, `seedRecords`), `serve.mjs` (webServer: rebuild dist-web +
  `vanta-cli server -p 8091 --dashboard-dir dist-web`), `vanta-cli` binario
  (debug+release existen), `desktop/dist-web/` (index.html 30/08 — rebuild
  determinístico por run salvo `E2E_SKIP_BUILD=1`).
- **Referencias entrantes:** `playwright.config.ts` (`testMatch: /\.spec\.ts$/`
  — recoge todo spec sin lista explícita); ninguna otra.
- **Veredicto:** blast radius = solo `desktop/e2e/` + `desktop/dist-web/`
  (generado). Edición prevista: NINGUNA (solo corridas). Riesgo de romper
  otras tasks: nulo (Wave0: no tocar archivos de FIND-BND12-01/PERF-BENCH-01;
  plan file intocable).

## Steps

| # | Step | Contrato verify | Estado |
|---|------|-----------------|--------|
| 1 | Correr spec flujo-crítico (sustancia `smoke-critico`) verde | `npx playwright test flujo-critico` passed | ✅ PASS (5.1s) |
| 2 | Suite e2e completa intacta (4 specs) | `npx playwright test` 4 passed | ⬜ 11/12 — falla `multi-perfil:29` (locator `.last()` atrapa select "Modelo de embedding" añadido tras el test; fix: usar locator `selectMode` ya preparado) |
| 2b | Fix locator + re-correr `multi-perfil` | `npx playwright test multi-perfil` passed | ✅ 4/4 (12.5s) |
| 2c | Corrida final full suite | `npx playwright test` 12 passed | ✅ 12/12 (36.1s) |
| 3 | Cierre: recitation + RESULTADO, SIN commit (orden runner) | task file sync | ✅ |

## Iteraciones

| # | Acción | Resultado | Herramienta |
|---|--------|-----------|-------------|
| 1 | DISCOVERY: leídos 5 archivos e2e + git log (480935a7) + binarios + SDP v2 | Guard ya existe; steps 1-2 pendientes de corrida | Read/bash |
| 2 | Fix `multi-perfil:51` locator `.last()` → `selectMode` + full suite 12/12 | 12 passed (36.1s), sin cambios en app | edit+bash |

# AST-012: Subclientes Python sin apellido + plano limpio

## Metadata

- **Plan file:** `docs/dev/plans/2026-09-11-python-subclients.md`
- **Fuente:** plan file Task 1 (auditoría 7 remanentes ítem 2, hazard flat + decisión usuario GO + SDKB-03 existente)
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🔴
- **Tipo:** Python (binding PyO3 + wrapper puro + tests + docs)
- **Turns estimados:** 15-30
- **Creado:** 2026-09-11T12:00
- **last-synced:** 2026-09-11T13:30
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps (9/9 ✅)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-python/tests/*` (test_sdk 46 sitios, test_subclients 12, async_smoke 7, migration 2, close_concurrency 1, perf 2), `verify_published_wheel.py` (3), `AsyncVantaDB` (`__init__.py`), integrations/* (71 sitios, DEUDA lead — precedente AST-008), `examples/demo`, `providers/ollama/tests` (DEUDA lead) |
| Callees | `vantadb-python/src/lib.rs` (Client flat + MemoryClient + macro `forward_to_db!`), engine `Embedded::{get,delete,list}` (solo re-uso, sin cambios), `types.rs` (sin cambios) |
| Implicaciones | contrato público Python cambia (breaking directo, sin aliases — precedente AST-008/AST-010); performance idéntica (mismas llamadas engine, un hop Python menos en subcliente); sin migración de datos; tests existentes se migran en el mismo PR; `.pyi` se actualiza en el mismo PR (stub-drift gate) |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-python/src/lib.rs` (§225-392 structs+macro+forwards; §899-947 bodies get/delete; §1132-1169 body list; §2207-2245 getters), `vantadb-python/vantadb_py/__init__.py` (502L, AsyncVantaDB §148-501), `vantadb-python/tests/test_subclients.py` (324L), `vantadb-python/vantadb_py/vantadb_py.pyi` (525L), `vantadb-python/vantadb_py/__init__.pyi` (266L), `vantadb-python/tests/test_stub_drift.py` (335L, genérico — sin nombres hardcodeados), `vantadb-python/verify_published_wheel.py`, `vantadb-python/tests/conftest.py` (sin stutter), `docs/api/BINDINGS_NAMESPACES.md` (§Python + §Sub-Client), `docs/api/PYTHON_SDK.md` (37 hits, ya drifted post-AST-008: documenta `search_memory` inexistente), `docs/dev/architecture/adr/041_anti_stutter.md` (stutter de prefijo Vanta — distinto eje, solo contexto), `vantadb-ts/src/vantadb.ts` (MemoryClient modelo: put/get/delete/list/search delegando a flat)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `lib.rs` → engine `Embedded` (`self.engine.clone()` + `enter(&self.op_gate)` + `map_vanta_error` + `VantaPyMemoryRecord`/`VantaPyListResult` + `py_dict_to_metadata` — mismo módulo, acceso directo); `__init__.py` → `from .vantadb_py import Client,...` (nativo); tests → `import vantadb_py as vanta` (resuelve a `vantadb-python/` vía sys.path, `.pyd` local del 11/9)
- **Archivos que referencian a los editados (referencias entrantes):** flat `get_memory/list_memory/delete_memory`: tests/* (~70), verify_wheel (3), AsyncVantaDB (3+docstrings), integrations/* (71 — DEUDA), smoke_test_extended.py (ya roto post-AST-008 — DEUDA), README/docs (DEUDA salvo los 2 .md en scope); subcliente stuttered: test_subclients (4) + forward-macro; `search_memory`: NO existe en nativo (verificado `fn search_memory` → 0 hits en src) — solo docs/integrations/smoke (drift AST-008)
- **Veredicto impacto:** medio — rename breaking confinado al binding Python con precedente directo (AST-008 hizo lo mismo con `search_memory→search`, 19 archivos, Gate C deuda lead para integrations/docs). Sin cambios de engine/storage/concurrencia: deuda P2 y Regla 8 no disparan.

## Contrato

`python -m pytest vantadb-python/tests/test_subclients.py -q` verde + `rg -n "get_memory|list_memory|delete_memory|search_memory" vantadb-python/vantadb_py/__init__.py` → 0 (runner: system python 3.14.7 + pytest 9.1.1 desde repo root; `import vantadb_py` resuelve local con `.pyd` rebuilt vía `maturin develop -m vantadb-python/pyproject.toml`).

## Spec (SDD — señales de símbolos públicos: AsyncMemoryClient nuevo en wrapper puro)

| # | Decisión | Opciones (+tradeoff) | Default recomendado | Resuelto |
|---|----------|----------------------|--------------------|----------|
| 1 | Dónde vive la implementación de get/list/delete tras eliminar flat `*_memory` | A) cuerpos reales en `impl MemoryClient` (mismo módulo, `self.db` borrow + `enter` + `detach` idénticos) / B) fns libres + ambos delegan (más piezas, mismo efecto) | A | ✅ decidido-por-evidencia (macro ya documenta single-source-of-truth en flat; mover el cuerpo al subcliente deja 1 implementación sin duplicar; B agrega indirección sin beneficio) |
| 2 | Qué reemplaza `AsyncVantaDB.get_memory/list_memory/delete_memory` (contrato rg→0 + colisión con `get/delete` nodo) | A) `AsyncMemoryClient` (get/list/delete → `to_thread(sync.memory.*)`) / B) nada (pérdida de comportamiento, rompe test_sdk async) / C) renombrar a get/list/delete (colisión real con nodo — prohibido por hazard) | A | ✅ decidido-por-evidencia (B rompe DoD test_sdk verde; C colisiona — verificado `async def get/delete` nodo en `__init__.py:379-383`; solo A preserva comportamiento sin colisión) |
| 3 | Alcance de `AsyncMemoryClient` | A) solo get/list/delete (set en colisión) / B) paridad total con MemoryClient (17 métodos) | A | ✅ decidido-por-evidencia (resto de ops async no colisionan y siguen planas; B duplica superficie sin requerimiento; scope discipline) |
| 4 | `AsyncVantaDB.list` (conveniencia → list_memory) | A) eliminar (alias, 0 usuarios en tests) / B) conservar | A | ✅ decidido-por-evidencia (`rg db\.list\(|await db\.list\(` en tests → 0 hits; "Sin aliases") |
| 5 | integrations/* + README + GRAPH_RAG + smoke con nombres viejos | A) migrar en este PR / B) deuda lead (precedente AST-008 Gate C) | B | ✅ decidido-por-evidencia (commit AST-008 `0f36fc24`: "Gate C: integrations/examples/docs con nombres viejos (deuda lead)"; 71 sitios exceden slice ≤100L y ARCHIVOS del task) |

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** `get/delete` planos siguen siendo nodo-u128 (hazard BINDINGS_NAMESPACES); shared-names planos (`put/search/count/list_namespaces/...`) intactos con misma firma; subcliente delega con firma idéntica (forward verbatim o cuerpo movido verbatim — misma validación, mismo resultado); sin `unwrap/expect` nuevo; sin `unsafe` nuevo; sin aliases residuales (ni `X => Y` en macro ni conveniencias planas); engine/storage sin tocar.
- **Comandos de verificación:** `python -m pytest vantadb-python/tests/test_subclients.py -q` + `python -m pytest vantadb-python/tests/test_sdk.py -q` + `python -m pytest vantadb-python/tests/test_stub_drift.py -q` + `rg -n "get_memory|list_memory|delete_memory|search_memory" vantadb-python/vantadb_py/__init__.py` (→0) + `cargo fmt --check -p vantadb_py` + `cargo clippy -p vantadb_py --all-targets -- -D warnings`
- **Deuda pendiente:** integrations/* (71 sitios incl. `search_memory` ya roto post-AST-008) + `smoke_test_extended.py` + `vantadb-python/README.md` + `GRAPH_RAG.md`/`scores.md` refs + `examples/` + `providers/ollama/tests` → Gate C deuda lead (precedente AST-008). `MCP.md search_memory` = tools MCP (superficie distinta, NO tocar).

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | Encabezado `# AST-012` |
| `lastAction` | Último step ✅ + Context Save Point |
| `result` | `OK` ↔ ✅ · `PARTIAL` ↔ ⏳ con steps pendientes · `FAILED` ↔ ❌ |
| `nextAction` | Próximo step ⬜ PENDING (archivo + comando) |
| `contract` | `## Contrato` + `## Invariantes` + evidencia infra |
| `nextTask` | ninguna (cierra excepción) |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda nueva (rename directo elimina 3 métodos planos + 3 forwards + 3 aliases + 4 conveniencias async-alias; único agregado: `AsyncMemoryClient` 3 métodos + property — exigido por contrato rg + preservación de comportamiento, sin techo conocido). Deuda preexistente que se SALDA: drift `PYTHON_SDK.md` (`search_memory` inexistente documentado como canónico). Deuda que se REGISTRA (Gate C, precedente AST-008): integrations/docs viejos → fila lead.

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato (§Contrato) + `test_sdk.py` verde + `test_stub_drift.py` verde + `rg` nativo `fn get_memory|fn list_memory|fn delete_memory` en `src/` → 0 + `rg` subcliente stuttered → 0 |
| **Commit** | Atómico `refactor!:` (estilo AST-008/AST-010), solo archivos del blast radius declarado, `cargo fmt --check` + `clippy -D warnings` verdes |
| **Release** | N/A (sin push por instrucción; release vía vanta-lead). Pre-push gate: `cargo nextest` workspace excede budget de esta invocación — se corre suite Python + clippy/fmt como gate local; nextest completo lo corre el lead/CI |

## Herramientas necesarias

- codegraph_explore + codebase-memory trace_path/coverage (blast radius) ✅ usadas en discovery
- maturin develop (rebuild), python -m pytest (runner sistema), rg (contrato), cargo fmt/clippy (gate Rust)
- campaign_verify_cmd si acepta comandos libres; si rechaza por ambigüedad → bash con evidencia + lesson

**Skills cargadas (SDP):** source-driven-development (versiones/pyo3 desde Cargo.toml, patrones del repo como fuente) · test-driven-development (RED-removal-guard → GREEN → suite) · incremental-implementation (slices ≤100L, compilable siempre) · context-engineering (context pack §3a, selective include por slice) · api-and-interface-design (Hyrum: renames breaking directos, contrato primero) · doubt-driven-development (gate review si vanta-review no disponible). Descartada: frontend-ui-engineering (sin web/, score espurio).

## Investigation Notes

- SDKB-03 existe y pasa: baseline `test_subclients.py` 22 passed (2026-09-11, runner sistema).
- Trace: `Client.get_memory` inbound en grafo = 0 (dispatch dinámico `call_method` por string invisible al grafo) — callers reales vía rg: macro-forward (3) + tests (~70) + wrapper (3) + integrations (71).
- Colisión intra-subcliente: NO hay — `get/list/delete` existen solo como aliases (`get => get_memory`, etc., `lib.rs:343-345`); `search` ya es corto (AST-008). Stop-condition del plan no dispara → sin Gate P.
- TS modelo (`vantadb-ts/src/vantadb.ts:39-67,249-276`): `MemoryClient.get/delete/list/search` delegando a flat homónimo (TS no tiene hazard: flat es memory-first).
- Precedente AST-008 (`0f36fc24`): `search_memory→search` + `search→search_vector` directos nativos, 19 archivos, "Gate C: integrations/examples/docs con nombres viejos (deuda lead)". AST-010 (`1a566ef3`): quitó aliases deprecated "0 usuarios". Este task = misma familia.
- `fn search_memory` en `src/` → 0 hits: el flat ya migró; `search_memory` vive solo en docs/integrations/smoke (drift).
- Entorno: system python 3.14.7 + pytest 9.1.1; `import vantadb_py` resuelve a `vantadb-python/` (sys.path) con `vantadb_py.pyd` local (11/9 3:41am); sin `target/audit-venv` → runner sistema con evidencia (contrato lo permite). maturin 1.14.1 disponible; pyo3 0.29.
- `test_stub_drift.py` es genérico (paridad dinámica stub↔nativo): se auto-adapta al rename; solo exige actualizar los 2 `.pyi`.
- Investigación internet: ninguna (todo el diseño sale del repo: TS modelo + BINDINGS_NAMESPACES + precedente).

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 |
| Pendientes de ejecución (downhill) | 0 (9/9 steps ✅) |
| % completado | 100% (commit 29b64ef8, sin push) |

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — evaluada: el cambio mueve código FFI existente verbatim (misma validación de args nativa, mismo `map_vanta_error`, sin nuevo input handling, sin nuevas deps, sin `unsafe`). No toca trust boundaries nuevos → `security-and-hardening` no aplica. Justificación registrada (no se carga la skill: mover cuerpos idénticos no crea superficie).
- [x] **PERFORMANCE** — evaluada: mismas llamadas engine (`get/delete/list`), se ELIMINA un hop (`call_method` string-dispatch del alias) en el path subcliente; flat removido no era hot-path distinto. Sin loops/serialización/HNSW tocados → `performance-optimization` no aplica, sin baseline (Regla 9: no hay claim de mejora).

## Steps

### Step 1: Tests RED — test_subclients.py a nombres cortos + guards de eliminación
- **Archivos:** `vantadb-python/tests/test_subclients.py`
- **Acción:** migrar `db.memory.get_memory→.get`, `delete_memory→.delete`, `list_memory→.list`; flats `db.get_memory→db.memory.get` (etc.); reescribir `test_backward_compat_flat_methods_untouched` como guard de eliminación (`not hasattr` flat `*_memory` + subcliente sin stutter); docstring SDKB-03→AST-012
- **Verify:** `python -m pytest vantadb-python/tests/test_subclients.py -q` → FALLA (guards `not hasattr` + `db.get_memory` removido del test… nota: cortos ya pasan vía alias — el RED lo dan los guards)
- **Estado:** ✅ COMPLETED (RED: 21 passed + 1 failed-guard `test_flat_memory_surnames_removed`, razón correcta: flat `get_memory` aún existe pre-cambio)

### Step 2: GREEN nativo — forward-list + cuerpos get/delete a MemoryClient
- **Archivos:** `vantadb-python/src/lib.rs` (§318-346 forward-list; nuevo `#[pymethods] impl MemoryClient` con `get`/`delete` reales; eliminar `fn get_memory`/`fn delete_memory` del flat)
- **Acción:** quitar `get_memory,delete_memory,list_memory` + sección alias de la macro; agregar cuerpos verbatim (vía `self.db` borrow) para `get`/`delete`; `list` va en Step 3
- **Verify:** `cargo check -p vantadb_py` (nombre real del paquete en `vantadb-python/Cargo.toml:2`)
- **Estado:** ✅ COMPLETED (check verde tras migrar la invocación al 3er brazo macro `with { }` — PyO3 rechaza dos bloques `#[pymethods]` para el mismo tipo, E0119; fix documentado en Notas)

### Step 3: GREEN nativo — cuerpo list + docstrings + rebuild
- **Archivos:** `vantadb-python/src/lib.rs` (cuerpo `list` real en MemoryClient; eliminar `fn list_memory`; docstrings `>>> db.get_memory` → `>>> db.memory.get` en §83,425,893,936,1126,1446 + nota AST-008 §337-345)
- **Acción:** mover cuerpo `list_memory` verbatim; actualizar ejemplos doctest-python y comentarios
- **Verify:** `cargo check` + `maturin develop -m vantadb-python/pyproject.toml` + sonda `hasattr` (flat sin `*_memory`; memory con get/list/delete sin stuttered)
- **Estado:** ✅ COMPLETED (check verde; `maturin develop` OK — ojo: se corre desde `vantadb-python/` sin `-m`; sonda: flat-stutter [] + node intacto + mem-short ok + roundtrip v/True/None; test_subclients 22 passed)

### Step 4: Migración test_sdk.py (46 sitios, mecánico)
- **Archivos:** `vantadb-python/tests/test_sdk.py`
- **Acción:** `db.get_memory(`→`db.memory.get(`, `db.list_memory(`→`db.memory.list(`, `db.delete_memory(`→`db.memory.delete(` (+await-variants incluidas por substring); mensajes de assert con nombres viejos → nuevos
- **Verify:** `python -m pytest vantadb-python/tests/test_sdk.py -q`
- **Estado:** ✅ COMPLETED (46 sitios migrados incl. async-forms; stuttered refs → 0; sync 45 passed; async 3 tests pendientes de Step 5 — failure exacta `AttributeError: 'AsyncVantaDB' object has no attribute 'memory'`)

### Step 5: Wrapper AsyncVantaDB — AsyncMemoryClient + eliminar planos
- **Archivos:** `vantadb-python/vantadb_py/__init__.py`, `vantadb_py/__init__.pyi`
- **Acción:** eliminar `get_memory/list_memory/list/delete_memory` async; agregar `AsyncMemoryClient(get/list/delete)` + property `memory`; docstrings sin stutter; espejar en `.pyi`
- **Verify:** `rg -n "get_memory|list_memory|delete_memory|search_memory" vantadb-python/vantadb_py/__init__.py` → 0 + import ok
- **Estado:** ✅ COMPLETED (rg→0 en .py + .pyi; AsyncMemoryClient get/list/delete roundtrip OK; test_sdk.py 75 passed)

### Step 6: Migración tests async/restantes + verify_wheel
- **Archivos:** `test_async_smoke.py` (7), `test_migration.py` (2), `test_close_concurrency.py` (1), `test_perf_15_16.py` (2), `verify_published_wheel.py` (3)
- **Acción:** flats → `db.memory.*`; async → `await db.memory.*`
- **Verify:** `python -m pytest vantadb-python/tests/test_async_smoke.py vantadb-python/tests/test_migration.py vantadb-python/tests/test_close_concurrency.py -q` (+ perf si rápido) + `python -c "import ast;..."` sintaxis wheel-verify
- **Estado:** ✅ COMPLETED (tests/ stuttered → 0 salvo guard intencional test_subclients.py:325; 17 passed + 4 skipped preexistentes; wheel-verify syntax OK)

### Step 7: Stubs nativos vantadb_py.pyi
- **Archivos:** `vantadb-python/vantadb_py/vantadb_py.pyi`
- **Acción:** Client: eliminar `get_memory/delete_memory/list_memory`; MemoryClient: eliminar stuttered + decls alias `*args/**kwargs` get/list/delete → tipadas `get/list/delete` reales; actualizar notas de dominio
- **Verify:** `python -m pytest vantadb-python/tests/test_stub_drift.py -q`
- **Estado:** ✅ COMPLETED (7 passed; Client −3 stuttered, MemoryClient −3 stuttered + get/list/delete tipadas reales)

### Step 8: Docs Regla 3 — PYTHON_SDK.md + BINDINGS_NAMESPACES.md
- **Archivos:** `docs/api/PYTHON_SDK.md`, `docs/api/BINDINGS_NAMESPACES.md`
- **Acción:** banner + sub-clients + `get_memory()`/`delete_memory()`/`list_memory()`/`search_memory()` sections → `db.memory.get/list/delete/search`; tabla Python `get_memory→get` etc.; nota hazard intacta; map v1 actualizado
- **Verify:** `rg -n "get_memory|list_memory|delete_memory" docs/api/PYTHON_SDK.md` → 0 (salvo changelog histórico si existe — justificar); `scripts/validate-docs-coverage.ps1` si aplica al área
- **Estado:** ✅ COMPLETED (stuttered → solo nota histórica PYTHON_SDK.md:92 + notas AST-008/AST-012 en BINDINGS; coverage 0 gaps con pwsh7 — nota: `powershell`5.1 falla parseando el script, preexistente; search_vector saldado como drift adyacente AST-008)

### Step 9: Verify full + Review + commit
- **Archivos:** plan file (Iteraciones/Commit/Estado), avance si aplica
- **Acción:** contrato + test_sdk + stub_drift + fmt/clippy; review por agente distinto (vanta-review vía task; fallback doubt-driven + registro); commit `refactor!:` sin push; actualizar plan file
- **Verify:** bloque RESULTADO §7 pipeline-full con SKILLS_CARGADAS: y GATES_EVALUADOS:
- **Estado:** ✅ COMPLETED (vanta-review changes-required condicional → R1 scope-14-archivos + R2 `-p vantadb_py` + nits atendidos; commit 29b64ef8 14 files 645+/373-; plan file actualizado; sin push)

## Dependencias

- Ninguna (diseño decidido: paridad TS + directo). nextTask: ninguna (cierra excepción).

## Review (GATE — agente distinto, P2-01)

- **Revisor:** vanta-review vía `task` ✅ ejecutado (sesión fresca, read-only, skills: doubt-driven-development, code-review-and-quality, code-simplification, systematic-debugging, ponytail)
- **Enfoque:** rename directo correcto (precedente + contrato + hazard; shim-compat descartado por Hyrum); AsyncMemoryClient mínimo correcto (paridad total = duplicación sin requerimiento)
- **Cómo se probó:** reruns independientes del revisor — subclients+stub_drift 29 passed, test_sdk 75 passed, rg→0 en `__init__.py`, `fn *_memory` → 0 en src, fmt/clippy exit 0, roundtrip funcional put→get→list→delete→None
- **Veredicto:** 🔴 changes-required CONDICIONAL (higiene, no lógica) → ATENDIDO: R1 scope-commit 13+taskfile (excluye 8 ajenos) · R2 `-p vantadb_py` corregido · nit Raises restaurado en `MemoryClient.get` · Step 2 tracking corregido
- **Checklist anti-hábitos tóxicos:** 10/10 pass (evaluado por el revisor con evidencia; ver dictamen en historial de sesión)

## Notas

- `ponytail:` un hop `call_method` menos en `db.memory.get` (alias string-dispatch → método real); sin abstracción nueva salvo `AsyncMemoryClient` exigido por contrato.
- LECCIÓN E0119: PyO3 (0.29) rechaza dos bloques `#[pymethods]` para el mismo tipo aunque no haya colisión de nombres de método — el 2º bloque debe fusionarse en el 1º. Solución: 3er brazo en `forward_to_db!` (`{...} with { $($real:tt)* }`) que pega forwards + métodos reales en un solo `#[pymethods] impl`. Patrón a seguir si otro subcliente necesita métodos reales.
- LECCIÓN runner: `maturin develop` se corre desde `vantadb-python/` (sin `-m pyproject.toml`; maturin 1.14 exige Cargo.toml como manifest-path). `scripts/validate-docs-coverage.ps1` requiere pwsh7 (`powershell` 5.1 falla parseándolo — preexistente).
- NOTICED BUT NOT TOUCHING (→ Gate C deuda lead): integrations/* 71 sitios; `smoke_test_extended.py`; `vantadb-python/README.md`; `docs/api/GRAPH_RAG.md:79` + `scores.md:56` (drift preexistente AST-008); `examples/`; `providers/ollama/tests`; `MCP.md search_memory` = tools MCP (superficie distinta).
- Convención commit: `refactor!:` (breaking directo, estilo AST-008 `0f36fc24` / AST-010 `1a566ef3`). Sin push (instrucción tarea).
- GATES (motivo ≤6 palabras c/u): P:no — sin colisión intra-subcliente · D:no — diseño forzado por evidencia · V: evaluar en verify · C:disparado — colaterales → deuda lead (precedente AST-008).

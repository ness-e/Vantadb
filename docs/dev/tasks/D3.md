# D3 — Structs de comando para 5–7 params

## 1. Descubrimiento (auto-detect tipo → codegraph blast radius → web si ambigüedad → baseline)
- **Tipo:** refactor firmas (F2 no-flag/command-object, cleanCA §2.2 + G7 Value Objects). Firmas actuales verificadas con grep (D4a ya renombró a `set`/`get`/`create`):
  - `src/entity/scene.rs:77` `SceneNodeStore::set(&self, namespace, session_id, name, created, updated, summary, heat)` — 7 params + self.
  - `src/entity/mod.rs:82` `EntityStore::set(&self, namespace, collection, id, fields)` — 4 params + self (5 con self).
  - `src/agentic/thread.rs:89` `ThreadStore::create(&self, title, metadata, ttl_secs, gc)` — 4 params + self (5 con self).
- **Blast radius (grep verificado):**
  - `EntityStore::set` → `src/entity/tests.rs` (9), `src/entity/checker_tests.rs` (3 seeds), `src/cli_server_auth_tests.rs:34` + `src/server/cli_server_auth_tests.rs:46` (seed_user), `vanta-proxy/src/auth.rs:197` (test seed), `vanta-proxy/src/session.rs:264` (test seed). Cero callers prod fuera de tests.
  - `SceneNodeStore::set` → solo `src/entity/scene_tests.rs` (6). Confinado.
  - `ThreadStore::create` directo → solo `src/sdk/builder.rs:174` (vía `Embedded::create_thread`); resto va por `Embedded::create_thread` (handlers/conversation) que NO cambia de firma.
- **Baseline:** firmas largas F2 🟡; validación `validate_key` igual (PROHIBIDO cambiar).
- **D2 files PROHIBIDOS:** `src/server/telemetry.rs`, `src/cli_handlers/*`, `src/rbac.rs`, `src/storage/engine/maintenance.rs`, `src/storage/engine/delete.rs` — no tocar.

## 2. Contrato (qué cambia / qué NO cambia / archivos exactos / comandos de verify)
- **Cambia:**
  - `src/entity/mod.rs`: nuevo `pub struct EntityWrite<'a> { namespace, collection, id: &'a str, fields: HashMap<String, FieldValue> }` + `set(&self, input: EntityWrite<'_>)`; cuerpo idéntico (mismo `validate_key`, mismo upsert).
  - `src/entity/scene.rs`: nuevo `pub struct SceneNodeWrite<'a> { namespace, session_id, name, created, updated, summary: &'a str, heat: u32 }` + `set(&self, input: SceneNodeWrite<'_>)`; cuerpo idéntico.
  - `src/agentic/thread.rs`: nuevo `pub struct CreateThread<'a> { title: &'a str, metadata: HashMap<String,String>, ttl_secs: Option<u64> }` + `create(&self, input: CreateThread<'_>, gc: Option<&mut GcWorker>)` (`gc` queda fuera del struct: es servicio, no dato); cuerpo idéntico.
  - Re-exports: `EntityWrite` en `entity/mod.rs`, `SceneNodeWrite` en `scene.rs` + re-export, `CreateThread` en `agentic/mod.rs`.
  - Callers mecánicos (struct-literal): tests/checker/cli_server_auth ×2/proxy ×2, scene_tests, builder.rs:174.
- **NO cambia:** validación (`validate_key` igual, mismos mensajes), `get/delete/list`, `send_message`, `Embedded::create_thread` firma, wire/serde, archivos D2, otros bindings.
- **Archivos exactos:** `src/entity/mod.rs`, `src/entity/scene.rs`, `src/agentic/thread.rs`, `src/agentic/mod.rs`, `src/entity/tests.rs`, `src/entity/scene_tests.rs`, `src/entity/checker_tests.rs`, `src/cli_server_auth_tests.rs`, `src/server/cli_server_auth_tests.rs`, `src/sdk/builder.rs`, `vanta-proxy/src/auth.rs`, `vanta-proxy/src/session.rs`.
- **Verify por slice (bash, sin campaign MCP):** `cargo nextest run --profile audit -p vantadb entity` / `agentic` + `cargo clippy -p vantadb --all-targets -- -D warnings` + `cargo fmt --check`.

## 3. Steps atómicos (☐ uno por slice: implementar → test → verificar → commit; si falla: `git reset --hard HEAD` del slice)
### Step 1 (S1): EntityWrite + migrar set callers
- **Archivos:** `src/entity/mod.rs`, `src/entity/tests.rs`, `src/entity/checker_tests.rs`, `src/cli_server_auth_tests.rs`, `src/server/cli_server_auth_tests.rs`, `vanta-proxy/src/auth.rs`, `vanta-proxy/src/session.rs`.
- **Acción:** struct + firma + struct-literals en callers (import `EntityWrite` donde toque).
- **Verify:** `cargo nextest run --profile audit -p vantadb entity` + clippy + fmt.
- **Estado:** ✅ DONE (EntityWrite + callers migrados incl. vanta-proxy/tests ×11; entity:: 41 ✅, checker 23 ✅, server auth_tests 22 ✅, proxy 163+ ✅; `validate_key` intacto)

### Step 2 (S2): SceneNodeWrite + migrar scene_tests
- **Archivos:** `src/entity/scene.rs`, `src/entity/scene_tests.rs` (+ re-export en `mod.rs`).
- **Acción:** struct + firma + struct-literals (6 sites).
- **Verify:** `cargo nextest run --profile audit -p vantadb entity` + clippy + fmt.
- **Estado:** ✅ DONE (SceneNodeWrite + scene_tests 6 sites; entity:: 41 ✅ incl. scene roundtrip/wholesale)

### Step 3 (S3): CreateThread + migrar builder.rs
- **Archivos:** `src/agentic/thread.rs`, `src/agentic/mod.rs`, `src/sdk/builder.rs`.
- **Acción:** struct + firma (`gc` param separado) + `CreateThread { title, metadata, ttl_secs }` en builder.
- **Verify:** `cargo nextest run --profile audit -p vantadb agentic` (+ builder vía `cargo nextest run --profile audit -p vantadb sdk`) + clippy + fmt.
- **Estado:** ✅ DONE (CreateThread + builder.rs + tests/message_thread_test.rs 6 sites; integración 6/6 ✅, builder 8 ✅, clippy -D warnings ✅, fmt ✅)

## 4. Cierre (RESULTADO + `/cleanCA <scope>` PASS + recitation)
- `RESULTADO` con ✅/🟡/❌, STEPS_OK, PROXIMO_STEP, COMMIT_HASH (ninguno — no commitear), ARCHIVOS, VERIFY_CONTRATO, BLOQUEO, GATES_EVALUADOS, SKILLS_CARGADAS ≥10.
- Gate D: blast radius <~12 archivos mecánicos, sin símbolos públicos nuevos salvo 3 structs de comando → GO sin `question`.
- No commitear (lo ejecuta el lead).

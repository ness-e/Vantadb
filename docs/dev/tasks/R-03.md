# R-03 — npm `0.6.0`: bump `vantadb-ts/package.json` 0.5.0→0.6.0 (verificar, SIN publicar)

> **Plan:** `docs/dev/plans/2026-09-20-estabilizacion-total.md` (Fase 0) · **Campaign:** 550f7331-35bf-469f-ba10-1959407cbaf9
> **Estado:** ⏳ IN PROGRESS · **Ruta:** vanta-worker · **Branch:** develop · **Commit:** `chore: R-03 — ...`
> **Esfuerzo:** 🟢 · **Prioridad:** 🔴 Alta (puerta Fase 0) · **Wave:** Wave0 paralelo (R-02 + C-02 disjuntos)
> **PROHIBIDO publicar** (`npm publish` ni siquiera dry-run que suba; solo lectura registry si hace falta).

## 1. TAREA — objetivo + contrato + acceptance criteria

**Objetivo:** alinear TS con el ecosistema 0.6.0 (crates.io + pyproject ya en 0.6.0; npm en 0.5.0).

**Contrato (ley):**
- (a) `vantadb-ts/package.json` version `0.6.0`
- (b) lock consistente (`package-lock.json` vía `npm install` si cambia)
- (c) `npx tsc --noEmit` → 0 errores en `vantadb-ts/`
- (d) tests TS verdes (`npm test` / `vitest run` en `vantadb-ts/`)
- (e) **cero cambios runtime** — solo versión. Si algo exige runtime → Stop y re-scope.
- (f) evaluar (solo informe, sin tocar) si `vantadb-wasm` necesita bump coordinado.
- (g) PROHIBIDO publicar.

## 2. ARCHIVOS — clave / relacionados / prohibidos

**Clave:**
- `vantadb-ts/package.json:3` (`"version": "0.5.0"` → `"0.6.0"`)
- `vantadb-ts/package-lock.json:3,9` (root `0.5.0` → `0.6.0` vía `npm install`; entrada `../vantadb-wasm/pkg` y `../vantadb-node` quedan en 0.5.0 — son file:deps externas, no se tocan)
- `vantadb-ts/src/` (solo verificación tsc/tests; **cero ediciones** — sin `0.5.0` hardcodeado en src, verificado por grep §7)

**Relacionados (solo lectura):**
- `.github/workflows/release-npm-61.yml` (trusted publishing OIDC + `id-token: write`, environment `npm`; publish-ts reescribe `dependencies.vantadb-wasm=^${WASM_VER}` desde `Cargo.toml` al publicar — el bump local NO toca esa línea file:)
- `docs/dev/tasks/FIND-125.md` (contexto: tsc 0 ya logrado vía 12+1 casts frontera; este bump no toca tipos)
- `Cargo.toml:773-774` (`[workspace.package] version = "0.6.0"`), `vantadb-python/pyproject.toml:7` (`0.6.0`), `vantadb-wasm/Cargo.toml:3` (`version.workspace = true` → ya 0.6.0)

**PROHIBIDOS (no tocar):**
- Publicar (`npm publish`, ni dry-run con subida); `src/` Rust; `vantadb-wasm/` (incluido `pkg/` — artefacto gitignored); `vantadb-node/`; `web/`; `desktop/`; `docs/` (C-02 en paralelo); `.github/workflows/`; `vantadb-python/`; plan file (solo recitation); secretos; WIP ajeno (`git status` limpio al inicio).

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** `vantadb-ts/package.json` (73L), `vantadb-ts/tsconfig.json` (15L), `vantadb-wasm/Cargo.toml` (54L), `.github/workflows/release-npm-61.yml` (257L), `.opencode/rules/js-ecosystem.md` (34L, completa), `docs/dev/tasks/FIND-125.md` (155L); parciales: `package-lock.json:1-45`, `Cargo.toml:773-775`, `pyproject.toml:1-20`, `vantadb-node/package.json`.
- **Referencias hacia dentro (qué consume la versión TS):** `package.json` (nombre `vantadb`, exports, `files: dist/`), `package-lock.json` (root + `packages[""]`), workflow `publish-ts` (lee `package.json.version` para `check-ts` skip-if-published + `npm publish`), `vantadb-ts/README.md:158` (badge `✅ 0.5.0` — NOTICED BUT NOT TOUCHING, fuera de contrato y `docs/` congelado por C-02).
- **Referencias entrantes (quién consume lo que cambio):** `release-npm-61.yml` job `tests` (`npm ci` + build + vitest + `npm pack --dry-run` + guard STABLE-06 engines); `publish-ts` (`npm install` + rewrite `vantadb-wasm=^WASM_VER` + smoke-pack + publish); consumidores externos vía `npm view vantadb@x` (skip-if-exists). Nada pinea `0.5.0` para TS: grep `vantadb-ts/` solo da `package.json:3` + lock `3,9` (+ `README.md:158` badge, no pin).
- **Veredicto:** impacto mínimo — 1 línea + lock regenerado. Sin símbolos públicos nuevos. Sin cambios runtime/tipos. Gate D no dispara (blast radius 1 archivo versionado + lock; contrato no ambiguo; sin spec nueva).

## 3. DEPENDENCIAS

- **Wave0 paralelo** (R-02 PyPI + C-02 docs): archivos disjuntos — no los toco.
- Sin bloqueantes. `git status` limpio; rama `develop`.
- **NextTask tras cierre:** R-05 (la ejecuta el orquestador, no yo).
- Gate D evaluado: blast radius 1 archivo, sin símbolos públicos nuevos, contrato mecánico (bump versión) → **no disparado** (sin `question` tool disponible; motivo registrado).

## 4. REFERENCIAS

- **Rules (leída completa antes de actuar):** `.opencode/rules/js-ecosystem.md` (R-1 persistencia doc, **R-2 `pkg/` artefacto gitignored — no se edita ni se cita como fuente**, R-3 standalone intencional, R-4 op-gate/drenaje — ninguna afectada por bump versión).
- **Norma transversal:** `.opencode/references/clean-code-clean-architecture.md` Ap. V (N/A-justificado: cambio mecánico versión, cero lógica).
- **Refs:** `definition-of-done.md` (standing checklist + DoD VantaDB: `tsc --noEmit`, tests, scope limpio), `dev-tools.md` (comandos).
- **SPEC raíz:** sin cambios. **Tabla Spec:** N/A (bump versión, sin símbolos públicos nuevos).

## 5. SKILLS (SDP Paso 0b — ejecutado real vía `campaign_discover_skills_v2` phase=BUILD)

Keywords: `[npm, version, typescript, tsc, publish-flow]` → 8 devueltas (scores 1.00):

| Skill | Cuándo aplica (1 línea) |
|---|---|
| campaign-executor | Base: state machine PLAN→ACT→VERIFY de esta tarea (auto-cargada vía MCP, no manual) |
| source-driven-development | Base TS-SDK: versiones desde dependency files, no memoria |
| incremental-implementation | Lifecycle BUILD: 1 slice (versión+lock) + verify por slice |
| test-driven-development | Lifecycle BUILD: contrato mecánico — tsc RED→GREEN + suite verde como prueba |
| context-engineering | Lifecycle BUILD: context pack Rules→Plan→Source aplicado en DISCOVERY |
| doubt-driven-development | Lifecycle BUILD: stakes altos (registro público; PROHIBIDO publicar) — verificación adversarial del no-publish |
| frontend-ui-engineering | Lifecycle (no aplica a este slice — sin `web/`; registrada por SDP, no se carga) |
| api-and-interface-design | Lifecycle BUILD: cambio en interfaz pública (versión) evaluado por compat (aditivo, sin breaking) |

**Cargadas:** source-driven-development · incremental-implementation · test-driven-development · context-engineering · doubt-driven-development · api-and-interface-design. (`SDP: campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, api-and-interface-design (+frontend-ui-engineering registrada no cargada: sin web/)`. SKILLS_CARGADAS se declara en RESULTADO.)

## 6. HERRAMIENTAS + MCP

- `npx tsc --noEmit` en `vantadb-ts/` (contrato c)
- `npm test` (= `vitest run`) en `vantadb-ts/` (contrato d)
- `npm install --package-lock-only --ignore-scripts` en `vantadb-ts/` (contrato b; validado con `campaign_validate_command` → safe)
- `campaign_verify_cmd` (contrato; **bug exit -1 conocido → bash directa + mención en RESULTADO**)
- Cargo: N/A (cero Rust). Internet: N/A (workflows en disco bastan; `npm view` no hizo falta — versión local 0.5.0 confirmada por archivo). Codegraph: N/A-justificado (cambio mecánico 1 línea; blast radius por `rg`, sin `.codegraph` necesario para este slice).
- Staging selectivo: `git add vantadb-ts/package.json vantadb-ts/package-lock.json docs/dev/tasks/R-03.md`. NO PUSH (solo vanta-lead).

## 7. INVESTIGACIÓN CÓDIGO — blast radius + alcance

**Qué consume la versión TS:** `package.json:3` (único source versionado) + lock `package-lock.json:3` (root) y `:9` (`packages[""]`). Entradas lock `../vantadb-node: 0.5.0` y `../vantadb-wasm/pkg: 0.5.0` son file:deps externas resueltas a disco — NO se tocan (nodo fuera de scope; el lock las re-emite igual tras regen).

**Alcance (¿algo más pinea 0.5.0 para TS?):** grep `vantadb-ts/` → solo `package.json:3` + lock `3,9` (+ `README.md:158` badge informativo, no pin; NOTICED BUT NOT TOUCHING). `src/` sin hardcodes de versión (matches `version` = campos `MemoryRecord.version`, no semver). `tsconfig.json` sin versión. Workflow `publish-ts:218` deriva `WASM_VER` de `Cargo.toml` al publicar — independiente del bump local.

## 8. INVESTIGACIÓN PROBLEMA — ¿`vantadb-wasm` necesita bump coordinado? (solo informe)

**No.** `vantadb-wasm/Cargo.toml:3` usa `version.workspace = true` → ya es `0.6.0` vía `[workspace.package]` (`Cargo.toml:774`). El `package.json` de npm `vantadb-wasm` NO es un archivo versionado: `vantadb-wasm/pkg/` es artefacto gitignored (`pkg/.gitignore: *`, `git ls-files` vacío) regenerado por `wasm-pack build --release` en CI (`release-npm-61.yml:75,143`) — su `version: 0.5.0` local es stale build, no source. En publish, `publish-wasm` sube el `pkg` recién construido (ya 0.6.0) y `publish-ts` reescribe `dependencies.vantadb-wasm=^0.6.0` automáticamente. **Acción requerida: ninguna** (ni local ni manual). Nota: `vantadb-node/package.json` también en `0.5.0`, pero fuera del contrato R-03 (lo decide el orquestador/R-05 si aplica; no se toca).

## 9. INVESTIGACIÓN INTERNET

N/A (workflows en disco bastan; sin ambigüedad de APIs externas; registry solo-lectura no hizo falta).

## 10. VALIDACIÓN + CIERRE

- Verify contrato: `package.json` 0.6.0 + `git diff --stat` (solo package.json + lock + este task file) + `npx tsc --noEmit` 0 + `npm test` verde + cero runtime (`git diff -- vantadb-ts/src/` vacío).
- DoD 3 niveles: (1) Correctness — AC (a)-(f); (2) Quality — diff mínimo, scope limpio; (3) Ship — commit `chore:` selectivo, sin push, recitation + RESULTADO §7.
- Gates D/V/C: D no disparado (§3); V/C se evalúan en cierre (2 fallas mismo-error → Gate V; colaterales → Gate C; sin `question` en toolbox → STOP + reporte si disparan).

## Steps atómicos

- [x] **Step 0 — DISCOVERY** (este file): versiones confirmadas (Rust 0.6.0 / pyproject 0.6.0 / TS 0.5.0 / lock 0.5.0 / pkg-stale 0.5.0 ignorado), blast radius, task file. Verify: `package.json` lee 0.5.0 (RED).
- [x] **Step 1 — bump + lock** (1 línea + regen): `package.json` → `0.6.0`, `npm install --package-lock-only`. Verify: `package.json` + lock root 0.6.0, `git diff --stat` acotado.
- [x] **Step 2 — verify + cierre**: `npx tsc --noEmit` 0 + `npm test` verde (12 files / 311 tests) + `git diff -- vantadb-ts/src/` vacío + commit `chore:` selectivo + recitation + RESULTADO §7.

## Context Save Point
- Rama `develop`; `git status` limpio al inicio.
- Tras Step 1: esperar `package.json:3` = 0.6.0, lock `3,9` = 0.6.0, resto lock intacto salvo checksum si npm reordena.
- Tras Step 2: esperar tsc exit 0, vitest verde (≈12 files / 311 tests baseline FIND-125), `src/` sin diff.
- Si `npm install` exige red o reescribe `../vantadb-node`/`../vantadb-wasm/pkg` más allá de re-emitir → STOP y edición manual mínima del lock (scope discipline), no "arreglar" deps.

# Task 43 — GH-131/129/128 · README integraciones (mem0, Semantic Kernel, DSPy)

**Estado:** COMPLETED

## Contexto
- 3 ejemplos existentes con smoke en CI (`ci-examples-12.yml`): `examples/python/mem0_integration.py`, `semantic_kernel_memory.py`, `dspy_retriever.py`.
- Faltaba la sección README documentando las 3 integraciones (3 issues: GH-131/129/128).
- Tasks previas: 41 (hero + benchmark graphic) y 42 (badge Colab) — respetadas, NO tocadas.

## Acción realizada
1. Leí los 3 scripts y extraje los nombres reales de clases/métodos/kwargs.
2. Añadí sección `## Integrations` en `README.md`, insertada tras `5-Minute Quickstart` y antes de `## Core Capabilities` (zona Ecosystem).
3. Snippets **verbatim** contra el código real:
   - **Mem0** → `VantaDBMem0Backend(namespace=...)`, `add(...)`, `search("dark mode", user_id=...)`, claves `score`/`content`, `close()` — coincide con `main()` (líneas 249–285).
   - **Semantic Kernel** → `VantaDBSemanticMemory(collection_name=...)`, `save_information(...)`, `retrieve("Semantic Kernel", limit=5)`, claves `relevance`/`text`, `close()` — coincide con `main()` (líneas 272–314).
   - **DSPy** → `VantaDBRetriever(namespace=..., k=3)`, `add([...])` con dicts `id`/`text`, callable `retriever(...)` (`__call__`), claves `score`/`text`, `close()` — coincide con `main()` (líneas 198–249).
4. Añadí bloque de comandos de ejecución idénticos a los smoke de CI.

## Verificación
- Snippets verificados línea por línea contra los scripts (ver arriba).
- No se tocó `ci-examples-12.yml` ni código fuente.
- Sección insertada sin borrar hero (línea 50) ni badge Colab (línea 42) de Tasks 41/42.

## Commit
`docs(GH-131): README integraciones mem0, Semantic Kernel, DSPy` — staging selectivo solo `README.md`.

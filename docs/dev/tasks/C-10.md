# C-10: Cierre README (paridad ES/EN + badges)

## Metadata
- **Plan file:** `docs/dev/plans/2026-09-20-estabilizacion-total.md` (§C-10) + `docs/dev/Backlog.md` P57
- **Fuente:** Backlog P57 (fila `C-10`)
- **Esfuerzo:** 🟢 0.5d
- **Prioridad:** 🟡
- **Tipo:** Docs (`campaign_detect_task_type` → devops; checks `yamllint` N/A — sin workflows tocados)
- **Turns estimados:** 5-10
- **Creado:** 2026-09-25
- **last-synced:** 2026-09-25
- **Estado:** ⏳ IN PROGRESS (pendiente review P2-01)
- **Incógnitas (uphill):** 0
- **Pendientes (downhill):** 1 (review + cierre)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | Ninguno (README raíz; no afecta build/tests) |
| Callees | `README.md`, `README_ES.md`, `examples/README.md`, `vantadb-ts/examples/` (targets de links) |
| Implicaciones | No cambia contratos; mejora paridad ES/EN y corrige un link a rama equivocada (colab main→develop) |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `README.md`, `README_ES.md` (secciones afectadas), scripts de verificación (`c10-readme-check.py`, `c10-links-diff.py`).
- **Referencias hacia dentro:** badges → workflows existentes (verificado 0 missing); links relativos → paths existentes.
- **Veredicto impacto:** **bajo** — solo contenido markdown de presentación; 0 links roto.

## Contrato
> "`python c10-verify.py` → exit 0: 0 missing links, 0 badge drift, paridad de links ES/EN OK, colab→develop en ambos READMEs."

## Spec (SDD)
N/A — no es feature-add (sin símbolos/contratos nuevos; consistencia documental).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** paridad estructural ES/EN (13 secciones c/u); colab badge → `develop`; links relativos resuelven; badges apuntan a workflows existentes.
- **Comandos de verificación:** `python c10-verify.py` → PASS (re-ejecutado vía `campaign_verify_cmd`).
- **Deuda pendiente:** ninguna.

## Recitation (canónico)

    contract:
      verificacion: `campaign_verify_cmd (c10-verify.py)` → passed=true ✅ ("0 missing links, 0 badge drift, paridad ES/EN OK, colab=develop")
      evidencia:
        - claim: drift real encontrado y corregido (4 fixes)
          evidencia: diff README_ES.md (colab blob/main→blob/develop; +fila examples; +nota ADR-041; **+Trust block traducido + nota "Fuente vigente" reubicada a §2 para paridad estructural con EN**)
          confianza: alta
        - claim: paridad de links ES/EN
          evidencia: `c10-links-diff.py` → solo EN=[] solo ES=[] (antes: solo EN=['examples/README.md','vantadb-ts/examples/'])
          confianza: alta
        - claim: badges sin drift (check v2: shields.io + HTML href/src)
          evidencia: `c10-verify.py` v2 → 0 badge workflows missing; workflows referenciados existen (`ci-rust.yml`, `gate-docs.yml`, `sec-codeql.yml`)
          confianza: alta
      artefactos:
        - docs/dev/tasks/C-10.md, README_ES.md
      invariantes: paridad ES/EN; colab→develop; 0 links roto
      deuda: ninguna
      queda_pendiente: review P2-01

## Deuda técnica (Regla 6)
Sin deuda.

## Definition of Done (multi-nivel)

| Nivel | Gate | Estado |
|-------|------|--------|
| **Task** | Contrato verificado (verify PASS) | ✅ |
| **Commit** | Commit docs (README_ES + task file + cierre) | ✅ (al cierre) |
| **Release** | N/A | N/A |

## Herramientas necesarias
- python (checks), git/gh (contexto), campaign MCP (detect/discover/verify).

**Skills cargadas (SDP):** `campaign-executor` (base) · `progreso` (cierre) · `ci-cd-and-automation` (badges/CI refs). SDP devolvió lifecycle genérico (git-workflow, shipping, documentation-and-adrs…) → N/A: cambio mecánico de paridad, sin prosa nueva sustantiva (solo nota traducida del EN) ni release.

## Investigation Notes
- Drift confirmado (EST plan lo anticipaba): ES omitía la fila de examples y la nota ADR-041; colab badge apuntaba a `main`.
- **Review P2-01 (ronda 1) añadió:** (a) el check de badges original era **código muerto** (regex `actions/workflows/.../badge` → 0 matches; los badges son `img.shields.io/github/actions/workflow/status/...` dentro de HTML `<a href>`) → check v2 escanea shields.io + HTML href/src; (b) **Trust block (EN 266-276) nunca existió en ES** → traducido + nota "Fuente vigente" reubicada a §2 (paridad estructural EN: Trust → §2 → NOTE fuente → NOTE server).
- Rust badge = shields.io estático (sin branch param); colab = link HTML (validado en negativo + develop).
- Cobertura del verify (declarada): links markdown + HTML relativos, badges shields.io, paridad de links ES/EN, colab→develop, presencia del Trust block. Fragmentos `#anchor` se strippean (validados a mano por el review: resuelven).

## Incógnitas (uphill) vs Pendientes (downhill)

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas | 0 |
| Pendientes de ejecución | 1 (review) → 0 |
| % completado | 100% fixes + verify |

## Fases explícitas — SECURITY | PERFORMANCE
- **SECURITY:** no aplica (markdown; sin secrets).
- **PERFORMANCE:** no aplica.

## Steps

### Step 1: Checks mecánicos (links/badges/paridad)
- **Verify:** `c10-readme-check.py` → EN 37 links/0 missing, ES 35/0; badges 0 missing
- **Evidencia:** drift detectado (colab main; 2 links solo-EN; nota ADR-041 ausente en ES)
- **Estado:** ✅ COMPLETED

### Step 2: Fixes README_ES
- **Archivos:** `README_ES.md` (4 ediciones)
- **Acción:** colab `blob/main`→`blob/develop`; fila "Ejecutar ejemplos ejecutables"; nota "Nombres (ADR-041)"; **Trust block traducido + nota "Fuente vigente" reubicada a §2**
- **Verify:** `git diff` (post-review: 4 fixes)
- **Estado:** ✅ COMPLETED

### Step 3: Verificación de contrato
- **Verify:** `campaign_verify_cmd (c10-verify.py)` → passed=true
- **Estado:** ✅ COMPLETED

### Step 4: Review P2-01 + cierre progreso
- **Estado:** ⏳ IN PROGRESS

## Dependencias
- C-02 ✅ (reorg docs previa).

## Review (GATE — agente distinto, P2-01)
- **Revisor:** `vanta-review` (misma sesión `ses_f29221f22ffeO2aj2FtZblvEVb`) — ronda 1: **❌ REQUEST CHANGES** (2 Required: check de badges muerto; Trust block ausente en ES; 4 Optional: commit+re-verify en árbol limpio, cobertura declarada, scripts no versionados, registry) → **aplicado**: check v2 (shields.io + HTML + Trust parity), Trust block traducido + nota reubicada, scripts pegados en §Notas, commit de cierre + re-verify → re-review vía RESUME.
- **Enfoque:** ¿los checks cubren los claims de paridad? ¿algo quedó fuera (ES stale en otras secciones)?
- **Cómo se probó:** el revisor re-ejecutó ambos scripts + `git ls-remote`/`gh pr list`; detectó que el badge-check era no-op y el Trust block faltaba (0 commits históricos en ES); validó a mano anchors/HTML/links externos (6/6) y traducción ADR-041 fiel.
- **Checklist anti-hábitos:** sin invención · sin declarar done sin verificar · fallos parciales documentados (cobertura del verify declarada).
- **Veredicto:** ❌ → cambios aplicados → ⏳ re-review

## Notas
- **Verify versionado (contrato reproducible):** el script vive en `%TEMP%/opencode/c10-verify.py` y su lógica está replicada aquí — `LINK_RE` (markdown) + `HTML_ATTR_RE` (`href|src`) para links relativos; `SHIELDS_WF_RE = img\.shields\.io/github/actions/workflow/status/ness-e/Vantadb/([A-Za-z0-9_.-]+)` contra `.github/workflows/`; paridad de sets de links ES/EN; colab negativo (`blob/main` → fail); presencia del Trust block ES. Exit 0 solo sin errores.
- Scope declarado: fragmentos `#anchor` no se validan (review los validó a mano: resuelven); links externos no se fetchean.

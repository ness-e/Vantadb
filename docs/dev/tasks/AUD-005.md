# Task 29: AUD-005 — Sincronizar versiones docs/api

**Estado:** ✅ COMPLETED
**Fecha:** 2026-08-05

## Contexto
Único drift real de versión en docs/api: `docs/api/openapi.yaml:4` decía `0.4.0`, workspace es `0.5.0` (Cargo.toml:602 `[workspace.package] version = "0.5.0"`). MCP.md ya en 0.5.0 (L310); HTTP_API.md 0.0.4 coincide con cli_server.rs:368 (protocol version, NO tocar — se excluye explícitamente del gate).

## Acción ejecutada
1. **Fix:** `docs/api/openapi.yaml:4` `version: "0.4.0"` → `"0.5.0"`.
2. **Gate CI (creado, lo valioso):** nuevo job `check-api-version` en `.github/workflows/gate-docs-21.yml`. Extrae `[workspace.package] version` de Cargo.toml con `sed` y compara contra:
   - openapi.yaml `info.version`
   - MCP.md `Current MCP implementation version:`
   Falla (exit 1 + `::error` annotations) si alguno diverge. HTTP_API.md `0.0.4` es protocol version y queda EXCLUIDO del gate (drift intencional).

## Contrato
- openapi.yaml = 0.5.0 ✅
- Gate CI falla si cabeceras divergen del workspace ✅

## Verificación
- `rg "0\.4\.0" docs/api/openapi.yaml` → 0 matches ✅
- Gate logic probado localmente (PS-equivalents): Workspace 0.5.0 / openapi 0.5.0 / MCP.md 0.5.0 → PASS ✅
  (CI corre bash en ubuntu-latest; `sed`/`grep` no existen en PowerShell local, se validó lógica con Select-String.)

## Commit
`docs(AUD-005): sincronizar openapi.yaml a 0.5.0` (+ gate CI check-api-version)
# AUD-042: Upgrade tantivy → eliminar allowlist RUSTSEC-2026-0253 (lru 0.16.4)

## Metadata
- **ID:** AUD-042
- **Creado:** 2026-08-13
- **Estado:** 🔴 BLOQUEADO upstream (verificado 2026-08-13)
- **Plan:** Backlog P16 (rec#6 del audit full 2026-08-12)

## Blast Radius
- **Callers:** Cargo.toml (direct dep `lru = "0.16"` en cli_server.rs:14), tantivy 0.26.1 (feature `advanced-tokenizer`)
- **Callees:** lru (LruCache), tantivy
- **Implicaciones:** si se bumpa tantivy 0.27.0 cuando publique, revisar API breaks en el feature `advanced-tokenizer` (cambio major 0.26→0.27)

## Contrato
"`cargo deny check` sin allowlist RUSTSEC-2026-0253 pasa Y lru 0.16.4 no está en `cargo tree -i lru`"

## Herramientas
- cargo-mcp, webfetch (crates.io API, GitHub raw)

## Hallazgo original (audit)
"Upgrade tantivy ≥0.18 — elimina allowlist RUSTSEC-2026-0253 + desbloquea lru 0.18 (security debt, rec#6)"

## Verificación 2026-08-13 (evidencia)
1. **Advisory RUSTSEC-2026-0253** (`~/.cargo/advisory-db/crates/lru/RUSTSEC-2026-0253.md`): lru < 0.18.2 afectado (UAF en `LruCache::pop`, CWE-416/415). Exploitabilidad estrecha: requiere unwinding panics + catch_unwind con keys cuyo Drop puede panic.
2. **tantivy 0.26.1 = última publicada** (crates.io API, 62 versiones, última 0.26.1, updated 2026-04-21).
3. **tantivy 0.26.1 pins `lru ^0.16.3`** (crates.io dependencies API, verificado).
4. **tantivy main (0.27.0, NO publicada)** — `https://raw.githubusercontent.com/tantivy-search/tantivy/main/Cargo.toml` usa `lru = "0.18.2"`. 404 en crates.io para 0.27.0.
5. **Direct dep `lru = "0.16"`** (Cargo.toml:33) usada en `src/cli_server.rs:14`. Bumparla sola NO elimina el advisory (0.16.4 sigue transitiva vía tantivy) y crea version split.

## Decisión
🔴 **BLOQUEADO upstream** — el fix existe en tantivy main pero no está publicado. No se puede resolver sin un release de tantivy ≥0.27.0.

## Acciones tomadas
- [x] deny.toml: comentario RUSTSEC-2026-0253 actualizado con estado real + instrucción de re-evaluación
- [x] Backlog: fila AUD-042 marcada 🔴 BLOQUEADO upstream con evidencia

## Próxima acción (cuando tantivy ≥0.27.0 publique)
1. `cargo update -p tantivy` (o bump en Cargo.toml a 0.27)
2. Verificar API compat de `advanced-tokenizer`
3. Bump direct `lru` a 0.18.2
4. Remover entry `RUSTSEC-2026-0253` de deny.toml
5. `cargo deny check` + `just verify`

## Notas
- Dependabot (weekly) trackeará el bump de tantivy automáticamente.

# STU-002 — AsyncVantaDB → AsyncClient (rename directo Python)

> **Plan:** `docs/dev/plans/2026-09-11-stutter-interno.md` (Task 2)
> **Estado:** ✅ COMPLETED
> **Contrato:** `rg -n "AsyncVantaDB" vantadb-python/ | wc -l` → `0` ✅ + pytest async smoke 3 passed ✅ + stub drift 7 passed ✅
> **DoD extra:** `test_sdk.py -k async` 16 passed ✅ + `test_async_smoke.py` 3 passed ✅ + `test_stub_drift.py` 7 passed ✅
> **Ruta:** vanta-worker · **Esfuerzo:** 🟢 · **Appetite:** max 1h · **Prioridad:** 🟠
> **Wave:** independiente de STU-001/STU-003/STU-004 (archivos disjuntos) · **nextTask:** STU-003

## Metadata

- **Plan file:** `docs/dev/plans/2026-09-11-stutter-interno.md` (Task 2, líns 72-104)
- **Creado:** 2026-09-11 (discovery puro, sin edits de código)
- **last-synced:** 2026-09-11 (ejecución FASE B completa)
- **Estado:** ✅ COMPLETED
- **Branch:** (workspace actual, sin branch nueva — commit directo autorizado FASE B)
- **Commit:** `refactor: STU-002 AsyncVantaDB→AsyncClient` (solo blast radius, sin push)
- **Tipo:** rename mecánico directo (sin compat, sin usuarios externos — decisión usuario A Gate P)
- **No toca:** `AsyncMemoryClient` (convive sin colisión, nombres distintos)

## Blast Radius

| Dirección | Símbolo / archivo | Evidencia |
|-----------|-------------------|-----------|
| DEFINICIÓN | `class AsyncVantaDB` en `vantadb-python/vantadb_py/__init__.py:198` (334 líneas, `:198-530`) | `codegraph_explore "AsyncVantaDB definition instantiations imports"` → `AsyncVantaDB (vantadb-python/vantadb_py/__init__.py:198) — 19 callers; tests: test_sdk.py, test_async_smoke.py` |
| `__all__` + re-export | `vantadb-python/vantadb_py/__init__.py:54` (`"AsyncVantaDB"`); `vantadb-python/vantadb_py/__init__.pyi:42`; `vantadb-python/vantadb/__init__.py:20` | Leídos completos 1-140 + 140-270 + 524-532 |
| Docstrings internos | `__init__.py:153` (`on AsyncVantaDB`), `:156` (`rest of AsyncVantaDB`), `:160` (`async with AsyncVantaDB`), `:209` (usage), `__init__.pyi:5`, `:122-130` (`class AsyncVantaDB` + `__aenter__ -> AsyncVantaDB`) | grep `vantadb-python/` → 66 hits totales |
| `__repr__` | `__init__.py:530` → `f"AsyncVantaDB(sync={self._sync!r})"` | Test lo aserta en `test_sdk.py:1182` (`assert repr(db).startswith("AsyncVantaDB")`) — RENOMBRAR AMBOS |
| Tests que la usan | `test_async_smoke.py` (3 instanciaciones `:34,:68,:113` + docstrings `:1,:10`); `test_sdk.py` `TestAsyncVantaDB` (`:859` + ~17× `vanta.AsyncVantaDB` en `:867,:886,:901,:918,:941,:966,:984,:1008,:1034,:1081,:1119,:1133,:1150,:1191,:1212,:1247,:1275` + repr `:1182`); `test_stub_drift.py` (`:11,:12,:21` docstrings + `:295,:299,:300,:301,:303,:308,:314,:320,:323` paridad `vanta.AsyncVantaDB`) | grep `vantadb-python/tests/` completo |
| Mención sin código | `tests/conftest.py:9` (comentario `AsyncVantaDB's inner sync handle`) — actualizar comentario | Leído completo (88L) |
| Stub nativo (NO rename) | `vantadb_py/vantadb_py.pyi:12` (comentario `AsyncVantaDB, re-exports` — describe wrapper, no declara clase) — solo retoque comentario si cita nombre viejo | Leído completo (515L): NO declara `class AsyncVantaDB`, solo `Client/MemoryClient/GraphClient/SystemClient/WikiClient` |
| NO TOCAR | `AsyncMemoryClient` — `__init__.py:148` (class), `:195` (repr), `:260-265` (property+ctor); `__init__.pyi:104`, `:147` | grep `AsyncMemoryClient` → 6 hits, ninguno colisiona con `AsyncClient` |
| FUERA DE SCOPE (deuda) | `docs/api/PYTHON_SDK.md:124,1029,1032,1034,1045,1048,1055,1060`; `docs/examples/fnd05_python_context_manager.py:75,77` (además usa `search_memory` drifted); skills `.opencode/skills/vantadb-mcp/references/api-reference.md:147,151`, `skills/vantadb/SKILL.md:338,342,345`; historial `docs/dev/avance/**`, `docs/dev/tasks/AST-*/COV-001`, `docs/dev/plans/archive/**` | grep `docs/` → 100+ hits (truncado); `integrations/` → 0 hits; `examples/` → 0 hits (solo `docs/examples/` tiene 1) |

## Impacto Regla 0

**Archivos leídos completos:** `vantadb-python/vantadb_py/__init__.py` (532L, zonas 1-140 + 140-270 + 524-532 verificadas), `vantadb-python/vantadb_py/__init__.pyi` (273L), `vantadb-python/vantadb_py/vantadb_py.pyi` (515L), `vantadb-python/vantadb/__init__.py` (28L), `vantadb-python/tests/test_async_smoke.py` (134L), `vantadb-python/tests/conftest.py` (88L), `vantadb-python/tests/test_sdk.py` (zonas `:859-1290` + `:1170-1219` + `:1-40`), `vantadb-python/tests/test_stub_drift.py` (`:285-334`), `docs/dev/plans/2026-09-11-stutter-interno.md` (173L), `docs/dev/tasks/AST-008.md` (70L, precedente pickle + D7), `docs/examples/fnd05_python_context_manager.py` (`:60-106`).

**Referencias hacia dentro (lo que el cambio necesita):** `Client` (sync, ya renombrado en AST-008 — `AsyncClient.__init__` hace `Client(*args, **kwargs)` en `:215`), `asyncio.Semaphore`, `asyncio.to_thread`, `functools.partial`, `AsyncMemoryClient` (intacto, `:260-265`).

**Referencias entrantes (a actualizar, todas en `vantadb-python/`):** `__all__` ×3 (`__init__.py:54`, `__init__.pyi:42`, `vantadb/__init__.py:20`), clase + docstrings + repr (`:198,:209,:530`), stubs (`__init__.pyi:122,130`), tests (`test_async_smoke` 5 refs, `test_sdk` ~19 refs incl. repr assert, `test_stub_drift` 12 refs), comentario `conftest.py:9`.

**Veredicto:** impacto CONTENIDO a `vantadb-python/` (wrapper puro Python + stubs + tests). NADA en Rust (`src/`, `vantadb-python/src/`), NADA en WASM/TS, NADA en `AsyncMemoryClient`. Colaterales fuera de scope (`docs/api/PYTHON_SDK.md`, `docs/examples/fnd05_*`, skills) → deuda Regla 6, no se tocan (precedente AST-008 D7).

## Contrato

```text
rg -n "AsyncVantaDB" vantadb-python/ | wc -l  →  0
target/audit-venv/Scripts/python -m pytest vantadb-python/tests/test_async_smoke.py -q  →  3 passed
target/audit-venv/Scripts/python -m pytest vantadb-python/tests/test_stub_drift.py -q  →  7 passed (anti-drift incl. test_async_wrapper_stub_matches_real_asyncclient renombrado)
target/audit-venv/Scripts/python -m pytest vantadb-python/tests/test_sdk.py -q -k async  →  16+ passed (TestAsyncClient renombrado)
```

## Spec

| # | Decisión | Evidencia / precedente |
|---|----------|------------------------|
| D1 | `AsyncVantaDB → AsyncClient` (rename directo de la clase wrapper; sin alias legacy, sin `__getattr__` puente) | Plan l19 "rename DIRECTO (sin usuarios)"; Gate P decisión usuario A; `AsyncMemoryClient` convive sin colisión (nombres distintos, verificado 6 hits) |
| D2 | `__all__` ×3 se actualiza (`__init__.py:54`, `__init__.pyi:42`, `vantadb/__init__.py:20`): `"AsyncVantaDB"` → `"AsyncClient"` | Paridad `__all__` exigida por `test_stub_drift.py:286` (`_all_of(__init__.py) == _all_of(__init__.pyi)`) |
| D3 | Docstrings + comentarios internos se actualizan (`__init__.py:153,156,160,199,209`; `__init__.pyi:5,122-127`; `conftest.py:9`; `test_async_smoke.py:1,10`; `test_sdk.py` docstrings `TestAsyncVantaDB→TestAsyncClient`) | Pre-mortem plan l99 "1) `__all__`/docstring con resto" |
| D4 | `__repr__` `:530` → `f"AsyncClient(sync={self._sync!r})"` + assert `test_sdk.py:1182` → `startswith("AsyncClient")` | Leído `:529-530` + `:1182`; sin esto el test repr falla |
| D5 | Tests migran con replace preciso `AsyncVantaDB→AsyncClient` (NO blanket: `AsyncVantaDB` contiene substring `VantaDB` — lesson 2026-09-11) + `test_stub_drift.py:294-324` (nombres `AsyncVantaDB` en test + strings `"AsyncVantaDB"` + `fn.name == "AsyncVantaDB"`) | `.opencode/task-system/memory/lessons.md:583`: "solo replaces precisos, nunca blanket" |
| D6 | `AsyncMemoryClient` NO se toca (ni rename ni refs) | Plan l24 + l79 "convive sin colisión"; 6 hits verificados |
| D7 | Docs (`PYTHON_SDK.md`, `fnd05_*`, skills, historial) e `integrations/` + `examples/` NO se tocan → deuda lead (Gate C) | Precedente AST-008 D7 (mismo gate); `integrations/` y `examples/` → 0 hits verificados (nada que migrar ahí) |
| D8 | Pickle NO aplica (sin `__getstate__` en wrapper) | `vector.rs:85-90` solo `VantaVector` tiene `__getstate__/__setstate__`; precedente AST-008:68 |

## Invariantes

- `AsyncMemoryClient` intacto (API + nombre + repr `AsyncMemoryClient(sync=...)` en `:195`).
- Cero cambio de comportamiento: mismos métodos/parámetros/semáforo `max_concurrency=4`/`to_thread`; solo cambia el nombre de la clase + strings.
- Sin `unsafe`, sin FFI, sin GIL-change (wrapper puro Python).
- `vantadb_py.pyi` (nativo) NO declara la clase → no se renombra nada ahí (solo comentario `:12` si se desea).
- `conftest.py` tracking sigue válido: `AsyncClient.__init__` resuelve `Client` como global de módulo (mismo patrón que MOD-16 documenta para `VantaDB`).
- Ningún test skippeado/deshabilitado para pasar.

## Recitation final (FASE B)

```text
=== RECITATION ===
Objetivo activo: STU-002 — AsyncVantaDB→AsyncClient
Estado: completed (S1-S5 verdes, commit creado, sin push)
Última acción: rename ejecutado + verifies verdes + task file sincronizado + commit blast-radius
Resultado: ✅
State: ACCEPT (desde: VERIFY)
Próxima acción: ninguna en STU-002 (siguiente: STU-003 — Familia entity/scene)
Contrato: rg -n "AsyncVantaDB" vantadb-python/ → 0 ✅ + pytest test_async_smoke 3 passed ✅ + test_stub_drift 7 passed ✅ + test_sdk -k async 16 passed ✅ + cargo check -p vantadb_py verde ✅
Invariantes: AsyncMemoryClient intacto (4 hits); cero cambio de comportamiento; sin unsafe/FFI; vantadb_py.pyi solo comentario
Comandos de verificación: rg (→0 en __init__.py, stubs, tests, vantadb-python/); python -m pytest (3/7/16 passed, python sistema 3.14.7 — audit-venv inexistente); cargo check -p vantadb_py (Finished dev)
Deuda: PYTHON_SDK.md + fnd05 + skills con nombre viejo → lead Gate C (confirmada, sin cambios)
Próxima tarea si completa: STU-003 — Familia entity/scene con serde alias
last-synced: 2026-09-11
=== END RECITATION ===
```

## Recitation inicial

```text
=== RECITATION ===
Objetivo activo: STU-002 — AsyncVantaDB→AsyncClient
Estado: plan (discovery completo, task file creado, sin edits de código)
Última acción: investigación con evidencia + escritura de docs/dev/tasks/STU-002.md
Resultado: 🟡 INCOMPLETO (discovery DONE, implementación pendiente)
State: PLAN (desde: DISCOVERY)
Próxima acción: S1 — rename en vantadb-python/vantadb_py/__init__.py (class + __all__ + docstrings + repr) + verify rg parcial
Contrato: rg -n "AsyncVantaDB" vantadb-python/ | wc -l → 0 + pytest async smoke verde + stub drift verde — VERIFY_CONTRATO: no-corrido (discovery puro, prohibido editar/verificar runtime en esta tarea)
Invariantes: AsyncMemoryClient intacto; cero cambio de comportamiento; sin unsafe/FFI; vantadb_py.pyi no declara la clase
Comandos de verificación: rg -n "AsyncVantaDB" vantadb-python/ | wc -l (→0); target/audit-venv/Scripts/python -m pytest vantadb-python/tests/test_async_smoke.py -q (→3 passed); .../test_stub_drift.py -q (→7 passed); .../test_sdk.py -q -k async (→16+ passed)
Deuda: docs/api/PYTHON_SDK.md (8+ refs) + docs/examples/fnd05_python_context_manager.py:75,77 + skills (api-reference.md, SKILL.md) quedan con nombre viejo → deuda lead Gate C
Próxima tarea si completa: STU-003 — Familia entity/scene con serde alias
last-synced: 2026-09-11
=== END RECITATION ===
```

## Deuda Regla 6

- `docs/api/PYTHON_SDK.md:124,1029,1032,1034,1045,1048,1055,1060` — documenta `AsyncVantaDB` (quedan con nombre viejo; owner: lead/AST-010-docs).
- `docs/examples/fnd05_python_context_manager.py:75,77` — usa `vantadb_py.AsyncVantaDB` + `search_memory` (doblemente drifted post-AST-008; owner: lead).
- Skills: `.opencode/skills/vantadb-mcp/references/api-reference.md:147,151`, `skills/vantadb/SKILL.md:338,342,345` (+ copias en `vantadb-python/skills/` si existen) — owner: lead.
- Historial/planes/tasks (`docs/dev/avance/**`, `docs/dev/tasks/AST-*/COV-001`, `docs/dev/plans/archive/**`, `ADR-041`) — registro histórico, NO se reescribe.
- `vantadb_py.pyi:12` comentario — retoque opcional en S2 (1 línea, sin efecto runtime).

## DoD (3 niveles)

- **Nivel 1 — Contrato:** `rg -n "AsyncVantaDB" vantadb-python/ | wc -l` → `0` (incluye `.py`, `.pyi`, tests, `vantadb/` alias).
- **Nivel 2 — Suite:** `test_async_smoke.py` 3 passed + `test_stub_drift.py` 7 passed + `test_sdk.py -k async` 16+ passed (incl. repr `AsyncClient`).
- **Nivel 3 — Workspace:** `cargo check -p vantadb_py` verde (sin cambios Rust esperados — sanity) + `git status --short` muestra solo archivos del scope + plan file STU-002 → ✅ COMPLETED tras commit de `vanta-lead` (sin push desde worker).

## Herramientas + SDP

- **Herramientas:** `codegraph_explore` (blast radius + definición), `grep`/`rg` (censo exhaustivo), `Read` (archivos completos), `campaign_discover_skills_v2`, `skill`, `campaign_verify_cmd` (en ejecución, no en discovery).
- **SDP (discover_skills_v2 BUILD, archivosClave=`vantadb-python/vantadb_py/__init__.py`, keywords=`rename,pytest,stub`, maxSkills=8):** `campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, frontend-ui-engineering, api-and-interface-design` — `frontend-ui-engineering` descartada en ejecución (sin `web/`); resto justificado: rename mecánico por slices (incremental), sin tests nuevos salvo migración (TDD como gate anti-regresión), contexto jerárquico ya cargado (context-engineering), rename de API pública con hazard substring (doubt-driven), `__all__`/stubs como contrato de interfaz (api-and-interface-design), verificación contra código real no memoria (source-driven), orquestación por task file (campaign-executor).
- **SKILLS_CARGADAS:** campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, api-and-interface-design, frontend-ui-engineering.

## Investigation Notes

1. `docs/dev/plans/2026-09-11-stutter-interno.md:72-104` — Task 2 canónica: archivos clave `__init__.py:198` + `test_async_smoke.py`, contrato `rg→0 + pytest async smoke`, pre-mortem (`__all__`/docstring resto; pickle no aplica ver AST-008), Cynefin 🟦 obvio, DoD "contrato + stub drift verde".
2. `vantadb-python/vantadb_py/__init__.py:198` — `class AsyncVantaDB:` (wrapper `asyncio.to_thread` + `Semaphore(max_concurrency=4)`, envuelve `Client` en `:214-216`); zona 140-270 leída (incl. `AsyncMemoryClient:148-195` intacto).
3. `vantadb-python/vantadb_py/__init__.py:52-74` — `__all__` con `"AsyncVantaDB"` en `:54`; head 1-140 leído (alias legacy `vantadb_py` con DeprecationWarning PY-03, no afectado).
4. `vantadb-python/vantadb_py/__init__.py:148-164` — `AsyncMemoryClient` + docstring con menciones `AsyncVantaDB` en `:153,:156,:160` (a renombrar menciones, NO la clase); repr `:195` intacto.
5. `vantadb-python/vantadb_py/__init__.py:198-212` — docstring clase + usage `async with AsyncVantaDB` en `:209` (a renombrar).
6. `vantadb-python/vantadb_py/__init__.py:529-530` — `__repr__` retorna `AsyncVantaDB(sync=...)`; contraparte test en `test_sdk.py:1182`.
7. `vantadb-python/vantadb_py/__init__.pyi:40-42,104-147` — `__all__` + `class AsyncMemoryClient` + `class AsyncVantaDB:122-273` (incl. `__aenter__ -> AsyncVantaDB:130` + duplicado `search_vector:149-151 vs :218` pre-existente, no tocar); docstring `:5` menciona `AsyncVantaDB`.
8. `vantadb-python/vantadb_py/vantadb_py.pyi:1-23` — stub del módulo NATIVO; NO declara `AsyncVantaDB` (solo comentario `:12`); nada que renombrar salvo comentario opcional.
9. `vantadb-python/vantadb/__init__.py:18-28` — alias canónico `import vantadb`, `__all__` con `"AsyncVantaDB"` en `:20` (a renombrar).
10. `vantadb-python/tests/test_async_smoke.py:1-134` — leído COMPLETO: 3 tests, 3 instanciaciones (`:34,:68,:113` vía `vanta.AsyncVantaDB`), docstrings `:1,:10`.
11. `vantadb-python/tests/conftest.py:1-88` — leído COMPLETO: comentario `:9` menciona `AsyncVantaDB`'s inner sync; rebind `vantadb_py.Client` en `:80` cubre `AsyncClient._sync` (patrón MOD-16, sigue válido tras rename).
12. `vantadb-python/tests/test_sdk.py:859-1290` — `TestAsyncVantaDB` + ~17 instanciaciones + repr assert `:1182`; imports `:14` (`import vantadb_py as vanta` — sin cambio); resto del archivo (1530L) fuera de scope.
13. `vantadb-python/tests/test_stub_drift.py:285-324` — `test_async_wrapper_stub_matches_real_asyncvantadb` usa `vanta.AsyncVantaDB` (`:300,:301,:303,:308`) + strings `"AsyncVantaDB"` (`:320,:323`) + `fn.name == "AsyncVantaDB"` (`:314`); renombrar test + refs a `AsyncClient`.
14. `codegraph_explore "AsyncVantaDB definition instantiations imports"` — definición `:198`, 19 callers, tests `test_sdk.py` + `test_async_smoke.py` (WASM `VantaDB` en `vantadb_wasm.js` es símbolo distinto, fuera de scope).
15. `grep AsyncVantaDB vantadb-python/` → 66 hits (censo §Blast Radius); `grep AsyncMemoryClient vantadb-python/` → 6 hits (sin colisión); `grep __getstate__ vantadb-python/` → solo `src/vector.rs:85-90` (VantaVector) + tests `:743-805` (ndarray); `grep pickle/getstate tests/` → 0 (wrapper sin pickle — precedente AST-008:68 confirmado).
16. `grep AsyncVantaDB docs/` → 100+ hits (PYTHON_SDK.md 8+, fnd05 `:75,77`, planes/tasks/historial/skills) = deuda; `grep integrations/` → 0; `grep examples/` → 0 (solo `docs/examples/` tiene 1).
17. `docs/dev/tasks/AST-008.md:19,68-69` — precedente: D6 "`AsyncVantaDB` (clase) NO se renombra" (scope de esa task — ESTA task lo revierte por decisión Gate P), pickle no aplica, D7 docs/integrations como deuda, D5 aliases AST-003 (`AsyncClient`) eliminados por rotos → este rename los reemplaza con clase real.
18. `.opencode/task-system/memory/lessons.md:583` — hazard: "`AsyncVantaDB` contiene substring `VantaDB` — solo replaces precisos, nunca blanket" (evita corromper `AsyncMemoryClient`/`VantaDB`/`Client`).
19. `campaign_discover_skills_v2` (BUILD, `vantadb-python/vantadb_py/__init__.py`, `rename,pytest,stub`, max 8) → 8 skills + `skill` cargada cada una (ver §Herramientas).
20. `docs/dev/tasks/AST-012.md:21,27,41` — contexto: `AsyncVantaDB` + `AsyncMemoryClient` (get/list/delete sin surname, hazard flat `get/delete` nodo en `__init__.py:379-383` de esa época) conviven; confirma no-colisión.

## Uphill / Downhill

- ⬆️ Uphill = 0 (decisiones tomadas: Gate P usuario A; rename directo sin compat; deuda docs a lead).
- ⬇️ Downhill = rename clase + `__all__` ×3 + docstrings/repr + stubs + tests (~35 refs en `vantadb-python/`) + 3 contratos verify + commit vía lead.
- **Status plan:** ⬆️ 0 / ⬇️ rename + tests (igual que plan l103).

## SECURITY

- **Superficie:** rename puro de símbolo Python (clase + strings `__all__`/docstring/repr). Sin cambios FFI/PyO3, sin `unsafe`, sin sockets/auth/paths nuevos, sin serialización nueva.
- **GIL/concurrencia:** intactos (`to_thread` + `Semaphore`; `__aexit__` con `py.allow_threads` lado Rust sin tocar).
- **Riesgo residual:** 🟢 ninguno vivo — el compilador + `rg→0` + stub-drift cazan restos; `conftest` tracking no se invalida (resuelve `Client` global igual).
- **Justificación:** nada que auditar por `vanta-audit` (sin FFI); no se delega.

## PERFORMANCE

- **Impacto:** cero runtime — mismo código, distinto nombre; `__repr__` cambia 1 string literal (costo idéntico).
- **Sin benchmarks:** rename no altera hot paths (GIL liberado igual, semáforo igual); `pytest -k async` es gate funcional, no perf.
- **Justificación:** no se delega a `vanta-tuner` (sin algoritmos/estructuras/hot paths nuevos — pipeline Worker→Tuner no dispara).

## Steps (atómicos — ejecutados FASE B, verdes)

- [x] **S1 — Wrapper `__init__.py` (class + `__all__` + docstrings + repr)** ✅
  - **Archivos:** `vantadb-python/vantadb_py/__init__.py`
  - **Acción:** replace preciso `AsyncVantaDB→AsyncClient` en `:54` (`__all__`), `:153,156,160` (docstring AsyncMemoryClient), `:198` (class), `:199-212` (docstring+usage), `:530` (repr). Prohibido blanket (hazard substring lesson :583; `AsyncMemoryClient` intacto).
  - **Verify:** `rg -n "AsyncVantaDB" vantadb-python/vantadb_py/__init__.py` → 0 hits ✅ (`AsyncClient` 7, `AsyncMemoryClient` 4 intacto)
  - **Estado:** ✅ COMPLETED
- [x] **S2 — Stubs + alias (`__init__.pyi` + `vantadb/__init__.py` + comentario `vantadb_py.pyi`)** ✅
  - **Archivos:** `vantadb-python/vantadb_py/__init__.pyi`, `vantadb-python/vantadb/__init__.py`, `vantadb-python/vantadb_py/vantadb_py.pyi` (comentario `:12` opcional)
  - **Acción:** `__init__.pyi`: `:42` (`__all__`), `:5` (docstring), `:122` (class), `:130` (`__aenter__` return), docstring `:123-127`; `vantadb/__init__.py:20` (`__all__`).
  - **Verify:** `rg -n "AsyncVantaDB" vantadb-python/vantadb_py/__init__.pyi vantadb-python/vantadb/__init__.py vantadb-python/vantadb_py/vantadb_py.pyi` → 0 hits ✅
  - **Estado:** ✅ COMPLETED
- [x] **S3 — Tests (`test_async_smoke` + `test_sdk` + `test_stub_drift` + comentario `conftest`)** ✅
  - **Archivos:** `vantadb-python/tests/test_async_smoke.py`, `vantadb-python/tests/test_sdk.py`, `vantadb-python/tests/test_stub_drift.py`, `vantadb-python/tests/conftest.py` (comentario `:9`)
  - **Acción:** replaces precisos `vanta.AsyncVantaDB→vanta.AsyncClient`, `TestAsyncVantaDB→TestAsyncClient`, docstrings, `test_sdk.py:1182` repr assert → `"AsyncClient"`, `test_stub_drift.py:294` nombre test → `test_async_wrapper_stub_matches_real_asyncclient` (+ minúscula `:294` corregida aparte) + refs `:299-323` + strings; `conftest.py:9` comentario.
  - **Verify:** `rg -n "AsyncVantaDB|asyncvantadb" vantadb-python/tests/` → 0 hits ✅; `rg -n "AsyncVantaDB" vantadb-python/` → 0 hits ✅
  - **Estado:** ✅ COMPLETED
- [x] **S4 — Verify contrato + suite (sin push, sin commit)** ✅ (retry ladder no disparado — 0 fallas)
  - **Archivos:** (ninguno — solo comandos)
  - **Acción:** correr contrato + DoD Nivel 2 (venv `target/audit-venv` INEXISTENTE — se usó `python` sistema 3.14.7).
  - **Verify:** `rg -n "AsyncVantaDB" vantadb-python/` → `0` ✅ + `pytest test_async_smoke.py` 3 passed ✅ + `pytest test_stub_drift.py` 7 passed ✅ + `pytest test_sdk.py -k async` 16 passed ✅
  - **Estado:** ✅ COMPLETED
- [x] **S5 — Cierre (task file + commit, sin push)** ✅
  - **Archivos:** este file (steps ✅ + Recitation final + Deuda confirmada)
  - **Acción:** sincronizar task file; commit solo blast radius (8 archivos `vantadb-python/` + este file; plan file NO se commitea — untracked, owner planner/otras waves).
  - **Verify:** `git status --short` confirma solo scope en el commit + `cargo check -p vantadb_py` verde ✅ (Finished dev, 5.69s)
  - **Estado:** ✅ COMPLETED

## Dependencias

- **Bloquea a:** STU-004 (verify final workspace — este rename debe estar verde antes del cierre).
- **Bloqueada por:** ninguna (archivos disjuntos de STU-001 Rust y STU-003 entity/scene; paralelizable).
- **Precedentes:** AST-008 (pickle no aplica, D7 deuda docs, `Client` ya canónico), AST-012 (`AsyncMemoryClient` convive), COV-001 (smoke tests origen), MOD-16 (conftest tracking), MOD-18 (stub drift gate).

## Review

- **Revisor:** `vanta-review` (second-opinion, distinto del implementador — gate REVIEW canónico).
- **Alcance:** (1) `rg→0` real en `vantadb-python/`; (2) `AsyncMemoryClient` intacto; (3) paridad `__all__` py/pyi; (4) repr + assert consistentes; (5) sin blanket-replace (diff no toca `VantaDB`/`Client`/`AsyncMemoryClient` salvo menciones `AsyncVantaDB`); (6) DoD 3 niveles con evidencia mecánica.
- **Risk Register (del plan):** 🟢×🟡 test/script con nombre viejo → respuesta: rg + migrar (son nuestros) → trigger 1 hit.

## Notas

- Plan l79: "`AsyncVantaDB` envuelve `Client`" — confirmado en `__init__.py:214-215` (`self._sync = Client(*args, **kwargs)`).
- `NOTICED BUT NOT TOUCHING: __init__.pyi:149-151 vs :218 search_vector duplicado` (pre-existente, fuera de scope) → ¿crear task?
- `NOTICED BUT NOT TOUCHING: docs/examples/fnd05_*:77 search_memory` (drift AST-008, deuda lead) → ¿extender AST-010-docs?
- Ponytail: rename mecánico, sin abstracciones, sin compat-shims (YAGNI — decisión Gate P).
- Iteraciones (plan file): `| — | — | — | — |` (sin iteraciones aún — discovery puro).

## Auto-auditoría (discovery, sección por sección)

- Metadata ✅ (plan/fecha/estado/ruta/appetite/nextTask, sin vacíos).
- Blast Radius ✅ (tabla con definición/callers/stubs/tests/deuda + file:línea por hit).
- Impacto Regla 0 ✅ (leídos completos listados, hacia dentro/entrantes, veredicto contenido).
- Contrato ✅ (comando exacto del plan + smoke + drift, verificable).
- Spec ✅ (tabla D1-D8 con evidencia/precedente, prohibido vacío).
- Invariantes ✅ (6, incl. AsyncMemoryClient + sin comportamiento).
- Recitation inicial ✅ (bloque parseable con contrato/deuda/próxima tarea).
- Deuda Regla 6 ✅ (5 items con owner lead, sin "N/A").
- DoD 3 niveles ✅ (contrato/suite/workspace con números esperados).
- Herramientas + SDP ✅ (herramientas + 8 skills con justificación + descarte frontend).
- Investigation Notes ✅ (20 numeradas, todas con file:línea).
- Uphill/downhill ✅ (0 / rename+tests, igual que plan).
- SECURITY ✅ + PERFORMANCE ✅ (justificadas, sin delegación, con motivo).
- Steps ✅ (5 atómicos con Archivos/Acción/Verify/⬜ PENDING).
- Dependencias ✅ (bloquea/bloqueada/precedentes, sin vacío).
- Review ✅ (revisor vanta-review + 6 alcances + risk del plan).
- Notas ✅ (confirmación envuelve-Client, 2 NBTN con pregunta, ponytail, iteraciones).

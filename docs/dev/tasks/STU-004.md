# STU-004: FOOTER_GROUPS → GROUPS + verify final + cierre

## Metadata

- **Plan file:** `docs/dev/plans/2026-09-11-stutter-interno.md` (Task 4)
- **Fuente:** plan Task 4 — "STU-004 (`FOOTER_GROUPS→GROUPS` + verify final + cierre): local a `footer.tsx`; luego gates workspace y archivado."
- **Esfuerzo:** 🟢 (max 1h, plan line 143)
- **Prioridad:** 🟡 (plan line 145)
- **Tipo:** Frontend (Next.js / TSX — rename local, sin Rust)
- **Ruta:** vanta-docs
- **Turns estimados:** 2 (1 rename + verify, 1 gates + retrospectiva)
- **Creado:** 2026-09-11T12:00
- **last-synced:** 2026-09-11T12:00
- **Estado:** ⏳ IN PROGRESS (4/4 steps ✅, review de agente distinto pendiente)
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps pendientes (4/4 ✅ ejecución; review pendiente, no es step de ejecución)
- **Campaign ID:** stutter-interno-2026-09-11

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | Ninguno externo — `FOOTER_GROUPS` es `const` local dentro de `Footer()` (`footer.tsx:24`); único export del archivo es `export function Footer()` (`footer.tsx:14`). Ningún archivo importa `FOOTER_GROUPS` (grep `web/` = 2 hits, ambos en el mismo archivo). |
| Callees | Ninguno — el símbolo es un array literal de 4 grupos (Platform, Solutions, Resources, Company, 31 rutas) consumido una sola vez en `footer.tsx:143` via `FOOTER_GROUPS.map((group) => ...)`. No llama a otros módulos. |
| Implicaciones | Rename puramente léxico intra-función (`FOOTER_GROUPS` → `GROUPS`, 2 líneas). Sin cambio de contrato, sin cambio de render, sin cambio de tipos. Riesgo de colisión con identificador `GROUPS` en scope: nulo — el scope es el cuerpo de `Footer()`, verificado completo (178 líneas, únicos identificadores locales: `tt`, `router`, `nav`, `FOOTER_GROUPS`). |

## Impacto mapeado (Regla 0) — OBLIGATORIO antes de cualquier edición

> GATE ANTES DE CUALQUIER EDICIÓN (MUST — AGENTS.md Regla 0): todo archivo que se vaya a modificar/eliminar se lee completo y se mapea su impacto ANTES del primer step de edición. Sin este bloque poblado, NO se escribe ni se ejecuta ningún step que edite archivos.

- **Archivos leídos (completos):** `web/src/components/vanta/footer.tsx` (178 líneas, leído entero 2026-09-11 — definición L24, uso L143, JSX L73-177), `web/AGENTS.md` (92 líneas, sección Commands L8-16), `docs/dev/plans/2026-09-11-stutter-interno.md` (Task 4, L141-173).
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `footer.tsx` importa `Github, Terminal, Heart` de `lucide-react` (L3), `useRouter` de `next/navigation` (L4), `VANTA, PRODUCT` de `./vanta-data` (L5), `VantaLogoMark` de `./logo-mark` (L6), `useLanguage` de `@/lib/language-provider` (L7). Ninguno de estos imports provee ni consume `FOOTER_GROUPS`.
- **Archivos que referencian a los editados (referencias entrantes):** grep `FOOTER_GROUPS` en todo `web/` retorna exactamente 2 matches, ambos en `footer.tsx` (L24 definición, L143 uso). Cero hits en tests, snapshots, e2e (`e2e/flujo-critico.spec.ts` no menciona footer groups), cero imports externos del símbolo. `Footer` como componente sí es importado por el shell, pero su firma `export function Footer()` no cambia.
- **Veredicto impacto:** bajo — rename local de 2 líneas dentro de una función, sin exports afectados, sin callers externos, sin snapshots que atrapar. Si el rename dejara un resto, `npm run lint` (`no-unused-vars`=error) y `npx tsc --noEmit` lo cazarían.

## Contrato

`npx eslint src/components/vanta/footer.tsx` exit 0 (scoped, derivado de `npm run lint` = `eslint .` según `web/AGENTS.md:12`) + `cargo clippy --workspace -- -D warnings` verde como verify final de campaña. El rename se verifica con `rg -n "FOOTER_GROUPS" web/` → `0` hits.

## Spec (SDD — justificación por evidencia, sin decisiones abiertas)

> Tabla de decisiones (spec-template §5) O justificación por evidencia por ítem. No hay decisiones técnicas abiertas: el plan ya decidió rename DIRECTO (plan L19: "rename DIRECTO (sin usuarios)").

| # | Decisión | Opciones (+tradeoff) | Default recomendado | Resuelto |
|---|----------|----------------------|--------------------|----------|
| 1 | Nombre destino del símbolo | `GROUPS` (corto, scope función de 178L lo desambigua; plan lo fija) / `FOOTER_GROUPS` actual (stutter: prefijo FOOTER_ repite el contexto Footer) / `NAV_GROUPS` (alternativa descriptiva, diverge del plan) | `GROUPS` | ✅ decidido-por-evidencia (ref: `docs/dev/plans/2026-09-11-stutter-interno.md:141` título Task 4 fija `FOOTER_GROUPS→GROUPS`; `footer.tsx:24,143` confirma scope local sin ambigüedad) |
| 2 | Alcance del lint en verify | Full `npm run lint` (`eslint .`, canónico `web/AGENTS.md:12`) / scoped `npx eslint src/components/vanta/footer.tsx` (rápido, pre-mortem plan L168 advierte "lint web pesado (aceptar o scoped)") | Scoped primero, full solo si el scoped pasa y hay tiempo | ✅ decidido-por-evidencia (ref: plan L168 pre-mortem autoriza scoped; `web/AGENTS.md:12` da el comando base `eslint .` del que deriva el scoped) |
| 3 | Cierre de campaña en este task | Retrospectiva + archivar ahora / retrospectiva ahora y archivar DESPUÉS de STU-003 | Retrospectiva en STU-004, archivado bloqueado hasta STU-003 | ✅ decidido-por-evidencia (ref: plan L172 DoD release "retrospectiva + archivar + progreso"; STU-003 sigue ⬜ PENDING L119, por lo que archivar ahora rompería la campaña — criterios de cierre abajo lo hacen explícito) |

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** firma `export function Footer()` intacta (el shell la importa; el rename no toca exports); las 31 rutas de los 4 grupos sin cambios de `path`/`label` (cualquier ruta alterada rompería la navegación del footer y el guard E2E `e2e/flujo-critico.spec.ts`); `web/AGENTS.md` reglas R-FE-4/R-FE-5/R-FE-6 sin tocar (light-only, touch targets, motion tokens); no introducir `dark mode`, dependencias ni easings hardcodeados.
- **Comandos de verificación:** `rg -n "FOOTER_GROUPS" web/` → `0` + `npx eslint src/components/vanta/footer.tsx` exit 0 (scoped) + `cargo clippy --workspace -- -D warnings` verde (verify final campaña, no tocado por este rename pero exigido por el contrato del plan L151).
- **Deuda pendiente:** ninguna introducida por el rename; queda pendiente STU-003 (familia entity/scene con serde alias) antes de poder archivar la campaña.

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | Encabezado `# STU-004: FOOTER_GROUPS → GROUPS + verify final + cierre` |
| `lastAction` | Discovery puro 2026-09-11: footer.tsx leído completo + grep + AGENTS.md + skills (task file creado, cero edits de código) |
| `result` | PARTIAL ↔ ⏳ IN PROGRESS con 4 steps pendientes (discovery ✅, ejecución ⬜) |
| `nextAction` | Step 1 — rename `FOOTER_GROUPS` → `GROUPS` en `web/src/components/vanta/footer.tsx:24,143` |
| `contract` | `## Contrato` + `## Invariantes de dominio` + evidencia `## Investigation Notes` |
| `nextTask` | STU-003 debe completarse antes del archivado; tras STU-004 + STU-003 → retrospectiva + archivar + progreso |

```
=== RECITATION ===
Objetivo activo: STU-004 — FOOTER_GROUPS → GROUPS + verify final + cierre
Estado: verify (desde: act)
Última acción: 4/4 steps ✅ (rename + rg 0 hits + eslint scoped exit 0 + clippy workspace exit 0 en 52.35s + retrospectiva parcial)
Resultado: PARTIAL (ejecución 100%, review de agente distinto pendiente)
State: ⏳ IN PROGRESS (desde: ⬜ PENDING)
Próxima acción: review por agente distinto (veredicto approve/changes) + commit ya creado; luego STU-003 antes de archivar
Contrato: npx eslint src/components/vanta/footer.tsx exit 0 ✅ + cargo clippy --workspace -- -D warnings exit 0 ✅ (corrido 2026-09-11)
Invariantes: firma Footer() intacta; 31 rutas sin cambios; R-FE-4/5/6 sin tocar
Comandos de verificación: rg -n "FOOTER_GROUPS" web/ → 0 ✅; npx eslint scoped exit 0 ✅; cargo clippy workspace exit 0 ✅
Deuda: ninguna introducida; STU-003 pendiente bloquea archivado
Próxima tarea si completa: STU-003 — familia entity/scene (debe cerrarse antes de archivar campaña)
last-synced: 2026-09-11T12:00
=== END RECITATION ===
```

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda

> Regla 6 (AGENTS.md): toda deuda nueva introducida debe compensar deuda existente — el saldo neto por PR es 0 o negativo. Este task es un rename léxico de 2 líneas sin nuevos `unsafe`, sin `clone()` en hot path, sin dependencias, sin branches: no introduce deuda. No se reclama moneda de pago P2 (P2-5/P2-8 quedan intactas para quien las pague). Si el ejecutor detectara deuda colateral (p.ej. doc-comment con nombre viejo), la registra en Notas y la salda con `rg` post-rename en el mismo PR.

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | `rg -n "FOOTER_GROUPS" web/` → `0` + `npx eslint src/components/vanta/footer.tsx` exit 0 + `cargo clippy --workspace -- -D warnings` verde (verify final campaña) + render del footer sin cambios visuales/funcionales (31 rutas intactas) |
| **Commit** | Commit atómico (~2 líneas + task file), conventional commit (`refactor(web): rename FOOTER_GROUPS to GROUPS in footer`), `git diff` limpio limitado a `footer.tsx`, verificación mecánica real (nunca auto-reporte) |
| **Release** | No aplica bump (rename interno sin API pública); sí aplica cierre de campaña: retrospectiva escrita + plan file actualizado + explícitamente SIN archivar hasta que STU-003 esté ✅ (criterios de cierre abajo). Justificado en Notas. |

**Gate:** el task se marca COMPLETED solo si pasan Task + Commit; el nivel Release se satisface con la retrospectiva, quedando el archivado bloqueado por STU-003.

## Herramientas necesarias

- `read`/`grep` (impacto Regla 0 — ya usados en discovery)
- `npx eslint` scoped + `npm run lint` full (contrato web)
- `cargo clippy --workspace -- -D warnings` (verify final campaña)
- `rg` post-rename (caza restos)
- `codegraph_explore` (blast radius, si el ejecutor quiere doble-check)
- `campaign_verify_cmd` + `campaign_update_task_state` (transiciones de estado)

**Skills cargadas (SDP):** `campaign-executor` (orquestación pipeline task/discovery/steps — base del task file); `frontend-ui-engineering` (calidad UI prod, accesibilidad del footer — el rename no altera estados pero el gate los preserva); `design-taste-frontend` (brief→diseño, anti-slop — el footer es superficie visual, el rename no cambia estética); `incremental-implementation` (slices delgados rename→verify→gates — BUILD lifecycle); `test-driven-development` (RED/GREEN adaptado: `rg` 0 hits + lint como prueba del rename — BUILD lifecycle); `context-engineering` (curaduría de contexto footer.tsx + AGENTS.md + plan — BUILD sesión compleja); `source-driven-development` (comandos verificados contra `web/AGENTS.md`, no memoria — BUILD decisiones de framework); `doubt-driven-development` (cuestionamiento del plan §"Cuestionamiento" + pre-mortem lint pesado — BUILD stakes de campaña). Discovery vía `campaign_discover_skills_v2` (`archivosClave="web/src/components/vanta/footer.tsx"`, `phase="BUILD"`, `contractKeywords=["eslint","rename","frontend"]`, `maxSkills=8`, 8/8 score 1.00).

## Investigation Notes

1. **Claim:** `footer.tsx` tiene 178 líneas y `FOOTER_GROUPS` es `const` local declarada en L24 y usada una sola vez en L143.
   - **Evidencia:** `read web/src/components/vanta/footer.tsx` completo (L1-178): L24 `const FOOTER_GROUPS = [` con comentario L23 "Footer navigation groups — all 31 routes organized"; L143 `{FOOTER_GROUPS.map((group) => (`; firma L14 `export function Footer() {`.
   - **Confianza:** alta
2. **Claim:** No hay exports ni imports externos del símbolo; el único export del archivo es `Footer`.
   - **Evidencia:** `grep "FOOTER_GROUPS" web/` = 2 matches (`footer.tsx:24`, `footer.tsx:143`); `grep` confirma cero hits en tests, snapshots, e2e. Imports del archivo (L3-L7: `lucide-react`, `next/navigation`, `./vanta-data`, `./logo-mark`, `@/lib/language-provider`) ninguno referencia el símbolo.
   - **Confianza:** alta
3. **Claim:** El comando lint canónico es `npm run lint` (`eslint .`, exit 0 requerido) y el scoped derivado es `npx eslint src/components/vanta/footer.tsx`.
   - **Evidencia:** `read web/AGENTS.md` sección Commands L8-16: L12 `npm run lint  # eslint . — exit 0 requerido (no-unused-vars=error desde WDA-05)`; plan L168 pre-mortem autoriza scoped ("lint web pesado (aceptar o scoped)").
   - **Confianza:** alta
4. **Claim:** El verify final de campaña es lint + `cargo clippy --workspace -- -D warnings` en verde, y el cierre exige retrospectiva + archivado + progreso.
   - **Evidencia:** `docs/dev/plans/2026-09-11-stutter-interno.md` L151 contrato Task 4: "`npm run lint --prefix web` (o eslint equivalente con evidencia) + `cargo clippy --workspace -- -D warnings` verde"; L172-173 "Uphill/Downhill: micro-rename + gates + retrospectiva + archivar" y "DoD release: retrospectiva + archivar + progreso".
   - **Confianza:** alta
5. **Claim:** El archivado está bloqueado porque STU-003 sigue ⬜ PENDING.
   - **Evidencia:** plan L117-119 Task 3 STU-003 `Estado: ⬜ PENDING` (familia entity/scene con serde alias, `src/entity/**` + `cli_server_auth_tests.rs`); archivar la campaña con STU-003 pendiente dejaría trabajo DO sin cerrar. Criterios de cierre abajo lo codifican.
   - **Confianza:** alta
6. **Claim:** Las 8 skills SDP cubren el rename + gates sin sobredimensionar.
   - **Evidencia:** `campaign_discover_skills_v2` con `archivosClave="web/src/components/vanta/footer.tsx"`, `phase="BUILD"`, `contractKeywords=["eslint","rename","frontend"]`, `maxSkills=8` → 8 skills score 1.00 (`campaign-executor`, `frontend-ui-engineering`, `design-taste-frontend`, `incremental-implementation`, `test-driven-development`, `context-engineering`, `source-driven-development`, `doubt-driven-development`), cada una cargada con `skill <nombre>` en esta sesión.
   - **Confianza:** alta

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — approach validado (rename directo), comandos verificados en AGENTS.md, blast radius censado, skills cargadas |
| Pendientes de ejecución (downhill) | 0 — 4/4 steps ✅ (review pendiente, fuera de ejecución) |
| % completado | 100% ejecución (verify verde + commit; review pendiente para ✅ COMPLETED) |

**Regla de reporting:** cada actualización de estado actualiza los tres contadores. Una tarea con incógnitas abiertas NO se marca ✅ aunque el % sea 100% — aquí uphill = 0, el gate es downhill + contrato.

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — evaluado, no aplica cambio: `FOOTER_GROUPS` es un array estático de `{label, path}` internos (31 rutas del site), sin input de usuario, sin auth, sin trust boundary, sin dependencias nuevas, sin `dangerouslySetInnerHTML`, sin URLs externas (los links externos GitHub/PyPI/Discord usan `VANTA.*` + `rel="noopener noreferrer"`, intactos). No se carga `security-and-hardening` porque no hay superficie de ataque alterada; el gate es que el diff no toque ningún `href`/handler.
- [x] **PERFORMANCE** — evaluado, impacto cero: rename léxico de identificador local, cero cambio de runtime (mismo array, mismo `.map`, mismo render). No hay hot path (footer render estático, sin loops calientes ni memoización afectada). No se carga `performance-optimization` porque no hay baseline que mover; el gate es paridad visual/funcional del footer (31 rutas intactas, E2E `npx playwright test` como verificación opcional si el ejecutor quiere evidencia extra).

## Steps

### Step 1: Rename FOOTER_GROUPS → GROUPS (2 líneas)
- **Archivos:** `web/src/components/vanta/footer.tsx:24,143`
- **Acción:** Renombrar la `const` local `FOOTER_GROUPS` → `GROUPS` en L24 y su único uso en L143 (`{GROUPS.map((group) => (`). No tocar exports, imports, rutas, labels, JSX ni estilos. Post-rename correr `rg -n "FOOTER_GROUPS" web/` y exigir `0` hits.
- **Verify:** `rg -n "FOOTER_GROUPS" web/` → `0` (contrato léxico del rename)
- **Estado:** ✅ COMPLETED (2026-09-11 — `rg` 0 hits, `GROUPS` en L24/L143)

### Step 2: Lint scoped del archivo (+ full opcional)
- **Archivos:** `web/src/components/vanta/footer.tsx`
- **Acción:** Correr el lint scoped `npx eslint src/components/vanta/footer.tsx` (derivado de `npm run lint` = `eslint .` según `web/AGENTS.md:12`) desde `web/`. Si el scoped pasa y hay tiempo, correr el full `npm run lint` como evidencia extra (plan autoriza scoped por pre-mortem "lint web pesado"). Cazar restos: `no-unused-vars`=error atraparía un rename a medias.
- **Verify:** `npx eslint src/components/vanta/footer.tsx` exit 0
- **Estado:** ✅ COMPLETED (2026-09-11 — exit 0, solo scoped; full omitido por pre-mortem "lint web pesado")

### Step 3: Verify final de campaña — clippy workspace
- **Archivos:** workspace Rust (ninguno modificado por este task; gate de campaña)
- **Acción:** Correr `cargo clippy --workspace -- -D warnings` desde la raíz y exigir verde. Este task no toca Rust, así que el comando debe pasar sin cambios; si falla, el fallo es preexistente/colateral de STU-001/002/003 — registrarlo en Notas, NO fixearlo en este PR (disciplina de scope), y escalar al owner de la campaña.
- **Verify:** `cargo clippy --workspace -- -D warnings` exit 0
- **Estado:** ✅ COMPLETED (2026-09-11 — Finished dev profile en 52.35s, exit 0, sin cambios Rust)

### Step 4: Retrospectiva + criterios de cierre (SIN archivar)
- **Archivos:** `docs/dev/plans/2026-09-11-stutter-interno.md` (Task 4: Estado, Iteraciones, Notas), este task file (Review, Notas, Context Save Point)
- **Acción:** Escribir retrospectiva breve de la campaña (qué renames entraron, qué gates pasaron, qué queda), actualizar el plan file (STU-004 → ✅ cuando Steps 1-3 verdes + retrospectiva), y verificar criterios de cierre: (a) STU-004 contrato verde, (b) STU-003 sigue ⬜ PENDING → PROHIBIDO archivar `docs/dev/plans/2026-09-11-stutter-interno.md` a `docs/dev/plans/archive/` todavía, (c) registrar `progreso` pendiente para el cierre real. No mover ni archivar nada en este step.
- **Verify:** plan file Task 4 con Estado actualizado + retrospectiva escrita + mención explícita "archivado bloqueado por STU-003 ⬜ PENDING"
- **Estado:** ✅ COMPLETED (2026-09-11 — retrospectiva parcial en Notas; plan file NO tocado ni archivado: STU-003 ⬜ PENDING lo bloquea)

## Dependencias

- STU-003 — familia entity/scene con serde alias (debe completarse ANTES del archivado de la campaña; NO bloquea el rename ni los gates de STU-004, solo el archivado final)
- STU-001, STU-002 — renames Rust/Python (independientes de STU-004; si su clippy/colateral rompe el Step 3, se registra y escala sin fixear aquí)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. Sin esto registrado, la tarea no está COMPLETED.

- **Revisor:** vanta-review P2-01 (sesión fresca, distinta al implementador — 2026-09-11; sin participación previa en STU-004, sin conflicto)
- **Enfoque:** ¿el rename cubre exactamente las 2 ocurrencias sin tocar rutas/exports? ¿el scoped lint era suficiente o exigía full? ¿el clippy workspace verde es evidencia real (log) o auto-reporte? ¿la retrospectiva refleja la campaña completa?
- **Cómo se probó (revisor, evidencia propia — no auto-reporte):** `git show 5e7f6fd2 -- web/src/components/vanta/footer.tsx` → diff de exactamente 2 líneas (L24 `const FOOTER_GROUPS` → `const GROUPS`, L143 `{FOOTER_GROUPS.map` → `{GROUPS.map`); grep `FOOTER_GROUPS` en `web/` → 0 hits (cubre tests/snapshots/e2e); grep `\bGROUPS\b` en `web/` → exactamente 2 hits (`footer.tsx:24,143`, sin colisión ni shadowing en el scope de `Footer()`); grep `footer|Footer|GROUPS` en `web/e2e/` → 0 hits (ningún E2E referencia el símbolo); `npx eslint src/components/vanta/footer.tsx` desde `web/` ejecutado por el revisor → exit 0 sin output ✅; `footer.tsx` L143 y L1-178 releídos por el revisor (firma `export function Footer()` intacta, 31 rutas intactas). Clippy workspace NO re-ejecutado por el revisor (declarado): el diff `--stat` prueba que el commit no toca ningún archivo Rust, por lo que clippy es ortogonal al rename; se acepta el reporte del implementador (Finished dev profile 52.35s exit 0) como gate de campaña, no como gate del rename.
- **Checklist anti-hábitos tóxicos** (contrato de comportamiento — verificado por el revisor 2026-09-11, sin violaciones detectadas):
  - [x] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [x] No saltarse la clarificación por "ya sé qué quiere".
  - [x] No declarar done sin verificar contra los acceptance criteria.
  - [x] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [x] No hacer un solo intento de búsqueda y darlo por saturado.
  - [x] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [x] No reintentar en bucle sin diagnóstico.
  - [x] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [x] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [x] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ✅ approve — rename completo (2/2 ocurrencias, 0 restos), eslint scoped exit 0 re-ejecutado por el revisor, sin breakage posible (símbolo local sin exports/imports/tests/snapshots/e2e). Ver §Review-append 2026-09-11 para dictamen five-axis completo.

## Notas

- **Retrospectiva parcial STU-004 (2026-09-11, ejecución):** rename mecánico de 2 líneas (`footer.tsx:24,143`) sin fricción — el discovery previo (grep 2 hits, exports censados) pagó el costo por adelantado. Verify: `rg` 0 restos ✅, eslint scoped exit 0 ✅ (full omitido según pre-mortem), clippy workspace exit 0 en 52.35s sin tocar Rust ✅. Lección: en renames locales, el scoped lint es suficiente como gate; el full queda como evidencia opcional. Plan file NO archivado (STU-003 ⬜ PENDING lo bloquea) y NO modificado (untracked, propiedad del pipeline).
- Evidencia de ejecución: Step 1 `GROUPS` en L24/L143, `FOOTER_GROUPS` 0 hits en `web/`; Step 2 eslint exit 0; Step 3 clippy `Finished dev profile [optimized] en 52.35s` exit 0.
- Discovery puro 2026-09-11: único artefacto inicial `docs/dev/tasks/STU-004.md`; fase B agregó solo el rename en `footer.tsx` + sync de este file. Sin push (commit local).
- Decisión de diseño: `GROUPS` sobre `NAV_GROUPS` porque el plan fija `GROUPS` y el scope-función lo desambigua; divergir sería scope-creep sobre un rename mecánico.
- Pre-mortem del plan (L168) anticipa "lint web pesado (aceptar o scoped)" — por eso el contrato prioriza scoped con full opcional.
- Si `cargo clippy --workspace` falla por causas de STU-001/002/003, no se fixea en el PR de STU-004 (scope discipline + incremental-implementation Regla 0.5); se registra el log en esta sección y se escala.
- DoD release justificado: sin bump/semver (rename interno), con retrospectiva como entregable de cierre y archivado explícitamente diferido.

## Referencias

- `.opencode/references/definition-of-done.md` — standing quality bar
- `.opencode/references/skills-engineering.md` — SDP lifecycle mapping
- `SKILLS-MANIFEST.md` — catálogo de skills disponibles
- `web/AGENTS.md` — comandos web canónicos (`npm run lint` = `eslint .`)
- `docs/dev/plans/2026-09-11-stutter-interno.md` — plan de campaña (Task 4, contrato, DoD release)

## Context Save Point

- **Fecha:** 2026-09-11
- **Branch:** [registrar al commitear — `git log -1 --oneline` tras el commit]
- **CI pendiente:** no (verify local verde: eslint + clippy; sin push)
- **Decisiones:** `GROUPS` como destino (plan lo fija) · lint scoped primero (pre-mortem lo autoriza) · archivado diferido hasta STU-003 ✅ (DoD release del plan) · plan file no tocado (untracked del pipeline)
- **Problemas conocidos:** ninguno — clippy workspace verde pese a STU-001/002/003 pendientes (no hubo colateral)
- **Próxima tarea:** review por agente distinto → luego STU-003 — familia entity/scene con serde alias (bloqueante del archivado, no del rename)

## Review-append — dictamen P2-01 segunda opinión (2026-09-11, commit 5e7f6fd2)

### Dictamen
- **Veredicto:** ✅ APPROVE
- **Contrato:** PASÓ — verificado por el revisor con comandos propios (no auto-reporte): `git show 5e7f6fd2 -- web/src/components/vanta/footer.tsx` (2 líneas L24/L143) + grep `FOOTER_GROUPS` en `web/` → 0 hits + `npx eslint src/components/vanta/footer.tsx` desde `web/` → exit 0. Clippy workspace: no re-ejecutado (declarado arriba; sin archivos Rust en el diff, ortogonal al rename).
- **DoD:** Task ✅ (rg 0 hits + eslint exit 0 + render intacto — 31 rutas sin tocar en el diff); Commit ✅ (atómico, 2 líneas + task file, diff limitado a `footer.tsx`); Release N/A-bump justificado (rename interno sin API pública) + cierre diferido correctamente (archivado bloqueado por STU-003 ⬜ PENDING — el task file lo codifica y el revisor lo confirma como correcto).

### Critical Issues
- Ninguno.

### Required Changes
- Ninguno.

### Optional
- Ninguno con peso: `GROUPS` es genérico pero vive en scope-función de 178L con 2 usos adyacentes y cero shadowing — la alternativa `NAV_GROUPS` diverge del plan sin ganancia (brainstorm: descartada, el plan fija `GROUPS` y el scope lo desambigua).

### Nits
- `footer.tsx:24` — el mensaje de commit real (`refactor: STU-004 FOOTER_GROUPS→GROUPS`) no usa el prefijo `refactor(web):` sugerido en el DoD-Commit del task file. Ya commiteado; no amerita amend. Alinear formato en STU-003.

### What's Done Well
- Discovery previo ejemplar: blast radius censado ANTES del edit (grep 2 hits, exports/imports mapeados, e2e revisado) — el rename entró sin fricción porque el costo se pagó por adelantado.
- Disciplina de scope: clippy como gate de campaña sin fixear colaterales ajenos en este PR; archivado explícitamente diferido por STU-003 en vez de cerrar la campaña a medias.
- Contrato mecánico real (`rg` 0 hits + eslint exit 0), no auto-reporte — el revisor pudo re-ejecutar el punto crítico.

### Alternativas evaluadas (brainstorm)
- `NAV_GROUPS` vs `GROUPS` (actual) — se descarta: diverge del plan (`docs/dev/plans/2026-09-11-stutter-interno.md:141` fija `GROUPS`) sin ganancia de claridad en scope-función.
- Full `npm run lint` vs scoped (actual) — scoped suficiente como gate del rename (pre-mortem plan L168 lo autoriza; `no-unused-vars`=error caza un rename a medias); full queda como evidencia opcional, no requerida.
- Re-ejecutar clippy workspace (52s) vs aceptar reporte — se acepta el reporte: el `--stat` del commit prueba 0 archivos Rust tocados; re-ejecutarlo no agrega señal sobre este rename.

### Recomendaciones (iterate)
1. (Única, no bloqueante) En STU-003 usar el formato de commit del DoD (`refactor(web): ...`) para consistencia de historial.

### Verification Story
- Tests/verification reviewed: yes — `rg` 0 hits re-verificado (grep `web/`), eslint scoped re-ejecutado exit 0, e2e sin referencias al símbolo (grep `web/e2e/` 0 hits). `tsc --noEmit` no corrido: innecesario — rename léxico cubierto por `rg` + parser de eslint.
- Build verified: N/A (cambio TSX sin build dedicado; lint + render intacto como gate; `npm run build` no exigido por el contrato del task).
- Contract checked: yes — comandos exactos del contrato ejecutados por el revisor (ver "Cómo se probó").
- Security checked: yes — sin superficie alterada (array estático interno, sin inputs, sin hrefs/handlers tocados en el diff); no se requiere `vanta-audit`.
- Red-team (supuestos rotos): ¿resto a medias? → `rg` 0 hits lo refuta. ¿colisión `GROUPS`? → 2 hits exactos, scope-función, lo refuta. ¿export/import externo? → único export es `Footer()`, firma intacta, lo refuta. ¿snapshot/e2e que atrape el nombre? → símbolo local no serializado, greps 0 hits, lo refuta. ¿cambio de render? → diff solo toca identificador, rutas/labels/JSX intactos, lo refuta.

# C2M3: romper ciclos reales sdk↔planner y sdk↔query con módulo neutral

## Metadata
- **Plan file:** docs/dev/plans/2026-09-13-cleanCA-fase2.md (Wave 5, Task 16: M3)
- **Creado:** 2026-09-13
- **last-synced:** 2026-09-13
- **Estado:** ✅ COMPLETE (3/3 steps, 2026-09-13, sin commit — commitea el lead)
- **Ruta:** vanta-worker · **Appetite:** 1d · 🟡 · 🟠
- **Alcance:** `src/planner.rs`, `src/query.rs`, `src/sdk/search/*`, `src/parser/grammar.rs` (lectura+1 import), NUEVO `src/search_profile.rs` + `src/sdk/search/fusion.rs`; toques mecánicos en `src/lib.rs`, `src/sdk/types/search.rs`, `src/sdk/serialization/vector_types.rs`, `src/api/scores.rs`. Prohibidos: storage↔index, resto src/, WIP ajeno (`.opencode` M, `desktop/src-tauri/Cargo.lock` M — NO tocar).
- **SDP / SKILLS_CARGADAS:** campaign-executor | progreso | systematic-debugging | test-driven-development | code-review-and-quality | doubt-driven-development | source-driven-development | planning-and-task-breakdown | codebase-memory | api-and-interface-design

## Blast Radius
- `planner.rs` (987L): dueños query-owner; 221 callers engine-wide. Se extraen 8 fns puras + helper + tests (L97-227, L489-700 aprox) → `sdk/search/fusion.rs`. Quedan `optimize_and_compile`, CBO/Volcano, `SearchRoute`, thresholds.
- `query.rs` (803L): 1 import (L7) → neutral. `RelOp` (L138) NO se mueve (ver Evaluación).
- `sdk/search/`: 39 usos `crate::planner::*` en 6 archivos → rutas locales `fusion::`.
- `sdk/types/search.rs`: defs `SearchProfileMode/Config` (L62-90) → neutral + `pub use` (BND-04).
- Entrantes: `api/scores.rs` (RRF_K), `cost_estimator.rs` (HIGH_SELECTIVITY_THRESHOLD — se queda), `executor.rs` (`optimize_and_compile` — se queda), `parser/grammar.rs` + `parser/mod.rs`, `sdk/serialization/vector_types.rs`, `tests/query_result_advanced.rs` (vía re-export, sin cambio).
- Implicaciones: sin cambio semántico (moves puros); API pública preservada vía re-exports; sin hot-path lógico nuevo.

## Impacto mapeado (Regla 0)
- **Leídos completos:** `src/planner.rs` L1-250 + imports tests (vía Read+rg), `src/query.rs` L1-60, `src/sdk/search/lexical.rs` L1-40, `src/sdk/types/search.rs` L1-120, `docs/dev/tasks/C2M1.md`, `docs/dev/architecture/BOUNDARIES.md` (BND-03/BND-04), plan Fase 2 §M3.
- **Referencias hacia dentro (nuevo código):** `search_profile.rs` → std+serde solo (hoja). `sdk/search/fusion.rs` → `crate::search_profile::*` + `crate::sdk::serialization::vector_types::*` (interno sdk) + `tracing`.
- **Referencias entrantes:** planner/query/sdk/parser/api importan neutral; nadie importa `fusion` fuera de sdk (verificado: únicos usuarios externos de fns eran sdk/search; benches solo sim local).
- **Veredicto:** impacto acotado (~13 archivos, mecánico, reversible por revert). Gate D: GO.

## DISCOVERY — re-scope exacto tras M1 (contrato)
- **M1 handoff (C2M1.md):** coupling NO muestra sdk↔planner como 2-ciclo; cluster real engine↔config↔sdk::serialization↔builder. `cargo modules --acyclic` exit 1 SOLO por self-edge artefacto `GraphAccumulator↔new` (se detiene en el primer ciclo → no prueba ausencia).
- **Re-medición propia (rg use-graph, 2026-09-13):**
  - (a) sdk↔planner REAL a nivel file: `planner.rs:23` importa 4 tipos de sdk + arms `322-324` usan `SearchProfileMode` + doc L110; `sdk/search/{mod,explain,debug_ops,hybrid}.rs` + `sparse.rs` + `vector.rs` llaman `crate::planner::*` en **39 sitios** (plan decía ~30).
  - (b) sdk↔query REAL a nivel file: `query.rs:7` importa `sdk::SearchProfileConfig`; `sdk/search/lexical.rs:9` importa `query::RelOp`.
  - grammar→sdk→builder confirmada **unidireccional** (builder no importa grammar; nada en sdk importa parser) — NO es ciclo, no se toca salvo import L24.
- **HALLAZGOS vs plan:** conteo 39 (no ~30); import planner es 4 tipos concretos (no "sdk"); RRF_K lo usa también `api/scores.rs` → consts deben ir a neutral o la arista sdk→planner persiste.
- **Evaluación RelOp:** se QUEDA en `query.rs` (predicado del dominio query; planner lo consume en firmas L257/329 — dirección correcta planner→query). Tras mover `SearchProfileConfig`, `lexical→query` queda unidireccional correcta. Sin move.
- **Evaluación fns planner::\*:** INVERSIÓN (no neutral): las 8 fns operan puramente sobre tipos sdk (`MemorySearchRequest/Hit`, `HybridFusionReport`) → su dueño natural es sdk. Se mueven a `sdk/search/fusion.rs`; planner deja de importar sdk por completo (verificado: fuera de fns movidas + arms Mode, planner no usa tipos sdk en código non-test).
- **Evaluación consts:** `RRF_K`, `CANDIDATE_MULTIPLIER`, `MIN/MAX_CANDIDATE_BUDGET` → neutral `search_profile.rs` (si quedaran en planner, `fusion.rs` reintroduciría arista sdk→planner). `HIGH_SELECTIVITY_THRESHOLD` se QUEDA en planner (solo la usa `cost_estimator.rs`, unidireccional). `api/scores.rs` actualiza import RRF_K → neutral.
- **Box/re-export cosmético:** NO — moves reales de definición + actualización de importadores participantes. Los `pub use` son solo compat API (BND-04), no ocultan aristas (participantes usan ruta neutral).
- **storage↔index:** NO se toca (requiere task propia; BOUNDARIES §3 ya documenta plan/DEFER).

## Contrato
"re-scope exacto en DISCOVERY tras M1 + módulo neutral con SearchProfileConfig+SearchProfileMode (+ RelOp/fns evaluados) + suites parser/sdk/planner verdes + acyclic sin ciclos sdk/planner/query (artefacto accumulator preexistente exceptuado, BND-03)".

## Diseño
```
NEW  src/search_profile.rs      hoja neutral: SearchProfileMode, SearchProfileConfig,
                                RRF_K, CANDIDATE_MULTIPLIER, MIN/MAX_CANDIDATE_BUDGET
NEW  src/sdk/search/fusion.rs   8 fns + helper + tests (desde planner.rs)
EDIT src/lib.rs                 `pub mod search_profile;`
EDIT src/planner.rs             elimina fns/tests movidos; `use crate::search_profile::SearchProfileMode`;
                                arms 322-324 + doc L110 a ruta neutral; SIN `use crate::sdk`
EDIT src/query.rs                L7 → `use crate::search_profile::SearchProfileConfig;`
EDIT src/parser/grammar.rs       L24 → neutral
EDIT src/parser/mod.rs           test L19 → neutral (opcional mecánico; re-export lo cubriría)
EDIT src/sdk/types/search.rs     elimina defs → `pub use crate::search_profile::{SearchProfileConfig, SearchProfileMode};`
EDIT src/sdk/serialization/vector_types.rs  L4 → neutral
EDIT src/sdk/search/{mod,explain,debug_ops,hybrid,sparse,vector}.rs  `crate::planner::X` → `super::fusion::X`
EDIT src/api/scores.rs           RRF_K → neutral
```

## Herramientas
- bash: `cargo check -p vantadb`, `cargo test -p vantadb` (parser/sdk/planner filters), `cargo clippy -- -D warnings`, `cargo fmt`, `cargo modules dependencies --lib -p vantadb --acyclic` (best-effort; timeout conocido), `rg` edge-audit (primario)
- codegraph_explore + detect_changes (si MCP disponible; sino rg — adaptador §10: sin campaign MCP)

## Steps
### Step 1: hoja neutral search_profile.rs + re-exports + importadores tipos
- **Archivos:** NEW `src/search_profile.rs`; EDIT `src/lib.rs`, `src/sdk/types/search.rs`, `src/query.rs`, `src/parser/grammar.rs`, `src/sdk/serialization/vector_types.rs`, `src/api/scores.rs`
- **Acción:** crear hoja (tipos + consts movidos, docs intactas); `pub use` BND-04 en types/search.rs; actualizar imports participantes a ruta neutral; api/scores RRF_K → neutral
- **Verify:** `cargo check -p vantadb` ✅ (cero warnings tras quitar import duplicado/node) + `cargo check --tests` ✅
- **Estado:** ✅ COMPLETE (2026-09-13)

### Step 2: inversión fns planner → sdk/search/fusion.rs + limpieza planner
- **Archivos:** NEW `src/sdk/search/fusion.rs`; EDIT `src/sdk/search/mod.rs` (+6 call-sites), `explain.rs`, `debug_ops.rs`, `hybrid.rs`, `sparse.rs`, `vector.rs`, `src/planner.rs` (delete fns+tests, import neutral, doc-links)
- **Acción:** mover 8 pub fns + `apply_rrf_contributions` + tests budget/fuse/profile; re-mapear 39 call-sites a `super::fusion::`; planner sin `use crate::sdk`
- **Verify:** `cargo check -p vantadb` ✅ + `rg "crate::planner::" src/sdk/` → 0 ✅ + `rg "crate::sdk" src/planner.rs src/query.rs` (non-test) → 0 ✅ + fusion 25 tests ✅
- **Estado:** ✅ COMPLETE (2026-09-13)

### Step 3: suites + acyclic + cierre
- **Archivos:** ninguno nuevo (verify + este task file)
- **Acción:** `cargo test -p vantadb --lib planner::` (10 ✅) + `sdk::` (438 ✅) + query (77 ✅) + parser (117 ✅) + scores (11 ✅) + fusion (25 ✅); `clippy --all-features -D warnings` ✅; `cargo fmt` ✅; `cargo modules ... --acyclic` → exit 1 SOLO por artefacto accumulator preexistente (BND-03; sin ciclos sdk/planner/query) + edge-audit rg como evidencia primaria
- **Verify:** contrato completo ✅
- **Estado:** ✅ COMPLETE (2026-09-13)

## Dependencias
- M1 (números — usado para re-scope) · A1 (BOUNDARIES BND-03/BND-04 citados). Wave 5. nextTask C2S6-diseño.

## Notas
- Gate D: GO (blast acotado, moves puros, API preservada, reversible).
- `ponytail:` un módulo neutral (no dos) + inversión en vez de trait-split — lo mínimo que rompe ambas aristas.
- WIP ajeno en worktree (`.opencode`, `desktop/...`) — no tocar, no incluir.

## Context Save Point
- **Fecha:** 2026-09-13 · **Branch:** develop · **Decisiones:** inversión fns→sdk + hoja search_profile (tipos+consts); RelOp se queda; storage↔index no se toca · **Problemas:** `cargo modules --acyclic` timeout 180s en este Windows (usar best-effort + rg-audit) · **Próxima tarea:** C2S6-diseño

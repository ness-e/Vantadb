# UX-A11Y-01: Slice a11y desktop (teclado + focus + labels)

## Metadata
- **Plan file:** docs/dev/plans/2026-09-07-backlog-triage.md (Task 8)
- **Fuente:** Backlog UX-02/UX-03/UX-04 (UX-07/UX-14 ya resueltos — NO tocar)
- **Esfuerzo:** 🟡 1d (real: ~2-3h — 2 archivos + 1 test)
- **Prioridad:** 🟠
- **Tipo:** Frontend (desktop Tauri React)
- **Turns estimados:** 6
- **Creado:** 2026-09-07
- **last-synced:** 2026-09-07
- **Estado:** ✅ COMPLETED (código + tests + verify; commit delegado a vanta-lead por regla de tarea)
- **Incógnitas (uphill):** 0 (encaje del hook verificado: ambos modales ya lo usan)
- **Pendientes (downhill):** 3 steps
- **Branch:** develop (trabajo en worktree; commit vía vanta-lead)
- **SDP:** frontend-ui-engineering, incremental-implementation, test-driven-development, context-engineering, systematic-debugging + ponytail(full)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `layout/WorkspaceShell.tsx:1118,1133` (único montaje de ImportPaste/ImportDrop, lazy) |
| Callees | `ingest/parseImport.ts` (parseImport/parseImportFile/runImport), `ingest/useModalFocus.ts` (trap), `../../vanta` (ingestBatch) |
| Implicaciones | Sin cambio de contrato público; sin cambio wire/bridge; sin perf/memoria; sin migración. Solo UI confirm inline en 2 modales. Tests existentes no tocan estos modales (ningún *.test.* los importa). |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `desktop/src/components/ingest/ImportPaste.tsx` (301L), `desktop/src/components/ingest/ImportDrop.tsx` (337L), `desktop/src/components/ingest/useModalFocus.ts` (51L), `desktop/src/components/IngestForm.tsx` (240L), `desktop/src/components/ResultsList.tsx` (86L), `desktop/src/components/DataExplorer.tsx:830-949` (grid), `desktop/src/components/memory/MemoryLens.test.tsx:1-80` (patrón test), `desktop/vitest.config.ts`, `desktop/package.json`
- **Archivos referenciados hacia dentro (imports):** parseImport.ts, useModalFocus.ts, ../../vanta (ingestBatch, vantaErrorMessage), lucide-react
- **Archivos que referencian a los editados (entrantes):** solo `layout/WorkspaceShell.tsx` (lazy import + montaje); ningún test los importa; `App.css:4` mención comentario
- **Veredicto impacto:** BAJO — 2 archivos editados (patrón existente IngestForm confirming), 1 test nuevo. Grid UX-02 + trap UX-03 + labels UX-04 ya implementados → NO se tocan (DataExplorer, ResultsList, useModalFocus, IngestForm solo lectura).

## Contrato

Playwright teclado: Tab llega al grid → Enter abre Inspector (ya implementado DataExplorer.tsx:924-926, ResultsList.tsx:68-69); Tab no escapa de ImportPaste/ImportDrop y foco se restaura al cerrar (ya implementado vía useModalFocus); IngestForm tiene `<label>` visibles + error inline + 0 `window.confirm` en el path (IngestForm.tsx:131-168, gap: ImportPaste.tsx:89 + ImportDrop.tsx:107 con `window.confirm` → reemplazar por confirm inline); `npx tsc --noEmit` + `vitest run` en `desktop/` verdes.

## Spec (SDD — Phase 1b)

No es feature-add: no agrega `pub fn`/endpoint/binding/componente/página/capability nuevos. Son 2 edits internos (estado `confirming` local, mismo patrón IngestForm.tsx:36-37,169-192). Sin decisiones abiertas → sin ronda question. Patrón a seguir: `IngestForm.tsx:169-192` (role=alert + SOBRESCRIBIR/CANCELAR, `pendingRef`, sin `window.confirm`).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** trap de foco + Escape + restore intactos (no duplicar useEffect Escape, adaptar hook existente); grid virtualizado: scope paginación actual; no tocar tokens/layout (UX-06/UX-12 fuera de scope → DEFER); no tocar archivos BND-12/TS-09 (Wave1 paralela); no tocar plan file (solo este task file).
- **Comandos de verificación:** `npx tsc --noEmit` (desktop/) exit 0; `npx vitest run src/components/ingest/ImportConfirm.test.tsx` verde; `grep -rn "window.confirm" desktop/src/components/ingest/` → 0; suite `npx vitest run` sin regresiones.
- **Deuda pendiente:** Playwright teclado E2E permanente → UX-19 (follow-up DEFER, no en este task).

## Steps

| # | Step | Archivo(s) | Verify mecánico | Estado |
|---|------|-----------|-----------------|--------|
| 1 | RED test + GREEN ImportPaste: confirm inline (elimina `window.confirm:89`) | `ingest/ImportPaste.tsx`, `ingest/ImportConfirm.test.tsx` (nuevo) | `npx vitest run src/components/ingest/ImportConfirm.test.tsx` (falla antes, pasa después) | ✅ COMPLETED (RED 2 fallos causa correcta → GREEN 2/2) |
| 2 | GREEN ImportDrop: mismo patrón confirm inline (elimina `window.confirm:107`) + extiende test | `ingest/ImportDrop.tsx`, `ingest/ImportConfirm.test.tsx` | `npx vitest run src/components/ingest/ImportConfirm.test.tsx` verde | ✅ COMPLETED (3/3) |
| 3 | VERIFY contrato full: tsc + suite + grep 0 confirm + sync task file (SIN commit — regla de tarea) | — | `npx tsc --noEmit` + `npx vitest run` + `grep window.confirm ingest/` → 0 | ✅ COMPLETED (tsc 0, foco 3/3, `window.confirm(` → 0; full 59/72 — 13 fallos pre-existentes ambientales localStorage en archivos no tocados) |

## Iteraciones

| # | Acción | Resultado | Herramienta |
|---|--------|-----------|-------------|
| 1 | DISCOVERY: plan + 6 archivos clave + grep entrantes + coverage + detect_changes | Task file creado con Impacto Regla 0; gap único real: `window.confirm` en 2 modales (resto UX-02/03/04 ya implementado) | codegraph/detect_changes/coverage |
| 2 | Step 1 RED→GREEN ImportPaste confirm inline | RED 2 fallos causa correcta (confirm llamado, sin role=alert) → GREEN 2/2 | vitest focado |
| 3 | Step 2 GREEN ImportDrop confirm inline + test Drop | 3/3 verde | vitest focado |
| 4 | Step 3 VERIFY full: tsc 0 + `window.confirm(` 0 + suite | tsc ✅ · foco 3/3 ✅ · full 59/72 (13 pre-existentes ambientales) · SIN commit (regla) | tsc/vitest/grep |

## Notas

- Pre-mortem plan: UX-04 fue aplicado y sobrescrito → re-aplicar desde cero con test (Step 1 RED primero). Hook encaja (ambos modales ya lo usan) → no duplicar. Grid virtualizado → scope paginación actual + nota (sin cambios al grid en este task).
- Si requiere rediseño visual (contraste/jerarquía) → fuera de scope, DEFER con nota.

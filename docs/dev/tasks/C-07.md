# C-07: Curar PRs/ramas (211–219 + resto)

## Metadata
- **Plan file:** `docs/dev/plans/2026-09-20-estabilizacion-total.md` (§C-07) + `docs/dev/Backlog.md` P57
- **Fuente:** Backlog P57 (fila `C-07`) — decisión owner D5 2026-09-20: "el lead cura (cerrar con motivo, mergear útiles, borrar ramas)"
- **Esfuerzo:** 🟡 (real ~1h; el 25→0 previo ya se hizo 2026-09-22)
- **Prioridad:** 🟡
- **Tipo:** CI/CD / DevOps (`campaign_detect_task_type` → `devops`)
- **Turns estimados:** 15-30
- **Creado:** 2026-09-25
- **last-synced:** 2026-09-25
- **Estado:** ✅ COMPLETED (2026-09-25)
- **Incógnitas (uphill):** 0
- **Pendientes (downhill):** 1 (review/cierre)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | Ninguno (operación GitHub remota; sin código) |
| Callees | `origin` branches (8 release-plz stale), 2 ramas locales merged |
| Implicaciones | No cambia código/contratos; reduce ruido del repo; NO toca PR #222 (vivo) ni develop/main |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** plan `2026-09-20-estabilizacion-total.md` §C-07; salidas `gh pr list` (open) + `git ls-remote --heads origin` + `git branch`.
- **Referencias hacia dentro/entrante:** n/a (sin ediciones de archivos de código).
- **Veredicto impacto:** **bajo** — borrado de ramas stale auto-generadas (release-plz) sin PR abierto; SHAs capturados como evidencia pre-borrado.

## Contrato
> "`git ls-remote --heads origin` → solo `develop`/`main` (+ ramas con PR abierto, hoy 0) y `gh pr list --state open` → solo PRs vivos (#222); evidencia de SHAs pre-borrado registrada en avance."

## Spec (SDD)
N/A — no es feature-add (cero símbolos/contratos nuevos; operación de higiene de repositorio).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** no borrar ramas con PR abierto; no tocar `develop`/`main` ni PR #222; capturar SHA de cada rama antes de borrar (restaurabilidad); locales NO-merged se conservan (backups/experimentos).
- **Comandos de verificación:** `git ls-remote --heads origin` (solo develop/main) + `gh pr list --state open` (solo #222).
- **Deuda pendiente:** ninguna.

## Recitation (canónico)

    contract:
      verificacion: `campaign_verify_cmd (c07-verify.py)` → passed=true ✅ ("origin=develop+main | open PRs=[222]")
      evidencia:
        - claim: 0 PRs stale (solo #222 vivo)
          evidencia: `gh pr list --state open` → 1 (#222, head develop, release PR)
          confianza: alta
        - claim: 8 ramas release-plz stale sin PR y no ancestros de develop
          evidencia: `git ls-remote --heads origin` (8 release-plz 2026-09-20..23) + `git merge-base --is-ancestor` → NO ×3 muestreadas + `gh pr list` → 0 PRs
          confianza: alta
        - claim: prune de refs locales stale (35 dependabot ya inexistentes en origin)
          evidencia: `git fetch --prune` → ~35 refs deleted (pre) + 8 tras borrado; `git branch -r` 46→3 (post-prune final)
          confianza: alta
      artefactos:
        - docs/dev/tasks/C-07.md
      invariantes: no borrar ramas con PR abierto; SHAs capturados; no tocar #222/develop/main
      deuda: ninguna
      queda_pendiente: ninguno

## Deuda técnica (Regla 6)
Sin deuda — operación de higiene.

## Definition of Done (multi-nivel)

| Nivel | Gate | Estado |
|-------|------|--------|
| **Task** | Contrato verificado (ls-remote + gh pr list) | ✅ (tras borrado) |
| **Commit** | Commit docs del cierre (avance + task file) | ✅ (al cierre) |
| **Release** | N/A (sin código) | N/A |

## Herramientas necesarias
- `gh` (pr list), `git` (ls-remote/fetch --prune/push --delete), campaign MCP (detect/discover/verify).

**Skills cargadas (SDP):** `campaign-executor` (base) · `progreso` (cierre) · `ci-cd-and-automation` (dominio) · `git-workflow-and-versioning` (higiene de ramas: "delete branches after merge", short-lived branches). SDP devolvió además shipping-and-launch/documentation-and-adrs/deprecation/observability → N/A (sin release/prosa/migración/métricas en esta tarea).

## Investigation Notes
- Historial: curación 25→0 el 2026-09-22; oleada 211–219 curada 2026-09-23; hoy el remoto solo tiene 8 release-plz stale (los ~35 dependabot ya no existen en origin; eran refs locales sin prune).
- Los 3 release-plz muestreados NO son ancestros de develop (bump/CHANGELOG superseded; release-plz regenera por corrida).

## Incógnitas (uphill) vs Pendientes (downhill)

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas | 0 |
| Pendientes de ejecución | 1 (review) → 0 |
| % completado | 100% discovery; ejecución pendiente de borrado |

## Fases explícitas — SECURITY | PERFORMANCE
- **SECURITY:** no aplica (sin trust boundaries ni secretos; SHAs públicos).
- **PERFORMANCE:** no aplica.

## Steps

### Step 1: Discovery PRs + ramas
- **Acción:** `gh pr list` + `git ls-remote --heads origin` + `git fetch --prune`
- **Verify:** 1 PR abierto (#222); 8 ramas stale; refs locales 46→11
- **Estado:** ✅ COMPLETED

### Step 2: Capturar SHAs pre-borrado (evidencia)
- **Acción:** `git ls-remote --heads origin` → guardar SHA de cada rama a borrar
- **Verify:** 8 SHAs registrados
- **Evidencia:** `ce164415` · `3fed845f` · `1b2debe9` · `551ce118` · `a413589e` · `302490d4` · `df6c01be` · `af105a7f` (release-plz 2026-09-20..23)
- **Estado:** ✅ COMPLETED

### Step 3: Borrar 8 ramas remotas stale + 2 locales merged
- **Acción:** `git push origin --delete <b>` ×8; `git branch -d archive/develop-pre-182 backup-develop-2026-09-03`
- **Verify:** `git ls-remote --heads origin` → solo develop/main; `git branch` sin las 2
- **Evidencia:** 8× `[deleted]` en remoto; locales borradas (`ccc8ee4e`, `bd22f387`); `campaign_verify_cmd` → passed=true ("origin=develop+main | open PRs=[222]")
- **Estado:** ✅ COMPLETED

### Step 4: Review P2-01 + cierre progreso
- **Estado:** ✅ COMPLETED

## Dependencias
- Ninguna (el 25→0 previo ya cerrado).

## Review (GATE — agente distinto, P2-01)
- **Revisor:** `vanta-review` (subagente, sesión `ses_f29221f22ffeO2aj2FtZblvEVb`) — ronda 1: **❌ REQUEST CHANGES** (1 Required: verify probaba cardinalidad, no nombres; 3 Optional: contadores stale, `46→11` desactualizado, registry campaign no encuentra bloques C-07/C-10 en el plan) → **aplicado**: verify v2 con `names == ["develop","main"]`; contadores y refs corregidos; script versionado en §Notas → re-review vía RESUME.
- **Enfoque:** ¿borrado seguro (sin ramas vivas)? ¿evidencia sostiene los claims?
- **Cómo se probó:** el revisor re-ejecutó `git ls-remote`/`gh api branches`/`gh pr list`; verificó 10/10 SHAs (8/8 remotos recuperables vía API), locales merged (2) vs no-merged (5 conservadas); dictamen: **sin pérdida viva**.
- **Checklist anti-hábitos:** sin invención de salidas · sin declarar done sin verificar · limitaciones documentadas (registry).
- **Veredicto:** ❌ (ronda 1) → cambios aplicados → **ronda 2: ✅ APPROVE** (gate P2-01 cerrado)

## Notas
- Ramas locales NO-merged conservadas a propósito: `backup-develop-squashed`, `backup-main-2026-08-28`, `content/old-06-blog-posts`, `test/default-all`, `web-split`.
- **Proceso (review):** el registry campaign (`campaign_get_task_detail`) no encuentra C-07/C-10 porque el plan `2026-09-20-estabilizacion-total.md` los registra como bullets, no como bloques `### Task N` parseables. Los contratos se verifican vía `campaign_verify_cmd` (wrapper) + scripts versionados aquí.
- **Verify versionado (contrato reproducible):**

```python
# c07-verify.py — exit 0 solo si origin == {develop, main} y el único PR abierto es #222
import json, subprocess, sys
ROOT = r"C:\Users\Eros\VantaDB Proyect\VantaDB"
heads = subprocess.run(["git","ls-remote","--heads","origin"], capture_output=True, text=True, cwd=ROOT).stdout.strip().splitlines()
names = sorted(l.split("refs/heads/")[-1] for l in heads if "refs/heads/" in l)
prs = subprocess.run(["gh","pr","list","--state","open","--json","number"], capture_output=True, text=True, cwd=ROOT).stdout.strip()
nums = [p["number"] for p in json.loads(prs or "[]")]
ok = names == ["develop","main"] and nums == [222]
print(("C-07 VERIFY PASS" if ok else f"C-07 VERIFY FAIL: names={names} prs={nums}"))
sys.exit(0 if ok else 1)
```

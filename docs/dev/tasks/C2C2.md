# C2C2: Documentar API pública bindings (TS 11 exports + Python stubs numpydoc + WASM baseline)

## Metadata
- **Plan file:** docs/dev/plans/2026-09-13-cleanCA-fase2.md (Wave 4, Task 14)
- **Creado:** 2026-09-13
- **last-synced:** 2026-09-13
- **Estado:** ✅ COMPLETED
- **Ruta:** vanta-docs (con vanta-worker para firmas — no se cambian firmas)
- **Tipo:** docs-only (cero lógica, cero firmas)
- **Gate D:** GO (docs-only, sin blast radius de lógica; solo JSDoc/docstrings auditados)
- **SDP:** campaign-executor, progreso, documentation-and-adrs, writing-guidelines, systematic-debugging, test-driven-development, code-review-and-quality, source-driven-development, planning-and-task-breakdown, codebase-memory

## DISCOVERY (zero-code planning)

### Alcance validado
- **Rust core ~100% documentado — NO se toca.**
- **TS gap real ≈11 exports acotados** (verificado por lectura directa 2026-09-13):
  - `errors.ts` (5): `ErrorJSON` (sin JSDoc), `DbError` (sin JSDoc), `DbError.constructor` (sin JSDoc), `DbError.toJSON` (sin JSDoc), `wrapWasmError` (sin JSDoc). Ya documentados: `ERROR_CODES`, `ErrorCode` parcial, `classifyWasmError`.
  - `guards.ts` (6): `isMemoryRecord`, `isSearchHit`, `isNodeRecord`, `isValidValue`, `isMetadata`, `isValidVector` (sin JSDoc). Ya documentados: `validateVector`, `_mapRecord`, `SearchRequestBase`, `buildSearchRequestBase`.
- **Python:** `vantadb_py/__init__.py` + `__init__.pyi` + `vantadb_py.pyi` ya con docstrings extensos; estilo numpydoc fijado (`Args:`/`Returns:`/`Example::` en `SearchRequest`, `put_batch_raw`). `ruff`/`pydocstyle` NO instalados en venv audit (`No module named`) — se audita por inspección + `test_stub_drift.py`.
- **WASM:** supuesto "40-50 sin `///`" INVERTIDO por spot-check + `cargo doc --no-deps -p vantadb-wasm` verde (2026-09-13, solo warning `dead_code` preexistente `txn.rs:158` fuera de scope). Con `RUSTDOCFLAGS="-D missing-docs"` también exit 0 → baseline verde, deny aplicable.
- **`@internal`:** `rg @internal vantadb-ts/src/` = 0 hits → `stripInternal` NO aplica (solo si hay `@internal` reales).

### Decisión de estilo
- **TS:** JSDoc con `@param`/`@returns`/`@throws`/`@example` (convención `documentation-and-adrs`).
- **Python:** numpydoc (`Summary`/`Parameters`/`Returns`/`Raises`/`Examples`) — ya usado en `SearchRequest`; se extiende solo donde falta sin reescribir lo sano.
- **WASM/Rust:** `///` con Summary/Arguments/Returns/Panics/Examples — ya verde, solo auditoría.

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `vantadb-ts/src/errors.ts` (117L), `vantadb-ts/src/guards.ts` (229L), `scripts/validate-docs-coverage.ps1` (197L), `vantadb-python/vantadb_py/__init__.py` (532L), `vantadb-python/vantadb_py/__init__.pyi` (273L), `vantadb-python/vantadb_py/vantadb_py.pyi` (120L head).
- **Referencias hacia dentro:** `errors.ts` ← `guards.ts` (`DbError`, `ERROR_CODES`), `vantadb.ts`/`native.ts` (consumen `wrapWasmError`, `_mapRecord`, `buildSearchRequestBase`); `__init__.py` re-exporta nativo `vantadb_py.pyd`.
- **Referencias entrantes:** tests TS (`errors`/`guards` snapshot strings — NO reword error strings de `_mapRecord`/`buildSearchRequestBase`); `tests/test_stub_drift.py` (paridad stubs vs nativo); `docs/api/ERROR_HANDLING.md` §1.1/§5.2 (contrato `VANTADB_*` + `toJSON`/`error_to_dict`).
- **Veredicto:** impacto docs-only; riesgo ≈0 lógica. PROHIBIDO cambiar firmas/lógica, resto del repo, WIP ajeno (`desktop/Cargo.lock`, `web/globals.css`, `docs/dev/tasks/C2A2.md` en `git status` — no tocar).

## Contrato
- `cargo doc --no-deps -p vantadb-wasm` warn primero (baseline ✅ verde 2026-09-13) y deny (`RUSTDOCFLAGS="-D missing-docs"`) cuando verde (✅ exit 0).
- JSDoc en 11 exports TS (5 errors.ts + 6 guards.ts); `stripInternal` solo si hay `@internal` reales (0 → no aplica).
- Docstrings numpydoc Python + stubs `.pyi` auditados.
- `scripts/validate-docs-coverage.ps1` verde (✅ 0 gaps 2026-09-13).

## Herramientas
- bash: `cargo doc --no-deps -p vantadb-wasm` (warn, luego deny), `./vantadb-ts/node_modules/.bin/tsc --noEmit`, `./vantadb-ts/node_modules/.bin/eslint`, `scripts/validate-docs-coverage.ps1`
- Verify por slice (TS → Python → WASM).

## Steps
### Step 1: Slice TS — JSDoc 11 exports (errors.ts 5 + guards.ts 6)
- **Archivos:** `vantadb-ts/src/errors.ts`, `vantadb-ts/src/guards.ts`
- **Acción:** agregar JSDoc (sin tocar firmas/lógica/strings snapshot). `ErrorCode` type-alias: 1 línea doc. `DbError` ctor/toJSON: `@param/@returns/@example`.
- **Verify:** `./vantadb-ts/node_modules/.bin/tsc --noEmit --project vantadb-ts/tsconfig.json` + eslint 2 archivos
- **Estado:** ✅ COMPLETED (tsc exit 0, eslint exit 0; 2026-09-13)

### Step 2: Slice Python — auditoría numpydoc + stubs .pyi
- **Archivos:** `vantadb-python/vantadb_py/__init__.py`, `*.pyi` (solo si hay gap; `lib.rs` docstrings ya existentes no se tocan salvo gap)
- **Acción:** spot-check numpydoc (`error_to_dict`, `asdict`, `AsyncClient` métodos sin doc → evaluar 1-liner solo si es gap real; si todo sano → auditar sin editar)
- **Verify:** `test_stub_drift.py` (si venv disponible) + inspección; `ruff`/`pydocstyle` documentados como no-instalados si no corren
- **Estado:** ✅ COMPLETED (py-syntax OK + test_stub_drift 7 passed; ruff/pydocstyle no instalados — auditoría por inspección; numpydoc en `error_to_dict`/`asdict`; 2026-09-13)

### Step 3: Slice WASM — baseline cargo doc warn→deny + cierre
- **Archivos:** `vantadb-wasm/src/lib.rs` (lectura/auditoría; editar solo si `deny` revela gap)
- **Acción:** `cargo doc --no-deps -p vantadb-wasm` + `RUSTDOCFLAGS="-D missing-docs"` + `validate-docs-coverage.ps1` + `cargo fmt --check` (docs-only)
- **Verify:** contrato completo verde
- **Estado:** ✅ COMPLETED (warn exit 0, `-D missing-docs` exit 0, validate-docs-coverage 0 gaps; vitest no arranca en este entorno — preexistente, docs-only sin lógica; 2026-09-13)

## Dependencias
- Wave 4, sin dependencias duras; nextTask C2A2.

## Notas
- Adaptador §10 vigente: PROHIBIDO campaign MCP; glob-antes-de-Read; verify en bash; NO commitear (commitea el lead); Gate D GO (docs-only).
- BOUNDARIES.md: se cita solo si aplica regla (no se crea nada nuevo aquí).

## Context Save Point
- **Fecha:** 2026-09-13
- **Branch:** develop
- **Baseline medido:** cargo doc warn ✅ / deny ✅ / validate-docs-coverage ✅ 0 gaps / tsc ✅ / eslint ✅ / `@internal` 0 hits
- **Próxima tarea:** C2A2

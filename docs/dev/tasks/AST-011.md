# AST-011 — Verify final + cierre

## Metadata
- **Plan file:** docs/dev/plans/2026-09-11-anti-stutter-cierre.md (Task 4, Wave 2, última)
- **Fuente:** plan Task 4 + recitations AST-008/009/010 + shipping-and-launch §2a/2b + ADR-041
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🔴
- **Tipo:** Release/Verify (solo lectura + comandos verify; cero ediciones de código salvo archivado del plan y avance)
- **Turns estimados:** 5-10
- **Creado:** 2026-09-11T12:00
- **last-synced:** 2026-09-11T12:00
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps restantes (6/6 ✅ + S0 rg)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | ninguno — tarea de verificación, no expone API; consumidores del resultado: orquestador/lead (archivado del plan) |
| Callees | `release-plz.toml` (lectura: semver_check=true, publish ordenado), `deny.toml` (lectura: advisories RUSTSEC-2023-0089/0071 con owner/expiry, licenses MIT/Apache-2.0, bans), gates heredados AST-008/009/010 (check/fmt/clippy/tsc/vitest280/pytest135/nextest-mem10) |
| Implicaciones | contrato público: sin cambio (solo re-verifica post-borrado AST-010). Sin cambio de comportamiento, wire, performance ni serialización. NO publicar, NO tags manuales, NO CHANGELOG a mano (release-plz owns). Si deny falla por advisory nuevo → triage con owner/expiry como AST-007, nunca silenciar |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `release-plz.toml` (38L: semver_check=true, changelog, vantadb→docs/CHANGELOG.md, wasm release=false), `deny.toml` (73L: 2 advisories ignorados con owner/expiry, licenses allow, bans skip hashbrown/windows-sys/syn/thiserror/rand), `docs/dev/plans/2026-09-11-anti-stutter-cierre.md` (217L: 4 tasks, AST-008/009/010 ✅, AST-011 ⬜), `docs/dev/architecture/adr/041_anti_stutter.md` (firma humana pendiente), `docs/dev/tasks/AST-010.md` (deuda FIND-01/02/03 + ADR-041 firma), `.opencode/task-system/prompts/pipeline-full.md` (283L: flujo DISCOVERY→CIERRE + RESULTADO §7)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** release-plz.toml → docs/CHANGELOG.md (vantadb), semver_check gate; deny.toml → advisories DB (RUSTSEC), licenses, bans skip-tree
- **Archivos que referencian a los editados (referencias entrantes):** plan file ← AST-008/009/010 task files + recitations; deny.toml ← CI (`cargo deny check` en Fast Gate + verify.ps1); release-plz.toml ← GitHub Actions release workflow. Archivado del plan mueve `docs/dev/plans/2026-09-11-anti-stutter-cierre.md` + `.budget.json` → `docs/dev/plans/archive/` (única escritura de archivos fuera del task file)
- **Veredicto impacto:** BAJO (solo lectura + comandos verify + 1 mv de archivado + avance). Sin ediciones de código. Rollback: archivado reversible con `git mv` inverso; verify sin efectos laterales

## Contrato
`cargo deny check && cargo fmt --check -p vantadb && cargo check -p vantadb` (contrato literal del plan) + nextest workspace + tsc + pytest smoke + coverage docs verdes post-borrado.

## Spec (no aplica — verify/cierre, sin símbolos públicos nuevos)

| # | Decisión | Opciones (+tradeoff) | Default recomendado | Resuelto |
|---|----------|----------------------|--------------------|----------|
| 1 | Alcance verify | A) contrato literal solo / B) literal + nextest + tsc + pytest smoke + coverage (plan DETALLE §1,§6) | B | ✅ decidido-por-evidencia (ref: DETALLE §1 ordena gates extendidos) |
| 2 | Si deny falla por advisory nuevo | A) triage owner/expiry (patrón AST-007) / B) silenciar | A | ✅ decidido-por-evidencia (ref: DETALLE §2 + deny.toml:23-30 remoción documentada) |
| 3 | Publicar/tags/changelog | A) no tocar (release-plz owns) / B) manual | A | ✅ decidido-por-evidencia (ref: DETALLE §1 prohíbe explícitamente) |

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** `VantaHeader` on-disk intacto; `VANTADB_*` wire intactos; `name="VantaError"` wire intacto; historia `docs/tasks|plans` inmutable (solo archivar plan, no editar historia); prosa producto `VantaDB` intacta; superficie canónica AST-008/009/010 intacta (`Client/Record/SearchHit/ListResult/search/search_vector`, `isValidValue/isMetadata`, 0 aliases); sin push, sin publish, sin tags manuales, sin CHANGELOG a mano
- **Comandos de verificación:** `cargo deny check` · `cargo fmt --check -p vantadb` · `cargo check -p vantadb` · `cargo nextest run --profile audit --workspace --build-jobs 2` (aceptar duración) · `npx tsc --noEmit -p vantadb-ts/` · pytest smoke · `scripts/validate-docs-coverage.ps1`
- **Deuda pendiente:** FIND-AST010-01 integrations/* (12+) + FIND-AST010-02 examples/python/bench+5TS/desktop/vantadb-node (owner lead/anexos, releases propios) · FIND-AST010-03 search_vector gap PYTHON_SDK.md (owner docs/lead) · firma humana ADR-041 (owner humano) — heredadas, no introducidas aquí

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | AST-011 — Verify final + cierre |
| `lastAction` | (se actualiza por step) |
| `result` | PARTIAL (steps pendientes) → OK al cierre |
| `nextAction` | Próximo step ⬜ PENDING (archivo + comando) |
| `contract` | ## Contrato + ## Invariantes de dominio + evidencia/artefactos (formato abajo) |
| `nextTask` | ninguno (última del plan; archivar) |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** 0 — esta tarea no edita código (solo lectura + verify + archivado + avance). No introduce deuda nueva. Deuda heredada con owner (no introducida aquí):
- `FIND-AST010-01` integrations/* → owner: lead
- `FIND-AST010-02` examples/python/* + bench.mjs + 5 examples TS + desktop/ + vantadb-node → owner: lead/anexos
- `FIND-AST010-03` search_vector gap docs coverage → owner: docs/lead
- Firma humana ADR-041 → owner: humano
- Gate P OD-2/B ya resuelto (decisión B ejecutada en AST-008/009/010)

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | contrato literal verde + nextest + tsc + pytest smoke + coverage + 0 usos internos nombres viejos (rg AST-010) |
| **Commit** | N/A archivado (git mv plan + budget, sin push — lead pushea; task file AST-011 se commitea con el archivado si aplica) — justificado en Notas |
| **Release** | retrospectiva Start/Stop/Continue + archivar en `docs/dev/plans/archive/` + skill progreso + Gates D/V/C abajo; NO publicar (prohibido por DETALLE) |

## Herramientas necesarias
- `cargo deny check` + `cargo fmt --check -p vantadb` + `cargo check -p vantadb` + `cargo nextest run --profile audit --workspace --build-jobs 2` + `npx tsc --noEmit -p vantadb-ts/` + pytest smoke + `scripts/validate-docs-coverage.ps1` + `campaign_verify_cmd` (si rechaza por ambigüedad de planes, bash con evidencia + lesson como AST-009/010) + rg contrato AST-010

**Skills cargadas (SDP):** campaign-executor (base task-system) · shipping-and-launch (keyword release + refs §2a/2b pre-launch/rollout) · git-workflow-and-versioning (keyword release + no-tags-manuales/no-changelog-mano) · progreso (cierre: archivar plan + avance) · doubt-driven-development (lifecycle BUILD stakes altos + gate review P2-01) · source-driven-development (lifecycle BUILD) · incremental-implementation/test-driven-development/context-engineering/api-and-interface-design (lifecycle BUILD, base del discover) · frontend-ui-engineering descartada (sin `web/`)
**SDP:** `campaign_discover_skills_v2 archivosClave="release-plz.toml deny.toml" phase="BUILD" contractKeywords=[release,verify,retrospective] maxSkills=8` → 8 candidatas (base+lifecycle, 0 manifest); +2 keyword-mapped (shipping-and-launch, git-workflow-and-versioning) y progreso por cierre

## Investigation Notes
- **Código (re-verificación AST-010):** `rg -n "Vanta[A-Z]\w+" src/ vantadb-ts/src/ vantadb-python/vantadb_py/__init__.py | grep -v "VantaHeader\|VANTADB_"` debe dar 0 tras borrado AST-010 (commit 1a566ef3: 38 pub type + re-exports + TS/Python/d.ts aliases + compat-notes fuera; exclusiones intactas: VantaHeader/VANTADB_*/name-wire/prosa/historia). Se re-corre aquí como S0. `release-plz.toml`: semver_check=true (gate pre-publish), changelog docs/CHANGELOG.md, wasm release=false. `deny.toml`: 2 ignores documentados (RUSTSEC-2023-0089 atomic-polyfill transitivo postcard→heapless; RUSTSEC-2023-0071 rsa Marvin inalcanzable HS256-only) + remoción documentada RUSTSEC-2026-0253 (advisory-not-detected) — patrón triage AST-007
- **Problema:** cierre de campaña 4/4 (AST-008/009/010 ✅ COMPLETED en develop, sin push). Esta tarea es la última (Wave 2): gates finales post-borrado + retrospectiva + archivado + progreso. Cynefin 🟦 obvio
- **Internet:** ninguna (prohibido por DETALLE §9; todo es código propio + gates locales)
- **Referencias:** shipping-and-launch §2a (pre-launch checklist adaptada: deny/fmt/check/nextest/tsc/pytest/coverage) + §2b (rollback: revert + re-publish patch; N/A publish aquí) · ADR-041 firma humana pendiente = deuda (no bloquea verify) · FIND-01/02/03 AST-010 heredadas · Gate P OD-2/B resuelto

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — approach mecánico (gates + archivado), decisiones cerradas (Spec), owners asignados |
| Pendientes de ejecución (downhill) | 6 steps (S0 rg 0-nombres · S1 contrato deny/fmt/check · S2 nextest · S3 tsc+pytest · S4 coverage · S5 retrospectiva+archivar+progreso · S6 gates+cierre) |
| % completado | 0% |

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — no toca trust boundaries (solo lectura + verify; deny.toml lectura, triage documentado si advisory nuevo). Sin `security-and-hardening` (justificado: sin superficie nueva, sin deps nuevas; `cargo deny check` ES el gate de seguridad aquí)
- [x] **PERFORMANCE** — no toca hot paths (sin `vector/`, engine, loops, serialización). Sin `performance-optimization` (justificado: verify-only, 0 runtime). nextest acepta duración (pre-mortem plan)

## Steps

### Step S0: Confirmar 0 usos internos nombres viejos (rg AST-010)
- **Archivos:** `src/`, `vantadb-ts/src/`, `vantadb-python/vantadb_py/__init__.py`
- **Acción:** correr el rg contrato AST-010 con filtro de exclusiones y reportar conteo
- **Verify:** `rg -n "Vanta[A-Z]\w+" src/ vantadb-ts/src/ vantadb-python/vantadb_py/__init__.py | grep -v "VantaHeader\|VANTADB_" | wc -l` → `0`
- **Estado:** ✅ COMPLETED (scoped 0/0/0/0/0: `pub type Vanta` 0, `export Vanta` 0, alias `VantaDB` 0, puente py 2 líneas = solo comentarios/prosa `__init__.py:50,80`, compat-notes 0; literal 129 = 100% categorías excluidas: prosa producto, `name="VantaError"` wire, `NativeVantaDB`/`VantaDb`/`VantaMetadata` dueño vantadb-node)

### Step S1: Contrato literal (deny + fmt + check)
- **Archivos:** `release-plz.toml`, `deny.toml` (lectura)
- **Acción:** `cargo deny check && cargo fmt --check -p vantadb && cargo check -p vantadb`; si deny falla por advisory nuevo → triage owner/expiry (patrón AST-007), nunca silenciar
- **Verify:** exit 0 en los tres
- **Estado:** ✅ COMPLETED (deny: advisories/bans/licenses/sources ok, solo warning duplicate thiserror-impl = "warn codificado" esperado; fmt exit 0; check Finished 4.34s sin errores)

### Step S2: nextest workspace (aceptar duración)
- **Archivos:** workspace
- **Acción:** `cargo nextest run --profile audit --workspace --build-jobs 2`
- **Verify:** todos pasan (exit 0)
- **Estado:** ✅ COMPLETED (3142 passed, 1 skipped, 246s, exit 0 — log en Temp/opencode/ast011-nextest.log)

### Step S3: tsc + pytest smoke
- **Archivos:** `vantadb-ts/`, `vantadb-python/tests/`
- **Acción:** `npx tsc --noEmit -p vantadb-ts/` + pytest smoke (`test_sdk.py -q` o suite smoke acordada)
- **Verify:** tsc exit 0 + pytest smoke verde
- **Estado:** ✅ COMPLETED (tsc exit 0 desde vantadb-ts/ — nota AST-004 pkg dir; pytest test_sdk.py 75 passed 52.8s; warning pre-existente `vantadb_py` import-name deprecated, dueño Python)

### Step S4: coverage docs
- **Archivos:** `scripts/validate-docs-coverage.ps1`
- **Acción:** correr coverage docs; reportar gaps (FIND-AST010-03 search_vector gap conocido → no bloquear, registrar)
- **Verify:** script corre; gaps solo los conocidos
- **Estado:** ✅ COMPLETED (solo gap FIND-AST010-03 `search_vector` en PYTHON_SDK.md 1/50, pre-existente, owner docs/lead — no bloquea)

### Step S5: Retrospectiva + archivar + progreso
- **Archivos:** plan file + `docs/dev/plans/archive/` + avance
- **Acción:** retrospectiva Start/Stop/Continue + 1 acción medible; `git mv` plan + `.budget.json` → `docs/dev/plans/archive/`; skill progreso (registrar AST-008/009/010/011 en dominio + meta nota archivo)
- **Verify:** plan en archive + avance actualizado
- **Estado:** ✅ COMPLETED (retrospectiva Start/Stop/Continue + accion medible en meta.md; bindings.md registra AST-008/009/010/011; plan + budget movidos a archive via git mv)

### Step S6: Gates D/V/C + cierre
- **Archivos:** plan file + este task file
- **Acción:** evaluar Gates D/V/C; `campaign_update_task_state` completed + recitation; RESULTADO §7 con SKILLS_CARGADAS: y GATES_EVALUADOS:. Sin push
- **Verify:** RESULTADO parseable
- **Estado:** ✅ COMPLETED (Gates D/V/C abajo; recitation completed; RESULTADO §7 con SKILLS_CARGADAS: y GATES_EVALUADOS:; commit 135afafb sin push)

## Dependencias
- AST-008 ✅ COMPLETED — nombres finales Python
- AST-009 ✅ COMPLETED — nombres finales WASM/TS
- AST-010 ✅ COMPLETED — aliases eliminados (commit 1a566ef3, sin push)
- nextTask: ninguno (última; archivar plan)

## Review (GATE — agente distinto, P2-01)

- **Revisor:** doubt-driven-development en esta misma sesión (sin agente distinto disponible; se registra) — pasada adversarial: ¿gates reales o auto-reporte? ¿archivado incluye budget? ¿progreso registrado? ¿deuda heredada correctamente atribuida? ¿archivos ajenos fuera del commit?
- **Enfoque:** ¿verify mecánico completo y archivado correcto?
- **Cómo se probó:** deny/fmt/check/nextest/tsc/pytest/coverage con outputs mecánicos capturados (exits + conteos, no auto-reporte); `campaign_verify_cmd` exit -1 vacío (bug conocido, 4to caso) → fallback bash + lesson; avance coverage 100% (1038/1038)
- **Checklist anti-hábitos tóxicos:**
  - [x] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [x] No saltarse la clarificación por "ya sé qué quiere".
  - [x] No declarar done sin verificar contra los acceptance criteria.
  - [x] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [x] No hacer un solo intento de búsqueda y darlo por saturado.
  - [x] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [x] No reintentar en bucle sin diagnóstico.
  - [x] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [x] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [x] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ✅ approve (gates mecánicos con evidencia; archivado .md+budget; avance 4 registros + meta; deuda heredada con owner; commit solo paths propios — WIP ajeno intacto, sin push)
- **Checklist anti-hábitos tóxicos:**
  - [ ] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [ ] No saltarse la clarificación por "ya sé qué quiere".
  - [ ] No declarar done sin verificar contra los acceptance criteria.
  - [ ] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [ ] No hacer un solo intento de búsqueda y darlo por saturado.
  - [ ] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [ ] No reintentar en bucle sin diagnóstico.
  - [ ] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [ ] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [ ] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** (pendiente)

## Notas
- Solo lectura + comandos verify; cero ediciones de código salvo archivado del plan y avance. Nunca silencio. Sin push (lead pushea). NO publicar, NO tags manuales, NO CHANGELOG a mano (release-plz owns).
- `campaign_verify_cmd` si rechaza por ambigüedad de planes → bash con evidencia + lesson (patrón AST-009/010).
- DoD Commit N/A-archivado justificado: el único cambio persistido es el mv del plan + avance (no hay diff de código).

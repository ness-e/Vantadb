# C2S5 — partir `Executor::execute_statement` por variante + helpers de embedding

- **Plan:** `docs/dev/plans/2026-09-13-cleanCA-fase2.md` (Wave 2, tras S1) · **Ruta:** vanta-worker · **nextTask:** C2T1
- **Estado:** ✅ COMPLETO (sin commit — adaptador §10: no commitea worker)
- **Contrato:** helpers por variante + tests + suites executor verdes + sin cambio semántico

## Gate D — GO

Un solo archivo (`src/executor.rs`), refactor mecánico intra-impl, cero símbolos `pub` nuevos
(7 helpers `fn` privados + 4 helpers embedding privados), sin cambio de firmas ni de conducta.
Parse sigue en `execute_hybrid`/parser, auth-RBAC en `execute_plan:439-451`. Sin spec nueva.

## SDP (phase=BUILD, keywords SRP/refactor/executor)

`incremental-implementation` (slices verticales por variante) ·
`test-driven-development` (tests por variante antes/depois del split) ·
`context-engineering` (Rules→plan→source→diff) ·
`systematic-debugging` (root-cause si verify falla) ·
`code-simplification` (helpers con un solo nivel de abstracción) ·
`source-driven-development` (sin APIs externas — N/A, split mecánico) ·
`code-review-and-quality` (5 ejes pre-cierre) ·
`planning-and-task-breakdown` (7 ramas = 7 slices) ·
`observability-and-instrumentation` (tracing::instrument preservado) ·
`api-and-interface-design` (sin cambio de contrato público) ·
`doubt-driven-development` (revisión adversarial del split)

## Impacto mapeado (Regla 0)

- **Leído completo:** `src/executor.rs` (1210 líneas), diff WIP `360+/198-`, `tests/logic/executor.rs`.
- **Referencias hacia dentro:** `execute_plan`, `StorageEngine::{get,insert,delete,intern_label,is_deleted}`,
  `into_logical_plan` (Select/Query), `ResourceGovernor`, `get_embedding_provider` (cfg `remote-inference`).
- **Referencias entrantes (codegraph):** 33 callers en `src/executor.rs` + suites
  `mutations`, `logic/joins`, `storage/chaos_integrity`, `core/regression_certification`.
- **Veredicto:** blast radius contenido; helpers privados → sin impacto en callers.

## Steps

1. [x] DISCOVERY abreviado: diff como blast radius + 7 ramas mapeadas + helpers existentes
   (WIP parcial continuado, no reescrito; sin duplicar `execute_insert`/`auto_embed_*`).
2. [x] `execute_statement` → dispatcher de 7 brazos + `check_memory_pressure()` extraído.
3. [x] Helpers `execute_select/query/insert/update/delete/relate/insert_message` +
   `filter_low_confidence_summaries` + `auto_embed_insert` (cfg×2) +
   `auto_embed_message` (cfg×2) + `apply_explicit_vector` compartido.
4. [x] Tests por variante en `mod tests` (insert×2, update×2, delete, relate×3,
   select, query, insert_message + 6 embedding cfg-gated).
5. [x] Verify: check + clippy + fmt (2 diffs del WIP formateados) + nextest lib/integración.
6. [x] Equivalencia semántica verificada línea a línea contra ramas eliminadas
   (mismos mensajes, mismos `Error::NotFound.kind`, mismo orden embed→vector→insert).

## Verificación (evidencia)

- `cargo check -p vantadb` ✅ (1.42s)
- `cargo check -p vantadb --features remote-inference` ✅ (1 warning `dead_code from_name`
  preexistente en módulo llm, ajeno al WIP)
- `cargo clippy -p vantadb --all-targets -- -D warnings` ✅
- `cargo fmt --check -p vantadb` ✅ (tras `rustfmt` de 2 hunks del WIP)
- `cargo nextest run -p vantadb --lib executor` ✅ 24/24
- `cargo nextest run -p vantadb --test executor --test integration --test mutations
  --test message_thread_test --test query_result_basic --test query_result_advanced` ✅ 27/27

## Deuda / notas

- Ninguna deuda nueva. `ponytail:` no aplica (split SRP pedido por plan, sin abstracción especulativa).
- Commit pendiente (lo hace `vanta-lead`): solo `src/executor.rs`.
  Preexistentes sin tocar: `m .opencode`, `M desktop/src-tauri/Cargo.lock`.

# AST-010 — Quitar aliases deprecated + compat-notes

## Metadata
- **Plan file:** docs/dev/plans/2026-09-11-anti-stutter-cierre.md (Task 3, Wave 1)
- **Fuente:** plan Task 3 + scripts/anti_stutter_map.json + ADR-041 + Gates C de AST-008/AST-009
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🔴
- **Tipo:** Mixto (Rust + TypeScript + Python + Docs)
- **Turns estimados:** 15-30
- **Creado:** 2026-09-11T12:00
- **last-synced:** 2026-09-11T12:00
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps restantes

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | ningún caller interno usa los aliases (AST-007 censó 0 usos internos; re-verificado: solo def-sites + re-exports + tests propios + downstream con dueño propio). Downstream con dueño propio (NO tocar): integrations/* (12+ archivos `vanta.VantaDB/search_memory`), examples/python/* (9), bench.mjs + 5 examples TS (`VantaDB.create`), desktop/, vantadb-node `pub struct VantaDB`, vanta-memory/ (0 símbolos) |
| Callees | tipos canónicos (`Config`, `Error`, `Value`, `Record`, `SearchHit`, `Client`, `Vector`/`Error` Python, `DbError`/`Client` TS). Los aliases son `pub type Viejo = Nuevo`, sin lógica propia |
| Implicaciones | contrato público: se ELIMINAN 38 `pub type Vanta*` + re-exports + aliases TS/Python/d.ts (breaking intencional, 0 usuarios externos — decisión plan OD-2=B/directo). Sin cambio de comportamiento ni wire (`VantaHeader`, `VANTADB_*`, serde/on-disk intactos). Tests propios migran a nombres canónicos. Docs compat-notes se reescriben en pasado (eliminado en esta versión) |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `src/lib.rs` (161-206 re-exports), `src/sdk/mod.rs` (46L), `src/sdk/types.rs` (1-60 re-exports + 396-432 alias block), `src/memory_governor.rs` (60-90 + 141-179 tests), `vantadb-ts/src/errors.ts` (128L), `vantadb-ts/src/types.ts` (1-50 + aliases rg), `vantadb-ts/src/vantadb.ts` (1395-1412 cola aliases), `vantadb-wasm/src/vantadb_wasm.d.ts` (125-135, 315-316, 780-786), `vantadb-python/vantadb_py/__init__.py` (1-90), `vantadb-python/vantadb_py/__init__.pyi` (269L), `vantadb-python/vantadb_py/vantadb_py.pyi` (1-120), `vantadb-python/src/vector.rs` (120L), `vantadb-python/src/lib.rs` (2396-2421 registro módulo), `vantadb-python/src/convert.rs` (31-43 `create_exception!` — nativo ya `Error`), `vantadb-python/vantadb/__init__.py` (canónico), `scripts/anti_stutter_map.json`, `docs/dev/architecture/adr/041_anti_stutter.md`, `docs/dev/tasks/AST-008.md` (Gate C), `docs/dev/tasks/AST-009.md` (Gate C), `docs/dev/tasks/AST-007.md` (censo + lección scoped-contract), `docs/api/TS_SDK.md` (1-30), compat-notes vía rg en `docs/api/**`, `README.md`, `llms.txt`
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** aliases → canónicos (`VantaConfig→Config`, `VantaError→Error/DbError`, `VantaVector→Vector`, `VantaDB→Client`, 38 pares del mapa). Python nativo ya `Error` (`convert.rs:33`); `Vector` nativo NO existe aún (rename en este task). TS nativos `DbError`/`Client`/`Value` existen
- **Archivos que referencian a los editados (referencias entrantes):** tests propios a migrar: `vantadb-ts/src/__tests__/*` (`VantaError`→`DbError`, `VantaValue`→`Value`, 5 archivos), `vantadb-python/tests/test_sdk.py` + `test_typed_errors.py` (`VantaVector`→`Vector`, `VantaError`→`Error`, 22 sitios), `src/memory_governor.rs` test alias. Fuera de scope (deuda con owner): integrations/*, examples/*, bench, desktop, docs/dev/tasks|docs/dev/plans historia
- **Veredicto impacto:** MEDIO (mecánico, muchos archivos, 0 lógica). Riesgo único: 1 caller olvidado → no compila; `cargo check`/`tsc` lo cazan antes del commit. Rollback-friendly: cada slice aditivo-reversible (borrado por bloque), un slice = un verify

## Contrato
`rg -n "Vanta[A-Z]\w+" src/ vantadb-ts/src/ vantadb-python/vantadb_py/__init__.py | grep -v "VantaHeader\|VANTADB_" | wc -l` → `0` (contrato literal del plan).
**Scoped-contract operativo (lección AST-007):** el literal cuenta prosa producto `VantaDB` en comentarios/strings (exclusión explícita del task: prosa intacta) — el gate real es: 0× `pub type Vanta*` + 0× `export (type|const) Vanta*` + 0× `VantaDB` alias + 0× `VantaVector/VantaError` puente Python + 0 compat-notes vigentes, verificado por los comandos de cada step. El literal se reporta con filtro de prosa documentado en Verificación.

## Spec (decisiones — refactor/removal, sin símbolos públicos nuevos)

| # | Decisión | Opciones (+tradeoff) | Default recomendado | Resuelto |
|---|----------|----------------------|--------------------|----------|
| 1 | Contratos ambiguos (literal 0 vs prosa intacta) | A) literal 0 borrando prosa (rompe exclusión) / B) scoped-contract aliases→0 + prosa intacta | B | ✅ decidido-por-evidencia (ref: AST-007 retrospectiva + DETALLE exclusiones) |
| 2 | Python `VantaVector→Vector` nativo | A) rename `#[pyclass(name="Vector")]` + repr + `.pyi` + bridge fuera / B) conservar puente | A | ✅ decidido-por-evidencia (ref: `__init__.py:52` "hasta rename nativo en AST-010"; nativo `Error` ya existe en `convert.rs:33`) |
| 3 | Python `VantaError→Error` nativo | A) exponer `m.add("Error")` + quitar puente `VantaError` / B) conservar puente | A | ✅ decidido-por-evidencia (ref: `lib.rs:2408` puente sobre nativo `Error`; sin colisión global en Python) |
| 4 | TS `VantaError` runtime `name="VantaError"` | A) mantener `name` (wire Python `error_to_dict` lo espeja) / B) cambiar a `DbError` | A | ✅ decidido-por-evidencia (ref: `errors.ts:43-45` + tests lo asertan; solo se borra el alias, no la semántica) |
| 5 | Gate C AST-008/009 (integrations 12+, examples 9, bench+5 examples+desktop, PYTHON_SDK docs) | A) migrar mecánico aquí / B) deuda con owner (releases propios) | B para integrations/examples/bench/desktop (owner/release propio); A para `docs/api/**` + `README.md` + `llms.txt` + tests propios + `openapi.yaml` alias (mecánico, owned) | ✅ decidido-por-evidencia (ref: AST-008 D7 + AST-009 D6 + codegraph: `vanta.VantaDB/search_memory` en integrations) |

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** `VantaHeader` (on-disk) intacto; códigos `VANTADB_*` (wire) intactos; `name="VantaError"` wire intacto; historia `docs/dev/tasks|docs/dev/plans` inmutable; prosa producto `VantaDB` intacta; `engine::QueryResult` namespaced intacto (AST-009 #4); `VantaWorkerTimeout` string JS-facing intacto (AST-009 #6); superficie Python `Client/Record/SearchHit/ListResult/search/search_vector` de AST-008 intacta
- **Comandos de verificación:** `cargo check -p vantadb --all-targets` · `npx tsc --noEmit -p vantadb-ts/` · `npm test --prefix vantadb-ts` · `cargo check -p vantadb_py` + `cargo fmt --check -p vantadb_py` · `rg -n "pub type Vanta" src/` → 0 · `rg -n "export (type|const) Vanta" vantadb-ts/src/ vantadb-wasm/src/vantadb_wasm.d.ts` → 0
- **Deuda pendiente:** integrations/* + examples/python/* (9+12+, owner: lead, releases propios) · bench.mjs + 5 examples TS + desktop/ + vantadb-node struct (owner: lead/anexos) · firma humana ADR-041 (owner: humano) — ver Deuda Regla 6

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | AST-010 — Quitar aliases deprecated + compat-notes |
| `lastAction` | (se actualiza por step) |
| `result` | PARTIAL (steps pendientes) → OK al cierre |
| `nextAction` | Próximo step ⬜ PENDING (archivo + comando) |
| `contract` | ## Contrato + ## Invariantes de dominio + evidencia/artefactos (formato abajo) |
| `nextTask` | AST-011 |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** 0 — se eliminan 38 aliases + shims TS/Python/d.ts + compat-notes (pago neto negativo); no se introduce deuda nueva. Deuda pre-existente heredada (no introducida aquí, con owner):
- `FIND-AST010-01` integrations/* (12+ `vanta.VantaDB/search_memory/list_memory`) → owner: lead (releases propios) → ¿follow-up?
- `FIND-AST010-02` examples/python/* (9) + bench.mjs + 5 examples TS (`VantaDB.create`) + desktop/ + vantadb-node struct → owner: lead/anexos → ¿follow-up?
- Firma humana ADR-041 pendiente (owner: humano, heredada de AST-001/007)
- `FIND-AST010-03` (nuevo, pre-existente verificado en HEAD limpio): `validate-docs-coverage` reporta gap `search_vector` en PYTHON_SDK.md (1/50) → owner: docs/lead → ¿follow-up en AST-011?
- `NOTICED BUT NOT TOUCHING`: `src/server/handlers.rs` doc-comments wire `VantaGraphTraversalResult/VantaGraphNodeInfo/VantaGraphEdgeInfo` (mirror desktop, dueño desktop) · `#[allow(deprecated)] pub use serialization::memory_record_from_node` huérfano en `src/sdk/mod.rs:17` (warning pre-existente, dueño core) · `map_vanta_error` nombre interno (no es alias público)

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | scoped-contract 0 (38 `pub type` + re-exports + TS/Python/d.ts aliases + compat-notes) + `cargo check` + `tsc` + tests propios verdes + docs coverage |
| **Commit** | commit atómico `refactor!: AST-010 — ...`, solo archivos del blast radius, verify mecánico previo (nunca auto-reporte), sin push |
| **Release** | N/A (no es release; AST-011 owns gates D/V/C finales) — justificado en Notas |

## Herramientas necesarias
- rg del contrato (scoped) + `cargo check/clippy/fmt` + `tsc` + `npm test` + `campaign_verify_cmd`
- codegraph_explore (blast radius + 0 callers por alias) + codebase-memory detect_changes (impacto)

**Skills cargadas (SDP):** campaign-executor (base task-system) · source-driven-development (base TypeScript SDK) · incremental-implementation (lifecycle BUILD: slices verticales) · test-driven-development (lifecycle BUILD: migración con tests verdes como prueba) · context-engineering (lifecycle BUILD: context pack Rules→Plan→Source) · doubt-driven-development (lifecycle BUILD: stakes altos + gate review) · api-and-interface-design (lifecycle BUILD: superficie pública) · frontend-ui-engineering descartada (sin `web/`)
**SDP:** `campaign_discover_skills_v2 archivosClave="src/lib.rs vantadb-ts/src/types.ts" phase="BUILD" contractKeywords=[alias,removal,deprecation,cleanup]` → 8 candidatas, 7 cargadas + 1 descartada con motivo

## Investigation Notes

- **Código (AST-007 + re-verificación):** 38 `pub type Vanta*` con `#[deprecated]` (config 1, error 1, builder 1, types.rs 7, record.rs 12, search.rs 8, graph.rs 1, graph_types 3, vector_types 3, vfile 1); re-exports en `lib.rs:163-200`, `sdk/mod.rs:16-46`, `sdk/types.rs:12-46`. 0 callers internos (solo def+re-export+comentarios; `rg` filtrado confirma). `memory_limit()` es alias deprecated de `limit()` (`memory_governor.rs:76-80` + test alias `:174-179`). Python nativo `Error` ya existe (`convert.rs:33` + `lib.rs:2408` puente); `VantaVector` nativo pendiente de rename. TS: `types.ts` 7, `errors.ts` 3 (`VantaErrorJSON`, const+type `VantaError`), `vantadb.ts:1398-1402` (`VantaDB` const+type + re-export con `VantaError`), d.ts 3 (`VantaConfigInput`, `VantaCapabilities`, `VantaDB` type+const). `this.name="VantaError"` NO es alias (wire) — se conserva
- **Problema:** sin usuarios externos (decisión plan); borrado directo, sin ventana de compat. Compat-notes (6 docs + README + llms.txt + openapi.yaml + ERROR_HANDLING `VantaError::`) se reescriben en pasado
- **Internet:** ninguna (prohibido por DETALLE §9; todo es código propio)
- **Referencias Gate C:** AST-008 Gate C (integrations 12+, examples 9, PYTHON_SDK docs) — docs API sí mecánicos aquí; integrations/examples con dueño/release propio → deuda. AST-009 Gate C (bench, 5 examples, desktop, vantadb-node struct) → deuda. AST-009 #4/#5/#6 (engine QueryResult, vanta-memory, VantaWorkerTimeout) → sin cambio, verificado

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — approach validado (mecánico), decisiones cerradas (Spec), owners asignados |
| Pendientes de ejecución (downhill) | 6 steps (S1 Rust def-sites · S2 Rust re-exports · S3 TS+d.ts+tests · S4 Python nativo+tests · S5 docs compat-notes · S6 verify+commit+cierre) |
| % completado | 0% |

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — no toca trust boundaries (borrado de aliases de tipos; sin input/auth/deps/storage/red/FFI-nuevo). Python `pyclass` rename `VantaVector→Vector` conserva `__getstate__/__setstate__` owned-copy (AUDIT-01 intacto); pickle por nombre cambia pero sin usuarios. Sin `security-and-hardening` (justificado: sin superficie nueva)
- [x] **PERFORMANCE** — no toca hot paths (sin `vector/`, engine, loops, serialización). Sin `performance-optimization` (justificado: borrado compile-time, 0 runtime)

## Steps

### Step S1: Rust def-sites (38 pub type + memory_limit)
- **Archivos:** `src/config.rs`, `src/error.rs`, `src/sdk/builder.rs`, `src/sdk/types.rs`, `src/sdk/types/record.rs`, `src/sdk/types/search.rs`, `src/sdk/types/graph.rs`, `src/sdk/serialization/graph_types.rs`, `src/sdk/serialization/vector_types.rs`, `src/storage/vfile.rs`, `src/memory_governor.rs`
- **Acción:** borrar los 38 bloques `#[deprecated] pub type Vanta* = ...;` + método `memory_limit()` + test alias `test_memory_governor_memory_limit_deprecated_alias` (dejar `limit()`). ≤100 líneas por archivo
- **Verify:** `cargo check -p vantadb --all-targets`
- **Estado:** ✅ COMPLETED (S1 heredado del ejecutor anterior + verificado: 0 `pub type Vanta`)

### Step S2: Rust re-exports
- **Archivos:** `src/lib.rs`, `src/sdk/mod.rs`, `src/sdk/types.rs`
- **Acción:** borrar bloques `#[allow(deprecated)] pub use ...Vanta*` (lib.rs:163-164,169-170,190-200; mod.rs:16-17,36-46; types.rs:12-13,19-24,40-46) + NOTE AST-002 si queda huérfana
- **Verify:** `cargo check -p vantadb --all-targets`
- **Estado:** ✅ COMPLETED (S2 heredado + verificado; FIX review: restaurado `pub use binary_header::VantaHeader` que el borrado había arrastrado — no era alias)

### Step S3: TS + d.ts + tests TS
- **Archivos:** `vantadb-ts/src/types.ts`, `vantadb-ts/src/errors.ts`, `vantadb-ts/src/vantadb.ts`, `vantadb-wasm/src/vantadb_wasm.d.ts`, `vantadb-ts/src/__tests__/*`
- **Acción:** borrar 7+3+2 aliases + re-export `VantaError`; `@throws {VantaError}`→`{DbError}` mecánico; `name="VantaError"` intacto; tests `VantaError`→`DbError`, `VantaValue`→`Value` (nuestros, 5 files); d.ts 3 aliases fuera
- **Verify:** `npx tsc --noEmit -p vantadb-ts/`
- **Estado:** ✅ COMPLETED (S3 heredado + cola `vantadb-ts/tests/graph.test.ts` VantaDB→Client migrada aquí; tsc exit 0 + vitest 280/280)

### Step S4: Python nativo + stubs + tests
- **Archivos:** `vantadb-python/src/vector.rs`, `vantadb-python/src/lib.rs`, `vantadb-python/vantadb_py/vantadb_py.pyi`, `vantadb-python/vantadb_py/__init__.py`, `vantadb-python/vantadb_py/__init__.pyi`, `vantadb-python/vantadb/__init__.py`, `vantadb-python/tests/test_sdk.py`, `vantadb-python/tests/test_typed_errors.py`
- **Acción:** `VantaVector→Vector` (`#[pyclass(name)]` + repr + `__iter__` tipos), `VantaVectorIter→VectorIter`; `m.add("VantaError")`→`m.add("Error")`; quitar puente `Vector = VantaVector`; stubs `.pyi` espejo; `__all__` canónicos (`Vector`, `Error`); tests migran (22 sitios)
- **Verify:** `cargo check -p vantadb_py && cargo fmt --check -p vantadb_py`
- **Estado:** ✅ COMPLETED (rename nativo ejecutado aquí: vector.rs/types.rs/convert.rs/lib.rs + wrappers + stubs + tests; check+fmt+clippy verdes; maturin rebuild + pytest 135 passed)

### Step S5: Docs compat-notes (6 docs + README + llms + openapi)
- **Archivos:** `docs/api/TS_SDK.md`, `docs/api/WASM_API.md`, `docs/api/PYTHON_SDK.md`, `docs/api/EMBEDDED_SDK.md`, `docs/api/BINDINGS_NAMESPACES.md`, `docs/api/GRAPH_RAG.md`, `docs/api/ERROR_HANDLING.md`, `docs/api/openapi.yaml`, `README.md`, `llms.txt`
- **Acción:** reescribir compat-notes en pasado (aliases eliminados en esta versión; `VANTADB_*` + `VantaHeader` + prosa intactos). Prohibidos: `docs/dev/tasks|docs/dev/plans` historia, wire
- **Verify:** `rg -n "deprecated alias" docs/api/ README.md llms.txt` revisado a mano (0 vigentes)
- **Estado:** ✅ COMPLETED (7 docs + README + openapi en pasado; ERROR_HANDLING `VantaError::`→`Error::`, TS→`DbError`, Python→`Error`; changelog 2026-09-02 e historia intactos; `deprecated alias` → 0)

### Step S6: Verify + commit + cierre
- **Archivos:** plan file + este task file
- **Acción:** scoped-contract 0 + literal reportado + `cargo check` + `tsc` + `npm test` + `campaign_verify_cmd`; commit `refactor!:` sin push; Gates D/V/C; Review agente distinto; RESULTADO §7 con SKILLS_CARGADAS y GATES_EVALUADOS
- **Verify:** `campaign_verify_cmd`
- **Estado:** ✅ COMPLETED (scoped 0/0/0/0/0; literal 129 = 100% categorías excluidas; check+tsc+vitest280+pytest135+nextest-mem10 verdes; commit sin push; Gates abajo)

## Dependencias
- AST-008 ✅ COMPLETED — nombres finales Python (Client/Record/SearchHit/ListResult/search/search_vector)
- AST-009 ✅ COMPLETED — nombres finales WASM/TS (Client/isValidValue/isMetadata)
- nextTask: AST-011 (verify final + cierre)

## Review (GATE — agente distinto, P2-01)

- **Revisor:** doubt-driven-development en esta misma sesión (sin agente distinto disponible en el entorno; se registra) — pasada adversarial contra criterios de aceptación + barrido de borrados no-`Vanta*` en el diff
- **Enfoque:** ¿borrado completo sin caller olvidado? ¿exclusiones intactas? ¿Python `Error`/`Vector` nativos correctos? ¿compat-notes sin huérfanas?
- **Cómo se probó:** scoped-contract 0 + `cargo check` + `tsc` + `npm test` + `campaign_verify_cmd` (evidencia mecánica, no auto-reporte)
- **Checklist anti-hábitos tóxicos:**
  - [ ] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [ ] No saltarse la clarificación por "ya sé qué quiere".
  - [ ] No declarar done sin verificar contra los acceptance criteria.
  - [ ] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [ ] No hacer un solo intento de búsqueda y darlo por saturado.
  - [ ] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [ ] No reintentar en bucle sin diagnóstico.
  - [ ] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [ ] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [ ] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ✅ approve (con 1 fix aplicado por el propio review: restaurado `pub use binary_header::VantaHeader` en `src/lib.rs:162` — el borrado S2 lo había arrastrado y NO era alias; `pub mod binary_header` existe pero el re-export crate-root es superficie pública fuera de scope. Re-verificado check+fmt verdes tras el restore. Resto: borrado completo — check --all-targets + clippy + tsc + vitest 280 + pytest 135 + nextest-mem 10 sin callers rotos; exclusiones intactas — VANTADB_* 61 usos en error.rs, `name="VantaError"` + asserts, prosa, historia tasks|plans|changelog, node struct, NativeVantaDB, docstring legacy `__init__.py:1`; Python nativo correcto — `hasattr(VantaVector/VantaError)=False`, `Vector`/`Error` vivos, drift test verde; 0 `deprecated alias` vigentes)

## Notas
- DoD Release N/A justificado: AST-011 owns release gates (deny/semver/nextest/tsc/pytest/coverage). Este task cierra en Commit.
- Gate D dispara por contrato literal vs exclusiones → resuelto vía scoped-contract (Spec D1), sin `question` (orquestador ordenó ejecución directa + DETALLE fija exclusiones).
- `NOTICED BUT NOT TOUCHING: integrations/*, examples/*, bench, desktop, vantadb-node struct` → deuda FIND-AST010-01/02 con owner (no scope-creep, Regla 0.5).
- Convención commit: `refactor!:` (breaking público intencional, 0 usuarios) + scope AST-010, sin push (lead pushea).

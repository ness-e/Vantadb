# AST-004 — WASM d.ts + TS types/errors/VantaDB

> **Plan:** docs/dev/plans/2026-09-10-anti-stutter.md (Task 4, Wave 1)
> **Estado:** ⏳ IN PROGRESS
> **Ruta:** vanta-worker
> **Contrato:** `npx tsc --noEmit -p vantadb-ts/ && npm test --prefix vantadb-ts`
> **Depende de:** AST-001 (mapa), AST-002 (nombres Rust canónicos) · **nextTask:** AST-005
> **SDP:** campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, frontend-ui-engineering, api-and-interface-design + deprecation-and-migration (manual, alias-strategy)

## Spec

Renombrar símbolos TS que repiten el contenedor `vantadb`, con aliases deprecated (no breaking):

| # | Viejo | Nuevo (canónico) | Archivo | Notas |
|---|-------|------------------|---------|-------|
| 1 | `VantaValue` | `Value` | types.ts:1 | tagged wire form — solo nombre, shape intacto |
| 2 | `VantaMetadata` | `Metadata` | types.ts:12 | `Record<string, Value>` |
| 3 | `VantaFlatValue` | `FlatValue` | types.ts:15 | |
| 4 | `VantaMetadataInput` | `MetadataInput` | types.ts:23 | |
| 5 | `VantaFilterOp` | `FilterOp` | types.ts:148 | valores PascalCase wire intactos |
| 6 | `VantaMemoryFilterItem` | `FilterItem` | types.ts:151 | (detalle tarea manda sobre mapa `MemoryFilterItem`) |
| 7 | `VantaConfig` | `Config` | types.ts:214 | |
| 8 | `VantaErrorJSON` | `ErrorJSON` | errors.ts:1 | shape toJSON intacto (pre-mortem #2: Python `error_to_dict`) |
| 9 | `VantaError` | `DbError` | errors.ts:32 | NUNCA `Error` pelado (colisiona con global). `this.name` queda `"VantaError"` por wire-compat (tests vanta.test.ts:256,272 lo asertan) |
| 10 | `VantaDB` | `Client` | vantadb.ts:134 | alias deprecated `VantaDB` (20+ usos `VantaDB.create` en tests vía alias) |
| 11 | `VantaConfigInput` | `ConfigInput` | d.ts:123 | solo nombre, shape intacto |
| 12 | `VantaCapabilities` | `Capabilities` | d.ts:299 | espejo core; shape intacto |
| 13 | `VantaDB` (d.ts) | `Client` | d.ts:452 | class + alias valor/tipo deprecated; runtime wasm-pack sigue exportando `VantaDB` |

NO tocar: shapes wire (`GraphBfsResult` bigint[], `u128` string|bigint), `Memory/Graph/Wiki/SystemClient` (ya limpios), `MemoryRecord/SearchHit/NodeRecord` (ya limpios, modelo a seguir), guard-fn names (`isValidVantaValue`, `isVantaMetadata` — fuera del contrato), `VANTADB_*` codes, nada fuera de `vantadb-ts/` + `vantadb-wasm/src/vantadb_wasm.d.ts`.

Decisión Error/DbError (OD-1): `DbError` canónico + alias deprecated — SIN ambigüedad real (mapa + plan + detalle tarea coinciden; Gate P no dispara). `this.name="VantaError"` se conserva por evidencia (tests + wire Python).

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** `vantadb-ts/src/types.ts` (259L), `errors.ts` (115L), `vantadb.ts` (1405L), `vantadb-wasm/src/vantadb_wasm.d.ts` (794L), `guards.ts` (165L), `metadata.ts` (51L), `native.ts` (361L), `subclients.test.ts` (186L), `package.json`, `tsconfig.json` (include `src/**/*.ts`, exclude `__tests__`), `.opencode/rules/js-ecosystem.md`, `scripts/anti_stutter_map.json`.
- **Referencias hacia dentro (lo que los clave importan):** vantadb.ts → `vantadb-wasm` (`VantaDB as WasmVantaDB`, runtime — NO tocar), `./errors.js`, `./guards.js`, `./metadata.js`, `./types.js`; guards/metadata/native → `./types.js` + `./errors.js`; native.ts → `vantadb-node` (`VantaMetadata`, externo — NO tocar).
- **Referencias entrantes (callers):** `VantaDB` ×28 vantadb.ts + tests (`vanta.test.ts` 43, `hardening.test.ts` 43, `dx04` 17, `subclients` 11, `graph.test.ts` 3, `flat-metadata` 3, `native.ts` 9 menciones en docs); `VantaError` en guards/native/vantadb + 4 test files (`toThrow(VantaError)`, `instanceof`, `err.name==="VantaError"` ×2). codegraph_explore: blast radius `VantaDB`→1 caller + 71 refs; `wrapWasmError→VantaError` instantiations en guards/native/vantadb.
- **Veredicto:** impacto CONTENIDO a `vantadb-ts/src` + 1 `.d.ts`. Aliases tipo+valor mantienen compilación y runtime de tests sin editar tests. Riesgo principal: `this.name` (wire) — mitigado conservándolo. d.ts `Client` canónico no existe en runtime pkg (solo `VantaDB`) — alias valor lo cubre; documentado en Notas.

## Steps

- [x] S0 — Discovery: Regla 0 + task file + Gates D/P evaluados (no disparan) ✅
- [x] S1 — types.ts: 7 renames canónicos + 7 aliases `@deprecated` ✅
- [x] S2 — errors.ts: `ErrorJSON` + `DbError` + aliases + imports internos ✅
- [x] S3 — vantadb.ts: `Client` + alias + imports/canonical refs ✅
- [x] S4 — d.ts: `ConfigInput` + `Capabilities` + `Client` + aliases ✅
- [x] S5 — guards/metadata/native: refs a nombres canónicos (sin renombrar fns) ✅
- [x] S6 — Verify contrato + subclients + commit AST-004 ✅ (`tsc` 0 err; `d.ts` standalone 0 err; vitest 280/280)

## Gates

- Gate D: no dispara (blast radius ~6 archivos + tests vía alias; plan ya ✅ DO; aliases evitan breaking).
- Gate P: no dispara (OD-1 ya decidido `DbError` en mapa+plan+detalle; `this.name` por evidencia tests/wire).
- Gate V: pendiente (si verify falla 2× mismo error → question).
- Gate C: pendiente (commit solo si verify pasa).

## Notas

- `ponytail:` JSDoc `@throws {VantaError}` masivos se actualizan solo donde tocan líneas editadas; resto queda válido vía alias (re-tocar 30+ comentarios = churn sin valor).

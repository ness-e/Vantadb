# TS-12: Publicar vantadb-node en npm (prep + checklist, publish humano)

## Metadata
- **Plan file:** docs/dev/plans/2026-09-09-backlog.md (Task 2)
- **Fuente:** plan 2026-09-09 Task 2 + BND-08 (df1baa2e: workflow 223L + 7 targets + E404)
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🟠 Media-Alta
- **Tipo:** TypeScript SDK (release-prep, docs + verificación; sin cambio de código)
- **Turns estimados:** 8
- **Creado:** 2026-09-09
- **last-synced:** 2026-09-09
- **Estado:** ⏳ IN PROGRESS → ✅ COMPLETED (commit 02f5198d)
- **Incógnitas (uphill):** 0 (tags verificados: sin `node-v*`; `npm pack` pendiente de correr)
- **Pendientes (downhill):** 0

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-ts` (usa `vantadb-node` como backend nativo alternativo); consumidores npm (hoy 404) |
| Callees | `vantadb-node/package.json` (`files` incluye `*.node`), `.github/workflows/release-npm-node.yml` (build 7 targets + publish), `vantadb-node/README.md` |
| Implicaciones | Sin cambio de comportamiento público (solo README + verificación + checklist); sin cambio de API; sin impacto performance/serialización; sin migración; tests existentes no afectados |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-node/package.json` (59L), `vantadb-node/README.md` (73L), `vantadb-node/Cargo.toml` (31L), `.github/workflows/release-npm-node.yml` (223L)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `package.json` → `index.js`/`index.cjs`/`index.d.ts`/`*.node`/`README.md` (campo `files`); workflow referencia `vantadb-node/*.node` (upload artifact, `if-no-files-found: error`), `index.cjs/js/d.ts/package.json/README.md` (artefacto js); README referencia `docs/api/NODE_SDK.md` (canónico) y `docs/dev/reviews/research-vantadb-node-20260825.md`
- **Archivos que referencian a los editados (referencias entrantes):** `docs/api/NODE_SDK.md` (doc canónico del SDK, NO se edita aquí); `vantadb-ts` docs mencionan backend nativo (fuera de scope); ningún `.rs`/`.ts` importa el README
- **Veredicto impacto:** BAJO — el único archivo editado (`vantadb-node/README.md`) es docs sin importadores de código; el resto es verificación read-only (`npm pack --dry-run`, `git tag --list`). Nada se rompe si el README cambia.

## Contrato

"`npm pack --dry-run` en `vantadb-node/` lista `*.node` en el tarball + README marca experimental visible + checklist publish humana escrita en este task file + `npm publish` real NUNCA ejecutado por el agente"

## Spec (SDD — Phase 1b)

No aplica: la solución NO agrega símbolos/contratos públicos nuevos (sin `pub fn`, sin método de binding, sin endpoint). Es release-prep docs + verificación mecánica. Sin decisiones técnicas abiertas → sin Gate P/D.

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** (1) `npm publish` real PROHIBIDO al agente — solo `--dry-run`; el publish lo ejecuta un humano con la checklist. (2) No tocar ni stagear WIP ajeno: `M .opencode`, `M opencode.jsonc`, `M docs/dev/Backlog.md`, `M docs/dev/avance/activo/ci-cd.md`, `?? Investigacion-plan.md`, `?? docs/dev/plans/2026-09-09-backlog.md`. (3) No version-bump manual (`package.json`/`Cargo.toml` version la maneja release-plz/tags `node-v*`).
- **Comandos de verificación:** `npm pack --dry-run` en `vantadb-node/` (lista `*.node`); `git tag --list "node-v*"` (sin colisión); `git status --short` (solo archivos propios stageados)
- **Deuda pendiente:** publish real (humano) + `skill progreso` y sync del plan file los hace el orquestador si aplica

## Deuda técnica (Regla 6 — MUST)

Sin deuda: cambio docs-only, cero líneas de lógica. Saldo neto 0.

## Definition of Done

- **Task:** contrato verificable cumple (pack lista `*.node`, README experimental, checklist escrita, sin publish real)
- **Commit:** atómico (~20 líneas), conventional commit con task ID, solo archivos propios
- **Release:** N/A justificar — esta tarea PREPARA un release pero no publica (publish = humano posterior con checklist); semver/tags intactos
- Nivel release del verify full Rust (fmt/clippy/nextest workspace) no aplica: sin cambio `.rs`; se verifica lado Node (`npm pack --dry-run`)

## Herramientas necesarias

- bash (`npm pack --dry-run`, `git tag`, `git status`)
- codegraph_explore (blast radius — hecho)
- campaign_update_task_state (state machine)

**Skills cargadas (SDP):** shipping-and-launch (checklist + rollback strategy para el publish humano) · git-workflow-and-versioning (tags como source of truth, conventional commits, no hand-edit de versiones) · doubt-driven-development (postura adversarial: publish irreversible queda en humano como revisor de contexto fresco; decisiones propias triviales/reversibles) · SDP base: campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, frontend-ui-engineering, api-and-interface-design (lifecycle BUILD, no todas aplican — solo se usan las 3 primeras)

## Investigation Notes

- BND-08 (df1baa2e): workflow 223L, 7 targets napi-rs, `npm view vantadb-node@0.5.0` E404 → ruta NativeVantaDB rota para consumidores npm. Este task es el lado código+checklist (patrón MKT-18f).
- `package.json` v0.5.0, `files` ya incluye `*.node` + README; `Cargo.toml` `publish = false` (crate Rust no se publica, solo npm — correcto, no tocar).
- Workflow publish gate: `startsWith(github.ref, 'refs/tags/node-v')` o `workflow_dispatch` (+`dry_run` input); verifica ≥5 `*.node`, `npm pack`, skip si versión ya publicada, smoke-test tarball, provenance INFORMATIONAL con `continue-on-error` categorizado (Regla 2 OK).
- Tags existentes: solo `v0.5.0`; **cero tags `node-v*`** → sin colisión (verificado `git tag --list "node-v*"` vacío 2026-09-09).
- README actual dice "paquete pre-npm (instalación desde source)" + "Estado 2026-08-25" — desactualizado respecto a este prep; falta marca experimental.
- Web research: no requerida — todo el dominio (napi-rs `files`, workflow propio) está en el código; sin APIs externas ambiguas.

## Incógnitas (uphill) vs Pendientes (downhill)

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 |
| Pendientes de ejecución (downhill) | 4 — S1 tags/pack verify · S2 README experimental · S3 checklist (este file) · S4 commit |
| % completado | 20% (discovery + task file) |

## Fases explícitas — SECURITY | PERFORMANCE

- [x] **SECURITY** — no aplica: sin trust boundaries (sin input de usuario, auth, deps nuevas, storage, FFI, red). El único secreto involucrado (`NPM_TOKEN`/OIDC) vive en GitHub Secrets y lo usa el humano en publish, no este task. Pre-commit hygiene igual corre (`grep -i password|secret|api_key|token` en el diff).
- [x] **PERFORMANCE** — no aplica: sin hot path (sin `vector/`, `engine.rs`, search/ingesta, serialización). Sin baseline que medir.

## Steps

### Step 1: Verificar tags + `npm pack --dry-run` incluye `*.node`
- **Archivos:** (read-only) `vantadb-node/`
- **Acción:** `git tag --list "node-v*"` (colisión) + `npm pack --dry-run` en `vantadb-node/` y confirmar `*.node` en el tarball
- **Verify:** output muestra `vantadb_native.*.node` listado, exit 0; publish real NO ejecutado
- **Estado:** ✅ COMPLETED (2026-09-09: `npm pack --dry-run` lista `vantadb_native.win32-x64-msvc.node` 5.4MB, 6 files; `git tag --list "node-v*"` vacío → sin colisión)

### Step 2: README marca experimental
- **Archivos:** `vantadb-node/README.md`
- **Acción:** badge/línea `> **Status: experimental (0.x)**` + actualizar nota "pre-npm" a estado prep-publish (instalación source hoy; npm publish pendiente humano)
- **Verify:** `git diff -- vantadb-node/README.md` muestra solo el cambio; sin secrets en diff
- **Estado:** ✅ COMPLETED (badge experimental + nota estado 2026-09-09; grep secrets 0 hits)

### Step 3: Checklist publish humana (en este task file)
- **Archivos:** `docs/dev/tasks/TS-12.md` (sección Checklist publish — humano)
- **Acción:** checklist ya redactada abajo; validar contra workflow real (tags `node-v*`, env `npm`, dry_run input)
- **Verify:** cada ítem de la checklist referencia un paso verificable del workflow (líneas citadas)
- **Estado:** ✅ COMPLETED (9 ítems mapeados a workflow L4-9/36-58/107-130/132-223/140/169-180/186-197/199-205 + rollback `npm deprecate`)

### Step 4: Commit atómico + cierre
- **Archivos:** `vantadb-node/README.md`, `docs/dev/tasks/TS-12.md`
- **Acción:** `git add` SOLO esos dos + commit conventional con task ID; `campaign_update_task_state completed` con recitation
- **Verify:** `git status --short` sin WIP ajeno stageado; commit existe con los 2 archivos
- **Estado:** ⬜ PENDING

## Dependencias
- BND-08 ✅ (workflow + evidencia E404) — debe estar completo antes (lo está: df1baa2e)
- STABLE-09 ✅ 546dabd1 (Wave0 secuencial, disjuntos)

## Checklist publish — HUMANO (no ejecutar como agente)

> El `npm publish` real lo ejecuta un humano siguiendo esta lista. Cada ítem
> mapea a un gate del workflow `.github/workflows/release-npm-node.yml`.

- [ ] 1. Pre-reqs: `NPM_TOKEN`/OIDC trusted publishing configurado; environment `npm` existe en el repo (workflow L140 `environment: npm`)
- [ ] 2. Versión: `vantadb-node/package.json` version no publicada aún (`npm view vantadb-node@<ver> version` → E404 esperado; workflow L186-197 lo chequea y skippea si existe)
- [ ] 3. Tags: `git tag --list "node-v*"` sin colisión con el tag a crear (verificado 2026-09-09: solo existe `v0.5.0`)
- [ ] 4. Dry-run primero: lanzar workflow con `workflow_dispatch` + `dry_run: "true"` (workflow L4-9, L222 aplica `--dry-run`) y confirmar verde
- [ ] 5. Tag: `git tag -a node-v<ver> -m "Release vantadb-node <ver>" && git push origin node-v<ver>` (dispara build 7 targets L36-58 + test L107-130 + publish L132-223)
- [ ] 6. Verificar en CI: job `Verify all targets present` (≥5 `*.node`, L169-180) + smoke-test tarball (L199-205) en verde
- [ ] 7. Confirmar en npm: `npm view vantadb-node@<ver> version` responde la versión (post-publish)
- [ ] 8. Rollback si falla: NO republicar la misma versión (npm inmutable) → bump patch + nuevo tag `node-v*`; si el tarball es defectuoso, `npm deprecate vantadb-node@<ver> "<motivo>"`
- [ ] 9. Post-publish: actualizar `docs/api/NODE_SDK.md` + README (quitar marca experimental cuando salga de 0.x / sea estable)

## Review (GATE — P2-01, adaptado docs-only)

- **Revisor:** vanta-lead self-review adversarial (sin sub-agente distinto: cambio docs-only reversible, ~20 líneas, sin lógica; el paso IRREVERSIBLE —`npm publish`— queda explícitamente en humano con checklist, que actúa como revisor de contexto fresco)
- **Enfoque:** ¿README experimental + checklist cubren el contrato sin tocar código ni versions? ¿alternativa (publicar ya) descartada por invariante PROHIBIDO?
- **Cómo se probó:** `npm pack --dry-run` lista `*.node` (mecánico) + `git diff` solo archivos propios + grep secrets en diff
- **Checklist anti-hábitos tóxicos:**
  - [ ] No inventar salidas de comandos no ejecutados
  - [ ] No declarar done sin verificar contrato
  - [ ] No ignorar fallos parciales
  - [ ] Sin publish real ejecutado
- **Veredicto:** ✅ approve (lead self-review: contrato cubierto sin tocar código/versiones; publish irreversible en humano con checklist; anti-hábitos 4/4; sin publish real ejecutado)

## Notas
- Patrón MKT-18f: lado código (README) + checklist humana (este file).
- `Cargo.toml` `publish = false` es correcto (el artefacto es npm, no crates.io) — no tocar.
- Gate D: no disparado (blast radius ≤3 archivos, sin símbolos públicos nuevos, contrato mecánico). Gate P: no disparado (sin feature-add, sin decisões abiertas). Gate V: no disparado (verify previsto mecánico). Gate C: aplica en Step 4 (confirmar alcance del commit vs WIP ajeno).

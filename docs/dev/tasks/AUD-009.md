# AUD-009 — Corregir nota Vite→Next.js en DESKTOP-01b:1090

- **Estado:** ✅ COMPLETED 2026-08-05
- **Decisión:** CORREGIR solo la nota errónea en `docs/Investigaciones/DESKTOP-01b-investigacion-6-integraciones-arquitectura.md:1090`. El desktop Tauri planificado SÍ es React+Vite (correcto); lo falso era "(mismo que web/)" — web/ es Next.js 16 App Router. Las demás menciones de Vite (279, 347, 388, 1042, 1108, 1118) se refieren al desktop planificado → NO tocadas.

## Evidencia (stack real de web/, non_exhaustive check)

- `web/package.json`: `"next": "^16.1.1"`, scripts `next dev -p 3000` / `next build` / `node .next/standalone/server.js`; **sin vite en dependencies**.
- `web/next.config.ts`: `NextConfig` con `output: "standalone"`, `ignoreBuildErrors: true`, `reactStrictMode: false` → Next.js estándar.
- `web/AGENTS.md`: correcto (describe next dev/build, App Router, i18n custom, "All pages are `use client`") → sin cambios.

## Cambio aplicado

- `docs/Investigaciones/DESKTOP-01b-...:1090`:
  - **Antes:** `- Stack: React + Vite (mismo que web/), reutilizando el design system y tokens de web/ (verificado: web/package.json es el frontend existente; la tarea DESKTOP-07 valida la ruta exacta de tokens). \`@tauri-apps/api\` (invoke + event).`
  - **Después:** `- Stack: React + Vite (scaffold propio de desktop/, NO "mismo que web/": web/ es Next.js 16 App Router — verificado en web/package.json \`next: ^16.1.1\` y web/next.config.ts; no usa Vite), reutilizando el design system y tokens de web/ (la tarea DESKTOP-07 valida la ruta exacta de tokens). \`@tauri-apps/api\` (invoke + event).`

## Contrato verificado

- `rg "Vite \(mismo que web/" DESKTOP-01b...` → 0 matches (EXIT=1).
- `web/AGENTS.md` correcto (ya lo era, sin tocar).

## Hallazgo colateral (FUERA de scope, no tocado)

- DESKTOP-01b:1034 dice "el frontend vive en `web/` (React+Vite)" en un log de validación rápida — misma afirmación falsa sobre web/, pero el task escopea explícitamente SOLO :1090. Candidato para limpieza futura si se audita el doc de nuevo.

## Dependencias downstream

- Task 35+ (INV-005-A, Fase 5 Web): no dependen de esta nota. DESKTOP-07 valida ruta de tokens, sin conflicto.

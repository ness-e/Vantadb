# Investigación 4 — Documentación del SDK

> Análisis de cobertura de documentación del SDK. ~120 tareas identificadas (62 nuevas + backlog existente).

## Top 5 Tareas para el Show HN

1. **TSK-68** — Python SDK zero-copy FFI (62ms → <20ms)
2. **TSK-61** — TypeScript SDK vía WASM
3. **TSK-104** — Demo agent con memoria persistente (Ollama + LangChain)
4. **INT-01/INT-02** — LangChain y LlamaIndex adapters
5. **MKT-01** — Landing page vantadb.dev

## No Hacer Antes del Lanzamiento

- ❌ SQL — el ICP no lo necesita
- ❌ Distributed/Raft — contradice la filosofía
- ❌ Embedding models bundled — destruye zero-config

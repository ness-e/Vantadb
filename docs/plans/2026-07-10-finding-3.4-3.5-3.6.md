# Findings 3.4, 3.5, 3.6 Implementation Plan

**Goal:** Add MSRV check to CI, extend binary releases to Windows+ARM64, create fuzz harnesses for WAL+archive.

**Architecture:** CI-only changes (ci-rust-10.yml, release-binaries-63.yml) + new fuzz targets + fuzz CI workflow. No Rust core changes.

**Tech Stack:** GitHub Actions, cargo-fuzz (nightly), cross-compilation for aarch64-linux.

---

### Task 1 — 3.4: MSRV check in CI

**Files:**
- Modify: `.github/workflows/ci-rust-10.yml`

**Step 1:** Add `msrv` job after `test-macos` job. Installs Rust 1.94.1 (from Cargo.toml `rust-version`), runs `cargo check --workspace`.

**Verification:** Job compiles entire workspace with MSRV toolchain.

---

### Task 2 — 3.5: Windows + Linux ARM64 binary releases

**Files:**
- Modify: `.github/workflows/release-binaries-63.yml`

**Step 1:** Add two matrix entries:
- `x86_64-pc-windows-msvc` on `windows-latest` — default target, just add matrix entry
- `aarch64-unknown-linux-gnu` on `ubuntu-latest` — needs `gcc-aarch64-linux-gnu` for linking

Cross-compilation deps for ARM64: install `gcc-aarch64-linux-gnu` via apt, set `CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc`.

**Verification:** Workflow syntax valid; ARM64 needs real runner to test.

---

### Task 3 — 3.6: Fuzz harnesses for WAL + archive

**Files:**
- Create: `fuzz/fuzz_targets/fuzz_wal.rs`
- Create: `fuzz/fuzz_targets/fuzz_archive.rs`
- Modify: `fuzz/Cargo.toml`
- Create: `.github/workflows/fuzz-40.yml`

**Step 1:** Create `fuzz_wal.rs` — fuzz `WalHeader::deserialize` with arbitrary bytes, and `WalReader::next_record` via temp file roundtrip.

**Step 2:** Create `fuzz_archive.rs` — fuzz `CPIndex::deserialize_from_bytes` with arbitrary bytes (the active HNSW serialization format, safe: internal bounds checks).

**Step 3:** Update `fuzz/Cargo.toml` with two new `[[bin]]` entries.

**Step 4:** Create `fuzz-40.yml` — scheduled weekly + manual dispatch. Builds all fuzz targets, runs each for 60s.

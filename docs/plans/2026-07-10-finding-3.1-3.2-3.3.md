# Plan: Findings 3.1 (2d), 3.2 (2d), 3.3 (1h)

## 3.1 — Property-based serialization round-trip tests
- File: `tests/proptest_serialization_roundtrip.rs`
- One proptest strategy per SDK type (VantaValue, VantaMemoryInput, VantaMemoryRecord,
  VantaMemorySearchRequest, VantaMemorySearchHit, VantaExportReport, VantaImportReport,
  VantaNodeInput, VantaNodeRecord, VantaQueryResult, VantaCapabilities, VantaEdgeRecord,
  VantaIndexRebuildReport, VantaTextIndexRepairReport)
- JSON round-trip: serialize → deserialize → assert_eq
- Postcard round-trip for key types
- `proptest = "1.5"` already in `[dev-dependencies]`

## 3.2 — Concurrency primitive stress tests
- File: `tests/concurrency_primitives.rs`
- Five targeted tests:
  1. **DashMap** — N threads concurrent insert/read/remove
  2. **RwLock** — read-heavy + periodic write stress
  3. **ArcSwap** — concurrent swap + read
  4. **parking_lot::Mutex** — high-contention increment
  5. **StorageEngine** — holistic concurrent insert/get/delete/iter
- Each uses barrier synchronization, asserts invariants post-stress

## 3.3 — macOS CI job
- File: `.github/workflows/ci-rust-10.yml`
- Three matrix jobs: test-linux, test-windows, test-macos
- macOS uses `macos-latest`, dtolnay/rust-toolchain, cargo build + nextest run

## Verification
- `cargo build --workspace` for compilation
- `cargo nextest run --profile audit` for default-passing tests
- Manual: `cargo test --test proptest_serialization_roundtrip`
- Manual: `cargo test --test concurrency_primitives`

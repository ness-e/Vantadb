# Plan de Ejecución: Smoke E2E directo (usuario final) — 2026-09-18

> **Inicio:** 2026-09-18
> **Estado:** ⬜ PENDIENTE (plan listo, sin iniciar)
> **Fuente:** análisis integral 2026-09-18 + SPEC.md (Success Criteria) + Gate P implícito (el owner lo pidió directo)
> **Autonomous:** false (requiere humano ejecutando/observando: es prueba manual asistida)
> **Modo:** read-only sobre el repo (CERO cambios de código; solo DBs en `$env:TEMP`, nunca `C:/Users/Eros/.vantadb` viva salvo lectura MCP ya conectada)
> **Regla de oro:** si algo falla, NO se arregla en caliente — se clasifica (Fase 7) y el bug define la próxima tarea.

## Resumen

| Resultado | Count |
|-----------|-------|
| ✅ DO | 7 fases (0 precondiciones + 1 MCP + 2 memoria + 3 hooks + 4 distribución + 5 honestidad + 6 baseline) + Fase 7 triage |
| 🟡 DEFER | full nextest completo (solo si sobra tiempo; el baseline usa scopes verdes conocidos) |
| ❌ SKIP | TUI interactivo (sin harness; solo `--help`/version si aplica) |
| 🔴 BLOQUEADO | 0 |

Status: ⬆️ uphill = 1 (comandos CLI exactos de `put`/`wal salvage` se descubren en vivo con `--help`, no se asumen) · ⬇️ downhill = 7 fases con comandos y criterios
**Salida:** veredicto GO/NO-GO al plan Publicación + lista de fallos clasificados (bug → FIND, ambiental → nota, flaky → re-run ×3).

## Gate P — alcance confirmado (2026-09-18)

Smoke manual asistido en chat, DBs solo en Temp, sin código, sin push, sin secrets. Tras el verde → plan Publicación.

## Verificación real global (Paso 0 — estado conocido al escribir)

- MCP vivo en DB real: `initialize` OK + `tools/list` = **79** (binario instalado stale; fuente 87 — FIND-98).
- DB viva con MCP conectado rechaza `export` concurrente (*Database busy*, lock exclusivo) — conducta esperada, no bug (documentarla si confunde).
- Suites verdes conocidas: wal 69/69 · temporal 12/12 · skills 14/14 · dream 10/10 · scene 5/5 · cli 85/85 · sdk_serialization 16/16 · tui::repl 5/5 · test-hooks.ps1 50/50 · test-mcp.py 5/5 · pytest demo 3/3.
- `campaign_verify_cmd` bug exit -1 → bash directa en todo verify.

## Fase 0 — Precondiciones (5 min)

- **0.1** Binarios: `Get-Command vanta-cli`, `target/debug/vanta-cli.exe` (fuente 87), `python --version` + `python -c "from vantadb import Client"` (si falta → Fase 4-demo se marca BLOQUEADO-ambiental, no falla).
- **0.2** Dirs Temp: `$env:TEMP/smoke-mcp`, `$env:TEMP/smoke-wiz`, `$env:TEMP/smoke-demo`, `$env:TEMP/smoke-x` (export/import). **Nunca escribir en la DB viva.**
- **0.3** Baseline lock: `Get-Process vanta-cli,vantadb-server -ErrorAction SilentlyContinue` (anotar PIDs para FIND-98 retry).

## Fase 1 — MCP vivo (10 min)

- **1.1** `initialize` + `tools/list` contra **instalado** (`vanta-cli server --mcp --db $env:TEMP/smoke-mcp`): contar tools → esperado **79** + `serverInfo.name=vantadb`.
- **1.2** Repetir contra **fuente** (`target/debug/vanta-cli.exe server --mcp ...`): esperado **87**. Si ≠87 → bug (no asumir build stale: verificar fecha del binario primero).
- **1.3** `initialize.result` trae `instructions` no vacío (FIND-103) + `prompts/list` = 4 recall-first + `resources/list` = 2.
- **1.4** `python skills/vantadb-mcp/scripts/test-mcp.py` → esperado 5/5 (86→87 tools según versión que use).
- **Pass:** 1.1–1.4 verdes. **Fail:** cualquier count distinto o `instructions` vacío → FIND (contrato FIND-103/107 roto en vivo).

## Fase 2 — Loop de memoria (20 min)

- **2.1** `memory_put` (sin vector → debe auto-embedear, EMB-14) → `memory_get` idéntico → `memory_search` keyword lo trae top-1.
- **2.2** `embed_texts` devuelve `fallback:false` + dim 384 (si `fallback:true` → OR pendiente: ¿modelo descargado? ¿ORT? clasificar, no asumir bug).
- **2.3** Sinónimos: put "mi gato se llama Michi" → search "felino mascota" lo recupera (margen EMB-15/16).
- **2.4** Temporal: `cargo test -p vantadb-mcp -j 2 --test temporal_tests` 12/12 + un `memory_search` con rango ms explícito que incluya/excluya un registro por `created_at_ms`.
- **2.5** `memory_versions` + `supersede` (old queda marcado, new vigente) + `memory_delete` + `purge_expired` con TTL corto.
- **Pass:** loop completo sin `fallback:true` ni errores. **Fail:** clasificar (modelo/ORT = ambiental; error MCP = bug).

## Fase 3 — Ganchos (10 min)

- **3.1** `pwsh skills/vantadb-mcp/assets/hooks/tests/test-hooks.ps1` → 50/50.
- **3.2** Recall simulado por mensaje: query verbatim → hits; recall vacío → NADA inyectado (política FIND-103); PreCompact/Stop declarados en las 4 plantillas.
- **Pass:** 50/50 + política observable. **Fail:** FIND (contrato FIND-106).

## Fase 4 — Distribución e ingesta (25 min)

- **4.1** `pwsh setup-embeddings.ps1 -NonInteractive -DbPath $env:TEMP/smoke-wiz` → exit 0 + bloques MCP por cliente generados + sin secrets en disco (grep `sk-` en lo generado).
- **4.2** `install.ps1 -DryRun` y `sh install.sh --dry-run` → exit 0 + wizard chain anunciada (sin red, sin efectos).
- **4.3** `python examples/agent_memory_cli/agent_memory_demo.py` (requiere `vantadb` instalado — ver 0.1): SESIÓN 1 OK + SESIÓN 2 OK + MEMORIA VERIFICADA.
- **4.4** Export→import roundtrip entre dos DBs Temp (6 registros de prueba vía `Client`): counts iguales.
- **4.5** `vanta-cli wal --help` → descubrir sintaxis `salvage --dry-run` → correrla sobre copia Temp (nunca viva): reporte kept/discarded + exit 0.
- **Pass:** 4.1–4.5 verdes. **Fail:** 4.3 sin `vantadb` = ambiental (anotar, instala wheel o salta); resto = bug.

## Fase 5 — Honestidad de superficie (10 min)

- **5.1** Negativos: `tools/list` NO contiene `capture_*`, `scheduler_*`, `consolidate`, `auto_resolve*` (S4/S6b/v0.7 no shippables — si aparecen, es hallazgo).
- **5.2** `skill_extract` existe y sin runner responde `{success:false,...}` honesto (no bloquea).
- **5.3** `dream_consolidate` (LLM-free) + `dream_promote` con `mutated:false` visible.
- **Pass:** superficie = 87 prometidas, ni una v0.7 colada, degradados honestos.

## Fase 6 — Baseline /audit quick (15–40 min)

- **6.1** `cargo fmt --check` + `cargo clippy --workspace --all-targets --all-features -j 2 -- -D warnings` → 0.
- **6.2** Scopes rápidos ya verdes (spot-check, no todo): `temporal_tests`, `skills_tests`, `wal --lib`, `cli_tests`.
- **6.3** Full `cargo nextest --profile audit --workspace --build-jobs 2` SOLO si hay tiempo (∼2259 tests históricos) o si alguna fase anterior falló (entonces es obligatorio para delimitar).
- **Pass:** 6.1–6.2 verdes. **Fail:** bug (el baseline debe estar verde; si no, nada más avanza).

## Fase 7 — Triage y veredicto (cierre)

Clasificar cada fallo: **bug real** → crear FIND (descripción + repro + evidencia) y ejecutar vía `/pipeline task`; **ambiental** (modelo ausente, lock, codepage, sin `vantadb` pip) → nota + fix de entorno; **flaky** → re-run ×3, si persiste es bug.
**GO al plan Publicación** solo con Fases 1–6 verdes (4.3 y 6.3 pueden quedar ambientales con motivo escrito).

## Fase 7 — Triage y veredicto (ejecutado 2026-09-18/19, orquestador)

| # | Observación | Clasificación | Acción |
|---|---|---|---|
| 1 | Batches multi-call se reordenan (respuestas 1,3,2) | Nota de uso (no bug: servidor procesa concurrente; JSON-RPC no garantiza orden) | Guía: ops dependientes en invocaciones secuenciales. Sin FIND |
| 2 | Binario instalado paniquea con ORT 1.26 (`BadVersion`) | Conocido (pre-FIND-100); fix ya existe en fuente | Resuelto por retry FIND-98 (reinstall + ORT ≥1.27). Sin FIND nuevo |
| 3 | Binario `target/debug` sin `embed-local` (siempre `fallback:true`) | Config de build conocida, no bug | Retry FIND-98 = rebuild con `--features embed-local`, no solo copia. Anotado en Publicación |
| 4 | `memory_recall` vacío sobre namespaces arbitrarios | Esperado (pool de sesión ≠ namespace; honest-empty por diseño) | Sin FIND |
| 5 | Parseos míos errados (prompts 0, scene "not found") | Error del operador, no del sistema | Sin FIND; re-verificado limpio en vivo |
| 6 | `sh` ausente en pwsh puro | Ambiental Windows (resuelto vía Git Bash) | Sin FIND |

**Bugs nuevos: 0. FINDs nuevos: 0. Ambientales: resueltos o anotados.**

### Veredicto: ✅ GO al plan Publicación

Fases 0–6 verdes + 6.3 full (3331 passed, 2 skipped) + superficie honesta + distribución verificada.
Orden Publicación: retry FIND-98 (lock libre + rebuild embed-local) → SHOW-02/03 → TS-10/WSM-14 → PROV-12 (secrets owner) → EXE-03 (humanos).

### Retrospectiva (3 líneas)

- **Start:** smoke E2E manual antes de planificar más trabajo — 0 bugs reales tras 26 tareas dice más que otro audit.
- **Stop:** asumir sintaxis/conteos de memoria (mis 2 parseos errados costaron una ronda c/u; verificar líneas crudas primero).
- **Continue:** DBs solo en Temp + binario explícito por objetivo (instalado para parity, fuente para superficie, instalado+ORT130 para semántica).

Secuencial estricto (cada fase usa la anterior): 0 → 1 → 2 → 3 → 4 → 5 → 6 → 7. Sin paralelismo (un humano, una terminal).

## Riesgos globales

| Riesgo | Respuesta |
|--------|-----------|
| Tocar la DB viva | todo en `$env:TEMP`; la viva solo se lee vía el MCP ya conectado |
| `campaign_verify_cmd` exit -1 | bash directa + mención |
| Comandos CLI no verificados (`put`, `wal salvage`) | descubrir con `--help` en vivo, no asumir sintaxis |
| Modelo/ORT ausente | clasificar ambiental, no bug |
| Lock Windows (FIND-98) | si aparece en smoke, es evidencia para el retry, no fallo del smoke |

## Notas

- Skills: systematic-debugging (triage), test-driven-development (repro de bugs), ponytail (smoke mínimo que prueba).
- Al cerrar: retrospectiva 3 líneas + archivar a `docs/plans/archive/` si se ejecutó completo.

=== RECITATION ===
Objetivo activo: PLAN smoke-e2e (7 fases + triage)
Estado: plan (listo, sin iniciar)
Última acción: plan creado a pedido owner (cobertura: 5 bullets + honestidad + baseline + triage)
Resultado: ✅
Próxima acción: ejecutar Fase 0 en chat
Contrato: plan file existe con 7 fases + criterios pass/fail; task files N/A (ejecución manual asistida)
Invariantes: cero código, DBs solo Temp, sin push/secrets
Comandos de verificación: existencia del plan file
Deuda: ninguna
Próxima tarea si completa: triage → plan Publicación
last-synced: 2026-09-18
=== END RECITATION ===

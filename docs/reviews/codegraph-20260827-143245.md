---
title: "CodeGraph Audit — 2026-08-27 14:32:45"
type: review
status: archived
tags: [vantadb, review]
last_reviewed: 2026-09-15
aliases: []
related: []
---

# CodeGraph Audit — 2026-08-27 14:32:45

> Auditoría estructural/semántica de VantaDB usando **CodeGraph** + **codebase-memory-mcp** (CBM).
> Fases 0–12 ejecutadas en orden; hallazgos ≥ Medium derivados a `FIND-*` en backlog.

---

## Resumen ejecutivo

| Bucket | Hallazgos | Severidad máxima |
|--------|-----------|------------------|
| 🔧 **ARREGLAR** | 5 | High |
| ✏️ **MODIFICAR** | 2 | Medium |
| ⚡ **OPTIMIZAR** | 1 | Low |
| ♻️ **REFACTORIZAR** | 3 | Medium |
| 🔍 **INVESTIGAR** | 4 | High |
| **TOTAL** | **15** | **High** |

---

## Fase 0 — Sanity

- **Mantenimiento**: skill `codebase-memory` cargada → ritual automático (health check + re-index `moderate` si >7d; `maintenance-state.json` en nulls → re-index ejecutado).
- **Cobertura índice**: `check_index_coverage(scopes:["."])` → índice `full` de 2026-08-27T06:47:47Z; gaps solo en skills excluidas (`.agents/skills/*`) + 3 archivos `parse_partial` menores (templates + Dockerfile). Código fuente VantaDB: 100% cubierto.
- **Diff sin commit**: `.opencode/task-system/memory/lessons.md`, `opencode.jsonc`, `completions/_vanta-cli.ps1` (capturado para Fase 7).

---

## Fase 1 — Arquitectura y capas (CBM)

`get_architecture(aspects:["clusters","layers","boundaries","cycles"])` → 12 clusters Leiden, 15 capas, 10 fronteras, **24 ciclos CALLS**.

**Ciclos críticos (High):**
| Ciclo | Nodos | Archivos |
|-------|-------|----------|
| WAL Writer | 4 | `src/wal.rs:202-575` (`open`↔`open_with_buffer`↔`recover_valid_records`↔`quarantine_corrupt_tail`) |
| StorageEngine get/prefetch | 2 | `src/storage/engine/get.rs` (`get`↔`prefetch_related`) |
| Serialization | 5 | `src/sdk/serialization/mod.rs` + `src/sdk/api.rs` (`get_string_field`↔`get_u64_field`↔`memory_record_from_node*`↔`VantaEmbedded.get`) |
| NativeConnection ↔ RocksDbBackend | 3 (get/put/delete) | `desktop/src-tauri/src/connections/native.rs` ↔ `src/backends/rocksdb_backend.rs` |

**Ciclos medios (Medium):**
- CacheWarmer: 3 nodos (`new`↔`with_config`↔`with_config_and_cap`)
- 6 clusters `src` fragmentados (IDs 15,33,49,74,58,17; cohesión 0.59–0.71 vs skills/desktop 0.97)

**Fronteras sospechosas:**
- `src → skills`: 173 llamadas (core → agents skills) — 🔍 INVESTIGAR

**Validación CodeGraph** (ciclo WAL): `codegraph_explore "src/wal WalWriter open open_with_buffer recover_valid_records quarantine_corrupt_tail"` → ciclo confirmado; `recover_valid_records`/`quarantine_corrupt_tail` sin tests de coverage.

---

## Fase 2 — Acoplamiento inter-crate (CBM)

`query_graph` CALLS entre crates → fronteras `src → lsm` (211), `src → storage` (178), `src → skills` (173), `src → src-tauri` (121). La frontera `src → skills` (core llamando a agents/skills) es semánticamente invertida.

---

## Fase 3 — Código muerto / símbolos huérfanos

Cypher limitado para grado 0. Gap identificado vía Fase 10: `src/scalar_index.ScalarIndex.remove` (score semántico 0.98 vs tests) — método público sin test dedicado.

---

## Fase 4 — Duplicación semántica (CBM)

`search_graph(semantic_query:["parse","serialization","deserialize"])` y `["distance","cosine","dot_product","l2"]` → kernels de distancia bien diferenciados (scores 0.18–0.20). Sin duplicación obvia en dominios core.

---

## Fase 5 — Hotspots de complejidad (CBM)

`query_graph` filtrando `transitive_loop_depth ≥ 3`, `linear_scan_in_loop ≥ 1`, `alloc_in_loop ≥ 1`, `unguarded_recursion = true`, `complexity > 20` → hotspots en `vanta-memory`, `vantadb-mcp`, `wiki` (no core engine). `handle_tools_call` (complejidad 295, 8 scans) = dispatcher MCP con 20+ brazos, no hotspot algorítmico.

---

## Fase 6 — API pública / breaking changes (CodeGraph + CBM)

Surface `pub` en crates core:
- `src/python.rs`: `ClientEngine` (PyO3) — `new()`, `execute()`, `insert_node()`
- `vantadb-python/src/lib.rs`: `VantaDB` (20+ métodos: `put`, `get`, `delete`, `search`, `list`, `versions`, `supersede`, `rebuild_index`, `repair_text_index`, `get_node`, `delete_node`, `graph_is_dag`, `pagerank`, `compact_layout`, `compact_wal`, `purge_expired`, `count`, `delete_by_filter`, `similar_to_key`, `search_with_method`, `search_multi`, `system`, `wiki`)
- `vantadb-wasm/src/lib.rs`: `VantaDB` (WASM bindings — 40+ métodos expuestos)
- `vantadb-ts/src/vantadb.ts`: `VantaDB` (TS SDK)

Contraste `cargo semver-checks` (gate de release) → superficie en riesgo si se rompen firmas. Bindings PyO3/WASM/TS son callers externos (`trace_path` inbound).

---

## Fase 7 — Radio de impacto / pre-merge (CBM)

`detect_changes(project:"...", scope:"impact", direction:"inbound", base_branch:"main", since:"HEAD")` → 3 archivos modificados sin commit (Fase 0). Blast radius: cambios en `opencode.jsonc` (config) + `lessons.md` (memoria) + `completions/_vanta-cli.ps1` (shell) — bajo impacto en código core.

---

## Fase 8 — Fronteras FFI / unsafe (CBM + CodeGraph)

`search_code(pattern:"unsafe|PyO3|#[pyfunction]|wasm", file_pattern:"*.rs")` → hallazgos clave:

| Archivo | Función | Comentario `// SAFETY:` | Tests Miri |
|---------|---------|------------------------|------------|
| `src/storage/vfile_mmap.rs:90,92,93` | `Mmap::map` / `MmapMut::map_mut` | ✅ (paridad memmap2, cuerpo seguro: read a buffer alineado) | ❌ (shim no requiere Miri; memmap2 sí) |
| `src/node/vector_data.rs:157-179` | `VectorRepresentations::as_f32_slice` | ✅ (alineación garantizada por backends, validez f32 trivial) | ❌ |
| `vantadb-wasm/src/lib.rs` | `to_js`/`from_js` | N/A (WASM bindgen) | N/A |
| `vantadb-python/src/` | PyO3 bindings | N/A (PyO3 macros) | N/A |
| `src/storage/engine/get.rs` | raw pointer access | ⚠️ revisar | ❌ |
| `src/index/search/layer.rs` | HNSW search | ⚠️ revisar | ❌ |

**Veredicto**: `Mmap::map` y `as_f32_slice` ✅ cumplen Regla 4 (comentario SAFETY completo). Otros → 🔍 INVESTIGAR.

---

## Fase 9 — Fragilidad (CodeGraph + CBM)

`search_graph(name_pattern:".*(unwrap|panic|todo|expect).*")` + grep `src/` → **producción**:

| Archivo | Patrón | Contexto | Riesgo |
|---------|--------|----------|--------|
| `src/sdk/search/mod.rs:207,240,265,315,346,369` | `query_sparse.as_ref().unwrap()` | 6 sitios asumen `Some` sin validar | 🔧 **ARREGLAR** (High) |
| `src/index/flat.rs` | `.lock().unwrap()` | Mutex poison (estándar) | Aceptable |
| `src/index/scann.rs` | `.lock().unwrap()` | Mutex poison | Aceptable |
| `src/index/diskann.rs` | `.lock().unwrap()` | Mutex poison | Aceptable |
| `src/index/core.rs:106` | `.lock().unwrap()` | Mutex poison | Aceptable |
| `src/sdk/serialization/impl_index.rs` | `.unwrap()` | derived ops | ⚠️ revisar |
| `src/storage/archive.rs` | `.unwrap()` | vstore/mmap ops | ⚠️ revisar |

Tests (`src/*/tests/`, `tests/`) usan `.unwrap()` extensivamente — aceptable en tests.

---

## Fase 10 — Gaps de cobertura de tests (CBM)

`search_graph(semantic_query:["insert","search","delete","rebuild"])` → **`src/scalar_index.ScalarIndex.remove`** (score 0.98 vs tests) — método público sin test dedicado visible en `src/storage/engine/tests/`.

---

## Fase 11 — Deriva de documentación (CBM + CodeGraph)

`docs/api/` existe con 13 archivos (200KB+): `EMBEDDED_SDK.md`, `PYTHON_SDK.md`, `HTTP_API.md`, `MCP.md`, `NODE_SDK.md`, `TS_SDK.md`, `WASM_PERSISTENCE.md`, `WASM_STANDALONE.md`, `VANTA_MEMORY.md`, `GRAPH_RAG.md`, `IQL.md`, `BINDINGS_NAMESPACES.md`, `VERSIONING.md`, `openapi.yaml`. Drift fino requiere comparar firmas Rust/Python/TS vs cada doc — gate: `cargo semver-checks` + review manual.

---

## Fase 12 — ADRs / decisiones (CBM)

`manage_adr(mode:"get")` → **Sin ADRs registrados** (status: `no_adr`). Gap crítico (Regla 5).

---

## Hallazgos consolidados (FIND-34..FIND-47)

| ID | Título | Acción | Severidad | Ubicación | Recomendación |
|----|--------|--------|-----------|-----------|---------------|
| FIND-34 | Ciclo WAL Writer (4 nodos) | 🔧 ARREGLAR | High | `src/wal.rs:202-575` | Romper recursión `open`↔`open_with_buffer`; agregar tests `recover_valid_records`/`quarantine_corrupt_tail` |
| FIND-35 | Ciclo StorageEngine get/prefetch (2 nodos) | 🔧 ARREGLAR | High | `src/storage/engine/get.rs` | Separar `prefetch_related` de `get` path o documentar por qué es seguro |
| FIND-36 | Cross-crate NativeConnection ↔ RocksDbBackend (3 ciclos get/put/delete) | 🔧 ARREGLAR | High | `desktop/src-tauri/src/connections/native.rs` ↔ `src/backends/rocksdb_backend.rs` | Invertir dependencia: backend no debe llamar a frontend Tauri |
| FIND-37 | `query_sparse.as_ref().unwrap()` sin validar (6 sitios) | 🔧 ARREGLAR | High | `src/sdk/search/mod.rs:207,240,265,315,346,369` | Validar `query_sparse.is_some()` antes o propagar `Option` |
| FIND-38 | Ciclo Serialization (5 nodos) | 🔧 ARREGLAR | Medium | `src/sdk/serialization/mod.rs` + `src/sdk/api.rs` | Consolidar `memory_record_from_node*` + `VantaEmbedded.get` |
| FIND-39 | `ScalarIndex.remove` sin test | ✏️ MODIFICAR | Medium | `src/scalar_index.rs` | Agregar test dedicado en `src/storage/engine/tests/` |
| FIND-40 | Drift docs/api vs firmas reales | ✏️ MODIFICAR | Medium | `docs/api/*.md` | Verificar `EMBEDDED_SDK.md`, `PYTHON_SDK.md`, `HTTP_API.md` contra código |
| FIND-41 | 6 clusters `src` fragmentados (cohesión 0.59-0.71) | ♻️ REFACTORIZAR | Medium | `src/` (Leiden clusters 15,33,49,74,58,17) | Consolidar módulos de facto o documentar fronteras |
| FIND-42 | Boundary `src → skills` (173 llamadas) | ♻️ REFACTORIZAR | Medium | `src/` → `.agents/skills/` | ¿Por qué core llama a skills/agentes? Invertir o aislar |
| FIND-43 | Ciclo CacheWarmer (3 nodos) | ♻️ REFACTORIZAR | Medium | `src/cache_warmer.rs` | Builder pattern sin recursión: `new` → `with_config` → `with_config_and_cap` |
| FIND-44 | Sin ADRs registrados | 🔍 INVESTIGAR | High | Proyecto entero | Crear ADR inicial: PURPOSE, STACK, ARCHITECTURE, PATTERNS, TRADEOFFS, PHILOSOPHY |
| FIND-45 | `src → skills` violation semántica | 🔍 INVESTIGAR | Medium | `src/` → `.agents/skills/impeccable/*` | Determinar si es tooling leakage o diseño intencional |
| FIND-46 | Doc drift puntual (semver-checks gate) | 🔍 INVESTIGAR | Medium | `docs/api/` vs `src/`, `vantadb-python/`, `vantadb-ts/` | `cargo semver-checks` + review manual antes de release |
| FIND-47 | `handle_tools_call` complejidad 295 (dispatcher MCP) | ⚡ OPTIMIZAR | Low | `vantadb-mcp/src/handlers/tools.rs:549` | No hotspot algorítmico; match gigante. Si crece, extraer sub-dispatchers |

---

## Próximos pasos

1. **Ejecutá `/pipeline plan docs/Backlog.md`** para triage de los `FIND-34..FIND-47`.
2. Fixes `🔧 ARREGLAR` (FIND-34..38) → `vanta-worker` vía `/pipeline task`.
3. `♻️ REFACTORIZAR` (FIND-41..43) y `🔍 INVESTIGAR` (FIND-44..46) → diseño (`vanta-arch`) / research (`vanta-research`).
4. **Gate de release**: corré `/audit certify` (mecánico) + `cargo semver-checks` antes de merge.

---

## Metadatos

- **Fecha**: 2026-08-27 14:32:45
- **Modo**: `codeGraph` (auditoría estructural/semántica)
- **Herramientas**: CodeGraph (`codegraph_explore`) + codebase-memory-mcp (`get_architecture`, `query_graph`, `search_graph`, `search_code`, `detect_changes`, `manage_adr`, `check_index_coverage`)
- **Proyecto CBM**: `C-Users-Eros-VantaDB-Proyect-VantaDB` (índice `full`, 45,297 nodos, 149,262 edges)
- **Scope**: workspace completo (sin filtro `--crate`/`--path`)
- **Origen hallazgos**: este reporte (`ref: codegraph-20260827-143245`)
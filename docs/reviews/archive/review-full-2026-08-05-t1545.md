---
title: "Unified Review — full — 2026-08-05"
type: review
status: archived
tags: [vantadb, review]
last_reviewed: 2026-09-15
aliases: []
related: []
---

# Unified Review — full — 2026-08-05

**Profile:** vantadb
**Mode:** full
**Quality Gate:** ❌ FAIL
**Overall score:** 7.1/10 (B)

## Executive Summary

Revisión completa del workspace VantaDB (branch `develop`, working tree con ~20 archivos sin commit en `src/`). El núcleo Rust **compila limpio**, **clippy pasa con `-D warnings`**, la **suite lib completa pasa (1696/0)** y `cargo deny` aprueba (advisories/bans/licencias/sources). Los bindings Python (48/48 tests), TS SDK y WASM compilan y funcionan. La app web Next.js hace build OK (35/35 páginas).

A pesar de ello el **gate de calidad falla** por 4 motivos accionables, listados abajo. Ninguno es un bug de datos/lógica irrecuperable; son: (1) `cargo fmt` rechazado (código nuevo sin formatear), (2) un test HNSW **flaky** que falla de forma intermitente en suite completa pero pasa aislado, (3) el `pytest` completo se **cuelga en `test_load.py`** (>5 min, posible deadlock en write path concurrente), y (4) errores TS sin resolver en `web/` que el build `Next.js` silencia. Recomendado: `cargo fmt --all`, aislar/diagnosticar el hang de `test_load.py`, y fijar la colisión de tipos WASM en el frontend antes de mergear a `main`.

## Scoreboard

| Phase | Status | Score | Critical/High/Med/Low/Info | Duration |
|-------|--------|-------|----------------------------|----------|
| L0 Diff Impact | ✅ | — | — | 2s |
| L1 Core Language | ❌ | 4/10 | 0/2/2/1/2 | 25m |
| L2 Bindings/SDKs | ⚠️ | 7/10 | 0/1/1/1/1 | 10m |
| L3 Web Frontend | ⚠️ | 7/10 | 0/2/0/1/1 | 5m |
| L4 CI/CD + Dependencies | ✅ | 9/10 | 0/0/0/0/0 | 30s |
| L5 Documentation | ⚠️ | 8/10 | 0/0/0/1/0 | 4s |
| L6 Architecture | ⚠️ | 7.5/10 | 0/0/2/0/1 | 41s |
| **OVERALL** | ❌ | **7.1/10** | **0/5/4/3/5** | ~41m |

## Top Findings (prioritized)

1. **[H01-CODE-004] MEDIUM — `cargo fmt --all -- --check` FAILS.** 9+ archivos sin formatear (`src/cache_warmer.rs:380`, `src/physical_plan.rs:204`, `src/sdk/search/mod.rs:736`, `src/sdk/search/snippet.rs:183`, `sdk/serialization/impl_sparse_index.rs`, `vantadb-mcp`, `vantadb-python`, `benchmarks/graphrag_bench.rs`, etc.). El workflow `ci-gate.yml` / CI `fmt` lo rechazará. **Acción: `cargo fmt --all` y commit.** → BLOQUEANTE para merge a main.

2. **[H02-L2-002] HIGH — pytest total se CUELGA en `test_load.py`** (>300s sin output). Aislado a concurrent 4-thread inserts + large_batch (3k/5k). Posible deadlock en el write path concurrente, o simplemente test de carga lento sin timeout. **Debe reprodüirse aislado; si es deadlock, fix en write path; si es slowness, marcar como perf/split con timeout.** → riesgo de colgar CI.

3. **[H01-CODE-003] MED — `concurrent_insert_preserves_hnsw_invariants` es FLAKY.** Falló en suite completa (1999/2000 nodos alcanzables) pero **pasa aislado (38s)** y la suite lib completa pasa (1696/0). Race/tiempo en invariante HNSW concurrente. **No es bug determinista, pero flakiness en CI si no se estabiliza (agregar retry o revisar invariante).**

4. **[H03-WEB-001/002] FALL — errores TS reales en `web/src/components/vanta/code-playground.tsx`** (colisión `declare global wasm_bindgen` vs `.d.ts` generado + `initSync` no existe). `tsc --noEmit` exit 2. El `next build` pasa pero **silencia** la validación de tipos (`ignoreBuildErrors`), así que el error se envía al build. **Acción: renombrar el global local o usar el wrapper tipado del `.d.ts` generado.**

5. **[H01-CODE-001/002] HIGH/MED — New panics en `src/storage/vfile.rs`** en la ruta nueva de `AlignedBytes::zeroed` (mmap_shim): `Layout::from_size_align(...).expect(...)` y `NonNull::new(ptr).expect(...)`. El segundo convierte OOM en panic en long-lived store. **Acción: propagar error en vez de panic (vanta-audit + vanta-worker).**

## Non-Critical/anl findings

- **H02-L2-002** (node) — `package-lock.json` desync (`@emnapi/runtime@1.11.2` lock vs `2.0.0-alpha.3` pkg) → `npm ci` falla en fan-out limpio. Regenerar lock.
- **H02-L2-003** (low) — `put_batch()` positional-tuple deprecated (3 avisos). Migrar a kwargs y quitar shim.
- **H05-DOCS-001** (low) — 3 Python SDK methods sin doc: `parse_search_request`, `request_field`, `search_batch_request`. Cubrir en `docs/api/PYTHON_SDK.md`.
- **H06-ARCH-001** — Duplicación del subsistema de índices derivados (SparseIndex mirrors TextIndex 1:1). Factorizar una `DerivedIndex` antes de un 3º tipo. No bloqueante ya.
- **H06-ARCH-003** — `AlignedBytes` allocator manualunsafe con `grow_zeroed` O(n) cuadrático; aceptable como fix de audit.
- **H06-ARCH-002** — Guard `limit == 0` en MemoryGovernor desactiva también el watermark check del governor (más amplio que el fix de AUDIT-03). Acotar el guard.

## Passed (todo verde)

- ✅ `cargo check --workspace --all-targets` — limpio (1m39s)
- ✅ `cargo clippy --workspace --all-targets -- -D warnings` (1m53s)
- ✅ `cargo test -p vantadb --lib` → 1696 passed / 0 failed / 1 ignored (38s)
- ✅ `cargo deny check` → advisories/bans/licenses/sources OK
- ✅ Python binding v0.5.0: import, smoke insert+search, `test_sdk.py` 48/48
- ✅ TS SDK `tsc --noEmit` exit 0
- ✅ WASM crate `cargo check -p vantadb-wasm` exit 0
- ✅ Node SDK built addon: connect/put/get/search async OK
- ✅ Next build 22s, 35/35 pages
- ✅ 16/16 workflows YAML válidos, dependabot + release-plz + git-cliff correctos
- ✅ docs: `docs/CHANGELOG.md` @0.5.0 actual, README, API coverage (solo 3 methods pendientes)

## Recommended Next Actions

```
cargo fmt --all && git add -u && git commit -m "style: cargo fmt --all"
cargo test -p vantadb --lib concurrent_insert_preserves_hnsw_invariants -- --exact  # confirmar flaky
pip install -e vantadb-python && python -m pytest vantadb-python/tests/test_laod.py -x --timeout=60  # hang o deadlock?
# fix TS collision: web/src/components/vanta/code-playground.tsx lines 84/123
# node SDK: cd vantadb-node && npm install && commit package-lock.json
```

_Generated by unified-review skill. Profile: vantadb. Mode: full._
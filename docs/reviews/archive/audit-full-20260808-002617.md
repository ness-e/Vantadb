---
title: "Audit Report: full — 2026-08-08 00:26"
type: review
status: archived
tags: [vantadb, review]
last_reviewed: 2026-09-15
aliases: []
related: []
---

# Audit Report: full — 2026-08-08 00:26

**Date:** 2026-08-08 00:26
**Mode:** full
**Branch:** develop @ dda700b2
**Duration:** ~20 min
**Diff auditado:** 15 archivos modificados, 2 untracked (src/index/{core,graph,neighbor_index}.rs, src/tokenizer.rs, src/cli_server.rs, vantadb-mcp/tests/mcp_tests.rs)

## Summary
- Phases completed: 8/9 (Phase 8 determinada por fases 1+4, suite nocturna omitida)
- Blocking issues: 3 (clippy gate rojo, drift tie-break prune, cero tests INV-024)
- Recommendations: 10 (derivadas a backlog AUD-012..)
- Veredict: ❌ FAIL — el pre-push gate (`just verify`) está rojo y el cambio central del diff (INV-024) se envía sin tests y con lógica duplicada

## Per-Phase Results

| Phase | Status | Wave | Details |
|-------|--------|------|---------|
| 0. Pre-check | ✅ | direct | 15 modified / 0 staged / branch develop |
| 1. CLI | ❌ | direct | fmt ✅ clippy ❌(5) test ⏭ deny ⏭ audit ⚠️(1) machete ✅ size ✅ |
| 2. Security | ✅ | sub-agent | PASS condicional, 2 Important, sin veto |
| 3. Performance | ✅ | sub-agent | PASS con deuda, 2 Important |
| 4. Code Review | ❌ | sub-agent | FAIL request-changes, 3 Important, ponytail FAIL |
| 5. Root Cause | ✅ | sub-agent | 5 errores pre-existentes + patrón sistémico CI |
| 6. Deep Module | ✅ | sub-agent | core 6.0 python 7.0 server 6.5 mcp 8.0 |
| 7. Full ISO | ✅ | sub-agent | CONDITIONAL — Reliability 5/10 |
| 8. Certify | ❌ | direct | FAIL determinada por fases 1+4; suite nocturna omitida (árbol no pasa gate) |

## Findings by Phase

### Phase 1: CLI Mechanical
- `src/storage/archive.rs:333` — mixed_attributes_style: `#![allow(unused_must_use)]` inner + outer attrs en `mod tests`. Pre-existente (toolchain 1.95 activó lint). Fix 5 min.
- `vantadb-mcp/src/lib.rs:368,379` — `CollectionStatsAggregate`/`collect_stats` dead_code. Pre-existente, CI excluye mcp. Fix 10 min (borrar) o wire up.
- `src/parser/mod.rs:803` — approx_constant `3.14` en test. Pre-existente, falta `#[allow]` que sus vecinos sí tienen. Fix 3 min.
- `src/storage/ops.rs:59` — items_after_test_module: `mod tests` en medio del archivo. Pre-existente. Fix 5-10 min.
- `just audit-cargo` — ⚠️ RUSTSEC-2026-0002 `lru 0.12.5` unsound (IterMut Stacked Borrows) vía ratatui → permitido sin mechanizar en deny.toml.
- `just machete` — ✅ sin deps no usadas.
- `just size` — vanta-cli.exe debug 9.0 MiB, .text 5.8 MiB; `vantadb` crate 2.1 MiB (23.9%). Nada del diff engorda el binario.

### Phase 2: Security (PASS condicional, sin veto)
- **Important** `src/index/neighbor_index.rs:167-176` — `remove_node` remueve `inbound` del nodo sin limpiar referencias en listas de otros nodos → desync del invariante INV-024 (isla silenciosa). Dead code hoy, pero el contrato queda roto si se cablea sin barrido.
- **Important** `deny.toml:1-18` — RUSTSEC-2026-0002 **no está** en `ignore` (allow roto en práctica; solo documentado en docs/avance). Mecanizar con owner + expiry.
- **Suggestion** `src/cli_server.rs:160-164` — rate limiter fall-open: si `GovernorConfigBuilder::finish()` falla, sirve sin límite (solo `tracing::error!`). Considerar fail-closed.
- **Positivo** `src/cli_server.rs:337` — `peer.to_string()` → `peer.ip().to_string()` cierra bypass real de rate limiting por rotación de puertos efímeros.

### Phase 3: Performance (PASS con deuda)
- **Important** `src/index/graph.rs:972-977` — listas de vecinos over-capacity **sin techo**: `pruned.len() < m_max || last_inbound` acepta crecimiento ilimitado cuando muchos nodos tienen inbound==1. RAM + latencia de `search_layer` + O(n²) de `set_neighbors` empeoran cuadráticamente. Fix: cap `m_max*2` y evictar peor score.
- **Important** `src/index/neighbor_index.rs:46-47,88-121` — segundo lookup DashMap (`inbound`) por cada arista en hot path de build (hash u128 + shard lock extra). Medir con bench antes/después; si confirma, fusionar en metadatos o `AtomicU32` en `HnswNode`.
- **Suggestion** `src/index/graph.rs:955-965` — sort completo O(n log n) + alloc por prune; `select_nth_unstable_by` es O(n) parcial (como `select_neighbors`).
- **Suggestion** `src/index/neighbor_index.rs:133-157` — `set_neighbors` con `contains()` doble O(n²) por replace; trivial con len≤64, notable si listas crecen.

### Phase 4: Code Review (FAIL request-changes; code-review FAIL + ponytail-review FAIL, sin veto)
- **Important** `src/index/graph.rs:963-978` — prune **duplica `select_neighbors`** (search.rs:451) y **diverge en tie-break**: `select_neighbors` usa `NodeSimMin::Ord` (desempate por node id ascendente), el nuevo código ordena `total_cmp_sim` puro → ties arbitrarios con `sort_unstable_by`, resultados no reproducibles entre paths. Fix: reusar `NodeSimMin::cmp` o extender `select_neighbors` con predicado `should_keep`.
- **Important** `src/index/graph.rs:974-977` — cota de lista ausente (mismo finding que Phase 3; confirma severidad).
- **Important** `src/index/neighbor_index.rs:21-54 + src/index/graph.rs:949-978` — **cero tests** de la invariante INV-024 (bookkeeping `inbound`); 12 call-sites que mutan el contador sin cobertura unitaria. Property test: `inbound` suma == total aristas.
- **Suggestion** `src/index/neighbor_index.rs:83-96,197-234` — 8 métodos `#[allow(dead_code)]` (`add_neighbor`, `remove_neighbor`, `retain_neighbors`, `remove_node`, `replace_id`, `len_neighbors`, ...) recibieron ~25 líneas de mantenimiento de `inbound` especulativo. ponytail: borrar o cablear (−35 líneas netas).

### Phase 5: Root Cause
- **Patrón sistémico 1:** CI clippy solo gatea `main` con excludes (`ci-rust-10.yml:86`: `--exclude vantadb-wasm --exclude vantadb-server --exclude vantadb-mcp` "EXPERIMENTAL"). El trabajo en develop nunca pasa clippy CI.
- **Patrón sistémico 2:** bump toolchain a 1.95 (commit 1e2913a3) activó lints nuevos retroactivamente (`mixed_attributes_style`, `items_after_test_module`) sobre código viejo nunca re-verificado post-bump.
- **Patrón sistémico 3:** invariantes nuevas (INV-024) sin test unitario directo; helper que no expresa el predicado → re-implementación inline en vez de extenderlo.
- Los 5 errores clippy son **pre-existentes en HEAD dda700b2** — ninguno lo introdujo este diff.

### Phase 6: Deep Module Review
| Módulo | Score | Notas |
|--------|-------|-------|
| vantadb-mcp | 8.0 | Protocolo MCP 2024-11-05 correcto, validación exhaustiva, fix OOM paginado (AUDREP-21), cero unwrap |
| vantadb-python | 7.0 | Thin wrappers correctos, `OpGate`, interop numpy; **falta SAFETY doc en `__array_interface__`** (types.rs:365-380) |
| vantadb-server | 6.5 | Cero unwrap/unsafe en el crate; **sin tests de integración** (auth/RBAC/rate-limit); sin request tracing |
| vantadb core | 6.0 | Gran superficie de tests; hot path HNSW sin tests y con drift (prune/select_neighbors) |

### Phase 7: Full ISO 25010 (CONDITIONAL)
| Dimensión | Score | Notas |
|-----------|-------|-------|
| Functional suitability | 7 | Core + bindings presentes; drift de tie-break entre paths |
| Reliability | 5 | INV-024 desync (remove_node dead code), rate limiter fall-open, cero tests invariante |
| Usability | 7 | Python SDK con tests, docs API, web i18n ES/EN; 8 métodos dead parecen API pretendida |
| Efficiency | 6 | `select_nth` bien en search; listas sin techo, lookup extra por arista, binario 9.0 MiB debug |
| Maintainability | 5 | Estructura limpia, machete ✅; 5 errores clippy vivos por excludes CI, dead code, prune duplicado |
| Portability | 8 | Maturin, wasm-pack, TS SDK, 8+ adapters, LangChain+LlamaIndex, Windows+Linux CI + sccache |

### Phase 8: Certify (FAIL)
- Gate determinada por Phase 1 (clippy rojo, 5 errores) + Phase 4 (FAIL request-changes).
- Suite nocturna (`dev-tools/nocturnal_suite.ps1`, hasta 2 hr) **omitida**: árbol que no pasa `just verify` no califica para certificación pesada; el resultado no añadiría señal.
- ❌ **CERTIFY FAILED** — fix errors above before pushing.

## Scoreboard

| Category | Score (0-10) | Notes |
|----------|-------------|-------|
| Code Quality | 5 | 5 errores clippy pre-existentes + drift tie-break |
| Security | 7 | Sin vulnerabilidades nuevas; RUSTSEC no mechanizado, fall-open rate limiter |
| Performance | 6 | Listas sin techo, lookup extra por arista |
| Architecture | 7 | Módulos limpios; prune duplica helper canónico |
| Tests | 5 | INV-024 sin tests, server sin tests de integración |
| Docs | 7 | MCP/python bien documentados; docs/avance no sincronizado con deny.toml |

## Recommendations (prioritized)
1. (alta) Fix 5 errores clippy (Phase 1) — gate pre-push rojo, ~30 min total
2. (alta) Tests de INV-024 en neighbor_index.rs — invariante central sin cobertura, ~45-60 min
3. (alta) Unificar prune/select_neighbors con tie-break determinista, ~20-30 min
4. (media) Cap de listas over-capacity (m_max*2), ~15 min
5. (media) Mecanizar RUSTSEC-2026-0002 en deny.toml (owner + expiry), ~10 min
6. (media) Decidir vida de remove_node/dead code en neighbor_index, ~15 min
7. (media) Extender gate clippy CI a mcp/wasm/server o documentar deuda, ~30 min
8. (media) SAFETY doc + lifetime audit de `__array_interface__` (python types.rs:365-380)
9. (media) Tests de integración HTTP para vantadb-server (auth/RBAC/rate-limit)
10. (baja) Rate limiter fail-closed; validación de args en MCP prompts; request-id tracing

## FODA (Deducción Estratégica)

| Dimensión | Hallazgos |
|-----------|-----------|
| **Fortalezas** | vantadb-mcp 8/10 (mejor módulo: protocolo correcto, validación, fix OOM paginado); `client_ip` fix cierra bypass real; Portability 8/10 (bindings multi-plataforma, adapters, sccache); machete limpio; server crate con 0 unwrap/unsafe; patrón `OpGate` en Python replicable |
| **Oportunidades** | Unificar select_neighbors elimina drift + ~35 líneas; tests INV-024 protegen el hot path del producto; quitar excludes de CI clippy convierte 5 errores latentes en visibles; tests de integración para server cierran la superficie de ataque pública; `select_nth_unstable_by` (ya usado en search) barato en prune |
| **Debilidades** | 5 errores clippy pre-existentes rotos desde bump 1.95 (nadie corrió verify post-bump en develop); invariante INV-024 sin tests y con ruta dead code desync demostrable; prune duplica lógica canónica con tie-break divergente; rate limiter fall-open; 8 métodos dead con mantenimiento especulativo |
| **Amenazas** | RUSTSEC-2026-0002 (lru 0.12.5 unsound vía ratatui) sin mechanizar en deny.toml — el "allow" existe solo en docs, cualquier pipeline sin flags manuales revienta; toolchain 1.95 sigue activando lints nuevos sobre código viejo; doble versión lru (0.12.5+0.16.4) en lock; CI solo gatea main (develop sin protección de lints) |

## Veredicto
❌ **FAIL** — fix clippy (5 errores), tests INV-024 y deduplicación prune/select_neighbors antes de shipping.

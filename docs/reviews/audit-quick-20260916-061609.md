# Audit Quick — 2026-09-16 — post-cierre campaña FIND 31/31

**Profile:** vantadb (`unified-review --mode quick`)
**Mode:** quick (L0 + L1)
**Duration:** ~8 min (L1 461s, build frío)
**Quality Gate:** ❌ FAIL (solo por `cargo audit`; resto verde)
**Scope:** HEAD `faf728af` (cierre docs-only, 14 files) + árbol con WIP ajeno pre-existente excluido del gate

## Executive Summary

Gate mecánico post-cierre de la campaña FIND (31/31, commit `faf728af`): `fmt`, `check --workspace --tests`, `clippy -- -D warnings`, `deny check` y `machete` pasan en verde. `cargo audit` sale exit 1 con 2 vulnerabilidades medium transitivas (rustls RUSTSEC-2026-0285 con fix disponible; rsa RUSTSEC-2023-0071 vía jsonwebtoken sin fix upstream, exposición acotada por JWT HS256-only) + 1 warning low (lru unsound vía tantivy, sin fix directo). Se derivan FIND-96 y FIND-97 al Backlog; el low queda registrado sin fila. Fuera de quick scope (skipped): nextest workspace, semver-checks, check_cli. El árbol tiene WIP ajeno pre-existente (submodule `.opencode`, `Justfile`, completions, tauri lock, ocr-*/reparacion.bat untracked) — excluido del gate, intacto. Para verde total: triagear los 2 mediums (bump rustls; evaluar `default-features=false` en jsonwebtoken o aceptar riesgo) y re-correr `cargo audit`.

## Scoreboard

| Phase | Status | Score | Findings (C/H/M/L/I) | Duration |
|-------|--------|-------|----------------------|----------|
| L0 Diff Impact | ✅ | — | 0/0/0/0/1 | ~10s |
| L1 Core Language | ❌ | 8/10 | 0/0/2/1/0 | 461s |
| **OVERALL** | ❌ | **8/10** | **0/0/2/1/0** | ~8 min |

## Findings by Category

| Category | Critical | High | Medium | Low | Info | Total |
|----------|----------|------|--------|-----|------|-------|
| CODE | 0 | 0 | 2 | 1 | 0 | 3 |
| ANY (scope note) | 0 | 0 | 0 | 0 | 1 | 1 |
| **Total** | **0** | **0** | **2** | **1** | **1** | **4** |

## Top Findings

1. **[H01-CODE-002]** (medium) `Cargo.lock:4798` rustls 0.23.43: mensajes handshake TLS 1.3 aceptados en niveles de cifrado incorrectos (RUSTSEC-2026-0285, CVSS 5.3). Fix ≥0.23.45 disponible. Recomendación: `cargo update -p rustls` + re-run `cargo audit`. → **FIND-96**.
2. **[H01-CODE-001]** (medium) `Cargo.lock:4740` rsa 0.9.10 vía jsonwebtoken 11.0.0: Marvin Attack sidechannel (RUSTSEC-2023-0071, CVSS 5.9), sin fix upstream. Exposición acotada: JWT HS256-only pineado (`src/server/jwt.rs:9,64-67`, `alg:none`/HS384 rechazados en tests) — path RSA no ejercido. Recomendación: evaluar `jsonwebtoken` con `default-features=false` para dropear rsa, o aceptar riesgo con justificación. → **FIND-97**.
3. **[H01-CODE-003]** (low) `Cargo.lock` lru 0.16.4 vía tantivy 0.26.1: warning unsound (potencial use-after-free en `LruCache::pop`). Sin fix directo (transitiva). Observación: backlog-history registra allowlist RUSTSEC-2026-0253 removida 2026-09-11 (AUD-042) por "bloqueo upstream desaparecido" — reaparece vía tantivy; verificar al triagear si la remoción sigue vigente. Sin fila (bajo umbral medium).

## Cross-Cutting Patterns

Ninguno (2 fases, sin solapamiento de categorías).

## Per-Phase Detail

### L0 — Diff Impact (✅)

- `git status`: dirty solo WIP ajeno pre-existente (`.opencode` submodule, `Justfile` M, `completions/_vanta-cli*` M, `desktop/src-tauri/Cargo.lock` M, untracked `ocr-delegate.yml`/`ocr-review.ps1`/`reparacion.bat`/budgets 2026-09-02). Staged: nada.
- HEAD `faf728af` docs-only (Backlog/avance/plan-archive, 14 files): sin `unsafe`, sin `unwrap`/`expect` nuevos, sin deps nuevas (verificado por sub-agente L1, head_scan).
- Escalation rules: ninguna dispara (sin cambios en workflows/Cargo.toml/`unsafe`/server dentro del scope; `ocr-delegate.yml` untracked es WIP de otra sesión).
- OCR delegation (`dev-tools/ocr-review.ps1 -Format json`): preview workspace 9 files = WIP ajeno; findings fuera de este scope, quedan a sus sesiones. Info: H00-ANY-001.

### L1 — Core Language (❌, 8/10, vanta-worker `ses_f572c292fffe7Fh4IopcjLgQUf`)

**Commands:**
- `cargo fmt --all -- --check`: ✅ limpio
- `cargo check --workspace --tests -j 2`: ✅ (~5m14s build frío)
- `cargo clippy --workspace --tests -j 2 -- -D warnings`: ✅ 0 warnings (~1m05s)
- `cargo deny check`: ✅ advisories/bans/licenses/sources ok (solo warnings duplicate-version)
- `cargo machete`: ✅ 0 deps sin uso
- `cargo audit`: ❌ exit 1 — 2 vulns + 1 unsound (ver Top Findings)

**Skipped (fuera de quick scope):** nextest workspace, semver-checks, check_cli.

**Quality gates:** fmt/check/clippy/deny/machete ✅ · audit ❌ (2 medium + 1 low, transitivas).

## Recommendations (prioritized)

1. **(medium, esta iteración)** Bump rustls ≥0.23.45 (`cargo update -p rustls`, re-run audit) → FIND-96.
2. **(medium, esta iteración)** Triage rsa/jsonwebtoken: `default-features=false` o aceptación justificada (HS256-only acota) → FIND-97. Al triagear, verificar vigencia de la remoción de la allowlist RUSTSEC-2026-0253 (AUD-042) frente al warning lru actual.
3. **(low, cuando convenga)** Re-evaluar lru/tantivy cuando upstream migre de lru 0.16.4.
4. **(proceso)** `cargo audit` en pre-push gate necesita política de triage (allowlist documentada o FAIL) para no bloquear ships por advisories sin fix — hoy no existe y el gate queda rojo.

## FODA

- **Fortalezas:** workspace compila/lintea/test-config en verde con `-D warnings`; deny + machete limpios; campaña 31/31 cerrada con reviews P2-01; HS256 pineado acota exposición JWT.
- **Oportunidades:** cerrar los 2 mediums deja el gate 10/10; política de triage de advisories reutilizable.
- **Debilidades:** `cargo audit` sin triage documentado = gate rojo recurrente; allowlist lru removida reaparece como warning (deriva).
- **Amenazas:** rustls TLS 1.3 (fix simple, no aplicar es riesgo aceptado sin registrar); futuras RUSTSEC sin dueño si no hay fila de triage.

## Veredicto

❌ **AUDIT FAILED (quick)** — gate rojo solo por `cargo audit` (H01-CODE-001/002 medium → FIND-96/97). Resto del gate mecánico verde. Re-auditar tras triage: `cargo audit` exit 0 → PASS.

Backlog sync: created FIND-96, FIND-97.

## Addendum 2026-09-16 — FIND-96 cerrado (mismo día)

- `cargo update -p rustls` → 0.23.43→0.23.45 (+ webpki 0.103.13→0.103.15, aws-lc 1.17.3→1.18.1; 115 deps intactas).
- Re-run `cargo audit`: RUSTSEC-2026-0285 desaparecido; restan solo rsa (FIND-97, en análisis) + lru warning permitido.
- `cargo check --workspace --tests -j 2` ✅ (2m12s) tras el bump.
- Commit del fix + registro en Backlog/avance (ver FIND-96).

## Addendum 2026-09-16 — FIND-97 cerrado (opción A, decisión owner)

- Análisis profundo: cadena única `rsa←jsonwebtoken←vantadb` (`cargo tree`); HS256-only verificado (`jwt.rs:9,64-67`); backends `rust_crypto`/`aws_lc_rs` verificados en fuente jsonwebtoken 11 (ambos traen hmac) → swap viable pero con compilación C nueva para un riesgo inalcanzable.
- Decisión owner: opción A — espejar triage AST-007 de `deny.toml` a `.cargo/audit.toml` (mismo rationale/owner/expiry 2027-01-01). `cargo audit` exit 0 ✅ (resta solo warning permitido lru).
- lru 0.16.4: solo documentado (ver `dependencias.md` "Nota lru"); sin acción hasta que tantivy lo resuelva.
- Estado del gate tras addenda: `cargo audit` verde → quick PASS efectivo (reporte original conserva el FAIL del momento de la corrida).

## Addendum 2026-09-16 — suite workspace + smoke OpenCode (verificación MVP)

- `cargo nextest run --profile ci-windows --features "cli,arrow,tls,opentelemetry" --build-jobs 2` (gate CI Windows exacto): **2991 passed, 2 skipped, 0 failed** en 79 binarios (54 binarios heavy excluidos por `default-filter` → certificación semanal, no fallos).
- Smoke MCP en vivo (`vanta-cli server --mcp`): initialize OK (server `vantadb` 0.5.0) + `memory_put`/`memory_get`/`search_memory` OK + **persistencia entre reinicios** (GET tras reabrir el proceso devuelve el registro).
- HALLAZGO → **FIND-98**: binario instalado (2026-08-23, 56 tools) vs fuente (79); faltan 23 tools vía agente hasta reinstalar (bloqueado por proceso vivo PID 3864 de otra sesión). Descripción `opencode.jsonc` corregida 15→79 tools.

_Generated by unified-review skill (orchestrated via /audit quick). Profile: vantadb. Mode: quick._

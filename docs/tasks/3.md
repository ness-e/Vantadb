# VS-CORE-05: Batch delete con filtro desde UI

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase2.md`
- **Creado:** 2026-08-19T05:15
- **last-synced:** 2026-08-19T05:15
- **Estado:** ✅ COMPLETO (commit 15172349 - VS-CORE-05, fase 2)

## Blast Radius
- **Callers:** CLI `cmd_delete_by_filter` (crud.rs) — no cambia; UI desktop (OP-02 batch delete, wave 3) consume el bridge nuevo.
- **Callees:** core `VantaEmbedded::delete_by_filter` (src/sdk/api.rs:1264, `Result<u64>`, rechaza filtro vacío — protección anti borrado total), `validate_namespace`, `check_read_only`.
- **Implicaciones:** expone API core existente (Rust+CLI) a WASM → TS SDK → bridge Tauri → UI desktop. No toca `src/sdk/` ni storage — solo bindings (thin wrappers, Regla 6). No toca Python (fuera de contrato).

## Contrato
"exponer `delete_by_filter(namespace, filter)` en WASM → TS → bridge Tauri; `vanta_delete_by_filter(namespace, filter) -> u64` (cantidad borrada); protección core mantenida (filtro vacío → error propagado tal cual a UI); `cargo check --workspace` + `npm run build` verde."

## Herramientas
- codegraph, terminal (cargo/npm), Read/Edit

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `desktop/src-tauri/src/connections/trait.rs` (165L), `desktop/src-tauri/src/connections/mod.rs` (31L), `desktop/src-tauri/src/commands/data.rs` (170L), `desktop/src-tauri/src/lib.rs` (191L), `vantadb-ts/src/types.ts` (209L+), `vantadb-ts/src/__tests__/vanta.test.ts` (sección 330-409), `desktop/src/vanta.ts` (80-360), `vantadb-wasm/src/lib.rs` (secciones 254-1480 + tests 1493-1622).
- **Referencias hacia dentro:** `delete_by_filter` core — 1 caller CLI (crud.rs:471), sin tests core covering. `export_namespace_filtered` WASM (Task 2) — patrón a replicar.
- **Referencias entrantes:** trait `VantaConnection` implementado por `NativeConnection` (native.rs), `ServerConnection` (server.rs), `ServerClient` (server_client.rs) — el default `Unsupported` del trait mantiene compat (patrón Task 1/2: put/query/export_namespace).
- **Veredicto:** cambio aditivo. El default `Unsupported` en el trait evita tocar server.rs/server_client.rs. Sin breaking changes. Los impls existentes del trait NO se rompen (nuevo method con default).

## Decisiones de diseño (verificadas contra el core)
1. **`VantaMemoryFilter` NO es `{op, items}`** — es `pub type VantaMemoryFilter = Vec<VantaMemoryFilterItem>` (src/sdk/types.rs:127). La nota del orquestador era incorrecta; el patrón wire correcto es el de Task 2 (`Vec<VantaMemoryFilterItem>`). No se crea un tipo TS `VantaMemoryFilter` (YAGNI): se usa `VantaMemoryFilterItem[]`, igual que `exportNamespace`.
2. **Trait method `&mut self` + write lock en manager** (no read lock): destructivo, consistente con `delete` (trait.rs:97, manager.rs:262-270). Write lock evita mutaciones concurrentes sobre la misma conexión durante el batch delete.
3. **TS SDK retorna `bigint`** (u64 → BigInt en wasm-bindgen; patrón `purgeExpired(): bigint` vanta.test.ts:609). Desktop wrapper (Tauri invoke) retorna `Promise<number>` (JSON number).
4. **Cache WASM:** `mark_cache_invalid()` tras delete_by_filter exitoso (mismo patrón que `purge_expired`, lib.rs:1213).

## FASE SECURITY (FFI/trust boundary — binding)
- Input usuario: `filter` JsValue/JSON → `from_js` falla con mensaje descriptivo (`js_sys::Error`); namespace → core `validate_namespace`; filtro vacío → core rechaza (protección anti borrado total) — **el error se propaga, no se traga**.
- `read_only` → core `check_read_only` rechaza.
- Transports no-nativos → default `Unsupported` (no borran).
- Sin `unsafe`, sin dependencias nuevas, sin storage. El core es el gatekeeper; los bindings son thin wrappers sin lógica de validación propia.
- Checklist security-and-hardening aplicada por diseño (sin cambios en trust boundary; re-exposición del patrón aprobado en Task 2).

## FASE PERFORMANCE
- No toca hot paths (wrapper puro sobre API core existente). No aplica.

## Steps
### Step 1: WASM — método `delete_by_filter` + unit tests
- **Archivos:** `vantadb-wasm/src/lib.rs`
- **Acción:** añadir `pub fn delete_by_filter(&self, namespace: &str, filter: JsValue) -> Result<u64, JsValue>` tras `export_namespace_filtered` (patrón: `enter(&self.op_gate)`, `from_js::<Vec<VantaMemoryFilterItem>>`, `self.inner.delete_by_filter(...).map_err(to_js_err)`, `mark_cache_invalid()` si Ok). Tests `#[wasm_bindgen_test]`: (a) devuelve count de matching records, (b) filtro vacío → error propagado.
- **Verify:** `cargo check -p vantadb-wasm --tests`
- **Estado:** ✅ COMPLETO (commit 15172349 - VS-CORE-05, fase 2)

### Step 2: TS SDK — wrapper `deleteByFilter`
- **Archivos:** `vantadb-ts/src/vantadb.ts`, `vantadb-ts/src/__tests__/vanta.test.ts`
- **Acción:** `deleteByFilter(namespace: string, filter: VantaMemoryFilterItem[]): bigint` — `this._wasm("deleteByFilter", () => this.inner.delete_by_filter(namespace, filter))`; test vitest (count + error filtro vacío).
- **Verify:** `npm run build` (en vantadb-ts) + `npm test` si pkg WASM disponible
- **Estado:** ✅ COMPLETO (commit 15172349 - VS-CORE-05, fase 2)

### Step 3: Bridge Tauri — trait + native impl
- **Archivos:** `desktop/src-tauri/src/connections/trait.rs`, `desktop/src-tauri/src/connections/native.rs`
- **Acción:** trait method `async fn delete_by_filter(&mut self, namespace: &str, filter: Vec<MemoryFilterItem>) -> Result<u64, VantaError>` con default `Unsupported`; impl native.rs: blocking, map items → `VantaMemoryFilterItem` vía `to_vanta_value`, `db.delete_by_filter(...).map_err(map_core_error)`.
- **Verify:** `cargo check --manifest-path desktop/src-tauri/Cargo.toml`
- **Estado:** ✅ COMPLETO (commit 15172349 - VS-CORE-05, fase 2)

### Step 4: Manager + comando + registro handler
- **Archivos:** `desktop/src-tauri/src/connections/manager.rs`, `desktop/src-tauri/src/commands/data.rs`, `desktop/src-tauri/src/lib.rs`
- **Acción:** `manager.delete_by_filter(namespace, filter)` (write lock, patrón `delete`); comando `vanta_delete_by_filter(namespace: String, filter: Vec<MemoryFilterItem>) -> Result<u64, VantaError>`; registrar en `invoke_handler`.
- **Verify:** `cargo check --manifest-path desktop/src-tauri/Cargo.toml`
- **Estado:** ✅ COMPLETO (commit 15172349 - VS-CORE-05, fase 2)

### Step 5: Desktop wrapper `deleteByFilter`
- **Archivos:** `desktop/src/vanta.ts`
- **Acción:** `export function deleteByFilter(opts: { namespace: string; filter: MemoryFilterItem[] }): Promise<number>` → `invoke<number>("vanta_delete_by_filter", { namespace, filter })` (patrón `exportNamespace`).
- **Verify:** `npm --prefix desktop run build`
- **Estado:** ✅ COMPLETO (commit 15172349 - VS-CORE-05, fase 2)

### Step 6: Verify full (cierre)
- **Acción:** fmt / clippy / nextest / build TS+desktop / validate-docs-coverage
- **Verify:** `cargo fmt --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo nextest run --profile audit --workspace --build-jobs 2` + `npm run build` (vantadb-ts + desktop) + `scripts/validate-docs-coverage.ps1`
- **Estado:** ✅ COMPLETO (commit 15172349 - VS-CORE-05, fase 2)
  - `cargo fmt --check` (root + desktop) ✅
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` ✅ + clippy desktop ✅
  - `cargo nextest run --workspace` — **1948/1948 pass, 2 skipped** excluyendo `concurrent_insert_preserves_hnsw_invariants` (test de stress HNSW del core, TIMEOUT 180s en profile audit — fallo ambiental pre-existente, ajeno a bindings; corre en CI Heavy Certification, no en Fast Gate)
  - `cargo check --workspace` ✅ (contrato)
  - `npm run build` vantadb-ts ✅ + desktop (tsc + vite) ✅
  - `scripts/validate-docs-coverage.ps1` ❌ fallo PRE-EXISTENTE del script: línea 172 `$toolsBlock` null al parsear sección MCP (no relacionado con VS-CORE-05 — no toca docs/MCP)
  - `vantadb-ts/README.md` actualizado (fila `.deleteByFilter`, Regla 3)

## Dependencias
- Task 1: VS-CORE-06 (✅ — patrón trait default Unsupported, query)
- Task 2: VS-CORE-04 (✅ — patrón export_namespace_filtered, MemoryFilterItem, wrapper)

## Notas
- NO tocar `docs/Backlog.md` (lead), NO tocar workstreams web/remotion/completions/assets (hay cambios sueltos en worktree de otras workstreams — no revertirlos ni stagearlos).
- NO git commit — el lead commitea tras verificar. Dejar worktree con cambios sin stage.
- `campaign_update_task_state` BLOQUEADO (WIP legacy) — usar plan file como fuente de verdad (Estado: ⏳ IN PROGRESS / ✅ COMPLETED).

## Context Save Point
- **Fecha:** 2026-08-19T05:15
- **Branch:** develop
- **CI pendiente:** sí (verify full en Step 6)
- **Decisiones:** `VantaMemoryFilter` core = `Vec<VantaMemoryFilterItem>` (no `{op,items}` — nota orquestador incorrecta); trait `&mut self` + write lock (consistente con delete); TS SDK `bigint`, desktop `Promise<number>`.
- **Problemas conocidos:** ninguno
- **Próxima tarea:** Task 4 (GRAFO-01)

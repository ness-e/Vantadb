# AUD-001 — Fix Dockerfile (CI/Docker invendible) — ✅ COMPLETED

- **Commit:** `1ffe523c38bb4ba96a82b6b0416bbe883925124b`
- **Branch:** develop
- **Fecha:** 2026-08-05
- **Skill:** ci-cd-and-automation, source-driven-development
- **Contrato:** (1) docker build exit 0 — **no ejecutable**: Docker no está instalado en esta máquina (`docker: The term 'docker' is not recognized`). Verificación estática + documentación según contrato. (2) Healthcheck — verificación estática.

## Diagnóstico (confirmado en repo)

- `Cargo.toml:5` → `rust-version = "1.94.1"` (MSRV); `Cargo.toml:604` workspace `rust-version = "1.94.1"`. Dockerfile tenía `ARG RUST_VERSION=1.94.0` → build falla.
- Workspace members reales (`Cargo.toml:583-589`): `.`, `vantadb-python`, `vantadb-server`, `vantadb-mcp`, `vantadb-wasm`. Los 8 COPY (líneas 32-39 originales) apuntaban a `vantadb-{mem0,letta,crewai,dspy, haystack,litellm,openai,ollama}/Cargo.toml` — crates **inexistentes** (integración movida a `integrations/` como paquetes Python; `vantadb-litellm` no existe ni ahí). Build falla en el primer COPY (línea 32).
- El skeleton loop original también creaba skeletons para python/wasm/8 integraciones — innecesarios: `cargo build --package vantadb-server` solo necesita root + `vantadb-server` + `vantadb-mcp` (ver deps en `vantadb-server/Cargo.toml:10-11`).
- `rm -rf vantadb-*/src/` original: glob referenciaba crates inexistentes. Nota: `rm -rf` nunca falla con globs sin match; el fallo real era el COPY.

## Cambios (solo Dockerfile, +11/-17)

1. `ARG RUST_VERSION=1.94.0` → `1.94.1` (≥ MSRV).
2. Eliminados los 8 `COPY vantadb-{mem0,letta,crewai,dspy,haystack,litellm,openai,ollama}/Cargo.toml`.
3. Skeleton RUN reducido a los 3 targets reales: root `src/main.rs`, `vantadb-server/src/main.rs`, `vantadb-mcp/src/lib.rs`. `vantadb-python`/`vantadb-wasm` son workspace members pero no deps → no requieren skeleton.
4. `rm -rf src/ vantadb-*/src/` → `rm -rf src/ vantadb-server/src vantadb-mcp/src` (paths que existen; el skeleton root `src/main.rs` no existe en repo — verificado `src/main.rs -> False` — y debe borrarse antes de `COPY . .` para no dejar un bin fantasma).

## Verificación (estática — Docker no disponible)

| Check | Resultado |
|---|---|
| RUST_VERSION=1.94.1 ≥ MSRV 1.94.1 (Cargo.toml:5, :604) | ✅ |
| COPY `Cargo.toml`, `Cargo.lock` existen | ✅ |
| COPY `vantadb-{server,mcp,python,wasm}/Cargo.toml` existen | ✅ |
| 0 referencias a crates inexistentes (COPY / skeleton / rm) | ✅ |
| `[profile.ci]` definido (Cargo.toml:558) — usado por `cargo build --profile ci` | ✅ |
| `.dockerignore` excluye `target/`, `.cargo/` (context limpio) | ✅ |
| Runtime: `curl` instalado + HEALTHCHECK válido; binary en `/build/target/ci/vantadb-server` | ✅ |

## Smoke-test pendiente

Docker NO disponible en esta máquina. Ejecutar en CI/máquina con Docker:
```
docker build -t vantadb-server:aud-001 .
docker run -d --name vanta-smoke -p 8080:8080 vantadb-server:aud-001
# esperar ~10s (start-period) y validar:
docker exec vanta-smoke curl -sf http://localhost:8080/health
docker rm -f vanta-smoke
```

## Decisiones / notas

- Se mantuvo un `rm` con paths explícitos (en vez de eliminar la línea completa): el skeleton root `src/main.rs` debe borrarse antes de `COPY . .` — el repo root no tiene `main.rs`, así que de lo contrario persistiría como bin extra en la crate raíz.
- Fuera de alcance (no tocar): WAL/vector/storage, plan file, otras tareas.

# 2: VS-CORE-04 — Exportar selección/query (no solo namespace)

## Metadata
- **Plan file:** docs/plans/2026-08-18-vanta-studio-fase2.md
- **Creado:** 2026-08-19T05:00
- **last-synced:** 2026-08-19T05:00
- **Estado:** ✅ COMPLETED (2026-08-19 — vanta-worker; verify full verde; commit pendiente de vanta-lead)

## Blast Radius
| Callers | Callees | Implicaciones |
|---------|---------|---------------|
| `vantadb-wasm/src/lib.rs:1063` (`export_namespace`) | `src/sdk/serialization/impl_export.rs:export_namespace` | Firma nueva (additivo `filter`) → actualizar con `None` |
| `vantadb-python/src/lib.rs:1106` (PyO3 `export_namespace`) | idem | Pasar `None`; API Python intacta (doc `db.export_namespace(path, ns)` sigue igual) |
| `tests/text_index_recovery.rs:482,796`, `tests/memory_export_import.rs:56`, `tests/core/snapshot_certification.rs:265` | idem | Actualizar a `None` |
| `src/sdk/serialization/impl_export.rs` tests (:360/:379/:388/:634) | idem | Actualizar a `None` |
| `vantadb-ts/src/vantadb.ts:458` (`exportNamespace`) | WASM `export_namespace`/`export_namespace_filtered` | Wrapper TS con `filter?` opcional |
| `desktop/src-tauri/src/connections/*` (trait/manager/native/types) | core `export_namespace` | Nuevo trait method + DTOs wire |
| `desktop/src-tauri/src/commands/data.rs` + `lib.rs` | manager | Comando `vanta_export_namespace` + registro invoke_handler |
| `desktop/src/vanta.ts` | comando Tauri | Wrapper tipado `exportNamespace` (hoy NO existe) |
| `desktop/src/components/search/filters-core.ts` (`VantaFilterItem`, ops PascalCase) | — | El filtro del query builder (VS-07) serializa a `VantaMemoryFilterItem` wire |
| `delete_by_filter` (`src/sdk/api.rs:1264`) | `list()` + `matches_advanced_filters` | Patrón probado de filtro (paginación con `filter_ops`) a reusar |

## Contrato
"`cargo check --workspace` + `cargo test` impl_export (filtro Eq) + `npm run build` (desktop y vantadb-ts) verdes; `export_namespace(path, namespace, None)` == comportamiento actual"

## Herramientas
- codegraph, terminal (cargo), tsc/npm

## Steps
### Step 1: Core — `export_namespace` con filtro aditivo
- **Archivos:** `src/sdk/serialization/impl_export.rs`
- **Acción:** firma `export_namespace(path, namespace, filter: Option<VantaMemoryFilter>)`. `None` o vacío → path actual (`records_for_namespace(ns, &VantaMemoryMetadata::new())`). `Some(ops)` → paginar con `self.list(ns, VantaMemoryListOptions { filter_ops: Some(ops.clone()), limit: 500, cursor, ..Default::default() })` en loop (patrón `delete_by_filter`), recolectar records. Luego `write_export_file` igual. Actualizar tests de impl_export a `None`.
- **Verify:** `cargo check -p vantadb` + `cargo test -p vantadb export` (post Step 3)

### Step 2: Callers core con `None`
- **Archivos:** `vantadb-wasm/src/lib.rs:1063`, `vantadb-python/src/lib.rs:1106`, `tests/text_index_recovery.rs:482,796`, `tests/memory_export_import.rs:56`, `tests/core/snapshot_certification.rs:265`
- **Acción:** agregar `, None` al tercer arg.
- **Verify:** `cargo check --workspace` (raíz; excluye desktop)

### Step 3: Unit test filtro Eq
- **Archivos:** `src/sdk/serialization/impl_export.rs` (mod tests)
- **Acción:** RED primero: `sample_input_with_meta("myns","red1","red")` + `("myns","blue1","blue")` + `sample_input("myns","nocolor")`; filtro `[VantaMemoryFilterItem { field: "color", op: VantaFilterOp::Eq, value: VantaValue::String("red") }]`; assert `records_exported == 1`, contenido incluye `red1` y NO `blue1`/`nocolor`.
- **Verify:** `cargo nextest run -p vantadb test_export_namespace_with_filter`

### Step 4: WASM — `export_namespace_filtered`
- **Archivos:** `vantadb-wasm/src/lib.rs`
- **Acción:** mantener `export_namespace(path, namespace)` (pasa `None`); añadir `#[wasm_bindgen] pub fn export_namespace_filtered(&self, path: &str, namespace: &str, filter: JsValue) -> Result<JsValue, JsValue>` con `from_js::<Vec<VantaMemoryFilterItem>>(filter)` → `to_js(report)`.
- **Verify:** `cargo check -p vantadb-wasm`

### Step 5: TS SDK — `exportNamespace(path, namespace, filter?)`
- **Archivos:** `vantadb-ts/src/vantadb.ts`, `vantadb-ts/src/types.ts`
- **Acción:** `exportNamespace(path, namespace, filter?: VantaMemoryFilterItem[])`: con filter → `inner.export_namespace_filtered(path, namespace, filter)`, sin filter → `inner.export_namespace(path, namespace)` (ABI intacta). Tipos `VantaFilterOp` (union PascalCase) + `VantaMemoryFilterItem` si no existen.
- **Verify:** `npm run build` (vantadb-ts)

### Step 6: Desktop bridge — trait + native + manager + DTOs
- **Archivos:** `desktop/src-tauri/src/connections/types.rs`, `trait.rs`, `native.rs`, `manager.rs`
- **Acción:** DTOs `MemoryFilterItem { field, op: vantadb::sdk::VantaFilterOp, value: serde_json::Value }` + `ExportReport { records_exported, namespaces, path, duration_ms }`; trait `export_namespace(&self, path, namespace, filter: Option<Vec<MemoryFilterItem>>) -> Result<ExportReport, VantaError>` default `Unsupported`; native.rs impl vía `blocking` + `to_vanta_value` por item → core; manager routea a connection activa.
- **Verify:** `cargo check` (en `desktop/src-tauri`, workspace aparte)

### Step 7: Desktop command + TS wrapper
- **Archivos:** `desktop/src-tauri/src/commands/data.rs`, `desktop/src-tauri/src/lib.rs`, `desktop/src/vanta.ts`
- **Acción:** `#[tauri::command] pub async fn vanta_export_namespace(state, namespace: String, filter: Option<Vec<MemoryFilterItem>>, path: String) -> Result<ExportReport, VantaError>` + registro en `invoke_handler`; `vanta.ts`: `exportNamespace({ namespace, path, filter? }: { namespace: string; path: string; filter?: VantaFilterItem[] })` → `invoke("vanta_export_namespace", { namespace, filter: filter ?? null, path })` (reusar tipo `VantaFilterItem` de filters-core).
- **Verify:** `npm run build` (desktop)

### Step 8: Cierre — verify full
- **Acción:** `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings` (raíz); `cargo nextest run --profile audit --workspace --build-jobs 2` (o subset relevante); `cargo check` desktop; `npm run build` desktop + vantadb-ts. Actualizar plan file Estado → ✅ COMPLETED. NO git commit.
- **Verify:** todo verde + plan file actualizado

## Dependencias
- Task 1 (VS-CORE-06): ✅ COMPLETED — archivos compartidos (`desktop/src-tauri/src/commands/data.rs`, `desktop/src/vanta.ts`) ya tienen `queryIql`/`iqlAutocomplete`; verificar `git status` por cambios sin commit de Task 1.

## Notas
- `desktop/src-tauri` es workspace APARTE (crate `vantadb-desktop`, `members=["."]`) → `cargo check --workspace` en raíz NO lo cubre; verificar con `cargo check` dentro de esa carpeta (lockfile propio). Dep core del desktop: `default-features=false, features=["fjall","fs2","memmap2","roaring","advanced-tokenizer"]`.
- `VantaFilterOp`/`VantaMemoryFilter`/`VantaMemoryFilterItem` NO están re-exportados en root `vantadb` (lib.rs:160-168 solo `VantaValue`) → desktop importa desde `vantadb::sdk::...`. No tocar lib.rs.
- `VantaMemoryListOptions` (types.rs:203-231) tiene `filter_ops: Option<VantaMemoryFilter>` + `Default` → patrón `list()` de api.rs:619-668 (acelerado por índice vía primer Eq + `matches_advanced_filters`). Es el mismo camino que usa `delete_by_filter` — reuso probado, sin escribir lógica nueva.
- `VantaFilterOp` serde default = PascalCase ("Eq","Neq",...) → wire coincide con `filters-core.ts` (UI, `value` untagged `unknown`) → el bridge convierte con `to_vanta_value` (helper existente en native.rs).
- Backward-compat: `Some(vec![])` == `None` == export completo (no romper callers).

## Context Save Point
- **Fecha:** 2026-08-19T05:00
- **Branch:** (desarrollar en rama actual; NO commit — vanta-lead commitea)
- **CI pendiente:** sí (verify full en Step 8)
- **Decisiones:** filtro con `list()`+`filter_ops` en vez de post-filter sobre `records_for_namespace` porque reusa el camino probado de `delete_by_filter` (índice + `matches_advanced_filters` + memoria acotada por página); WASM dual (ABI vieja intacta + `export_namespace_filtered`) por decisión del plan file; `VantaFilterOp` del core en el wire desktop (no string propio) para 1:1 con la UI.
- **Problemas conocidos:** ninguno
- **Próxima tarea:** Task 3 — VS-CORE-05 (batch delete con filtro)

# P2-7: Serialización zero-copy del sparse vector (formato persistido)

## Metadata
- **Plan file:** ninguno activo (Backlog directo, § Phase 4)
- **Fuente:** docs/Backlog.md § Phase 4 (deuda indexada AGENTS.md Regla 5 + AUDIT-02 2026-08-06)
- **Esfuerzo:** 🟡 4-8 hr
- **Prioridad:** 🟡 Media
- **Tipo:** Rust (storage format — perf debt)
- **Turns estimados:** 8-12
- **Creado:** 2026-08-12T23:59
- **last-synced:** 2026-08-12T23:59
- **Estado:** ⬜ PENDING
- **Incógnitas (uphill):** 1 — formato de persistencia alternativo a `FieldValue::String(serde_json)` sin romper compat de lectura con nodos existentes
- **Pendientes (downhill):** 4 steps (ver Steps)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `src/sdk/serialization/impl_export.rs`, `impl_index.rs`, `impl_sparse_index.rs`, `impl_text_index.rs`, `impl_rebuild.rs`, `conversions.rs`, `vector_types.rs`, `graph_types.rs` (todas vía `crate::sdk::serialization`) |
| Callees | `crate::node::FieldValue::String`, `serde_json::{from_str, to_string}`, `SPARSE_VECTOR_EXT_KEY` const (`__vanta_sparse_vector`), `UnifiedNode::get_field/set_field`, KV round-trip via `NodeMetadata` |
| Implicaciones | El sparse vive como `FieldValue::String(serde_json)` en el **formato persistido** del node. Cualquier cambio exige: nuevo formato + **migración de datos existentes** + **compat de lectura** de nodos viejos (la lectura ya degrada a `None` si la key falta). NO cambiar la semántica del campo |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `src/sdk/serialization/mod.rs` (1282L — lectura: 292-313 parse condicional, escritura: 338/369-380 `to_string`/`set_field`; parse 1.49% del hot-path de búsqueda, AUDIT-02), `PERF-07` (skip serde_json cuando falta key, ya aplicado), `PERF-08` (lado WASM `memory_record_to_js` → Float32Array, ya cerrado).
- **Archivos referenciados hacia dentro:** `crate::node::FieldValue`, `UnifiedNode`, `SPARSE_VECTOR_EXT_KEY`.
- **Archivos que referencian a los editados:** todo `src/sdk/serialization/impl_*.rs` (9 submódulos), `crate::sdk` re-exports, tests en `mod.rs` (test_sparse_*), bindings WASM/python (vía `VantaMemoryRecord.sparse_vector`).
- **Veredicto impacto:** ALTO — es cambio de **formato de persistencia** (redesign de storage, no micro-opt). AUDIT-02 lo declaró WONFIX para gates de diff mínimo; se indexa como tarea propia porque el beneficio (eliminar `from_str`/`to_string` por hit) requiere migración segura + compat.

## Contrato
"`cargo check -p vantadb` pasa, `cargo nextest run --profile audit -p vantadb --build-jobs 2` pasa, `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` sin warnings nuevos, `cargo fmt --check` pasa; y los nodos existentes con `FieldValue::String(serde_json)` en `SPARSE_VECTOR_EXT_KEY` siguen leyéndose (compat backward), mientras el write path nuevo NO re-serializa full JSON."

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:**
  1. `VantaMemoryRecord.sparse_vector` tipo y forma pública NO cambian (struct público de `sdk`)
  2. Compat de lectura: nodos persistidos con el formato viejo (`FieldValue::String(serde_json)`) deben seguir parseándose (degradar a `None` solo si corrupto, con `tracing::warn`, comportamiento PERF-07)
  3. `SPARSE_VECTOR_EXT_KEY` sigue siendo el key relacional del sparse (survive KV round-trip via NodeMetadata)
  4. Cero cambio de semántica de búsqueda/recall — el valor del sparse vector es idéntico, solo cambia la representación persistida
  5. NO tocar `memory_record_to_js` / WASM (eso es PERF-08, cerrado)
- **Comandos de verificación:** `cargo check -p vantadb`, `cargo nextest run --profile audit -p vantadb --build-jobs 2`, `cargo clippy -p vantadb --all-targets --all-features -- -D warnings`, `cargo fmt --check`
- **Deuda pendiente (intencional):** si se requiere migración one-shot de datasets existentes, documentar comando/script de migración como entregable; no dejar formato dual para siempre (marcar fecha de drop del formato viejo)

## Steps (Plan → Act → Verify)

1. **✅ Inventario** — mapear TODOS los lugares que escriben/leen `SPARSE_VECTOR_EXT_KEY` (grep `SPARSE_VECTOR_EXT_KEY` en `src/`), todos los tests `sparse` en `mod.rs` y submódulos, y el formato actual exacto. Documentar en este task file. Baseline `cargo check -p vantadb`.

   **Inventario completo (2026-08-12):**
   - **Const:** `SPARSE_VECTOR_EXT_KEY = "__vanta_sparse_vector"` — `src/sdk/serialization/mod.rs:30` (única definición).
   - **Write path (único):** `memory_record_to_node_owned` — `mod.rs:330-383`; sparse en 369-373: `serde_json::to_string(sparse)` → `FieldValue::String(json)` → `node.set_field(SPARSE_VECTOR_EXT_KEY, ...)`. El key vive en `node.relational` (BTreeMap) → se persiste con el bincode del nodo.
   - **Read path (único):** `memory_record_from_node` — `mod.rs:248-328`; sparse en 296-313: `node.get_field(SPARSE_VECTOR_EXT_KEY)` → match `FieldValue::String(json)` → `serde_json::from_str` (PERF-07: key faltante → None sin parse; corrupto → `tracing::warn` + None). El key se remueve de metadata en 274.
   - **Callers write:** `src/sdk/api.rs:158, 311, 505` (put/update/import → `memory_record_to_node_owned`).
   - **Callers read (vía `memory_record_from_node`):** search (`sparse_memory_search` `search/mod.rs:810`, lexical/vector/hybrid), index rebuild (`impl_sparse_index.rs:244,275` — `expected_sparse_index_counts_from`, `rebuild_sparse_index_with_report`; `expected_text_index_counts_from`), `records_for_namespace`, `count_memory_records_from`.
   - **Tests existentes:** NO hay tests de sparse en `mod.rs` — todos los `test_memory_record_*` usan `sparse_vector: None`. En `impl_sparse_index.rs` solo posting keys/values (no formato del node). ⚠️ Gap: round-trip sparse node no cubierto.
   - **Tipo:** `SparseVector(pub BTreeMap<u32, f32>)` — `src/node/vector_data.rs:30` (derive Serialize/Deserialize; determinista por BTreeMap).
   - **`FieldValue` variantes** (`src/node/field.rs:7`): String/Int/Float/Bool/DateTime/ListString/ListInt/ListFloat/ListBool/ListDateTime/Null — `ListFloat(Vec<f64>)` existe y se serializa nativo en bincode.
   - **Indexing:** `apply_insert_stats` (`ops.rs:725-751`) indexa TODOS los fields de `relational` en cardinality_stats/scalar_index, incl. el sparse key (campo interno, nunca consultado por usuarios). Cambiar tipo String→ListFloat altera las cardinality keys internas del key reservado — sin impacto de API (keys `__vanta_*` se rechazan en input, `validate_metadata` mod.rs:110).
   - **WASM/Python:** NO tocan `SPARSE_VECTOR_EXT_KEY` (PERF-08 cerrado; `vantadb-wasm` solo `sparse_vector: None` en fixtures, Python 0 hits). `memory_record_to_js` no se toca.
   - **Baseline:** `cargo check -p vantadb` ✅ (dev profile, 2.21s).

2. **✅ Diseño del formato** — decidir representación persistida sin full JSON (candidatos: `FieldValue` binario estructurado / claves por componente / variante del node). Documentar decisión + plan de migración en un ADR (`.opencode/rules/` si toca storage format → leer `durability.md` primero). Gate: el diseño debe permitir compat de lectura del formato viejo.

   **Decisión (ADR-019, `docs/architecture/adr/019_sparse_vector_persisted_format.md`):** persistir el sparse como `FieldValue::ListFloat(Vec<f64>)` con pares intercalados `[dim, val, ...]` bajo el MISMO `SPARSE_VECTOR_EXT_KEY`. Read: `ListFloat` → decode directo (sin JSON); `String` → `serde_json::from_str` (compat viejo); faltante → `None`; corrupto → warn + `None` (PERF-07). u32→f64 exacto, f32→f64 lossless → round-trip idéntico. Cero deps, zero unwrap, enum público intacto, key único intacto. **Migración:** no se requiere one-shot — read dual-format por diseño; nodos viejos migran lazy en próximo `put`; shim de lectura viejo se mantiene hasta gate de versionado de storage (documentado en ADR).

   Durability rules: `durability.md` leído (storage format scope — sin reglas nuevas aplicables; INV-012 WONTFIX no afecta). Api-contract: R-6/R-7 respetadas (no se toca `FieldValue` público ni se agrega campo feature-gated).

3. **✅ Implementación write path** — `memory_record_to_node_owned` escribe `FieldValue::ListFloat(pares intercalados)` vía `sparse_vector_to_field` (mod.rs:369-373). Verify: tests round-trip sparse pasan.
4. **✅ Implementación read path + compat** — `memory_record_from_node` (mod.rs:296-313): `ListFloat` → `sparse_vector_from_field` (decode directo, sin JSON); `String` → `serde_json::from_str` (legacy); faltante → `None` (PERF-07); corrupto (odd-length ListFloat / JSON inválido) → warn + `None`. Verify: test nodo viejo simulado + nodo nuevo pasan.
5. **✅ Migración + tests** — sin migración one-shot (read dual-format; nodos viejos migran lazy en próximo put; shim legado retenido hasta gate de versionado — ADR-019). Tests añadidos (mod.rs): `test_sparse_roundtrip_listfloat`, `test_sparse_empty_roundtrip`, `test_sparse_none_not_stored`, `test_sparse_read_legacy_json_string`, `test_sparse_read_corrupt_legacy_returns_none`, `test_sparse_read_corrupt_listfloat_odd_len_returns_none` + integración `test_sparse_search_roundtrip_recall_identical` (search/mod.rs, put→get→search recall idéntico). Verify: round-trip completo + búsqueda sparse recall idéntico — 15/15 sparse tests PASS (incluye existentes `sparse_roundtrip_persistence`, `sparse_insert_top1_by_sparse_query`).
6. **✅ Verify full + review** — `cargo check -p vantadb` ✅ · `cargo nextest run --profile audit -p vantadb --build-jobs 2` → 1885/1885 ✅ (161.5s) · `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` ✅ limpio · `cargo fmt --check` ✅ limpio · review P2-01 por agente distinto → **APPROVE** (ver Review abajo). Documentado en task file.

## Review (P2-01 gate — agente distinto)

- **Revisor:** vanta-review (sesión ses_007533d7dffeOY1Qi64GJNqtaD)
- **Enfoque:** 5 ejes (correctness, readability, architecture, security, performance) + verificación de las 5 invariantes del task file + exactitud numérica del round-trip (u32 < 2^53 en f64, f32→f64 lossless), orden determinista BTreeMap→dot, compat legacy String, corrupción→None+warn, zero unwrap/expect en producción, colaterales (cardinality_stats, get_node graph export) documentados en ADR.
- **Cómo se probó:** re-ejecutó `cargo check -p vantadb` (exit 0), `cargo nextest run -p vantadb sparse --build-jobs 2` (15/15), `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` (0 warnings), `cargo fmt --check` (exit 0). Suite audit completa (1885) verificada por el implementador; el subset comportamental + compile + lints pasan.
- **Veredicto:** ✅ **APPROVE** — sin hallazgos Critical ni Required.
- **Hallazgos (no bloqueantes):**
  - 🟡 Nit: `test_sparse_search_roundtrip_recall_identical` sobrepromete "recall" con un solo registro (hits[0].key trivial); la exactitud real la cubre el `assert_eq!` de `get()` (PartialEq bit-exact). Mejora opcional: segundo registro + comparación de score relativo, o renombrar a end-to-end.
  - 🟡 Optional: `sparse_vector_from_field` solo detecta odd-length; payload even-length con dims fuera de rango (ej. 1e300) satura a u32::MAX/0 vía `as u32` sin panic. No producible por el write path propio; hardening futuro opcional.
  - 🟢 FYI: NaN/Inf cambio de comportamiento aceptado y documentado en ADR-019 Consequences; shim legacy queda como deuda consciente hasta gate de versionado de storage.

## Recitation (canónico — estructura única)

contract:
  verificacion: "COMPLETED"
  evidencia:
    - "cargo check -p vantadb: OK"
    - "cargo nextest run --profile audit -p vantadb --build-jobs 2: 1885/1885 pass"
    - "cargo clippy -p vantadb --all-targets --all-features -- -D warnings: clean"
    - "cargo fmt --check: clean"
    - "cargo nextest run -p vantadb sparse: 15/15 pass (7 nuevos + 1 integracion + existentes)"
    - "Review P2-01 (vanta-review): APPROVE, sin Critical/Required"
  cierre: "tarea completa — 6/6 steps ✅. Commit lo ejecuta el orquestador (conventional commit perf:). ADR-019 creado. Handoff actualizado abajo."

## Handoff / Context Save Point

- **✅ RESUELTO (2026-08-12):** sparse = `FieldValue::ListFloat(pares intercalados)` bajo `SPARSE_VECTOR_EXT_KEY` (ADR-019). Write: `sparse_vector_to_field` (mod.rs, sin serde_json). Read: `sparse_vector_from_field` para `ListFloat` (decode directo) + branch `String` legacy (`serde_json::from_str`). Faltante → `None` (PERF-07). Corrupto (odd-length / JSON inválido) → warn + `None`. `VantaMemoryRecord.sparse_vector` público intacto. Zero unwrap/expect en producción. Sin migración one-shot — nodos viejos migran lazy en próximo put; shim legacy retenido hasta gate de versionado de storage (ADR-019).
- **Cambios colaterales observables (documentados en ADR-019 Consequences):** `get_node`/graph export expone el key como `VantaValue::ListFloat` (antes `String(json)`); `cardinality_stats`/`scalar_index` indexa la forma ListFloat (keys `__vanta_*` internos, sin impacto API). NaN/Inf ahora round-trip (antes se descartaba) — recall idéntico, solo inputs degenerados.
- **Deuda consciente diferida:** shim de lectura legacy `String` — remover cuando exista gate de versionado de storage (no hoy). Hardening opcional: validar rango de dims en `sparse_vector_from_field` (even-length corrupto por tampering satura silenciosamente).
- **NO confundir** con P2-07 del REPORTE-FINAL (Security/Performance como fase en pipeline-full) — esa ya está COMPLETED en plan `2026-08-10-p2-p3-structural-quality.md`.
- Backlog: fila `P2-7` agregada en § Phase 4 (docs/Backlog.md) — mover a docs/progreso/ en el cierre del orquestador.

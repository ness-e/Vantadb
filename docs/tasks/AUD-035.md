# AUD-035 — Split de megafiles core: `sdk/search/mod.rs`, `storage/engine/ops.rs`, `index/search.rs` (patrón REVIEW-05)

- **Estado:** completed-implementation (2026-08-16) — 1/3 commiteado (`5d96b536`), 2/3 implementados y verificados en working tree (commit pendiente = orquestador)
- **Prioridad:** 🔴 (megafiles core)
- **Área:** `src/sdk/search/`, `src/storage/engine/`, `src/index/search/`
- **Routing:** vanta-worker (Rust core SDK)
- **Fuente:** `docs/Backlog.md` ← `docs/reviews/audit-full-20260812-231204.md` (2026-08-12)
- **Patrón de referencia:** REVIEW-05, commit `92852f9f` — archivo original → `mod.rs` orquestador delgado + submódulos cohesivos; visibilidad `pub(crate)`/`pub(super)` mínima; tests movidos a `tests.rs` del dir con `#[cfg(test)] mod tests;`. API pública intacta (sin breaking changes), cero reescritura de lógica.

## Scope (3 megafiles)

| # | Megafile | L original | Estado al inicio de esta sesión | Estado final |
|---|----------|-----------|-------------------------------|--------------|
| 1 | `src/sdk/search/mod.rs` | 2521L | ✅ COMPLETO + COMMITEADO (`5d96b536`) | ✅ mod.rs orquestador 343L |
| 2 | `src/storage/engine/ops.rs` | 2131L | 🟡 extraído (delete/get/insert/txn) | ✅ ops.rs 362L + 4 módulos, verificado |
| 3 | `src/index/search.rs` | 2054L | 🟡 solo tests.rs extraído | ✅ search/mod.rs 52L + 6 submódulos, verificado |

**Nota:** el registro en `docs/progreso/README.md` marca AUD-035 ✅ completo, pero es PREMATURO (solo cubre el split 1). El trabajo real de los splits 2 y 3 NO está commiteado ni verificado. Esta sesión lo completa. **NO se tocan** `docs/Backlog.md` ni `docs/progreso/` (migración = orquestador).

**Fuera de scope — megafiles no citados por AUD-035** (hallazgos separados → nuevos items de backlog): `src/sdk/api.rs` (2023L), `src/sdk/graph.rs` (1978L), `src/parser/mod.rs` (1718L), `src/config.rs` (1595L), `src/sdk/types.rs` (1591L), `src/sdk/serialization/mod.rs` (1464L), `src/server/cli_server.rs` (1444L).

## Phase 1 — Tipo / Checks obligatorios

Rust core SDK:
- `cargo check -p vantadb` → exit 0
- `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` → exit 0
- `cargo nextest run --profile audit -p vantadb --build-jobs 2` → verde (timeout ~900000 ms)
- `cargo fmt --check` sobre archivos tocados → exit 0

## Phase 2 — Discovery / Blast Radius (ya ejecutado)

**Split 1 (sdk/search):** `VantaEmbedded::search` → `vantadb-mcp/src/handlers/tools.rs`; `explain_memory_search` → `vantadb-python/src/lib.rs:1767`, `vantadb-wasm/src/lib.rs:1024`; `search_multi`/`search_all` → interno. 29 métodos en impl único → 29 en submódulos (dictamen vanta-review: cuerpos IDENTICAL pre/post).

**Split 2 (storage/engine/ops.rs):** métodos sobre `StorageEngine` — los callers usan el tipo, no la ruta de módulo. `mod.rs` re-exporta `BatchInsertOptions, InsertMode` desde `ops` (patrón pre-existente con init/maintenance/stats/partition). Campos privados accesibles desde sub-módulos de `engine` (regla de privacidad Rust: descendientes del módulo del struct).

**Split 3 (index/search.rs):** módulo `pub(crate) mod search` (index/mod.rs:16). Símbolos consumidos fuera del módulo:
- `SearchProfile` → `graph.rs:1` (`use crate::index::search::SearchProfile;`) → requiere re-export en mod.rs
- `search_nearest` (pub, método CPIndex) → `graph.rs` (tests), `ivf.rs` (tests), `core.rs` (tests), `physical_plan/vector.rs:64`, `sdk/api.rs:1174` — método sobre CPIndex, funciona desde cualquier submódulo
- `select_neighbors` (pub(crate), método CPIndex) → `graph.rs` (insert/shrink, 3 call sites)
- `search_ivf`/`search_scann`/`search_with_method` (pub(crate)) → interno + tests.rs
- `give_nl`/`NL_POOL` → solo interno (search.rs)
- Implicación: split puramente mecánico; sin investigación web (cero dependencias externas, cero API pública nueva).

## Phase 3 — Investigación web

No aplica (refactor interno sin dependencias externas ni ambigüedad de diseño). Omitida.

## Phase 4 — Pasos atómicos

### Split 1 — `src/sdk/search/mod.rs` (2521L → 343L) — ✅ COMPLETO

1. ✅ **[CHECK-IN]** `cargo check -p vantadb` en árbol sano (10.7s green)
2. ✅ **Estructura de submódulos** en `src/sdk/search/` (lexical, vector, sparse, hybrid, explain, audit, debug_ops, multi) + `mod.rs` orquestador
3. ✅ **Tests movidos** → `src/sdk/search/tests.rs` (29 fns, imports explícitos)
4. ✅ **Visibilidad** `pub(super)` en 4 métodos cruzados; `pub fn` de bindings intactos
5. ✅ **[VERIFY]** check / nextest sdk (366/366) / fmt / clippy
6. ✅ **Review** vanta-review P2-01: **approve** (cuerpos IDENTICAL, sin fugas de API)
7. ✅ **Commit** `5d96b536` — selectivo a `src/sdk/search/*` (10 archivos)

### Split 2 — `src/storage/engine/ops.rs` (2131L → 362L) — 🟡 EXTRACCIÓN HECHA, FALTA VERIFICAR/CERRAR

1. ✅ **[CHECK-IN]** baseline: `cargo check -p vantadb` → **exit 0** (2026-08-16, árbol con splits en progreso compila)
2. ✅ **Módulos extraídos** (sin commitear):
   - `src/storage/engine/delete.rs` (delete/purge — 13.9KB)
   - `src/storage/engine/get.rs` (get/get_many — 14.3KB)
   - `src/storage/engine/insert.rs` (insert/batch_insert — 40.9KB)
   - `src/storage/engine/txn.rs` (txn/snapshot/MVCC — 15.7KB)
   - `mod.rs` cableado: `mod delete; mod get; mod insert; mod txn;` + doc-link actualizado a `insert::StorageEngine::batch_insert_with_opts`
3. ✅ **API pública** — `mod.rs:49` sigue re-exportando `BatchInsertOptions, InsertMode` desde `ops`; métodos sobre `StorageEngine` intactos
4. ✅ **[VERIFY]** check/clippy/nextest/fmt globales pasaron sobre el árbol final (2026-08-16; nextest 1886/1886, 1 skip)
5. ✅ **[HYGIENE]** doc-comments de módulo duplicado corregidos (delete.rs → delete/purge/tombstones; get.rs → reads; insert.rs → insert/batch; txn.rs → transaccional)
6. ⬜ **Commit** — NO (lo ejecuta el orquestador; esta sesión NO commitea)

### Split 3 — `src/index/search.rs` (2054L → orquestador) — ⬜ PENDIENTE

1. ✅ **[CHECK-IN]** baseline: `cargo check -p vantadb` → **exit 0** (2026-08-16)
2. ✅ **Tests extraídos** (sesión anterior) → `src/index/search/tests.rs` (1379L, `#[cfg(test)] mod tests;` en search/mod.rs)
3. ✅ **Convertir** `src/index/search.rs` → `src/index/search/mod.rs` (orquestador 52L)
4. ✅ **Crear submódulos** (bloques cohesivos del impl CPIndex):
   - `pool.rs` (16L) ← `NL_POOL` thread_local + `give_nl`
   - `profile.rs` (80L) ← `SearchProfile` (variantes debug/release)
   - `layer.rs` (393L) ← `search_layer` (hot path HNSW)
   - `neighbors.rs` (63L) ← `select_neighbors` + `use_flat_search`
   - `nearest.rs` (163L) ← `search_nearest` (entry point)
   - `alternate.rs` (116L) ← `search_ivf` + `search_scann` + `search_with_method`
   - `mod.rs` (52L) ← `impl VecIndex for CPIndex` (35L) + `pub(crate) use profile::SearchProfile;` + `#[cfg(test)] mod tests;`
5. ✅ **Visibilidad:** `give_nl` → `pub(super)` (la usa layer.rs); `use_flat_search` → `pub(super)` (la usa nearest.rs + tests); métodos `SearchProfile` internos → `pub(super)` (start/end_compute, record_*, log — los usa layer.rs/nearest.rs); `SearchProfile` → re-export `pub(crate)` en mod.rs (la usa graph.rs); `pub fn search_nearest` / `pub(crate) search_layer/select_neighbors/search_ivf/search_scann/search_with_method` SIN cambios
6. ✅ **Tests:** `use super::*` → imports explícitos (patrón aprobado en sdk/search); `HashSet` movido al header (era import mid-file); `euclidean_distance_squared_f32` gated `#[cfg(miri)]` (solo lo usa miri)
7. ✅ **[VERIFY]** check / clippy / nextest / fmt (mecánica global, ver RESULTADO)
8. ⬜ **Commit** — NO (lo ejecuta el orquestador)

## Definition of Done (contrato de verificación mecánica — TODOS deben pasar)

- `cargo check -p vantadb` → exit 0
- `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` → exit 0
- `cargo nextest run --profile audit -p vantadb --build-jobs 2` → exit 0, 0 failures (timeout ~900000 ms)
- `cargo fmt --check` sobre archivos tocados → exit 0
- `src/sdk/search/mod.rs` < 400L (343L ✅) · `src/storage/engine/ops.rs` < 400L (362L ✅) · `src/index/search/mod.rs` < 200L
- API pública intacta: `search`, `search_with_method`, `explain_memory_search`, `search_multi`, `search_all`, `search_nearest`, `select_neighbors`, `BatchInsertOptions`, `InsertMode` — signaturas idénticas
- `rg "fn (search_nearest|select_neighbors|search_ivf|search_scann|search_with_method)" src/index/search/` → signaturas `pub`/`pub(crate)` intactas

## Phase 5 — Review

- Split 1: ✅ dictamen vanta-review P2-01 approve (2026-08-16, sesión fresca)
- Splits 2+3: ⬜ pendiente — enviar a `vanta-review` (agente distinto) antes de commit; foco: (a) cero cambio de semántica/signaturas, (b) visibilidad mínima correcta, (c) imports de tests coherentes. Nota implementador: el único ajuste de visibilidad respecto al original es `pub(super)` en 5 métodos de `SearchProfile` + `give_nl` + `use_flat_search` (eran privados en un único archivo; ahora cruzan submódulos hermanos) — mismo patrón aprobado en el split 1

## RESULTADO (bloque exigido al finalizar)

```
RESULTADO: ✅ COMPLETO
- checks: check ✅ (exit 0) · clippy ✅ (exit 0, -D warnings, all-targets/all-features) · nextest ✅ (1886/1886 passed, 1 skipped, 0 failures) · fmt ✅ (exit 0)
- split 1: commit 5d96b536 (sdk/search, mod.rs 343L) — ✅ review approve
- split 2: ops.rs 362L; módulos delete.rs/get.rs/insert.rs/txn.rs — ✅ verificado + doc-comments corregidos
- split 3: search/mod.rs 52L; submódulos pool(16L)/profile(80L)/layer(393L)/neighbors(63L)/nearest(163L)/alternate(116L) + tests.rs 1379L
- signaturas públicas verificadas: search_nearest/pub(crate) search_layer/select_neighbors/search_ivf/search_scann/search_with_method/BatchInsertOptions/InsertMode intactas ✅
- commits: NINGUNO (los ejecuta el orquestador)
- nota: build del binario CLI regenera completions/ (subcomando tui — stale pre-existente) → restaurados a HEAD para no ensuciar el diff
- pendiente: review vanta-review (P2-01) de splits 2+3 por agente distinto
```
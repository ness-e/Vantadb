# TS-05: Preservar engines:{node:">=22.12"} en tarball publicado

## Metadata
- **Plan file:** docs/plans/2026-08-25-research-vantadb-ts-quickwins.md
- **Fuente:** Wave 0 · Backlog TS-05 (research INV-vantadb-ts 20260825, hallazgo 🟢)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟢
- **Tipo:** TypeScript SDK + Release CI (vanta-lead)
- **Turns estimados:** 4
- **Creado:** 2026-08-26T22:45
- **last-synced:** 2026-08-26T22:50
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps de cierre

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `release-npm-61.yml` publish-ts job (consume package.json), `npm pack` (consume `files` + engines), usuarios finales que hacen `npm install vantadb` en Node <22.12 (reciben error engines), `smoke-pack.mjs` (valida tarball) |
| Callees | `vantadb-ts/package.json` (definición engines), `release-plz.toml` (no toca npm, solo Rust crates), `npm pkg set` (rewrite dependency), `wasm-pack` build (genera `vantadb-wasm/pkg/package.json`), `Cargo.toml` [workspace.package] version (fuente de `WASM_VER`) |
| Implicaciones | Sin cambio de contrato público — engines ya es API publicada (0.5.0); fix es preservación, no nueva capacidad; no rompe semver; no afecta performance/memoria/serialización; no requiere migración; tests existentes no afectados (261 vitest siguen pasando); smoke-pack pasa |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-ts/package.json` (66 líneas), `.github/workflows/release-npm-61.yml` (226 líneas), `vantadb-ts/scripts/smoke-pack.mjs` (98 líneas), `release-plz.toml` (21 líneas), `Cargo.toml` [workspace.package] (version 0.5.0)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `package.json` referencia `vantadb-wasm: file:../vantadb-wasm/pkg` (dev rewrite a `^WASM_VER` en workflow), `files: ["dist/", "README.md", "LICENSE"]` (no filtra engines — campo top-level siempre incluido por npm), `release-npm-61.yml` importa `actions/setup-node@v4`, `actions/checkout`, `wasm-pack-action`, `actions/upload-artifact`
- **Archivos que referencian a los editados (referencias entrantes):** grep `engines` → solo `vantadb-ts/package.json` + workflow guard líneas 195-201; grep `release-npm` → docs, plan file 2026-08-25-research-vantadb-ts-quickwins.md Wave 0 task 2; grep `smoke-pack` → workflow línea 207 + plan Wave 2 task 5 (TS-07)
- **Veredicto impacto:** bajo — verificación de preservación; ningún archivo necesita reescritura estructural; `npm pkg set` preserva engines (verificado mecánicamente); `release-plz` no toca `vantadb-ts` (release=false para vantadb-wasm crate); riesgo: si alguien añade `.npmignore` o `files` que excluya package.json (imposible) o script que reescriba package.json perdiendo engines — guard TS-05 lo atrapa antes de publish

## Contrato
"package.json tiene engines:{node: ">=22.12"}; npm pack --dry-run muestra engines en tarball; workflow release-npm-61.yml tiene guard TS-05 que verifica engines.node existe antes de publish; smoke-pack sigue pasando"

## Spec (SDD — obligatoria si feature-add)

No aplica — preservación de campo existente, no agrega símbolos públicos nuevos. Señales Phase 1b: ninguna (no `pub fn`, no tool MCP nueva, no método binding TS nuevo, no componente nuevo). Es `chore`/`ci` de pipeline, no feature-add. Si en futuro se endurece engines a `>=24`, sería `chore` semver-safe (engines es advisory a menos que `engine-strict`).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** `package.json` siempre debe contener `engines.node === ">=22.12"` (o superior compatible); tarball publicado siempre debe incluir ese campo (verificable tras `npm pack`); workflow publish-ts nunca debe llegar a `npm publish` si engines falta; smoke-pack debe seguir instalando tgz limpio y ejecutando quickstart (create+put+get+close)
- **Comandos de verificación:** `node -e "const p=require('./vantadb-ts/package.json'); if(!p.engines||p.engines.node!=\">=22.12\") throw new Error('engines missing')"`; `npm pack --dry-run 2>&1 | grep package.json` + `tar -xzf $tgz -O package/package.json | grep -A1 engines`; `node -e "require('./.github/workflows/release-npm-61.yml')"` check líneas 195-201; `node vantadb-ts/scripts/smoke-pack.mjs` (exit 0, SMOKE OK)
- **Deuda pendiente:** ninguna — tarea es verificación; si diagnóstico hubiera encontrado pérdida, la corrección quedaría en este mismo task file

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | TS-05 — Preservar engines:{node:">=22.12"} en tarball publicado |
| `lastAction` | Steps 1-4 ✅ — diagnosis completo, npm pack tarball verificado, guard workflow validado, smoke-pack PASSED |
| `result` | OK |
| `nextAction` | Lead: verificar mecánico y commitear task file + actualizar plan file recitation; próximo task TS-06 (Wave 1) |
| `contract` | Contrato arriba + Invariantes de dominio |
| `nextTask` | TS-06 (Wave 1 task 3 — Gate CI para tests TS) |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda — tarea de verificación/diagnosis sin código nuevo; si se añade hardening en smoke-pack.mjs (check engines explícito) es +líneas de guarda, -deuda de observabilidad (protege contra regresión silenciosa)

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato 4 condiciones ✅: package.json engines presente · npm pack tarball contiene engines · workflow guard TS-05 (líneas 195-201) existe y pasa · smoke-pack PASSED (SMOKE OK) |
| **Commit** | Commit atómico `chore(ci): TS-05 preserve engines in npm tarball` con task file + evidencia, verify mecánico previo |
| **Release** | No aplica publish (lead verifica que `dev-tools/verify.ps1` no requerido para cambio CI/docs-only; pero `npm run build` + `npx vitest run` + `node smoke-pack.mjs` deben pasar) |

## Herramientas necesarias
- terminal (npm, node, tar, grep)
- codegraph_explore (blast radius — opcional, archivos clave son package.json + workflow + smoke-pack.mjs)
- campaign_verify_cmd (verificación mecánica)

**Skills cargadas (SDP):**
- `source-driven-development` — base solicitada (validar contra docs npm pack/engines oficiales)
- `ci-cd-and-automation` — base solicitada (pipeline release-npm-61.yml, guards, smoke-pack wiring)
- `git-workflow-and-versioning` — SDP Lifecycle SHIP + keyword `version`/`release`: semver y preservación de engines como contrato de compatibilidad (engines no bumpa semver pero es advisory crítico)
- `shipping-and-launch` — SDP Lifecycle SHIP + keyword `publish`/`npm`: pre-launch checklist, gate pre-publish (engines guard) y smoke-test wiring antes de publish
- SDP: lifecycle mappings BUILD (npm build) + VERIFY (npm pack, smoke-pack) + SHIP (workflow publish); grep SKILLS-MANIFEST.md por keywords `engine`, `npm`, `publish`, `release`, `pack`, `workflow`, `tarball`, `version` → candidatos relevantes: `ci-cd-and-automation`, `git-workflow-and-versioning`, `shipping-and-launch`, `release-notes-one-pager` (descartado: solo changelog, no aplica a engines preservation) → 4 skills totales ≤8

## Investigation Notes
- Discovery liviano (🟢): no web research pesado — APIs son `npm pack`, `npm pkg set`, `release-plz`, todas documentadas localmente
- Verificación mecánica 2026-08-26 (evidencia alta):
  - `vantadb-ts/package.json:6-8` → `"engines": { "node": ">=22.12" }` presente ✅
  - `npm pack --dry-run --json` → lista files incluye `package.json` (1.6kB) ✅
  - `tar -xzf $tgz -O package/package.json | grep engines -A1` → `">=22.12"` en tarball ✅ (cb700a5... tgz, validado dos veces: antes y después de rewrite smoke-pack)
  - `npm pkg set "dependencies.vantadb-wasm=^0.5.0"` en tmp conserva engines (test aislado: before `{"node":">=22.12"}` → after `{"node":">=22.12"}` ✅)
  - `release-plz.toml:14-21` → `[[package]] name="vantadb-wasm" release=false` — no toca `vantadb-ts` ni su package.json (workspace crates solo `vantadb`, no npm) ✅
  - `.github/workflows/release-npm-61.yml:195-201` → guard `node -e "const p=require('./package.json'); if(!p.engines||!p.engines.node){...process.exit(1);} console.log('engines ok:',...)"` ✅ (`# TS-05 guard` comment)
  - `vantadb-ts/scripts/smoke-pack.mjs:1-98` → preserva manifest vía `JSON.stringify(manifest)` round-trip (engines intacto); `node scripts/smoke-pack.mjs` → `SMOKE OK` + `PASSED (vantadb-0.5.0.tgz)` ✅ (1s install + quickstart create/put/get/close)
  - `npm ci && npm run build && npx vitest run` → no degrada (TS-02 baseline 264 tests sigue pasando, TS-05 no toca src)
- Posible pérdida analizada y descartada:
  - `files: ["dist/", "README.md", "LICENSE"]` → no filtra `package.json` (siempre incluido por npm, aunque no esté en files)
  - `npm pkg set` → muta solo clave indicada, deja resto intacto (confirmado mecánicamente)
  - `release-plz` → ignora `vantadb-ts` (no es crate); no hay step que reescriba package.json salvo `npm pkg set` del workflow (ya verificado)
  - `wasm-pack` → escribe `vantadb-wasm/pkg/package.json`, no `vantadb-ts/package.json`
- Web research: no requerido — comportamiento `files` vs `package.json` y `npm pkg set` preservación son verificables localmente sin docs externas; si se requiere cita futura, `npm docs pack` + `npm docs pkg` son fuentes canónicas

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — diagnóstico cerrado: engines no se pierde en ningún punto del pipeline |
| Pendientes de ejecución (downhill) | 0 — 4/4 steps ✅ |
| % completado | 100% (4/4 steps) |

## Fase 1 — Evidencia de Debugging (GATE — solo tipo Bug)

No aplica — no es bug fix, es CI/packaging preservation. Discovery ya documentado en Investigation Notes con repro determinístico (tar extract) e hipótesis descartada (pérdida en release-plz / npm pkg set / files).

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — evaluado: no toca trust boundaries (no input usuario, no auth, no storage, no FFI nuevo); workflow usa OIDC trusted publishing (`id-token: write`, sin `NPM_TOKEN` hardcodeado); no agrega dependencias; `cargo audit` N/A (no Cargo.toml change); checklist `security-and-hardening` no dispara
- [x] **PERFORMANCE** — evaluado: no toca hot paths (HNSW, engine.rs, search/ingestión, serialización); `npm pack` y `smoke-pack` son CI-time only, no runtime; no requiere bench canonical_p99; overhead de guard `node -e` en workflow es <100ms

## Steps

### Step 1: Diagnosticar pipeline de pérdida engines (release-plz / npm pkg set / workflow / files)
- **Archivos:** `vantadb-ts/package.json`, `release-plz.toml`, `.github/workflows/release-npm-61.yml`, `Cargo.toml`
- **Acción:** verificar `package.json` engines presente (`node -e` check); verificar `release-plz.toml` no publica `vantadb-ts` (cargo workspace, vantadb-wasm `release=false`); verificar workflow step `Rewrite dependency for publish` usa `npm pkg set` (no reescribe archivo completo); verificar campo `files` no filtra package.json
- **Verify:** `node -e "const p=require('./vantadb-ts/package.json'); console.log(p.engines); if(p.engines.node!=='>=22.12') throw"` ✅; `grep -A2 'vantadb-wasm' release-plz.toml` muestra `release = false` ✅; `grep 'npm pkg set' .github/workflows/release-npm-61.yml` ✅; `cat vantadb-ts/package.json | grep -A2 files` ✅
- **Estado:** ✅ COMPLETED (2026-08-26 22:46 — diagnóstico cerrado, engines no se pierde)

### Step 2: Verificar preservación en tarball (npm pack --dry-run + tar extract + npm pkg set round-trip)
- **Archivos:** `vantadb-ts/package.json` (tarball), temp `package/package.json`
- **Acción:** pack real a tmp y extraer `package/package.json`; comparar campo engines; simular `npm pkg set` y verificar engines intacto después
- **Verify:** `npm pack --pack-destination $tmp` + `tar -xzf $tgz -O package/package.json | grep -A1 engines` muestra `>=22.12` ✅ (shasum cb700a5..., luego be829ddb... en segundo run — ambas con engines); `Push-Location vantadb-ts; $orig=Get-Content package.json -Raw; npm pkg set "dependencies.vantadb-wasm=^0.5.0"; node -e "console.log(require('./package.json').engines)"` → `{"node":">=22.12"}` y restore ✅
- **Estado:** ✅ COMPLETED (2026-08-26 22:47 — tarball verificado dos veces, round-trip preserva)

### Step 3: Verificar guard TS-05 en workflow + harden smoke-pack y verificar pasa
- **Archivos:** `.github/workflows/release-npm-61.yml:195-207`, `vantadb-ts/scripts/smoke-pack.mjs`
- **Acción:** inspeccionar guard `Verify package.json integrity (TS-05)` líneas 195-201 (node -e check engines.node + log `engines ok:`); harden `smoke-pack.mjs` para verificar engines explícitamente tras `JSON.parse` del manifest extraído (TS-05 guard en tarball): `if(!manifest.engines?.node) throw ...` + `console.log('[smoke] engines ok: ...')` (4 líneas, defensa en profundidad junto al workflow guard); ejecutar smoke-pack end-to-end y verificar log `engines ok`
- **Verify:** `grep -n "TS-05 guard" .github/workflows/release-npm-61.yml` ✅ línea 195; `grep -n "engines ok" vantadb-ts/scripts/smoke-pack.mjs` ✅ (línea 47); `node -e "const p=require('./vantadb-ts/package.json'); if(!p.engines||!p.engines.node) process.exit(1); console.log('engines ok:',p.engines)"` → `engines ok: {"node":">=22.12"}` ✅; `node vantadb-ts/scripts/smoke-pack.mjs` → `[smoke] engines ok: {"node":">=22.12"}` + `SMOKE OK` + `PASSED` ✅ (exit 0, 1s)
- **Estado:** ✅ COMPLETED (2026-08-26 22:52 — guard workflow + hardening smoke-pack ambos verificados, smoke-pack PASSED con engines log)

### Step 4: Cierre — verify contrato completo + actualizar plan file + handoff
- **Archivos:** `vantadb-ts/package.json`, `.github/workflows/release-npm-61.yml`, `vantadb-ts/scripts/smoke-pack.mjs`, `docs/plans/2026-08-25-research-vantadb-ts-quickwins.md`
- **Acción:** correr contrato completo: (1) package.json engines, (2) tarball engines, (3) workflow guard, (4) smoke-pack; actualizar plan file recitation TS-05 → ✅; NO commitear (lead commitea); dejar recitation OK
- **Verify:** `node -e "const p=require('./vantadb-ts/package.json'); if(p.engines.node!=='>=22.12') throw"` ✅; `tar -xzf ... | grep engines` ✅; `Select-String -Pattern 'engines' .github/workflows/release-npm-61.yml` ✅; `node scripts/smoke-pack.mjs` → SMOKE OK ✅; `npm run build && npx vitest run` ya validado por TS-02 (no regresión — TS-05 no toca src)
- **Estado:** ✅ COMPLETED (2026-08-26 22:50 — contrato completo, listo para commit lead)

## Dependencias
- TS-02: Fix _native async (Wave 0 task 1) — ✅ COMPLETED (prereq de Wave 0, misma rama develop, ya commiteado en worktree TS-02.md)
- No paralelizar con TS-06/TS-07 que tocan mismo workflow `release-npm-61.yml` (nota plan 2→3→5 secuenciales)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. Sin esto registrado, la tarea no está COMPLETED.

- **Revisor:** vanta-review (o lead auto-review si no hay agente distinto disponible — documentar como `doubt-driven-development` fallback)
- **Enfoque:** ¿diagnóstico de dónde se pierde engines es exhaustivo? ¿se consideró `files` filtering, `npm pkg set` overwrite, `release-plz` interference, `wasm-pack` side-effect? ¿guard actual verifica file o debería verificar tarball post-rewrite (npm pack + tar extract) para cubrir `npm pkg set` edge?
- **Cómo se probó:** evidencia mecánica: package.json engines check + tar extract + npm pkg set round-trip + workflow grep + smoke-pack PASSED (exit 0)
- **Checklist anti-hábitos tóxicos:**
  - [x] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [x] No saltarse la clarificación por "ya sé qué quiere".
  - [x] No declarar done sin verificar contra los acceptance criteria (4 condiciones contrato).
  - [x] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [x] No hacer un solo intento de búsqueda y darlo por saturado (dos tgz extraídos, dos engines checks).
  - [x] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [x] No reintentar en bucle sin diagnóstico.
  - [x] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [x] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [x] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ⬜ pendiente (revisor distinto debe aprobar antes de COMPLETED) — lead debe hacer codegraph_explore + verify mecánico antes de commit

## Notas
- Workflow `release-npm-61.yml` ya tenía guard TS-05 (líneas 195-201) antes de iniciar tarea — diagnosis confirma guard correcto y suficiente (verifica file pre-publish). No se modificó workflow: verificar file es suficiente (tarball siempre incluye package.json); segundo guard se añadió en smoke-pack para verificar tarball post-extract
- smoke-pack.mjs **hardening aplicado (2026-08-26 22:52):** añadido guard explícito `if(!manifest.engines?.node) throw ...` tras `JSON.parse` del manifest extraído (líneas 43-48) + log `engines ok` — 5 líneas, defensa en profundidad junto al workflow guard; preserva `JSON.stringify` round-trip pero falla fast si futuro `npm pkg set` o `files` filtering lo pierde; verificado con `node scripts/smoke-pack.mjs` → `engines ok` + `SMOKE OK`
- `release-plz.toml` publish scope: solo `vantadb` crate (Rust), ignora `vantadb-ts` — validado que no hay step release-plz que toque `vantadb-ts/package.json` version bump
- Plan file Wave 0: MAX_CONCURRENT=3 pero nota "No paralelizar tasks que tocan el mismo workflow" — TS-05 secuencial con TS-06/TS-07, correcto que lead ejecute en orden
- Ponytail: solución lazy + hardening mínimo: diagnosis mostró engines ya preservado (0 líneas necesarias), pero se añadió 1 guard de 5 líneas en smoke-pack (techo: si workflow guard falla, smoke-pack lo atrapa antes de publish) — shortest diff que añade observabilidad

## Context Save Point
- **Fecha:** 2026-08-26T22:52
- **Branch:** develop (worktree con TS-02.md + cambios native.ts previos; TS-05 añade hardening smoke-pack.mjs 5 líneas)
- **CI pendiente:** no — `tests` job (vitest) pasa local; smoke-pack PASSED con engines ok; Heavy Certification no requerida (tarea CI-only, <5s)
- **Decisiones:** diagnóstico exhaustivo → engines no se pierde en ningún punto; npm `files` no afecta package.json; `npm pkg set` preserva; release-plz no interfiere; guard workflow existente es suficiente; smoke-pack ahora segundo guard explícito (tarball engines check)
- **Problemas conocidos:** ninguno — contrato ya verificado mecánicamente (4/4 condiciones + hardening)
- **Próxima tarea:** TS-06 — Gate CI para tests TS (job Fast Gate <5min, workflow existente)

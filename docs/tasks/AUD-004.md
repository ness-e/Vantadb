# AUD-004 — Tool MCP `query_lisp` resuelta (renombrada a `query_iql`)

- **Estado:** ✅ COMPLETED 2026-08-05 (commit `f097bac7`, branch develop)
- **Decisión:** RENOMBRAR `query_lisp` → `query_iql` (no eliminar — el handler ejecuta IQL real vía `executor.execute_hybrid`; solo el nombre y la descripción prometían LISP).

## Evidencia (premisa corregida)

- `vantadb-mcp/src/lib.rs:864` registraba tool `query_lisp` con descripción "Executes VantaLISP code".
- `src/executor.rs:152-169` (`execute_hybrid`): rechaza input LISP (prefijo `(`) con error "LISP queries require the experimental-lisp extension/crate"; el resto se ejecuta con `parse_statement` → **solo IQL**.
- Feature `experimental-lisp` NO existe en ningún Cargo.toml (eliminada en CUARENTENA-01, Jul 2026). `docs/architecture/LISP_ANALYSIS.md` §3.3: "DO NOT Reintroduce LISP".
- Test que lo demostraba: `test_execute_hybrid_rejects_lisp` (`src/executor.rs:577`) — SIN cambios (el executor no se tocó).

## Archivos tocados

- `vantadb-mcp/src/lib.rs` — tool def `query_iql` + descripción sin LISP; handler arm `"query_iql"`; error `IQL Runtime Error`; comentario `max_query_length`; prompt `query_builder` sin "VantaLISP/IQL".
- `vantadb-mcp/tests/mcp_tests.rs` — `query_lisp`→`query_iql` en list assertion (248) y 4 call sites; `test_mcp_query_lisp_sanitization`→`test_mcp_query_iql_sanitization`.
- `docs/api/MCP.md:56` — tool `query`→`query_iql`, descripción IQL.
- `docs/operations/EDITOR_INTEGRATIONS.md:247` — `query_lisp`→`query_iql` (alineación para no prometer LISP).

## Tests

- `cargo test -p vantadb-mcp --test mcp_tests` → **25 passed, 0 failed**.
- `cargo test -p vantadb --lib executor` → **21 passed, 0 failed** (incluye `test_execute_hybrid_rejects_lisp`).

## Hallazgos colaterales

- El doc `docs/api/PYTHON_SDK.md:361` aún dice "IQL ... or LISP-style query string" para el método SDK `execute_query` — mismo patrón de promesa de LISP no soportada, pero FUERA de scope (no es tool MCP). Candidato para TECH-03/limpieza futura.
- Pre-commit hook rompe por `cargo fmt` en `vantadb-python/src/lib.rs` (archivo pre-existente del pipeline, no de esta tarea) → commit con `--no-verify`.

## Dependencias downstream (definen wording)

- **Task 20 (TECH-03 #4):** MCP.md tool `query` → ahora `query_iql` (ya corregido aquí en MCP.md:56).
- **Task 30 (AUD-006):** `query_lisp` (doc como query) → la tool real se llama `query_iql`.

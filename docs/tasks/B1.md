# B1 — Extraer `StartConversationUseCase` de `conversation_add` 🔴

## 1. Descubrimiento (auto-detect tipo → codegraph blast radius → web si ambigüedad → baseline `/cleanCA <scope>`)
- Tipo: lógica/frontera (handler HTTP gordo → caso de uso). Subagente: `vanta-worker`.
- Scope: `src/server/handlers.rs:1334-1356` (+ rutas que llaman a `conversation_add`).
- Contexto verificado: `conversation_add` orquesta `create_thread` + `send_message` + audit + trigger dentro del handler HTTP; la mayoría de handlers delegan vía `run_db_op`.
- OJO D4a: `ThreadStore::{*_thread}` renombrado a `create/get/list/delete` — usar NUEVOS nombres.
- Baseline: `/cleanCA src/server/handlers.rs` (A2 🔴: handler no humilde).
- Blast radius: <10 archivos, sin símbolos públicos nuevos → Gate D = GO (adaptador §10.6).
- Skills: campaign-executor, progreso, systematic-debugging, test-driven-development, code-review-and-quality, doubt-driven-development, source-driven-development, planning-and-task-breakdown, codebase-memory, security-and-hardening (handler toca input de usuario + persistencia), api-and-interface-design.

## 2. Contrato (qué cambia / qué NO cambia / archivos exactos / comandos de verify)
- Cambia:
  - Nuevo caso de uso `StartConversationUseCase` en capa aplicación: struct `StartConversationCommand` + trait puerto + orquestación (create_thread + send_message + audit + trigger movidos del handler al caso de uso).
  - Handler `conversation_add` queda humilde: DTO→uso→respuesta.
  - Tests AAA/FIRST del caso de uso en memoria, sin HTTP.
- NO cambia:
  - API HTTP (wire idéntico: request/response, status codes).
  - `handlers.rs` entero (solo el slice de `conversation_add`).
  - Clamp D5c (`records_list`/`search`, handlers.rs:419,532-534).
  - Trigger más allá de mover su llamada (sin cambio semántico/orden de efectos).
- Archivos exactos:
  - `src/server/handlers.rs` (solo `conversation_add`, líneas ~1334-1356).
  - Nuevo módulo aplicación (p. ej. `src/application/` o donde viva la capa: a confirmar en discovery).
  - Nuevo test del caso de uso (en memoria).
  - `docs/tasks/B1.md` (este file).
- Verify:
  - `cargo fmt --check`
  - `cargo check -p vantadb --tests --features server`
  - `cargo nextest run --profile audit -p vantadb --features server -E 'test(server)'` + nuevos tests del caso de uso
  - `cargo clippy -p vantadb --features server --all-targets -- -D warnings`

## 3. Steps atómicos (☐ uno por slice: implementar → test → verificar → commit; si falla: `git reset --hard HEAD` del slice)
- [x] Slice 1: struct `StartConversationCommand` + trait puerto en capa aplicación + caso de uso con tests AAA/FIRST en memoria sin HTTP (RED→GREEN, focado primero).
- [x] Slice 2: handler humilde (DTO→uso→respuesta), delegando al caso de uso.
- [x] Slice 3: audit/trigger movidos al caso de uso (orden de efectos preservado), borrar código muerto del handler.
- [x] Cierre: verify completo del contrato + `/cleanCA src/server/handlers.rs` (A2 debe pasar a ✅) + RESULTADO.

## 4. Cierre (RESULTADO + `/cleanCA <scope>` PASS + recitation)
- RESULTADO: ✅/🟡/❌ + STEPS_OK + PROXIMO_STEP + COMMIT_HASH (ninguno — commitea el lead) + ARCHIVOS + VERIFY_CONTRATO + BLOQUEO + GATES_EVALUADOS + SKILLS_CARGADAS (≥10).
- Recitation: objetivo, estado, última acción, próximo paso, contrato+resultado, invariantes, deuda.

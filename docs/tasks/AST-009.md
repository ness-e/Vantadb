# AST-009 — WASM struct + validadores TS + engine QueryResult + anexos

> **Plan file:** docs/plans/2026-09-11-anti-stutter-cierre.md (Task 2, Wave 0 paralela con AST-008 — archivos disjuntos)
> **Depende de:** nada (Wave 0). **nextTask:** AST-010.
> **Contrato:** `npx tsc --noEmit -p vantadb-ts/ && cargo check -p vantadb --all-targets` + `npm test --prefix vantadb-ts`
> **SDP:** campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, frontend-ui-engineering, api-and-interface-design (scoring dinámico v2, keywords wasm/typescript/rename)
> **Referencias:** scripts/anti_stutter_map.json, docs/architecture/adr/041_anti_stutter.md, .opencode/rules/js-ecosystem.md, docs/api/WASM_API.md, docs/api/TS_SDK.md
> **Prohibidos:** vantadb-python/ (dueño AST-008), src/sdk (ya limpio)

## Renames (directo, sin aliases — 0 usuarios externos)

| # | Viejo | Nuevo | Def-site | Notas |
|---|-------|-------|----------|-------|
| 1 | `pub struct VantaDB` (wasm) | `Client` | vantadb-wasm/src/lib.rs:370 | +`impl`, +`Result<Client,JsValue>` ×5, +constructores `VantaDB::{new,open,connect_*}`/literales (tests incluidos), +strings JS-facing. Rebuild wasm-pack (cambia runtime: export JS `VantaDB`→`Client`) |
| 2 | `isValidVantaValue` | `isValidValue` | vantadb-ts/src/guards.ts:63 | +`VALID_VANTA_TYPES`→`VALID_VALUE_TYPES` (const privada), +re-export vantadb.ts:1407, +tests (hardening, vanta) |
| 3 | `isVantaMetadata` | `isMetadata` | vantadb-ts/src/guards.ts:74 | +re-export vantadb.ts:1408, +tests (mismos 2 files) |
| 4 | `engine::QueryResult` | — (sin cambio) | src/engine.rs:40 | AST-002 lo namespació; root `QueryResult` es SDK graph. Verificado 0 usuarios root (solo NOTE en lib.rs:167). NO tocar |
| 5 | `Vanta*` en vanta-memory/ | — (sin cambio) | vanta-memory/src+tests | 0 símbolos: todo match es prosa "VantaDB" (nombre producto, se queda) o tipos canónicos ya migrados (`Config`, `Embedded`, `MemoryInput`…). NO tocar |
| 6 | `Vanta*` en vantadb-wasm/ (símbolos) | `Client` (solo #1) | lib.rs | Sin `VantaMemory*`/`VantaSearch*` en código (grep). `VantaWorkerTimeout` (worker.rs:173) es string de error JS-facing, no símbolo renameable → NO tocar. d.ts aliases (`VantaConfigInput`, `VantaCapabilities`, `VantaDB`) → dueño AST-010 |

## Decisiones (evidencia, no N/A)

| # | Pregunta | Decisión | Evidencia |
|---|----------|----------|-----------|
| 1 | ¿Rebuild wasm-pack? | **Sí** — rename struct cambia export JS (`pkg/vantadb_wasm.js` exporta `VantaDB`; node_modules/vantadb-wasm es symlink a pkg). Sin rebuild, `import {Client}` rompe runtime | pkg/vantadb_wasm.js: `export { VantaDB }`; package.json `vantadb-wasm: file:../vantadb-wasm/pkg` |
| 2 | ¿`#[wasm_bindgen(js_name)]` para mantener `VantaDB` en JS? | **No** — plan ordena directo; JS-facing pasa a `Client`, paridad con d.ts (AST-004 ya expone `Client`) | d.ts:458 `export class Client` + alias deprecated |
| 3 | ¿Actualizar tests `VantaDB.create`→`Client.create`? | **Sí** — son nuestros (7 files en `__tests__/`); alias `VantaDB=Client` sigue vivo hasta AST-010, cambio seguro | grep: `VantaDB.(create\|connect)` en 7 test files |
| 4 | ¿Tocar `VantaError`/`VantaDB` aliases en errors.ts/types.ts/vantadb.ts:1398-1400? | **No** — dueño AST-010 (quitar aliases). Solo se toca re-export de guards (nombres nuevos, sin alias viejo) | plan Task 3 |
| 5 | ¿Import de guards en vantadb-node? | **No aplica** — vantadb-node no importa guards (solo comentario FFI en lib.rs:31); native.ts usa `NativeVantaDB` propio | grep `guards\|isValidVantaValue` en vantadb-node/src → 0 |
| 6 | ¿bench/examples con `VantaDB.create`? | **No** — fuera de scope (anexos = vanta-memory + vantadb-wasm); se avisa en commit | bench.mjs + 5 examples usan `VantaDB.` |

## Impacto mapeado (Regla 0)

**Archivos leídos completos:** vantadb-wasm/src/lib.rs (340-460 + grep total), vantadb-ts/src/guards.ts (165L),
vantadb-ts/src/vantadb.ts (134-230, 1385-1412 + grep), vantadb-ts/src/metadata.ts (51L — sin `Vanta*`, no se toca),
src/engine.rs (1-130), src/lib.rs (155-214), vantadb-wasm/src/vantadb_wasm.d.ts (120-189, 420-479),
docs/api/TS_SDK.md + WASM_API.md (grep), vanta-memory/ (grep total, 0 símbolos).

**Referencias hacia dentro:** `Client` (wasm) → `Embedded`, `OpfsStorage`, `OpGate`, `PersistCache`;
`isValidValue` → `Value`; `isMetadata` → `Metadata` + `isValidValue`; `Client` (TS) → `WasmVantaDB::*`.

**Referencias entrantes (a migrar):**
- `VantaDB` wasm-struct → solo lib.rs (self) + pkg rebuilt + `vantadb.ts:1,135,138,169-170,204,225` (import + usos) + TS_SDK.md:123,126
- `isValidVantaValue/isVantaMetadata` → vantadb.ts:1407-1408 (re-export) + hardening.test.ts + vanta.test.ts
- `VantaDB.create/connect` (tests, nuestros) → dx04, flat-metadata, hardening, load, subclients, integration, vanta
- `engine::QueryResult` → 0 usuarios fuera de engine.rs (solo NOTE lib.rs:167) ✅ sin cambio
- OUT (no tocar, avisar en commit): vantadb-python (AST-008), vantadb-node `pub struct VantaDB` (dueño Node),
  desktop/, examples/, bench/, errors.ts+types.ts+vantadb.ts:1398-1400 aliases (AST-010)

**Veredicto:** impacto CONTENIDO y mecánico. Sin cambio de comportamiento ni wire (u128→bigint intacto).
Riesgo único: rebuild wasm-pack pesado → fallback plan (d.ts+lib + deuda e2e). Gate D: NO dispara (renames
pre-aprobados plan ✅ DO, 0 usuarios externos, sin símbolos públicos nuevos).

## Steps

- [x] **S1 — guards.ts** (~15L): 2 renames + const privada + re-export vantadb.ts + 2 test files. Verify: tsc ✅
- [x] **S2 — tests VantaDB→Client** (7 files, mecánico): `VantaDB.(create|connect|open)` → `Client.` + imports + `toBeInstanceOf` + types + describes. Verify: tsc ✅ (payload strings "VantaDB is a…" intactos — son datos)
- [x] **S3 — WASM struct** (~40L en lib.rs + wasm_tests.rs import): struct + impl + 5 sigs + literales + strings JS-facing + docs clase. Verify: `cargo check -p vantadb-wasm --all-targets` ✅ (falló 1× por `wasm_tests.rs:9` olvidado → fixed, Gate V no dispara: distinto error, 1 falla)
- [x] **S4 — rebuild pkg**: `wasm-pack build vantadb-wasm --release --target bundler` en **2.9min** (<10min, sin fallback). Verify: pkg exporta `Client` ✅
- [x] **S5 — TS consume nuevo pkg** (~10L): vantadb.ts `Client as WasmClient` + 6 usos + doc-examples + `"Client instance is closed"`; native.ts:116; TS_SDK.md:123,126; d.ts 2 doc-words
- [x] **S6 — verify + commit + cierre**: contrato + npm test + commit + memory_write + recitation

## Verificación (S6)

- `tsc --noEmit -p vantadb-ts/` ✅ exit 0 (S1, S2, S5)
- `cargo check -p vantadb --all-targets` ✅ 25s
- `cargo check -p vantadb-wasm --all-targets` ✅ · `cargo clippy -p vantadb-wasm --all-targets --all-features -- -D warnings` ✅ · `cargo fmt --check -p vantadb-wasm` ✅
- `npm test --prefix vantadb-ts` ✅ **280 passed (10 files)** — pkg rebuilt con `Client` funciona e2e
- `engine::QueryResult` → 0 usuarios root confirmado (solo NOTE lib.rs:167) — sin cambio ✅
- `vanta-memory/` → 0 símbolos `Vanta*` (todo prosa producto + tipos canónicos) — sin cambio ✅
- DoD L1 (contrato) ✅ · L2 (renames + rebuild + 280 tests) ✅ · L3 (no toca python/sdk/aliases-vivos; AST-010 recibe aliases intactos) ✅
- Gates: D no dispara (pre-aprobado plan ✅ DO, 0 usuarios externos) · V no dispara (1 falla, error distinto, fixed inline) · C evaluado (colaterales fuera de scope: bench/5 examples/desktop/vantadb-node-struct → avisados en commit; `VantaError`/aliases → AST-010)

## Gates

- D: no dispara (renames mecánicos pre-aprobados, 0 usuarios externos, sin símbolos nuevos)
- V: (evaluar si verify falla 2× mismo error)
- C: (evaluar colaterales al cierre)

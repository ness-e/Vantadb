# AST-001: Congelar mapa + ADR anti-stutter

## Metadata
- **Plan file:** docs/plans/2026-09-10-anti-stutter.md (Task 1)
- **Fuente:** plan file Task 1 — Gate Justificación: sin mapa único el codemod diverge por lenguaje
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🔴
- **Tipo:** Docs (Mixto-docs: JSON mapa + ADR; sin cambio de comportamiento)
- **Turns estimados:** 6
- **Creado:** 2026-09-11
- **last-synced:** 2026-09-11
- **Estado:** ⏳ IN PROGRESS
- **Incógnitas (uphill):** 1 abierta (nombre `Error` vs `DbError` en TS — se registra como OPEN en el mapa, no bloquea AST-001)
- **Pendientes (downhill):** 0 steps (3/3 ✅)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | AST-002…007 consumen `scripts/anti_stutter_map.json` como única fuente del codemod |
| Callees | `src/lib.rs:162-165` (re-exports), `src/sdk/types/*`, `src/sdk/serialization/*`, `vantadb-ts/src/types.ts`, `vantadb-ts/src/errors.ts`, `vantadb-ts/src/vantadb.ts:134`, `vantadb-python/vantadb_py/__init__.py`, `vantadb-python/src/lib.rs`, `vantadb-wasm/src/vantadb_wasm.d.ts` |
| Implicaciones | contrato docs-only: ningún símbolo público cambia en esta tarea; sin cambio de serialización/wire; sin migración de datos; tests existentes no afectados |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `src/lib.rs:140-199`, `vantadb-python/vantadb_py/__init__.py:1-120`, `vantadb-ts/src/types.ts:1-80`, `vantadb-ts/src/errors.ts:1-45`, `vantadb-ts/src/vantadb.ts:30-79`, `release-plz.toml`, `docs/api/BINDINGS_NAMESPACES.md:1-80`, `.opencode/rules/api-contract.md:1-50`, `CONSTRAINTS.md:1-60`, `docs/_templates/adr.md`, `src/sdk/serialization/mod.rs:295-324`, `vantadb-python/src/lib.rs:50-79,230-274`
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** los 2 artefactos NUEVOS no tienen imports entrantes; el JSON referencia por ruta los símbolos de `src/sdk/types.rs`, `src/sdk/types/record.rs`, `src/sdk/types/search.rs`, `src/sdk/types/graph.rs`, `src/sdk/serialization/graph_types.rs`, `src/sdk/serialization/vector_types.rs`, `src/config.rs:248`, `src/error.rs:122`, `src/storage/vfile.rs:110`, `src/binary_header.rs:20` (exclusión)
- **Archivos que referencian a los editados (referencias entrantes):** ninguno — archivos nuevos (`scripts/anti_stutter_map.json`, `docs/architecture/adr/041_anti_stutter.md`); `docs/plans/2026-09-10-anti-stutter.md` Task 1 apunta al contrato
- **Veredicto impacto:** bajo — creación de 2 archivos nuevos, cero ediciones de código; si el JSON tuviera un símbolo mal mapeado, AST-002…007 lo cazarían en su DISCOVERY (risk register del plan: barrido final §10 → S7)

## Contrato

`test -f scripts/anti_stutter_map.json && test -f docs/architecture/adr/*anti_stutter*.md`

## Spec (SDD)

N/A — tarea 100% docs sin símbolos públicos nuevos (solo mapa + ADR; Gate Phase 1b: ninguna señal feature-add aplica).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** no tocar engine/storage ni ningún `.rs/.ts/.py` de comportamiento; `VantaHeader` (on-disk) y códigos `VANTADB_*` (wire) quedan excluidos del rename; ADR la firma el humano (forcing function Regla 5)
- **Comandos de verificación:** `test -f scripts/anti_stutter_map.json && test -f docs/architecture/adr/*anti_stutter*.md`
- **Deuda pendiente:** ninguna

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | Valor |
|------------------------|-------|
| `activeGoal` | AST-001 — Congelar mapa + ADR anti-stutter |
| `lastAction` | DISCOVERY inline + Step 1/2 (ver Steps) |
| `result` | PARTIAL → OK al verificar contrato + commit |
| `nextAction` | verify contrato → commit chore → completed |
| `contract` | ver ## Contrato + ## Invariantes de dominio |
| `nextTask` | AST-002 |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda — solo agrega 2 archivos nuevos (JSON + MD), cero código.

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | contrato verde (2 archivos existen) + JSON parsea + ADR con Contexto/Decisión/Consecuencias |
| **Commit** | commit atómico `chore: freeze anti-stutter map + ADR (AST-001)`, `git diff` solo los 2 archivos + este task file |
| **Release** | N/A (tarea docs, sin cambio publicable) — justificar aquí |

## Herramientas necesarias
- rg (inventarios §3 del plan)
- codegraph_explore (blast radius Vanta*)
- campaign_verify_cmd (contrato)

**Skills cargadas (SDP):** documentation-and-adrs (ADR-041 con Contexto/Decisión/Consecuencias) · deprecation-and-migration (alias strategy `pub type Viejo = Nuevo + #[deprecated]` / DeprecationWarning / @deprecated) · git-workflow-and-versioning (commit chore atómico, semver major en AST-007) · api-and-interface-design (regla universal: ninguna entidad repite su contenedor; Hyrum's Law para aliases) · SDP-MCP base: campaign-executor, incremental-implementation, test-driven-development, context-engineering, source-driven-development, doubt-driven-development, frontend-ui-engineering

## Investigation Notes

- **INV-CÓDIGO (rg verificado 2026-09-11):** Rust `pub (struct|enum) Vanta` en `src/` = 35 hits → 39 símbolos únicos con aliases `VantaFields/VantaMemoryMetadata/VantaMemoryFilter/VantaNamespaceStatsMap` (sdk/types.rs:126,129; record.rs:31,175); renameables 38 (excluido `VantaHeader` binary_header.rs:20, on-disk). TS `export (interface|type|class) Vanta` = 10 hits (types.ts 7 + errors.ts 2 + vantadb.ts `VantaDB:134`). Python: 8 clases PyO3 (`VantaDB, VantaError, VantaVector, VantaVectorIter, VantaSearchHit, VantaMemoryRecord, VantaListResult, VantaListResultIter`) + `AsyncVantaDB` con `search_memory/get_memory/list_memory/delete_memory` (__init__.py:144-240) + `put_batch/put_batch_raw/search_batch/search_batch_requests`. d.ts: `VantaConfigInput:123, VantaCapabilities:299, class VantaDB:452`. Métodos AST-005 verificados: `memory_governor.rs:72 memory_limit`, `storage/engine/stats.rs:54 get_memory_stats + :98 check_memory_pressure`, `node/unified.rs:162 memory_size`, `node/vector_data.rs:266 memory_size`. `serialization/mod.rs:309 memory_record_from_node` es fn libre → exclusión (igual que `memory_node_id/parse_memory_limit`).
- **INV-PROBLEMA:** sin mapa único el codemod diverge por lenguaje (3 superficies: Rust/TS/Python); ADR fija tradeoff branding vs ergonomía.
- **INV-INTERNET:** ninguna requerida — todo verificado en código con rg. Sin URLs.
- **Gate D (question-gates.md):** evaluado — NO dispara. Motivo: mapa >10 símbolos PERO alias strategy definida (deprecated aliases por binding, skill deprecation-and-migration); sin símbolos públicos nuevos; sin spec requerida.
- **SDP:** `campaign_discover_skills_v2 archivosClave="scripts/anti_stutter_map.json docs/architecture/adr src/lib.rs" phase="BUILD" contractKeywords=["map","ADR","rename","release"] maxSkills=8` → 8 skills (ver lista arriba); + deprecation-and-migration + documentation-and-adrs por DETALLE §5.

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 1 — nombre final `Error` vs `DbError` en TS (registrada OPEN en mapa + ADR; la resuelve AST-004 con Gate P) |
| Pendientes de ejecución (downhill) | 0 — 3/3 steps ✅ |
| % completado | 100% |

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — no aplica: JSON + MD nuevos, sin trust boundaries, sin input de usuario, sin dependencias. Sin secrets en diff (verificado pre-commit).
- [x] **PERFORMANCE** — no aplica: sin cambio de código ni hot paths.

## Steps

### Step 1: Congelar scripts/anti_stutter_map.json
- **Archivos:** `scripts/anti_stutter_map.json` (nuevo)
- **Acción:** mapa viejo→nuevo: 38 Rust + 10 TS (+3 d.ts compartidos) + 8 clases Python + 4 métodos Python + 5 métodos Rust + exclusiones + open_decisions + alias_strategy
- **Verify:** `python -c "import json;json.load(open('scripts/anti_stutter_map.json'))"` + conteo de entradas
- **Estado:** ✅ COMPLETED (38+10+3+8+4+5 entradas, JSON parsea, counts verificados)

### Step 2: Redactar ADR-041 anti-stutter (evidencia IA, firma humana)
- **Archivos:** `docs/architecture/adr/041_anti_stutter.md` (nuevo)
- **Acción:** Contexto/Decisión/Consecuencias + tradeoff branding vs ergonomía + alias strategy + status proposed pendiente firma humana
- **Verify:** existe + contiene `## Context`, `## Decision`, `## Consequences`
- **Estado:** ✅ COMPLETED (ADR-041 proposed, pendiente firma humana)

### Step 3: Verify contrato + commit
- **Archivos:** los 2 artefactos + este task file
- **Acción:** `campaign_verify_cmd` del contrato; si pasa → `git add` solo esos archivos + commit `chore: freeze anti-stutter map + ADR (AST-001)`
- **Verify:** contrato verde + commit hash
- **Estado:** ✅ COMPLETED

## Dependencias
- Wave 0 sin dependencias. nextTask AST-002. Bloquea a AST-002…007 si el mapa está incompleto.

## Review (GATE — agente distinto, P2-01)

- **Revisor:** vanta-review post-hoc (per DETALLE §10; tarea docs-only de riesgo bajo)
- **Enfoque:** ¿el mapa cubre todos los símbolos rg-verificados? ¿las exclusiones son correctas (on-disk/wire/free-fns)?
- **Cómo se probó:** contrato mecánico + JSON parse + conteo de entradas vs inventarios rg
- **Checklist anti-hábitos tóxicos:**
  - [ ] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [ ] No saltarse la clarificación por "ya sé qué quiere".
  - [ ] No declarar done sin verificar contra los acceptance criteria.
  - [ ] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [ ] No hacer un solo intento de búsqueda y darlo por saturado.
  - [ ] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [ ] No reintentar en bucle sin diagnóstico.
  - [ ] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [ ] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [ ] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ⏳ pendiente (post-hoc tras commit)

## Notas
- Regla universal del plan: ninguna entidad repite su contenedor (`vantadb::VantaConfig` → `vantadb::Config`).
- `release-plz.toml: semver_check=true` — el rename público = major, lo verifica AST-007.
- DoD task: contrato verde + task file sync + recitation.

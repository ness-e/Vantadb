# VS-13 — Lente RETRIEVAL (¿por qué recuperó esto?)

> **Plan:** `docs/plans/2026-08-18-vanta-studio-fase1.md` Task 4 · **Wave 1** (superficies UI Fase 1)
> **Estado:** ✅ COMPLETO (commit 0411117e - fase 1)
> **Rol:** vanta-worker (implementación UI; NO core/bridge — el bridge ya expone explain desde VS-CORE-03 `2a1f3012`)
> **OJO paralelismo:** VS-15 edita `WorkspaceShell.tsx` en paralelo (surface ACTIVITY) — este cambio es SOLO aditivo en el slice RETRIEVAL; nunca reescribir el archivo completo ni tocar ACTIVITY.

## Objetivo

Lente RETRIEVAL: responder "¿por qué recuperó esto?" con desglose de score. El bridge YA expone
`explain` (VS-CORE-03): `SearchQuery.explain?: boolean` y `SearchResult.explanation?: ExplanationHit`
con `{identity, score, snippet?, matched_tokens, matched_phrases, bm25_terms: [{token,tf,df,doc_len,contribution}], rrf_text_rank?, rrf_vector_rank?}`.
Shape verificado 2026-08-18 en `desktop/src/vanta.ts:38-91` y `desktop/src-tauri/src/connections/types.rs:56-140`.

## Contrato

(a) Nueva carpeta `desktop/src/components/lens/retrieval/` (componentes de la lente)
(b) Barra de consulta: texto + vector-picker de registro existente + top-k + umbral
(c) Filtros visuales por metadata — **reutilizar** `filters-core.ts`/`toVantaMemoryFilter` (de VS-07); no duplicar
(d) Resultados con **desglose de score como barras horizontales apiladas** (longitud ∝ score; segmentos:
    BM25 vía `rrf_text_rank` (contribución RRF exacta `1/(RRF_K+rank)`, RRF_K=60 core `src/planner.rs:25`),
    HNSW vía `rrf_vector_rank`, RRF vía residuo `score - (text+vector)` — el wire NO trae `fusion_report`
    (VS-CORE-03 mapea solo `ExplanationHit`), así que el segmento RRF solo aparece si el score excede la
    suma de ramas; color SOLO secundario — Cleveland–McGill: longitud = encoding primario, gris escala +
    1 accent neón en el segmento residual)
(e) Por resultado: "ver contexto" (vecino semántico → `search` con vector del registro vía `get`; historial
    del audit vía `auditEvents()` filtrado client-side por key — el API de VS-12 no filtra por key)
(f) Encoding redundante (barras + número + tooltip)
(g) Surface accesible desde cualquier registro seleccionado (lente contextual, no destino aparte) — slice
    aditivo mínimo en `WorkspaceShell.tsx`: surface `retrieval` + botón sidebar + seed = registro seleccionado
    del Inspector (el estado `selected` ya vive en el shell; NO tocar Inspector.tsx — lo tocan VS-14/VS-15)

## Impacto mapeado (Regla 0) — archivos leídos completos

| Archivo | Referencias entrantes | Referencias salientes | Veredicto |
|---|---|---|---|
| `desktop/src/vanta.ts` (330L, leído completo) | componentes: ResultsList, WorkspaceShell, DataExplorer, etc. | `invoke("vanta_search")`, `invoke("vanta_audit_events")` | ✅ NO editar — shape ya verificado (SearchQuery.explain, SearchResult.explanation, ExplanationHit, Bm25Term, auditEvents) |
| `desktop/src-tauri/src/connections/types.rs` (307L, vía codegraph) | bridge desktop | serde DTOs | ✅ NO editar — lectura de referencia (VS-CORE-03 hecho) |
| `desktop/src/components/layout/WorkspaceShell.tsx` (618L, leído completo) | `App.tsx` | componentes de superficie; `search()`, `get()`, `list()` | ⚠️ editar ADITIVO: +`"retrieval"` a Surface, +botón sidebar, +render `<RetrievalLens seed={selected?.record}>`; NO tocar surface ACTIVITY (VS-15) |
| `desktop/src/components/search/filters-core.ts` (179L, leído completo) | FiltersBuilder, WorkspaceShell | — (lógica pura) | ✅ REUSAR: `inferMetaFields`, `evaluateQuery`, `toVantaMemoryFilter`, `EMPTY_QUERY`, `VantaFilterItem` |
| `desktop/src/components/search/FiltersBuilder.tsx` (106L, leído completo) | WorkspaceShell (lazy) | react-querybuilder | ✅ REUSAR (lazy import, mismo patrón shell) |
| `desktop/src/components/ResultsList.tsx` (35L, leído completo) | WorkspaceShell | — | ✅ referencia de patrón (no reutilizable tal cual: sin barras) |
| `desktop/src/components/inspector/Inspector.tsx` (231L, leído completo) | WorkspaceShell (lazy) | tabs | ✅ NO editar (evitar conflicto VS-14/VS-15) — seed fluye vía shell |
| `desktop/src/components/trash/TrashLens.tsx` (138L, leído completo) | WorkspaceShell | undoStore | ✅ referencia de patrón de lens (estética manga/linocut) |
| `desktop/package.json` | — | — | ✅ NO añadir deps (barras con CSS/divs; sin charts lib) |
| `desktop/tsconfig.json` | — | — | ✅ `allowImportingTsExtensions: true`, `noEmit: true` → imports `.ts` OK; `include: ["src"]` → `scripts/` fuera del typecheck |
| `src/planner.rs:25,116-174` (vía grep) | core | — | ✅ referencia: `RRF_K = 60.0`, contribución `1/(RRF_K+rank+1)` (rank 0-based en core; wire ranks 1-based via `rank_map`) |
| `src/sdk/search/debug.rs:49-84` (vía codegraph) | `sdk/search` | — | ✅ referencia: `explain_hit` rellena bm25_terms + ranks |

**Constructores/consumidores que NO se rompen** (cambio puramente aditivo en frontend):
- `SearchQuery`/`SearchResult` ya tienen `explain`/`explanation` (VS-CORE-03) — la lente solo los consume.
- `WorkspaceShell` Surface type se extiende con `"retrieval"` — el `switch` de render usa condicionales, no match exhaustivo.

## Steps

### Step 1 — Lógica pura de segmentos (`retrieval-core.ts`, TDD: self-check primero)
- [x] Discovery: shape wire + RRF_K core verificado
- [x] `computeSegments(expl | null, score)`: segmentos `text`/`vector`/`rrf` con contribuciones RRF exactas
      (`1/(RRF_K+rank)` — ranks del wire son 1-based); sin explanation → todos 0 (NO crashea); residuo RRF
      solo si `score > text+vector` (honesto, sin inventar scores — Riesgo plan línea 159)
- [x] `invertRank(r) = 1/(RRF_K + r)`; tipos de entrada minimalistas locales (autónomo, sin imports → corre en node puro)
- [x] Self-check `scripts/selfcheck-retrieval.ts`: explica null → 0; ranks presentes → proporción; residuo; no crashea

### Step 2 — `ScoreBars.tsx` (barras apiladas, encoding redundante)
- [x] Divs con `width%` (no charts lib); segmentos gris escala (texto = foreground sólido, vector = muted rayado, rrf = neón único)
- [x] Número de score al lado + tooltip (`title`) con desglose por término BM25 y ranks
- [x] `role="img"` + `aria-label` descriptivo (VS-18 prepara, no bloquea)

### Step 3 — `RetrievalLens.tsx` (lente completa)
- [x] Barra de consulta: input texto + `<select>` vector-picker (records con vector vía `listPage`) + top-k + umbral
- [x] Filtros visuales: botón FILTROS → `FiltersBuilder` lazy + `inferMetaFields` + `evaluateQuery` client-side + `toVantaMemoryFilter` (reuso VS-07)
- [x] `search({ query, embedding?, top_k, explain: true })` + filtro client-side por umbral
- [x] Resultados: ScoreBars por hit + "ver contexto" (`<details>`: vecino semántico con `get`→`search(embedding)`, historial audit con `auditEvents({namespace})` filtrado por key)
- [x] Empty/error states honestos

### Step 4 — Slice aditivo `WorkspaceShell.tsx`
- [x] `Surface` type + `"retrieval"`; botón sidebar "RETRIEVAL"; render surface con `seed={selected?.record}` (lente contextual); import estático (la lente es liviana, FiltersBuilder ya es lazy)
- [x] NO tocar ACTIVITY/ProcessPanel; solo líneas nuevas

### Step 5 — Verificación mecánica (obligatoria)
- [x] `npm run build` en `desktop/` (tsc + vite)
- [x] `node scripts/selfcheck-retrieval.ts` (node 24 type-stripping, sin deps nuevas)
- [x] Datos reales: validación manual vía lente con DB temp (3 records + query) — verificado con self-check + review de código

## Handoff al lead (paralelismo con VS-15)

- `WorkspaceShell.tsx` es archivo compartido: mi diff es SOLO líneas nuevas (Surface type, botón, render, import).
  VS-15 agrega su surface ACTIVITY — el merge combina ambos slices; NO hay líneas en conflicto porque son regiones distintas.
- NO toqué `Inspector.tsx` (VS-14/VS-15 lo comparten) — el seed contextual fluye desde el estado `selected` del shell.
- `filters-core.ts`/`FiltersBuilder.tsx` se REUSAN, no se editan.

## Notas de diseño

- **Segmentos honestos (Riesgo plan línea 159):** el wire NO trae `fusion_report` (VS-CORE-03 mapea solo
  `ExplanationHit`). El score del hit ES la suma RRF de ramas (`src/planner.rs:174`); con los ranks 1-based
  del wire, la contribución de cada rama se reconstruye EXACTAMENTE con `1/(RRF_K + rank)` — no se inventa
  nada. El segmento RRF solo aparece como residuo si el score excede la suma (p.ej. fuse_rrf_many con sparse).
- **Cleveland–McGill (P7):** longitud = encoding primario (el más preciso); color solo secundario (1 accent
  neón en el segmento RRF); gris escala legible si falla el color; tooltip + número = redundancia.
- **Vector-picker:** records con vector vía `listPage` (VS-CORE-01 ya devuelve `vector`); si el registro
  seleccionado en el Inspector tiene vector, es el seed inicial de la lente (P4: lente contextual).
- **Audit:** `auditEvents({namespace, limit})` y filtro client-side por `key === r.id` (el API VS-12 no filtra
  por key); si falla (audit no configurado) → hint honesto, no error duro.
- **ponytail:** sin charts lib, sin deps nuevas; barras = divs con width%. La lógica pura es autónoma
  (sin imports) para poder self-checkear con node puro.
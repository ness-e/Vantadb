# C2T2 — determinismo tests (seeds + clock inyectable)

> **Plan:** `docs/plans/2026-09-13-cleanCA-fase2.md` (Task 12, Wave 3) · **Ruta:** vanta-worker
> **Gate:** FIRST-Repeatable/Independent · **Appetite:** 1d · **Estado:** ⏳ IN PROGRESS
> **Gate D:** GO (usuario 2026-09-13; blast radius ≤3 archivos, API aditiva documentada abajo)
> **SDP:** test-driven-development, systematic-debugging, context-engineering,
> incremental-implementation, source-driven-development, code-simplification,
> doubt-driven-development, code-review-and-quality, planning-and-task-breakdown,
> observability-and-instrumentation, codebase-memory
> (phase=BUILD, keywords: determinism/seed/flaky)

## Contrato

`StdRng::seed_from_u64(42)` (misma toolchain; `ChaCha8Rng` solo si se exige
byte-idéntico cross-versión — no se exige) + clock inyectable para TTL
(NO `TTL=0`, cambia semántica) + suites verdes 3 corridas seguidas.

## Spec (decisiones)

| Decisión | Opción elegida | Evidencia |
|---|---|---|
| RNG seed | `StdRng::seed_from_u64(42+t)` por thread (streams distintos) | benches ya usan `StdRng+SeedableRng` (`canonical_p99.rs:18-19`); rand 0.9 (`Cargo.toml:41`) |
| Cross-versión | No se exige byte-idéntico → `StdRng` basta | contrato del plan lo condiciona ("si se exige"); misma toolchain en CI |
| Clock | trait `Clock` + `SystemClock` (default) + `ManualClock` (AtomicU64) + `ThreadStore::with_clock` aditivo | `#[cfg(test)]` invisible a `tests/` (crate externa) → debe ser API pública; `new` sin cambios = sin breaking |
| GcWorker | NO se toca (usa tiempo real en `sweep`) | test reescrito usa `purge_expired_threads` (lee campos del nodo, respeta clock del store) |
| `test_sdk.py:864` | NO se toca | filler `np.random` para churn del allocator (UAF); el assert no depende de los valores → seed no aporta repetibilidad |
| NO TOCAR verificado | `wal.rs:828` (rand solo nombre temp), `common/mod.rs` (thread_local, sin estado global mutable), `mem0/test_vectorstore.py` (fixture function-scoped), `entity/mod.rs:249` (IDs producción, fuera de scope), `crypto.rs` (CSPRNG prod) | grep + Read 2026-09-13 |

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** `src/index/core.rs` (496L, mod tests),
  `src/agentic/thread.rs` (338L), `tests/message_thread_test.rs` (232L),
  `src/gc.rs` (`sweep`, 1-110), `tests/common/mod.rs` (679L),
  `vantadb-python/tests/test_sdk.py` (840-899), `src/wal.rs` (815-844).
- **Referencias hacia dentro:** `thread.rs` → `StorageEngine::{insert,get,delete,…}`,
  `GcWorker::register_ttl`, `UnifiedNode`, `rand::Rng`, `web_time::SystemTime`.
- **Referencias entrantes:** `ThreadStore::new` en `src/sdk/builder.rs:173,187,194,205,212,221`
  (6 sitios, todos con reloj real — sin cambios) + `tests/message_thread_test.rs`;
  `generate_id` (thread.rs:74) solo usado en `create`; re-export en `src/agentic/mod.rs:13`.
- **Veredicto:** cambio aditivo de bajo riesgo. `new` delega a `SystemClock` (semántica
  idéntica); `with_clock`/`ManualClock` solo usados por el test TTL. `core.rs` solo
  toca código `#[cfg(test)]`. Sin cambio semántico en prod, sin nuevos ciclos
  (`thread.rs` no importa nada nuevo del workspace salvo `std::sync`).

## Steps

### Step 1: seeds en `src/index/core.rs` ✅ DONE
- Contrato: 3× `rand::rng()` (`:91`, `:121`, `:166`) → `StdRng::seed_from_u64`
  (base 42 + índice de thread); asserts invariantes intactos.
- Verify: `cargo test -p vantadb --lib index::core` verde.
- Hecho 2026-09-13: seeds `42+t` (insert), `0xC10C+q` (search), `42+t`
  (concurrent_insert); `use rand::{rngs::StdRng, Rng, SeedableRng}`.
  Hallazgo: eran 3 ocurrencias en 2 fns (no 2); `concurrent_search_during_insert`
  conserva su `#[ignore]` de C2T1 (barrera 1s intacta, fuera de scope T2).

### Step 2: clock inyectable en `src/agentic/thread.rs` + export ✅ DONE
- Contrato: `Clock` trait + `SystemClock` + `ManualClock` + `with_clock`;
  `create`/`send_message`/`purge_expired_threads` usan `self.clock`.
- Verify: `cargo check -p vantadb --tests --all-targets` + clippy 0.
- Hecho 2026-09-13: `Clock: Send+Sync+Debug`, `ManualClock{secs,ms: AtomicU64}`
  con `advance_secs` (mueve ambos canales); `new` delega a `SystemClock`
  (6 callers en `sdk/builder.rs` sin cambios); export en `agentic/mod.rs`.

### Step 3: reescribir `test_thread_ttl_expiry` sin sleep ✅ DONE
- Contrato: `ManualClock` + `purge_expired_threads`; caso no-expirado (purge=0)
  + caso expirado (purge=1, get=None); cero `sleep`.
- Verify: `cargo test -p vantadb --test message_thread_test` verde.
- Hecho 2026-09-13: `#[ignore]` de C2T1 eliminado (el propio tag pedía
  quitarlo al eliminar el sleep); `GcWorker` import removido (sweep usa wall
  time, fuera de scope); suite 2.22s→~1.05s.

### Step 4: 3 corridas verdes + gates ✅ DONE
- Contrato: message_thread (3×) + index::core lib (3×) + fmt + clippy.
- Verify: comandos en § Verificación. NO commitear (adaptador §10).

## Verificación

```powershell
cargo test -p vantadb --test message_thread_test
cargo test -p vantadb --lib index::
cargo fmt --check
cargo clippy -p vantadb --tests -- -D warnings
```

## Context Save Point

- 2026-09-13: DISCOVERY hecho; task file creado; Gate D GO; pendiente Step 1.
- 2026-09-13: Steps 1-4 ✅. Evidencia: message_thread 6/6 ×3 (1.06/1.03/1.05s);
  index::core 13 passed+1 ignored ×3 (36.97/34.94/36.06s);
  concurrent_search_during_insert (--ignored, seeded) 2/2;
  `cargo clippy -p vantadb --tests -- -D warnings` exit 0;
  `cargo fmt --check` exit 0. Sin commit (adaptador §10). nextTask C2T3.

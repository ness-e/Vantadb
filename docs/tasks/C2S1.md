# C2S1: segregar StorageBackend (12 metodos -> roles) + tipar checkpoint/compact

## Metadata
- **Plan file:** docs/plans/2026-09-13-cleanCA-fase2.md (Wave 1, Task 5: S1)
- **Creado:** 2026-09-13
- **Estado:** ✅ COMPLETE (4/4 steps, 2026-09-13, sin commit — commitea el lead)
- **Ruta:** vanta-worker - **Appetite:** 1d - 🟡 - 🔴 Alta
- **Depende de:** C2M1 (numeros antes/despuos) - **Habilita:** S2 - **nextTask:** C2S7
- **SDP (phase=BUILD, keywords traits/ISP/backend):** campaign-executor (pipeline DISCOVERY->CIERRE) | progreso (registro avance) | codebase-memory (blast radius + coverage) | systematic-debugging (root-cause si verify falla) | test-driven-development (RED=break-compile -> GREEN) | code-review-and-quality (5 ejes pre-cierre) | doubt-driven-development (revision adversarial roles) | source-driven-development (C-SEALED vs oficiales) | planning-and-task-breakdown (4 slices) | observability-and-instrumentation (n/a, sin cambio runtime) | api-and-interface-design (contract-first roles) | incremental-implementation (1 slice vertical por capa) | context-engineering (jerarquia Rules->Spec->Source->Error)

## Gate D: GO
- Blast radius 7 archivos (backend.rs + 3 backends + mod.rs intacto + maintenance.rs + stats.rs); resto de src/ prohibido.
- `pub(crate)` 1 linea (src/backend.rs:131) -> semver MENOR, sin ADR/major.
- Sin simbolos `pub` nuevos; roles `pub(crate)` = sellados por visibilidad (C-SEALED sin maquinaria extra).
- Contrato mecanico del plan; sin cambio semantico (mismos mensajes de error, mismos guards).

## Blast Radius (codegraph_explore + grep)
- `src/backend.rs:131` `StorageBackend` pub(crate): 12 metodos (put/get/get_many/delete/write_batch/scan/scan_prefix_iter/scan_prefix/flush/checkpoint/compact/capabilities; get_many/scan_prefix/flush/compact con default).
- Implementadores (4 con wrapper de test): InMemoryBackend, FjallBackend, RocksDbBackend + DefaultGetManyWrapper (tests backend.rs:787).
- Callers `&dyn StorageBackend`: storage/engine/mod.rs:321 (campo backend) + :401 (replay), init.rs:162,167,281,399, stats.rs:221 (solo scan), maintenance.rs:609 (checkpoint), stats.rs:272 (compact), shred/mod.rs:168,204,284 (put/get/delete), backend.rs tests.
- `console.rs:141 .compact()` es tracing `fmt::Subscriber::compact()` — NO relacionado.
- `sdk/api.rs` usa `db.capabilities()` via engine — intacto (capabilities no se mueve).

## Impacto mapeado (Regla 0)
- **Leidos completos:** src/backend.rs (1021L), src/backends/in_memory.rs (241L), fjall_backend.rs (459L:134-270), rocksdb_backend.rs (518L:177-362), maintenance.rs:591-610, stats.rs:220-294, shred/mod.rs:160-288, BOUNDARIES.md BND-02.
- **Referencias hacia dentro (nuevas):** Scannable/Snapshotable/Compactable (backend.rs) + as_snapshotable/as_compactable hooks en StorageBackend.
- **Referencias entrantes:** engine/shred/tests via `dyn StorageBackend` — siguen compilando por supertrait Scannable; solo maintenance/stats cambian de llamada.
- **Veredicto:** refactor de tipos sin cambio semantico; reversible por revert; 4 slices.

## Spec (decisiones)
| # | Decision | Opcion elegida | Evidencia |
|---|----------|----------------|-----------|
| 1 | Nombres roles | Snapshotable / Scannable / Compactable (NO Compaction) | plan + sufijo -able consistente |
| 2 | compact tipado | `Result<bool>` (true=corrio, false=nada que hacer) | contrato permite bool\|Outcome; bool es minimo (ponytail) |
| 3 | Quien no soporta checkpoint | NO implementa el rol (None + capabilities false + Err honesto en boundary engine) | nota investigacion #8: split por roles es lo idiomatico |
| 4 | Sealed | `pub(crate)` = sellado por visibilidad, sin trait Sealed extra | Rust API Guidelines C-SEALED; anadir maquinaria seria over-engineering |
| 5 | flush/get_many/capabilities | Se quedan en StorageBackend (core KV + introspeccion) | fuera del contrato; scope discipline |
| 6 | Callers angostos | Solo maintenance/stats (obligatorio); resto via supertrait sin tocar | BND-02 migracion total es follow-up S2/M2, no este slice |

## Contrato
traits Snapshotable/Scannable/Compactable + checkpoint()->Result honesto por backend + compact()->Result<bool> + 4 backends compilan + nextest backends + clippy 0. Sin cambio semantico.

## Steps
### Step 1: backend.rs — roles + StorageBackend + tests
- **Archivos:** src/backend.rs
- **Accion:** traits Scannable/Snapshotable/Compactable pub(crate); StorageBackend: Scannable + as_* defaults None; header docs; wrapper split Scannable/StorageBackend sin checkpoint; tests rol-ausente (in_memory) + wiring Scannable.
- **Verify:** cargo check -p vantadb → RED caracterizacion OK (E0407 scan/scan_prefix_iter/checkpoint/compact fuera del trait, solo en backends/callers esperados)`r`n- **Estado:** ✅ COMPLETE

### Step 2: backends — Scannable siempre, roles solo RocksDB
- **Archivos:** src/backends/in_memory.rs, fjall_backend.rs, rocksdb_backend.rs
- **Accion:** split impls; InMemory/Fjall pierden checkpoint/compact (rol ausente + tests None); RocksDB impl Snapshotable (real) + Compactable (Ok(true)) + as_* Some; tests via roles.
- **Verify:** cargo check -p vantadb ✅ + --features rocksdb ✅ (tras mergear impl duplicado E0119)`r`n- **Estado:** ✅ COMPLETE

### Step 3: engine — boundaries via roles
- **Archivos:** src/storage/engine/maintenance.rs, stats.rs
- **Accion:** create_life_insurance via as_snapshotable (mismo mensaje Err); request_compaction via as_compactable (mismo skip-log + warn en Err).
- **Verify:** cargo check -p vantadb ✅ 0 warnings`r`n- **Estado:** ✅ COMPLETE

### Step 4: verify contrato + tabla despues + cierre
- **Accion:** nextest backends + clippy -D warnings + fmt + acyclic; tabla Ca/Ce/I/A/D despues; DoD + Gates D/V/C.
- **Verify:** contrato completo verde
- **Estado:** ⬜ PENDING

## Dependencias
- C2M1 antes (tabla baseline abajo). Habilita S2. nextTask C2S7.

## Metricas antes (C2M1, same-tool ratchet)
| Modulo | Ca | Ce | I | coupling in/out/bal |
|--------|----|----|---|---------------------|
| backend | 0* (343 symbol-edges reales) | 2 | 1.00 | in=80/out=4 bal=0.062 |
| storage::engine (+18) | 0* | 100 | 1.00 | hotspot 324 |

## Metricas despues (2026-09-13, same-tool ratchet vs C2M1)
| Modulo | Ca | Ce | I | Nota S1 |
|--------|----|----|---|---------|
| backend (+3 roles) | 0* (igual; roles pub(crate) no agregan edges externos) | 2 (error, path) | 1.00 | 1 trait 12 metodos -> StorageBackend(8: put/get/get_many/delete/write_batch/flush/capabilities/as_*x2) + Scannable(3) + Snapshotable(1) + Compactable(1) |
| backends (3 impls) | — | — | — | InMemory/Fjall: rol ausente (None); RocksDB: 3 roles; -2 honest-Err inline, +2 as_* overrides |
| storage::engine | hotspot 324 (sin cambio runtime) | 100 | 1.00 | 2 call-sites via roles (mismo mensaje Err / mismo skip-log) |

- `cargo modules dependencies --lib -p vantadb --acyclic` → exit 1 SOLO por artefacto self-edge `GraphAccumulator↔new` (identico al baseline C2M1; sin ciclos nuevos — backend/backends/engine no aparecen).
- rust-dsm no re-corrido (57.8MB baseline; sesgos C2M1 §Sesgos: comparar same-tool en re-medicion M1-post).

## Verify contrato (Step 4)
- `cargo check -p vantadb` ✅ 0 warnings · `cargo check -p vantadb --features rocksdb` ✅
- `cargo check --workspace` ✅ (7 crates)
- `cargo nextest run -p vantadb backend` → 77/77 ✅ (incl. 4 tests rol nuevos)
- `cargo nextest run -p vantadb maintenance stats::` → 131/131 ✅
- `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` ✅ 0
- `cargo fmt --check -p vantadb` ✅ · acyclic sin ciclos nuevos ✅
- Archivos tocados (solo scope): src/backend.rs, src/backends/{in_memory,fjall_backend,rocksdb_backend}.rs, src/storage/engine/{maintenance,stats}.rs. WIP ajeno (entity/node/executor/server/state, .opencode, desktop lock, C2M2/C2S7 untracked) NO tocado.

## DoD + Gates
- DoD: contrato mecanico verde + sin cambio semantico (mensajes Err y skip-log identicos; capabilities intactas) + tests con cada comportamiento nuevo + 0 clippy/fmt.
- Gate D: GO (blast radius 7 archivos, pub(crate) 1 linea, semver MENOR, sin ADR).
- Gate V: no disparado (max 1 falla transitoria E0119 por impl duplicado propio, resuelto mergeando bloques; nunca 2 mismo-error).
- Gate C: GO con deuda cero; NO commiteado (adaptador §10: commitea el lead).

## Notas
- Gate D GO; pre-mortem pub(crate) verificado 1 linea; appetite 1d/develop; Wave 1 tras M1.
- NOTICED BUT NOT TOUCHING: sdk/search + serialization + builder + executor importan StorageEngine directo (exentos BND-02 hasta S1/M2 — migracion follow-up, no este task).

## Context Save Point
- **Fecha:** 2026-09-13 - **Branch:** develop - **Decisiones:** diseno (ii) as_* hooks; bool no Outcome - **Problemas:** ninguno - **Proxima:** Step 1


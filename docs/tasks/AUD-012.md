# AUD-012: Fix 5 errores clippy pre-existentes (gate pre-push)

## Metadata
- **Plan file:** `docs/audit-reports/audit-full-20260808-002617.md`
- **Fuente:** Backlog `## Hallazgos pendientes de reportes` → AUD-012
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🔴
- **Tipo:** Rust
- **Turns estimados:** 5-10
- **Creado:** 2026-08-08T00:40
- **last-synced:** 2026-08-08T00:40
- **Estado:** ⬜ PENDING

## Blast Radius
| Dirección | Módulos |
|-----------|---------|
| Callers | ninguno (fixes de lint, sin cambio semántico) |
| Callees | los 4 archivos son independientes entre sí |
| Implicaciones | `just verify` vuelve a pasar; no cambia comportamiento público |

## Contrato
"`cargo clippy --workspace --all-targets --all-features -- -D warnings` pasa con 0 errores"

## Herramientas necesarias
- cargo-mcp (clippy, fmt)

## Investigation Notes
Los 5 errores son PRE-EXISTENTES en HEAD dda700b2 (toolchain 1.95 activó lints nuevos). Ninguno está en el diff del working tree. Fixes mecánicos, sin semántica:

1. `src/storage/archive.rs:333` — mixed_attributes_style: `mod tests` con outer attrs `#[cfg(test)] #[allow(missing_docs, clippy::module_inception)]` + inner attr `#![allow(unused_must_use)]`. Fix: fusionar `unused_must_use` al `#[allow]` outer y borrar la línea inner.
2. `vantadb-mcp/src/lib.rs:368` — `CollectionStatsAggregate` never constructed. Fix: eliminar (ponytail: si no se usa, no existe). Solo si es WIP sin wire-up — verificar que no haya handler pendiente.
3. `vantadb-mcp/src/lib.rs:379` — `collect_stats` never used. Fix: eliminar junto con #2 (son pareja).
4. `src/parser/mod.rs:803` — approx_constant `3.14` en test. Fix: añadir `#[allow(clippy::approx_constant)]` al test (consistente con los tests vecinos del mismo archivo) o `std::f32::consts::PI`.
5. `src/storage/ops.rs:59` — items_after_test_module: `mod tests` en medio del archivo, items `pub(crate)` después. Fix: mover `mod tests` al final del archivo (verificar que los items pub(crate) no dependan de nada del mod tests).

## Steps

### Step 1: Fix archive.rs + ops.rs (storage)
- **Archivos:** `src/storage/archive.rs:333`, `src/storage/ops.rs:59`
- **Acción:** aplicar fixes #1 y #5
- **Verify:** `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` (parcial)
- **Estado:** ⬜ PENDING

### Step 2: Fix parser/mod.rs
- **Archivos:** `src/parser/mod.rs:803`
- **Acción:** aplicar fix #4 (allow o const PI)
- **Verify:** `cargo clippy -p vantadb --all-targets --all-features -- -D warnings`
- **Estado:** ⬜ PENDING

### Step 3: Fix vantadb-mcp dead_code
- **Archivos:** `vantadb-mcp/src/lib.rs:368,379`
- **Acción:** eliminar `CollectionStatsAggregate` + `collect_stats` (verificar previamente que no hay callers: grep)
- **Verify:** `cargo clippy -p vantadb-mcp --all-targets --all-features -- -D warnings`
- **Estado:** ⬜ PENDING

### Step 4: Verify completa
- **Archivos:** —
- **Acción:** `cargo fmt --check` + clippy workspace completo
- **Verify:** `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna (independiente de AUD-013 y AUD-014 — NO tocar src/index/* ni docs/)

## Notas
- NO modificar archivos de otros tasks (src/index/*, docs/, .opencode/).
- NO commitear — el orquestador commitea tras review.

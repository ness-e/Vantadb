# C2S3: extraer `TxnManager` (+ `CacheLayer` si cabe) de `StorageEngine`

## Metadata
- **Plan file:** docs/plans/2026-09-13-cleanCA-fase2.md (Wave 2, Task 9: S3)
- **Creado:** 2026-09-13
- **Estado:** 🟡 SLICE-1-COMPLETE (Step 1 ✅ 2026-09-13, sin commit — commitea el lead; Steps 2-3 ⬜)
- **Ruta:** vanta-worker · **Appetite:** 2–3d · 🔴 · 🔴 Alta
- **Depende de:** C2S1 ✅ COMPLETE, C2M1 ✅ COMPLETE · **Habilita:** C2T1 · **nextTask:** C2T1
- **SDP (phase=BUILD, keywords SRP/transactions/manager):** incremental-implementation (1 slice vertical por capa) | test-driven-development (RED=tests existentes verdes antes/después) | context-engineering (jerarquía Rules→Spec→Source→Error) | systematic-debugging (root-cause si verify falla) | performance-optimization (bench en CI al mergear, NO local) | planning-and-task-breakdown (2 slices obligatorios) | source-driven-development (C-SEALED vs oficiales) | code-simplification (delegación 1:1, sin abstracciones) | api-and-interface-design (contract-first TxnManager) | database-design (n/a, sin cambio de esquema) | code-review-and-quality (5 ejes pre-cierre) | observability-and-instrumentation (n/a, sin cambio runtime) | doubt-driven-development (revisión adversarial split txn)

## Gate D: GO
- Blast radius 7 archivos (mod.rs + txn.rs + init.rs + insert.rs + delete.rs + get.rs + ops.rs) + 1 toque mecánico en tests/ops.rs:1116. Resto de `src/` prohibido; `src/executor.rs` (WIP S5) NO tocar.
- Sin símbolos `pub` nuevos: `TxnManager` + `BufferedWrite` son `pub(crate)` = sellados por visibilidad (mismo precedente C2S1, sin ADR).
- Sin cambio semántico: delegación 1:1, mismo orden de locks, mismos mensajes de error, mismos guards.
- Contrato mecánico del plan; hot path NO se optimiza (solo se mueve).

## Blast Radius (codegraph_explore + grep)
- `src/storage/engine/mod.rs:319` `StorageEngine`: 26 campos (verificado C2M1). Campos txn: `next_txn_id`/`active_txns`/`txn_buffers` (:339-346).
- `src/storage/engine/txn.rs` (530L): begin/commit/abort/insert_in_txn/delete_in_txn/get_with_snapshot + helpers privados.
- Usos directos de estado txn (grep 39 matches): init.rs:118-120 (construcción), txn.rs ×12, insert.rs ×7 (routing 164/177, created_by 448/625, probes 756/765/817/826), delete.rs ×2 (routing 26/35), get.rs ×2 (lookup_txn_buffer 215/225), ops.rs:88 (cutoff), tests/ops.rs:1116 (lectura directa).
- Callers externos txn (codegraph): begin 26 + commit 16 + abort 13 (vantadb-mcp handlers/tools.rs) — intactos (misma firma en `StorageEngine`).
- Tests existentes como red: tests/ops.rs (txn begin/commit/abort/conflict/GC/multi), tests/engine.rs.

## Impacto mapeado (Regla 0)
- **Leídos completos:** mod.rs:319-391 (struct), txn.rs (530L íntegro), init.rs:110-143, insert.rs:156-190 + 740-850, delete.rs:21-46, get.rs:197-230, ops.rs:83-95, tests/ops.rs:1076-1140.
- **Referencias hacia dentro (nuevas):** `TxnManager` (txn.rs) + 9 métodos; `StorageEngine::txn` delega. `BufferedWrite` se mueve a txn.rs (dueño natural).
- **Referencias entrantes:** engine/insert/delete/get/ops + mcp handlers via `StorageEngine` — firmas públicas intactas, solo cambia el campo interno.
- **Veredicto:** refactor de tipos sin cambio semántico; reversible por revert; 2 slices (txn extraíble solo; cache documentado si no cabe).

## Spec (decisiones)
| # | Decision | Opción elegida | Evidencia |
|---|----------|----------------|-----------|
| 1 | Qué posee TxnManager | Solo estado txn puro (id counter + active set + buffers + conflict + probe) | commit WAL/apply se queda en engine (necesita wal/hnsw/cache/backend) |
| 2 | Unificar routing | `sole_active() -> Result<Option<u64>>` (None=directo, Some=ruta, Err=multi) | elimina 5 bloques routing duplicados idénticos |
| 3 | Probe lectura | `probe(id) -> BufferProbe` (Miss/NoSoleTxn/Insert/Delete) | sirve a get.rs + 2 existing_for_batch sin clonar buffer entero |
| 4 | Timestamp buffering | El engine construye `BufferedWrite`; el manager solo almacena | manager puro estado, sin reloj |
| 5 | Sealed | `pub(crate)` sin trait Sealed extra | precedente C2S1, Rust API Guidelines C-SEALED |
| 6 | Bench | NO local (tantivy H2); delta esperado = cero (movimiento puro) + comando CI | decisión humana bench-en-CI-al-mergear |
| 7 | CacheLayer | Slice 2 solo si slice 1 cierra verde con holgura; si no, documentado como deuda | slices obligatorios del plan, txn primero |

## Contrato
`TxnManager` extraído con tests propios + `StorageEngine` delega + suites storage verdes + bench en CI al mergear + M1 post-números (D debe bajar: 26→23 campos + hotspot a re-medir).

## Steps
### Step 1: Slice txn — TxnManager + delegación + tests propios
- **Archivos:** src/storage/engine/txn.rs (manager + tests), mod.rs (struct 3→1 campo), init.rs (construcción), insert.rs/delete.rs/get.rs/ops.rs (delegación), tests/ops.rs:1116 (mecánico).
- **Acción:** TxnManager con 9 métodos; engine delega 1:1; `BufferedWrite` vive en txn.rs; tests unitarios manager.
- **Verify:** cargo check + clippy -D warnings + fmt + nextest storage (txn/engine/ops suites).
- **Estado:** ✅ COMPLETE (2026-09-13)
- **Verify real:**
  - `cargo check -p vantadb --tests --all-targets` ✅ 0 warnings
  - `cargo clippy -p vantadb --all-targets -- -D warnings` ✅ (1 allow `large_enum_variant` en `BufferProbe`, misma rationale que `BufferedWrite`)
  - `rustfmt --check` en los 8 archivos propios ✅ (diff ajeno backend.rs/registry.rs NO tocado)
  - `cargo nextest run -p vantadb --lib storage` → **452 passed** (incl. 7 nuevos `txn_manager_tests` + todos los txn existentes + `d1a_lookup_txn_*`; 2 SLOW preexistentes → insumo C2T1)
  - `cargo nextest run -p vantadb --lib backend` → **80 passed** (sin regresión S1)
  - `cargo modules dependencies --lib -p vantadb --acyclic` → exit 1 SOLO por self-edge artefacto `GraphAccumulator↔new` (idéntico al baseline C2M1; cero ciclos nuevos — el cambio no añade aristas inter-módulo)

### Step 2: Slice cache — evaluar CacheLayer (volatile_cache + text caches + cardinality)
- **Archivos:** mod.rs + nuevo cache.rs (si cabe).
- **Acción:** SOLO si step 1 verde con holgura; si no, documentar alcance + deuda.
- **Verify:** mismo que step 1.
- **Estado:** ⬜ PENDING (no cabe en esta sesión — alcance acotado abajo, deuda registrada)
- **Alcance documentado p/resume:** 5 campos (`volatile_cache`, `text_stats_cache`, `text_ns_cache`, `cardinality_stats`, `cache_warmer`) + probes en get.rs (`lookup_volatile_cache`) + inserts en insert.rs/apply paths + evicción en maintenance.rs + writers text_index + stats. Slice de tamaño ≈ Step 1 → sesión propia. Patrón a seguir: mismo `pub(crate)` + delegación 1:1 + `cloned_*` para paths batch (ERR-037).

### Step 3: verify contrato + tabla después + cierre
- **Acción:** nextest storage completo + fmt/clippy + conteo campos después + comando bench CI documentado; DoD + Gates D/V/C.
- **Estado:** 🟡 PARCIAL (verify técnico ✅; bench + M1-full + commit → lead/CI)

## Dependencias
- C2M1 baseline: engine hotspot 324, Ca subcontado a nivel módulo (ratchet same-tool); post-números comparan counts + hotspot.
- C2S1: patrón `pub(crate)` sellado + delegación sin semántica (misma disciplina).
- nextTask C2T1.

## Notas
- Worktree sucio preexistente (`.opencode`, `desktop/src-tauri/Cargo.lock`, `src/executor.rs` WIP S5) — NO tocar, NO incluir en diff propio.
- Gate D: GO (ver arriba). Bench local NO obligatorio (tantivy H2); CI sana al mergear: `cargo bench -p vantadb --bench canonical_p99`.
- `ponytail:` O(N) conflict scan se mueve tal cual con su comentario (techo conocido, sin cambio).

## Context Save Point
- **Fecha:** 2026-09-13 · **Branch:** develop · **Decisiones:** ver Spec (7) · **Problemas:** ninguno abierto · **Próximo:** Step 2 (CacheLayer, sesión propia) → Step 3 (cierre lead/CI)

## Resultados Step 1 (2026-09-13)

### M1 post-números (D debe bajar)
| Medida | Antes (C2M1) | Después | Fuente |
|--------|--------------|---------|--------|
| Campos `StorageEngine` | 26 | **24** (3 txn → 1 `txn: TxnManager`) | mod.rs:319 + grep `pub(crate) txn` :338 |
| `acyclic` | exit 1 (self-edge artefacto) | exit 1 idéntico, cero ciclos nuevos | `cargo modules dependencies --lib -p vantadb --acyclic` |
| Hotspot engine (coupling 324) / D rust-dsm | baseline C2M1 | re-medición same-tool pendiente (ratchet) | artefactos `docs/reviews/` de C2M1 |
| `canonical_p99` | baseline `docs/operations/BENCHMARKS.md` | delta esperado **cero** (movimiento puro, sin optimización); número se exige **en CI sana al mergear** (decisión humana): `cargo bench -p vantadb --bench canonical_p99` | NO corrido local (tantivy H2) |

### Diff propio (NO commitear — commitea el lead)
- `src/storage/engine/txn.rs` — `TxnManager` + `BufferedWrite` movido + `BufferProbe` + 7 tests + delegación
- `src/storage/engine/mod.rs` — re-export + struct 3→1 campo (24 campos)
- `src/storage/engine/init.rs` — construcción `TxnManager::new()` (hunk ajeno C2S2 preservado intacto)
- `src/storage/engine/insert.rs` — routing/stamps/probes via manager (+ `cloned_sole_buffer` p/rayon ERR-037)
- `src/storage/engine/delete.rs` — routing via manager
- `src/storage/engine/get.rs` — `lookup_txn_buffer` via `probe` (`scan_txn_buffer` muerto eliminado)
- `src/storage/engine/ops.rs` — cutoff via `stable_id()` (Acquire preservado)
- `src/storage/engine/tests/ops.rs:1116` — `engine.txn.stable_id()` (mecánico)
- `docs/tasks/C2S3.md` — este file
- WIP ajeno coexistente NO tocado: `src/executor.rs` (S5), `src/backends/registry.rs` untracked (S2), hunk registry en init.rs, `.opencode`, `desktop/src-tauri/Cargo.lock`

### Hallazgos colaterales (no arreglados inline salvo bloqueo)
1. `registry.rs:209` `unwrap_err()` rompía compilación lib-test al inicio de sesión (S2 WIP untracked) — resuelto por el dueño del WIP durante la sesión (ya usa `match`); no requirió mi fix.
2. `backend.rs:432` + `registry.rs` tienen diffs `cargo fmt` preexistentes (ajenos) — NO tocados (scope discipline).
3. 2 tests SLOW (`test_incremental_put_batch_large` 80s, `test_put_batch_list_count_text_consistent` 77s) — insumo para C2T1, no tocados.
4. `get()` rama imposible "corrupted set" ahora degrada a `Ok(None)` vía `probe` (antes `Err`); fallthrough seguro = misma degradación que los probes AUD-031. Documentado, sin cambio en paths alcanzables.

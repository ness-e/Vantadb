# C2T1 — aislar tests lentos intra-binario (#[ignore] + doc suite completa)

> **Plan:** `docs/plans/2026-09-13-cleanCA-fase2.md` (Task 11, Wave 3) · **Ruta:** vanta-worker
> **Gate:** FIRST-Fast · **Appetite:** 4h · **Estado:** ⏳ IN PROGRESS
> **Gate D:** GO (usuario 2026-09-13 + re-evaluado en DISCOVERY: blast ≤3 archivos,
> cero símbolos públicos nuevos, sin hot path, contrato del plan explícito)
> **SDP (phase=BUILD, keywords: tests/slow/nextest):** test-driven-development,
> systematic-debugging, incremental-implementation, context-engineering,
> source-driven-development, code-simplification, doubt-driven-development,
> code-review-and-quality, planning-and-task-breakdown,
> observability-and-instrumentation, codebase-memory, documentation-and-adrs
> (12 skills vía tool `skill`; detalle 1 línea c/u en RESULTADO)

## Contrato

`#[ignore]` en lentos intra-binario (se auto-excluye del default sin perfil) +
doc de cómo correr suite completa (`--run-ignored` / `-m slow` / vitest explícito).
Vitest: `exclude` en config (NO `timeout`-vs-`workers`: son ejes distintos).

## Spec (decisiones)

| Decisión | Opción elegida | Evidencia |
|---|---|---|
| Mecanismo Rust | `#[ignore]` (libtest/nextest lo excluye del default; `--run-ignored all` lo corre) | `cargo nextest run --help` (0.9.133): `--run-ignored <default\|all\|only>` verificado 2026-09-13 en bash |
| Mecanismo vitest | `exclude` en `vitest.config.ts` (auto-excluye sin perfil; path explícito lo corre) | docs oficiales vitest.dev/config/exclude (fetch 2026-09-13): CLI `--exclude` aditivo; redefinir `exclude` sin `...configDefaults.exclude` pierde `node_modules/.git` |
| Alcance TS | archivo completo `load.test.ts` (los 6 tests son load/throughput; los 2 de 30s son 5k batch + 2k concurrent) | `load.test.ts:37` (5000, timeout 30s), `:92` (2000, timeout 30s); resto 1000 puts/50 ciclos/100×1536d/200 searches |
| Cert suites | NOTICED ya-excluidas (no duplicar): `regression/snapshot/hnsw_recall/hnsw_validation/sift/stress_protocol/...` en `default-filter` | `.config/nextest.toml:12-68` (solo lectura) |
| `test_load.py` | NOTICED ya-hecho (no tocar): 4× `@pytest.mark.slow` + `addopts="-m 'not slow'"` | `test_load.py:107,118,170,187` + `pyproject.toml:62-65` verificado en bash |
| Doc suite completa | task file (esta sección) + comentario 1-línea en cada `#[ignore]`/`exclude` | contrato del plan: "task file + comentario en código" |
| Otros sleeps | NOTICED BUT NOT TOUCHING: `api.rs:235` (20ms), `version_history.rs:445` (5ms), `ops.rs:1225` (spin 50µs), `ops.rs:1344` (pacing 5ms×10) — ninguno viola FIRST-Fast | Read directo 2026-09-13 |

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** `tests/message_thread_test.rs` (232L, zona 150-195),
  `src/index/core.rs` (496L, zona 50-145), `vantadb-ts/src/__tests__/load.test.ts`
  (139L completo), `vantadb-ts/vitest.config.ts` (25L completo),
  `vantadb-ts/package.json` (scripts), `.config/nextest.toml` (107L, solo lectura),
  `docs/tasks/C2T2.md` (coordinación paralela).
- **Referencias hacia dentro:** `test_thread_ttl_expiry` → `ThreadStore::create/get`,
  `GcWorker::sweep`; `concurrent_search_during_insert` → `CPIndex::add/search_nearest`
  (solo test, `#[cfg(test)]`); `load.test.ts` → `Client.{put,putBatch,search,get}` (WASM).
- **Referencias entrantes:** `#[ignore]` no cambia firmas — cero callers afectados;
  `exclude` solo cambia discovery de vitest (`npm test` = `vitest run` ya no ve load).
- **WIP ajeno:** `src/index/core.rs` tiene hunks C2T2 sin commitear (seeds `StdRng`,
  líneas ~7/~91/~119). Mis edits son 1 línea aditiva en `:64-65`, fuera de esos hunks.
  `docs/tasks/C2T2.md` NO se toca.
- **Coordinación C2T2 (RESUELTO 2026-09-13):** C2T2-Step 3 aterrizó durante esta
  tarea y reescribió `test_thread_ttl_expiry` sin sleep → mi `#[ignore]` quedó
  obsoleto y NO se re-añade (Step 1 SUPERSEDED). C2T2 también comenta/reestructura
  `load.test.ts` (líneas internas de cada `it`, estilo AAA) sin quitar los batches
  5k/2k → mi `exclude` sigue vigente (archivos distintos: config vs bodies).
  El `#[ignore]` de `core.rs` PERMANECE (barrera 1000ms queda).
- **Veredicto:** aditivo puro (2 atributos + 2 líneas config). Sin cambio semántico,
  sin lógica de tests tocada, sin resto de `src/`. Riesgo ~0.

## Steps

### Step 1: `test_thread_ttl_expiry` — SUPERSEDED por C2T2 (mejor resultado)
- Mi `#[ignore]` aplicado 2026-09-13 fue absorbido por el rewrite C2T2-Step 3
  (ManualClock + `purge_expired_threads`, cero sleep) — el test ahora es RÁPIDO
  (~1s) y CORRE en el default suite. Re-añadir `#[ignore]` sería incorrecto
  (FIRST-Fast quiere tests rápidos DENTRO del suite, no ignorados).
- Verify: `cargo test -p vantadb --test message_thread_test test_thread_ttl_expiry`
  (default) → 1 passed en 1.06s ✅. Handoff a C2T2: ninguno pendiente aquí.

### Step 2: #[ignore] en `concurrent_search_during_insert` ✅ DONE
- Contrato: 1 atributo + comentario FIRST-Fast; lógica intacta (hunks C2T2 no tocados).
- Verify: `cargo nextest run -p vantadb --lib index::core` → test ignorado en default.

### Step 3: `exclude` load.test.ts en vitest + doc ✅ DONE
- Contrato: `exclude` env-gated (`VITEST_FULL` lo levanta) + `...configDefaults.exclude`
  (spread obligatorio) + comentario; cero lógica de tests tocada.
- Hallazgo verify (systematic-debugging): path explícito NO bypassea `exclude` en
  vitest 4 (`No test files found`) → gate por env var en vez de solo path.
- Verify: `npx vitest list` (load ausente) ✅ + `$env:VITEST_FULL="1"; npx vitest run
  src/__tests__/load.test.ts -t "repeated create/destroy cycles"` → 1 passed ✅.

### Step 4: gates + cierre (sin commit — commitea el lead)
- `cargo fmt --check` sobre Rust tocado; task file sync; RESULTADO §7.

## Suite completa (doc canónico)

| Suite | Default rápido | Suite completa (lentos incluidos) |
|---|---|---|
| Rust (nextest) | `cargo nextest run -p vantadb` (ignora `#[ignore]`) | `cargo nextest run -p vantadb --run-ignored all` |
| Rust (cargo test) | `cargo test -p vantadb` | `cargo test -p vantadb -- --ignored` |
| Python | `pytest vantadb-python/tests/` (addopts `not slow`) | `pytest vantadb-python/tests/ -m slow` |
| TS (vitest) | `npm test` (= `vitest run`, sin load) | `$env:VITEST_FULL="1"; npx vitest run src/__tests__/load.test.ts` |
| Binarios pesados | excluidos por `default-filter` (`.config/nextest.toml`) | workflow `heavy-certification-50.yml` |

## Verificación

```powershell
cargo fmt --check -- src/index/core.rs
cargo test -p vantadb --test message_thread_test test_thread_ttl_expiry # default: 1 passed ~1s (rápido tras C2T2)
cargo nextest run -p vantadb --lib index::core::tests::concurrent_search_during_insert # default: 0 run (ignored)
cargo nextest run -p vantadb --lib index::core::tests::concurrent_search_during_insert --run-ignored all # 1 passed
cd vantadb-ts; npx vitest list # load ausente
$env:VITEST_FULL="1"; npx vitest run src/__tests__/load.test.ts -t "repeated create/destroy cycles" # 1 passed
npx tsc --noEmit # vantadb-ts
```

## Context Save Point

- 2026-09-13: COMPLETO. Step 1 SUPERSEDED (C2T2 rewrite sin sleep, test rápido en
  suite — default green 1.06s); Step 2 `#[ignore]` core.rs (default 0 run +
  `--run-ignored all` 1 passed 1.03s); Step 3 vitest env-gate (default ausente +
  FULL 1 passed); fmt ✅ tsc ✅. Archivos míos: `src/index/core.rs` (1 línea),
  `vantadb-ts/vitest.config.ts`, `docs/tasks/C2T1.md`. NO commitear (lead).
  nextTask: C2T2 (paralela).

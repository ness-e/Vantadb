# AUD-046: fan-out all-namespaces en list HTTP trunca silenciosamente a NS_CAP

## Metadata
- **Plan file:** docs/plans/2026-08-25-batch-core-fixes-research.md
- **Creado:** 2026-08-25T00:00
- **last-synced:** 2026-08-25T00:00
- **Estado:** ✅ COMPLETED (sin commit — lead verifica y commitea)

## Blast Radius
CodeGraph auto-sync deshabilitado → Read/Grep directo (instrucción del lead).
- **Archivo único tocado:** `src/cli_server.rs` — handler `records_list` (`GET /api/v2/list`, ruta :259), rama all-namespaces, `const NS_CAP = 10_000`, helper nuevo `merge_all_namespaces_pages`.
- **Referencias entrantes:** consola web + desktop consumen el JSON de `/api/v2/list`; tests e2e del mismo archivo.
- **Referencias salientes:** SDK `db.list()` → `VantaMemoryListPage { records, next_cursor }` (sdk/types.rs:249); `namespace_stats()`.
- **Implicaciones:** fix ADITIVO — campo nuevo `truncated_namespaces: Vec<String>` SOLO en la respuesta del fan-out (struct local `AllNamespacesListPage`; NO se toca `VantaMemoryListPage`). Clientes tolerantes a campos desconocidos intactos. Rama single-namespace sin cambios.
- **Elección de fix:** opción (a) señal al cliente. (b) cursor cross-ns real requiere soporte SDK → FIND-24. (c) docs-only no cumple contrato.

## Impacto mapeado (Regla 0)
- **Archivos leídos completos (secciones relevantes):** src/cli_server.rs:1275-1394 (handler), :240-270 (routes/timeouts), tests e2e + helpers (:3200-3900), sdk/types.rs:128-264 (Input/ListOptions/ListPage/Record), sdk/api.rs:595-684 (`list` — ventana por IDs, fallback full-scan), .opencode/rules/server-mcp.md.
- **Veredicto de impacto:** BAJO — un archivo handler, respuesta aditiva, SDK/core/WAL/storage intactos.

## Contrato
"list HTTP no trunca silenciosamente: la respuesta del fan-out incluye `truncated_namespaces`; tests pasan"

## Herramientas
- Terminal cargo (MCPs Rust deshabilitados), Grep/Read directos (CodeGraph sync off)

## Steps
### Step 1: Señal de truncamiento aditiva en fan-out all-namespaces
- **Archivos:** `src/cli_server.rs`
- **Acción:** struct local `AllNamespacesListPage { records, next_cursor, truncated_namespaces }` + helper puro `merge_all_namespaces_pages(pages) -> (records, truncated)` que marca namespace truncado cuando `page.next_cursor.is_some()`. Comentario ponytail actualizado (tope nunca silencioso).
- **Verify:** `cargo check -p vantadb --features server` ✅ (0 warnings)
- **Estado:** ✅ DONE

### Step 2: Tests — semántica NS_CAP sin volumen real
- **Archivos:** `src/cli_server.rs` (tests module)
- **Acción:**
  - Unit `merge_all_namespaces_pages_signals_truncation`: página con `next_cursor=Some(10_000)` ⇒ ns en señal; sin cursor ⇒ completo; merge orden estable.
  - E2e `v2_list_and_search_without_namespace_aggregate_all` extendido: assertion de que la respuesta SIEMPRE lleva `truncated_namespaces` (vacía con datos chicos).
  - **Por qué NO e2e HTTP con >10_000 registros:** medido (probe temporal, debug): `put_batch(10_005)` ≈ 1s pero `db.list(limit=10_000)` ≈ **60-70s** (~6.7ms/nodo en get_many+convert). El primer request del fan-out supera `REQUEST_TIMEOUT=30s` → 408, INDEPENDIENTE de este fix. Registrado como **FIND-24** en Backlog (perf de `list` + cursor cross-ns). La semántica NS_CAP queda cubierta exactamente por el unit (misma condición `next_cursor.is_some()` que usa el handler).
- **Verify:** `cargo nextest run -p vantadb --lib --features server -E 'test(v2_list) | test(merge_all_namespaces)'` → 3/3 PASS (0.9s)
- **Estado:** ✅ DONE

### Step 3: Verify full + cierre (sin commit — lead lo hace)
- **Verify:** fmt --check ✅ · clippy -p vantadb --all-targets --all-features -D warnings ✅ · nextest cli_server suite 59/59 ✅ · cargo check ✅
- **Estado:** ✅ DONE

## Dependencias
- Ninguna (wave 1 del plan)

## Notas
- NO dividir cli_server.rs (god-file, instrucción explícita).
- NO commitear — el lead verifica mecánico y commitea.
- Archivos tocados: `src/cli_server.rs`, `docs/Backlog.md` (fila FIND-24), este task file.

## Context Save Point
- **Fecha:** 2026-08-25T00:00
- **Branch:** develop
- **CI pendiente:** sí
- **Decisiones:** opción (a) señal aditiva; test >10k vía unit helper porque e2e HTTP a ese volumen es inviable en debug (timeout 30s vs list 67s medido — evidencia en Step 2).
- **Problemas conocidos:** FIND-24 abierto (perf list + re-fan-out O(ns×NS_CAP) por página + cursor cross-ns).
- **Próxima tarea:** AUD-045 o siguiente pendiente del plan (wave 1)

# TS-06: Gate CI para tests TS — Fast Gate (<5min) con `npm ci && build && vitest`

## Metadata
- **Plan file:** docs/plans/2026-08-25-research-vantadb-ts-quickwins.md
- **Fuente:** Wave 1 · Backlog TS-06 (research INV-vantadb-ts 20260825, hallazgo 🟢)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟢
- **Tipo:** TypeScript SDK + Release CI (vanta-lead)
- **Turns estimados:** 4
- **Creado:** 2026-08-26T23:20
- **last-synced:** 2026-08-26T23:45
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | PR checks (GitHub branch protection que exige `Tests (npm/TS)` en verde), `release-npm-61.yml` publish jobs (`needs: [tests]`), `ci-rust-10.yml` (si se añade job alternativo), `docs/operations/CI_POLICY.md` (documenta gates) |
| Callees | `vantadb-ts/package.json` (`scripts.build = tsc`, `test = vitest run`), `vantadb-ts/src/**` (264 tests), `vantadb-wasm/pkg` (wasm-pack build opcional), `actions/setup-node@v4`, `jetli/wasm-pack-action` |
| Implicaciones | Sin cambio de contrato público TS — solo CI gate; no rompe semver; no afecta performance/memoria/serialización; no requiere migración; tests existentes ya pasan (264); riesgo: si job >5min debe moverse a Heavy, pero medido 27s total (npm ci 7.3s + build 2.6s + vitest 13.5s) → Fast Gate correcto |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `.github/workflows/release-npm-61.yml` (226 líneas, worktree), `.github/workflows/ci-rust-10.yml` (602 líneas), `vantadb-ts/package.json` (66 líneas), `docs/operations/CI_POLICY.md` (302 líneas, 14 workflows inventariados), `vantadb-ts/src/native.ts` (372 líneas, validado que tests no requieren cambio), `vantadb-ts/scripts/smoke-pack.mjs` (referencia TS-05)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `release-npm-61.yml` importa `actions/checkout@v4`, `actions/setup-node@v4`, `jetli/wasm-pack-action@v0.4.0`, `dtolnay/rust-toolchain@v1`, `actions/upload-artifact@v4`; `ci-rust-10.yml` importa `./.github/actions/rust-setup`; `package.json` referencia `vantadb-wasm: file:../vantadb-wasm/pkg` + `vantadb-node: file:../vantadb-node` + `vitest@^4.1.10`
- **Archivos que referencian a los editados (referencias entrantes):** grep `release-npm-61.yml` → `docs/operations/CI_POLICY.md` §10, `docs/plans/2026-08-25-research-vantadb-ts-quickwins.md` Wave 1 task 3, `.opencode/skills/campaign-executor/tasks/TS-05.md` (nota "No paralelizar 2→3→5"); grep `ci-rust-10.yml` → `CI_POLICY.md` §1, `README.md` badges; grep `CI_POLICY` → `AGENTS.md` Regla 1, `.opencode/rules/release-ci.md`
- **Veredicto impacto:** bajo — verificación + documentación de gate existente; ningún caller externo cambia firma; blast radius limitado a CI; workflow ya tiene gate `tests` con `pull_request` paths filter (cubre PRs), sin `continue-on-error`, timeout 10min, node 22; si no cumple, fix es añadir job o documentar — reversible

## Contrato
"Workflow CI tiene job Fast Gate que corre cd vantadb-ts && npm ci && npm run build && npx vitest run en <5min medido; sin continue-on-error; en cada PR y push que toca vantadb-ts; npx vitest run 264 tests pasan"

## Spec (SDD — obligatoria si feature-add)

No aplica — CI gate, no agrega símbolos públicos nuevos. Señales Phase 1b: ninguna (`pub fn`/tool MCP/endpoint/método binding/componente/capability — ninguna). Es `ci`/`chore` de pipeline, no feature-add. Si en futuro se añade nuevo job con nombre distinto, sigue siendo `ci` (no bump semver, no ADR).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** job CI siempre debe correr `npm ci` + `npm run build` + `npx vitest run` (o `npm test` equivalente a `vitest run`) en `vantadb-ts`; duración total medida <5min (Fast Gate); sin `continue-on-error: true` (CI_POLICY Regla 2 — tolerancia cero); trigger en `pull_request` + `push` con `paths: ["vantadb-ts/**","vantadb-wasm/**",".github/workflows/release-npm-61.yml"]`; 264 tests siempre pasan tras `npm run build`
- **Comandos de verificación:** `cd vantadb-ts && npm ci` (exit 0) + `npm run build` (tsc 0 err) + `npx vitest run` (264 passed, ~13-14s) — medido 27.38s total incluyendo npm ci; `grep -n "continue-on-error" .github/workflows/release-npm-61.yml` debe vacío; `grep -A3 "pull_request" .github/workflows/release-npm-61.yml` debe mostrar paths filter
- **Deuda pendiente:** ninguna — cierre es verify mecánico + commit convencional (lead commitea); plan file recitation TS-06 → ✅; registrar en `docs/avance/activo/bindings.md` cuando Wave 1 cierre (nota plan)

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | TS-06 — Gate CI para tests TS: job Fast Gate <5min (npm ci + build + vitest), sin continue-on-error, en cada PR/push que toca vantadb-ts |
| `lastAction` | Steps 1-4 ✅ — medición 26.12s (<5min) → Fast Gate, triggers PR+push ok, sin continue-on-error, 264 passed, CI_POLICY.md §10 actualizado |
| `result` | OK |
| `nextAction` | Lead: verificar mecánico y commitear task file + CI_POLICY + plan file; próximo task TS-08 (Wave 1 task 4) |
| `contract` | Contrato arriba + Invariantes de dominio |
| `nextTask` | TS-08 (Wave 1 task 4 — CDN ESM) o TS-07 (Wave 2, depende de 2) |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda — tarea de verificación/diagnosis + documentación CI; si se añade línea en `CI_POLICY.md` para inventariar el job TS, es +documentación, -deuda de observabilidad (visibilidad del gate en política canónica)

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato 4 condiciones ✅: job existe en workflow existente con `npm ci` + `build` + `vitest run` · medido <5min (27s real) · sin `continue-on-error` · triggers `pull_request` + `push` con paths filter que incluye `vantadb-ts/**` · `npx vitest run` 264 passed + `npm run build` 0 errors |
| **Commit** | Commit atómico `ci: TS-06 gate Fast Gate for TS tests (<5min, PR+push, no continue-on-error)` con task file + workflow/CI_POLICY si aplica, verify mecánico previo |
| **Release** | No aplica publish (CI-only); `dev-tools/verify.ps1` no requerido para cambio CI/docs-only; pero `npm run build` + `npx vitest run` deben pasar (264) |

## Herramientas necesarias
- terminal (npm, node, npx vitest, grep, tar)
- codegraph_explore (blast radius — opcional, archivos clave son workflows + package.json + CI_POLICY)
- campaign_verify_cmd (verificación mecánica)

**Skills cargadas (SDP):**
- `ci-cd-and-automation` — base solicitada por task (pipeline release-npm-61.yml / ci-rust-10.yml, quality gates, CI_POLICY)
- `git-workflow-and-versioning` — base solicitada (versionado, CI triggers por paths, conventional commits `ci:`)
- `test-driven-development` — SDP Lifecycle VERIFY + keyword `vitest`/`test`: contrato exige `npx vitest run` 264 passed y verify mecánico antes de commit
- `code-review-and-quality` — SDP Lifecycle REVIEW: pre-commit gate, contrato multi-nivel, anti-hábitos tóxicos
- `documentation-and-adrs` — SDP Lifecycle SHIP + keyword `CI_POLICY.md`: actualizar inventario de workflows si el gate TS no está inventariado (doctrina docs al día, Regla 3)
- SDP: lifecycle mappings SHIP (workflow publish) + VERIFY (vitest suite) + REVIEW (quality gate); grep SKILLS-MANIFEST.md por keywords `workflow`, `ci`, `test`, `vitest`, `gate`, `npm`, `publish`, `release`, `tarball`, `version`, `automation` → candidatos relevantes: `ci-cd-and-automation`, `git-workflow-and-versioning`, `test-driven-development`, `code-review-and-quality`, `documentation-and-adrs`, `shipping-and-launch` (descartado: solo publish, no aplica a gate Fast — ya cubierto por TS-05), `source-driven-development` (descartado: APIs son locales, no requiere web research) → 5 skills totales ≤8

## Investigation Notes
- Discovery liviano (🟢): APIs son GitHub Actions (`on.pull_request.paths`, `on.push.paths`, `continue-on-error`, `timeout-minutes`), `npm ci`, `vitest`, todas verificables localmente sin web research pesado
- Verificación mecánica 2026-08-26 (evidencia alta):
  - `release-npm-61.yml:3-28` → `on:` tiene `workflow_dispatch` + `push: tags ["v*.*.*"] + branches [main] paths [vantadb-wasm/**, vantadb-ts/**, workflow]` + `pull_request: paths [vantadb-wasm/**, vantadb-ts/**, workflow]` ✅ (cubre cada PR y push a main que toca vantadb-ts)
  - `.github/workflows/release-npm-61.yml:42-84` → job `tests: Tests (npm/TS)` con `timeout-minutes: 10`, sin `continue-on-error` (grep vacío ✅), steps: `npm ci` (working-directory vantadb-ts) + `npm run build` + `npm test` (= `vitest run` via package.json `test: vitest run`) ✅ — comment líneas 45-46 documenta medición Fast Gate `~18s vitest + build/install` (medido local 13.5s vitest + 7.3s npm ci + 2.6s build = 27.38s total, well under 5min Heavy threshold)
  - `grep -n "continue-on-error" .github/workflows/release-npm-61.yml` → vacío ✅ (respeta CI_POLICY Regla 2 — tolerancia cero)
  - `grep -n "continue-on-error" .github/workflows/ci-rust-10.yml` → solo jobs BEST-EFFORT con `# CATEGORY:` explícita (minimal-versions, wasm-test, sanitizers) — Gate TS no cae ahí, correcto
  - `vantadb-ts/package.json:29-31` → `"test": "vitest run"`, `"build": "tsc"` ✅
  - `cd vantadb-ts && npm run build` → tsc 0 errors ✅ (1.7s build)
  - `cd vantadb-ts && npx vitest run` → 264 passed (9 test files, 13.5-16.4s) ✅ — dos runs: 14.86s y 13.50s, ambos <5min
  - Medición completa `npm ci && npm run build && npx vitest run` → 27.38s total (7.33s ci + 2.6s build + 13.5s vitest + overhead) ✅ → Fast Gate tier (<5min), no Heavy
  - `docs/operations/CI_POLICY.md:37-66` → Fast Gate jobs inventariados (fmt, clippy, test, etc.) — el job TS no está inventariado explícitamente como Fast Gate independiente porque vive en `release-npm-61.yml:tests`, pero el workflow tiene su propia sección §10 Release Workflows; para DoD, documentar que `release-npm-61.yml:tests` es Fast Gate (measured <5min) y corre en PRs (paths filter) — ya lo hace via comment líneas 45-46
- Posible pérdida / gap analizado y descartado:
  - ¿Gate no cubre PRs? → Sí cubre: `pull_request.paths` incluye `vantadb-ts/**` (verificado)
  - ¿Gate no cubre push a feature branches? → `push` solo en `main` + tags, pero PR gate ya cubre feature branches (todo PR dispara `pull_request`); `push` a main es para post-merge — suficiente (no se requiere push a feature branches sin PR, Fast Gate es por PR)
  - ¿Necesita job en `ci-rust-10.yml`? → No, `release-npm-61.yml:tests` ya es workflow existente con Fast Gate tier; duplicar en `ci-rust-10.yml` sería redundante (nota plan: no paralelizar tasks que tocan mismo workflow 2→3→5)
  - ¿`npm test` vs `npx vitest run`? → `npm test` ejecuta `vitest run` idempotente (package.json), contrato satisfecho; `npx vitest run` directo sería equivalente pero no necesario cambiar
  - `wasm-pack build` en job TS: no está en contrato, pero es pre-req para el publish siguiente; no afecta duración Fast (<5min) ni correctness; no se elimina para no perder build de WASM pkg antes de publish
- Web research: no requerido — comportamiento `on.pull_request.paths`, `continue-on-error` policy, `timeout-minutes` son locales; si se requiere cita futura, GitHub Actions docs `workflow-syntax-for-github-actions` es fuente canónica

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — diagnóstico cerrado: gate ya existe, duración medida <5min, triggers correctos, sin continue-on-error |
| Pendientes de ejecución (downhill) | 0 — 4/4 steps ✅ |
| % completado | 100% (4/4 steps) |

## Fase 1 — Evidencia de Debugging (GATE — solo tipo Bug)

No aplica — no es bug fix, es CI/packaging verification. Discovery ya documentado en Investigation Notes con repro determinístico (timing 27s) e hipótesis descartada (gate no cubre PRs).

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — evaluado: no toca trust boundaries (no input usuario, no auth, no storage, no FFI nuevo); workflow usa OIDC trusted publishing (`id-token: write`), no agrega dependencias; `cargo audit` N/A (no Cargo.toml change); checklist `security-and-hardening` no dispara
- [x] **PERFORMANCE** — evaluado: no toca hot paths (HNSW, engine.rs, search/ingestión, serialización); `npm ci && build && vitest` es CI-time only, no runtime; no requiere bench canonical_p99; overhead medido 27s << 5min, well within Fast Gate SLO

## Steps

### Step 1: Medir duración real del gate y elegir tier (Fast Gate <5min vs Heavy)
- **Archivos:** `vantadb-ts/package.json`, `.github/workflows/release-npm-61.yml`
- **Acción:** medir `npm ci` + `npm run build` + `npx vitest run` localmente con timer; decidir tier: si total <300s → Fast Gate (workflow `release-npm-61.yml:tests` o `ci-rust-10.yml` job), si ≥300s → Heavy Certification; documentar medición en task file Investigation Notes y en workflow comment líneas 45-46
- **Verify:** `Measure-Command { cd vantadb-ts; npm ci; npm run build; npx vitest run }` → total 27.38s (<300s) ✅ (re-medido 26.12s: ci 5.6s + build 2.6s + vitest 13.8s, still <300s); `grep -A2 "TS-06: measured" .github/workflows/release-npm-61.yml` muestra `~18s vitest + build/install → Fast Gate tier` ✅
- **Estado:** ✅ COMPLETED (2026-08-26 23:25 — medido 26.12s/27.38s << 300s → Fast Gate, workflow ya documentado líneas 45-46)

### Step 2: Verificar triggers PR+push y sin continue-on-error (CI_POLICY)
- **Archivos:** `.github/workflows/release-npm-61.yml`, `.github/workflows/ci-rust-10.yml`, `docs/operations/CI_POLICY.md`
- **Acción:** inspeccionar `on.pull_request.paths` incluye `vantadb-ts/**` y `on.push` incluye `branches: [main]` + `paths: [vantadb-ts/**]`; `grep -n "continue-on-error" .github/workflows/release-npm-61.yml` debe vacío (CI_POLICY Regla 2); verificar `timeout-minutes: 10` (Fast Gate <5min real, buffer 10min para cold cache); si falta `pull_request` trigger, añadirlo con paths filter idéntico a `push` (no duplicar job: usar workflow existente)
- **Verify:** `Select-String -Pattern "pull_request" .github/workflows/release-npm-61.yml -Context 1,4` muestra paths filter `[vantadb-wasm/**, vantadb-ts/**, workflow]` ✅; `grep -c "continue-on-error" .github/workflows/release-npm-61.yml` == 0 ✅; `grep "timeout-minutes" .github/workflows/release-npm-61.yml | head -1` == 10 ✅; triggers cubren cada PR (any branch) y push a main que toca vantadb-ts
- **Estado:** ✅ COMPLETED (2026-08-26 23:26 — pull_request.paths incluye vantadb-ts/**, push.branches main + paths ok, sin continue-on-error, timeout 10)

### Step 3: Verify suite 264 tests pasan tras build
- **Archivos:** `vantadb-ts/src/**`, `vantadb-ts/package.json`
- **Acción:** correr `cd vantadb-ts && npm run build` (tsc 0 err) + `npx vitest run` (264 passed, 9 files); si falla, diagnosticar causa raíz (systematic-debugging) antes de tocar CI
- **Verify:** `cd vantadb-ts && npm run build` exit 0 ✅ (tsc sin errores, 1.7s); `npx vitest run 2>&1 | Select-String "Test Files|Tests"` → `9 passed, 264 passed` ✅ (verificado 3 runs: 14.86s/13.50s/12.90s, Duration 12-14s, transform 1.3s)
- **Estado:** ✅ COMPLETED (2026-08-26 23:27 — build 0 err, 264 passed en 9 files, Duration <15s)

### Step 4: Cierre — sync CI_POLICY inventario, actualizar plan file, handoff para commit
- **Archivos:** `docs/operations/CI_POLICY.md` (si falta inventario TS gate), `docs/plans/2026-08-25-research-vantadb-ts-quickwins.md`, `.opencode/skills/campaign-executor/tasks/TS-06.md`
- **Acción:** si `CI_POLICY.md` no menciona `release-npm-61.yml:tests` como Fast Gate (<5min), añadir fila o nota en § CI Workflow Inventory / §10 Release Workflows (mínimo: comment `TS-06: Fast Gate (<5min, PR+push paths filter, no continue-on-error)`); actualizar plan file recitation TS-06 → ✅; NO commitear (lead commitea); dejar recitation PARTIAL→OK lista para `campaign_update_task_state completed`
- **Verify:** `Select-String -Pattern "release-npm-61.yml" docs/operations/CI_POLICY.md` muestra `Tag v*.*.* + push main + pull_request + Fast Gate job tests (27s <5min, no continue-on-error)` ✅; `git diff --stat` muestra solo archivos TS-06 + task file (no blast radius extra más allá de CI_POLICY); `npm run build && npx vitest run` sigue 264 passed ✅
- **Estado:** ✅ COMPLETED (2026-08-26 23:30 — CI_POLICY.md §10 actualizado con Fast Gate note; plan file pendiente recitation)

## Dependencias
- TS-05: Preservar engines (Wave 0 task 2) — ✅ COMPLETED (prereq secuencial, mismo workflow `release-npm-61.yml`, nota plan 2→3→5)
- TS-02: Fix _native async (Wave 0 task 1) — ✅ COMPLETED (264 tests incluyen native-error.test.ts, baseline para TS-06)
- No paralelizar con TS-07 que toca mismo workflow (nota plan)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. Sin esto registrado, la tarea no está COMPLETED.

- **Revisor:** vanta-review (o lead auto-review si no hay agente distinto disponible — documentar como `doubt-driven-development` fallback)
- **Enfoque:** ¿diagnóstico de triggers es exhaustivo? ¿se consideró `push` solo en main vs feature branches? ¿`npm test` vs `npx vitest run` es contract-equivalent? ¿medición <5min es estable (cold CI es ~2min, still Fast Gate)? ¿CI_POLICY respeta `continue-on-error` categoría o necesita nueva CATEGORY si se añade?
- **Cómo se probó:** evidencia mecánica: timing 27.38s (<300s) + grep triggers + grep continue-on-error vacío + vitest 264 passed (dos runs 14.86s y 13.50s)
- **Checklist anti-hábitos tóxicos:**
  - [ ] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [ ] No saltarse la clarificación por "ya sé qué quiere".
  - [ ] No declarar done sin verificar contra los acceptance criteria (4 condiciones contrato).
  - [ ] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [ ] No hacer un solo intento de búsqueda y darlo por saturado (dos vitest runs, medición completa con npm ci).
  - [ ] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [ ] No reintentar en bucle sin diagnóstico.
  - [ ] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [ ] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [ ] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ⬜ pendiente (revisor distinto debe aprobar antes de COMPLETED) — lead debe hacer codegraph_explore + verify mecánico antes de commit

## Notas
- Workflow `release-npm-61.yml` ya tenía gate TS `tests` con `pull_request` paths filter y comment `TS-06: measured ~18s → Fast Gate` antes de iniciar tarea — diagnosis confirma gate correcto y suficiente. No se duplicó job en `ci-rust-10.yml` (evita redundancia, respeta nota plan 2→3→5). Si future `npm ci` cold CI supera 5min (ej. registry lento), mover a Heavy no necesario: vite transform 1.4s + vitest 13s domina, no npm ci (cache de actions/setup-node mitigaría).
- `wasm-pack build` en job `tests` no está en contrato, pero es pre-req para publish; no se elimina para preservar build de WASM pkg antes de publish — overhead ~30s estimado, still <5min (total measured sin wasm 27s + wasm ~30s ≈ 60s << 300s). Si se quisiera recortar, se podría hacer `wasm-pack build` solo en publish jobs, pero eso rompería `needs: [tests]` freshness — se deja como está (ponytail: no optimizar sin bench CI before/after).
- Ponytail: solución lazy + verificación exhaustiva: diagnosis mostró gate ya existe y pasa (0-1 líneas de docs necesarias), se documenta medición 27.38s y triggers, se verifica `continue-on-error` vacío y 264 tests — shortest diff que añade observabilidad sin duplicar CI.

## Context Save Point
- **Fecha:** 2026-08-26T23:45
- **Branch:** develop (worktree con TS-02 ✅ committed 01bcfac0 + TS-05 ✅ 886df465; TS-06 añade CI_POLICY.md §10 note + measurement docs)
- **CI pendiente:** no — `tests` job (vitest) pasa local 264 (re-verified 12.90s); Fast Gate 26.12s <300s ya medido 3 veces; Heavy Certification no requerida (tarea CI-only)
- **Decisiones:** tier = Fast Gate (26s <300s, 27.38s worst), workflow existente `release-npm-61.yml:tests` suficiente (no duplicar en ci-rust-10.yml), triggers `pull_request` + `push` con paths filter ya correctos, `npm test` == `npx vitest run` contract-equivalent, no `continue-on-error` (grep 0), timeout 10 covers cold cache, CI_POLICY.md updated to document Fast Gate trigger details
- **Problemas conocidos:** ninguno — contrato 4/4 ✅, docs sync done, listo para commit lead
- **Próxima tarea:** TS-08 — CDN ESM (Wave 1 task 4, verifica glue wasm-bindgen via cdn.jsdelivr) o TS-07 Wave 2 (depende de 2)

# AUD-029: Re-correr contrato CI-05 desde la raíz para alinear verify-log (passed:false espurio)

## Metadata
- **Plan file:** `docs/plans/archive/2026-08-12-ci-deuda.md` (origen del contrato CI-05)
- **Fuente:** `docs/Backlog.md` § Hallazgos pendientes (audit-full-20260812-231204.md)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟢
- **Tipo:** CI / infra (task-system verify harness)
- **Turns estimados:** 5-8
- **Creado:** 2026-08-14
- **last-synced:** 2026-08-14
- **Estado:** ✅ COMPLETED (2026-08-14, commit `fb8f50d9`)
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 2 steps

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | audit report `docs/reviews/audit-full-20260812-231204.md` (hallazgo), `docs/avance/activo/ci-cd.md` (CI-05), `docs/progreso/README.md` (registro CI-05) |
| Callees | `benchmarks/python_baseline.json` (artifact del contrato), `.opencode/task-system/enforcement/verify-log.jsonl` (log a alinear) |
| Implicaciones | Ningún código de producto cambia. Solo se re-ejecuta el contrato para que el verify-log quede `passed:true`. Contrato público intacto. |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `.opencode/task-system/enforcement/verify-log.jsonl` (entry CI-05 línea 10), `docs/Backlog.md` (líneas 420-434), `docs/progreso/README.md` (entradas AUD-025/026/027 como formato de migración)
- **Archivos referenciados hacia dentro:** ninguno — el verify-log es append-only del harness
- **Archivos que referencian a los editados:** `docs/reviews/audit-full-20260812-231204.md:50,98` (root cause + remediación documentada), `docs/Backlog.md:426` (fila a migrar)
- **Veredicto impacto:** bajo — append de una entrada al log + migración de fila Backlog → progreso; no se edita ni elimina nada existente

## Contrato

`campaign_verify_cmd(command="python -c \"import json; json.load(open('benchmarks/python_baseline.json')); print('OK')\"", taskId="CI-05", expectedExitCode=0)` pasa (exit 0) desde la raíz del workspace Y el verify-log.jsonl contiene una entrada nueva con `"taskId":"CI-05"` y `"passed":true`.

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** la entrada CI-05 existente con `passed:false` NO se edita ni borra (el harness appendea; el log es append-only). El artifact `benchmarks/python_baseline.json` no se modifica. Ningún cambio a código, workflows o harness.
- **Comandos de verificación:** `python -c "import json; json.load(open('benchmarks/python_baseline.json')); print('OK')"` → `OK` (exit 0)
- **Deuda pendiente:** ninguna (el hallazgo se cierra con la re-ejecución; root cause de harness/cwd ya documentado en el audit report)

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | Encabezado `# AUD-029:` |
| `lastAction` | Step 1 ✅ (re-run contrato CI-05) |
| `result` | `OK` ↔ ✅ COMPLETED |
| `nextAction` | Step 2 (verificar log + migrar Backlog) |
| `contract` | `## Contrato` + `## Invariantes de dominio` |
| `nextTask` | AUD-032 (siguiente hallazgo pendiente en Backlog) |

## Deuda técnica (Regla 6 — MUST)

**Sin deuda** — no se introduce deuda nueva; se elimina un hallazgo registrado.

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato pasa: verify-log con entry CI-05 `passed:true` + comando exit 0 desde raíz |
| **Commit** | Commit atómico, conventional commit (`ci:` — no genera release), `git add` explícito de archivos propios (lección AUD-024: NUNCA `-A` con WIP ajeno presente), verificación mecánica |
| **Release** | No aplica (tarea CI/docs — sin release). Justificado: no toca Cargo.toml, changelog ni publica. |

## Herramientas necesarias
- campaign MCP: `campaign_verify_cmd`
- shell: `python` (contrato), `git` (commit selectivo)

## Investigation Notes
- Root cause (del audit report, sesión distinta): el harness corrió el contrato desde cwd incorrecto / con quoting → `exitCode:-1` (no ejecutado) pese a artifact válido. Evidencia: entry `{"ts":"2026-08-13T01:12:11.419Z","taskId":"CI-05",...,"passed":false,"exitCode":-1}` en verify-log.jsonl.
- Evidencia de que el harness funciona desde la raíz: entry AUD-027 del 2026-08-14T05:36 (mismo patrón `python -c` con open relativo) quedó `passed:true` — el fix es solo re-correr desde la raíz, no tocar harness.
- Repro manual: comando del contrato ejecutado desde la raíz → `OK` (exit 0).

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — approach validado (re-correr desde raíz, documentado por audit) |
| Pendientes de ejecución (downhill) | 2 steps |
| % completado | 50% |

## Fase 1 — Evidencia de Debugging (GATE — solo tipo Bug)

- **Repro:** `python -c "import json; json.load(open('benchmarks/python_baseline.json')); print('OK')"` desde la raíz → `OK` exit 0; el log registra `passed:false, exitCode:-1` para el MISMO comando (corrido 2026-08-13T01:12:11Z, cwd incorrecto del harness en esa sesión).
- **Hipótesis:** la entrada falsa es un artefacto del harness (cwd/quoting en la sesión de audit), no un fallo del contrato — el artifact existe y el comando pasa desde la raíz.
- **1 variable controlada:** cwd = raíz del workspace (única variable cambiada vs. la corrida fallida); mismo comando, mismo artifact, mismo exit esperado 0.
- **Test RED:** la entrada CI-05 del log con `passed:false, exitCode:-1` (evidencia persistida 2026-08-13) — el estado actual ES el RED.
- **Gate:** cumplido — repro, hipótesis y 1 variable controlada documentadas ANTES del step de fix. El "fix" es re-ejecutar el contrato (sin cambio de código: escalera ponytail rung 1 — el harness ya funciona, AUD-027 lo demuestra).

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — No aplica: no toca trust boundaries, input de usuario, auth, datos ni dependencias. Cambio = append de log + docs.
- [x] **PERFORMANCE** — No aplica: no toca hot paths; es un comando python one-shot de validación de artifact.

## Steps

### Step 1: Re-ejecutar contrato CI-05 desde la raíz
- **Archivos:** `.opencode/task-system/enforcement/verify-log.jsonl` (append por harness)
- **Acción:** ejecutar `campaign_verify_cmd` con el comando del contrato CI-05, `taskId="CI-05"`, `expectedExitCode=0` — el harness appendea la entrada con `passed:true`.
- **Verify:** salida del tool = exit 0 / `passed: true`
- **Estado:** ✅ COMPLETED

### Step 2: Verificar log + migrar Backlog → progreso (Trigger 1)
- **Archivos:** `.opencode/task-system/enforcement/verify-log.jsonl`, `docs/Backlog.md`, `docs/progreso/README.md`
- **Acción:** grep `"taskId":"CI-05"` en verify-log → última entrada `passed:true`. Eliminar fila AUD-029 de `docs/Backlog.md` (no tacharla). Agregar entrada `### AUD-029:` en `docs/progreso/README.md` siguiendo formato AUD-025/026/027.
- **Verify:** grep del log + `scripts/validate-docs-coverage.ps1` (si aplica a docs editadas)
- **Estado:** ⬜ PENDING

## Dependencias
- CI-05 (plan 2026-08-12-ci-deuda.md Task 4) — el contrato cuyo log se alinea; ya completado, artifact versionado.

## Review (GATE — agente distinto, P2-01)

- **Revisor:** vanta-review (agente distinto al implementador) — sesión `ses_0012f63efffeTbYSFumds2vgXu`
- **Enfoque:** validar que la re-ejecución desde la raíz es el approach correcto (vs. editar el entry viejo del log o modificar el harness) y que el cierre del hallazgo es legítimo.
- **Cómo se probó:** evidencia mecánica — entry nuevo en verify-log con `passed:true` + comando exit 0 (re-ejecutado por el propio revisor); entrada vieja intacta (append-only); alternativas descartadas con justificación (editar línea 10 = falsifica historial; modificar harness = innecesario, AUD-027 prueba que funciona).
- **Checklist anti-hábitos tóxicos:** verificado por el revisor — sin bloqueantes, sin mejoras requeridas.
- **Veredicto:** ✅ APPROVE (2026-08-14)

## Notas
- WIP ajeno presente en el working tree: `.opencode/skills/campaign-executor/tasks/AUD-024.md` (M, de otra sesión) y entradas AUD-025/026/027 ya presentes en verify-log.jsonl sin commitear (registros de verificación de trabajo ya commiteado). `git add` EXPLÍCITO de archivos propios (lección AUD-024); el verify-log.jsonl se agrega completo porque es el archivo del contrato (sus entries previas corresponden a commits ya en HEAD).
# CI-07 — SHA pinning de acciones de terceros en GitHub Actions

> Plan: `docs/plans/2026-08-12-ci-deuda.md` (Task 6)
> Contrato: "toda acción de terceros pinneada a SHA de 40 hex; actionlint pasa; sin cambio de versión"
> Estado: ⬜ PENDING → ✅ COMPLETO (2026-08-12)
> Branch: develop

## Objetivo

Supply chain hardening (Regla 4): reemplazar TODOS los refs de acciones de **terceros** que usan
tags/branches (`@v4`, `@stable`, `@release-plz-v0.3.160`) por su SHA completo de 40 hex,
verificando que el SHA corresponde al tag/branch ACTUAL de la acción (sin major bump silencioso).
Las acciones internas (`./.github/actions/...`, `./.github/workflows/ci-gate.yml`) NO se pinean.

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** `.github/workflows/release.yml` (53L), `.github/workflows/ci-gate.yml` (59L, sin uses de terceros — no requiere cambio). Inventario completo de `uses:` con `rg` en los 17 workflows (~177 líneas, 25 internas + 129+ terceros).
- **Referencias hacia dentro (workflow → acciones):**
  - Internas (NO pinear): `./.github/actions/rust-setup`, `./.github/workflows/ci-gate.yml` (23 refs).
  - Terceros ya pineados a SHA (NO tocar — contrato "sin cambio de versión"): `actions/setup-python`, `PyO3/maturin-action`, `dtolnay/rust-toolchain@2c7215f...`/`@fa04a145...`, `Swatinem/rust-cache`, `taiki-e/install-action`, `actions/cache`, `actions/upload-artifact`, `actions/download-artifact`, `actions/github-script`, `al-cheb/configure-pagefile-action`, `pypa/gh-action-pypi-publish`, `actions/attest-build-provenance`, `softprops/action-gh-release`, `actions-rust-lang/setup-rust-toolchain`, `jetli/wasm-pack-action`, `github/codeql-action/*` (~80 refs).
  - Terceros con TAG/BRANCH a pinear (4 patrones):
    | Acción | Ref actual | SHA verificado | Origen del SHA |
    |---|---|---|---|
    | `actions/checkout` | `@v4` | `11d5960a326750d5838078e36cf38b85af677262` | API `commits/v4` + `git ls-remote` tag v4 (lightweight) = v4.4.0 |
    | `actions/setup-node` | `@v4` | `49933ea5288caeca8642d1e84afbd3f7d6820020` | API `commits/v4` + `git ls-remote` tag v4 = v4.4.0 |
    | `dtolnay/rust-toolchain` | `@stable` | `4360b52568e2003a75bf9bc1d59f33a8e3fc893c` | API branches/stable (2026-08-05) |
    | `release-plz/action` | `@release-plz-v0.3.160` (**tag muerto**) | `2eb1d8bcb770b4c48ccfaad919734b38b51958c9` | API commits/v0.5.131 (tag ACTUAL, ya usado en 6e68594d) |
- **Referencias entrantes (quién usa los workflows):** GitHub Actions (CI) — los 17 workflows los consume el runtime de GitHub. Nada más los referencia dentro del repo.
- **Veredicto de impacto:** bajo. El cambio es texto YAML en `uses:` que apunta exactamente al mismo commit que hoy resuelve el tag (checkout/setup-node/dtolnay), más 2 refs en `release.yml` que hoy apuntan a un tag MUERTO (`release-plz-v0.3.160` no existe — workflow roto si se dispara). Pinear al tag real `v0.5.131` (mismo que usó el repo en 6e68594d) **repara** el workflow y no es bump silencioso: se documenta en commit + task file. No toca lógica, secrets, triggers, permissions, ni `continue-on-error`.
- **Dirty state ajeno (NO tocar):** `src/sdk/serialization/mod.rs`, `src/sdk/search/mod.rs`, `.opencode/skills/campaign-executor/tasks/P2-7.md`, `docs/architecture/adr/019_sparse_vector_persisted_format.md`. Scope: SOLO `.github/workflows/*.yml` + task file + sync plan Task 6.

## Steps

- [x] **S0. DISCOVERY** — inventario de uses en 17 workflows (177 líneas); resolución de 4 SHAs vía API GitHub + git ls-remote; task file creado con Impacto mapeado (2026-08-12)
- [x] **S1. BATCH core** — `faec5826`: pineados `ci-rust-10.yml` (17), `ci-web-11.yml` (2), `ci-examples-12.yml` (2), `gate-docs-21.yml` (5) → actionlint exit 0
- [x] **S2. BATCH heavy/perf** — `73bbf6e1`: pineados `heavy-certification-50.yml` (11), `heavy-bench-nightly-51.yml` (4), `perf-bench-40.yml` (1) → actionlint exit 0
- [x] **S3. BATCH release** — `97c21d81`: pineados `release.yml` (2 checkout + 2 rust-toolchain@stable + fix release-plz 2 refs muertas → v0.5.131 SHA), `release-wheels-60.yml` (3), `release-npm-61.yml` (6), `release-binaries-63.yml` (2), `release-adapters-62.yml` (2), `release-sbom-64.yml` (1) → actionlint exit 0
- [x] **S4. BATCH security/fuzz/chaos** — `b84e4186`: pineados `sec-codeql-30.yml` (1), `fuzz-40.yml` (3), `chaos-45.yml` (1) → actionlint exit 0
- [x] **S5. CIERRE** — actionlint global exit 0; contrato verificado (0 uses de terceros sin SHA; 143 pineados + 34 internos legítimos); task file + sync plan Task 6 ✅ COMPLETO

## Contrato / Verificación

- `actionlint` exit 0 sobre cada workflow modificado y sobre el directorio completo al cierre.
- `rg -n "uses:" .github/workflows/` — toda línea de terceros termina en SHA de 40 hex (o patrón `@sha # version`); solo `./.github/actions/` y `./.github/workflows/ci-gate.yml` quedan sin SHA.
- Sin cambio de versión: `actions/checkout` sigue siendo v4 (SHA del tag v4 actual), `setup-node` v4, `dtolnay/rust-toolchain` stable (SHA del branch stable actual). Única excepción documentada: `release-plz/action` ref muerta → tag real v0.5.131 (no bump silencioso — el repo ya lo había usado).
- Git: commits convencionales `ci(pinning): CI-07 — ...` por batch, sin tocar dirty state ajeno.

## Resultado

- ✅ COMPLETO — 16 workflows editados (ci-gate.yml sin uses de terceros); 4 patrones pineados (67 refs tag/branch→SHA); actionlint global exit 0; contrato verificado (0 uses de terceros sin SHA restantes). Commits: `faec5826` (core), `73bbf6e1` (heavy/perf), `97c21d81` (release + fix release-plz), `b84e4186` (security/fuzz/chaos).
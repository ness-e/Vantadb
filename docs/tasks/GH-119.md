# GH-119: Vectara → VantaDB migration guide

## Metadata
- **Plan file:** n/a (tarea directa del backlog, no vía plan file)
- **Creado:** 2026-08-04
- **Estado:** ✅ COMPLETED

## Contrato
"Test-Path docs/tutorials/migrate-from-vectara.md → True; grep 'corpus-export' en el archivo → presente; issue #119 cerrado en GitHub (solo si gh disponible y autenticado)"

## Resultado
- ✅ Contrato verificado: `Test-Path` → True, `corpus-export` → 6 matches, issue #119 cerrado vía `gh issue close` (gh autenticado, cuenta ness-e)
- Commit: `ebfb3363` — `docs(GH-119): add Vectara migration guide` (branch develop)

## Blast Radius
- Callers: `docs/tutorials/index.md` (migration track M3)
- Callees: `docs/api/PYTHON_SDK.md` (API usada: `import_file`, `reindex_hnsw_from_text`, `rebuild_index`, `search_memory`, `get_memory`, `list_memory`, `flush`)
- Research base: `docs/audit-reports/vectara-competitive-research-2026-07-27.md` (read-only)
- Format template: `docs/tutorials/03-migrating-from-chromadb.md`

## Pasos
### Step 1: Research
- Leído research existente (Vectara shutdown 2026, gaps de mercado)
- Leído formato de tutoriales existentes (ChromaDB migration como template)
- Verificado schema JSONL de import en `src/sdk/types.rs` (`VantaMemoryExportLine`, `schema_version: 1`) y `src/sdk/serialization/impl_export.rs` / `mod.rs`
- Verificada API Vectara v2 vía websearch: `GET /v2/corpora/:corpus_key/documents` paginado con `page_key`, documentos con `parts[].text`, OAuth2 client credentials

### Step 2: Escribir guía
- `docs/tutorials/migrate-from-vectara.md` — workflow 4 pasos (export → convert → import → verify), script de export Vectara v2, script de conversión, tabla de mapeo de conceptos (corpus→namespace, document→record, query→search_memory, etc.), feature comparison, known limitations, rollback plan

### Step 3: Índice
- `docs/tutorials/index.md` — fila M3 en migration track + línea "read them when..." actualizada

### Step 4: Verify + commit + issue
- Verify contrato ✅ → commit `ebfb3363` → issue #119 cerrado

## Notas
- Boomerang embeddings no son extraíbles → la guía documenta 2 caminos: embed durante conversión (recomendado) o `reindex_hnsw_from_text()` post-import
- Metadata VantaDB es scalar-only → el script de conversión filtra valores no escalares
- No se tocaron archivos fuera de `docs/tutorials/` (regla de la tarea)
- NO se hizo push; NO se actualizó plan file

## Context Save Point
- **Fecha:** 2026-08-04
- **Branch:** develop
- **CI pendiente:** no (docs-only; pre-commit hook pasó)
- **Decisiones:** Verificar API Vectara v2 contra docs oficiales (constraint #0) antes de escribir el script de export. Formato del archivo de import = schema `VantaMemoryExportLine` (schema_version 1) para que `import_file()` lo lea directo.
- **Problemas conocidos:** ninguno
- **Próxima tarea:** n/a

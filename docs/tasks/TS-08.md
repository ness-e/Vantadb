# TS-08: CDN ESM — verificar empíricamente jsDelivr vs esm.sh y documentar §WASM bundle

## Metadata
- **Plan file:** docs/plans/2026-08-25-research-vantadb-ts-quickwins.md
- **Fuente:** Wave 1 · Backlog TS-08 (research INV-vantadb-ts 20260825, hallazgo 🟢)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟢
- **Tipo:** TypeScript SDK + WASM docs (research/verification)
- **Turns estimados:** 3
- **Creado:** 2026-08-26T21:40
- **last-synced:** 2026-08-27T01:44
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps de cierre

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | Consumidores CDN: `https://cdn.jsdelivr.net/npm/vantadb@latest/+esm`, `https://esm.sh/vantadb@latest` ; docs `vantadb-ts/README.md#zero-install-cdn-usage` ; devs que evalúan zero-install sin bundler |
| Callees | `vantadb-wasm/pkg/vantadb_wasm.js` (bundler target glue: `import * as wasm from "./vantadb_wasm_bg.wasm"`), `vantadb-wasm/pkg/vantadb_wasm_bg.wasm` (1.3 MB), `vantadb-wasm/pkg/package.json` (sideEffects), `vantadb-ts/dist/vantadb.js` (import de `vantadb-wasm`), `vantadb-ts/README.md` §WASM bundle, `docs/api/WASM_PERSISTENCE.md` §WebAssembly ESM import |
| Implicaciones | Sin cambio de contrato público — solo docs. Si la tabla CDN miente, devs pierden tiempo. Bundler target no cambia; alternativa esm.sh ya verificada (inlines wasm como base64). No afecta semver/runtime. |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-ts/README.md` (356 líneas), `vantadb-wasm/pkg/vantadb_wasm.js` (9 líneas), `vantadb-wasm/pkg/vantadb_wasm_bg.js` (primeras 30 líneas), `vantadb-wasm/Cargo.toml` (51 líneas), `docs/api/WASM_PERSISTENCE.md` (182 líneas), `docs/api/TS_SDK.md` (558 líneas), `docs/api/WASM_STANDALONE.md` (75 líneas)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `vantadb_wasm.js` importa `"./vantadb_wasm_bg.wasm"` (bundler target) + `"./vantadb_wasm_bg.js"`; `vantadb-ts/README.md` §WASM bundle referencia `vantadb_wasm.js` + `vite-plugin-wasm` + `cdn.jsdelivr.net/+esm` + `esm.sh` + `wasm-pack --target web`; `WASM_PERSISTENCE.md` líneas 115-117 documenta `import * as wasm from "./vantadb_wasm_bg.wasm"` como bundler/web target constraint
- **Archivos que referencian a los editados (referencias entrantes):** grep `vantadb_wasm_bg.wasm` → solo `vantadb-wasm/pkg/vantadb_wasm.js:2`; grep `cdn.jsdelivr` → `vantadb-ts/README.md:102` + plan file + review archive; grep `esm.sh` → mismo README; grep `WASM bundle` → `vantadb-ts/README.md:80-132` + `desktop/README.md` stub; `docs/api/README.md` no existe (canónico es `docs/api/TS_SDK.md` + `WASM_PERSISTENCE.md`), verificado con `Test-Path` → False
- **Veredicto impacto:** bajo — task es verificación documental empírica. README ya contiene tabla verificada 2026-08-26 (jsDelivr ❌ vs esm.sh ✅) con Rollup failure reason y alternativa mínima. Riesgo es stale claim: si CDN cambia, docs desactualiza. Verificación webfetch mecánico confirma claim vigente 2026-08-27. Un hardening menor en `WASM_PERSISTENCE.md` (mencionar jsDelivr explícitamente) satisface contrato "docs cubre jsDelivr failure reason" sin tocar código.

## Contrato
"README §WASM bundle documenta empíricamente si cdn.jsdelivr.net/npm/vantadb@latest/+esm funciona (con evidencia Rollup error) y muestra alternativa mínima (esm.sh o importmap+fetch/demo); docs cubre jsDelivr failure reason (import del .wasm binario como ES module)"

## Spec (SDD — obligatoria si feature-add)

No aplica — docs/research verification, no agrega símbolos públicos nuevos. Señales Phase 1b: ninguna (`pub fn`/tool/endpoint/binding nuevo). Es research que valida documentación existente; si se requiere hardening docs es ≤5 líneas en `WASM_PERSISTENCE.md`. Criterio question-gates.md: no dispara Gate D spec-first.

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** `vantadb_wasm.js` bundler target debe seguir importando `".wasm"` como ES module (evidencia de failure reason); `vantadb-ts/README.md` §Zero-install CDN debe mantener tabla jsDelivr ❌ (Rollup: failed to resolve internal import) vs esm.sh ✅ (base64 inlined wasm, no sidecar fetch); alternativa mínima debe ser copiable (esm.sh import ejemplo) o fetch/demo; WASM binary size ~1.3 MB no debe documentarse con número inventado
- **Comandos de verificación:** `Get-Content vantadb-wasm/pkg/vantadb_wasm.js -Raw` contiene `import * as wasm from "./vantadb_wasm_bg.wasm"`; `curl -s https://cdn.jsdelivr.net/npm/vantadb-wasm@0.5.0/+esm` devuelve `Failed to bundle using Rollup: failed to resolve an internal import`; `curl -s https://esm.sh/vantadb@latest` devuelve `export * from "/vantadb@0.5.0/es2022/vantadb.mjs"` (ok); `curl -s https://esm.sh/vantadb-wasm@0.5.0/es2022/vantadb-wasm.mjs | grep -q "base64"` verifica inlined wasm; `Select-String -Path vantadb-ts/README.md -Pattern "Zero-install CDN"` + `Select-String -Path docs/api/WASM_PERSISTENCE.md -Pattern "jsDelivr"`; `cd vantadb-ts && npm run build` y `scripts/validate-docs-coverage.ps1` (si aplica)
- **Deuda pendiente:** ninguna — hardening docs opcional ≤5 líneas; sin deuda técnica nueva por PR

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | TS-08 — CDN ESM verificar empíricamente jsDelivr vs esm.sh y documentar §WASM bundle |
| `lastAction` | Steps 1-3 ✅ — glue verificado, CDN empírico via curl, docs coverage validado + hardening WASM_PERSISTENCE |
| `result` | OK |
| `nextAction` | Lead: verify full mecánico (fmt/clippy/nextest/docs-coverage) y commit; actualizar plan file recitation |
| `contract` | Contrato arriba + Invariantes de dominio |
| `nextTask` | TS-06 (Wave 1 task 3 — Gate CI para tests TS) |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Cero o negativo — task de verificación/docs no introduce deuda. Si se añade hardening de 3-4 líneas en `WASM_PERSISTENCE.md` (mencionar jsDelivr failure), es -deuda de claridad (evita que próximo dev re-intente jsDelivr). No hay unsafe/clone/hot path.

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | 4 condiciones ✅: (1) `vantadb_wasm.js` contiene `import * as wasm from "./vantadb_wasm_bg.wasm"` (bundler target evidencia), (2) `curl cdn.jsdelivr/npm/vantadb-wasm@0.5.0/+esm` devuelve Rollup failure stub, (3) `vantadb-ts/README.md` §Zero-install CDN tabla jsDelivr ❌ + Rollup quoted error + esm.sh ✅ con snippet copiable, (4) `docs/api/WASM_PERSISTENCE.md` cubre jsDelivr failure reason (import .wasm como ES module) — verificado via grep |
| **Commit** | Commit atómico `docs(ts): TS-08 verify CDN ESM jsDelivr vs esm.sh` con task file + evidencia + hardening docs si aplica, diff limpio (solo docs/README), verify mecánico previo |
| **Release** | No aplica publish — docs-only, semver patch en próximo release si se bump; esm.sh alternative no afecta versionado |

## Herramientas necesarias
- terminal (curl, grep, Select-String)
- codegraph_explore (blast radius — leer glue)
- campaign_verify_cmd (verificación mecánica)
- webfetch/curl (empirical CDN fetch)

**Skills cargadas (SDP):**
- `documentation-and-adrs` — base solicitada (documentar CDN result + failure reason en README/docs)
- `source-driven-development` — base solicitada (verificar wasm-bindgen bundler target vs web target contra docs oficiales)
- `browser-testing-with-devtools` — SDP Lifecycle VERIFY + keyword `cdn`/`esm`/`browser`: alternativa empírica requiere entender browser module loading (fetch vs ES import del .wasm); justificada: valida que esm.sh inlines wasm para browser ESM
- `git-workflow-and-versioning` — SDP Lifecycle SHIP + keyword `cdn`/`esm`: versionado de CDN URLs (`@latest` vs `@0.5.0`) y contrato de docs como API pública
- `code-review-and-quality` — SDP Lifecycle REVIEW: pre-commit gate para docs (evitar claim inventado, verificar URLs resuelven)
- SDP: lifecycle BUILD (read glue) + VERIFY (curl empírico) + REVIEW (docs quality) + SHIP (docs) mapeados; grep SKILLS-MANIFEST.md por keywords `cdn`, `esm`, `wasm`, `documentation`, `browser`, `bundle` → candidatos relevantes: las 5 arriba; `playwright-cli` descartado (CLI browser no necesario, curl +mdn suficiente); `shipping-and-launch` descartado (no deploy); ≤8 total

## Investigation Notes
- Discovery liviano (🟢): glue y CDN verificables localmente + curl, no requiere research pesada de wasm-bindgen beyond guide
- Evidencia mecánica 2026-08-27 (confianza alta):
  - `vantadb-wasm/pkg/vantadb_wasm.js:2` → `import * as wasm from "./vantadb_wasm_bg.wasm"` ✅ (bundler target — binary como ES module). Confirmado con `Get-Content -Raw` → 246 bytes. `vantadb-wasm/Cargo.toml` no especifica target; default wasm-pack es `bundler` (verified: glue JS size 246 bytes vs `web` target sería fetch-based). `docs/api/WASM_PERSISTENCE.md:115-116` ya documenta ambos targets emiten `import * as wasm ...`
  - `curl https://cdn.jsdelivr.net/npm/vantadb@latest/+esm` → 200 OK, 8300 bytes, header `Link: </npm/vantadb-wasm@0.5.0/+esm>` (sub-import), body `import{VantaDB}from"/npm/vantadb-wasm@0.5.0/+esm"` pero bundled SDK ok. La falla REAL está en el sub-import: `curl https://cdn.jsdelivr.net/npm/vantadb-wasm@0.5.0/+esm` → 200 con stub: `Failed to bundle using Rollup v4.62.2 and esbuild v0.28.1: failed to resolve an internal import.` ✅ — coincide exactamente con README line 102 explicación.
  - `curl https://esm.sh/vantadb@latest` → 200 `export * from "/vantadb@0.5.0/es2022/vantadb.mjs"` ✅ ; `curl https://esm.sh/vantadb@0.5.0/es2022/vantadb.mjs` → 128KB, import-able. `curl https://esm.sh/vantadb-wasm@0.5.0/es2022/vantadb-wasm.mjs` → 1.8 MB con blob base64 de ~1.3 MB + `WebAssembly.instantiate` path (inlined wasm, no sidecar fetch). Verified: file contains `charCodeAt` + `TextEncoder` + huge base64 string `zLssCU7r8...` (wasm binary). Confianza: alta — esm.sh build inlines .wasm como byte array, por eso browser funciona sin fetch separado.
  - `vantadb-ts/README.md:98-114` → tabla `cdn.jsdelivr.net/npm/vantadb@latest/+esm | ❌ No | ... Rollup cannot resolve it ... "Failed to bundle using Rollup: failed to resolve an internal import"` ✅ (cita error verbatim) + `esm.sh/vantadb@latest | ✅ Yes | ... inlines the .wasm binary as a base64 byte array` + snippet html `import { VantaDB } from "https://esm.sh/vantadb"` verified copiable ✅ + fallback `wasm-pack build --target web` con `fetch()` + `WebAssembly.instantiateStreaming` ✅
  - `docs/api/WASM_PERSISTENCE.md:113-133` → §WebAssembly ES module import ya documenta `import * as wasm from "./vantadb_wasm_bg.wasm"` y que stable Chrome/Edge no soporta WASM como ES module (verified 2026-08-19, cita MDN ESM integration). Gap: no menciona jsDelivr +esm failure explícitamente — se hardenará con 3 líneas. `docs/api/README.md` no existe (Test-Path False) — el contrato se satisface via `vantadb-ts/README.md` + `WASM_PERSISTENCE.md`.
- Web research: curl empírico + MDN ESM integration + wasm-bindgen guide (js-snippets caveats, bundler vs web targets) son fuentes canónicas. Si lead requiere cita webfetch, usar `https://wasm-bindgen.github.io/wasm-bindgen/reference/js-snippets.html` y MDN ESM integration `https://developer.mozilla.org/en-US/docs/WebAssembly/Guides/JavaScript_interface/ESM_integration`.
- Posible duda: ¿importmap+fetch/demo playground requerido? README ya ofrece alternativa mínima esm.sh (corta, copiable) + self-host `wasm-pack build --target web` (fetch path). Importmap no es necesario cuando esm.sh resuelve zero-install; ofrecer importmap sería redundante. Decisión documentada: esm.sh es alternativa mínima suficiente (verificado funciona); importmap+fetch es alternativa self-host, ya cubierta con `wasm-pack web`.
- Verificación pendiente gate citas (TSYS-13): URLs `https://cdn.jsdelivr.net/npm/vantadb@latest/+esm`, `https://esm.sh/vantadb@latest`, wasm-bindgen guide, MDN ESM — todas resuelven 200 (curl verificó cdn/esm.sh; MDN/wasm-bindgen guide se verifica via webfetch si red disponible).

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — glue, CDN empírico, docs coverage cerrados |
| Pendientes de ejecución (downhill) | 0 — 3 steps completos |
| % completado | 100% (3/3 steps) |

## Fase 1 — Evidencia de Debugging (GATE — solo tipo Bug)

No aplica — no es bug, es research/verification. Repro determinístico: `curl` para CDN + `Get-Content` para glue (ya ejecutado, evidencia alta). Hipótesis: jsDelivr Rollup no resuelve `.wasm` ES import → confirmado por stub error.

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — evaluado: no toca trust boundaries (no input usuario, no auth, no storage, no FFI nuevo); CDN es third-party transform pero docs solo referencia URL, no ejecuta código; `cargo audit` N/A (no Cargo.toml change); checklist `security-and-hardening` no dispara (no input handling)
- [x] **PERFORMANCE** — evaluado: no toca hot paths (HNSW, engine.rs, search/ingestión, serialización); WASM binary size ~1.3 MB ya documentado, no cambia; esm.sh base64 overhead (~33%) es tradeoff conocido, documentado como inlined bytes; no requiere bench canonical_p99

## Steps

### Step 1: Verificar wasm-bindgen glue (bundler target) y evidenciar import .wasm
- **Archivos:** `vantadb-wasm/pkg/vantadb_wasm.js`, `vantadb-wasm/Cargo.toml`, `docs/api/WASM_PERSISTENCE.md:113-133`
- **Acción:** leer glue completo (`Get-Content -Raw`), confirmar línea 2 `import * as wasm from "./vantadb_wasm_bg.wasm"`; confirmar `package.json` type module y `files` incluye `.wasm`; mapear que este import es la causa del failure Rollup (binary como ES module). Si gap en docs, anotar hardening pendiente.
- **Verify:** `campaign_verify_cmd command="powershell -Command \"Get-Content vantadb-wasm/pkg/vantadb_wasm.js -Raw | Select-String -Pattern 'import \* as wasm from'\""` ✅ ; grep WASM_PERSISTENCE para `import \* as wasm` ✅
- **Estado:** ✅ COMPLETED (2026-08-27 — glue 246 bytes, línea 2 exacta, bundler target)

### Step 2: Verificar empíricamente CDN behavior (jsDelivr Rollup error vs esm.sh inlined wasm)
- **Archivos:** `vantadb-ts/README.md:98-114` (tabla CDN), evidencia curl tmp files
- **Acción:** curl jsDelivr `vantadb-wasm@0.5.0/+esm` → assert contiene `Failed to bundle using Rollup`; curl esm.sh `vantadb@latest` → contiene re-export; curl esm.sh `vantadb-wasm.mjs` → contiene base64 blob + `charCodeAt` indicio de inlining; validar README snippet `https://esm.sh/vantadb` es copiable y coincide con esm.sh response.
- **Verify:** `campaign_verify_cmd command="curl.exe -s https://cdn.jsdelivr.net/npm/vantadb-wasm@0.5.0/+esm | Select-String -Pattern 'Failed to bundle'"` ✅ ; `curl.exe -s https://esm.sh/vantadb@latest | Select-String -Pattern "export"` ✅ ; `curl.exe -s https://esm.sh/vantadb-wasm@0.5.0/es2022/vantadb-wasm.mjs | Select-String -Pattern "base64|charCodeAt"` ✅
- **Estado:** ✅ COMPLETED (2026-08-27 — jsDelivr ❌ stub verificado, esm.sh ✅ inlined verificado)

### Step 3: Validar docs coverage y harden WASM_PERSISTENCE para "docs cubre jsDelivr failure reason"
- **Archivos:** `vantadb-ts/README.md:98-120` (Zero-install CDN), `docs/api/WASM_PERSISTENCE.md:113-133` (WebAssembly ES module import), `docs/api/TS_SDK.md` (Runtimes references)
- **Acción:** grep README para `Zero-install CDN`, `cdn.jsdelivr`, `esm.sh`, `Failed to bundle` todos con match; grep WASM_PERSISTENCE para `import \* as wasm` (presente) y si falta `jsDelivr`/`Rollup`/`cdn.jsdelivr` → añadir 3 líneas: "jsDelivr's `+esm` Rollup pipeline fails for the same reason: it cannot resolve the `.wasm` ES import in the bundler glue and serves a stub that throws 'failed to resolve an internal import' — use esm.sh (inlines wasm as base64) or rebuild with `--target web` (fetch)." Verificar docs/api/README.md no existe pero TS_SDK.md ya linka a WASM_PERSISTENCE indirectamente via Runtimes table (no requiere cambio). Si hardening aplicado, re-verificar con grep y `cargo fmt --check`.
- **Verify:** `Get-Content vantadb-wasm/pkg/vantadb_wasm.js -Raw | Select-String -Pattern 'import \* as wasm from'` ✅ (línea 2 exacta, bundler target); `curl https://cdn.jsdelivr.net/npm/vantadb-wasm@0.5.0/+esm | Select-String 'Failed to bundle'` ✅ (Rollup stub, 2 matches, verified 2026-08-27); `curl https://esm.sh/vantadb@latest` ✅ + esm.sh wasm mjs 1.57 MB (inlined base64) ✅; `Select-String -Path vantadb-ts/README.md -Pattern 'Zero-install CDN'` ✅ (1 match, line 98); `Select-String -Path docs/api/WASM_PERSISTENCE.md -Pattern 'jsDelivr'` tras hardening ✅ (línea 129, CDN note added 4 lines); `cargo fmt --check` ✅ exit 0; `cargo clippy -p vantadb --all-targets -- -D warnings` ✅ (Finished dev, 1m17s); `npm run build` ✅ en vantadb-ts (tsc ok)
- **Estado:** ✅ COMPLETED (2026-08-27T01:44 — hardening 4 líneas en WASM_PERSISTENCE.md + verify full mecánico)

## Dependencias
- TS-02, TS-05 (Wave 0 — ✅ COMPLETED, ya commiteados en develop, no bloquean TS-08)
- TS-08 es Wave 1 independiente de TS-06 (Gate CI) — no comparten workflow npm (TS-08 es docs-only)
- WASM_PERSISTENCE.md hardening no depende de wasm-pack rebuild (solo docs)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. Sin esto registrado, la tarea no está COMPLETED.

- **Revisor:** vanta-review (o vanta-docs si review no disponible — docs task)
- **Enfoque:** ¿tabla CDN es empíricamente exacta (jsDelivr error verbatim, esm.sh base64 evidence)? ¿failure reason (import .wasm como ES module) trazado correctamente a bundler target glue? ¿alternativa mínima es वास्तवmente mínima (esm.sh snippet 3 líneas vs importmap+fetch)? ¿docs cubre failure reason más allá de README (WASM_PERSISTENCE)? ¿números de tamaño (~1.3 MB) con fuente correcta (pkg/vantadb_wasm_bg.wasm Length 1364555)?
- **Cómo se probó:** evidencia mecánica: Get-Content glue + curl jsDelivr stub + curl esm.sh redirect + esm.sh wasm mjs base64 + README grep + WASM_PERSISTENCE grep
- **Checklist anti-hábitos tóxicos:**
  - [ ] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [ ] No saltarse la clarificación por "ya sé qué quiere".
  - [ ] No declarar done sin verificar contra los acceptance criteria.
  - [ ] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [ ] No hacer un solo intento de búsqueda y darlo por saturado.
  - [ ] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [ ] No reintentar en bucle sin diagnóstico.
  - [ ] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [ ] No degradar el chequeo de errores en paths de dinero/seguridad.
  - [ ] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ⬜ pendiente (revisor distinto debe aprobar antes de COMPLETED) — lead debe hacer verify mecánico antes de commit

## Notas
- `docs/api/README.md` listado en Archivos clave no existe (Test-Path False); canónico es `docs/api/TS_SDK.md` + `WASM_PERSISTENCE.md`. Se valida grep contra esos dos; no crear docs/api/README.md redundante (ponytail: no duplicar docs).
- ponytail: hardening docs es 3 líneas en WASM_PERSISTENCE (techo: si jsDelivr cambia pipeline, docs quedará stale — manterner fecha verified 2026-08-27). No añadir importmap playground si esm.sh ya es suficiente para zero-install (YAGNI).
- Tamaño WASM: `vantadb_wasm_bg.wasm` 1,364,555 bytes (~1.3 MB) — README dice ~1.3 MB correcto (no inventar).
- Rollup version citada en README: Rollup v4.62.2 + esbuild v0.28.1 (verificado en curl header comment del jsDelivr bundle `Bundled by jsDelivr using Rollup v4.62.2 and esbuild v0.28.1`). esm.sh inlines wasm: evidencia es blob base64 + `charCodeAt` + `WebAssembly` instantiation, no sidecar fetch.

## Context Save Point
- **Fecha:** 2026-08-27T01:40 UTC
- **Branch:** develop (worktree con TS-02.md+TS-05.md completados, TS-08.md nuevo)
- **CI pendiente:** no — docs-only, pero lead debe correr `scripts/validate-docs-coverage.ps1` + `cargo fmt --check` antes de commit
- **Decisiones:** glue es bundler target con `import * as wasm from .wasm` (cause Rollup fail); jsDelivr ❌ verified stub, esm.sh ✅ verified inlined base64; alternativa mínima es esm.sh (3 líneas) — importmap+fetch es fallback self-host ya documentado (`wasm-pack --target web`); docs coverage ya completo en README, hardening menor en WASM_PERSISTENCE para cubrir "docs" estricto; sin nueva deuda
- **Problemas conocidos:** ninguno — contrato ya verificado mecánicamente (3/3 conditions hasta hardening)
- **Próxima tarea:** TS-06 — Gate CI para tests TS (job Fast Gate <5min)

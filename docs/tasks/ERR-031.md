# ERR-031 — 🔵 `VecIndex::add` traga rechazos (solo warn, sin Result)

- Estado: ✅ COMPLETO
- Fecha: 2026-08-12
- Rama: develop

## Contexto

`VecIndex::add` originalmente retornaba `()` y solo hacía `warn!` ante rechazos
(non-full vector under Scann/DiskAnn, read-only IVF, zero-norm under cosine).
Un futuro `Arc<dyn VecIndex>` perdería inserts silenciosamente.

## Work previo (de sesiones previas, verificado en esta sesión)

- `339107b0` — fix(ERR-031): trait `add` → `Result<()>` + las 5 impls
  (Flat infallible → Ok, Scann/DiskAnn non-full → Err ValidationError,
  IVF read-only → Err en vez de panic, CPIndex propaga error de add).
- `918e57b1` — chore: fix clippy -D warnings en tests de index (colateral):
  `let _ = idx.add(...)` en flat/scann/diskann (~20 call sites).
- `3b0000cc` — test(python): non-zero vectors (COV-001, colateral).

## Gap identificado en esta sesión

No existía test que verificara los paths de rechazo de DiskAnn/Scann/IVF.
Solo existía `test_add_zero_norm_vector_rejected` (graph.rs) para CPIndex.
El contrato exige "rechazo de VecIndex::add se propaga como Err (no silencioso)".

## Steps

- [x] S1. DISCOVERY: verificar git history (339107b0, 918e57b1) + estado actual
- [x] S2. Blast radius: callers de `add` en src/index (core.rs, graph.rs, tests) — todos manejan Result
- [x] S3. Implementación: tests de rechazo en diskann.rs y scann.rs (non-full → Err, no inserta)
- [x] S4. Verify full: fmt + clippy + nextest workspace
- [x] S5. Cierre: commit + skill progreso + task state completed

## Impacto mapeado (Regla 0)

- Archivos leídos completos: src/index/search.rs (vía codegraph), mod.rs (trait),
  diskann.rs, scann.rs, flat.rs, ivf.rs, core.rs
- Referencias entrantes a `VecIndex::add`: core.rs (tests), search.rs, graph.rs,
  ivf.rs, flat.rs, scann.rs, diskann.rs — todos manejan Result (let _ / expect)
- Referencias salientes: ninguna — solo se agregan tests en módulos `#[cfg(test)]`
- Veredicto: bajo impacto — cambios limitados a tests

## Contrato

- Verificación: `cargo nextest run --profile audit --workspace --build-jobs 2` ✅
- Verificación: `cargo fmt --check` ✅
- Verificación: `cargo clippy --workspace --all-targets --all-features -- -D warnings` ✅
- Comportamiento: rechazo de VecIndex::add se propaga como Err ✅ (tests nuevos + zero-norm existente)

## Evidencia

- commit 339107b0 (fix principal), 918e57b1 (colateral clippy)
- tests nuevos: `test_diskann_rejects_non_full_vector`, `test_scann_rejects_non_full_vector`
- `test_add_zero_norm_vector_rejected` (graph.rs:1409) ya verifica rechazo CPIndex
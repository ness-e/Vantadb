# VS-09: Command palette (Ctrl+K)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase0.md`
- **Creado:** 2026-08-18
- **last-synced:** 2026-08-18
- **Estado:** ✅ DONE
- **Tipo:** frontend (React/TSX, Vite, Tailwind v4 — tokens/utilities VS-01) + dep nueva `cmdk`

## Impacto mapeado (Regla 0)
- **Archivos a crear (1):** `desktop/src/components/palette/CommandPalette.tsx`
- **Archivos a modificar (2):** `desktop/package.json` + `desktop/package-lock.json` (dep `cmdk@^1.1.1` — npm install preservó `react-querybuilder` de VS-07), `desktop/src/components/layout/WorkspaceShell.tsx` (aditivo mínimo: `export type Surface`, estado `paletteOpen`, Ctrl+K global toggle palette, `runSearch(q)` extraído de `handleSearch`, `handlePaletteSearch`, render `<CommandPalette>` lazy con Suspense, placeholder de búsqueda sin "(Ctrl+K)")
- **Referencias hacia dentro (quién los importa):** `CommandPalette` → solo `WorkspaceShell` (lazy import). `PaletteSurface`/`NamespaceOption` exportados por si otra superficie los consume.
- **Referencias hacia fuera:** `CommandPalette` importa `Command`/`useCommandState` de `cmdk` (v1.1.1, README oficial) y `list`/`vantaErrorMessage` de `../../vanta` (export JSONL). `WorkspaceShell` ya conecta `handleUndo`/`handleDelete` reales de VS-08 (`undoStore` en `desktop/src/store/undo.ts` — creado por VS-08 EN PARALELO, consumido, no reescrito).
- **Fuente protegida (SOLO LECTURA):** `desktop/src/components/inspector/`, `home/`, `search/` (VS-06/07), `desktop/src/store/undo.ts` (VS-08), `desktop/src-tauri/`, `src/`, `web/`.
- **Archivos que NO toco (dominio ajeno):** `desktop/src/store/undo.ts` — VS-08 lo creó en paralelo; la palette lo consume vía props del shell (`onUndo`/`onDelete`), sin import directo. `desktop/src/components/search/` — VS-07 (FiltrosBuilder) estaba en vuelo; no lo toqué.
- **Veredicto:** aditivo puro en `desktop/src/` + 1 dep. El Ctrl+K del Topbar dejó de enfocar la búsqueda y ahora abre la palette (VS-03 lo anunciaba como stopgap); el placeholder perdió "(Ctrl+K)" para no mentir. La búsqueda global sigue en su input (Alt+B / ítem "Buscar key…" la enfocan).

## Blast Radius
| Callers | Callees | Implicaciones |
|---|---|---|
| WorkspaceShell (único, lazy) | CommandPalette → cmdk (Command/Command.Dialog/useCommandState) + list()/vantaErrorMessage | La palette es chunk lazy (56 kB / 19 kB gzip): el shell inicial no paga cmdk. Las acciones navegan superficies ya existentes; undo/delete delegan a los handlers reales de VS-08. |

## Contrato
- `npm run build` en `desktop/` verde (tsc strict + vite) — verificado 3×.
- Acciones: abrir namespace (→ MEMORIAS, mismo comportamiento que el sidebar), buscar key (→ MEMORIAS + búsqueda global; fallback cuando ningún comando matchea), exportar (.jsonl desde `list({limit:500})` — VS-CORE-04 trae export filtrado en Fase 2), borrar/undo (hooks → handlers reales de VS-08), toggle tema, abrir lentes (ACTIVITY/ÍNDICES/IQL — IQL Fase 2, lente placeholder).
- Disparo global Ctrl+K (toggle), atajos visibles en cada ítem de Acciones (Alt+B buscar, Alt+E exportar, Alt+D borrar, Ctrl+Z undo, Alt+T tema; los Alt se disparan in-palette).
- IQL NO incluido como acción de búsqueda (el bridge no expone `vanta_query` en Fase 0) — solo navegación a la superficie placeholder.
- Estética tokens/utilities VS-01 (borde 4px, sombra 6px, neon, fonts Anton/Space Mono, dark automático vía vars).

## Herramientas
- `npm install cmdk` en `desktop/` (instaló `cmdk@^1.1.1`)
- `npm run build` en `desktop/` (verificación mecánica por step; 3× final)
- Verificación de API contra README oficial de cmdk (`https://raw.githubusercontent.com/pacocoursey/cmdk/main/README.md`) + inspección del bundle (`node_modules/cmdk/dist/index.d.ts`): `Command.Dialog` acepta `overlayClassName`/`contentClassName`/`label`; `Command.Item` usa `data-selected`; `useCommandState` para el fallback de búsqueda.

## Steps
### Step 1: Instalar cmdk + verificar API
- **Archivos:** `desktop/package.json` (+`cmdk@^1.1.1`), `desktop/package-lock.json`
- **Acción:** `npm install cmdk` — preserva `react-querybuilder` (VS-07, sin commitear). API verificada contra README oficial + types del bundle.
- **Verify:** `node -e "require('./package.json').dependencies.cmdk"` → `^1.1.1`
- **Estado:** ✅ DONE

### Step 2: CommandPalette.tsx (nuevo)
- **Archivos:** `desktop/src/components/palette/CommandPalette.tsx`
- **Acción:** export default + `PaletteSurface`/`NamespaceOption`. `Command.Dialog` controlado (`open`/`onOpenChange`); grupos Navegación (RESUMEN/MEMORIAS), Lentes (ACTIVITY/ÍNDICES/IQL), Abrir namespace (dinámico desde `namespaces` del shell, → MEMORIAS como el sidebar), Acciones (Buscar key…, Exportar .jsonl, Borrar, Deshacer, Tema). Fallback en `Command.Empty` vía `useCommandState`: "Buscar key '{search}'…". Atajos Alt+E/T/B/D/U in-palette (listener keydown con guard `e.altKey`). CSS en `<style>` local con selectores `[cmdk-*]` + vars `--color-*`/`--font-*` de VS-01 (los portales de Radix Dialog escapan al body → selectores globales, única instancia).
- **Verify:** `npx tsc --noEmit` (Step 4)
- **Estado:** ✅ DONE

### Step 3: WorkspaceShell — registro + disparo global (aditivo mínimo)
- **Archivos:** `desktop/src/components/layout/WorkspaceShell.tsx`
- **Acción:** `export type Surface` (VS-08 la extendió con "papelera" en paralelo — respetada). Ctrl+K global → `setPaletteOpen(o => !o)` (reemplaza el foco a búsqueda de VS-03; el kbd del Topbar sigue anunciando el atajo). `runSearch(q)` extraído de `handleSearch` (conserva top_k 50/8 de VS-07); `handlePaletteSearch(q)` → MEMORIAS + query o foco en búsqueda global. `handleUndo`/`handleDelete` REALES de VS-08 (llegaron en paralelo mientras trabajaba — eliminé mis stubs para no duplicar). Render `<CommandPalette>` lazy al final del root con Suspense fallback null. Placeholder búsqueda sin "(Ctrl+K)".
- **Nota de conflicto en vivo:** durante la implementación, VS-07 (filters) y VS-08 (undo/trash) editaron ESTE MISMO archivo en paralelo; el build falló 2× por su WIP (exports de `FiltersBuilder` inexistentes, imports `TrashLens`/`undoStore` sin uso). No toqué sus archivos; al aterrizar su WIP (`search/filters-core.ts`, `store/undo.ts`, render PAPELERA), tsc+build volvieron a verde. Mi única corrección fue eliminar mis stubs duplicados de `handleUndo`/`handleDelete` cuando el store real apareció.
- **Verify:** `npm run build` ×3
- **Estado:** ✅ DONE — 3× verde (tsc exit 0; chunks: CommandPalette 56.36 kB/gzip 19.01 kB lazy; warning chunk >500 kB pre-existente de Inspector/CodeMirror VS-06).

### Step 4: Cierre — task file + plan + progreso
- **Acción:** crear `VS-09.md` (este archivo), actualizar plan file VS-09 → ✅ HECHO. Sin commit (worktree compartido VS-07/VS-08 en vuelo — el lead commitea). Memoria decisions/lessons.
- **Verify:** plan file fila VS-09 ✅
- **Estado:** ✅ DONE

## Dependencias
- VS-01 (tokens/utilities) — ✅ usados (borde 4px, sombra 6px, neon, font-display/tech, dark automático vía vars).
- VS-03 (WorkspaceShell) — ✅ base; Ctrl+K del Topbar migrado de "foco búsqueda" a "abre palette".
- VS-08 (`store/undo.ts` + handlers reales) — ✅ consumido en paralelo: la palette expone `onUndo`/`onDelete` y el shell pasa las acciones reales del store.
- VS-07 (filters) — coexistencia: `runSearch` conserva `top_k: filterActive ? 50 : 8`.
- VS-CORE-04 (export filtrado) — no bloquea; el export Fase 0 es JSONL client-side desde `list()`.

## Notas
- **cmdk v1.1.1** (verificado en README oficial): `Command.Dialog` compone Radix Dialog — `open`/`onOpenChange`, `overlayClassName`, `contentClassName`, `label` (aria). El FAQ dice "Listen for ⌘K automatically? No, do it yourself" → el listener vive en WorkspaceShell.
- **IQL:** el ítem navega a la superficie placeholder Fase 2; NO hay acción de query IQL (bridge sin `vanta_query` en Fase 0) — contrato respetado.
- **Abrir namespace:** no existe estado de "namespace activo" en la app (VS-03 sidebar tampoco lo tiene) → el ítem navega a MEMORIAS, idéntico al sidebar. Real namespace-filter llega con VS-05/VS-04.
- **Borrar:** `handleDelete` de VS-08 requiere un registro seleccionado en el Inspector (`undoStore.softDelete(record)`); sin selección muestra notice. Correcto para Fase 0 — el batch delete con confirmación es Fase 2 (VS-CORE-05).
- **Atajos Alt in-palette:** funcionan solo mientras la palette está montada (abierta); Alt+letra no inserta texto en el input → sin conflicto con typing. Ctrl+Z global es de VS-08 (guarda inputs/CodeMirror).
- **Ponytail:** el `<style>` local con selectores `[cmdk-*]` evita tocar `index.css` (dominio VS-01) y pelear con los estilos inline de Radix; los portales son globales pero la app tiene UNA palette. Export JSONL reutiliza `list()` existente — cero lógica nueva de bridge.
- **Coexistencia de tareas:** `desktop/package.json` tenía `react-querybuilder` (VS-07) sin commitear — `npm install cmdk` lo preservó. NO commitear (el lead commitea el worktree compartido).

## Context Save Point
- **Fecha:** 2026-08-18
- **Branch:** develop
- **CI pendiente:** no (frontend-only; verify full Rust no aplica — no se tocó `src/`)
- **Decisiones:** Ctrl+K abre palette (VS-03 lo anunciaba como stopgap); undo/delete por props (no import directo al store de VS-08 — evita acoplar el orden de merge); export Fase 0 = JSONL client-side (VS-CORE-04 lo formaliza en Fase 2); atajos Alt in-palette + Ctrl+Z global (VS-08).
- **Problemas conocidos:** trabajo en vivo compartido de VS-07/VS-08 sobre WorkspaceShell (2 builds fallaron por su WIP; resuelto al aterrizar); warning chunk >500 kB de Inspector (VS-06, pre-existente); el store de VS-08 aún puede cambiar forma (si `undoStore.undo()` cambia firma, se ajusta en el shell, la palette no cambia).
- **Próxima tarea:** VS-04 (HOME/overview) — queda como única tarea Fase 0 pendiente, o el cierre de VS-07/VS-08 (sus task files + merge del worktree).
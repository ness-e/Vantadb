# INV-011: Core-Server Separation — auditoría

## Metadata
- **Fuente:** docs/Backlog.md línea 201
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟡
- **Tipo:** Mixto (rust, server, packaging)
- **Turns estimados:** 5-10
- **Creado:** 2026-08-03
- **Estado:** ⬜ PENDING

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | Cargo.toml (features), vantadb-server/Cargo.toml, src/lib.rs (module gates) |
| Callees | axum, tokio, tower_governor, tower-http (solo con feature `server`) |
| Implicaciones | Sin implementación — auditoría. No rompe contratos. |

## Contrato
"Reporte de auditoría documentado en Backlog + verificación: `cargo tree --no-default-features -F cli -e features` NO lista axum/tokio/tower_governor; `cargo check --no-default-features -F cli` compila sin server deps"

## Herramientas necesarias
- cargo-mcp (tree, check)
- grep / read (verificación estática de gates)

## Investigation Notes
- `src/cli_server.rs` → gated por `#[cfg(feature = "server")]` en lib.rs:72-73 ✅
- `src/cli_handlers/server.rs` → dentro de módulo `cli`-gated, con cfg parcial `server` en líneas 188/207 (a verificar)
- Default features (`cli,arrow,fjall,roaring,advanced-tokenizer,memmap2,fs2,sysinfo`) NO incluyen server deps ✅
- `opentelemetry` feature arrastra `opentelemetry_sdk/rt-tokio` (tokio runtime, no axum)
- Feature `server = ["cli", "dep:tokio", "dep:axum", "dep:tower_governor", "dep:tower-http"]` — **acopla server→cli** (pregunta: ¿puede server existir sin cli?)
- `vantadb-server/Cargo.toml` activa `features = ["cli", "server"]` — `cli` redundante porque server ya lo incluye

## Steps

### Step 1: Verificar gates estáticos de módulos server-only
- **Archivos:** `src/lib.rs`, `src/cli_server.rs`, `src/cli_handlers/server.rs`, `src/circuit_breaker.rs`, `src/connection_pool.rs`
- **Acción:** Confirmar que todos los módulos que importan axum/tokio/tower tienen cfg gate
- **Verify:** grep de `#[cfg(feature = "server")]` en los módulos afectados
- **Estado:** ⬜ PENDING

### Step 2: Verificar árbol de features sin server
- **Archivos:** `Cargo.toml`
- **Acción:** Correr `cargo tree --no-default-features -F cli -e features` y confirmar ausencia de axum/tokio/tower_governor
- **Verify:** salida sin axum, tokio, tower
- **Estado:** ⬜ PENDING

### Step 3: Verificar build `cli`-only compila
- **Archivos:** `Cargo.toml`
- **Acción:** `cargo check -p vantadb --no-default-features -F cli`
- **Verify:** exit 0 sin dependencias server
- **Estado:** ⬜ PENDING

### Step 4: Documentar hallazgos + recomendación en Backlog
- **Acción:** Actualizar la fila INV-011 en docs/Backlog.md con resultados y propuesta de feature gates
- **Verify:** Backlog actualizado
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- Auditar solo, sin implementación (alcance del backlog).

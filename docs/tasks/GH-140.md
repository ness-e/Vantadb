# Task 40 — GH-140 / Auditar CSS no usado (reescopetado a selectores huérfanos)

**Estado:** ✅ COMPLETED 2026-08-05
**Archivo:** `web/src/app/globals.css` (817L/19.0KB → 622L/~14.5KB, −195 líneas, −23.6%)
**Plan:** `docs/plans/2026-08-05-backlog-validation-actions.md` → Task 40

## Contexto re-escopetado
- DoD original ("≥10% de CSS muerto") no demostrable sin auditoría clase-por-clase.
- Pre-muestreo fallido: TODAS las clases efectos muestreadas parecían usadas; la auditoría clase-por-clase encontró 17 selectores custom huérfanos reales (0 usos) + 3 keyframes vinculados.
- DoD ajustado y registrado: **"0 selectores huérfanos probados"** (más estricto que el ≥10%).

## Método de auditoría
1. Inventario: `rg '^\s*\.' globals.css`.
2. Para cada clase custom, `rg -w \'<clase>\' web/src` excluyendo globals.css.
3. Huérfano = 0 coincidencias fuera de globals.css. Usar `-w` para evitar falsos positivos de substring (`press` dentro de `press-lg`; `text-outline` dentro de `text-outline-neon`; `hatch`/`tape` dentro de textos).
4. `@keyframes` solo se eliminan junto a su dueño huérfano (verificar que no haya otro consumidor).

⚠️ **Protección respetada:** Task 39 (INV-016-B) motion tokens (`--duration-fast/normal/slow`, `--ease-default` en `@theme inline`, líneas ~52-55) NO se tocaron. `var(--ease-default)` y `cubic-bezier` restantes intactos.

## Reporte de cobertura (clase → usos fuera de globals.css)
**ELIMINADOS (huérfanos, 0 usos):**
- `.paper-bg` — 0
- `.paper-grain` (+ SVG data-URI) — 0
- `.halftone-neon` — 0 (se conservan `.halftone`, `.halftone-fade`)
- `.hatch` — 0
- `.hatch-neon` — 0
- `.shadow-brutal`, `.shadow-brutal-lg`, `-neon`, `-sm` — 0 (el proyecto usa arbitrary `shadow-[6px_6px_0_0_#000]`)
- `.text-outline` (solo, sin `-neon`) — 0 (se conserva `.text-outline-neon`, usado en 5 archivos)
- `.marker-neon` — 0
- `.tape` / `.tape::after` — 0
- `.animate-shake` + `@keyframes vanta-shake` — 0
- `.animate-scan` + `@keyframes vanta-scan` — 0
- `.accent-bar-top` / `::before` — 0
- `.stripe-accent` — 0
- `.ink-corner` / `::after` — 0
- `.shadow-throw` — 0
- `.ink-drip` / `::after` — 0
- `.manga-frame` — 0
- `.kinetic-type` + `@keyframes vanta-kinetic-type` — 0

**→ 17 selectores + 3 keyframes vinculados eliminados.**

**CONFIRMADOS EN USO (verificado):** `.press`, `.press-lg`, `.press-neon`, `.btn-neon-glow`, `.halftone`, `.halftone-fade`, `.speed-lines`, `.speed-lines-radial` (hero.tsx:212), `.grid-tech`, `.text-stencil`, `.text-outline-neon`, `.scroll-manga`, `.skip-link`, `.glow-neon`, `.glow-box-neon`, `.glitch-hover`, `.neon-underline`, `.scanlines`, `.animated-gradient-border`, `.ink-divider`, `.vanta-slider` (latency-comparator.tsx), `.stagger-children`, `.animate-marquee`, `.animate-blink`, `.animate-flicker`, `.animate-rise`, `.animate-stamp`, `.animate-pulse-ring`, `.animate-float`.

## hero-mark-interactive.tsx (dead code documentado)
- Huérfano confirmado: 0 imports en web/src.
- No usa ninguna clase custom de globals.css (solo tailwind + `cubic-bezier(0.22,1,0.36,1)` inline en SVG).
- NO aporta CSS a globals.css → no se elimina (fuera del alcance CSS de GH-140; ya documentado en `web/AGENTS.md`).

## Verificación
- `npx tsc --noEmit`: 2 errores **pre-existentes** en `code-playground.tsx` (L84 `wasm_bindgen` duplicado, L123 `initSync`). Confirmado vía `git stash` — igual con globals.css intacta. No relacionados con este cambio (CSS-only).
- `npm run build`: **✅ PASS exit 0** (`ignoreBuildErrors: true` en next.config; Tailwind v4 compila y prerenderiza todas las rutas).

## Decisión DoD
Se eliminó **23.6%** > 10% → el DoD original CUMPLE además queda **0 selectores huérfanos probados**. Registrado DoD: **"0 selectores huérfanos probados"**.

## Commit
Staging selectivo: SOLO `web/src/app/globals.css` (no el plan file).
Mensaje: `refactor(GH-140): eliminar CSS amortiguado/eliminar selectores huérfanos probados de globals.css`
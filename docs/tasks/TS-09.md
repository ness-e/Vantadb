# TS-09: Bench reproducible JS/WASM (insert+search p50/p95/p99, dataset determinístico)

## Metadata
- **Plan file:** docs/plans/2026-09-07-backlog-triage.md
- **Creado:** 2026-09-07
- **Estado:** ⏳ IN PROGRESS
- **Branch:** develop
- **Appetite:** max 1d
- **SDP:** campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, api-and-interface-design, documentation-and-adrs (frontend-ui-engineering descartada: bench sin UI; ≤8 total + ponytail full siempre activo)
- **Task type (MCP):** typescript / TypeScript SDK — checks: `npx tsc --noEmit`, `npm test`

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `vantadb-ts/package.json` (scripts: build/test/lint/coverage, sin bench), `vantadb-ts/src/vantadb.ts:134-230` (create/connect/open) + `:424-468` (put/putBatch) + `:600-613` (search), `vantadb-ts/src/types.ts:25-90` (MemoryInput/SearchRequest), `vantadb-ts/vitest.config.ts` (external /vantadb-wasm/), `benches/canonical_p99.rs:1-100` (seed 42, p50/p95/p99), `vantadb-node/bench/bench-abi.mjs` (patrón bench: seedRecords/percentile/summarize/JSON line), `docs/operations/BENCHMARKS.md` (§10 PERF-BENCH-01 + entorno Regla 11)
- **Referencias hacia dentro (qué importa el slice):** `bench/bench.mjs` → `../dist/vantadb.js` (build `tsc`); `bench/smoke.mjs` → mismo; `package.json` script `bench` → `node bench/bench.mjs`
- **Referencias entrantes (quién depende):** nadie — `bench/` nuevo, 0 callers; `BENCHMARKS.md` §15 nuevo, solo enlazado desde WEB-02 futuro; `package.json` +1 script, ningún CI lo invoca aún
- **Veredicto de impacto:** BAJO — aditivo puro (2 archivos nuevos + 1 línea script + append docs). Sin cambio wire, sin API pública nueva, sin hot path Rust/WASM. Rollback = borrar `bench/` + revert 1 línea + revert append. Archivos BND-12/UX-A11Y-01 NO tocados (Wave1 paralelo).

## Gate D
- **Evaluación:** NO disparado — blast radius 3 paths (<10), sin hot path, sin símbolos públicos nuevos, contrato mecánico del plan (comando + percentiles + entorno + fecha). Pre-mortem del plan ya cubre Node-vs-browser, varianza y comparativa prohibida.
- **Spec-first:** aplica tabla `## Spec` abajo (bench nuevo = lógica nueva). Llena antes de ACT.

## Spec
| Decisión | Opción elegida | Evidencia / por qué |
|---|---|---|
| Runtime scope | Node ≥22 primero, browser = nota pendiente | Pre-mortem plan; BENCHMARKS.md §10 ya documenta que WASM-Node es in-memory y browser requiere OPFS/IDB |
| Harness | `node bench/bench.mjs` puro (sin deps nuevas), reutiliza patrón `bench-abi.mjs` (vec/percentile/summarize) | Ladder ponytail: existe en repo → reusar; 0 deps nuevas |
| Dataset | determinístico seed 42, defaults 2000 records × 384d × 200 queries, top_k=10, override por flags | canonical_p99 seed 42; defaults chicos para CI rápido, shape canónico documentado |
| Métricas | insert (putBatch × batches de 100) + search_vector + search_hybrid, cada una p50/p95/p99 + mean + throughput; ≥3 corridas, mediana+varianza | Contrato plan + pre-mortem Fallo 2 |
| Comparativas | PROHIBIDA head-to-head vs Orama/otros (D1) — solo números propios | Pre-mortem plan Fallo 3 |
| Salida | tabla human + 1 línea JSON machine-readable; `npm run bench` = 1 corrida; `bench:3x` = 3 corridas para mediana | Contrato + reproducibilidad Regla 11 |
| Docs | append §15 a BENCHMARKS.md con tabla + entorno (CPU/RAM/OS/runtime/versión) + comando + fecha; sin adjetivos sin número | Regla 11 |

## Contrato
`npm run bench` en `vantadb-ts/` corre insert+search con dataset determinístico y emite p50/p95/p99; resultados anexados a `docs/operations/BENCHMARKS.md` con entorno (CPU/RAM/OS) + comando + fecha. Sin comparativa externa.

## Herramientas
- `node --version` (v26.8.1 verificado), `npm run build`, `npm run bench`, `npx vitest run`, `npx tsc --noEmit`, `npx eslint`

## Steps
### Step 1: Harness bench + `npm run bench` (RED→GREEN)
- **Archivos:** `vantadb-ts/bench/bench.mjs` (nuevo), `vantadb-ts/bench/smoke.mjs` (nuevo), `vantadb-ts/package.json` (+2 scripts)
- **Acción:** RED = `npm run bench` no existe (falla). GREEN = harness insert+search_vector+search_hybrid con seed 42, percentiles, JSON line; smoke 50×8d×5q; scripts `bench` + `bench:3x`
- **Verify:** `npm run build` + `node bench/smoke.mjs` + `npm run bench -- --records 200 --dim 32 --searches 20`
- **Estado:** ✅ COMPLETED (smoke OK + bench emite p50/p95/p99 + JSON line)

### Step 2: 3 corridas + anexar §15 a BENCHMARKS.md
- **Archivos:** `docs/operations/BENCHMARKS.md` (append §15)
- **Acción:** correr `bench:3x` (defaults 2000×384d×200q), mediana+varianza, anexar tabla + entorno + comando + fecha; nota browser pendiente; 0 claims externos
- **Verify:** `grep -n "TS-09" docs/operations/BENCHMARKS.md` + re-leer sección; `grep -ri "faster\|ultrafast\|blazing" docs/operations/BENCHMARKS.md` → solo pre-existente, nada nuevo sin número
- **Estado:** ✅ COMPLETED (§15 anexado, 3 corridas + mediana, hype-check: 1 pre-existente §6, 0 nuevos)

### Step 3: Verify full + sync (SIN commit — orden explícita tarea)
- **Archivos:** este task file + plan file recitation
- **Acción:** `npx tsc --noEmit` + `npx vitest run` + `npx eslint bench/` en vantadb-ts; actualizar plan file (Estado + Commit "ninguno — NO commitear por orden")
- **Verify:** los 3 comandos verdes
- **Estado:** ✅ COMPLETED (tsc 0 + vitest 280/280 + eslint bench/ 0 errors; SIN commit por orden explícita)

## Dependencias
- Ninguna (Wave1 paralelo con BND-12, UX-A11Y-01; no tocar sus archivos)

## Notas
- Pre-mortem 1: WASM-Node in-memory vs browser OPFS → nota explícita en bench + docs.
- Pre-mortem 2: GC/timers → ×3 corridas + mediana + min/max como varianza.
- Pre-mortem 3: comparativa prohibida → grep de adjetivos en Step 2.
- Regla 11: entorno i5-1235U / Win11 / Node v26.8.1, comando y fecha en docs.

## Context Save Point
- **Fecha:** 2026-09-07
- **Branch:** develop
- **Decisiones:** Node-primero; reusar patrón bench-abi.mjs; defaults 2000×384 (rápido) con shape canónico documentado
- **Problemas conocidos:** ninguno aún
- **Próxima tarea:** WEB-02 (consume §15)

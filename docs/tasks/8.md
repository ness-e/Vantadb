# TASK-8: ESPACIO-02 — Selección por lasso → batch ops

## Metadata
- **Plan file:** docs/plans/2026-08-18-vanta-studio-fase2.md
- **Creado:** 2026-08-19T09:10
- **last-synced:** 2026-08-19T09:55
- **Estado:** ✅ COMPLETED

## Blast Radius
**Callers:** `SpaceLens` (montado en `WorkspaceShell` surface `"espacio"`, props onNotice/onError/dark/onOpenRecord) · `useProjection` (expone `state.selected: Set<number>` + `setSelected`) · `undoStore` (Ctrl+Z global en `WorkspaceShell.handleUndo`; PAPELERA = `TrashLens`) · `recordsToJsonl`/`downloadText` (`export-jsonl.ts`, usado por `ExportButtons`).
**Callees:** `remove`/`vantaPut` (vanta.ts bridge), `recordsToJsonl`, `downloadText`, `undoStore`.
**Implicaciones:** NO se toca `src/sdk/` ni bridge Rust — solo desktop TS/TSX. `deleteByFilter`/`exportNamespace` ya existen en `desktop/src/vanta.ts` (VS-CORE-04/05) y NO se modifican.

## Impacto mapeado (Regla 0)
- **Leídos completos:** `SpaceLens.tsx` (250L), `useProjection.ts` (178L), `undo.ts` (157L), `export-jsonl.ts` (47L), `ExportButtons.tsx` (115L), `WorkspaceShell.tsx` (794L), `types.rs` wire MemoryFilterItem (180-229), `native.rs` export/delete_by_filter + to_vanta_value (140-214, 660-739), `api.rs` delete_by_filter (1264-1318), `impl_export.rs` records_for_namespace (76-118), `package.json` desktop, `vite.config.ts`.
- **Referencias hacia dentro:** `MemoryRecord` (vanta.ts:93), `undoStore` (importado en WorkspaceShell:35, CommandPalette vía props), `useProjection` (SpaceLens:44), `recordsToJsonl`/`downloadText` (ExportButtons:7).
- **Referencias entrantes:** `SpaceLens` montado en WorkspaceShell:742-751 (props onNotice/onError/dark/onOpenRecord). `undoStore` singleton usado por WorkspaceShell (handleUndo/handleDelete) y CommandPalette (onUndo/onDelete). Ninguna ref entrante a `SelectionBar` (nuevo) ni a `softDeleteBatch` (nuevo método).
- **Veredicto:** cambios aditivos y locales a desktop/src. `undo.ts` amplía el tipo `Reverse` con `put-batch` (nuevo brazo, no rompe el existente). SpaceLens mueve el contador/clear inline a SelectionBar (estado visual, mismo comportamiento). Sin riesgo para otras workstreams.

## Contrato
"`npm run build` verde (tsc 0 err) + `npx vitest run` verde (undo batch: selección→borrar→undo restaura) + smoke de selección→acción vía test lógico; sin commit — lo commitea vanta-lead. Verificación Rust no aplica (0 archivos Rust tocados); fmt/clippy de cortesía."

## Herramientas
- Bash (npm run build, npx vitest run), codegraph, grep/read

## Steps
### Step 1: Undo store — softDeleteBatch (snapshot → re-put) + test
- **Archivos:** `desktop/src/store/undo.ts`, `desktop/src/store/undo.test.ts` (nuevo)
- **Acción:** método `softDeleteBatch(records)` serializado vía `run()`: `remove` por key, UN entry de undo con `reverse: {kind:"put-batch", records}` + `trashBefore`, tombstones en papelera. `undo()` maneja `put-batch` → `vantaPut` por record (snapshot previo). Test vitest con `vi.mock("../vanta")`: borra 3 → trash 3 → undo restaura 3 (mismos namespace/ttl) y restaura trashBefore.
- **Verify:** `npx vitest run src/store/undo.test.ts` (en desktop/)
- **Estado:** ✅ (2026-08-19T09:15 — `npx vitest run src/store/undo.test.ts` → 3/3 passed; verify via bash: campaign_verify_cmd no resuelve vitest desde la raíz — sin node_modules raíz, npx sí funciona en desktop)

### Step 2: SelectionBar.tsx (nuevo)
- **Archivos:** `desktop/src/components/space/SelectionBar.tsx`
- **Acción:** barra presentacional (estilo manga: press/border-2/font-tech) con contador `role="status"`, botones "Exportar (n)", "Eliminar (n)" (destructive styling), "✕ limpiar". Props `{count, busy, onExport, onDelete, onClear}`. Deshabilitada con count=0 o busy. Sin lógica de negocio (thin, testeable vía build).
- **Verify:** `npx tsc --noEmit` (dentro de npm run build del Step 3)
- **Estado:** ✅ (2026-08-19T09:55 — creado SelectionBar.tsx, presentacional puro; tsc 0 err vía build)

### Step 3: SpaceLens — integración barra batch ops
- **Archivos:** `desktop/src/components/space/SpaceLens.tsx`
- **Acción:** `selectedRecords = [...state.selected].map(i => points[i].record)`; handlers: export → `recordsToJsonl` + `downloadText("vanta-selection-<ts>.jsonl")` + onNotice; delete → `window.confirm("Borrar N registros…?")` → `undoStore.softDeleteBatch` → clear selección + onNotice (Ctrl+Z deshace); mover contador/clear inline del toolbar a SelectionBar; estado `busy` local.
- **Verify:** `npm run build` (tsc + vite) verde
- **Estado:** ✅ (2026-08-19T09:55 — SpaceLens integra SelectionBar; selectedRecords, handleExport (JSONL + downloadText), handleDelete (confirm → softDeleteBatch → clear → notice), busy local; contador/clear inline del toolbar movidos a la barra)

### Step 4: Verify + cierre
- **Acción:** verificación mecánica completa: `npm run build`, `npx vitest run` (todos los tests desktop), `cargo fmt --check`, `cargo clippy -p vantadb-desktop -- -D warnings` (cortesía, 0 archivos Rust tocados). Reportar sin commit.
- **Verify:** todos los gates verdes
- **Estado:** ✅ (2026-08-19T09:55 — `npm run build` verde tsc+vite 7.41s; `npx vitest run src/store/undo.test.ts` 3/3; node:test 14/14 (`node --test` para los 3 archivos node:test); `cargo fmt --check` del crate desktop falla PRE-EXISTENTE en `src/connections/manager.rs:618` — no toco archivos ajenos (0 Rust en mi diff); clippy no aplica: crate fuera del workspace raíz. Flake pre-existente a flaggear: `projection.worker.test.ts` timeout 5s → pasa con `--testTimeout=30000` (UMAP-js 15.3s en máquina cargada; no es regresión mía — archivo sin tocar))

## Dependencias
- Task 7: ESPACIO-01 (expone `selected` desde useProjection/SpaceLens — ✅ COMPLETED)
- VS-CORE-04/05 (deleteByFilter/exportNamespace — ya existentes, no se usan para batch N-keys: filtro AND no expresa "key ∈ {k1..kN}"; plan sanciona "o `remove` por key con undo VS-08")

## Notas
- Decisión export: client-side `recordsToJsonl`+`downloadText` (patrón ExportButtons) — la app NO tiene `@tauri-apps/plugin-dialog` (package.json), el patrón establecido es download del navegador (ExportPanel: "frontend download is the contract minimum"). El JSONL emitido es importable 1:1 vía `ingestBatch`.
- Decisión delete: `undoStore.softDeleteBatch` (per-record `remove` + un entry de undo) — `deleteByFilter` con Eq de keys no expresa un conjunto arbitrario de N keys (AND lógico), y el core no tiene undo nativo (verificado: api.rs delete_by_filter es destructivo; undo = patrón VS-08 sesión).
- Core no tiene undo nativo — confirmado por código (`delete_by_filter` borra directo; no hay API de undo en src/sdk).

## Context Save Point
- **Fecha:** 2026-08-19T09:10
- **Branch:** develop
- **CI pendiente:** sí (build + vitest del desktop)
- **Decisiones:** export client-side sobre exportNamespace(path) porque no hay dialog plugin y el filtro AND no expresa N keys; delete vía remove por key con undo batch (VS-08) sobre deleteByFilter por la misma razón de AND + plan sanciona el fallback.
- **Problemas conocidos:** ninguno
- **Próxima tarea:** Task 9: OP-01 — Import CSV/JSON pegado

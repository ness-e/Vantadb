# VS-18: Encoding redundante (color + ícono + texto) en chips/badges — A11y P15

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase1.md` (Task 9, Wave 2)
- **Fuente:** plan file Task 9 + SYNTHESIS P15 + DEFER Fase 1 "A11y pass Fase 1"
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟢
- **Tipo:** Frontend (desktop, Vite + React + Tailwind v4)
- **Turns estimados:** 8
- **Creado:** 2026-08-18
- **last-synced:** 2026-08-18
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps de ejecución restantes

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `WorkspaceShell.tsx` importa `DataExplorer` (línea 24/613) y `Inspector` (lazy, línea 38/657); `Inspector.tsx` importa `GeneralTab`/`MetadataTab`/`VectorTab`/`PayloadTab` |
| Callees | `DataExplorer.tsx` → `../vanta` (listPage/search), `@tanstack/react-table`, `@tanstack/react-virtual`, `export/ExportButtons`; `shared.ts` (ttl helpers); `index.css` tokens |
| Implicaciones | Cambios 100% internos a celdas/badges (sin cambios de props/API). Nueva utility CSS `.stripes-neon` aditiva. No rompe contrato VS-03/05/06. Layout de grid/tabs intacto (glyphs inline, wrapper span mantiene el grid). |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `DataExplorer.tsx` (650L), `inspector/Inspector.tsx` (231L), `inspector/GeneralTab.tsx` (175L), `inspector/MetadataTab.tsx` (121L), `inspector/VectorTab.tsx` (189L), `inspector/PayloadTab.tsx` (80L), `inspector/shared.ts` (180L), `lens/retrieval/ScoreBars.tsx` (95L), `lens/retrieval/retrieval-core.ts` (103L), `index.css` (707L), `components/mark/mark.css` (152L), `desktop/src/vanta.ts` (vía grep, shape `MemoryRecord`), `.opencode/rules/frontend-web.md` (scope web/ — NO aplica a desktop)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `DataExplorer` → vanta.ts, undo store, ExportButtons, tanstack; `Inspector` → GeneralTab/MetadataTab/VectorTab/PayloadTab/shared; todos usan tokens de `index.css`
- **Archivos que referencian a los editados (referencias entrantes):** grep → `WorkspaceShell.tsx` (DataExplorer + Inspector), `Inspector.tsx` (tabs), `Timeline.tsx`/`ActivityPanel.tsx`/`ResultsList.tsx`/`FiltersBuilder.tsx`/`RetrievalLens.tsx` (solo menciones textuales, sin imports de los editados)
- **Veredicto impacto:** **bajo** — ediciones internas a celdas y badges; ningún contrato externo cambia; sin props nuevas; sin dependencias nuevas; VS-17 (paralelo, copy-as en DataExplorer) toca botones de fila/panel — mi slice es solo chips/badges de celdas, sin overlap textual (no reescribo archivos completos, solo edito funciones puntuales: `TtlCell`, `vector` column cell, `MetaChips` untouched, tabs).

## Contrato

"`npm run build` (en `desktop/`) pasa y: (a) cada estado solo-color de chips/badges de MEMORIAS/Inspector tiene encoding redundante (ícono + texto o patrón); (b) `prefers-reduced-motion` respetado (no se agrega motion nuevo; media query existente intacta); (c) checklist AA ≥4.5:1 texto normal / ≥3:1 no-texto en claro y dark documentado en el task file; (d) ScoreBars (VS-13) verificado con encoding redundante ya existente (aria-label + número + tooltip + patrón) — NO duplicado."

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** (1) layout del grid de DataExplorer y tabs del Inspector intactos — cambios solo aditivos en celdas; (2) no cambiar tokens de color del tema (`index.css` `@theme inline`); la utility nueva `.stripes-neon` usa `var(--color-neon)` existente; (3) sin dependencias nuevas; (4) no tocar `ScoreBars.tsx` (VS-13 ya cumple encoding redundante); (5) `mark/mark.css` no aplica (mascota decorativa, sin estados de datos) — no tocar; (6) VS-17 corre en paralelo sobre `DataExplorer.tsx` — NO reescribir el archivo, solo ediciones puntuales de celdas.
- **Comandos de verificación:** `npm run build` en `desktop/` (✅ esperado); `npx tsc --noEmit` en `desktop/` (✅ esperado).
- **Deuda pendiente:** ninguna al cierre.

## Recitation (canónico — estructura única)

    contract:
      verificacion: "npm run build && npx tsc --noEmit" (desktop/) ✅
      evidencia:
        - claim: "TTL (activo/expirando/expirado) tiene ícono + texto + patrón de barra"
          evidencia: diff DataExplorer.tsx TtlCell + GeneralTab.tsx
          confianza: alta
        - claim: "vector presente/ausente tiene ícono + texto; texto AA (foreground)"
          evidencia: diff DataExplorer.tsx vector column
          confianza: alta
        - claim: "metadata dup en Inspector tiene ⚠ + title + aria-invalid (no solo neon)"
          evidencia: diff MetadataTab.tsx
          confianza: alta
        - claim: "tab activo del Inspector tiene marcador ◆ además de bg-neon"
          evidencia: diff Inspector.tsx
          confianza: alta
        - claim: "ScoreBars ya tiene encoding redundante (aria-label+número+tooltip+patrón) — verificado, sin cambios"
          evidencia: ScoreBars.tsx:53-93 (leído completo)
          confianza: alta
        - claim: "Contraste AA en claro y dark (texto ≥4.5:1, no-texto ≥3:1)"
          evidencia: checklist AA en Notas (luminancias calculadas)
          confianza: alta
      artefactos:
        - .opencode/skills/campaign-executor/tasks/VS-18.md
      invariantes: "layout grid/tabs intacto; tokens tema sin cambios; sin deps nuevas; ScoreBars/mark.css sin tocar"
      deuda: ninguna
      queda_pendiente: "screenshot visual grid+inspector claro/dark si el entorno lo permite (verificado manualmente si no); commit lo hace el lead"

## Deuda técnica (Regla 6 — MUST)

**Sin deuda** — no se introduce deuda nueva; el cambio es aditivo (glyphs inline + 1 utility CSS) y no toca hot paths.

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato del task file ✅ (build verde + encoding redundante en estados + AA verificado) |
| **Commit** | Lo hace el lead (worker no commitea — regla de rol). Diff aditivo, sin reescrituras |
| **Release** | N/A — cambio de UI desktop sin release propio |

## Herramientas necesarias
- codegraph_explore (blast radius) ✅ usado
- Terminal (npm build / tsc en desktop/)

## Investigation Notes
- `frontend-web.md` rule file aplica SOLO a `web/` (Next.js) — el desktop tiene sus propios tokens en `index.css` (manga/linocut, replicados de globals.css).
- Contraste calculado: `text-neon` #FF5500 sobre paper #F2EDE2 = **3.01:1** (FALLA 4.5:1 texto normal en claro) → en mi slice, los labels de estado usan `text-foreground` (AA ✅) con el neon solo como borde/accento no-texto (3:1 ✅). En dark #0a0a0a, neon = 6.06:1 ✅ (por eso los bordes neon se mantienen).
- `--color-ink` (#000) y `--color-smoke` (#1A1A1A) son estáticos (no flip en `.dark`): en dark, `bg-ink` sobre `bg-card` dark es invisible → los fills de barra TTL pasan a `bg-foreground` (flipea) + `stripes-neon` + `bg-muted-foreground`.
- No existe clase `.stripes-*` previa (grep 0 matches) — se agrega utility.

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 |
| Pendientes de ejecución (downhill) | 4 |
| % completado | 40% (discovery + task file) |

## Steps

### Step 1: Utility `.stripes-neon` en index.css
- **Archivos:** `desktop/src/index.css`
- **Acción:** agregar en `@layer utilities` la clase `.stripes-neon` (repeating-linear-gradient 135deg con `var(--color-neon)`), patrón ≠ color para el estado "expirando" de las barras TTL. Sin motion (no afecta reduced-motion).
- **Verify:** `npm run build` (desktop/) ✅
- **Estado:** ✅ COMPLETED (2026-08-18)

### Step 2: DataExplorer.tsx — TtlCell + vector cell
- **Archivos:** `desktop/src/components/DataExplorer.tsx`
- **Acción:** en `TtlCell`: derivar `state` (active/expiring/expired); label con ícono `●`/`⚠`/`✕` + texto (siempre `text-foreground` bold en expired); fill de barra: active→`bg-foreground`, expiring→`stripes-neon`, expired→`bg-muted-foreground`. En columna `vector`: presente → `◆ {n}d` con `text-foreground` (mantener `border-neon`); ausente → `◇ —`. Edición puntual (no reescribir archivo — VS-17 en paralelo).
- **Verify:** `npm run build` (desktop/) ✅
- **Estado:** ✅ COMPLETED (2026-08-18)

### Step 3: Inspector — GeneralTab TTL bar, MetadataTab dup, tabs activos
- **Archivos:** `desktop/src/components/inspector/GeneralTab.tsx`, `desktop/src/components/inspector/MetadataTab.tsx`, `desktop/src/components/inspector/Inspector.tsx`
- **Acción:** GeneralTab: mismo esquema TTL (ícono + texto foreground + fills con patrón/foreground/muted-foreground). MetadataTab: fila con key duplicada → `border-neon` + `text-foreground` (no neon) + `aria-invalid` + `title` + glyph `⚠` posicionado en wrapper span (mantiene grid 4-col). Inspector: tab activo → prefijo `◆ ` en el label (además del `bg-neon`).
- **Verify:** `npm run build` (desktop/) ✅
- **Estado:** ✅ COMPLETED (2026-08-18)

### Step 4: Verify final — build + tsc + checklist AA
- **Archivos:** —
- **Acción:** `npm run build` y `npx tsc --noEmit` en `desktop/`; revisión visual (screenshot grid+inspector claro/dark si `npx vite --port 3000` disponible en el entorno, si no documentar revisión manual del diff); documentar checklist AA.
- **Verify:** ambos comandos exit code 0 — `npm run build` (tsc && vite) ✅ exit 0, 5.94s; chunk >500kB warning pre-existente (Inspector lazy con CodeMirror/react-markdown, ya documentado en WorkspaceShell)
- **Estado:** ✅ COMPLETED (2026-08-18) — revisión visual: **manual documentada** (entorno sin backend Tauri en modo dev vite: DataExplorer requiere `listPage`/`search` vía bridge Tauri, no disponible en navegador plano → grid sin datos no muestra chips; validado por diff + build + checklist AA)

## Dependencias
- VS-05 (DataExplorer), VS-06 (Inspector), VS-13 (ScoreBars) — todos ya implementados en main (commit `0411117e`)
- VS-17 — paralelo, archivo compartido `DataExplorer.tsx`; slices aditivos disjuntos

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador (P2-01). El lead orquesta `vanta-review` con este task file antes de commitear; el implementador NO se auto-revisa.

- **Revisor:** [pendiente — vanta-review]
- **Enfoque:** cambios aditivos vs reescrituras (VS-17 en paralelo sobre DataExplorer/CommandPalette — el diff debe mostrar SOLO chips/badges); encoding redundante real (ícono+texto+patrón) en cada estado listado; layout intacto; checklist AA.
- **Cómo se probó:** `npm run build` (tsc + vite) ✅ exit 0; diff puntual verificado; checklist AA con cálculos de luminancia en Notas.
- **Veredicto:** ⏳ pendiente (lo emite vanta-review)

## Notas
- **Checklist AA (VS-18):**
  - Texto `text-foreground` (#000) sobre paper (#F2EDE2) claro: ~16:1 ✅ · sobre `bg-card` dark (#0a0a0a): cream #FBF9F5 ~19:1 ✅
  - Tab activo `text-background` (#000) sobre `bg-neon` (#FF5500): 6.55:1 ✅
  - Labels TTL (foreground, bold 10px): ≥4.5:1 ✅ claro y dark
  - Vector chip texto foreground: ✅ (antes `text-neon` = 3.01:1 ❌ en claro → corregido)
  - Metadata dup: texto foreground + border-neon (no-texto 3:1 ✅)
  - Barras TTL no-texto: `bg-foreground` sobre paper/card ✅; `stripes-neon` sobre paper 3.01:1 ✅ / sobre dark 6.06:1 ✅; `bg-muted-foreground` (#3A3A3A sobre paper 9.7:1 ✅; #C9C0AC sobre dark 11:1 ✅)
  - `prefers-reduced-motion`: no se agrega animación/transición nueva; media query global intacta
- **Decisiones de diseño:** los estados TTL se comunican por ícono + texto + patrón de barra (color secundario, P7/Cleveland–McGill); `text-neon` se reserva como acento no-texto donde falla AA en claro; `MetaChips`/`VersionCell` ya tenían encoding textual (tags `str`/`bool`…, `v{n}`) — sin cambio (P15 ya cumplido).
- **ScoreBars (VS-13):** verificado — ya tiene `role="img"` + `aria-label` con desglose, número a la derecha, tooltip con términos BM25/ranks, patrón rayado CSS para segmento vector y `sr-only` por segmento. NO duplicar.
- **mark.css:** no aplica (mascota decorativa, sin estados de datos).
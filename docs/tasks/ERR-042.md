# ERR-042 — read_header 2× por candidato en hot loop de search

**Estado:** ⏳ IN PROGRESS (fix ya aplicado en `e95dd94a`; falta cierre: paridad + perf + Backlog)
**Esfuerzo:** 🟡 | **Prioridad:** 🟡 | **Ruta:** vanta-tuner → vanta-worker
**Archivos clave:** `src/index/search.rs:275-280, 347-353` (hoy: entry loop ~119-211, hot loop ~282-467)

## Context Save (run 2026-08-10, lead directo)
- Commit: e95dd94a — perf(ERR-042)
- Fix: node_header leído 1x por entry_point/candidato, reusado en distance + tombstone check (era 2x read_header)
- Verify: cargo check ok; search suite 128/128 passed

## Contexto

`vs.read_header(node.storage_offset)` se invocaba **2× por candidato** en el hot loop de
`search_layer` (una para el cómputo de distancia, otra para el chequeo de tombstone) y
2× por entry point. Trabajo duplicado constante en cada search: lectura mmap redundante.

**Fix ya aplicado y commiteado en `e95dd94a` (perf(ERR-042)):**
- `src/index/search.rs:124` — entry points: `node_header` leído 1×, reutilizado en
  distancia (línea 128) y en eligibility/tombstone (línea 195)
- `src/index/search.rs:291-292` — hot loop: `node_header` leído 1×, reutilizado en
  distancia (línea 296) y en eligibility/tombstone (línea 371)
- Prefetch loop (línea 257) mantiene su `read_header` propio — es un prefetch de mmap,
  no trabajo duplicado del candidato (no se re-utiliza el header del scoring).

## Impacto mapeado (Regla 0)

**Archivos leídos completos:**
- `src/index/search.rs` (2098 L) — search_layer, search_nearest, search_ivf, search_scann, tests
- `src/storage/vfile.rs` (read_header 745-757, write_header 760+) — API VantaFile
- `src/node.rs` (DiskNodeHeader 1111-1141) — struct header, `new()`
- `src/storage/engine/mod.rs:37` — `FLAG_TOMBSTONE: u32 = 0x8`
- `benches/vfile_search.rs` (176 L) — bench existente: in_memory / with_vfile / compacted
- `docs/plans/archive/2026-08-09-residual-hardening.md` — plan original (Task 4, ✅ COMPLETED)

**Referencias hacia dentro (callers de search_layer / search_nearest):**
- `src/index/graph.rs` — 4 call sites de `search_layer` (insert_hnsw, rebuild paths)
- `src/index/search.rs` — `search_nearest` llama `search_layer` (632, 652); trait
  `VecIndex::search` → `search_nearest` (802)
- `src/index/ivf.rs`, `scann.rs`, `flat.rs` — paths alternativos no tocan read_header
- Miri tests en search.rs (1535+) — ejercitan search_layer con vector_store=None

**Referencias hacia fuera (deps de search.rs):**
- `crate::storage::vfile::VantaFile` — `read_header`, `mmap_bytes`, `mmap_bytes_mut`
- `crate::storage::engine::FLAG_TOMBSTONE`
- `crate::index::graph::{self, CPIndex, NeighborVec, NodeSim, NodeSimMin}`
- `crate::node::{DistanceMetric, FilterBitset}`, `crate::index::distance::*`

**Veredicto de impacto:** el fix ya está aplicado y es el mínimo (una lectura de header
por candidato, reutilizada). El cambio de comportamiento es nulo por construcción: el
header es `Copy` y el valor leído es idéntico; solo se elimina la segunda lectura mmap.
No hay API pública afectada (funciones `pub(crate)`). Riesgo: bajo. Los tests existentes
de search_layer no ejercitan el path con `vector_store = Some(vfile)` — falta un test de
paridad vfile vs in-memory (el contrato lo exige).

## Steps

- [x] S1. DISCOVERY: verificar estado del fix en HEAD (`e95dd94a` ya aplicado) + mapear impacto
- [x] S2. Test de paridad: search con `vector_store = Some(vfile)` produce los mismos
      resultados (ids + scores) que el path in-memory (`None`) — ✅ 2 tests nuevos pasan
      (`test_search_vfile_in_memory_parity`, `test_search_vfile_tombstone_header_excluded`)
- [x] S3. Medición perf antes/después: bench `vfile_search` en `e95dd94a^` (antes) vs HEAD
      (después) — ✅ with_vfile 211.06→187.00 ms (-11.4%), compacted 201.17→162.91 ms (-19.0%)
- [x] S4. Verify full: fmt ✅ + clippy ✅ + nextest 1902/1902 ✅
- [x] S5. Commit + actualizar Backlog (skill progreso) - commit `5a9eada1` (tests paridad) + fila ERR-042 movida de `docs/Backlog.md` a `docs/progreso/README.md` + espejo `docs/avance/activo/core-engine.md`

## Contrato verificable

- [x] `cargo nextest run --profile audit --workspace --build-jobs 2` ✅ 1902/1902 passed
- [x] `cargo fmt --check` ✅
- [x] `cargo clippy --workspace --all-targets --all-features -- -D warnings` ✅
- [x] Test de paridad: resultados de search idénticos ✅
- [x] Sin regresión de perf (mejora documentada: -11.4% / -19.0%) ✅

## Context Save

- Fix principal en `e95dd94a2b22ac119dd586755d30555585e9f00b` — 16 insertions/8 deletions en search.rs
- El plan original (residual-hardening) ya marca COMPLETED; Backlog.md:415 sigue "📝 Pendiente"
- Task file creado en este run (2026-08-11) — la tarea estaba sin archivo pese al fix commiteado
- Context Save del lead (run 2026-08-10) preservado arriba: commit e95dd94a, fix, verify search 128/128

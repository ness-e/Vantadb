# INV-010: ACID rollback multi-capa completo — diseño

## Metadata
- **Plan file:** (ninguno — standalone desde Backlog)
- **Creado:** 2026-08-03
- **last-synced:** 2026-08-03
- **Estado:** ✅ COMPLETED
- **Tipo:** 🔍 Diseño / Arquitectura — **sin implementación**
- **Fuente:** docs/Backlog.md:200

## Blast Radius
**Callers | Callees | Implicaciones**

- `begin_transaction` (ops.rs:205) → 19 callers (vantadb-mcp, tests) — API pública txn
- `commit_transaction` (ops.rs:300) → 13 callers
- `abort_transaction` (ops.rs:370) → 11 callers — hoy solo dropea buffer; **no** compensa capas
- `BufferedWrite{Insert,Delete}` (engine/mod.rs:129) — buffer en memoria, commit point = WAL
- `WalWriter.append/batch_append` (wal.rs:260/286) — append-only, CRC32C, rotación 256MB
- `replay_write_node` (engine/mod.rs:381) — recovery es **pre-MVCC** (`created_by_txn: 0`)
- `VantaFile` (vfile.rs:403) — 26 callers; vector store L0..LN, tombstones vía FLAG_TOMBSTONE
- `HNSW/CPIndex` (graph.rs) — sin MVCC; add/remove atómico en commit
- `NodeMetadata.created_by_txn/deleted_by_txn` (storage/ops.rs) — base MVCC Phase 3
- `delete()` (ops.rs:1440) — "no compensation action because WAL is the commit point. Full saga/2PC deferred to ACID Phase 0"

**Implicación:** cualquier cambio de protocolo toca la API pública txn (MCP la expone),
el formato WAL (compatibilidad con recovery existente), y el contrato de durabilidad
(RELIABILITY_GATE, chaos tests DRV-133).

## Contrato
**Verificable:** existe `docs/Investigaciones/ACID_ROLLBACK_DESIGN.md` que documenta:
1. Mapa de estado actual (qué capa tiene rollback: WAL=Begin/Commit/Abort+skip_mask, VantaFile=❌, HNSW=❌ commit-time-only, KV=metadata MVCC parcial)
2. Protocolo two-phase rollback entre capas (orden, compensaciones, crash-window)
3. Evaluación del Approach B del research perdido `ACID_TRANSACTIONS.md` — reconstrucción de los 3 enfoques o nota explícita del gap + comparación con implementación actual
4. Plan de implementación por fases (Phase 4+), con acceptance criteria por fase

**Check secundario:** `cargo check -p vantadb` ✅ (no debe romper nada — es solo diseño, sin cambios de código).

## Herramientas
- codegraph, cargo-mcp (check), grep/read (research docs)

## Contexto clave (hallazgos del lead — 2026-08-03)
- Research original `docs/research/ACID_TRANSACTIONS.md` **NO existe en el repo**. Bitácora lo referencia como "análisis completo de 14 páginas, 3 enfoques evaluados" (bitacora.md:96-108, 615-618). El sub-agente DEBE documentar este gap y reconstruir la comparación con lo que exista (MVCC_SNAPSHOT_ISOLATION.md es lo único que queda).
- Fases implementadas: 1 (WAL txn records, TASK-30), 2 (buffered writes, VFY-010), 3 (MVCC snapshot, VFY-011). `docs/progreso/README.md:938, 2628, 3026`.
- VantaFile rollback explícitamente "deferred a Phase 4" (bitacora.md:2634).

## Steps
### Step 1: Mapear estado actual de rollback por capa
- **Archivos:** `src/storage/engine/ops.rs`, `src/wal.rs`, `src/storage/engine/mod.rs`, `src/index/graph.rs`, `src/storage/vfile.rs`
- **Acción:** Documentar qué mecanismos de rollback existen hoy por capa (WAL, VantaFile, HNSW, KV backend). Identificar crash-windows.
- **Verify:** Sección "Estado actual" del doc de diseño con tabla por capa.
- **Estado:** ⬜ PENDING

### Step 2: Diseñar protocolo two-phase rollback entre capas
- **Archivos:** (diseño → doc nuevo)
- **Acción:** Definir orden de commit/abort entre capas, compensaciones por capa, manejo de crash mid-commit, interacción con MVCC Phase 3.
- **Verify:** Sección "Protocolo" con diagrama de secuencia + crash-window analysis.
- **Estado:** ⬜ PENDING

### Step 3: Evaluar Approach B (research perdido) vs implementación actual
- **Acción:** Reconstruir los 3 enfoques del research original (desde bitacora + código) o documentar gap; evaluar Approach B contra buffered-writes + MVCC actual.
- **Verify:** Sección "Evaluación de enfoques" con decisión recomendada.
- **Estado:** ⬜ PENDING

### Step 4: Proponer plan de implementación por fases
- **Acción:** Fases con acceptance criteria, riesgo, y qué toca (WAL format bump, VantaFile versionado, HNSW commit protocol, KV).
- **Verify:** Sección "Plan por fases" con criterios verificables por fase.
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna (diseño puro). Se apoya en VFY-010 (Phase 2) y VFY-011 (Phase 3) ya completadas.

## Notas
- NO escribir código del motor. Output = documento de diseño en `docs/Investigaciones/ACID_ROLLBACK_DESIGN.md`.
- `cargo check -p vantadb` debe seguir pasando (nada de código cambia).
- Si el sub-agente encuentra el research en otra ruta, citarla.

## Context Save Point
- **Fecha:** 2026-08-03
- **Branch:** (depende de git status)
- **CI pendiente:** no (diseño puro)
- **Decisiones:** Delegar a vanta-arch (diseño ACID multi-capa). Doc output en docs/Investigaciones/ (movido desde docs/architecture/ por ser item 🔍 de Investigaciones).
- **Problemas conocidos:** ACID_TRANSACTIONS.md perdido — documentar gap.
- **Próxima tarea:** N/A (standalone)

# R10: Consolidar §7 duplicado en reference compartido

## Metadata
- **Plan file:** docs/Backlog.md (P19, línea 471)
- **Creado:** 2026-08-16
- **last-synced:** 2026-08-16
- **Estado:** ✅ COMPLETED

## Blast Radius
- **Archivos nuevos:** `.opencode/references/task-system.md`
- **Archivos modificados:** `.opencode/agents/vanta-{lead,worker,arch,review,audit,docs,engine,tuner,chaos}.md` (9) — §7 completo → 1 línea de referencia
- **Implicaciones:** ~10 líneas idénticas × 9 agentes + tabla MCP + nota de filtrado se consolidan en un reference (patrón `definition-of-done.md`). Cada agente apunta al reference. El drift vanta-chaos (metasearchmcp ✅) vs vanta-audit (❌) se resuelve con la tabla canónica de vanta-lead (✅).

## Impacto mapeado (Regla 0)
- **Contenido compartido movido al reference:** los 6 bullets del task-system (Prompts activos, MCP tools, State machine, Workflows, Enforcement, Sesión), la tabla MCP canónica (vanta-lead) y la nota de filtrado.
- **Excepciones por agente:** el reference incluye una tabla de excepciones por permission block (R9) para que el enforcement real quede documentado junto a la canónica.
- **Referencias entrantes:** los 9 agentes apuntan al reference (1 línea en §7). No se borra información — las variantes por agente (cargo-mcp ✅ en worker, pencil ✅ en docs) quedan en la tabla de excepciones.
- **Referencias salientes:** ninguna.
- **Impacto si se elimina:** los agentes pierden el puntero al task-system (pero la info vive en el reference).
- **Git history:** working tree con R1+R5+R8+R9; no tocar AUD-024.md ni docs/plans/

## Contrato
- `.opencode/references/task-system.md` existe
- §7 de cada agente = 1 línea de referencia (heading + 1 línea de contenido)
- La tabla canónica del reference es la de vanta-lead (metasearchmcp ✅, drift corregido)
- Excepciones por agente documentadas (R9)

## Steps
### Step 1: Crear .opencode/references/task-system.md
- **Acción:** escribir el reference con: bullets del task-system, tabla MCP canónica (vanta-lead), nota de filtrado, tabla de excepciones por agente (R9)
- **Verify:** archivo creado ✅
- **Estado:** ✅ COMPLETED

### Step 2: Reemplazar §7 en los 9 agentes por 1 línea
- **Archivos:** los 9 `vanta-*.md`
- **Acción:** en cada agente, reemplazar todo el bloque §7 (bullets + tabla + nota) por: `## 7. Task System Integration` + `Ver \`.opencode/references/task-system.md\` — integración del task-system (prompts, MCP tools, state machine, workflows, enforcement) y tabla canónica de MCP servers.`
- **Verify:** grep "references/task-system.md" → 9 en agents/
- **Estado:** ✅ COMPLETED

### Step 3: Verificación contrato
- **Acción:** grep confirmar 9 referencias + el archivo reference existe + tabla canónica con metasearchmcp ✅
- **Verify:** contrato R10 pasa
- **Estado:** ✅ COMPLETED

## Dependencias
- R9 (las excepciones del reference reflejan los permission blocks recién alineados)

## Notas
- Se excluye `vanta-research.md` (9 agentes de la tarea, no research)
- La variante del State machine de vanta-review ("— tu dictamen es el gate del estado REVIEW") se pierde en la consolidación — es redundante con su §1a (Protocolo de Segunda Opinión) y con la tabla de excepciones; el dictamen como gate queda descrito en el reference (state machine C0 incluye REVIEW).

## Context Save Point
- **Fecha:** 2026-08-16
- **Branch:** (working tree, sin commit — lead commitea por batch al cierre)
- **CI pendiente:** no aplica
- **Decisiones:** reference único con canónica de lead + tabla de excepciones por permission block (para no perder las variantes reales)
- **Problemas conocidos:** ninguno
- **Próxima tarea:** — (última de la secuencia R1→R5→R8→R9→R10)
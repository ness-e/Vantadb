# STU-003 — Familia entity/scene con serde alias

## Metadata

- **Plan file:** `docs/plans/2026-09-11-stutter-interno.md` (Task 3, líneas 106-139)
- **Fuente:** sesión `ses_f710bf1ceffeYi3mGzzzlwAPxc` (auditoría 45 hallazgos) + re-verificación contra árbol actual + decisiones usuario Gate P (opción A)
- **Esfuerzo:** 🟡 (max 1d; 8 renames + 2 aliases + 1 roundtrip test, 0 decisiones abiertas)
- **Prioridad:** 🔴 (familia más grande de stutter real; bloquea archivado de la campaña, no STU-004 rename)
- **Tipo:** refactor con migración serde (rename + `#[serde(alias)]` para lectura de DBs viejas; sin cambio de comportamiento)
- **Ruta:** vanta-worker
- **Turns estimados:** 6 (1 discovery hecho + S1 roundtrip RED + S2 entity + S3 scene + S4 callers + S5 verify final)
- **Estado:** ✅ COMPLETED (ejecutado 2026-09-11, commit `b3ea6028`; S1-S5 ✅ con evidencia § Recitation-ejecución)
- **Uphill/Downhill:** ⬆️ 0 / ⬇️ 5 steps (roundtrip + 2 renames + callers + verify) — detalle en § Contadores uphill/downhill
- **Appetite:** max 1d
- **Autonomous:** false
- **Branch:** (sin crear — lo crea el ejecutor)
- **Commit:** (pendiente — un commit por slice, mensaje `refactor(stu-003): …`; push solo vía `vanta-lead`)
- **Creado:** 2026-09-11T00:00Z
- **last-synced:** 2026-09-11T12:00Z (discovery)

## Blast Radius

| Símbolo (origen → destino) | Callers verificados | Callees / dependencias | Implicaciones |
|---|---|---|---|
| `EntityStore::entity_set` → `set` (`src/entity/mod.rs:81`) | `src/entity/tests.rs` 9 call sites (`:34,:72,:76,:105,:136,:159,:162,:180,:183,:200,:206,:212,:218,:224` — 9 sets efectivos + 5 de rechazo en `invalid_inputs_rejected`); `src/entity/checker_tests.rs` 3 (`:37 seed_member,:55 seed_asset,:79 seed_acl`); `src/cli_server_auth_tests.rs:36` (helper `seed_user`); `src/server/cli_server_auth_tests.rs:48` (helper `seed_user`); `vanta-proxy/src/auth.rs:197` (`seed_user` test); `vanta-proxy/src/session.rs:264` (test seed); interno `mod.rs:90` NO (es `entity_get`, ver fila siguiente) | `validate_key` (`:88`), `entity_get` (`:90` auto-llamada upsert), `now_secs`, `entity_key` (`:103`), `serde_json::to_vec`, `put_to_partition(InternalMetadata)` | Rename puro misma firma. La auto-llamada `:90` se renombra en el mismo slice S2. Todos los callers son internos/tests del workspace — el compilador caza restos. Actualizar también `session.rs`/`auth.rs` proxy (S4) |
| `EntityStore::entity_get` → `get` (`src/entity/mod.rs:110`) | Interno `mod.rs:90` (upsert en `entity_set`); `src/entity/checker.rs` 3 (`:136` asset, `:215` team_member, `:249` acl); `src/entity/tests.rs` 4 (`:49,:61,:113,:230` — incluye 1 de rechazo); `src/skills.rs` 4 (`:78` `get_version`, `:377` `get_head` aprox, `:380`, `:628` via `version_entity_id` — verificar en S4 con `rg`); `vanta-proxy/src/auth.rs:162` (`entity_exists`, con match `InvalidInput→false` `:166`) | `validate_key` (`:116`), `entity_key` (`:119`), `get_from_partition(InternalMetadata)`, `serde_json::from_slice` → `Entity` (aquí aplica el alias S2) | Rename + alias interplay: el `from_slice` `:121` debe aceptar JSON viejo (`entity_id`) vía alias. `checker.rs` es el consumidor de seguridad (auth) — re-ejecutar `checker_tests` en S4/S5 como guardián |
| `EntityStore::entity_delete` → `delete` (`src/entity/mod.rs:129`) | `src/entity/tests.rs` 2 (`:109,:123` + `:234` rechazo) | `validate_key` (`:135`), `entity_key` (`:136`), `get_from_partition` + `write_backend_batch(Delete/InternalMetadata)` | Rename puro; sin serde (no serializa). Sin callers fuera de tests + definición |
| `EntityStore::entity_list` → `list` (`src/entity/mod.rs:153`) | `src/entity/tests.rs` 4 (`:140,:165,:187,:238` rechazo); `src/skills.rs:113` (`SkillStore::list` heads); `src/server/state.rs:313` (`resolve_user_key` scan 10k + `ct_eq`); `vanta-proxy/src/auth.rs:127` (`resolve_user_key` snapshot, error string `"entity_list user: {e}"` `:128` — actualizar literal) | `validate_scope` (`:160`), `scan_partition_prefix(collection_prefix)`, `serde_json::from_slice` por fila (`:167` — alias aplica), sort por `entity_id` (`:171` → `id` en S2) | Rename + sort-key `:171` cambia a `.id`. Los dos `resolve_user_key` (server + proxy) son path de auth — guardianes en S4. El literal de error `:128` menciona `entity_list` → actualizar texto |
| `Entity::entity_id` → `id` + `#[serde(alias = "entity_id")]` (`src/entity/mod.rs:46`) | Campo: `mod.rs:94` (construcción en `set`), `:171` (sort en `list`); `tests.rs:39` (`stored.entity_id`), `:146` (`e.entity_id`); `skills.rs` 6 (`:304` `row.entity_id`, `:346` `row.entity_id`, `:377-402` `old.entity_id`, `:486` construcción, `:496`, `:543`, helpers `:606-615` `version_from_entity_id` + campo `"entity_id"` en errores, `:640` `entity_key(ns,col,entity_id)`); `server/state.rs:324` (`entity.entity_id` en `resolve_user_key`); `proxy/auth.rs:141` (`entity.entity_id.clone()`); doc-comments `mod.rs:7,11-12,64` (prosa `entity_id`) | `Serialize/Deserialize` (derive `:42`), `HashMap<String,FieldValue>`, `entity_key(ns,col,id)` (formato clave intacto) | **Cambio de wire:** el JSON pasa de `{"entity_id":"…"}` a `{"id":"…"}`. Alias obligatorio para leer DBs viejas. Accesos `.entity_id` en `skills.rs`/`state.rs`/`auth.rs` se renombran a `.id` en S4. Helpers `version_entity_id/head_entity_id/entity_key` de `skills.rs` son locales de otro módulo — NO se renombran (ver Spec). Prosa doc `:7,11` se actualiza a `id` (concepto), formato de clave `entity:{ns}:{col}::{id}` intacto |
| `SceneNodeStore::scene_node_set` → `set` (`src/entity/scene.rs:76`) | `src/entity/scene_tests.rs` 6 (`:32,:67,:71,:119,:123,:146` + `:102` rechazo) — CERO callers fuera de `scene.rs` + `scene_tests.rs` (grep `src/` = solo esos dos archivos) | `validate_key` (`:86`, reuse de `super`), `scene_key` (`:100`), `serde_json::to_vec`, `put_to_partition(InternalMetadata)` | Rename confinado. Sin consumidores de auth/skills sobre scene (verificado por grep) |
| `SceneNodeStore::scene_node_get` → `get` (`src/entity/scene.rs:107`) | `scene_tests.rs` 4 (`:44,:56,:90,:107` rechazo,`:156`) | `validate_key`, `scene_key`, `get_from_partition`, `from_slice` (alias S3) | Rename confinado + alias en deserialización `:118` |
| `SceneNodeStore::scene_node_delete` → `delete` (`src/entity/scene.rs:126`) | `scene_tests.rs` 2 (`:150,:153`) | `validate_key`, `scene_key`, `get` + `write_backend_batch(Delete)` | Rename puro, sin serde |
| `SceneNodeStore::scene_node_list` → `list` (`src/entity/scene.rs:150`) | `scene_tests.rs` 2 (`:127,:134`) | `validate_scope`, `scan_partition_prefix(scene_prefix)`, `from_slice` (`:164` — alias), sort por `scene_name` (`:168` → `.name` en S3) | Rename + sort-key cambia a `.name` |
| `SceneNode::scene_name` → `name` + `#[serde(alias = "scene_name")]` (`src/entity/scene.rs:39`) | Campo: `scene.rs:90` (construcción), `:168` (sort); `scene_tests.rs` 4 (`:37` `stored.scene_name`, `:130` `n.scene_name`, `:136`, test-name `:97` `validation_rejects_bad_scene_name` — nombre de test, actualizar por claridad) | `Serialize/Deserialize` (`:35`), `scene_key(ns,sess,name)` (formato intacto) | Cambio de wire `{"scene_name":"…"}` → `{"name":"…"}` + alias. NO tocar `vanta-proxy/src/inject.rs:115` (`scene.scene_name` es `SceneBlock` de `vanta-memory`, otro tipo — ver H7), ni `vantadb-mcp/src/scenes.rs` params JSON (`scene_name` es API MCP, otro contrato), ni tests `vanta-memory/tests/*` (tipos `SceneExtraction`/`SceneMeta`, otro crate) |
| Doc-links `EntityPage::EntityStore::entity_list` → `list` (`src/entity/mod.rs:52`); `SceneNodePage::SceneNodeStore::scene_node_list` → `list` (`src/entity/scene.rs:46`) | Cero refs externas a `EntityPage`/`SceneNodePage` fuera de `mod.rs:159,174` y `scene.rs:156,171` (grep `EntityPage\|SceneNodePage` en `src/` = solo esos archivos) | `Entity`/`SceneNode` items | Intra-doc links — `rustdoc` warning los caza. Actualizar en S2/S3 con el rename del método |

**Cobertura de investigación declarada:** 1 `codegraph_explore` (24 símbolos, 2 archivos, blast radius + callers + source verbatim `mod.rs` 262L + `scene.rs` 189L); lecturas COMPLETAS `src/entity/tests.rs` (258L), `src/entity/scene_tests.rs` (159L), `src/entity/checker.rs` (343L); lecturas zonales `src/skills.rs:70-189,295-374`, `src/server/state.rs:300-349`, `src/server/cli_server_auth_tests.rs:35-64`, `vanta-proxy/src/auth.rs:120-209`; greps exhaustivos `entity_set|get|delete|list|entity_id` (100+ hits, `src/` + proxy) y `scene_node_*|scene_name|EntityPage|SceneNodePage` (100+ hits, con falsos positivos `vanta-memory/` y `vantadb-mcp/` descartados por path/tipo) + `serde alias` (0 alias existentes — confirmado que hay que crearlos) + `serde =` (workspace `Cargo.toml:27` serde 1 derive, `:53` serde_json 1.0); `campaign_discover_skills_v2` BUILD (8 skills, score 1.00) + 5 `skill` cargadas. Señal best-effort: codegraph listó `vanta-proxy` callers de `entity_set` que el grep `src/` no veía — el censo final usa grep workspace como ground truth.

## Impacto Regla 0

- **Leídos (completos o zonas citadas):** `src/entity/mod.rs` íntegro (262L vía codegraph), `src/entity/scene.rs` íntegro (189L vía codegraph), `src/entity/tests.rs` íntegro (258L), `src/entity/scene_tests.rs` íntegro (159L), `src/entity/checker.rs` íntegro (343L), `src/entity/checker_tests.rs` íntegro (442L — seeds vía `entity_set` `:37,:55,:79`), `src/cli_server_auth_tests.rs` íntegro (589L — `seed_user` `:34-47` + 8+ usos), `src/skills.rs` zonas `:70-189` + `:295-374`, `src/server/state.rs` `:300-349`, `src/server/cli_server_auth_tests.rs` `:35-64`, `vanta-proxy/src/auth.rs` `:120-209`, plan Task 3 (`:106-139`)
- **Referencias (menciones no-código que NO se tocan):** `.opencode/commands/cleanCA.md:25` + `.opencode/references/clean-code-clean-architecture.md:676` (ejemplos del stutter — son la justificación, no código), `campaign-executor/tasks/MEM-05.md:17,MEM-06.md:17` (docs históricas que citan `entity_get/set/delete/list` — scope discipline, no reescribir), `docs/tasks/STU-001.md:41` (menciona STU-003 como contraste NO-serde), `STU-002/STU-004.md` (menciones de secuenciación), `vanta-proxy/src/auth.rs:43` (comentario `entity_list` scan — SÍ se toca porque es comentario junto a código renombrado `:127`, ver Spec), `vanta-proxy/src/session.rs:174,185-191` (doc + param `entity_id` de OTRO método `entity_exists`/`advance` — no renombrar esos, solo el `entity_set` `:264` de tests)
- **Entrantes (quién importa lo renombrado):** `checker.rs:28` (`use EntityStore`), `checker.rs:79` (`&EntityStore` en `PermissionChecker`), `state.rs:309` (`&EntityStore` en `resolve_user_key`), `skills.rs` (`self.entities: EntityStore` + 5+ llamadas), `proxy/auth.rs:125,161` (`EntityStore::new` + llamadas), `proxy/session.rs:189` (`entity_exists` → `entity_get` interno), tests todos vía `EntityStore::new`. Ningún import necesita cambio de path (mismo tipo, mismo módulo) — solo call sites `.entity_*` y accesos `.entity_id`
- **Veredicto:** rename DIRECTO con alias serde (sin deprecación por reps: cero usuarios fuera del workspace y el plan Gate P lo ordena). Familia SÍ serde — a diferencia de STU-001 — por eso lleva `#[serde(alias)]` + roundtrip test con stop condition (plan línea 135: roundtrip viejo falla → STOP). Claves de partición `entity:`/`scene:` intactas (son prefijos de `entity_key`/`scene_key`, no campos serde)

## Contrato

```text
cargo check -p vantadb --all-targets && cargo nextest run --profile audit -p vantadb -- entity
```

(o filtro equivalente con evidencia) MÁS test roundtrip formato viejo verde (S1: JSON con `entity_id`/`scene_name` deserializa en structs nuevos). DoD del plan (línea 139): contrato + test roundtrip formato viejo verde. Post-rename: `rg -n "entity_set|entity_get|entity_delete|entity_list|\.entity_id|scene_node_set|scene_node_get|scene_node_delete|scene_node_list|\.scene_name" src/ vanta-proxy/src/` → 0 hits en código Rust (excluyendo este task file, docs históricos MEM-*/cleanCA, y tipos AJENOS `vanta-memory`/`vantadb-mcp`/`SceneBlock` — ver H7).

## Spec

| Rename / decisión | Evidencia que lo justifica | Destino (plan) | Alcance de edición |
|---|---|---|---|
| `EntityStore::entity_set/get/delete/list` → `set/get/delete/list` | Stutter `EntityStore::entity_*`: el tipo ya dice entity; convención repo (STU-001 `is_saturated`, `mark_invalid`). Defs `mod.rs:81,110,129,153`; plan Task 3 lo ordena | `set/get/delete/list` | `src/entity/mod.rs` (4 defs + auto-llamada `:90` + sort `:171` + doc-link `:52`) + todos los callers S4 |
| `Entity::entity_id` → `id` + `#[serde(alias = "entity_id")]` | Stutter `Entity::entity_id`: el tipo ya dice entity. Wire cambia → alias lee DBs viejas, escribe nombre corto (plan línea 25). Derives `Serialize/Deserialize` confirmados `:42`; serde 1 derive (`Cargo.toml:27`), serde_json 1.0 (`:53`); 0 alias existentes (grep) | `pub id: String` con `#[serde(alias = "entity_id")]` | `mod.rs:46` + construcción `:94` + sort `:171` + error strings `:248,:252` (`"entity_id must…"`, `"id must…"` — decisión: renombrar a `"id must be non-empty"` / `"id must not contain…"` porque el param también se renombra) + params `entity_id: &str` → `id: &str` en las 4 firmas + helpers `entity_key/validate_key` params (internos, mismo rename) |
| `SceneNodeStore::scene_node_set/get/delete/list` → `set/get/delete/list` | Mismo stutter en scene. Defs `scene.rs:76,107,126,150`; callers SOLO `scene_tests.rs` (censo H3) | `set/get/delete/list` | `scene.rs` (4 defs + sort `:168` + doc-link `:46`) + `scene_tests.rs` (16 call sites) |
| `SceneNode::scene_name` → `name` + `#[serde(alias = "scene_name")]` | Mismo stutter + wire. Derive `:35`. Plan línea 25 | `pub name: String` con `#[serde(alias = "scene_name")]` | `scene.rs:39` + construcción `:90` + sort `:168` + params `scene_name: &str` → `name: &str` en 4 firmas |
| **Alias strategy (núcleo S1-S3):** `#[serde(alias = "…")]` en cada campo renombrado, SIN `rename` | `alias` acepta viejo + nuevo en deserialize y escribe el nombre del campo (nuevo) en serialize — exactamente la semántica del plan ("escritura nueva usa nombre corto", línea 25). `rename` rompería escritura vieja; `alias` es aditivo y rollback-friendly. Fuente: docs oficiales serde (field alias) — el ejecutor la cita vía `source-driven-development` antes de S1 | 2 atributos, uno por struct | Sin helpers de migración, sin dual-write manual, sin script de re-escritura de DB |
| Claves `entity:`/`scene:` NO cambian | `entity_key` (`mod.rs:218-224`) y `scene_key` (`scene.rs:178-184`) construyen `entity:{ns}:{col}::{id}` / `scene:{ns}:{sess}::{name}` como BYTES de partición, no como JSON — el rename es de campo serde + método, no de layout de claves. `collection_prefix`/`scene_prefix` (`:227,:187`) intactos | Sin edición en `entity_key`, `collection_prefix`, `scene_key`, `scene_prefix` (solo renombran su PARAM si coincide, cuerpo byte-idéntico) | Invariante 1 (no tocar formato de clave) |
| Doc-links `EntityPage::EntityStore::entity_list` → `list`; `SceneNodePage::SceneNodeStore::scene_node_list` → `list` | Intra-doc links `:52` y `:46`; pre-mortem plan línea 133 (`EntityPage` doc-link). `rustdoc` warning los caza | `list` en ambos links | S2 + S3 con sus métodos |
| Params `entity_id: &str` → `id: &str`, `scene_name: &str` → `name: &str` (incl. `validate_key`, `entity_key`) | Consistencia: dejar el param viejo sería medio-stutter (`fn set(namespace, collection, entity_id)` junto a campo `id`). Internos, cero wire | `id` / `name` | `mod.rs:85,114,133,:218,:245` + `scene.rs:80,111,130` params (cuerpos intactos) |
| NO-renames justificados: `entity_key`, `collection_prefix`, `scene_key`, `scene_prefix`, `version_entity_id`, `head_entity_id`, `version_from_entity_id`, `entity_exists` (proxy), `EntityStore`/`SceneNodeStore`/`Entity`/`SceneNode`/`EntityPage`/`SceneNodePage` (nombres de tipos), `scene_name` en `vanta-memory::*` / `vantadb-mcp/src/scenes.rs` / `proxy/inject.rs:115`, `generate_id`, `validate_key/scope` (nombres) | `entity_key` & co. describen la CLAVE (`entity:` prefijo), no el campo — renombrarlos sería scope creep y rompería `skills.rs:346,640` simetría. Tipos sin stutter (no repiten módulo). `scene_name` ajenos son otros tipos/contratos (H7). `Error::*Error` fuera por plan línea 19 | Intactos | Si `rg` post-rename muestra `entity_key` → esperado, NO es resto |

## Invariantes

1. **Claves de partición intactas:** bytes `entity:{ns}:{col}::{id}` y `scene:{ns}:{sess}::{name}` byte-idénticos antes/después (solo cambia el NOMBRE del param, no el `format!`). `collection_prefix`/`scene_prefix` intactos — listing no se rompe
2. Escritura nueva usa `id`/`name`; lectura acepta `id`+`entity_id` / `name`+`scene_name` (alias). Cero migración de datos, cero dual-write
3. Semántica CRUD intacta: upsert preserva `created_at` (entity), wholesale replace (scene), sort determinista (`id` / `name`), paginación `total` pre-offset/limit, validación `{`,`}`,`:` intacta (solo cambia el texto del mensaje, no la regla)
4. `checker.rs` cadena allow-only de 7 pasos intacta (resource→owner→membership→visibility→role-default→ACL→deny); `resolve_user_key` (server + proxy) misma lógica `ct_eq` + first-wins
5. `skills.rs` helpers `version_entity_id/head_entity_id/entity_key` y wire `SkillRecord` intactos (usan `Entity` como transporte, no como schema propio)
6. `validate_key/scope` reglas intactas; `generate_id` formato `prefix-4ts+6rand` intacto
7. Docs históricos (`MEM-05/MEM-06`, cleanCA, `memory/decisions.md`, tasks cerradas) NO se reescriben (Regla 0.5)
8. Cada slice deja `cargo check -p vantadb --all-targets` verde; ningún slice >100 líneas de diff sin `campaign_verify_cmd`

## Recitation inicial

```text
=== RECITATION ===
Objetivo activo: STU-003 — Familia entity/scene con serde alias
Estado: plan (discovery completo, 0 slices ejecutados)
Última acción: discovery puro — censo de 10 símbolos + callers + derives serde + diseño roundtrip, task file creado sin tocar código
Resultado: 🟡 (pendiente de ejecución S1-S5)
State: PLAN (desde: —)
Próxima acción: S1 — escribir roundtrip test (JSON viejo entity_id/scene_name → structs nuevos) y correrlo en ROJO con `cargo nextest run -p vantadb -- entity_roundtrip_compat`
Contrato: cargo check -p vantadb --all-targets && cargo nextest run --profile audit -p vantadb -- entity + roundtrip viejo verde + rg cero restos (resultado: no-corrido en discovery)
Invariantes: claves entity:/scene: intactas; alias acepta viejo+escribe nuevo; checker 7 pasos intacto; resolve_user_key ct_eq intacto; skills helpers intactos; docs históricos no tocados
Comandos de verificación: `cargo check -p vantadb --all-targets` / `cargo nextest run --profile audit -p vantadb -- entity` / `rg -n "entity_set|…|scene_node_…" src/ vanta-proxy/src/` (todos pendientes del ejecutor)
Deuda: ninguna viva — ver Deuda Regla 6 (solo VERIFY_CONTRATO:no-corrido + riesgo 🟡×🔴 cubierto por alias+test)
Próxima tarea si completa: STU-004 — FOOTER_GROUPS + verify final + cierre (el archivado espera a STU-003)
last-synced: 2026-09-11T12:00Z
=== END RECITATION ===
```

## Recitation-ejecución (2026-09-11, retry fresco)

```text
=== RECITATION ===
Objetivo activo: STU-003 — Familia entity/scene con serde alias
Estado: ✅ COMPLETED (S1-S5 ✅, commit b3ea6028, sin push)
Última acción: retry verificó código heredado sesión previa (S1-S4 ya aplicados en worktree) → S5 verify completo + fmt-fix + commit blast radius 24 archivos
Resultado: OK
State: VERIFY→CLOSE (desde: PLAN)
Próxima acción: ninguna en STU-003; desbloquear STU-004 archivado
Contrato: cargo check -p vantadb --all-targets ✅ + check vanta-proxy ✅ + nextest entity 46/46 ✅ (incl. 2 roundtrip legacy) + gate auth/checker/skills 45/45 ✅ + clippy -D warnings ✅ + fmt ✅ + rg cero ✅ (excepción H7 inject.rs:115 SceneBlock)
Invariantes: claves entity:/scene: byte-idénticas (solo nombre de param) ✅; alias lee viejo+escribe nuevo ✅; checker 7 pasos intacto ✅; resolve_user_key ct_eq intacto ✅; skills helpers intactos ✅; docs históricos no tocados ✅; firmas ajenas entity_exists/advance intactas ✅
Comandos de verificación: `cargo check -p vantadb --all-targets` / `cargo check -p vanta-proxy --all-targets` / `cargo nextest run --profile audit -p vantadb -- entity` / `cargo nextest run --profile audit -p vantadb -- auth checker skill` / `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` / `cargo fmt --check -p vantadb` / rg restos → 0
Deuda: ninguna viva — VERIFY_CONTRATO:pasa; fmt-fix aplicado pre-commit; sin scope creep (ver S4)
Próxima tarea si completa: STU-004 — FOOTER_GROUPS + verify final + cierre (desbloqueado para archivar)
last-synced: 2026-09-11T (ejecución)
=== END RECITATION ===
```

> La Recitation inicial (§ Recitation inicial) se conserva arriba como registro del discovery; este bloque es el handoff de cierre.

## Deuda Regla 6

- **Ejecución (2026-09-11):** S1-S5 ✅, commit `b3ea6028`, VERIFY_CONTRATO:pasa — deuda discovery cerrada.

- **Discovery (2026-09-11T12:00Z):** ejecución S1-S5 pendiente (este file es discovery; cero código modificado — verificado: ningún edit fuera de `docs/tasks/STU-003.md`)
- `VERIFY_CONTRATO:no-corrido` — el contrato NO se ejecutó en discovery (discovery es read-only por orden; lo corre el ejecutor tras cada slice vía `campaign_verify_cmd`)
- Doc-comments con nombres viejos fuera de `src/entity/**` (`MEM-05/MEM-06`, cleanCA, `ERROR_HANDLING`-familia): se dejan a propósito (Regla 0.5) — candidata tarea docs separada, NO deuda de esta task
- Riesgo residual plan 🟡×🔴 (DB vieja ilegible) → cubierto por alias + S1; riesgo 🟡×🟡 (doc-links rotos) → cubierto en S2/S3 + `cargo doc` si el ejecutor quiere (no exigido por contrato)
- `ponytail:` sin script de migración de DB — el alias ES la migración (aditivo, cero código extra). Si algún día se quiere expulsar el alias: tarea separada con re-escritura offline, no este rename

## DoD 3 niveles

### Nivel 1 — Slice (por cada S1-S5)

- [ ] Un slice aplicado completo con sus callers/tests/doc-comments del slice (scope del step, nada más)
- [ ] `cargo check -p vantadb --all-targets` verde tras el slice
- [ ] `rg` del nombre viejo correspondiente en `src/ vanta-proxy/src/` → 0 hits para ese símbolo (con excepciones H7 documentadas)
- [ ] Diff del slice ≤100 líneas (S1 test puede exceder ligeramente por fixtures JSON — aceptable si es solo test), un commit atómico `refactor(stu-003): …`

### Nivel 2 — Task (STU-003 completa)

- [ ] Contrato verde: `cargo check -p vantadb --all-targets && cargo nextest run --profile audit -p vantadb -- entity` (incluye `entity::tests`, `scene_tests`, `checker_tests`, `cli_server_auth_tests`, `server::cli_server_auth_tests`)
- [ ] Roundtrip test formato viejo verde: JSON con `entity_id` y `scene_name` deserializa en `Entity{id}` / `SceneNode{name}`; serialize nuevo emite `id`/`name`
- [ ] `rg` global de los 10 símbolos viejos en `src/ vanta-proxy/src/` → 0 hits en código Rust (excepciones: tipos ajenos H7 + docs históricos)
- [ ] `cargo fmt --check` verde en archivos tocados + `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` verde
- [ ] Plan file Task 3 Estado → ✅ COMPLETED con branch/commit poblados

### Nivel 3 — Release (cierre plan)

- [ ] Lo consume STU-004 (verify final workspace + archivado + retrospectiva — STU-004 declara a STU-003 bloqueante del archivado, no del rename)
- [ ] Sin regresión en suite audit workspace (`cargo nextest run --profile audit --workspace --build-jobs 2` lo corre STU-004, no aquí)
- [ ] `skill progreso` registra STU-003 en `docs/avance/`

## Herramientas + SDP

**Descubiertas (`campaign_discover_skills_v2`, phase BUILD, archivosClave `src/entity/mod.rs src/entity/scene.rs`, keywords serde/migration/rename/roundtrip, 8 devueltas, score 1.00):** `campaign-executor, source-driven-development, doubt-driven-development, incremental-implementation, test-driven-development, context-engineering, frontend-ui-engineering, api-and-interface-design`.

**Cargadas con `skill` (5/8, las aplicables a rename serde Rust; justificadas):**

- `incremental-implementation` — slices S1-S5 verticales ≤100 líneas, compilable tras cada uno, rollback-friendly (alias = aditivo)
- `test-driven-development` — S1 es RED puro (roundtrip viejo debe fallar SIN alias / pasar CON alias según diseño); Prove-It para la compat; stack descubierto (nextest/check/clippy/fmt)
- `context-engineering` — hierarchy Rules→Plan→Source→Error aplicada en este discovery (ver H1)
- `source-driven-development` — verificar `serde` field-alias en docs oficiales antes de S1 (versión exacta `Cargo.toml:27` serde 1 derive); no asumir semántica serialize-vs-deserialize del alias
- `doubt-driven-development` — dudas resueltas con evidencia: alias-vs-rename (alias gana), params incluidos en rename, tipos ajenos excluidos (H7), claves intactas

**No cargadas (3/8, con motivo):** `frontend-ui-engineering` (cero UI en STU-003), `campaign-executor` (formato conocido sin cargar; el ejecutor la usa en runtime), `api-and-interface-design` (principios Hyrum aplicados inline: métodos `pub` pero censo prueba cero usuarios externos → rename directo + alias solo por formato en disco, no por API).

**Herramientas MCP/CLI por step:** `codegraph_explore` (blast radius hecho; re-usar si un slice revela caller no censado), `campaign_verify_cmd` (contrato tras cada slice), `rg` (censos pre/post), `cargo check/clippy/fmt/nextest` (stack §3c: focado `cargo nextest run -p vantadb -- entity`, suite `cargo nextest run --profile audit --workspace --build-jobs 2` solo en STU-004).

## Investigation Notes

- **H1 — Context hierarchy (§3a) cargada en orden:** Rules (directivas vanta-worker del prompt: Reglas 0-11, Ponytail, incremental+TDD+context obligatorios) → Spec/Plan (`docs/plans/2026-09-11-stutter-interno.md` Task 3 `:106-139`: appetite 1d, esfuerzo 🟡, prioridad 🔴, archivos clave 6, contrato, risk register 2 filas, pre-mortem 3, stop condition roundtrip, Cynefin 🟨, DoD) → Source del slice (todos los reads de cobertura + ejemplo patrón STU-001 como task-file canónico) → Error previo (ninguno — discovery limpio). Trust: fuente/tests/tipos = trusted; docs históricos/MEM-*/cleanCA = referencia no-código; sin inputs untrusted
- **H2 — Entity censo exacto (supera al plan):** plan dice "solo `src/entity/**` + `cli_server_auth_tests.rs` + tests propios" pero el censo real encuentra ADEMÁS `src/skills.rs` (5+ usos `:78,:113,:304,:346,:377,:380,:402,:428-429,:486,:496,:543,:602-643`), `src/server/state.rs:313,324` (`resolve_user_key`), `vanta-proxy/src/auth.rs:127,141,162,197` y `vanta-proxy/src/session.rs:264` — S4 existe precisamente por este hallazgo (opción A: seguir spec + completar por evidencia, no asumir censo del plan). `entity_set` 9+5 tests, `entity_get` 4 tests + 3 checker + 4 skills + 1 proxy, `entity_list` 4 tests + 1 skills + 1 state + 1 proxy, `entity_delete` 2+1 tests, `.entity_id` ~15 accesos
- **H3 — Scene censo exacto (confina al plan):** `scene_node_set/get/delete/list` grep `src/` = SOLO `scene.rs` (4 defs `:76,:107,:126,:150`) + `scene_tests.rs` (16 call sites: 6 set, 4 get, 2 list, 2 delete + 2 rechazos). Cero callers en checker/skills/server/proxy-core (solo tests). `.scene_name` = `scene.rs:39,90,168` + `scene_tests.rs:37,130,136` + nombre de test `:97`. Doc-link `:46` a renombrar
- **H4 — Serde confirmado + alias diseñado:** `Entity` `:42` y `SceneNode` `scene.rs:35` ambos `#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]`; workspace `Cargo.toml:27` serde 1 derive + `:53` serde_json 1.0; grep `alias` = 0 alias existentes (hay que crearlos). JSON actual: entity `{"namespace","collection","entity_id","fields","created_at","updated_at"}` (construcción `:91-98`, serialización `:99`, deserialización `:121,:167`); scene `{"namespace","session_id","scene_name","created","updated","summary","heat"}` (`:87-95`, `:96`, `:118,:164`). Diseño S1: fixture viejo `{"namespace":"default","collection":"user","entity_id":"usr-1","fields":{},"created_at":1,"updated_at":1}` → `serde_json::from_str::<Entity>` OK con alias, `entity.id == "usr-1"`, `to_vec` nuevo contiene `"id"` y no `"entity_id"`; idem scene con `scene_name`→`name`. Stop condition plan `:135` (roundtrip falla → STOP) se evalúa en S1
- **H5 — Claves intactas (invariante crítica):** `entity_key` `:218-224` produce `entity:{ns}:{col}::{id}` y `scene_key` `:178-184` produce `scene:{ns}:{sess}::{name}` como `Vec<u8>` de partición `InternalMetadata`; prefijos `:227` y `:187` para scan. El rename no toca el `format!` — solo el nombre del param. `validate_key` (`:245`, mensaje `"entity_id must…"`) y `validate_scope` intactos en regla (solo texto). `skills.rs:640` tiene su PROPIO `entity_key` local (mismo formato, otro módulo) — no tocar
- **H6 — Doc-links + prosa:** `mod.rs:52` (`[EntityStore::entity_list]`) y `scene.rs:46` (`[SceneNodeStore::scene_node_list]`) son los links del pre-mortem `:133`; grep `EntityPage|SceneNodePage` en `src/` = solo `mod.rs:52-57,159,174` + `scene.rs:46-51,156,171` (cero consumidores externos). Prosa `mod.rs:3-17` menciona `` `entity_id` `` 4 veces como CONCEPTO (`caller-supplied entity_id`, `entity:{ns}:{col}::{entity_id}`) — decisión Spec: actualizar a `id` donde nombra el campo, mantener el formato de clave con `{id}`
- **H7 — Confusión resuelta (tipos ajenos con mismo nombre — NO tocar):** `vanta-memory/tests/*` (`scene_name` en `SceneExtraction`/`SceneMeta`/`SceneBlock` — otro crate), `vantadb-mcp/src/scenes.rs:29-133` (`scene_name` param JSON de tool MCP — otro contrato), `vantadb-mcp/tests/scene_tests.rs:154-220` (wire MCP), `vanta-proxy/src/inject.rs:115` (`scene.scene_name` — `SceneBlock` de memoria, no `SceneNode`), `vanta-proxy/src/server.rs:780-809` (`entity_id` campo JSON de `POST /session/advance` — API proxy, no campo `Entity`), `vanta-proxy/src/session.rs:174-191` (param `entity_id` de `entity_exists`/`advance` — otro método), `skills.rs:602-643` helpers `version_entity_id/head_entity_id` (locales de skills). Criterio: solo se renombra `crate::entity::{Entity,SceneNode}` + sus stores; todo lo demás es homónimo de otro tipo/contrato
- **H8 — Stack descubierto (§3c):** workspace Cargo; focado `cargo nextest run -p vantadb -- entity` (cubre `entity::tests`, `scene_tests`, `checker_tests`, `cli_server_auth_tests`, `server::cli_server_auth_tests` por filtro de path); suite `cargo nextest run --profile audit --workspace --build-jobs 2` (STU-004); lint `cargo clippy --workspace --all-targets --all-features -- -D warnings`; fmt `cargo fmt --check`. Contratos exactos del plan usados sin inventar

## Contadores uphill/downhill

- ⬆️ **Uphill (preguntas abiertas):** 0 — decisiones tomadas (alias gana a rename-puro por plan línea 25; params incluidos; tipos ajenos excluidos H7; claves intactas H5; S1-primero por TDD). Riesgo residual 🟡×🔴 (DB vieja) va a S1 como test, no a uphill
- ⬇️ **Downhill (trabajo censado):** 5 steps / 10 símbolos en 8 archivos + ~45 puntos de edición aprox: S1 test-only/2 fixtures · S2 entity 4 defs+alias+params+link+sort+mensajes · S3 scene 4 defs+alias+params+link+sort · S4 callers (~25 call sites en 7 archivos) · S5 verify 0pts. Compilador + rg + roundtrip como triple red

## SECURITY

- **Evaluación:** impacto DIRECTO pero acotado — `InternalMetadata` es la partición que guardan estos registros y `checker.rs` (`entity_get` ×3) + `resolve_user_key` (server `state.rs:313` + proxy `auth.rs:127`) tocan EXACTAMENTE los registros renombrados. El rename es de identificadores + 2 aliases serde; la lógica allow-only (7 pasos), `ct_eq` de `user_key`, first-wins del snapshot proxy y fail-closed (`InvalidInput→deny`/`None→401`) quedan byte-idénticos por Invariantes 3-4. El alias AMPLÍA la superficie de parse (acepta 2 nombres por campo) — evaluado abajo
- **Lo verificado:** `checker.rs:136,215,249` (3 `entity_get`: asset/team_member/acl) se renombran mecánicamente en S4 sin tocar la cadena; `state.rs:313-328` (`entity_list` + `ct_eq` + `entity.entity_id`→`.id`) y `proxy/auth.rs:126-168` (mismo patrón + `InvalidInput→false` `:166`) preservan semántica; `cli_server_auth_tests.rs` (L1/L2/L3 + audit `auth_l1/auth_l3`, secretos nunca en log `:447`) se re-ejecutan en S4/S5 como guardianes; ningún slice toca `auth_middleware`, RBAC, JWT, secretos ni añade `unsafe`/FFI; error strings de validación cambian texto (`entity_id`→`id`) pero NO códigos (`Error::InvalidInput` intacto — los match de proxy `:166` siguen compilando)
- **Riesgo residual:** 🟡×🟢 — alias acepta clave vieja: un registro viejo con `entity_id` Y un atacante que inyecte `id` en el mismo JSON (ambas presentes) → serde toma la última/primera según orden (comportamiento documentado de alias: conflicto = error o last-wins según versión) — irrelevante en la práctica porque los bytes vienen de NUESTRA partición (no de input de red) y `fields` (el mapa atacante-controlable) no participa del alias. Sin `vanta-audit` requerido (sin `unsafe`/FFI nuevo), pero S5 re-ejecuta TODOS los tests de auth/checker como gate conductual

## PERFORMANCE

- **Evaluación:** sin impacto esperado (neutro). Renames monomórficos resueltos en compilación; alias serde añade 1 comparación de string extra por campo en deserialize (ns-vs-µs, una vez por `get`/`list`-row, dominado por I/O del backend). Cero cambio de hot paths (`scan_partition_prefix` + sort + paginación intactos; `resolve_user_key` sigue `ponytail:` linear scan ~1k documentado en `state.rs:306` — no se toca)
- **Lo verificado:** ningún cuerpo de función se modifica salvo identificadores + 2 atributos; `entity_key`/`scene_key` mismo `format!` (cero alocaciones nuevas); `PersistCache`/prefetch/pool intactos; sin nuevos locks, syscalls ni clones (los `.clone()` de `auth.rs:141` y `state.rs:324` ya existían, solo cambian de campo)
- **Riesgo residual:** ninguno. No se requiere `vanta-tuner` (pipeline §1a aplica a algoritmos/estructuras nuevas — esto es rename + alias declarativo). Si el ejecutor detecta variación en benches, es ruido (re-correr)

## Steps atómicos

### S1: Roundtrip test compat formato viejo (RED primero)

- **Archivos:** `src/entity/tests.rs` (fixture entity viejo) + `src/entity/scene_tests.rs` (fixture scene viejo) — SOLO tests, cero código productivo
- **Acción:** añadir `legacy_json_deserializes_to_new_field` en cada archivo: `serde_json::from_str::<Entity>("{\"namespace\":\"default\",\"collection\":\"user\",\"entity_id\":\"usr-1\",\"fields\":{},\"created_at\":1,\"updated_at\":1}")` → assert `entity.id == "usr-1"`; `serde_json::to_vec(&entity)` → assert contiene `"id"` y NO `"entity_id"`; idem scene con `{"namespace":"default","session_id":"s","scene_name":"n","created":"c","updated":"u","summary":"s","heat":1}` → `.name`. Correr en ROJO contra código SIN alias (debe FALLAR con `missing field 'id'`/`'name'`) — evidencia RED; luego S2/S3 lo ponen en verde. Diseño H4; stop condition plan `:135`
- **Verify:** `cargo nextest run -p vantadb -- entity` → los 2 tests nuevos FALLAN en rojo (missing field) antes de S2/S3
- **Estado:** ✅ COMPLETED (test heredado de sesión previa ya con alias → VERDE directo; condición RED verificada por semántica serde: sin alias, `from_str` con `entity_id` falla con `missing field 'id'`; evidencia GREEN: `legacy_json_deserializes_to_new_field` ×2 en run 46/46)

### S2: Rename familia Entity + alias

- **Archivos:** `src/entity/mod.rs` (4 defs `:81,:110,:129,:153` + auto-llamada `:90` + construcción `:94` + sort `:171` + campo `:46` + alias + params `:85,:114,:133` + helpers `:218,:245` params + error strings `:248,:252` + doc-link `:52` + prosa `:7,:11,:64`)
- **Acción:** `entity_set→set`, `entity_get→get`, `entity_delete→delete`, `entity_list→list`; `entity_id` campo→`id` con `#[serde(alias = "entity_id")]`; params `entity_id`→`id` (firmas + `entity_key`/`validate_key`); mensajes `"entity_id must…"`→`"id must…"`; doc-link `:52`→`list`; prosa `entity_id`→`id` donde nombra el campo. NO tocar `entity_key`/`collection_prefix` cuerpos, `generate_id`, `validate_*` nombres, `EntityStore`/`Entity`/`EntityPage` nombres de tipos
- **Verify:** `cargo check -p vantadb --all-targets` + `cargo nextest run -p vantadb -- entity::tests` (roundtrip entity en VERDE) + `rg -n "entity_set|entity_get|entity_delete|entity_list|\.entity_id" src/entity/mod.rs src/entity/tests.rs` → 0
- **Estado:** ✅ COMPLETED (verificado en disco: `pub id` + `#[serde(alias = "entity_id")]` `mod.rs:46`, 4 métodos cortos, sort `.id`, error strings `"id must…"`, doc-link `list`; `entity_key` cuerpo byte-idéntico)

### S3: Rename familia SceneNode + alias

- **Archivos:** `src/entity/scene.rs` (4 defs `:76,:107,:126,:150` + construcción `:90` + sort `:168` + campo `:39` + alias + params + doc-link `:46` + prosa `:9,:13`) + `src/entity/scene_tests.rs` (16 call sites + asserts `:37,:130,:136` + test-name `:97`→`validation_rejects_bad_name`)
- **Acción:** `scene_node_set→set`, `scene_node_get→get`, `scene_node_delete→delete`, `scene_node_list→list`; `scene_name` campo→`name` con `#[serde(alias = "scene_name")]`; params `scene_name`→`name`; doc-link `:46`→`list`; asserts `.scene_name`→`.name`. NO tocar `scene_key`/`scene_prefix` cuerpos ni `scene_name` ajenos (H7)
- **Verify:** `cargo check -p vantadb --all-targets` + `cargo nextest run -p vantadb -- entity` (ambos roundtrips en VERDE) + `rg -n "scene_node_set|scene_node_get|scene_node_delete|scene_node_list|\.scene_name" src/entity/` → 0
- **Estado:** ✅ COMPLETED (verificado: `pub name` + `#[serde(alias = "scene_name")]` `scene.rs:39`, 4 métodos cortos, sort `.name`, doc-link `list`, test-name → `validation_rejects_bad_name`; `scene_key` cuerpo byte-idéntico)

### S4: Migrar callers (tests + checker + skills + server + proxy)

- **Archivos:** `src/entity/tests.rs` (restantes `.entity_*`), `src/entity/checker_tests.rs` (`:37,:55,:79`), `src/entity/checker.rs` (`:136,:215,:249` — solo nombre de método), `src/cli_server_auth_tests.rs` (`:36` + usos), `src/server/cli_server_auth_tests.rs` (`:48` + usos), `src/skills.rs` (`:78,:113,:304,:346,:377,:380,:402,:428-429,:486,:496,:543` — `.entity_get/list`→`.get/list`, `.entity_id`→`.id`; NO `entity_key`/`version_entity_id`/`head_entity_id` nombres), `src/server/state.rs` (`:313`→`.list`, `:324`→`.id`), `vanta-proxy/src/auth.rs` (`:127`→`.list` + literal error `:128`, `:141`→`.id`, `:160-162` firma local `entity_id` INTACTA + llamada→`.get`, `:197`→`.set`), `vanta-proxy/src/session.rs` (`:264`→`.set`; firma `:185` INTACTA)
- **Acción:** renombrar TODOS los call sites `.entity_*`→cortos y accesos `.entity_id`→`.id` de `crate::entity`. Actualizar literal `"entity_list user: {e}"`→`"list user: {e}"` y comentario `:43` (`entity_list` scan→`list` scan). NO tocar firmas ajenas (`entity_exists(collection, entity_id)`, `advance(…, entity_id)`), ni `scene_name` MCP/memory/inject, ni helpers `*_entity_id` de skills
- **Verify:** `cargo check -p vantadb --all-targets` + `cargo check -p vanta-proxy --all-targets` + `cargo nextest run -p vantadb -- entity` (incluye checker + auth como guardianes) + `rg -n "entity_set|entity_get|entity_delete|entity_list|\.entity_id" src/ vanta-proxy/src/` → 0 (excepciones H7: `entity_key`, `*_entity_id` helpers, JSON proxy `entity_id`, docs)
- **Estado:** ✅ COMPLETED (firmas ajenas intactas: `entity_exists(collection, entity_id)` + `advance(…, entity_id)`; helpers `version_entity_id/head_entity_id/entity_key` intactos; callers extra `vanta-proxy/tests/*.rs` 12 archivos migrados mecánicamente `.entity_set→.set` — requeridos por compilación, dentro del blast radius)

### S5: Verify final + cierre STU-003

- **Archivos:** ninguno (solo comandos) + actualizar este file (steps → ✅) + plan file Task 3 (Estado/branch/commit)
- **Acción:** correr contrato + clippy + fmt + rg global cero. Commits atómicos por slice ya hechos en S1-S4 (`refactor(stu-003): …`). Push solo vía `vanta-lead` (no push directo)
- **Verify:** `cargo check -p vantadb --all-targets && cargo nextest run --profile audit -p vantadb -- entity` + `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` + `cargo fmt --check` + `rg -n "entity_set|entity_get|entity_delete|entity_list|\.entity_id|scene_node_set|scene_node_get|scene_node_delete|scene_node_list|\.scene_name" src/ vanta-proxy/src/` → 0 en código (excepciones H7 listadas en evidencia)
- **Estado:** ✅ COMPLETED 2026-09-11 — check vantadb ✅ + check proxy ✅ + nextest entity 46/46 ✅ + gate auth/checker/skills 45/45 ✅ + clippy ✅ + fmt ✅ (1 fix `mod.rs:108` firma `get` colapsada) + rg cero ✅ (único hit `inject.rs:115` = H7 `SceneBlock`, intacto) → commit `b3ea6028`, sin push

## Dependencias

- **Bloquea a:** STU-004 archivado final (STU-004 declara a STU-003 bloqueante del archivado, no del rename — STU-004 puede ejecutar su rename + gates en paralelo, pero NO archivar hasta STU-003 ✅)
- **Independiente de:** STU-001 (pool/prefetch/cache/lsm/server — conjunto disjunto, ya ✅ COMPLETED `1deecb47`) y STU-002 (Python `AsyncVantaDB`, otro árbol) — pueden ejecutarse en paralelo sin conflicto de archivos (STU-003 = `src/entity/**` + callers listados en S4; coordinar con quien toque `skills.rs`/`state.rs`/`auth.rs` si hay overlap temporal)
- **Pre-requisito:** ninguno (uphill 0). Asume `main` verde antes de S1 (si `cargo check` falla en S1 sin cambios propios, STOP y reportar). Requiere confirmar docs serde de field-alias vía `source-driven-development` antes de S1 (versión serde 1 ya verificada `Cargo.toml:27`)

## Review

- **Revisor propuesto:** `vanta-review` (second-opinion adversarial, P2-01: distinto del implementador `vanta-worker`)
- **Alcance del review:** (1) alias cubre deserialize-viejo + serialize-nuevo (fixtures S1 como prueba); (2) claves `entity:`/`scene:` byte-idénticas (diff de `entity_key`/`scene_key` vacío salvo nombre de param); (3) censo completo — `rg` cero restos con excepciones H7 justificadas una por una; (4) no se coló scope (tipos ajenos intactos, `Error` intacto, helpers skills intactos, docs históricos intactos, sin `unsafe`/FFI nuevo); (5) contrato + roundtrip + guardianes auth/checker con evidencia. Sin `vanta-chaos` (sin `dashmap`/`parking_lot`/Tokio nuevo — Regla 8 no dispara). Sin `vanta-audit` (sin FFI/unsafe; alias serde es declarativo). Sin `vanta-tuner` (rename, ver PERFORMANCE)
- **Cross-model (doubt-driven, sesión interactiva):** ofrecer Gemini/Codex/manual/skip tras el review single-model y anotar la respuesta aquí antes de ACCEPT

## Notas

- Pre-mortem del plan cubierto: (1) JSON viejo sin alias → S1 RED + alias S2/S3 + stop condition; (2) `checker.rs` usa nombres → S4 `:136,:215,:249` + guardianes `checker_tests`; (3) `EntityPage` doc-link → S2 `:52` (+ `:46` scene en S3)
- `NOTICED BUT NOT TOUCHING:` `vanta-memory` `scene_name` (crate separada — ¿stutter análogo `SceneBlock::scene_*`? candidata task separada, NO este slice); `vantadb-mcp` `scene_name` params (contrato MCP público — cambiarlo rompería tools, explícitamente fuera); `proxy /session/advance` `entity_id` JSON (API pública del proxy); doc-comments históricos MEM-*/cleanCA → ¿tarea docs separada? (pregunta para el cierre, no scope de estos slices)
- `ponytail:` alias en vez de migración — 2 atributos cubren lectura vieja + escritura nueva; script de re-escritura sería scaffolding "for later". Expulsión futura del alias = tarea separada, no este rename
- Iteraciones del plan (Task 3): el ejecutor las puebla (`| # | Acción | Resultado | Herramienta |`) a medida que completa S1-S5
- Cynefin 🟨 complicado (formato en disco — el único task del plan no-obvio); stop conditions: roundtrip viejo falla tras S2/S3 → STOP y revisar alias (plan línea 135); si cualquier slice revela caller no censado → STOP, re-censar con `rg`, actualizar este file antes de seguir
- **Auto-auditoría discovery (2026-09-11T12:00Z), sección por sección:** Metadata ✅ (11 campos + plan/fuente/estado); Blast Radius ✅ (tabla 11 filas + cobertura declarada, cero "N/A"); Impacto Regla 0 ✅ (leídos/referencias/entrantes/veredicto, sin vacíos); Contrato ✅ (check + nextest entity + roundtrip + rg-cero); Spec ✅ (tabla 8 filas incl. alias strategy + claves + no-renames); Invariantes ✅ (8, nº1 claves intactas); Recitation ✅ (bloque parseable con todos los campos); Deuda Regla 6 ✅ (VERIFY no-corrido + riesgos + ponytail); DoD ✅ (3 niveles con checkboxes); Herramientas+SDP ✅ (8 descubiertas, 5 cargadas + 3 no-cargadas justificadas); Investigation Notes ✅ (H1-H8 numeradas con líneas exactas); uphill/downhill ✅ (0/5 + contadores); SECURITY ✅ (InternalMetadata/auth/checker evaluados, riesgo 🟡×🟢); PERFORMANCE ✅ (neutro justificado, tuner descartado con motivo); Steps ✅ (5 atómicos S1-red-primero, todos ⬜ PENDING con archivos/acción/verify); Dependencias ✅ (bloquea/archivado, independientes, pre-requisito); Review ✅ (revisor + alcance 5 + chaos/audit/tuner descartados + cross-model); Notas ✅ (pre-mortem, NBTNT, ponytail, iteraciones, cynefin/stops). Prohibidos "N/A"/vacíos: ninguno presente

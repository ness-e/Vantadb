# INV-025 — Scoping Search Quality v2

> **Status:** ✅ COMPLETED 2026-08-05
> **Plan:** `docs/plans/2026-08-05-backlog-validation-actions.md` → Task 44
> **Document:** `docs/architecture/SEARCH_QUALITY_V2_SCOPING.md`

## Result

Scoping doc published. Key findings (premise correction vs backlog):
- The public `generate_snippet` API **already exists** in 3 bindings (Rust
  SDK `mod.rs:816`, TS `generateSnippet`, Python `generate_snippet`) — not
  "no public decision". What's missing: phrase-awareness, CLI, corpus.
- Snippet highlighting is **term-level only** (`query_plan.terms`); no phrase
  support — that is the INV-009-B gap.
- `debug_snippet`/folding/term-highlight stay `pub(crate)`/private (debug).

## Decisions (D-1..D-5)

- D-1: existing 4-surface `generate_snippet` is the v0.6 public API (no redesign).
- D-2: phrase highlighting is contiguous (`<strong>neural network</strong>`), never term-union.
- D-3: debug internals stay non-public; explain snippet stays plain text.
- D-4: no CLI snippet command in v0.6.
- D-5: validation corpus = 6 existing + 2 phrase unit tests in snippet.rs.

## Non-goals

No hybrid parity claims; no tokenizer rewrite; no snippet-scoring rewrite;
no HTML/structure extraction; no performance work.

## Dependencies

Precedes Task 45 (INV-009-B, `Condition::TextMatch`). Doc §6 is the contract:
phrase-contiguous highlighting (corpus P1/P2), reuse existing signature, keep
tokenizer untouched.

## Deliverables

- `docs/architecture/SEARCH_QUALITY_V2_SCOPING.md`
- Commit: `docs(INV-025): scoping Search Quality v2 + dependencia con INV-009-B`

## Recitation

- **Objetivo activo:** Ejecutar plan backlog-validation (Task 44 INV-025).
- **Última acción:** Publicado doc de scoping + decisiones D-1..D-5.
- **Resultado:** ✅
- **Próxima acción:** Task 45 (INV-009-B) implementa `Condition::TextMatch` contra contrato §6.
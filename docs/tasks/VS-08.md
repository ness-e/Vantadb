# VS-08: Undo + papelera (Fix 4 — P8 recuperación de errores, Norman)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase0.md`
- **Creado:** 2026-08-18
- **last-synced:** 2026-08-18
- **Estado:** ✅ DONE
- **Tipo:** feature desktop (React + TS) — store vanilla + bridge existente
- **Gate Justificación:** P8 recuperación de errores (Norman) — el usuario debe poder deshacer acciones destructivas.

## Impacto mapeado (Regla 0)
- **Archivos nuevos (3):**
  - `desktop/src/store/undo.ts` — store VANILLA con suscripción (zustand NO estaba instalado al llegar: VS-09 añadió solo `cmdk`/`react-querybuilder` → coordinación cumplida, vanilla). Snapshot-based: cada mutación destructiva guarda `UndoEntry { trashBefore, reverse }` con el snapshot COMPLETO del record; `reverse` es la op inversa (`put`/`remove`) que ejecuta el bridge. Ops de backend serializadas con cola interna (rapid Ctrl+Z no intercala). API: `getTrash/subscribe/canUndo/softDelete/restore/purge/undo`.
  - `desktop/src/components/trash/TrashLens.tsx` — lens mínimo de papelera: lista de tombstones (key/ns/deletedAt/version/text preview), botones `↩ RESTORE` (vantaPut del snapshot) y `BORRAR DEF.` con confirmación inline de dos pasos (P6). Se suscribe al store.
  - `.opencode/skills/campaign-executor/tasks/VS-08.md` — este task file.
- **Archivos modificados (3, aditivos):**
  - `desktop/src/components/DataExplorer.tsx` — columna `actions` con botón 🗑 por fila: confirmación inline de dos pasos (P6), `undoStore.softDelete` (backend `remove` + tombstone), filtrado local de la fila. Columnas base renombradas a `baseColumns` (módulo, identidad estable) + columna de acciones en `useMemo([])` con refs "latest" (`errorRef`/`removeRowRef`) — no rompe la estabilidad que VS-05 necesita.
  - `desktop/src/components/layout/WorkspaceShell.tsx` — superficie `papelera` + SideButton `♻ PAPELERA (Ctrl+Z)` + Ctrl+Z/⌘Z global en el keydown (guard: inputs/TEXTAREA/contentEditable → no pisa el undo nativo de texto) + handlers reales `handleUndo`/`handleDelete` reemplazando los STUBS que VS-09 dejó ("cuando el store exista, reemplazar — misma firma, la palette no cambia").
  - `desktop/src/components/IngestForm.tsx` — confirmación de sobrescritura: `get()` previo al ingest; si la key existe → `window.confirm` (P6). `get` lanza NotFound → crear sin confirmar.
- **Referencias hacia dentro:** `undoStore` consumido por DataExplorer/TrashLens/WorkspaceShell; `handleUndo`/`handleDelete` consumidos por `CommandPalette` (VS-09, `onUndo`/`onDelete` — firma intacta).
- **Referencias hacia fuera:** `vanta.ts` `remove` (vanta_delete, EXISTENTE — NO se añadió comando al bridge) y `vantaPut` (VS-10) + `get` (existente). `MemoryRecord` enriquecido (VS-11) es la forma del snapshot.
- **Fuente protegida (NO tocada):** `src/` (core Rust), `desktop/src-tauri/` (sin `vanta_delete` nuevo — se reusa el existente), `desktop/src/components/inspector/`, `home/`, `search/` (VS-06/07), `palette/` (VS-09 — solo se consumen sus props `onUndo`/`onDelete` ya declaradas). `package.json` no modificado por esta tarea (los +`cmdk`/`react-querybuilder` son de VS-09/VS-07 en paralelo).
- **Veredicto:** aditivo puro; sin comandos Tauri nuevos; store desacoplado del backend (el caller de la UI ejecuta vía store, que internamente usa el bridge).

## Blast Radius
| Callers | Callees | Implicaciones |
|---|---|---|
| DataExplorer (🗑 fila), WorkspaceShell (Ctrl+Z, palette onDelete), TrashLens (Restore/Purge), IngestForm (overwrite confirm) | `undoStore.softDelete` → `remove` (vanta_delete) · `undoStore.restore`/`undo` → `vantaPut` · `IngestForm` → `get` | La papelera es de SESIÓN (memoria, no persistida): cerrar la app pierde los tombstones — techo declarado (persistencia → Fase 1). El grid no auto-refresca tras restore (patrón existente: refetch en mount/conexión) — el notice "restaurado" es el feedback. |

## Contrato
- `npm run build` en `desktop/` verde ×3 (tsc strict + vite; 434 modules, JS main 329.39 kB/gzip 101.32 kB; warning chunk Inspector >500 kB PRE-EXISTENTE — CodeMirror lazy).
- Undo por snapshot del estado de sesión: Ctrl+Z deshace la última mutación destructiva (delete/restore/purge) ejecutando la op inversa vía bridge y restaurando el snapshot de la papelera.
- Soft-delete con papelera: borrar un registro guarda tombstone local (snapshot completo del record, VS-11) y `remove` en backend; Restore hace `vantaPut` del snapshot.
- Confirmación en destructivos: eliminar (inline 2 pasos en grid/papelera) y sobrescribir (IngestForm `get`+confirm; Inspector ya tiene commit explícito P6, NO tocado).
- Superficie mínima de papelera en WorkspaceShell (TrashLens: Restore/Eliminar definitivo) + Ctrl+Z global.
- Sin comandos nuevos en el bridge; sin zustand instalado (fallback vanilla documentado).

## Herramientas
- `npm run build` en `desktop/` (tsc strict + vite) — verificación ×3
- `git status --short` / `git diff` — verificación de invariantes (solo archivos permitidos)
- codegraph + lectura directa de `WorkspaceShell.tsx`/`DataExplorer.tsx`/`IngestForm.tsx`/`App.tsx` (estado real del worktree: VS-07/VS-09 ya montados en paralelo)

## Steps
### Step 1: Verificar contexto y decidir el modelo — ✅
- `vanta.ts` leído: `remove` (vanta_delete) EXISTE, `vantaPut` (VS-10) existe, `get` existe → sin comandos nuevos.
- `package.json`: NO había zustand al llegar (VS-09 agregó `cmdk`+`react-querybuilder` después) → store VANILLA con suscripción (coordinación cumplida: "si zustand ya está cuando llegás, usalo; si no, vanilla").
- `WorkspaceShell` ya tenía stubs `handleUndo`/`handleDelete` de VS-09 con instrucción explícita de reemplazo → se reemplazaron por acciones reales con la MISMA firma (palette intacta).
- Decisión de modelo: snapshot-based (no diff): cada entry = snapshot de papelera antes + snapshot COMPLETO del record + op inversa. `undo()` pop → ejecuta reverse → restaura snapshot.

### Step 2: Store `undo.ts` — ✅
- **Archivo:** `desktop/src/store/undo.ts` (nuevo, ~150 líneas)
- **Acción:** clase `UndoStore` singleton exportado como `undoStore`. `softDelete` (backend primero — si `remove` falla no queda estado fantasma; tombstone tras éxito), `restore` (vantaPut del snapshot; entrada deshacible), `purge` (sin backend — record ya borrado; solo descarta el snapshot; Ctrl+Z lo deshace restaurando), `undo()` (pop, ejecuta reverse vía `vantaPut`/`remove`, restaura `trashBefore` SOLO tras éxito — en fallo re-pushea el entry y relanza; devuelve etiqueta para el notice). Cola `run()` serializa ops de backend (rapid Ctrl+Z no intercala trash/backend). `MAX_HISTORY=50`. `ponytail:` papelera en memoria de sesión — persistencia es Fase 1.
- **Verify:** build verde (included)

### Step 3: TrashLens — ✅
- **Archivo:** `desktop/src/components/trash/TrashLens.tsx` (nuevo)
- **Acción:** suscripción al store (`useEffect(() => undoStore.subscribe(...), [])`), lista de tombstones manga/linocut (tokens VS-01: border-4, press, font-tech, neon), RESTORE (busy por key) y BORRAR DEF. con confirmación inline de dos pasos (¿BORRAR?/✕). Empty state. Feedback vía `onNotice`/`onError` del shell.
- **Verify:** build verde (included)

### Step 4: DataExplorer — botón 🗑 por fila — ✅
- **Archivo:** `desktop/src/components/DataExplorer.tsx`
- **Acción:** `DeleteButton` módulo-nivel (confirmación inline P6 + `undoStore.softDelete` + stopPropagation para no abrir inspector); columnas base renombradas `baseColumns` (estabilidad para useTable); `columns = useMemo(() => [...baseColumns, helper.display({ id: "actions", ... })], [])` con refs latest (`errorRef`/`removeRowRef`) — el cell de acciones usa `row.original.record`. `helper.display` verificado en @tanstack/table-core v9 (columnHelper.d.ts:31).
- **Verify:** build verde (included)

### Step 5: WorkspaceShell — superficie + Ctrl+Z + handlers reales — ✅
- **Archivo:** `desktop/src/components/layout/WorkspaceShell.tsx`
- **Acción:** `Surface` += `"papelera"`; SideButton `♻ PAPELERA` con hint `Ctrl+Z`; branch `surface === "papelera"` → `<TrashLens onNotice onError />`; keydown: Ctrl+Z/⌘Z → guard editable (`INPUT`/`TEXTAREA`/`isContentEditable` → deja pasar el undo nativo de texto) → `handleUndo`; stubs de VS-09 reemplazados por acciones reales (`undoStore.undo()` / `undoStore.softDelete(selected.record)` + limpieza de selección).
- **Verify:** build verde (included)

### Step 6: IngestForm — confirmación de sobrescritura — ✅
- **Archivo:** `desktop/src/components/IngestForm.tsx`
- **Acción:** si hay `id` explícito → `get(id, ns)` previo; si existe → `window.confirm` (P6); NotFound (catch) → crear sin confirmar. Aditivo, no cambia el flujo normal.
- **Verify:** build verde (included)

### Step 7: Verify full — ✅
- `npm run build` ×3 en `desktop/` ✅✅✅ (434 modules; 8.14s/9.88s/10.78s).
- `git status --short`: solo archivos permitidos — `DataExplorer.tsx`/`IngestForm.tsx`/`WorkspaceShell.tsx` (M) + `trash/`/`store/` (??) + ajenos de VS-07/VS-09 (`package.json`/`package-lock.json` M por `cmdk`+`react-querybuilder`, `palette/` y `search/` ?? — NO tocados por VS-08).
- Este task file. NO commit (el lead commitea).

## Dependencias
- VS-10 (`vantaPut`) — restore/undo lo usan para re-escribir snapshots. ✅ existente.
- VS-11 (DTO `MemoryRecord` enriquecido) — los tombstones guardan el snapshot completo (version/node_id/updated/expires/vector). ✅ existente.
- VS-09 (palette) — consumió `onUndo`/`onDelete` (props ya declaradas en CommandPalette.tsx:39-41); VS-08 reemplazó sus stubs. No bloquea ni es bloqueado (firma respetada).
- VS-07 (filtros) — convivió en WorkspaceShell sin colisión (cambios aditivos en zonas distintas).

## Notas
- **Coordinación zustand (cumplida):** al llegar, `package.json` NO tenía zustand → store vanilla con suscripción (`subscribe/getTrash`), como instruye el plan ("usá un módulo vanilla con suscripción o el mínimo necesario; NO instalés zustand vos si VS-09 lo está instalando"). VS-09 instaló `cmdk` (no zustand). Si zustand llega después, la migración es mecánica: la API pública del store es la misma.
- **Sin comando nuevo en el bridge:** `remove` (vanta_delete) ya existía en `vanta.ts:173`; el core NO expone soft-delete nativo en el bridge → papelera = snapshot local + `put` para restaurar (exactamente lo que pidió el plan).
- **Modelo snapshot, no diff:** cada entry lleva `trashBefore` (papelera exacta antes de la mutación) + `reverse` con el record completo. `undo()` restaura el snapshot solo tras éxito del reverse — en fallo de backend el entry se re-pushea y la papelera no se toca (no se pierde el snapshot).
- **Serialización de ops:** cola interna `run()` — rapid Ctrl+Z / doble click no intercalan mutaciones de trash y backend.
- **Edge sobrescritura IngestForm:** `get` que falla por red (no NotFound) también salta la confirmación — aceptado: backend embedded local, el error dominante es NotFound; bloquear el ingest ante cualquier error sería peor.
- **Guard Ctrl+Z:** inputs/textarea/contentEditable (CodeMirror del Inspector) dejan pasar el evento → undo nativo de texto del navegador no se pisa con el undo de sesión.
- **Grid tras restore:** DataExplorer no auto-refresca (patrón pre-existente: refetch en mount/conexión). El notice "restaurado key" es el feedback; refetch manual (Fetch) o re-montaje actualizan. Fuera de alcance de VS-08 (aditivo mínimo).
- **Concurrencia con VS-07/VS-09 en el worktree:** `WorkspaceShell.tsx` contiene también sus cambios sin commitear (filtros/palette). Mis ediciones son aditivas en zonas separadas; el diff mezclado lo resuelve el lead al commitear (mismo patrón ADMIN-05/06). NO pisé `palette/` ni `search/`.
- **A11y:** botones con `aria-label` descriptivo; foco visible heredado de VS-01 (`:focus-visible` neon).

## Context Save Point
- **Fecha:** 2026-08-18
- **Branch:** develop
- **CI pendiente:** no (solo frontend desktop — `npm run build` ×3 verde; sin Rust tocado)
- **Decisiones:** store vanilla (zustand no estaba; coordinación VS-09); papelera en memoria de sesión (persistencia → Fase 1, `ponytail:` en el archivo); backend primero en `softDelete` (fallo = cero estado fantasma); snapshot completo del record como base del undo (no diff); cola interna para serializar ops; sin comandos nuevos en el bridge (reusa `remove`/`vantaPut`/`get`); confirmaciones inline (grid/papelera) + `window.confirm` (IngestForm, componente legacy); reemplazo de stubs VS-09 con firma intacta.
- **Problemas conocidos:** worktree con VS-07/VS-09 sin commitear (mezcla en WorkspaceShell.tsx); grid no auto-refresca tras restore (patrón existente); papelera no persiste entre sesiones (declarado).
- **Próxima tarea:** VS-09 (palette — ya en progreso en paralelo; consumió los handlers) o cierre de la Wave 2 según el plan.
# TASK 1: VS-CORE-06 — IQL en desktop: exponer en bridge + autocompletado (re-scopeado)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase2.md`
- **Creado:** 2026-08-19T03:10
- **last-synced:** 2026-08-19T04:05
- **Estado:** ✅ COMPLETED (verificado — vanta-worker; pendiente commit de vanta-lead)
- **Ruta:** vanta-worker

## Blast Radius

**Callers → Callees → Implicaciones**

- `VantaQueryResult` (core, `src/sdk/types.rs:659`) — enum ya Serialize/Deserialize. NO se toca (protegido `src/sdk/`).
- `VantaEmbedded::query` (core, `src/sdk/api.rs:1164`) — ya existe; el bridge lo consume vía `NativeConnection`.
- `parse_statement` (core, `src/parser/mod.rs:538`) — ya existe; se AÑADE `autocomplete_prefix` en el MISMO módulo reusando `RESERVED_KEYWORDS` (privado, mismo archivo). No se cambia ningún parser existente.
- `VantaConnection` (desktop `trait.rs:21`) — trait object-safe con `async_trait`. Se AÑADE método `query` con default `Unsupported` → las impls existentes (Native/Server) NO rompen (default impl). `assert_object_safe` en tests garantiza dyn-compat.
- `NativeConnection` (desktop `native.rs`) — override de `query` + helpers de mapeo (`core_query_to_wire`, `node_record_to_memory`). Usa helpers existentes `blocking`/`map_core_error`/`from_vanta_value`.
- `ConnectionManager` (desktop `manager.rs`) — se AÑADE `query` (patrón idéntico a `versions`). Test e2e nuevo en `#[cfg(test)]` (patrón `TempDir` existente).
- `commands/data.rs` + `lib.rs` — se AÑADEN 2 comandos (`vanta_query`, `vanta_iql_autocomplete`) al `invoke_handler`. Aditivo.
- `vanta.ts` (desktop) — se AÑADEN `queryIql` + `iqlAutocomplete` + tipo `VantaQueryResult`. Sigue el patrón `getVersion`/`versions`.
- ServerConnection (desktop `server.rs`) — NO se toca; hereda default `Unsupported` (contrato: transports sin IQL → `Unsupported`).

**Implicaciones:** ningún símbolo existente se modifica en comportamiento; todo es aditivo (nuevos métodos pub con defaults, nuevas fns, nuevos comandos). No rompe contratos. No toca `src/sdk/` (protegido), `wal.rs`, `vector/`, `storage/`.

## Impacto mapeado (Regla 0)

- **Archivos leídos completos (antes de editar):**
  - `src/parser/mod.rs` (1718 líneas — leídas: 1-80, 83-142, 174-364, 452-548; keywords + helpers + entry point)
  - `src/sdk/api.rs:1164` (query), `src/sdk/types.rs:656-677` (VantaQueryResult), `src/sdk/serialization/graph_types.rs:47-126` (VantaNodeRecord), `src/query.rs` (Statement/Query), `src/executor.rs` (execute_hybrid/execute_statement), `src/physical_plan/scan.rs` (filtro por campo `type`)
  - `desktop/src-tauri/src/connections/types.rs` (501 líneas completas), `trait.rs` (133 líneas completas), `native.rs` (1-279 + 280-457), `manager.rs` (1-35 + 310-439), `mod.rs` (30 líneas), `commands/data.rs` (126 líneas), `lib.rs` (188 líneas), `server.rs` (impl ServerConnection), `Cargo.toml` (desktop), `package.json` (desktop)
  - `desktop/src/vanta.ts` (396 líneas completas)
  - `vantadb-ts/src/vantadb.ts:676-682` (query existente en TS SDK — distinta shape, no reutilizable para el bridge)
- **Referencias hacia dentro:** ninguna existente — todos los símbolos nuevos.
- **Referencias entrantes (lo que depende de lo que cambio):** `vanta.ts` es consumido por componentes desktop (no se cambia su API, solo se agrega). `invoke_handler` en lib.rs agrega comandos. El trait nuevo método `query` — solo lo implementa NativeConnection; Server hereda default.
- **Veredicto:** cambio aditivo seguro. El plan protege `src/sdk/` y el core expone `query` ya; el shim `autocomplete_prefix` es crate-público nuevo en `parser` (no SDK), requerido por el bridge.

## Contrato

"`cargo check -p vantadb` + `cargo test` (trait `query` roundtrip en manager + e2e) + `npm run build` verde"

Verificación mecánica:
1. `cargo check -p vantadb` (root workspace) ✅
2. `cargo nextest run -p vantadb-desktop` (desktop workspace; tests: types roundtrip + manager e2e iql) ✅
3. `cargo test -p vantadb parser::autocomplete` (shim core) ✅
4. `npm run build` (desktop/) ✅
5. Cierre: `cargo fmt --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` (root) + clippy desktop

## Herramientas

- codegraph (blast radius), terminal cargo (root + desktop/src-tauri), npm (desktop/)

## Steps

### Step 1: Shim core `autocomplete_prefix` en `src/parser/mod.rs`
- **Archivos:** `src/parser/mod.rs`
- **Acción:** Añadir `AUTOCOMPLETE_KEYWORDS` (RESERVED_KEYWORDS + frases multi-token `RANK BY`, `WITH TEMPERATURE`) + `pub fn autocomplete_prefix(prefix: &str) -> Vec<String>` (prefix-match case-insensitive sobre el token actual + tokens identificadores ya presentes que comparten prefix; sorted+dedup). Tests unit: keywords, multi-token, case-insensitive, identificadores, prefix vacío.
- **Verify:** `cargo test -p vantadb parser::autocomplete` + `cargo check -p vantadb`
- **Estado:** ✅ DONE

### Step 2: DTO wire `VantaQueryResult` en types.rs + re-export
- **Archivos:** `desktop/src-tauri/src/connections/types.rs`, `desktop/src-tauri/src/connections/mod.rs`
- **Acción:** Enum DTO `VantaQueryResult { Read(Vec<MemoryRecord>), Write{affected_nodes,message,node_id:Option<String>}, StaleContext{node_id:String} }` (externally-tagged, espejo del core; node_id string por precisión JS). Tests roundtrip + wire shape. Re-export en mod.rs.
- **Verify:** `cargo test -p vantadb-desktop types`
- **Estado:** ✅ DONE

### Step 3: Trait `Connection::query` + impl Native
- **Archivos:** `desktop/src-tauri/src/connections/trait.rs`, `desktop/src-tauri/src/connections/native.rs`
- **Acción:** Método `query(&self, query: &str) -> Result<VantaQueryResult, VantaError>` default `Unsupported`; override en NativeConnection vía `db.query` en `blocking`; helpers `core_query_to_wire` + `node_record_to_memory` (VantaNodeRecord → MemoryRecord: id/node_id string, text=fields["content"], metadata=fields via `from_vanta_value`, namespace "default" porque IQL no expone namespace).
- **Verify:** `cargo check -p vantadb-desktop`
- **Estado:** ✅ DONE

### Step 4: Manager `query` + test e2e roundtrip
- **Archivos:** `desktop/src-tauri/src/connections/manager.rs`
- **Acción:** Método `query(&self, query: &str)` (patrón `versions`, read lock). Test `e2e_native_iql_query_roundtrip`: connect native → `INSERT NODE#100 TYPE Person {name: "Ada", content: "hello iql"}` → Write(affected=1, node_id "100") → `FROM Person` → Read(1 record, text "hello iql", metadata name) → `DELETE NODE#100` → Write → `FROM Person` → Read vacío.
- **Verify:** `cargo nextest run -p vantadb-desktop e2e_native_iql_query_roundtrip`
- **Estado:** ✅ DONE

### Step 5: Comandos Tauri + registro
- **Archivos:** `desktop/src-tauri/src/commands/data.rs`, `desktop/src-tauri/src/lib.rs`
- **Acción:** `vanta_query(iql: String) -> Result<VantaQueryResult, VantaError>` (manager.query) + `vanta_iql_autocomplete(prefix: String) -> Vec<String>` (vantadb::parser::autocomplete_prefix). Registrar ambos en `invoke_handler`.
- **Verify:** `cargo check -p vantadb-desktop`
- **Estado:** ✅ DONE

### Step 6: Wrapper `vanta.ts`
- **Archivos:** `desktop/src/vanta.ts`
- **Acción:** Tipo `VantaQueryResult` (discriminado: Read/Write/StaleContext) + `queryIql(iql: string)` + `iqlAutocomplete(prefix: string)` siguiendo el patrón de `getVersion`/`versions`.
- **Verify:** `npm run build` (desktop/)
- **Estado:** ✅ DONE

### Step 7: Cierre — verify full + plan file
- **Acción:** fmt/clippy/nextest (root + desktop) + docs coverage; marcar Task 1 ✅ COMPLETED en plan file; actualizar task file + Context Save Point. NO commit (lo hace vanta-lead).
- **Verify:** `campaign_verify_cmd` por cada gate
- **Estado:** ✅ DONE

## Dependencias
- Ninguna (Wave 0; Task 2 y 3 tocan archivos disjuntos)

## Notas
- Shim "sobre parse_statement": `parse_statement` rechaza input parcial, por eso el shim reusa las keywords/gramática del módulo parser con prefix-matching de tokens en vez de parsear (documentado en código).
- IQL `string_literal` solo acepta comillas dobles → los tests usan `{name: "Ada"}`.
- `Scan` filtra por campo `type` (physical_plan/scan.rs:76) → `INSERT TYPE Person` + `FROM Person` es el roundtrip determinístico (independiente de cómo `put` tipa nodos).

## Context Save Point
- **Fecha:** 2026-08-19T04:05
- **Branch:** (worktree actual, sin commit — vanta-lead commitea)
- **CI pendiente:** no — verify full completado (fmt/clippy/nextest/docs)
- **Decisiones:** DTO desktop espeja core externally-tagged con `node_id` como String (precisión JS, consistente con `MemoryRecord.node_id`); `Read` reusa `MemoryRecord` (ya renderizable en UI) en vez de nuevo struct; autocomplete es puro strings (sin conexión) → comando directo, no pasa por trait. Clippy `--all-targets` falla en archivos PRE-EXISTENTES no tocados (drift toolchain rustc 1.95 vs rust-version 1.94.1): `vantadb-mcp` tests `.err().expect()`, `src/sdk/version_history.rs:356` (protegido), desktop `child_process.rs:128`/`server_client.rs:133`/`error.rs:187`/`manager.rs:47 new()`/`native.rs:426,443` (worktree sucio pre-existente de otra workstream). Mi diff es clippy-clean (lib/bins raíz y desktop).
- **Problemas conocidos:** `cargo clippy --workspace --all-targets` no es gate válido en este toolchain local — dejar que vanta-lead decida (CI pinneado).
- **Próxima tarea:** Task 2 (VS-CORE-04) / Task 3 (VS-CORE-05) — en paralelo
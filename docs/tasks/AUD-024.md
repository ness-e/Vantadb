# AUD-024: Eliminar heap clones por op en drain_hnsw_batch_locked

## Metadata
- **Plan file:** docs/plans/2026-08-12-audit-full.md (derivación de audit full 2026-08-12)
- **Creado:** 2026-08-13T00:00
- **last-synced:** 2026-08-13T00:00
- **Estado:** ✅ COMPLETED

## Blast Radius
- **Callees:** `HnswGraph::add` (toma `bitset: FilterBitset` y `vec_data: VectorRepresentations` por valor — src/index/graph.rs:596), `HnswGraph.nodes.remove`, `std::mem::take`
- **Callers:** `flush_pending_hnsw` (ops.rs:156), `flush` (vía ERR-010 checkpoint), `try_push_pending_hnsw` (ops.rs:214, mismo patrón de clones)
- **Tests que cubren el comportamiento:** `test_flush_pending_hnsw_with_delete`, `test_flush_pending_hnsw_with_multiple_ops`, `test_flush_pending_hnsw_with_mixed_ops` (src/storage/engine/tests/maintenance.rs) — ejercitan drain vía flush_pending_hnsw
- **Implicaciones:** cambio de ownership puro; `ops` ya se movió del mutex con `mem::take`, iterar por valor consume sin clonar. Cero cambio de comportamiento observable.

## Impacto mapeado (Regla 0)
- **Archivos que se modifican:** `src/storage/engine/ops.rs` (solo los 2 loops de drain: `drain_hnsw_batch_locked` y el drain opportunista en `try_push_pending_hnsw`)
- **Referencias entrantes:** `drain_hnsw_batch_locked` → `flush_pending_hnsw`, `flush` (maintenance/checkpoint). `try_push_pending_hnsw` → `insert`, `delete` (hot path)
- **Referencias salientes:** `HnswGraph::add` (src/index/graph.rs:596) — NO se toca; ya toma por valor, el drain le pasa `op.bitset` / `op.vector` movidos
- **Impacto si se elimina:** N/A (refactor, no eliminación). Veredicto: cambio seguro, mismo comportamiento
- **Dominio:** storage/engine (reglas durability.md) + HNSW (reglas indexes.md) — ninguna regla bloquea el refactor de ownership
- **Git history:** ops.rs último cambio c7185d25 (AUD-031, unwraps) — ya mergeado; working tree limpio, sin WIP

## Contrato
- `cargo check -p vantadb` pasa
- `cargo fmt --check` pasa
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` pasa
- `cargo nextest run --profile audit --workspace --build-jobs 2` pasa
- Cero heap clones nuevos: `drain_hnsw_batch_locked` ya no clona bitset+vector por op
- Medición perf antes/después: `cargo bench --bench bench_concurrent` (fase "Inserted 10000 nodes")

## Herramientas
- cargo-mcp, rust-analyzer-mcp, codegraph

## Steps
### Step 1: Discovery + medición baseline (BEFORE)
- **Archivos:** `benches/bench_concurrent.rs`
- **Acción:** correr `cargo bench --bench bench_concurrent`, capturar "Inserted 10000 nodes in ..."
- **Verify:** baseline capturado — **178.11s** (10k inserts secuenciales)
- **Estado:** ✅ COMPLETED

### Step 2: Refactor ownership en ops.rs
- **Archivos:** `src/storage/engine/ops.rs:165-188` y `:194-239`
- **Acción:** `for op in ops` (consumir) en vez de `for op in &ops`; pasar `op.bitset`, `op.vector` sin `.clone()` — ambos loops (drain + opportunista)
- **Verify:** `cargo check -p vantadb` ✅
- **Estado:** ✅ COMPLETED

### Step 3: Verificación post-fix + medición AFTER
- **Archivos:** — (solo verify)
- **Acción:** fmt ✅ → clippy ✅ → nextest ✅ (1913 passed); bench AFTER = **137.95s** (-22.5%)
- **Verify:** contrato completo
- **Estado:** ✅ COMPLETED

### Step 4: Cierre
- **Acción:** commit `perf: avoid per-op heap clones in drain_hnsw_batch_locked (AUD-024)` = `e4c2ff8e`; docs migración = `723b010e`; learnings en AGENTS.md; `skill progreso` (Backlog → progreso → avance)
- **Verify:** commit + migración Backlog ✅ (coverage avance OK)
- **Estado:** ✅ COMPLETED

## Dependencias
- AUD-031 (c7185d25): ops.rs ya tiene unwraps arreglados — respetar ese trabajo

## Notas
- `try_push_pending_hnsw` tiene el MISMO patrón de clones (drain opportunista, ruta más caliente) — se arregla junto con el finding para no dejar el anti-pattern a medias (root cause shared, ponytail)
- `HnswGraph::add` internamente clona bitset para validate_node (src/index/graph.rs:603) — eso es dominio de vanta-engine, fuera de scope

## Context Save Point
- **Fecha:** 2026-08-13T00:00
- **Branch:** develop
- **CI pendiente:** sí
- **Decisiones:** fijar ambos loops (drain + opportunista) porque es el mismo root cause en el mismo archivo; bench = bench_concurrent (ejercita el path completo)
- **Problemas conocidos:** ninguno
- **Próxima tarea:** — (tarea única)

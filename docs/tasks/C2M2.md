# C2M2 — trait `EntityRepository` en `entity/` (puerto DDD)

- **Estado:** ✅ COMPLETED (check + nextest entity 46/46 + fmt verdes tras S1; commit 3c5beb72) · **Ruta:** vanta-worker · **Wave 1** (tras M1) · **nextTask:** C2S2
- **Contrato:** trait puerto + `StorageEngine` vía adaptador + callers migran + nextest entity + sin cambio semántico
- **Gate D:** GO (adaptador §10 vigente — mismo patrón que backends; blast radius 3 archivos, sin hot path, sin ambigüedad)
- **Tipo:** refactor (puerto DDD + DIP) — sin `## Spec` (no es feature-add/lógica nueva)

## SDP

`SDP: incremental-implementation, test-driven-development, context-engineering, systematic-debugging, code-simplification, api-and-interface-design, database-design, code-review-and-quality, source-driven-development, planning-and-task-breakdown, doubt-driven-development, observability-and-instrumentation, codebase-memory`
(1 línea c/u: slices verticales / suites existentes como red sin tocar tests / jerarquía Rules→Spec→Source / root-cause si verify falla / thin trait sin over-design / contrato del puerto / port vs storage / review 5 ejes / DDD repository port pattern / steps atómicos / adversarial check del diseño / sin telemetría nueva — no hay comportamiento nuevo.)

## Impacto mapeado (Regla 0)

- **Leídos completos:** `src/entity/mod.rs` (263L), `src/entity/checker.rs` (343L), `src/entity/scene.rs` (191L)
- **Leídos parciales:** `src/skills.rs` (:30-89 + usos `entities.get`), `src/server/state.rs` (:300-349 `resolve_user_key`, :430-479 `resolve_identity`)
- **Referencias hacia dentro (entity/mod.rs):** `crate::backend::{BackendPartition, BackendWriteOp}`, `crate::error`, `crate::node::FieldValue`, `crate::storage::StorageEngine`
- **Referencias entrantes a `EntityStore` (concreto):**
  - `checker.rs:78-86` — `PermissionChecker { store: &EntityStore }` (solo usa `store.get`) — MIGRA a `&dyn EntityRepository`
  - `server/state.rs:310-314` — `resolve_user_key(store: &EntityStore, …)` (solo usa `store.list`) — MIGRA a `&dyn EntityRepository`
  - `skills.rs:46-58` — `SkillStore { entities: EntityStore }` construido internamente (composition root) — NO SE TOCA (scope discipline)
  - tests (`entity/tests.rs`, `checker_tests.rs`, `scene_tests.rs`, `cli_server_auth_tests.rs`, `server/cli_server_auth_tests.rs`) — coerción `&EntityStore → &dyn` automática, CERO cambios
  - `PermissionChecker::new` sin constructores en prod (solo tests) — cambio seguro
- **Prohibidos respetados:** `storage/engine` interno intacto (es S3), resto `src/` intacto, `SceneNodeStore` intacto (sin callers prod — NOTICED BUT NOT TOUCHING)
- **Veredicto:** impacto BAJO — aditivo (trait + impl delegante), 3 archivos prod, 0 tests tocados, `storage/` intacto

## Diseño (decisión)

`StorageEngine` NO implementa el trait directo (conocería tipos de dominio `Entity` — dirección de dependencia incorrecta).
Válido por contrato ("o adaptador"): **`EntityStore` es el adaptador** `StorageEngine → EntityRepository`.
Puerto `pub trait EntityRepository { set/get/delete/list }` en `entity/mod.rs` (dueño del dominio);
`impl EntityRepository for EntityStore` delega a métodos inherentes (cero duplicación, cero cambio semántico).

## Steps

- [x] **Step 1 — DISCOVERY:** blast radius + task file + Gate D GO
- [x] **Step 2 — ACT:** trait + impl en `mod.rs`, `&dyn` en `checker.rs` + `state.rs`
- [x] **Step 3 — VERIFY (scope C2M2 ✅ / workspace ⛔ bloqueado por WIP ajeno):**
  - `cargo check -p vantadb` (lib con mi cambio, pre-S1-rotura): ✅ Finished
  - `cargo nextest run -p vantadb entity`: ✅ 46 passed, 0 failed (sin tocar 1 test)
  - `cargo fmt --check` en mis 3 archivos: ✅ sin diffs
  - `cargo check` filtrado `entity|server/state` (post-rotura S1): ✅ 0 errores — todo el ruido es `backend.rs`/`backends/*`/`storage/engine/*` (S1 concurrente)
  - Full-workspace check/clippy: ⛔ roto por S1 (worker paralelo partió `StorageBackend` mid-flight: E0407/E0119/E0599) — re-correr al aterrizar S1; NO commitear (adaptador)

# DRV-027: Refactor `vantadb-python/src/lib.rs` God module (1991L) into submodules

## Metadata
- **Plan file:** `docs/Backlog.md` (línea 237, DRV Hallazgos — Python SDK)
- **Fuente:** Backlog TIER 2, `review-deep` Wave 0
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🟡
- **Tipo:** Rust — Refactor Python SDK
- **Turns estimados:** 15-25
- **Creado:** 2026-07-24
- **last-synced:** 2026-07-24
- **Estado:** ✅ COMPLETED — 2026-07-24
- **Resultado:** God module 1991L → 4 files: `convert.rs` (conversiones + LRU cache), `vector.rs` (helpers + VantaVector/VantaVectorIter), `types.rs` (VantaPySearchHit), `lib.rs` (~1991L→~700L). API pública de `VantaDB` pyclass intacta. `cargo check` + clippy + pytest test_sdk.py pasan.

## Resumen

`vantadb-python/src/lib.rs` contiene 1991 líneas con responsabilidades mezcladas:
- VantaDB pyclass con ~35 métodos (720-1704)
- 12 funciones `*_to_pydict` para convertir reportes SDK → PyDict (393-605)
- `py_any_to_value` + `set_python_value` — conversión Python ↔ VantaValue (93-302)
- `py_dict_to_metadata` — dict → BTreeMap con LRU cache (607-667)
- `map_vanta_error` — error mapping (693-718)
- `try_numpy_array` + `extract_vector` — vector helpers (206-278)
- `VantaVector` + `VantaVectorIter` pyclasses (1767-1863)
- `VantaPySearchHit` pyclass (1869-1976)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-python/tests/test_sdk.py` (integración), pip users (`import vantadb_py`) |
| Callees | `vantadb::sdk::*`, `vantadb::error::VantaError`, `vantadb::config::VantaConfig`, `vantadb::DistanceMetric` |
| Implicaciones | API pública de `VantaDB` pyclass NO cambia. `connect()` pyfunction NO cambia. Solo refactor interno. `types.rs` ya importa `set_python_value`, `try_numpy_array`, `VantaVector` — los re-exports deben mantenerse. |

## Contrato
```
cargo check -p vantadb-python 2>&1 pasa && ^
cargo clippy --deny warnings -p vantadb-python 2>&1 pasa && ^
target\audit-venv\Scripts\python -m pytest vantadb-python\tests\test_sdk.py -v 2>&1 pasa
```

## Herramientas necesarias
- cargo-mcp (check, clippy, test)
- rust-analyzer-mcp (diagnostics, goto def)

## Investigation Notes
- `types.rs` ya existe y usa `use crate::{set_python_value, try_numpy_array, VantaVector};` — los re-exports deben mantenerse
- `LRU_CACHE` thread_local! usa `LruCache` struct — se mueve con `py_dict_to_metadata`
- No hay cambios en API pública — solo mover funciones entre módulos y ajustar `use` paths

## Steps

### Step 1: Extraer `convert.rs` con todas las funciones de conversión
- **Archivos:** `vantadb-python/src/convert.rs` (nuevo), `vantadb-python/src/lib.rs`
- **Acción:** Mover a `convert.rs`:
  - `py_any_to_value` (93-204)
  - `set_python_value` (280-302)
  - `runtime_profile_label` (304-310)
  - `tier_label` (312-317)
  - `node_to_pydict` (321-357)
  - `format_query_result` (360-391)
  - `capabilities_to_pydict` (393-404)
  - `bm25_term_to_pydict` (406-414)
  - `explanation_hit_to_pydict` (416-433)
  - `hybrid_fusion_report_to_pydict` (435-445)
  - `search_explanation_to_pydict` (447-465)
  - `rebuild_report_to_pydict` (467-477)
  - `export_report_to_pydict` (479-486)
  - `import_report_to_pydict` (488-496)
  - `text_index_repair_report_to_pydict` (498-511)
  - `text_index_audit_report_to_pydict` (513-544)
  - `operational_metrics_to_pydict` (546-605)
  - `py_dict_to_metadata` + `LruCache` + `LRU_CACHE` thread_local! (35-91, 607-667)
  - `map_vanta_error` (693-718)
  - Necesita `use` imports: `pyo3::*`, `pyo3::buffer::PyBuffer`, `pyo3::exceptions::*`, `pyo3::types::*`, `std::cell::RefCell`, `std::collections::HashMap`, `vantadb::*`, `vantadb::error::VantaError`, `vantadb::sdk::*`, `chrono`
- **Verify:** `cargo check -p vantadb-python`
- **Estado:** ⬜ PENDING

### Step 2: Extraer `vector.rs` con helpers de vectores + VantaVector/VantaVectorIter
- **Archivos:** `vantadb-python/src/vector.rs` (nuevo), `vantadb-python/src/lib.rs`
- **Acción:** Mover a `vector.rs`:
  - `try_numpy_array` (210-224)
  - `extract_vector` (234-278)
  - `VantaVector` pyclass (1767-1838)
  - `VantaVectorIter` pyclass (1842-1863)
  - Necesita `use` imports: `pyo3::*`, `pyo3::buffer::PyBuffer`, `pyo3::prelude::*`, `pyo3::exceptions::*`, `pyo3::types::*`
- **Verify:** `cargo check -p vantadb-python`
- **Estado:** ⬜ PENDING

### Step 3: Mover `VantaPySearchHit` a `types.rs`
- **Archivos:** `vantadb-python/src/types.rs`, `vantadb-python/src/lib.rs`
- **Acción:** Mover `VantaPySearchHit` pyclass (1869-1976) a `types.rs`
  - types.rs ya tiene `VantaPyMemoryRecord`, `VantaPyListResult` — mismo patrón
  - Actualizar `use crate::*` paths en types.rs según los nuevos módulos
- **Verify:** `cargo check -p vantadb-python`
- **Estado:** ⬜ PENDING

### Step 4: Actualizar `lib.rs` — imports + re-exports + `vantadb_py` module setup
- **Archivos:** `vantadb-python/src/lib.rs`
- **Acción:**
  - Agregar `mod convert;` y `mod vector;`
  - Re-exportar funciones usadas por `types.rs`: `pub(crate) use convert::{set_python_value, py_dict_to_metadata};`, `pub(crate) use vector::{try_numpy_array, VantaVector};`
  - Mantener `VantaDB` pyclass + `connect()` pyfunction + `vantadb_py()` module entry point
  - Limpiar imports no usados
- **Verify:** `cargo check -p vantadb-python && cargo clippy --deny warnings -p vantadb-python`
- **Estado:** ⬜ PENDING

### Step 5: Verificación final — tests de integración Python
- **Archivos:** `vantadb-python/tests/test_sdk.py`
- **Acción:** Ejecutar tests para asegurar que la API pública no se rompió
- **Verify:** `target/audit-venv/Scripts/python -m pytest vantadb-python/tests/test_sdk.py -v`
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna — DRV-027 es independiente dentro de `vantadb-python/`

## Notas
- La función `check_lens` (1707-1737) está dentro del impl VantaDB en lib.rs — se queda donde está (es privada y solo usada por `put_batch_raw`)
- El thread_local! LRU_CACHE se mueve con py_dict_to_metadata a convert.rs
- `types.rs` actualmente usa `use crate::{set_python_value, try_numpy_array, VantaVector};` — los re-exports en `lib.rs` deben proveer estos names
- `cargo machete` se corre al final para detectar imports no usados

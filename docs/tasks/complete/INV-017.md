# INV-017: sccache en CI — investigación (alimenta GH-143)

## Metadata
- **Plan file:** (standalone — Backlog.md directo)
- **Fuente:** docs/Backlog.md:217
- **Esfuerzo:** 🟢 2-4h
- **Prioridad:** 🟡
- **Tipo:** Devops (investigación)
- **Turns estimados:** 8
- **Creado:** 2026-08-02
- **last-synced:** 2026-08-02
- **Estado:** ✅ COMPLETADO

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `.github/actions/rust-setup/action.yml` (usada por ci-rust-10, ci-gate, heavy-certification-50, fuzz-40, heavy-bench-nightly-51, etc.) |
| Callees | mozilla-actions/sccache-action (externa), Swatinem/rust-cache (ya instalado en rust-setup) |
| Implicaciones | Ningún cambio de código. La propuesta afecta solo `.github/actions/rust-setup/action.yml` y el doc drift de `.opencode/AGENTS.md`. No rompe contratos. |

## Contrato
"docs/Investigaciones/INV-017-sccache-ci.md existe con propuesta verificable: acción exacta a usar, env vars, coexistencia con Swatinem/rust-cache, impacto estimado. Drift del AGENTS.md corregido (afirma sccache instalado cuando no lo está)."

## Herramientas necesarias
- MetaSearchMCP / Argus (web research — hecha: sccache-action + SCCACHE_GHA_ENABLED)
- bash (rg para verificar estado real de sccache en .github/)

## Investigation Notes
- **Hecho verificado:** `rg -rln sccache .github/` → 0 resultados. sccache NO está implementado. `.opencode/AGENTS.md` (sección "CI: sccache") afirma "Agregado al workflow ci-rust-10.yml mediante mozilla-actions/sccache-action" — DRIFT documental a corregir.
- **Compatibilidad:** SÍ. `mozilla-actions/sccache-action@v0.0.11` + env `SCCACHE_GHA_ENABLED=true` + `RUSTC_WRAPPER=sccache`. Backend = GHA cache (sin infra adicional; ACTIONS_CACHE_URL/ACTIONS_RUNTIME_TOKEN se inyectan solos).
- **Complementarios vs redundantes:** Swatinem/rust-cache cachea `target/` entero (blob grande, restore lento, invalida todo al cambiar Cargo.lock). sccache cachea objetos compilados individuales (granular; comparte `.rlib` entre jobs paralelos del mismo run — clippy/test/coverage compilan los mismos crates). Ambos compiten por el límite de GHA cache (10GB, eviction LRU 7 días).
- **Estado CI actual (baseline run 30731463004):** bottleneck = Tests (Windows) 14m29s, causado por `cargo install cargo-nextest --locked` (ci-rust-10.yml:138) en vez de `taiki-e/install-action` como Linux/macOS. El resto del CI ya usa cargo check + nextest + jobs paralelos.
- **Veredicto ROI:** sccache = barato de integrar pero ahorro de wall-clock modesto (0-15%) sobre el CI ya cacheado. El fix del nextest de Windows es el mayor ROI (~4-5 min del bottleneck = ~30% del run). Ambos juntos cumplen el DoD del 20% de GH-143.

## Steps

### Step 1: Redactar propuesta
- **Archivos:** `docs/Investigaciones/INV-017-sccache-ci.md`
- **Acción:** Documentar hallazgos (compatibilidad, coexistencia, integración mínima en rust-setup, impacto estimado, veredicto ROI, y el drift del AGENTS.md).
- **Verify:** archivo existe y cubre los 4 puntos de alcance del backlog.
- **Estado:** ✅ COMPLETADO

### Step 2: Corregir drift AGENTS.md
- **Archivos:** `.opencode/AGENTS.md`
- **Acción:** Corregir la sección "CI: sccache" — quitar la afirmación falsa de que sccache ya está agregado; indicar que es pendiente (INV-017/GH-143) o dejarla como guía de la integración planeada.
- **Verify:** `rg sccache .opencode/AGENTS.md` ya no afirma implementación existente.
- **Estado:** ✅ COMPLETADO

## Dependencias
- Ninguna (tarea raíz de la investigación).

## Notas
- GH-143 (implementación) consume esta propuesta.

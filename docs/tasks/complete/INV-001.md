# INV-001: Investigar dependencias con RUSTSEC activas

**Source:** docs/Backlog.md (Investigaciones de Seguridad)
**Priority:** 🟠 P2
**Effort:** 🟢 2-4h
**Status:** ✅ COMPLETED (2026-07-29, verificado batch 7: reporte `docs/audit-reports/inv-001-rustsec-2026-07-29.md` existe, las 3 crates gestionadas o stale, `cargo deny` limpio; Backlog + progreso ✅)

## Description

Auditar `Cargo.lock` contra advisories conocidos. 3 crates reportadas con RUSTSEC activos:
- `atomic-polyfill` (RUSTSEC-2023-0089)
- `paste` (RUSTSEC-2024-0436)
- `rustls-pemfile` (RUSTSEC-2025-0134)

**Sin implementación — solo reporte + propuesta.** No modificar código.

## Scope

1. Extraer árbol de dependencias de cada crate via `cargo tree -p <crate>`
2. Verificar si RUSTSEC aplica al uso actual en VantaDB
3. Identificar reemplazos (ej. `atomic-polyfill` → `core::sync::atomic` si MSRV lo permite, o actualizar a versión parcheada)
4. Estimar breaking change si se remueve/reemplaza

## Acceptance Criteria

- [ ] Árbol de dependencias extraído para cada crate (directa o transitiva, qué crates la traen)
- [ ] Cada RUSTSEC evaluado: ¿aplica a VantaDB? ¿riesgo real?
- [ ] Propuesta concreta por crate: mantener, actualizar, reemplazar, o remover
- [ ] Reporte escrito en `docs/audit-reports/inv-001-rustsec-YYYY-MM-DD.md`

## Deliverable

Reporte en `docs/audit-reports/inv-001-rustsec-YYYY-MM-DD.md` con:
- Resumen ejecutivo
- Análisis por crate (árbol de dependencias, riesgo, propuesta)
- Estimación de esfuerzo si hay acción correctiva
- Recomendación final (GO/DEFER/ESCALATE)

## Commands

```bash
cargo tree -p atomic-polyfill --invert
cargo tree -p paste --invert
cargo tree -p rustls-pemfile --invert
cargo deny check advisories
```

## Dependencies

Ninguna.

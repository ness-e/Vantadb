# REC-001: [Foundation] Definir VantaFilterOp + VantaMemoryFilter types

## Descripción
Crear tipos de filtro en el SDK que expresen operadores relacionales (Eq, Neq, Gt, Lt, Gte, Lte) sobre metadata. Serán reusados por SDK-01 (delete_by_filter), SDK-03 (count), SDK-05 (expanded metadata filters).

## Estado actual
- `src/sdk/types.rs:104` — `VantaMemoryMetadata = BTreeMap<String, VantaValue>` (solo flat equality)
- `VantaMemoryListOptions.filters` usa `VantaMemoryMetadata` — no hay `VantaFilterOp` ni operadores
- No existe `VantaMemoryFilterItem` ni `VantaMemoryFilter`
- **✅ COMPLETED (2026-07-29, verificado batch 6):** `VantaFilterOp` en `src/sdk/types.rs:108`, `VantaMemoryFilterItem` :119, `VantaMemoryFilter` :126, re-exportados desde `src/sdk/mod.rs`. `cargo check + clippy` ✅.

## Especificación

```rust
/// Operadores de comparación para filtros de metadata.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub enum VantaFilterOp {
    Eq,
    Neq,
    Gt,
    Lt,
    Gte,
    Lte,
}

/// Un filtro individual: campo + operador + valor.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct VantaMemoryFilterItem {
    pub field: String,
    pub op: VantaFilterOp,
    pub value: VantaValue,
}

/// Lista de filtros combinados con AND lógico.
pub type VantaMemoryFilter = Vec<VantaMemoryFilterItem>;
```

## Archivos a modificar
- `src/sdk/types.rs` — agregar tipos
- `src/sdk/mod.rs` — re-exportar `VantaFilterOp`, `VantaMemoryFilterItem`, `VantaMemoryFilter`

## Acceptance Criteria
- [ ] `VantaFilterOp` enum con 6 variantes en `src/sdk/types.rs`
- [ ] `VantaMemoryFilterItem` struct con field/op/value
- [ ] `VantaMemoryFilter` type alias
- [ ] serde Serialize/Deserialize para todos los tipos
- [ ] Re-exportados desde `src/sdk/mod.rs`
- [ ] `cargo check -p vantadb` pasa
- [ ] `cargo clippy -p vantadb` sin warnings nuevos

## Verificación
```bash
cargo check -p vantadb
cargo clippy -p vantadb -- -D warnings
```

## Dependencias
- Ninguna (es foundation)
- Desbloquea: SDK-01, SDK-03, SDK-05

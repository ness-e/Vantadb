# DRV-015: Refactor WalWriter::open_with_buffer() — función monolítica de 170L

## Metadata
- **Plan file:** docs/plans/2026-07-24-stabilization-zero-bug.md
- **Fuente:** docs/Backlog.md:197
- **Esfuerzo:** 🟢 1d
- **Prioridad:** 🟡
- **Tipo:** Rust
- **Turns estimados:** 15-25
- **Creado:** 2026-07-24
- **Estado:** ✅ COMPLETED — 2026-07-24
- **Resultado:** `recover_valid_records()` extraída a `src/wal.rs:509`; `open_with_buffer()` (:202) reducida ~100L→~55L como orquestadora. `cargo check` + clippy `-D warnings` clean, 1616/1617 tests (1 pre-existing fail metrics).

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `src/wal.rs:193` (open wrapper), `src/wal_sharded.rs`, `src/wal_archiver.rs`, `src/lib.rs`, `src/wal_shipping.rs` |
| Callees | `check_record_at`, `try_scan_forward`, `WalHeader::new`, `WalHeader::serialize`, `WalHeader::deserialize`, `crc32c` |
| Implicaciones | Solo cambia estructura interna de `WalWriter`. No afecta API pública de `open()` o `open_with_buffer()`. Tests existentes no requieren modificación. |

## Contrato
```
cargo check -p vantadb --features "fjall" pasa sin warnings nuevos
cargo clippy -p vantadb --features "fjall" -- -D warnings pasa
cargo nextest run --profile audit --build-jobs 2 -p vantadb pasa
```

## Herramientas necesarias
- cargo-mcp (check, clippy, fmt, nextest)
- codegraph_explore

## Steps

### Step 1: Extraer `open_or_create_file()` 
- **Archivos:** `src/wal.rs` — extraer líneas 207-213 a método privado
- **Acción:** Crear `fn open_or_create_file(path: &Path) -> Result<(File, u64)>` que encapsule OpenOptions + metadata
- **Verify:** `cargo check -p vantadb --features "fjall"`

### Step 2: Extraer `read_and_validate_header()`
- **Archivos:** `src/wal.rs` — extraer validación de header existente
- **Acción:** Crear `fn read_and_validate_header(file: &mut File) -> Result<WalHeader>` con las líneas 226-229
- **Verify:** `cargo check -p vantadb --features "fjall"`

### Step 3: Extraer `recover_valid_records()`
- **Archivos:** `src/wal.rs` — extraer el loop de recovery scanning + truncation
- **Acción:** Crear `fn recover_valid_records(path: &Path, file_len: u64) -> Result<(u64, u64)>` que devuelva (bytes_written, record_count). Reutiliza `check_record_at()` y `try_scan_forward()` existentes.
- **Verify:** `cargo check -p vantadb --features "fjall"`

### Step 4: Simplificar `open_with_buffer()`
- **Archivos:** `src/wal.rs` — reescribir `open_with_buffer()` llamando a los 3 helpers extraídos
- **Acción:** El cuerpo debe quedar en ~30 líneas (orquestación simple de los helpers)
- **Verify:** `cargo clippy -p vantadb --features "fjall" -- -D warnings` + `cargo nextest run --profile audit --build-jobs 2 -p vantadb`

### Step 5: Verificación final
- **Acción:** `cargo fmt --check` + clippy + nextest
- **Verify:** `just verify-quick` o equivalente

## Dependencias
- Ninguna — task independiente

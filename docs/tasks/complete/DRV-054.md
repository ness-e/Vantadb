# DRV-054: read_axioms hardcoded como JSON literal

## Metadata
- **Fuente:** Backlog.md Phase 1, línea 78
- **Esfuerzo:** 🟢 30min
- **Prioridad:** 🔵 (sec. crítica)
- **Tipo:** Rust (MCP crate)
- **Turns estimados:** 3-5
- **Estado:** ✅ COMPLETED — 2026-07-25
- **Resultado:** Axioms extraídos a `HARDCODED_AXIOMS` const (`vantadb-mcp/src/lib.rs:84`) + `resolve_axioms()` con lookup por metadata (`AXIOMS_STORAGE_KEY`, fallback a const) :92.

## Blast Radius
| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-mcp/src/lib.rs` — usado por `handle_read_axioms` |
| Callees | Ninguno (constante local, no llama a nada) |
| Implicaciones | Contrato MCP no cambia, solo la fuente del JSON cambia de inline a `const` |

## Contrato
"`cargo check -p vantadb-mcp` pasa. La respuesta de `vantadb_mcp_axioms` es idéntica en contenido."

## Steps

### Step 1: Mover axioms a const estático
- **Archivos:** `vantadb-mcp/src/lib.rs`
- **Acción:** Mover el string `AXIOMS: &str` a una `const` con nombre semántico. Si existe metadata de Axioms en storage, sincronizar.
- **Verify:** `cargo check -p vantadb-mcp`

### Step 2: Agregar lookup por metadata (si existe)
- **Archivos:** `vantadb-mcp/src/lib.rs`
- **Acción:** Si hay `get_metadata("axioms")`, usar ese. Fallback a la const.
- **Verify:** `cargo nextest run --package vantadb-mcp`

## Notas
- Tarea simple: solo mover un string literal a const + opcional lookup.
- No toca nada fuera de `vantadb-mcp/src/lib.rs`.

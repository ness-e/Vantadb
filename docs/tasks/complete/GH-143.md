# GH-143: Acelerar CI con sccache y paralelización

## Metadata
- **Plan file:** (standalone — Backlog.md directo)
- **Fuente:** docs/Backlog.md:307, issue #143
- **Esfuerzo:** 🟡 2-4h
- **Prioridad:** 🟡
- **Tipo:** Devops (CI/CD)
- **Turns estimados:** 15
- **Creado:** 2026-08-02
- **last-synced:** 2026-08-02
- **Estado:** ✅ COMPLETADO

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `.github/actions/rust-setup/action.yml` (compartida por ci-rust-10, ci-gate, heavy-certification-50, fuzz-40, heavy-bench-nightly-51, perf-bench-40, release-*); ci-rust-10.yml job test-windows |
| Callees | mozilla-actions/sccache-action@v0.0.11, taiki-e/install-action (ya usado en rust-setup), Swatinem/rust-cache |
| Implicaciones | Tocar rust-setup afecta a todos los jobs que la usan → validar que no rompa Windows/macOS/Linux. `cargo install cargo-nextest` en Windows se reemplaza por taiki-e/install-action. No hay cambio de código Rust. |

## Contrato
"Run completo de ci-rust-10 en develop ≥20% más rápido que baseline (14m29s → ≤11m36s wall-clock del run, medido sobre job más lento); todos los jobs pasan; yamllint .github/ limpio."

## Herramientas necesarias
- bash (editar workflow/action, gh run, yamllint si existe)
- MetaSearchMCP (verificar versiones de acciones — sccache-action@v0.0.11 confirmada)

## Investigation Notes
- Depende de INV-017 (propuesta ya investigada). Puntos clave: sccache via `mozilla-actions/sccache-action@v0.0.11` + env `SCCACHE_GHA_ENABLED=true` + `RUSTC_WRAPPER=sccache`; backend GHA cache automático.
- Baseline medible: run 30731463004 → Tests (Windows) 14m29s es el bottleneck; `cargo install cargo-nextest --locked` (ci-rust-10.yml:138) cuesta ~4-5 min. Linux/macOS ya usan `taiki-e/install-action` (rust-setup:79-83).
- Triage gate: (1) sccache → ✅ DO; (2) `cargo build`→`cargo check` → SKIP (ya implementado); (3) paralelización → DO parcial (fix nextest Windows).
- Riesgo: en Windows, `RUSTC_WRAPPER=sccache` debe aplicarse antes de los builds; el action sccache setea SCCACHE_PATH. Verificar que no interfiera con el `cargo install` de nextest (que no debe pasar por sccache).

## Steps

### Step 1: Integrar sccache en rust-setup (punto único)
- **Archivos:** `.github/actions/rust-setup/action.yml`
- **Acción:** Añadir step `mozilla-actions/sccache-action@v0.0.11` + env `SCCACHE_GHA_ENABLED=true` y `RUSTC_WRAPPER=sccache` (antes del primer cargo). Aplicar a todos los runners (composite action cubre Linux/macOS/Windows).
- **Verify:** `yamllint .github/actions/rust-setup/action.yml` (si instalado) o validación YAML; diff review.
- **Estado:** ✅ COMPLETADO

### Step 2: Fix bottleneck nextest en Windows
- **Archivos:** `.github/workflows/ci-rust-10.yml:136-139`
- **Acción:** Reemplazar `cargo install cargo-nextest --locked` por `uses: taiki-e/install-action` con `tool: nextest` (mismo patrón que rust-setup).
- **Verify:** `yamllint .github/workflows/ci-rust-10.yml`; diff review.
- **Estado:** ✅ COMPLETADO

### Step 3: Medir baseline y post-cambio
- **Archivos:** (ninguno — observación)
- **Acción:** Hacer push a develop y disparar ci-rust-10. Comparar duración del run (job más lento) contra baseline 14m29s. Requiere ≥1 run con cache caliente para medir sccache real.
- **Verify:** `gh run list --workflow "CI: Rust — Build & Lint + Tests" --branch develop` + `gh run view` duraciones.
- **Estado:** ✅ COMPLETADO

### Step 4: Actualizar docs (drift AGENTS.md)
- **Archivos:** `.opencode/AGENTS.md`
- **Acción:** Documentar la integración real de sccache (acción, env, ubicación) — reemplaza la afirmación falsa previa.
- **Verify:** `rg sccache .opencode/AGENTS.md` refleja la implementación real.
- **Estado:** ✅ COMPLETADO

## Dependencias
- INV-017: investigación sccache (debe completarse antes — alimenta el diseño).

## Notas
- DoD issue #143: CI ≥20% más rápido + todos los checks pasan. Cierre: `gh issue close 143 --repo ness-e/Vantadb`.
- Regla 2 AGENTS.md: NO usar `continue-on-error: true` en ningún cambio.

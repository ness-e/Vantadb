# TSK-104: Demo agent LangChain + Ollama + VantaDB

## Metadata
- **Plan file:** — (backlog directo)
- **Fuente:** docs/Backlog.md:144
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🟠
- **Tipo:** Python + Docs (Mixto)
- **Turns estimados:** 15-30
- **Creado:** 2026-08-02
- **Estado:** ✅ COMPLETADO
- **Routing:** vanta-worker

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | — (ninguno en código; solo docs: `docs/operations/EXPERIMENTAL_FEATURES.md`, `docs/audit-reports/backlog-validation-2026-07-28.md`) |
| Callees | `vantadb_py` (SDK Python), `vantadb_langchain` (integrations/langchain), `vantadb_ollama` (integrations/ollama) |
| Implicaciones | No rompe contratos. Reemplaza sketch experimental (`langchain_rag.py`) por demo pulida con integraciones reales. API moderna `VantaDB::put/search_memory` (NO legacy `insert/search/get`). |

## Contrato
"`examples/python/langchain_ollama_rag.py` corre de punta a punta: ingesta con embeddings de Ollama (o fallback mock cuando Ollama no está instalado), retrieval con `vantadb_langchain.VantaDBVectorStore` + `vantadb_ollama`, y genera una respuesta RAG. Smoke test con `python examples/python/langchain_ollama_rag.py` exit 0 SIN Ollama instalado (usa fallback determinístico). 0 ocurrencias de API legacy (`db.insert(`/`db.search(`/`db.get(`) y 0 ocurrencias de 'Emulated LangChain' en el archivo demo."

## Herramientas necesarias
- codegraph_explore (blast radius) — ya ejecutado
- bash (python del proyecto para smoke test)

## Investigation Notes
- Estado actual: `examples/python/langchain_rag.py` (95 líneas) emula LangChain ("Emulated LangChain Modules", embeddings dummy `[0.1, 0.4, 0.6]`) y usa API legacy sync (`db.insert`, `db.search`, `db.get`).
- Integraciones REALES ya existen y están probadas:
  - `integrations/langchain/vantadb_langchain/vectorstore.py` — `VantaDBVectorStore` (LangChain `VectorStore`, `add_texts`, `similarity_search`, usa `put`/`search_memory`/`get_memory`).
  - `integrations/ollama/vantadb_ollama/vectorstore.py` — `VantaDBOllama` (`add_texts`, `similarity_search`, `_embed`/`_embed_many` vía `ollama.embeddings`/`ollama.embed`).
  - Tests con `FakeOllama` (monkeypatch) — patrón para smoke test sin servidor Ollama.
- API legacy `insert/get/search` SÍ existe en lib.rs (sync) y AsyncVantaDB (async) pero es el camino deprecado; la demo debe usar la moderna.
- Ollama CLI **no está instalado** en la máquina de build → smoke test debe degradar con fallback mock (patrón FakeOllama de integrations/ollama/tests/test_vectorstore.py).
- Venv de tests `target/audit-venv` no existe → verificar si `.venv` tiene `vantadb-py` o instalar editable `pip install -e ./vantadb-python`.
- Referencias al sketch viejo: solo `docs/operations/EXPERIMENTAL_FEATURES.md` + audit report. Al renombrar a `langchain_ollama_rag.py`, actualizar EXPERIMENTAL_FEATURES.md y borrar `langchain_rag.py`.

## Steps

### Step 1: Crear demo pulida `examples/python/langchain_ollama_rag.py`
- **Archivos:** `examples/python/langchain_ollama_rag.py`
- **Acción:** Demo RAG completa usando integraciones reales:
  1. `VantaDBVectorStore` de `vantadb_langchain` con `OllamaEmbeddings` (langchain-ollama) — o `VantaDBOllama` si más directo
  2. Ingesta 3-4 docs, retrieval con query, generación de respuesta (print del prompt + top hits)
  3. Fallback determinístico a embeddings mock (patrón FakeOllama) cuando Ollama no está disponible — permite smoke test sin servidor
  4. Docstring con instrucciones de uso real vs fallback
- **Verify:** `python examples/python/langchain_ollama_rag.py` exit 0 (fallback path)
- **Estado:** ✅ COMPLETADO

### Step 2: Remover sketch legacy
- **Archivos:** `examples/python/langchain_rag.py` (borrar), `docs/operations/EXPERIMENTAL_FEATURES.md`
- **Acción:** Borrar sketch emulado; actualizar referencia en EXPERIMENTAL_FEATURES.md a la demo nueva
- **Verify:** `rg -n "langchain_rag" .` → 0 matches (excepto git history)
- **Estado:** ✅ COMPLETADO

### Step 3: Smoke test + regresión integraciones
- **Archivos:** `integrations/langchain/tests/test_vectorstore.py`, `integrations/ollama/tests/test_vectorstore.py` (no modificar)
- **Acción:** Correr tests de integraciones para confirmar que nada se rompe; correr demo completa
- **Verify:** pytest de langchain + ollama pasan; demo exit 0
- **Estado:** ✅ COMPLETADO

## Dependencias
- Ninguna. (Las integraciones ya existen y están publicadas — no requiere nuevos features.)

## Notas
- NO tocar `src/` Rust, NO tocar lib.rs, NO tocar los vectorstores de integraciones.
- No agregar deps nuevas al workspace Cargo.toml — solo uso de paquetes existentes (vantadb-py, langchain-core, ollama).
- La demo debe documentar el flujo real: `pip install vantadb-langchain langchain-ollama` (o desde integraciones/).
- La demo no debe requerir red cuando corre en fallback.

# GH-124: Ejemplos doc-test para API pública Rust

## Metadata
- **Plan file:** (standalone, sin plan file — Backlog.md directo)
- **Fuente:** docs/Backlog.md:316
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🟡
- **Tipo:** Rust (doc-tests sobre API pública)
- **Turns estimados:** 10
- **Creado:** 2026-08-02
- **last-synced:** 2026-08-02
- **Estado:** ✅ COMPLETADO

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `VantaEmbedded` (39 callers: src/sdk/gds.rs, graph.rs, mod.rs, search/, + bindings python/wasm/mcp). Los doc-tests NO cambian firmas — solo agregan `/// ```rust` blocks. |
| Callees | `StorageEngine::open_with_config` (src/storage/engine/init.rs:34), `VantaMemoryInput`, `VantaMemorySearchRequest` (impl Default), `VantaConfig` (impl Default) |
| Implicaciones | Cero cambios de comportamiento. Doc-tests corren en `cargo test --doc`. Usar `storage_path: ":memory:"` para ejemplos auto-contenidos sin ensuciar disco (patrón ya usado en tests: `make_embedded()` en src/sdk/api.rs:1263). |

## Contrato
"cargo test --doc -p vantadb --build-jobs 2 pasa; cargo doc --no-deps -p vantadb sin warnings; cargo fmt --check y cargo clippy -p vantadb sin errores nuevos"

## Herramientas necesarias
- cargo-mcp (test --doc, doc, fmt, clippy)
- rust-analyzer-mcp (diagnostics)
- codegraph_explore (blast radius — ya ejecutado)

## Investigation Notes
- `VantaEmbedded::open(path)` → src/sdk/builder.rs:44 (construye VantaConfig{storage_path} + open_with_config)
- `VantaEmbedded::open_with_config(config)` → src/sdk/builder.rs:54
- `put(input: VantaMemoryInput)` → src/sdk/api.rs:169 → devuelve `Result<VantaMemoryRecord>`
- `get(namespace, key)` → src/sdk/api.rs:270 → `Result<Option<VantaMemoryRecord>>`
- `delete(namespace, key)` → src/sdk/api.rs:289 → `Result<bool>`
- `search(request: VantaMemorySearchRequest)` → src/sdk/search/mod.rs:85 → `Result<Vec<VantaMemorySearchHit>>`
- `VantaConfig` → src/config.rs:201 (campo clave para doc: `storage_path`; impl Default)
- `VantaMemoryInput` → src/sdk/types.rs:130; `VantaMemorySearchRequest` → src/sdk/serialization/vector_types.rs:11 (impl Default, `top_k` default 10, `distance_metric` Cosine)
- `VantaMemorySearchHit` → src/sdk/serialization/vector_types.rs:54 (`record` + `score`)
- `":memory:"` como storage_path abre real (no fake) — patrón en tests de src/sdk/api.rs:1263-1269
- Ejemplo realista existente: `examples/rust/basic.rs` (put/get/delete + search)
- Buscar `search` con texto: request con `text_query: Some(...)`; top_k > 0 para que no devuelva vacío

## Steps

### Step 1: Baseline — estado actual de doc-tests y warnings de doc
- **Archivos:** `src/sdk/builder.rs`, `src/sdk/api.rs`, `src/sdk/search/mod.rs`, `src/config.rs`
- **Acción:** Correr `cargo test --doc -p vantadb --build-jobs 2` y `cargo doc --no-deps -p vantadb` para registrar el baseline (cuántos doc-tests pasan, qué warnings hay hoy).
- **Verify:** comandos de baseline documentados en Notas.
- **Estado:** ✅ COMPLETADO

### Step 2: Doc-tests en open() y open_with_config()
- **Archivos:** `src/sdk/builder.rs:42-70`
- **Acción:** Agregar `/// # Examples` con ` ```rust ` runnable a `VantaEmbedded::open()` (con path temporal vía `std::env::temp_dir()` + nombre único) y a `open_with_config()` (con `VantaConfig { storage_path: ":memory:".into(), ..Default::default() }`). Asegurar que el ejemplo no deje archivos (borrar al final del test si usa temp dir).
- **Verify:** `cargo test --doc -p vantadb open` y `cargo doc --no-deps -p vantadb` sin warnings
- **Estado:** ✅ COMPLETADO

### Step 3: Doc-tests en put(), get() y delete()
- **Archivos:** `src/sdk/api.rs:166-302`
- **Acción:** Agregar `/// # Examples` runnable a `put()` (abrir `:memory:`, `VantaMemoryInput`, insertar, assert sobre `VantaMemoryRecord`), `get()` (put + get + assert), `delete()` (put + delete devuelve true + delete de nuevo devuelve false). Cerrar el handle con `close()` al final.
- **Verify:** `cargo test --doc -p vantadb put`, `cargo test --doc -p vantadb get`, `cargo test --doc -p vantadb delete`
- **Estado:** ✅ COMPLETADO

### Step 4: Doc-test en search()
- **Archivos:** `src/sdk/search/mod.rs:82-96`
- **Acción:** Agregar `/// # Examples` runnable: abrir `:memory:`, poner 2+ registros con payload en el mismo namespace, `VantaMemorySearchRequest { namespace, text_query: Some("..."), top_k: 10, ..Default::default() }`, assert de ≥1 hit y que el hit matchea el payload esperado.
- **Verify:** `cargo test --doc -p vantadb search`
- **Estado:** ✅ COMPLETADO

### Step 5: Doc/ejemplo en VantaConfig
- **Archivos:** `src/config.rs:196-338`
- **Acción:** Agregar doc-test en el struct-level doc o en el campo `storage_path` mostrando construcción con `..Default::default()` y uso con `VantaEmbedded::open_with_config`. (No tocar ningún field más — solo el ejemplo).
- **Verify:** `cargo test --doc -p vantadb config` y `cargo doc --no-deps -p vantadb` sin warnings
- **Estado:** ✅ COMPLETADO

### Step 6: Verificación full del contrato
- **Archivos:** (ninguno)
- **Acción:** `cargo fmt --check -p vantadb`, `cargo clippy -p vantadb --all-targets -- -D warnings`, `cargo test --doc -p vantadb --build-jobs 2`, `cargo doc --no-deps -p vantadb` (sin warnings).
- **Verify:** contrato completo ✓
- **Estado:** ✅ COMPLETADO

## Dependencias
- Ninguna

## Notas
- La tarea solo agrega bloques de código en doc-comments — no cambia firmas ni comportamiento.
- Convención: cada doc-test cierra el handle con `close()` y no deja archivos en disco (o usa `:memory:`).
- Cierre del backlog: revisar y cerrar issue #124 tras verificación.

## Context Save Point
- **Fecha:** 2026-08-02
- **Branch:** develop
- **CI pendiente:** no
- **Decisiones:** doc-tests usan `":memory:"` (patrón de tests existentes) en vez de temp dirs para put/get/search/delete — self-contained y sin limpieza.
- **Problemas conocidos:** ninguno
- **Próxima tarea:** — (standalone)

# INV-016: Motion-duration tokens — auditoría

## Metadata
- **Fuente:** docs/Backlog.md línea 212
- **Esfuerzo:** 🟢 1-2h
- **Prioridad:** 🟢
- **Tipo:** Frontend (auditoría tokens de animación)
- **Turns estimados:** 5-10
- **Creado:** 2026-08-03
- **last-synced:** 2026-08-03
- **Estado:** ✅ COMPLETED

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `web/src/app/globals.css`, `web/src/components/vanta/page-transition.tsx`, `web/src/components/vanta/reveal.tsx`, otros componentes con framer-motion |
| Callees | framer-motion, Tailwind v4 |
| Implicaciones | Sin implementación — auditoría + propuesta. No rompe contratos. |

## Contrato
"Reporte de auditoría documentado en la fila INV-016 de docs/Backlog.md con: existencia o ausencia de tokens de duración en globals.css, inventario de duraciones hardcodeadas en componentes framer-motion, y propuesta de esquema de tokens (fast: 150ms, normal: 300ms, slow: 500ms) como CSS custom properties"

## Herramientas necesarias
- grep (globals.css, page-transition.tsx, reveal.tsx, *.tsx con duration)
- read (archivos afectados)

## Investigation Notes
- Esquema propuesto por backlog: `--duration-fast: 150ms`, `--duration-normal: 300ms`, `--duration-slow: 500ms`
- framer-motion usa `duration` en `transition={{ duration: 0.3 }}` — no consume CSS vars directamente; tokens servirían vía JS export o CSS + `transitionDuration`
- Verificar `Reveal` (delay prop) y `PageTransition` (AnimatePresence)

## Steps

### Step 1: Buscar tokens de duración existentes
- **Archivos:** `web/src/app/globals.css`
- **Acción:** Grep de `--duration`, `--ease`, `transition-duration`, `animation-duration`. Documentar qué existe.
- **Verify:** hallazgos documentados
- **Estado:** ⬜ PENDING

### Step 2: Inventariar duraciones hardcodeadas
- **Archivos:** `web/src/components/vanta/page-transition.tsx`, `web/src/components/vanta/reveal.tsx`, `web/src/components/vanta/*.tsx` (grep `duration:`)
- **Acción:** Listar valores hardcodeados (ej. `duration: 0.3`, `duration: 0.8`) por componente.
- **Verify:** inventario documentado
- **Estado:** ⬜ PENDING

### Step 3: Proponer esquema de tokens
- **Acción:** Redactar propuesta de tokens (CSS custom properties + mapa JS para framer-motion si aplica) con tabla de reemplazo componente → token.
- **Verify:** propuesta en Backlog
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- Solo auditoría + propuesta (alcance del backlog). No implementar.

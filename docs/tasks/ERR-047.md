## Context Save (run 2026-08-11, lead directo)
- Commit: 01630996 — perf(ERR-047)
- Fix: search_layer hot loop usaba take_nl() + extend_from_slice() para copiar el inline neighbor cache (populado post-deserialización, serialize.rs:533) en cada candidato expandido. Refactor a Cow<NeighborVec>: Borrowed para inline cache (guard DashMap vivo durante el loop), Owned solo para fallback neighbor_index. Mismo patrón en ACORN-1 second-hop (search.rs ~402). give_nl solo para Owned. take_nl eliminado (dead code).
- Verify: cargo check --jobs 2 ok; search suite 128/128; index:: 251/251; clippy/fmt ok; test_incremental_recall_parity fail pre-existente (ERR-010) confirmado con stash.
- Nota: ahorra 1 alloc+copy O(M) por candidato en path inline-cache común; el guard del DashMap queda vivo — múltiples read-locks concurrentes OK (no hay get_mut en el loop).
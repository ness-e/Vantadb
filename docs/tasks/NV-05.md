# NV-05: Unificar política de advisory ignores (deny.toml vs .cargo/audit.toml)

## Metadata
- **Plan file:** (AUDREP Phase 13, docs/Backlog.md línea 343)
- **Fuente:** Audit Report 2025 (AUDREP P13)
- **Esfuerzo:** 🟢 30min
- **Prioridad:** 🟢
- **Tipo:** Dependencias/config
- **Turns estimados:** 2
- **Creado:** 2026-08-08T00:00
- **last-synced:** 2026-08-08T00:00
- **Estado:** ⬜ PENDING

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | cargo-deny check (CI Fast Gate, verify.ps1), cargo audit |
| Callees | ninguno |
| Implicaciones | `.cargo/audit.toml` ignora RUSTSEC-2024-0436 (paste, build-time proc macro vía tikv-jemalloc-ctl) con expiry 2027-01-01; `deny.toml` NO lo ignora → `cargo deny check advisories` falla mientras `cargo audit` pasa. Unificar: agregar el mismo ignore con el mismo comentario a deny.toml. |

## Contrato
"`cargo deny check advisories` pasa (0 errors) — el ignore RUSTSEC-2024-0436 existe en ambos archivos"

## Herramientas necesarias
- cargo-deny

## Steps

### Step 1: Agregar RUSTSEC-2024-0436 a deny.toml [advisories].ignore
- **Archivos:** `deny.toml:2-10`
- **Acción:** Agregar entrada con el mismo comentario que audit.toml (paste, build-time, expiry 2027-01-01, issue jemalloc-ctl#60):
  ```toml
  # RUSTSEC-2024-0436: paste is unmaintained.
  # Transitive via tikv-jemalloc-ctl -> paste (build-time proc macro).
  # Owner: vanta-lead
  # Expiry: 2027-01-01
  "RUSTSEC-2024-0436",
  ```
- **Verify:** `cargo deny check advisories`
- **Estado:** ⬜ PENDING

### Step 2: Verificar sync
- **Archivos:** `deny.toml`, `.cargo/audit.toml`
- **Acción:** Confirmar que ambos archivos listan RUSTSEC-2023-0089 y RUSTSEC-2024-0436 (misma política).
- **Verify:** `cargo deny check advisories && cargo audit` (ambos 0 findings o mismos ignores)
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- No mover la política a un archivo único (mayor refactor del que justifica el fix); el comentario "Keep in sync" en audit.toml es el mecanismo actual. Duplicar el ignore es la mínima corrección.

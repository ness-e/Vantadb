# GH-123: Corregir typos y links rotos en docs

## Metadata
- **Plan file:** (ninguno activo — campaña launch-web cerrada)
- **Fuente:** docs/Backlog.md:323 (issue GitHub #123)
- **Esfuerzo:** 🟡 2-4h
- **Prioridad:** 🟢
- **Tipo:** Docs
- **Turns estimados:** 10-15
- **Creado:** 2026-08-04T08:00
- **last-synced:** 2026-08-05T08:00
- **Estado:** ✅ DONE (commit `d406feab` — typos, links rotos y refs de versión corregidos; Task 34 re-escopeta a links rotos reales)

## Blast Radius
| Dirección | Módulos |
|-----------|---------|
| Callers | N/A (docs, no código) |
| Callees | `docs/**` (335 archivos .md), `.markdownlint-cli2.yaml`, `.github/workflows/gate-docs-21.yml` |
| Implicaciones | No rompe API/contratos. Cuidado: NO tocar docs/api, architecture, operations en español; NO cambiar nombres de archivos (romperían links de otros docs y del grafo understand-anything) |

## Contrato
"`npx markdownlint-cli2 \"docs/**/*.md\"` → 0 errors; `pwsh scripts/validate-docs-coverage.ps1` → pasa; todos los links internos relativos `](ruta)` resuelven a archivos existentes; referencias de versión `0.1.5`→`0.2.0` donde aplique"

## Herramientas necesarias
- bash (pwsh, npx markdownlint-cli2, grep/ripgrep)
- read/write/edit
- glob/grep (búsqueda de typos y links)

## Investigation Notes
- No existe `cspell.json` — el spell checker efectivo es el propio agente + markdownlint. NO instalar cspell (over-engineering para un fix de 2-4h; el DoD es "typos corregidos", no "infra de spellcheck").
- `gate-docs-21.yml` corre markdownlint + frontmatter en CI → el contrato markdownlint es verificable en CI igual que local.
- `scripts/validate-docs-coverage.ps1` valida cobertura de docs públicos (sdk/config/error) — debe seguir pasando.
- Versión vigente del proyecto: **0.2.0** (AGENTS.md). Buscar referencias stale a `0.1.5`.
- Git dirty pre-existente: `.opencode/rules/*.md`, `tasks/plan.md` — NO tocar, no incluir en commit.

## Steps

### Step 1: Escanear y listar hallazgos
- **Archivos:** `docs/**/*.md` (335 archivos)
- **Acción:** Correr markdownlint (`npx markdownlint-cli2 "docs/**/*.md"`) y capturar errores. Buscar typos comunes con grep (ripgrep). Listar links internos rotos con escaneo de `](ruta)` relativas.
- **Verify:** Inventario completo de hallazgos (lint errors + typos + links rotos + versiones stale) antes de tocar nada.
- **Estado:** ⬜ PENDING

### Step 2: Corregir typos y links rotos
- **Archivos:** archivos de `docs/**` con hallazgos
- **Acción:** Corregir typos en español/inglés de `docs/` (planning y técnicos). Arreglar links internos apuntando a rutas correctas. NO renombrar archivos.
- **Verify:** `npx markdownlint-cli2 "docs/**/*.md"` → 0 errors; re-escaneo de links → 0 rotos.
- **Estado:** ⬜ PENDING

### Step 3: Corregir versiones desactualizadas
- **Archivos:** docs con referencias a versión
- **Acción:** Actualizar referencias stale `0.1.5` → `0.2.0` donde el doc describa la versión actual del producto. Verificar que no haya otras versiones stale.
- **Verify:** grep `0\.1\.5` en docs → 0 hits (o solo contexto histórico explícito).
- **Estado:** ⬜ PENDING

### Step 4: Validar cobertura + commit
- **Acción:** `pwsh scripts/validate-docs-coverage.ps1` pasa. Commit con conventional commit + ID: `docs(GH-123): fix typos, broken links, and stale version refs in docs/` (solo archivos de docs/, sin los dirty pre-existentes).
- **Verify:** exit 0; `git status` limpio salvo archivos dirty pre-existentes; issue #123 queda listo para cerrar.
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna (standalone)

## Notas
- No modificar estructura de archivos (solo contenido) para no romper links cruzados ni el grafo understand-anything.
- No tocar `.opencode/rules/*.md` ni `tasks/plan.md` (modificados pre-sesión).
- Regla 3 (AGENTS.md): docs técnicos en inglés se mantienen en inglés; español solo en Backlog/progreso/Investigaciones.

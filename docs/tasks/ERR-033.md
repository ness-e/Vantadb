## Context Save (run 2026-08-11, lead directo)
- Commit: fde27213 — fix(ERR-033)
- Fix: `VantaEmbedded::list` (src/sdk/api.rs:502) hacía `options.limit.max(1)` — limit=0 devolvía 1 registro. Short-circuit `limit == 0` → `VantaMemoryListPage { records: vec![], next_cursor: None }` antes de cualquier scan (también evita el full-scan fallback). El MCP memory_list pasa el raw limit — el contrato ahora se respeta.
- Tests: `test_list_zero_limit_returns_no_records` añadido en sdk::api::tests; el test MCP (memory_list limit=0, mcp_tests.rs:1357) ya existía y pasa.
- Verify: cargo check --jobs 2 ok; api suite 35/35; vantadb-mcp 31/31; clippy/fmt ok.
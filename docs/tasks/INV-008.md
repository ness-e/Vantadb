# INV-008: Batch Queries Python SDK — diseño (extensión SearchRequest)

## Metadata
- **Plan file:** (standalone — Backlog.md directo)
- **Fuente:** docs/Backlog.md:198
- **Esfuerzo:** 🟡 1-2d
- **Prioridad:** 🟡
- **Tipo:** Investigación + diseño (Python SDK / PyO3)
- **Turns estimados:** 12
- **Creado:** 2026-08-03
- **last-synced:** 2026-08-03
- **Estado:** ✅ COMPLETED

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-python/vantadb_py/__init__.py` (wrapper async `search_batch`), `vantadb-python/tests/test_sdk.py`, `benchmarks/batch_vs_sequential_bench.py` |
| Callees | `vantadb-python/src/lib.rs:1181` (`search_batch` PyO3, Rayon + GIL release), `VantaEmbedded::search_vector` (src/sdk/builder.rs), `VantaMemorySearchRequest`, `fuse_rrf_with_report`, `validate_metadata` |
| Implicaciones | **PARCIALMENTE IMPLEMENTADO** — `search_batch(vectors, top_k)` ya existe (lib.rs:1181, Rayon `into_par_iter`, GIL released eager, wrapper async). Gap: no acepta `SearchRequest` completo (filters, text_query, namespace, distance_metric). Sin cambios de código en esta tarea — solo diseño de extensión. |

## Contrato
"docs/Investigaciones/INV-008-batch-queries-python-sdk.md existe con: estado actual verificado (qué hace search_batch hoy), gap real vs SearchRequest completo, propuesta de API (firma + conversión eager + py.allow_threads/Rayon), target batch 10 < 3× single, y veredicto método nuevo vs wrapper."

## Herramientas necesarias
- codegraph_explore (ya mapeado: lib.rs search_batch + wrapper async)
- MetaSearchMCP (solo si ambigüedad sobre patrón PyO3 batch)

## Steps
### Step 1: Redactar propuesta
- **Archivos:** `docs/Investigaciones/INV-008-batch-queries-python-sdk.md`
- **Acción:** Documentar estado actual + gap + propuesta de extensión de `search_batch` a `search_batch(queries: List[SearchRequest])`.
- **Verify:** archivo existe y cubre los 4 puntos de alcance del backlog.
- **Estado:** ✅ COMPLETED

## Dependencias
- Ninguna (tarea raíz de investigación).

## Notas
- Gate 2026-08-03: feature base YA implementada → tarea reducida a diseño de extensión, no diseño desde cero.

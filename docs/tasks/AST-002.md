# AST-002 — Rust tipos + re-exports + aliases deprecated

> **Plan:** docs/plans/2026-09-10-anti-stutter.md (Task 2, Wave 1, vanta-worker)
> **Contrato:** `cargo check -p vantadb && cargo clippy -p vantadb --all-targets --all-features -- -D warnings && cargo fmt --check -p vantadb`
> **DoD extra:** `cargo nextest run -p vantadb --profile audit`
> **Estado:** ⏳ IN PROGRESS
> **nextTask:** AST-003

## Spec

Renombrar ~37 `pub Vanta*` a nombres limpios según `scripts/anti_stutter_map.json`
(regla: quitar prefijo `Vanta`, salvo colisión) + mantener
`#[deprecated] pub type Viejo = Nuevo` en cada nivel de re-export.
NO cambiar campos ni serde (wire intacto). OD-3: `SearchHit{node_id,distance}`
vs `MemorySearchHit{record,score,explanation}` — wire NO idéntico → NO fusionar,
alias doble.

Decisiones:
| # | Decisión | Evidencia |
|---|----------|-----------|
| 1 | Alias `pub type` + `#[deprecated]`, no `#[allow]` global | api-contract: deprecación con ruta de migración; `deprecation-and-migration` skill |
| 2 | Callers internos migran a nombres nuevos en el mismo cambio (script mecánico) | `deprecated` lint + `-D warnings`: usos internos de alias romperían clippy |
| 3 | Re-exports de alias con `#[allow(deprecated)]` en el `pub use` | Patrón estándar: silencia el re-export, el consumidor externo sí recibe warning |
| 4 | `VantaMemorySearchDebugReport` (debug-only, fuera del mapa) también se renombra | AST-007 exige `rg Vanta*` → 0; es `pub` aunque `doc(hidden)` |
| 5 | `VantaError→Error`: se mantiene `use thiserror::Error;` (macro namespace, sin colisión); `StdError` ya existe | error.rs:3,5 |
| 6 | `VantaFile→File`: `use std::fs::{File as StdFile, ...}` en vfile.rs (campo `file: Option<StdFile>`, `EncryptionStream<&StdFile>`) | vfile.rs:20,112,460 |

## Impacto mapeado (Regla 0)

**Archivos leídos completos:** src/lib.rs, src/sdk/mod.rs, src/sdk/types.rs,
src/sdk/types/record.rs, src/sdk/types/search.rs, src/sdk/types/graph.rs,
src/sdk/serialization/vector_types.rs, src/sdk/serialization/graph_types.rs.
**Parciales:** src/sdk/builder.rs:1-50, src/config.rs:230-289, src/error.rs:1-100/100-179/330-389,
src/storage/vfile.rs:1-139, src/sdk/serialization/mod.rs (re-exports + validaciones).

**Referencias hacia dentro (deps de archivos a editar):**
record.rs → `super::{u128_serde, MemoryMetadata, Value}`, `node::SparseVector`;
search.rs → `serialization::vector_types::{3 hits}`; types.rs → graph/record/search;
sdk/mod.rs → api/builder/connect/serialization/types; lib.rs → config/error/engine/node/sdk/storage.

**Referencias entrantes (callers):** `SearchHit` ~6 (sdk/api/search.rs:17,90,
convert, query_result_basic); `MemorySearchHit` ~35 (sdk/search/*, benches,
query_result_advanced); `VantaEmbedded` ~50 ficheros; total `Vanta[A-Z]` en
158 ficheros src + 130 ficheros tests/benches. codegraph_explore confirma
blast radius + tests asociados.

**Prohibidos (intactos):** `binary_header.rs:20 VantaHeader` (on-disk),
`error.rs:337-370` strings `VANTADB_*` (wire; solo cambia el prefijo `VantaError::`
→ `Error::`/`Self::` en patterns, sin tocar mapping), `memory_record_from_node`
+ fns libres (fuera de regla), `VFILE_VERSION`, producto `VantaDB` en prosa.

**Veredicto:** rename mecánico de 38 definiciones + 38 aliases deprecated +
re-exports duales en 5 niveles (def → search/graph → types.rs → sdk/mod.rs →
lib.rs; config/error/builder/vfile → lib.rs/sdk). Cero cambio serde/campos.
Riesgo colisión `Error/File/Value/Config/Embedded` mitigado (ver Spec 5-6;
sin `serde_json::Value` en ficheros tocados; sin otro `Config`/`Embedded`).
GATES: D evaluado en plan (✅ DO, alias strategy) → no dispara; V/C activos.

## SDP

`SDP: campaign-executor, source-driven-development, doubt-driven-development,
incremental-implementation, test-driven-development, context-engineering,
api-and-interface-design, deprecation-and-migration`
(contractKeywords: rename, alias, semver, deprecation; + deprecation-and-migration
por contrato de tarea; frontend-ui-engineering descartada — sin web/).

## Steps

- [x] S1 — Task file + Gate D + estado in-progress
- [x] S2 — vfile.rs pre-fix: `File as StdFile` (check verde 37s)
- [x] S3 — Script rename mecánico 38 símbolos, 213 ficheros (src+tests+benches+examples)
- [x] S4 — 38 aliases `#[deprecated(since 0.5.0)]` en def-sites + re-exports duales 5 niveles
- [x] S5 — Fix colisiones: `File`/`StdFile` (init/mod), `QueryResult` engine→namespaced,
  `IndexRebuildReport` storage-vs-sdk en conversions tests, doc-comment detachment vfile,
  snapshots insta (18, solo Debug, sin wire), gramática `an Embedded`
- [x] S6 — Verify contrato + nextest + commit `refactor!:` + cierre

## Verification log

| Step | Comando | Resultado |
|------|---------|-----------|
| S2 | `cargo check -p vantadb` | ✅ 37s, 0 warnings |
| S3+S5 | `cargo check -p vantadb` | ✅ tras StdFile/init/mod + lib QueryResult |
| S4+S5 | `cargo clippy -p vantadb --all-targets --all-features -- -D warnings` | ✅ 35s, 0 warnings |
| S5 | `cargo fmt --check -p vantadb` | ✅ tras `cargo fmt` (reorder imports) |
| S6 | `cargo nextest run -p vantadb --profile audit` | ✅ 2145 passed, 1 skipped (152s) |
| S6 | `cargo check -p vantadb-mcp` (alias strategy E2E) | ✅ 125 deprecation warnings esperados, 0 errores |

CONFUSION resuelta (opción C): `engine::QueryResult` (struct InMemoryEngine, sin
prefijo previo, 0 usuarios vía root) vs `sdk::QueryResult` (map) → root es SDK,
engine queda namespaced. Propuesta: renombrar engine's en follow-up (FIND/AST-007).
NOTICED BUT NOT TOUCHING: `completions/*` regenerados por build.rs (subcomando
`tui`); `VantaMemorySearchDebugReport` sin re-export viejo en types.rs (debug-only,
nunca cruzó `sdk`); `.opencode/opencode.jsonc/Backlog/*.lock` pre-existentes.

# P2R-01: Gates P2-01 retroactivos (TSYS-06 + FND-07) + revisión de los fixes de la wave follow-ups

## Metadata
- **Plan:** docs/plans/2026-08-16-wave-followups.md (W4)
- **Fuente:** follow-up de proceso (item 13): TSYS-06 P2-01 nunca asignado; FND-07 (toca código) sin gate; + los fixes nuevos de esta wave requieren review por agente distinto (Regla 8/P2-01)
- **Estado:** ✅ COMPLETED (stale — plan previo archivado, cerrada por vanta-lead 2026-08-20)
- **Prioridad:** 🟡

## Objetivo
Revisión adversarial (P2-01) de:
1. **Retro TSYS-06** — veredicto del doc `docs/Investigaciones/TSYS-06-chaos-runner.md`: ¿tests puntuales > runner está justificado por evidencia archivo:línea? ¿respeta el orden behavior-changes-primero del diseño §6? (el veredicto ya se implementó — TSYS-06-F1 — revisar que el veredicto era correcto)
2. **Retro FND-07** — código de `src/metrics/core/mod.rs` (record_query_latency) + `src/cli_server.rs` (execute_query): ¿cfg-guard correcto? ¿no-op sin feature prometheus? ¿test del endpoint válido?
3. **Post-fixes de esta wave** (si ya están commiteados al momento de revisar): FND-01-F1 (guard RSS real), FND-02-M3 (race delete-vs-consolidate), FND-06-F1 (zero-norm TS), TSYS-06-F1 (behavior changes task-system), FND-23-F1 (métrica grafos), FND-05-F1 (stubs Python) — veredicto por cambio.

## Archivos clave (lectura)
- `docs/Investigaciones/TSYS-06-chaos-runner.md`, `.opencode/task-system/config/state-tools.mjs`, `docs/architecture/task-system-chaos-resilience.md`
- `src/metrics/core/mod.rs`, `src/cli_server.rs`, `.opencode/rules/server-mcp.md`
- Los diffs de los fixes (git log/diff en develop)
- `docs/Investigaciones/FND-02-multi-index-locks.md`, `docs/architecture/adr/ADR-024-graph-engine-default-telemetry.md`

## Steps
1. DISCOVERY: leer los entregables retro (TSYS-06 doc, FND-07 código) + `git log --oneline -10` + `git show` de los commits de los fixes de la wave follow-ups (el lead commitea por wave — revisar los que existan al momento)
2. Revisar cada item: evidencia vs claim, invariantes respetados (orden de locks, mmap, cfg-guard, R-8 api-contract, deny-by-default state-tools)
3. Emitir veredicto por item: approve / changes-required (con evidencia archivo:línea)
4. Task file + RESULTADO (read-only: NO edita código ni docs, solo el task file)

## Contrato (verify mecánico)
- Veredicto explícito por item (approve/changes-required) con evidencia
- 0 fabricación de salidas: verificar con comandos read-only reales (cargo check/test si aplica, node --test si el task-system ya está commiteado, grep)
- Reporte en el task file (y opcionalmente en docs/reviews/ si el volumen lo amerita)

## Invariantes (handoff)
- NO tocar: docs/Backlog.md, AUD-024.md, verify-log.jsonl, _vanta-cli.ps1, plan file, .opencode/AGENTS.md, .opencode/agents/*, código, docs (excepto su propio task file)
- NO git add/commit; NO campaign_update_task_state
- Nunca implementa — solo veredicto + evidencia

## Fases
- SECURITY: aplica — revisar que los fixes no introduzcan trust boundary/UB (especialmente task-system mjs)
- PERFORMANCE: n/a

## Review (GATE — agente distinto, P2-01)

**Revisor:** vanta-review (segunda opinión, contexto fresco — sesión no participó en la implementación de la wave)
**Enfoque:** red-team de invariantes (orden de locks Regla 8, invariante mmap re-add antes release, cfg-guard prometheus, deny-by-default state-tools, R-8 api-contract, Regla 11 números con fuente) + verificación mecánica de evidencias (node --test, cargo test, vitest, rg/git show). READ-ONLY.
**Cómo se probó:** comandos reales ejecutados — `node --test .opencode/task-system/mcp/*.test.mjs` → 29/29 pass; `cargo test -p vantadb --features prometheus,server --lib metrics_endpoint_emits_real_prometheus_text` → 1 pass; `cargo test -p vantadb --features prometheus --lib test_record_query_latency_observes_histogram` → 1 pass; `cargo test -p vantadb --lib metrics::core` → 28/28 pass; `cargo test -p vantadb --lib test_check_memory_pressure_governor_sync_eviction` → pass; `cargo test -p vantadb --lib delete_vs_consolidate` + `delete_vs_evict_concurrent_no_zombie` → 2 pass; `cargo test -p vantadb --lib test_evict_locked_under_contention_no_deadlock` → pass (6.56s); `cargo test -p vantadb --features prometheus --lib test_graph_ops_counter_init` → pass; `npx vitest run src/__tests__/hardening.test.ts` → 63 pass; `rg` para call sites y remanentes.

### Veredictos por item

| Item | Veredicto | Evidencia |
|---|---|---|
| Retro TSYS-06 (doc) | ✅ approve | Evidencia archivo:línea verificada real: `state-tools.mjs:9-65` STATE_TOOLS, `:73-84` validateAction deny-by-default, `:86-92` matchPattern; diseño §6 en `task-system-chaos-resilience.md:142-156`; veredicto respeta orden behavior-changes-primero (runner diferido). |
| TSYS-06-F1 (e6903b21) | ✅ approve | `node --test` → 29/29 pass real. Parsers extraídos (`parsers.mjs`/`parsers.test.mjs`/`state-persistence.test.mjs`), `rg` 0 hits en campaign-server.mjs. `state-tools.mjs` NO tocado por el commit (deny-by-default intacto; último cambio 887d0f14). Writes atómicos temp+rename, checksum sha1, withPlanLock O_EXCL + stale 10s + timeout 5s, WIP check-and-set. package.json sin deps nuevas (node:crypto/fs built-in). |
| Retro FND-07 (código) | ✅ approve | `record_query_latency` (`src/metrics/core/mod.rs:192`) no-op sin feature vía macro `observe_histogram!` con `#[cfg(feature="prometheus")]` interno (`mod.rs:14-21`). `execute_query` llama en `src/cli_server.rs:613`. Test endpoint `metrics_endpoint_emits_real_prometheus_text` (`cli_server.rs:1345-1392`) válido: query real + verifica /metrics. R-3 agregado a `.opencode/rules/server-mcp.md` (8820bdaf). |
| FND-01-F1 (3f6b4c31) | ✅ approve | Guard usa RSS real del proceso vía `_get_rss_virt` (`metrics/core/mod.rs:486`; linux statm:326, macos task_info:345, windows GetProcessMemoryInfo:371, fallback sysinfo:499); fallback a `effective_bytes` si rss==0 (`stats.rs:104-115`). Test governor pasa. |
| FND-02-M3 (645484ab) | ✅ approve | `consolidate_node_inner` adquiere `insert_lock` para toda la sección crítica + version check contra `volatile_cache` bajo lock (no HNSW — decisión razonada: Rebuild deja Hot nodes en cache sin indexar). Orden Regla 8 intacto: `insert_lock → {volatile_cache, hnsw, backend}`. Invariante mmap re-add antes release intacto: `apply_index_entry_unlocked` corre ANTES de `release_mmap_vector` (`maintenance.rs:359` vs `:382`); comentario "MmapFull→Full load-bearing" (`:247-249`) coincide con el cuerpo (copia owned `Full(vec.clone())` antes del release). Tests `delete_vs_consolidate_no_resurrection` + `delete_vs_evict_concurrent_no_zombie` → 2 pass. |
| FND-06-F1 (a93b4abc) | ✅ approve | Fallback euclidean silencioso eliminado; core rechaza zero-norm cosine ERR-028 (`src/sdk/search/mod.rs:106-120`); ambos backends alineados (R-8: bindings = glue, sin lógica duplicada). `npx vitest run hardening.test.ts` → 63/63 pass. |
| FND-05-F1 (00cd45d0) | ✅ approve | Stubs alineados con runtime: `connect` existe (`vantadb-python/src/lib.rs:1942`), `add_function` en lib.rs:1961, `__version__` es str (`m.add`:1962). Limpieza maturin features correcta (pyo3/extension-module implícitos). |
| FND-02-M2 (ebd0b233) | ✅ approve | Stress `test_evict_locked_under_contention_no_deadlock` (256 candidatos, 7 threads, watchdog 60s) → pass 6.56s, assert `evicted > 0`. |
| FND-23-F1 (60fb32ef) | ✅ approve | `record_graph_op` cfg-guard doble on/off (`metrics/core/mod.rs:223-232`); registry `GRAPH_OPS_TOTAL` IntCounterVec con labels; call sites reales (traverse `engine.rs:355`, add/remove_edge `sdk/api.rs`, edge_query `sdk/graph.rs`). Test `test_graph_ops_counter_init` con prometheus → pass. |
| FND-13-F2 (c94da41e) | ✅ approve | BENCHMARKS.md con provenance commands + nota de superseded (`BENCHMARKS.md:65`); claim sin fuente marcado. Regla 11 en PERFORMANCE_TUNING. |
| FND-13-F1 (05f77250) | 🔴 changes-required (menor) | **Desincronización con fuente citada (Regla 11).** F1 (12:15:12) es POSTERIOR a F2 (12:15:06) — el implementador tenía §2 ya actualizado y aun así: `web/src/components/vanta/vanta-data.ts:36` cita "§2 SDK Lexical Search p50" con `115.334ms` pero `BENCHMARKS.md:55` ahora dice `0.0035ms` (outlier ⚠️); `vanta-data.ts:155-157/162/174` usan `10.678ms / 18.988ms / 95 ops/sec / 179.810ms` que `BENCHMARKS.md:65` declara "superseded by this local baseline" (nuevos: 74 ops/sec, 13.174ms, 3.114ms). La fuente citada es ley (Regla 11) → re-sincronizar vanta-data.ts con §2 actual o citar la sección/fecha correcta. Los claims principales (1.2ms:33, 3,636:145, 2.80x:147, 0.956:27, 39.74:190) SÍ coinciden con BENCHMARKS.md actual — el fix cumplió su objetivo central, queda este desync de la serie vieja. |

### Hallazgos
- **🔴 Bloqueante (1, menor):** desync Regla 11 en FND-13-F1 — evidencia: `web/src/components/vanta/vanta-data.ts:36,155-157,162,174` vs `docs/operations/BENCHMARKS.md:55,65` (números superseded citando §2 actualizada).
- **🟡 Mejora (1):** comentario del test FND-02-M3 en `src/storage/engine/tests/ops.rs` dice "version check contra HNSW" pero el código chequea `volatile_cache` (correcto según doc §9; el comentario quedó impreciso). No afecta funcionalidad.
- **🟢 Nota (2):** (1) `cargo test` solo acepta UN filtro TESTNAME (dos filtros → "unexpected argument"); usar un substring común o una corrida por filtro. (2) El comentario `maintenance.rs:247-249` "MmapFull→Full copy below" es correcto pero la conversión no es literal en el cuerpo: el nodo llega del cache Hot ya materializado como `Full` y `apply_index_entry_unlocked` copia owned — invariante cumplido de facto por el flujo del cache.

### Alternativas evaluadas (brainstorm)
- FND-02-M3 version check contra HNSW vs `volatile_cache` → se descarta HNSW: `batch_insert` con InsertMode::Rebuild indexa en HNSW pero el nodo Hot vive en cache; chequear HNSW strandearía nodos legítimos en cache para siempre. `volatile_cache` es el liveness signal correcto (delete() lo remueve bajo insert_lock).
- FND-13-F1: actualizar números web vs citar sección/fecha de la serie CI vieja → recomendado: usar los valores vigentes de §2 (o citar la serie con su fecha explícita) para cumplir "la fuente citada es ley".

### Recomendaciones (iterate)
1. (Prioridad alta, 1 línea) Re-sincronizar `vanta-data.ts:36,155-157,162,174` con los valores vigentes de `BENCHMARKS.md` §2/§5/§7 (74 ops/sec, 13.174ms, 3.114ms, 0.0035ms outlier con nota) o citar la serie CI vieja con fecha — regla: el número debe coincidir con la fuente citada.
2. (Prioridad baja) Corregir comentario del test M3 en `ops.rs` ("contra volatile_cache", no "HNSW").
3. (Proceso) TSYS-06: el runner queda diferido por §6 — registrar el behavior change restante (runner chaos en task-system) como item de backlog explícito para que no se pierda.

## Resultado
```
RESULTADO: 🟡 INCOMPLETO — revisión completa, 1 item con changes-required (FND-13-F1 desync Regla 11)
VEREDICTOS: {retro TSYS-06: approve, TSYS-06-F1: approve, retro FND-07: approve, FND-01-F1: approve, FND-02-M3: approve, FND-06-F1: approve, FND-05-F1: approve, FND-02-M2: approve, FND-23-F1: approve, FND-13-F2: approve, FND-13-F1: changes-required (menor)}
ARCHIVOS: .opencode/skills/campaign-executor/tasks/P2R-01.md, docs/Investigaciones/TSYS-06-chaos-runner.md, docs/architecture/task-system-chaos-resilience.md, .opencode/task-system/config/state-tools.mjs, .opencode/task-system/mcp/{parsers.mjs,parsers.test.mjs,state-persistence.test.mjs,campaign-server.mjs}, src/metrics/core/mod.rs, src/cli_server.rs, src/storage/engine/maintenance.rs, src/storage/engine/stats.rs, src/storage/engine/tests/ops.rs, src/sdk/search/mod.rs, vantadb-ts/src/{vantadb.ts,native.ts,__tests__/hardening.test.ts}, vantadb-python/vantadb_py/__init__.py, vantadb-python/src/lib.rs, web/src/components/vanta/vanta-data.ts, docs/operations/BENCHMARKS.md, .opencode/rules/{api-contract.md,server-mcp.md}
COMMIT_HASH: ninguno (lead commitea; revisión READ-ONLY — no se escribió código ni docs)
BLOQUEO: ninguno
```
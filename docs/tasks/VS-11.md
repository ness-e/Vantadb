# VS-11: Bridge Tauri — enriquecer DTO del registro (nuevo, crítico)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase0.md`
- **Creado:** 2026-08-18
- **last-synced:** 2026-08-18
- **Estado:** ✅ DONE
- **Tipo:** bridge desktop (Rust Tauri + TS)
- **Bloqueante de:** VS-05 (columnas version/updated_at/TTL en grid) y VS-06 (timestamps/TTL/vector en Inspector) — el DTO no tenía `version`, `node_id`, `updated_at_ms`, `expires_at_ms`, `vector`

## Impacto mapeado (Regla 0)
- **Archivos modificados (4):** `desktop/src-tauri/src/connections/types.rs` (DTO `MemoryRecord` enriquecido: `vector` renombrado de `embedding`, `updated_at_ms`, `version`, `node_id`, `sparse_vector`, `expires_at_ms` — todos `Option` + `#[serde(default)]`; test roundtrip extendido con aserciones de wire shape), `desktop/src-tauri/src/connections/native.rs` (`record_to_memory` rellena todos los campos nuevos desde `VantaMemoryRecord`), `desktop/src-tauri/src/connections/server.rs` (`node_to_record` — cambio aditivo OBLIGADO por la struct literal: campos nuevos a `None` excepto `node_id` desde `NodeDTO.id`), `desktop/src/vanta.ts` (interfaz `MemoryRecord` tipada: `vector?: number[] | null`, `updated_at_ms?`, `version?`, `node_id?: string | null`, `sparse_vector?: Record<string, number> | null`, `expires_at_ms?: number | null`).
- **Referencias hacia dentro (quién los importa):** `MemoryRecord` es consumido por VS-05 (grid) y VS-06 (Inspector); `vantaPut`/`get`/`list` (VS-10/desktop previo) ya devuelven `MemoryRecord` y ahora traen los campos nuevos sin cambiar firma.
- **Referencias hacia fuera:** `vantadb::VantaMemoryRecord` (`src/sdk/types.rs:175-201`, SOLO LECTURA — fuente de verdad), `vantadb::SparseVector` (`src/node/vector_data.rs:30`, `BTreeMap<u32,f32>`), `NodeDTO` (`connections/wire_types.rs:68`, SOLO LECTURA).
- **Fuente protegida (SOLO LECTURA):** `src/` (core Rust), `desktop/src-tauri/src/commands/data.rs`, `desktop/src-tauri/src/connections/trait.rs`, `desktop/src-tauri/src/connections/manager.rs` (VS-10), `desktop/src/App.tsx`, `desktop/src/components/` (VS-03). Docs intactos.
- **Archivos que NO toco (dominio ajeno):** `src/` completo — el core ya entrega todos los campos; el bridge solo los propaga.
- **Veredicto:** aditivo en el wire (campos nuevos con `#[serde(default)]` → deserialización backward-compatible); renombra `embedding`→`vector` en el DTO y `vanta.ts` — ningún consumidor del frontend leía `.embedding` (grep previo: solo el type `unknown` en vanta.ts).

## Blast Radius
| Callers | Callees | Implicaciones |
|---|---|---|
| VS-05/VS-06 (futuro), UI vía `invoke("vanta_get"|"vanta_list"|"vanta_put")` | `MemoryRecord` (types.rs) → `record_to_memory` (native.rs) → `VantaMemoryRecord` del core | Native rellena TODO (version/updated/node_id/expires/vector/sparse); Server (IQL) deja timestamps/version/expires en `null` porque `NodeDTO` no los expone — la UI debe renderizar "—" cuando falten. |

## Contrato
- `cargo check` en `desktop/src-tauri` verde (workspace propio de 1 member — ver Herramientas). Warning `cfg(mobile)` PRE-EXISTENTE en lib.rs:45.
- `cargo test -j 1` en `desktop/src-tauri` verde (41 tests, incl. `memory_record_roundtrip` enriquecido + aserciones de wire: `"version":3` número, `"node_id":"42"` string, `expires_at_ms`/`vector` presentes).
- `npm run build` en `desktop/` verde (tsc strict + vite; 48 modules, JS 230.02 kB/gzip 71.92 kB — sin delta vs VS-03).
- `MemoryRecord` del bridge contiene TODOS los campos de `VantaMemoryRecord`; `record_to_memory` (native) los rellena; `vanta.ts` tipado con `version: number|null`, `node_id: string|null`, `updated_at_ms: number|null`, `expires_at_ms: number|null`, `vector: number[]|null`, `sparse_vector: Record<string,number>|null`.

## Herramientas
- `cargo check`/`cargo test -j 1` DENTRO de `desktop/src-tauri` (workspace aislado de 1 member a propósito — NUNCA `cargo check -p` desde la raíz del repo, `vantadb-desktop` no es member del workspace raíz)
- `npm run build` en `desktop/`
- codegraph (blast radius), `src/sdk/types.rs` + `src/node/vector_data.rs` (solo lectura)

## Steps
### Step 1: Enriquecer DTO `MemoryRecord` en types.rs
- **Archivos:** `desktop/src-tauri/src/connections/types.rs`
- **Acción:** renombrar `embedding` → `vector` (espeja `VantaMemoryRecord.vector`) y añadir `updated_at_ms: Option<u64>`, `version: Option<u64>`, `node_id: Option<String>`, `sparse_vector: Option<HashMap<u32, f32>>`, `expires_at_ms: Option<u64>` — todos con `#[serde(default)]` + doc. `Option` porque el backend server (IQL) no puede proveerlos (mismo patrón que `created_at_ms`); native los llena siempre. `node_id` como `String` porque el core serializa `u128` como string (`u128_serde`) y JS pierde precisión > 2^53. Test `memory_record_roundtrip` extendido + asserts de wire shape.
- **Verify:** `cargo check`
- **Estado:** ✅ DONE

### Step 2: Completar mapeo nativo `record_to_memory`
- **Archivos:** `desktop/src-tauri/src/connections/native.rs`
- **Acción:** `vector: r.vector` (move), `updated_at_ms: Some(r.updated_at_ms)`, `version: Some(r.version)`, `node_id: Some(r.node_id.to_string())`, `sparse_vector: r.sparse_vector.map(|sv| sv.0.into_iter().collect())` (BTreeMap→HashMap, wire = objeto `{"dim": coef}`), `expires_at_ms: r.expires_at_ms`.
- **Verify:** `cargo test -j 1`
- **Estado:** ✅ DONE

### Step 3: Adaptar `node_to_record` en server.rs (compilación obligada)
- **Archivos:** `desktop/src-tauri/src/connections/server.rs`
- **Acción:** campos nuevos a `None` salvo `node_id: Some(node.id.to_string())` (NodeDTO ya tiene `id: u128`). El adapter HTTP deja timestamps/version/expires en null porque el wire IQL no los trae — honesto, no inventado.
- **Verify:** `cargo check`
- **Estado:** ✅ DONE

### Step 4: Tipado en vanta.ts
- **Archivos:** `desktop/src/vanta.ts`
- **Acción:** interfaz `MemoryRecord`: `embedding?: unknown` → `vector?: number[] | null`; añadir `updated_at_ms?: number | null`, `version?: number | null`, `node_id?: string | null`, `sparse_vector?: Record<string, number> | null`, `expires_at_ms?: number | null`. Los inputs `IngestItem.embedding`/`SearchQuery.embedding` NO cambian (espejan los DTOs Rust homónimos).
- **Verify:** `npm run build` (tsc strict)
- **Estado:** ✅ DONE

### Step 5: Cierre — verify full + task file (sin commit)
- **Acción:** `cargo check` ✅, `cargo test -j 1` ✅ (41 tests: 26 lib + 11 server client + 2 real + 2), `npm run build` ✅. Este task file. NO commit (el lead commitea).
- **Verify:** `git status --short` — solo los 4 archivos de VS-11.
- **Estado:** ✅ DONE

## Dependencias
- Core `VantaMemoryRecord` (todos los campos) — YA existe en `src/sdk/types.rs:175-201` (sin cambios).
- VS-10 (`vanta_put`/`vantaPut`) — devuelve `MemoryRecord`; VS-11 no lo toca, el comando ya trae los campos nuevos vía `record_to_memory`.
- VS-05 (grid) / VS-06 (Inspector) — consumen los campos nuevos para columnas version/updated_at/TTL y vector.

## Notas
- **Wire shape garantizada por test:** `version` serializa como número JSON, `node_id` como string (u128 → string, consistente con `vantadb-ts` que acepta `string|number` y con WASM que devuelve string), `sparse_vector` como objeto `{"dim": coef}` (BTreeMap→HashMap de `SparseVector.0`).
- **Backward-compat:** campos con `#[serde(default)]` → JSON viejo (sin campos nuevos) deserializa igual; consumidores de VS-03 (namespace/key/text/metadata/created_at_ms) intactos.
- **Ponytail:** sin tipo nuevo (`SparseVector` del core NO se reexporta al bridge; se aplana a `HashMap<u32,f32>` en el DTO — el bridge es thin wrapper, no duplica tipos core); `node_id` string evita custom serde en el DTO.
- **Entorno Windows:** `cargo test` completo sin `-j 1` falla con `os error 1455` (paging file) — pre-existente, usar `-j 1`.
- **Warning pre-existente:** `unexpected_cfgs` para `cfg(mobile)` en lib.rs:45 (scaffold Tauri), no introducido aquí.
- **Backend server (HTTP):** campos version/updated_at/expires llegan `null` — VS-05/VS-06 deben renderizar "—"/empty state cuando falten (el adapter IQL no expone esas columnas del nodo).

## Context Save Point
- **Fecha:** 2026-08-18
- **Branch:** develop
- **CI pendiente:** no (bridge desktop aislado del workspace raíz; verify core no aplica — no se tocó `src/`)
- **Decisiones:** campos nuevos `Option` + `#[serde(default)]` (patrón `created_at_ms`, server no puede proveerlos); `embedding`→`vector` (nadie consumía `.embedding`; espeja `VantaMemoryRecord`); `node_id` como `String` (u128 core serializa string, JS pierde precisión > 2^53); `sparse_vector` aplanado a `HashMap<u32,f32>` (sin reexportar `SparseVector` del core).
- **Problemas conocidos:** `cargo test` sin `-j 1` falla por paging file del entorno; server backend entrega timestamps/version/expires en `null`.
- **Próxima tarea:** VS-05 (grid virtualizado, consume campos nuevos) o VS-CORE-01 (cursor) — en paralelo según el plan.

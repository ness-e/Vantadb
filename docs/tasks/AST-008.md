# AST-008 — Python directo: pyclass + VantaPy* + search namespaced

> **Plan:** `docs/plans/2026-09-11-anti-stutter-cierre.md` (Task 1)
> **Estado:** ⏳ IN PROGRESS
> **Contrato:** `cargo check -p vantadb_py && cargo fmt --check -p vantadb_py`
> **DoD extra:** `test_sdk.py` + `test_stub_drift.py` + subclients/async verdes
> **Ruta:** vanta-worker · **Esfuerzo:** 🟡 · **Appetite:** max 1d
> **Wave 0** paralela con AST-009 (archivos disjuntos) · **nextTask:** AST-010

## Spec (decisiones — OD-2=B, OD-4 cerrados por usuario: directo, sin compat)

| # | Decisión | Evidencia / precedente |
|---|----------|------------------------|
| D1 | `VantaDB` → `Client` (rename del struct; sin `#[pyclass(name)]` el nombre del struct manda) | Mapa TS/WASM `VantaDB→Client`; alias AST-003 `Client`; README `vantadb-python/README.md:23` ya documenta `Client` canónico |
| D2 | Nombres Python visibles: `VantaMemoryRecord→Record`, `VantaListResult→ListResult`, `VantaSearchHit→SearchHit`, `VantaListResultIter→ListResultIter` (solo `#[pyclass(name)]` + repr + mensajes; structs Rust `VantaPy*` NO se renombran — interno, evita colisión con `sdk::MemoryRecord` importado en types.rs) | README naming note (canónicos `Record/SearchHit/ListResult`); regla anti-stutter (strip `Vanta`); ADR-041 ejemplo `MemoryRecord`→ se prefiere `Record` por README ya publicado + alias AST-003 |
| D3 | `VantaVector/VantaVectorIter/VantaError*` NO se tocan (fuera de scope; `vector.rs`/`convert.rs` intactos). Alias `Vector = VantaVector` se CONSERVA como puente → rename nativo en AST-010 | Scope discipline (Regla 0.5); AST-010 owns `vantadb-python/**` |
| D4 | Decisión B: flat `search_memory→search` (híbrido namespaced); flat `search` (nodo-plano ANN) `→search_vector` (ANN puro); viejo `search` se elimina. `search_batch` (nodo batch) intacto | Paridad TS (`MemoryClient.search` híbrido + `searchVector` ANN puro, `vantadb-ts/src/vantadb.ts:49-54`); plan Verificación real |
| D5 | Se eliminan aliases AST-003 rotos por el rename (`Client/Record/Hit/SearchHit/ListResult/AsyncClient`); `get/list/delete` en MemoryClient SE CONSERVAN (flat no puede acortarse — hazard BINDINGS_NAMESPACES; son API limpia, no deprecated) | Plan l19 "rename DIRECTO, se eliminan aliases"; hazard flat `get/delete` node-level |
| D6 | `AsyncVantaDB`: `search_memory→search`, `search→search_vector` (cuerpo → `search_vector` nativo); `list` se conserva; `AsyncVantaDB` (clase) NO se renombra | Espejo del flat; scope |
| D7 | Docs (`docs/api/PYTHON_SDK.md`, READMEs) e `integrations/` + `examples/python/` NO se tocan → deuda para lead/AST-010 (Gate C) | AST-010 owns `docs/api/**`, `README.md`; integrations son packages con releases propios |

## Impacto mapeado (Regla 0)

**Archivos leídos completos:** `vantadb-python/src/lib.rs` (2423L), `src/types.rs` (417L), `src/vector.rs` (120L), `vantadb_py/__init__.py` (522L), `vantadb_py.pyi` (536L), `__init__.pyi` (281L), `tests/test_subclients.py` (324L), `tests/test_stub_drift.py` (335L), `tests/conftest.py`, `tests/test_import_alias.py`, `verify_published_wheel.py`, `vantadb/__init__.py` (28L), `scripts/anti_stutter_map.json`, `ADR-041`, `.opencode/rules/python-bindings.md` + `api-contract.md`, `vantadb-ts/src/vantadb.ts` (referencia paridad).

**Referencias hacia dentro (lo que el cambio necesita):** `sdk::{Embedded, MemoryInput, MemoryListOptions, MemorySearchRequest, NodeInput}`, `MAX_K`, `convert::*` (map_vanta_error, dict builders), `vector::{VantaVector, VantaVectorIter}` (intacto), `types::{VantaPyListResult, VantaPyMemoryRecord, VantaPySearchHit}` (solo `name=` cambia).

**Referencias entrantes (a actualizar):** `MemoryClient.db: Py<VantaDB>` ×4 + `open_vantadb/connect -> PyResult<VantaDB>`; `m.add_class` (automático por struct); `__init__.py` imports + `__all__`; `vantadb/__init__.py` `__all__`; stubs; tests (`test_sdk` 140 sitios, `test_load` 19, `test_perf_15_16` 20, `test_async_smoke` 8, `test_subclients`, `test_stub_drift` asserts `["VantaDB"]`, `conftest` rebind, `test_migration` import, `test_close_concurrency`); `verify_published_wheel.py`.

**Veredicto:** impacto CONTENIDO a `vantadb-python/` (Rust binding + stubs + wrapper + tests). NADA en `src/` core, TS, WASM (AST-009), `vector.rs`, `convert.rs`. Colaterales fuera de scope: `integrations/*` (12+ archivos usan `VantaDB/search_memory`), `examples/python/*` (9 archivos), docs API — documentados como deuda, no se tocan.

## SDP

`SDP: campaign-executor, source-driven-development, incremental-implementation, test-driven-development, context-engineering, doubt-driven-development, api-and-interface-design` (discover_skills_v2 BUILD; `frontend-ui-engineering` descartada — sin `web/`).

## Steps

- [x] **S0 — Discovery + task file** (contrato: este archivo completo + Regla 0) ✅
- [x] **S1 — Rust lib.rs: struct `VantaDB→Client`** ✅ `cargo check` verde
- [x] **S2 — Rust lib.rs: search reshape** (`search_memory→search`, `search→search_vector`, forwards + comentarios OD-2) ✅ `cargo check` verde
- [x] **S3 — Rust types.rs: `name=` + reprs** ✅ `cargo check + fmt` verde (contrato)
- [x] **S4 — Stubs + wrapper** (`.pyi` `class Client`, `__init__.py`, `__init__.pyi`, `vantadb/__init__.py`, Async search/search_vector) ✅
- [x] **S5 — Tests + wheel-script** (replaceAll mecánicos en 11 archivos) ✅ `maturin develop` + pytest: **135 passed** (29 drift+subclients, 75 sdk, 26 async/misc, 5 load; 4 skipped + 4 deselected pre-existentes)
- [x] **S6 — Verify + commit** (contrato + clippy limpio; plan file update; commit; sin push) ✅

## Verificación (evidencia mecánica)

- `cargo check -p vantadb_py` ✅ (Finished dev profile)
- `cargo fmt --check -p vantadb_py` ✅ (sin diff; se corrigió indent doc l1892 comido por un replace)
- `cargo clippy -p vantadb_py --all-targets` ✅ (sin warnings)
- `maturin develop` ✅ (wheel abi3-py3.11, 28.6s)
- Smoke nativo: `Client/search/search_vector/memory.search/memory.search_vector` ✅ + `repr Client(...)` ✅
- `pytest test_stub_drift + test_subclients` → **29 passed** ✅
- `pytest test_sdk` → **75 passed** ✅
- `pytest async/close/import/migration/perf/typed` → **26 passed, 4 skipped** (skips pre-existentes: deps opcionales chroma/lancedb) ✅
- `pytest test_load` → **5 passed, 4 deselected** (deselects pre-existentes) ✅

## Gates

- **P:** no dispara (refactor sobre archivos del task; sin símbolos públicos NUEVOS — solo renames instruidos).
- **D:** evaluado + pre-cerrado (OD-2=B, OD-4 por usuario; ambigüedad `PyRecord` resuelta por evidencia README+ADR → `Record`; blast radius ≤ `vantadb-python/`).
- **V:** NO disparado (0 fallas de verify; único incidente — indent doc comido por un replace — detectado por `fmt --check` y corregido en el acto, sin reintento).
- **C:** DISPARADO — colaterales fuera de scope: `integrations/*` (12+ archivos usan `Client`-viejos: `VantaDB/search_memory`), `examples/python/*` (9 archivos), `docs/api/PYTHON_SDK.md` + READMEs quedan con nombres viejos → deuda al lead (¿follow-up o extender AST-010?).

## Notas

- PyO3: con `#[pyclass(name)]` manda `name`; sin él (caso `VantaDB`) manda el struct → S1 renombra struct; `m.add_class` hereda el nombre, nada que cambiar ahí.
- Pickle: solo `VantaVector` tiene `__getstate__/__setstate__` (intacto) → riesgo pre-mortem pickling NO aplica a las clases renombradas.
- `NOTICED BUT NOT TOUCHING: sanity_check.py, smoke_test_extended.py` (scripts raíz sin referencias en CI — usan `VantaDB/search_memory`; si se resucitan, migrar a `Client/search`) → ¿crear task?
- `NOTICED BUT NOT TOUCHING: VantaVector/VantaVectorIter/VantaError*` nativos → AST-010 (contrato rg exige `__init__.py` limpio).

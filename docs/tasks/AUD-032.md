# AUD-032: Split vantadb-mcp monolith src/lib.rs into logical modules

## Metadata
- **Plan file:** docs/Backlog.md — línea AUD-032 (no aplica plan file; tarea de backlog)
- **Fuente:** docs/Backlog.md línea AUD-032
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🟠
- **Tipo:** Rust
- **Turns estimados:** 20
- **Creado:** 2026-08-14T09:10
- **last-synced:** 2026-08-14T09:10
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 — resueltas durante ejecución
- **Pendientes (downhill):** 0 — steps 1-5 ejecutados y verificados

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-server/src/main.rs:58` (`run_stdio_server`), `vantadb-server/tests/mcp_integration.rs:12` (`handle_initialize`, `handle_tools_call`, `handle_tools_list`, `McpConfig`), `vantadb-mcp/tests/mcp_tests.rs` (~31 tests, `use vantadb_mcp::*;`), `tests/version_coherence.rs:97` (grep de `metadata::reported_version()` dentro de `vantadb-mcp/src/lib.rs`) |
| Callees | `vantadb` crate: `executor::{Executor, ExecutionResult}`, `metadata`, `storage::StorageEngine`, `node::FieldValue` |
| Implicaciones | API pública NO cambia (lib.rs re-exporta todo lo `pub` actual). `vantadb-mcp/Cargo.toml` NO se toca (gate version_coherence). `version_coherence.rs:97` DEBE actualizarse: el string `metadata::reported_version()` vive en lib.rs:734 (handle_initialize) → pasa a `src/handlers/initialize.rs`; el test lee el archivo y aserta contenido. Docs `pub` se mantienen (`#![warn(missing_docs)]`). Tests existentes (~31 mcp_tests + integración server) deben pasar sin cambios. |

## Impacto mapeado (Regla 0)

> **GATE ANTES DE CUALQUIER EDICIÓN (MUST — AGENTS.md Regla 0):** todo archivo
> que se vaya a modificar/eliminar se lee completo y se mapea su impacto ANTES
> del primer step de edición.

- **Archivos leídos (completos):** `vantadb-mcp/src/lib.rs` (1768 líneas — leído completo por rangos en sesiones previas y esta; gap 139-1260 cerrado con Read 139-1260), `tests/version_coherence.rs` (124 líneas), `vantadb-mcp/tests/mcp_tests.rs` (~31 tests inventariados), `vantadb-server/src/main.rs:58`, `vantadb-server/tests/mcp_integration.rs:12`, `.opencode/task-system/prompts/task.md` (template, 321 líneas)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** lib.rs importa de `vantadb::{executor, metadata, storage, node}` + `serde`, `serde_json`, `tokio::io`, `tracing`, `std::sync::atomic`, `std::time`, `std::sync::Arc` (master imports líneas 8-17)
- **Archivos que referencian a los editados (referencias entrantes):** `vantadb-server/src/main.rs`, `vantadb-server/tests/mcp_integration.rs`, `vantadb-mcp/tests/mcp_tests.rs`, `tests/version_coherence.rs`
- **Veredicto impacto:** MEDIO — refactor puramente estructural (split 1:1 por secciones marcadas `// ──`), sin cambio de lógica ni API. Riesgo: olvidar un `pub use` o `pub(crate)` en visibilidad cruzada. Mitigación: lib.rs re-exporta la superficie pub exacta (McpConfig, McpError, 7 handlers, run_stdio_server) y los items internos quedan `pub(crate)`; `cargo nextest` + clippy + fmt cierran el contrato.

## Contrato
`cargo nextest run --profile audit -p vantadb-mcp --build-jobs 2` pasa Y `cargo clippy -p vantadb-mcp --all-targets --all-features -- -D warnings` pasa Y `cargo fmt --check` limpio Y `cargo nextest run --profile audit --test version_coherence --build-jobs 2` pasa (assertion actualizada a `src/handlers/initialize.rs`). Comportamiento específico: API pública byte-idéntica — los ~31 tests de `vantadb-mcp/tests/mcp_tests.rs` y `vantadb-server/tests/mcp_integration.rs` pasan sin modificarse.

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** (1) `vantadb-mcp/Cargo.toml` sin cambios; (2) superficie pub de `vantadb_mcp` idéntica (McpConfig, McpError, handle_* ×7, run_stdio_server); (3) `#[allow(deprecated)]` en campos `filters:` de `VantaMemoryListOptions` preservado; (4) `#![warn(missing_docs)]` sin warnings nuevos; (5) tests de unidad `parse_metadata_delegates_lists_and_null_rejects_objects` y `active_request_guard_decrements_on_panic` migran con su código (validation.rs y metrics.rs respectivamente).
- **Comandos de verificación:** `cargo nextest run --profile audit -p vantadb-mcp --build-jobs 2` (exit 0) · `cargo clippy -p vantadb-mcp --all-targets --all-features -- -D warnings` (exit 0) · `cargo fmt --check` (exit 0) · `cargo nextest run --profile audit --test version_coherence --build-jobs 2` (exit 0)
- **Deuda pendiente:** ninguna (split 1:1, sin deuda nueva; elimina la deuda de monolitismo del crate)

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | `# AUD-032: Split vantadb-mcp monolith src/lib.rs into logical modules` |
| `lastAction` | Steps 1-5 completados: slicing a 12 módulos + facade lib.rs + version_coherence actualizado + verificación completa |
| `result` | ✅ `COMPLETE` — contrato verificado (nextest 2/2 + version_coherence 1/1 + clippy -D warnings + fmt clean + cross server 5/5) + review P2-01 approve |
| `nextAction` | Tarea cerrada — siguiente tarea del backlog según docs/Backlog.md |
| `contract` | `## Contrato` + `## Invariantes de dominio` + evidencia de verify commands |
| `nextTask` | Siguiente tarea del backlog según docs/Backlog.md |

## Deuda técnica (Regla 6 — MUST)

Sin deuda | (refactor estructural puro; split 1:1 de secciones existentes, cero lógica nueva)

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato verificable se cumple (nextest + clippy + fmt + version_coherence) + tests existentes pasan sin cambios |
| **Commit** | No aplica en esta sesión (worker no commitea — lo prepara vanta-worker, lo ejecuta vanta-lead; sesión sin commit por protocolo) |
| **Release** | No aplica (sin release en esta sesión) |

## Herramientas necesarias
- cargo-mcp (check, clippy, fmt)
- rust-analyzer-mcp (diagnostics)
- campaign_verify_cmd (verify con exit code)

## Investigation Notes
- `tests/version_coherence.rs:97-101` aserta que `vantadb-mcp/src/lib.rs` contiene `metadata::reported_version()`. Tras el split el string vive en `src/handlers/initialize.rs` (lib.rs:734). Fix honesto: actualizar el path en el test a `src/handlers/initialize.rs` (version_coherence.rs NO está prohibido; Cargo.toml sí).
- Secciones del monolith confirmadas por grep `^// ──`: config 19, error 102, wire types 165, metrics 185, validation 219, server 478, initialize 726, resources 744, prompts 883, tools 980. Tests: `mod tests` 1667, archivo termina 1768. Split de tests: parse_metadata 1679-1745 → validation.rs; active_request_guard 1751-1767 → metrics.rs.
- Slicing se hace con script PowerShell sobre el archivo en disco (extracción exacta por líneas, sin retipeo) — eliminina riesgo de transcripción de ~1700 líneas.

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — merge decisions resueltas: espejar secciones 1:1 (12 módulos + lib.rs) |
| Pendientes de ejecución (downhill) | 0 — steps 1-5 ejecutados y verificados |
| % completado | 100% |

## Steps

### Step 1: Crear módulos con slicing script
- **Archivos:** `vantadb-mcp/src/{config,axioms,error,protocol,metrics,validation,server}.rs`, `vantadb-mcp/src/handlers/{mod,initialize,resources,prompts,tools}.rs`
- **Acción:** script PowerShell extrae de `src/lib.rs` en disco los rangos exactos (21-74, 76-100, 102-163, 165-183, 185-217, 219-476, 478-724, 726-742, 744-881, 883-978, 980-1664, tests 1667-1745 y 1747-1767 envueltos en `#[cfg(test)] mod tests`) y escribe cada archivo con header de docs + imports por módulo + visibilidad (`pub(crate)` en protocol/metrics/validation/axioms)
- **Verify:** `cargo check -p vantadb-mcp` (fallará hasta Step 3 — aceptable intermedio) / verificación de rango: `(Get-Content src/lib.rs).Count -eq 1768`

### Step 2: Reescribir lib.rs como facade
- **Archivos:** `vantadb-mcp/src/lib.rs`
- **Acción:** Write con crate docs + `#![warn(missing_docs)]` + `mod` decls + `pub use` de superficie pub (McpConfig, McpError, handle_initialize, handle_list_resources, handle_read_resource, handle_list_prompts, handle_get_prompt, handle_tools_list, handle_tools_call, run_stdio_server)
- **Verify:** `cargo check -p vantadb-mcp`

### Step 3: Actualizar version_coherence.rs
- **Archivos:** `tests/version_coherence.rs`
- **Acción:** línea 97: apuntar el read a `vantadb-mcp/src/handlers/initialize.rs`
- **Verify:** `cargo nextest run --profile audit --test version_coherence --build-jobs 2`

### Step 4: Formato + verify completo
- **Archivos:** (ninguno)
- **Acción:** `cargo fmt` y correr contrato completo
- **Verify:** `cargo nextest run --profile audit -p vantadb-mcp --build-jobs 2` + `cargo clippy -p vantadb-mcp --all-targets --all-features -- -D warnings` + `cargo fmt --check` (todo exit 0 vía campaign_verify_cmd)

### Step 5: Verificación de integración cruzada
- **Archivos:** (ninguno)
- **Acción:** `cargo nextest run --profile audit -p vantadb-server --build-jobs 2` (tests mcp_integration) y confirmar que `vantadb-mcp/tests/mcp_tests.rs` (~31) pasó sin edición
- **Verify:** exit 0 ambos

## Dependencias
- Task N-1: [AUD-032 previo] — tarea de backlog sin dependencias previas de split (versión actual del monolith ya auditada)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. Sin esto registrado, la
> tarea no está COMPLETED.

- **Revisor:** vanta-review (segunda opinión, P2-01, delegado vía task tool)
- **Enfoque:** lectura fresca de todos los módulos + cotejo de superficie pub 1:1 contra HEAD + alternativas evaluadas (handlers/ dir vs archivo único; namespace por sub-módulo vs flat; server.rs vs loop en facade) — todas descartadas a favor del split implementado
- **Cómo se probó:** `cargo check -p vantadb-mcp` (exit 0, `#![warn(missing_docs)]` limpio) · `cargo fmt --check` (exit 0) · `cargo test -p vantadb --test version_coherence` (1 passed) · verificación del worker: clippy `--all-targets --all-features -D warnings` (exit 0), nextest `-p vantadb-mcp` 2/2, cross `-p vantadb-server` 5/5, `mcp_tests.rs` sin editar
- **Checklist anti-hábitos tóxicos:** verificado — split 1:1 real (26 ins / 1758 del en lib.rs), 10 items pub idénticos, tests migrados (parse_metadata → validation.rs, active_request_guard → metrics.rs), `#[allow(deprecated)]` preservado en 3 sitios (resources.rs:101, tools.rs:298, validation.rs:251), cero deuda nueva
- **Veredicto:** ✅ approve — sin blockers. Hallazgo 🟡 no bloqueante: imports glob en server.rs:5-8 (recomendación opcional: imports explícitos, 5 min, no requerido). Nota operativa: WIP foráneo sin commitear en `vantadb-server/src/main.rs` + `vantadb-server/tests/cli_args.rs` (validación args CLI, otra tarea) — no mezclar con AUD-032.

## Notas
- Merge decisions (resueltas): espejar secciones 1:1 — `axioms.rs` separado de `config.rs` (25 líneas de consts + resolve_axioms, usado solo por dispatch_request); `handlers/initialize.rs` separado (17 líneas) para que version_coherence apunte a un archivo estable y no a handlers/mod.rs.
- `RpcRequest`/`RpcResponse` (protocol.rs), `McpMetrics`/`ActiveRequestGuard` (metrics.rs), helpers de validación (validation.rs) y consts axioms → `pub(crate)`. `json_value_to_vanta_value`/`json_array_to_vanta_value`/`mixed_array_error` quedan privados (solo los usa `parse_metadata`).
- El usuario autorizó continuar ("Continue if you have next steps") — no se requiere clarificación.

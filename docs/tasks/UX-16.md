# UX-16: dependencia fantasma lucide-react

## Metadata
- **Plan file:** docs/plans/2026-08-25-batch-colaterales-deuda-desktop.md (Task 14)
- **Fuente:** plan file Task 14
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🔴
- **Tipo:** Frontend (desktop Tauri + React + Vite)
- **Turns estimados:** 5
- **Creado:** 2026-08-25
- **Estado:** ✅ COMPLETED (2026-08-25; commit `a4ecd147` fix(desktop): UX-16 declare lucide-react dependency)
- **Incógnitas (uphill):** 0
- **Pendientes (downhill):** 3

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | — (package.json es manifiesto; no tiene callers) |
| Callees | node_modules/lucide-react; lockfile desktop/package-lock.json; `npm ci`/`npm run build`/Tauri build/CI |
| Implicaciones | NO cambia imports ni lógica; solo declara dependencia ya usada. Romper `npm ci` limpio era el bug; este fix lo repara. API/behavior público: ninguno. |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `desktop/package.json` (53 líneas, leído completo)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** grep `from "lucide-react"` en `desktop/src` → 14 archivos: WorkspaceShell.tsx:19, DataExplorer.tsx:38, CommandPalette.tsx:18, ActivityPanel.tsx:13, GraphLens.tsx:16, ImportDrop.tsx:16, HomeOverview.tsx:14, IqlConsole.tsx:13, GeneralTab.tsx:6, ImportPaste.tsx:9, ConsolidateLens.tsx:42, historial-tab.tsx:12, MetadataTab.tsx:5, SpaceLens.tsx:18. NINGUNO declarado en package.json.
- **Archivos que referencian a los editados (referencias entrantes):** npm/CI/Tauri leen `desktop/package.json` y `desktop/package-lock.json`.
- **Veredicto impacto:** bajo — agregar una dependency declarada que ya se consume en 14 archivos. Versión elegida: `1.34.0` (la resuelta en node_modules vía hoisting, con la que el build pasa hoy). No se tocan imports ni lógica.

## Contrato
"`lucide-react` está en desktop/package.json dependencies; `cd desktop && npm ci` limpio (exit 0) + `npm run build` exit 0"

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** no cambiar imports ni lógica en desktop/src; no tocar otras dependencias del package.json; lockfile debe quedar consistente con package.json.
- **Comandos de verificación:** `cd desktop && npm ci` (exit 0) + `npm run build` (exit 0)
- **Deuda pendiente:** ninguna

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda (declarar una dependency usada no introduce deuda; corrige deuda de packaging).

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | `lucide-react` declarado en package.json + `npm ci` limpio + `npm run build` exit 0 |
| **Commit** | El lead commitea (sub-agente NO commitea). Cambio: desktop/package.json + desktop/package-lock.json |
| **Release** | No aplica (no es crate/release) — solo packaging desktop |

## Fase 1 — Evidencia de Debugging (solo tipo Bug)

- **Repro:** `cd desktop && npm ci && npm run build` → build falla: `tsc` no resuelve `lucide-react` porque no está declarado en package.json y `npm ci` no lo instala (lockfile no lo referencia).
- **Hipótesis:** `lucide-react` es dependency fantasma — resuelve solo por hoisting de node_modules residual; el lockfile `desktop/package-lock.json` NO contiene la entrada `lucide-react` (verificado por grep = 0 matches). `npm ci` borra node_modules y reinstala desde lockfile → módulo ausente → `tsc`/build falla.
- **1 variable controlada:** agregar SOLO `lucide-react` a dependencies (versión 1.34.0) y regenerar lockfile.
- **Test RED:** `npm ci` (verificado como fallo: lockfile sin lucide-react) → verde tras el fix.

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [ ] **SECURITY** — NO aplica: declarar una dependency ya usada no toca trust boundaries ni agrega dependencia nueva (lucide-react ya está instalada/consumida).
- [ ] **PERFORMANCE** — NO aplica: no toca hot paths; no cambia bundle runtime (el módulo ya se importaba).

## Steps

### Step 1: Declarar lucide-react en desktop/package.json
- **Archivos:** `desktop/package.json`
- **Acción:** agregado `"lucide-react": "^1.34.0"` en `dependencies` (orden alfabético, entre `d3-force` y `react`).
- **Verify:** grep `"lucide-react"` en package.json → ✅ (package.json:28)
- **Estado:** ✅

### Step 2: Regenerar lockfile
- **Archivos:** `desktop/package-lock.json`
- **Acción:** `cd desktop && npm install` — lockfile ahora referencia `lucide-react@^1.34.0` (resuelve 1.34.0).
- **Verify:** grep `"lucide-react"` en package-lock.json → ✅
- **Estado:** ✅

### Step 3: Verificación contrato
- **Archivos:** —
- **Acción:** `cd desktop && npm ci && npm run build` → exit 0 ambos.
- **Verify:** exit 0 → ✅ (npm ci: added 484 packages; build: tsc + vite 2861 modules, ✓ built in 39.10s)
- **Estado:** ✅

## Dependencias
- Ninguna (Wave 0, independiente)

## Review (GATE — agente distinto, P2-01)
- **Revisor:** vanta-review (lead delega antes de marcar COMPLETED)
- **Enfoque:** ¿la versión 1.34.0 es la correcta? ¿se declaró sin tocar lógica/imports?
- **Cómo se probó:** `npm ci` + `npm run build` exit 0 (mecánico)
- **Veredicto:** pendiente

## Notas
- frontend-web.md (R-FE-*) aplica a `web/` (Next.js), NO a desktop/ (Tauri+Vite) — revisado, sin conflictos. El fix es el espejo de R-FE-2 (declarar dep viva vs eliminar dep muerta).
- Versión: node_modules resuelve `lucide-react@1.34.0` (leído de desktop/node_modules/lucide-react/package.json). Se declara `^1.34.0` = versionado reproducible vía lockfile.

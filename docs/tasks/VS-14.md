# VS-14 — Historial+Diff entre versiones (tab en Inspector)

> **Plan:** `docs/plans/2026-08-18-vanta-studio-fase1.md` — Task 5 (Wave 1)
> **Estado:** ✅ COMPLETO (commit 5796b2f9 - fase 1)
> **Dependencia:** VS-CORE-07 ✅ implementado (commits `be0812a4`/`b6997e59`) — bridge desktop expone `getVersion`/`versions` en `desktop/src/vanta.ts`
> **Precedente UI:** VS-06 (Inspector tabs), VS-18 (encoding redundante color+ícono+texto — `cdcaf268`)

## Contrato

(a) Tab nuevo en `desktop/src/components/inspector/Inspector.tsx` (VS-06 — cambio SOLO aditivo; VS-18 ya tocó el archivo)
(b) `desktop/src/components/inspector/historial-tab.tsx` (nuevo): lista de versiones (v1..vN con timestamp) vía `versions(ns, key)`
(c) Diff entre dos versiones seleccionadas: payload (line-diff), metadata (KV diff añadido/quitado/cambiado), vector (norma/dim + "cambió" sí/no) vía `getVersion(ns, key, ver)`
(d) **Revertir a vN es acción explícita**: botón → confirmación → `put` (P6) — NUNCA implícito
(e) Encoding redundante (VS-18: color + ícono + texto — reutilizar patrón)

## Verificación (mecánica, obligatoria)

- `npm run build` verde en `desktop/` (`tsc && vite build`)
- `node scripts/selfcheck-vs14.ts` — fixture de 3 versiones en la MISMA forma que devuelve el bridge (`MemoryRecord` de `vanta.ts`), sin deps

## Reglas

- NO commitear (commitea el lead). NO tocar plan file ni `docs/Backlog.md`.
- Sin dependencias nuevas.
- `desktop/src/components/layout/WorkspaceShell.tsx` NO se toca (archivo protegido — el merge lo hace el lead).

## Shape del bridge (verificado 2026-08-18, `desktop/src/vanta.ts`)

```ts
versions(key: string, namespace?: string): Promise<MemoryRecord[]>   // ascendente v1..vN
getVersion(key: string, version: number, namespace?: string): Promise<MemoryRecord>
vantaPut({ namespace?, key, payload, metadata?, expires_at_ms? }): Promise<MemoryRecord>  // P6
```

- `MemoryRecord`: `{ id, namespace, text, vector?, metadata?, created_at_ms?, updated_at_ms?, version?, node_id?, sparse_vector?, expires_at_ms? }`
- Cada snapshot histórico es un `MemoryRecord` completo (core `SnapshotRecord` incluye vector/sparse/expires_at_ms — verificado en `src/sdk/version_history.rs:59-128`).
- El último elemento de `versions()` == record actual (snapshot del record nuevo por put).

## Impacto mapeado (Regla 0)

### Archivos a tocar

| Archivo | Leído completo | Referencias entrantes | Referencias salientes | Veredicto |
|---|---|---|---|---|
| `desktop/src/components/inspector/Inspector.tsx` | ✅ (283 líneas) | `WorkspaceShell.tsx:737` (`<Inspector record onSaved onError />`) | GeneralTab/MetadataTab/PayloadTab/VectorTab/shared, favoritesStore, CopyButton, copy-as, vanta | **ADITIVO**: agregar tab a `InspectorTab` union + `TABS` + render + import. No rompe callers |
| `desktop/src/components/inspector/historial-tab.tsx` | NUEVO | — | vanta (`versions`, `getVersion`, `vantaPut`), `historial-diff`, shared (`fmtDateTime`, `fmtRelative`) | NUEVO |
| `desktop/src/components/inspector/historial-diff.ts` | NUEVO | `historial-tab.tsx`, `scripts/selfcheck-vs14.ts` | `import type { MemoryRecord }` (solo tipo, se erasa) | NUEVO (módulo puro React-free, patrón `activity/logic.ts` + `retrieval-core.ts`) |
| `desktop/scripts/selfcheck-vs14.ts` | NUEVO | — | `historial-diff.ts` | NUEVO (verify) |

### NO tocar

- `desktop/src/components/layout/WorkspaceShell.tsx` (protegido)
- `desktop/src/vanta.ts` (bridge ya expone la API)
- `docs/plans/…`, `docs/Backlog.md`

## Steps

### Step 1 ✅ (PLAN) — Discovery + task file
- Leído plan Task 5, pipeline-full, Inspector.tsx, GeneralTab/VectorTab/PayloadTab/shared, vanta.ts (bridge), core `version_history.rs` (shape snapshot), EventChip (patrón tone VS-18), selfchecks VS-15/VS-13 (patrón verify), package.json (`"type": "module"`, build = `tsc && vite build`), node v24.16.0 (type-stripping nativo → self-check importa `.ts` directo, patrón `selfcheck-retrieval.ts`).
- Task file creado. Impacto mapeado ✅.

### Step 2 ✅ (ACT) — `desktop/src/components/inspector/historial-diff.ts`
Módulo puro React-free (type-only imports):
- `diffPayload(before, after): DiffLine[]` — line-diff LCS (ctx/add/del)
- `diffMetadata(before, after): MetaDiff` — added/removed/changed (KV)
- `vecSummary(v): VecSummary | null` — dim + norma L2
- `vectorChanged(a, b): boolean` — dims difieren, o valores difieren (ε=1e-6), o uno ausente
- `diffVersions(a, b): VersionDiff` — composición para el tab

### Step 3 ✅ (ACT) — `desktop/src/components/inspector/historial-tab.tsx`
- Props: `{ record, onSaved, onError }` (mismos que pasa WorkspaceShell a Inspector — sin tocar shell)
- Carga `versions(record.id, record.namespace)`; lista v1..vN con `updated_at_ms` (fmtDateTime/fmtRelative), marca "● ACTUAL" en `v{record.version}` (VS-18: color+ícono+texto)
- Selección de diff: dos `<select>` (desde/base vs hasta/comparar), default últimos 2
- Al cambiar selección → `getVersion` × 2 (Promise.all) → `diffVersions` → render:
  - payload: líneas con prefijo `+`/`−`/` ` + color (encoding redundante), cap 60 líneas
  - metadata: grupos `+ añadido` / `− quitado` / `~ cambiado (antes → después)`
  - vector: dims + normas ambas versiones + "cambió: SÍ/no" (ícono + texto)
- Revertir (P6 explícito): botón `↺ REVERTIR` por versión (≠ actual) → panel confirmación inline (⚠ + texto que aclara: crea versión nueva, el vector NO se restaura) → `vantaPut({namespace, key, payload: v.text, metadata: v.metadata, expires_at_ms: v.expires_at_ms ?? undefined})` → `onSaved(updated)` → `loadVersions()` refresh → flash "✓ revertido a vN". Errores → `onError`.
- Empty/honest states: sin versiones retenidas; 1 sola versión → hint; diffError (versión evictada cap 32) → hint.

### Step 4 ✅ (ACT) — `Inspector.tsx` (aditivo)
- `InspectorTab` union += `"historial"` (línea 29), `TABS` += `{ id: "historial", label: "HISTORIAL" }` (línea 47), body += `{tab === "historial" && <HistorialTab record={record} onSaved={onSaved} onError={onError} />}`, import (línea 14). Sin cambios a layout/footer existentes.

### Step 5 ✅ (ACT) — `desktop/scripts/selfcheck-vs14.ts`
Fixture 3 versiones (shape exacta `MemoryRecord`): v1 payload 3 líneas / meta {tema, estado:viejo} / vec [1,0,0]; v2 payload 4 líneas con 1 editada / meta {tema, estado:nuevo, extra} / vec [1,0,0] (igual); v3 meta sin `extra` / vec [1,1,0] (cambió).
Asserts (27): line-diff (del/add/ctx, vacío→solo adds), KV diff (added/removed/changed), vectorChanged (iguales/distintos/dim/ausente), normas/dims, idempotencia (v2 vs v2), vecSummary(null/[] → null).

### Step 6 ✅ (VERIFY) — mecánico
- `node scripts/selfcheck-vs14.ts` (desde `desktop/`) — ✅ **PASS 27/27** (1 assert propio mal escrito corregido: normas de vectores distintos NO son iguales — bug del test, no del código)
- `npm run build` (desde `desktop/`) — ✅ `tsc && vite build` verde (451 módulos, 4.36s). Warning de chunk >500 kB pre-existente (Inspector incluye CodeMirror/react-markdown) — fuera de scope.
- `git status` — ✅ solo los 4 archivos de esta tarea (`M Inspector.tsx`, `?? historial-diff.ts`, `?? historial-tab.tsx`, `?? selfcheck-vs14.ts`, `?? VS-14.md`). Los `D web/public/*` + `?? web/hyperframes/` son pre-existentes — NO tocados.

## Invariantes

- P6: revertir NUNCA implícito (botón + confirmación obligatoria). ✅
- No romper layout/estilos de VS-05/06/18: cambio aditivo, reutiliza tokens (`text-neon`, `bg-neon`, `border-foreground`, `bg-background`, `font-tech`, `press`). ✅
- Encoding redundante: ningún estado solo-color (ícono + texto siempre). ✅
- Sin deps nuevas; módulo puro sin imports runtime (self-check node). ✅
- Worktree limpio salvo los `D web/public/*` pre-existentes (NO tocar). ✅

## Context Save Point

> (actualizado al cierre)

- ✅ Tarea completa: 6/6 steps. Verificación mecánica pasada (self-check 27/27 + build). 
- Nota: el MCP `campaign_update_task_state` devuelve `wipBlocked` (convención one-task-at-a-time con estado stale que lista decenas de tareas in-progress incluyendo VS-14 ya como in-progress). La implementación se completó igual — el lead actualiza el estado del plan.
- NO commitear (lead). Plan/Backlog intactos. `WorkspaceShell.tsx` intacto.
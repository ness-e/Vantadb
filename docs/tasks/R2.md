# R2: Crear agente vanta-research

## Metadata
- **Plan file:** docs/plans/2026-08-16-wave-r2-r7-fnd.md
- **Fuente:** docs/Backlog.md P19
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🔴
- **Tipo:** Docs / Planning
- **Turns estimados:** 5
- **Creado:** 2026-08-16T03:45
- **last-synced:** 2026-08-16T03:45
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | ninguno — archivo NUEVO; nadie lo referencia todavía (routing en vanta-lead.md es R6, fuera de scope) |
| Callees | skills: coordinated-web-search, source-driven-development, progreso (todas existentes en el proyecto) |
| Implicaciones | archivo nuevo → cero impacto en código existente; la tabla de routing de vanta-lead.md NO se toca (R6); la tabla de límites de herramientas en `.opencode/AGENTS.md` se actualiza en TSYS-11, no acá |
| Riesgo | bajo — markdown de agente, sin código ejecutable |

## Impacto mapeado (Regla 0)

> GATE ANTES DE CUALQUIER EDICIÓN (MUST — AGENTS.md Regla 0).

- **Archivos leídos (completos):** `.opencode/agents/vanta-docs.md` (157L), `.opencode/agents/vanta-audit.md` (173L), `.opencode/agents/vanta-review.md` (frontmatter 40L), `.opencode/agents/vanta-worker.md` (frontmatter — en prompt del worker), `SKILLS-MANIFEST.md` (grep: coordinated-web-search L341, source-driven-development L84/263), `.opencode/skills/progreso/SKILL.md` (existencia verificada), `.opencode/task-system/prompts/pipeline-full.md` §7 (formato bloque RESULTADO), `.opencode/task-system/prompts/task.md` (formato task file)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** los 9 agentes existentes usan frontmatter YAML idéntico (`name`, `description` folded, `mode`, `permission`) + secciones 1-7; skills referenciadas existen en el proyecto
- **Archivos que referencian a los editados (referencias entrantes):** ninguno — `vanta-research.md` es nuevo; vanta-lead.md lo referenciará en R6 (fuera de scope)
- **Veredicto impacto:** bajo — creación de archivo nuevo; no se modifica ni elimina nada existente

## Contrato
"`.opencode/agents/vanta-research.md` existe y contiene `mode: subagent`, las 3 skills nombradas (`coordinated-web-search`, `source-driven-development`, `progreso`) y tools read-only (edit/bash deny o ausentes)" — verificado con `campaign_verify_cmd` (grep mecánico).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** no tocar `docs/Backlog.md`, `.opencode/skills/campaign-executor/tasks/AUD-024.md`, `vantadb-wasm/src/lib.rs` (WIP de otras tareas); NO git add/commit (lead commitea al cerrar la wave); NO tocar la tabla de routing de `vanta-lead.md` (es R6); mismo frontmatter/estilo que los 9 agentes existentes
- **Comandos de verificación:** `campaign_verify_cmd command="rg -n \"mode: subagent|coordinated-web-search|source-driven-development|progreso|edit: deny|bash: deny\" .opencode/agents/vanta-research.md"` → exit 0
- **Deuda pendiente:** ninguna (R6 — routing table en vanta-lead.md — es tarea separada, depende de R2+R3)

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | R2: Crear agente vanta-research |
| `lastAction` | Step 1 ✅ (agente escrito), Step 2 ✅ (contrato verificado: campaign_verify_cmd exit 0) |
| `result` | `OK` ↔ ✅ COMPLETED |
| `nextAction` | Step 1 (escribir agente) + Step 2 (verify contrato) |
| `contract` | `## Contrato` + `## Invariantes de dominio` |
| `nextTask` | R7 (wave 0, mismo plan) |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda — archivo nuevo, no introduce deuda.

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato mecánico pasa (grep mode: subagent + 3 skills + read-only) |
| **Commit** | NO aplica en este sub-agente — lead commitea al cerrar la wave (regla del plan file) |
| **Release** | NO aplica — markdown de agente, sin release |

**Gate:** ✅ solo si el contrato mecánico pasa. Niveles Commit/Release no aplican (justificado arriba).

## Herramientas necesarias
- codegraph_explore (blast radius — verificado, sin símbolos relevantes: archivo md nuevo)
- campaign_verify_cmd (contrato mecánico)

## Investigation Notes
- Las 3 skills existen en el proyecto con nombres exactos: `coordinated-web-search` (`.agents/skills/coordinated-web-search/`), `source-driven-development` (`.opencode/skills/source-driven-development/`), `progreso` (`.opencode/skills/progreso/` — skill base del task system, no listada en SKILLS-MANIFEST pero presente en el proyecto; consistente con pipeline-full.md L14 que la trata como base)
- Frontmatter canónico de los 9 agentes: `name`, `description` (folded `>-`), `mode: subagent`, `permission:` (claves por tool). Los 9 usan todo `allow` (deuda TSYS-11, AGENTS.md L161); R2 pide read-only explícito → valores `deny` para edit/bash/lsp y tools no aplicables manteniendo las MISMAS claves (forma idéntica)
- Estructura del cuerpo: 7 secciones idénticas (1 Domain Boundaries, 2 Technical Constraints, 3 Context Requirements, 4 Output Template, 5 Composition, 6 Relevant Skills & References, 7 Task System Integration con tabla MCP) + tabla de límites de herramientas de AGENTS.md L140-152 (leaf: task deny)

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — approach validado contra estructura existente |
| Pendientes de ejecución (downhill) | 2 — Step 1 (escribir agente), Step 2 (verify contrato) |
| % completado | 30% |

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [ ] **SECURITY** — NO aplica: archivo markdown de agente, sin trust boundaries, sin dependencias, sin FFI/red. No se carga security-and-hardening.
- [ ] **PERFORMANCE** — NO aplica: no toca hot paths. No se carga performance-optimization.

## Steps

### Step 1: Escribir `.opencode/agents/vanta-research.md`
- **Archivos:** `.opencode/agents/vanta-research.md` (nuevo)
- **Acción:** crear agente research siguiendo el frontmatter y las 7 secciones de los 9 agentes existentes; tools read-only + web (codegraph, metasearchmcp, argus, read/grep/glob, webfetch/websearch); skills §6: coordinated-web-search, source-driven-development, progreso; salida estándar: digest ≤500 palabras + bloque RESULTADO (formato pipeline-full.md §7)
- **Verify:** `Test-Path .opencode/agents/vanta-research.md`
- **Estado:** ✅ DONE

### Step 2: Verify contrato mecánico
- **Archivos:** `.opencode/agents/vanta-research.md`
- **Acción:** `campaign_verify_cmd` con grep que confirme `mode: subagent` + 3 skills + tools read-only (edit/bash deny)
- **Verify:** exit 0 + grep de cada token — ✅ PASÓ: `campaign_verify_cmd` exit 0, stdout confirma mode:subagent (L9), edit:deny (L12), bash:deny (L16), coordinated-web-search (L107), source-driven-development (L108), progreso (L109)
- **Estado:** ✅ DONE

## Dependencias
- Ninguna previa. R6 (routing table en vanta-lead.md) DEPENDE de R2+R3 — no se ejecuta acá.

## Review (GATE — agente distinto, P2-01)

- **Revisor:** no ejecutado en este sub-agente — contrato 100% mecánico (grep), tarea 🟢 markdown sin lógica; el lead valida el diff al cerrar la wave (plan file L7). Justificación: el DoD del usuario para R2 exige verificación mecánica vía campaign_verify_cmd, no review externo.
- **Veredicto:** ⏳ pendiente del lead

## Notas
- El DoD del usuario (1: contenido; 2: NO tocar routing de vanta-lead; 3: mismas convenciones) es la fuente de verdad; el task file documenta el cumplimiento.
- El bloque RESULTADO final usa `COMMIT_HASH: ninguno` (regla: lead commitea).
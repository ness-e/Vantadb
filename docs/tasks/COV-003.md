# COV-003: Rust CLI binary tests

## Metadata
- **Plan file:** docs/plans/2026-08-12-cov-coverage.md (Task 3)
- **Fuente:** plan file Task 3 (líneas 34-41)
- **Esfuerzo:** 🟡 1d | **Prioridad:** 🟢
- **Tipo:** Rust
- **Turns estimados:** 6
- **Creado:** 2026-08-12T00:00
- **last-synced:** 2026-08-12T00:00
- **Estado:** ✅ COMPLETED (pendiente commit del orquestador)
- **Incógnitas (uphill):** 0
- **Pendientes (downhill):** 0 steps

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `src/bin/vanta-cli.rs` (dispatches `cmd_*` handlers) |
| Callees | `vantadb::cli_handlers::*`, `vantadb::migration::*`, `vantadb::cli_server` (feature-gated) |
| Implicaciones | Solo se AGREGAN tests en `tests/cli_tests.rs`. No se modifica lógica de producto. `cli_tests` está excluido del `default-filter` de nextest → la medición de coverage debe incluirlos explícitamente. |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `src/cli_handlers/mod.rs`, `src/bin/vanta-cli.rs`, `tests/cli_tests.rs` (estructura + helpers `setup_temp_db`/`seed_record`/`seed_embedded`), `src/cli_handlers/migrate.rs` (fn `cmd_migrate`, líneas 110-307), `src/cli_handlers/server.rs` (fn `cmd_server`).
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `cli_tests.rs` → `vantadb::cli_handlers`, `vantadb::config`, `vantadb::VantaEmbedded`, `vantadb::sdk`, `vantadb::node`, `vantadb::cli`. `cmd_migrate` → `crate::migration::{MigrationEngine, FormatKind}`, `crate::schema::{StorageHeader, CURRENT_SCHEMA_VERSION, HEADER_SIZE}`. `cmd_server` → `crate::cli_server::run` (feature `server`), `cmd_server_mcp` (spawns `vantadb-server` bin).
- **Archivos que referencian a los editados (referencias entrantes):** Único archivo editado = `tests/cli_tests.rs`, referenciado por `Cargo.toml` (`[[test]] name = "cli_tests" required-features = ["cli"]`). No otro módulo lo importa.
- **Veredicto impacto:** **BAJO**. Solo se añaden funciones `#[test]` a un archivo de test existente. No se toca código de producto, no cambian APIs, no se altera el `default-features`.

## Contrato
"`cargo nextest run --profile audit --features cli --test cli_tests --build-jobs 2` pasa (todos los tests CLI nuevos + existentes en verde) Y la coverage de `src/cli_handlers/*` medida vía `cargo llvm-cov nextest --features cli --test cli_tests` sube desde ~0% (handlers no ejecutados en el gate por estar `cli_tests` excluido del default-filter) a un valor sustancial que empuja el root coverage ~+7pts (hacia ~88%)."

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** No modificar handlers de producto; no cambiar `nextest.toml`; no alterar `Cargo.toml` features. Los tests usan solo `tempfile` (ya dependencia del test, ver `setup_temp_db`).
- **Comandos de verificación:** `cargo nextest run --profile audit --features cli --test cli_tests --build-jobs 2` (debe pasar); `cargo llvm-cov nextest --features cli --test cli_tests --profile audit` (medir coverage de handlers).
- **Deuda pendiente:** `cmd_server` solo se cubre en la rama de error (feature `server` ausente) — el path HTTP real (bind de puerto/tokio) queda fuera de alcance por requerir feature `server` + red. `cmd_migrate` se cubre en error + `dry_run` (sin mutación, sin prompt interactivo).

## Recitation (canónico — estructura única)

contract:
  verificacion: "cargo nextest run --profile audit --features cli --test cli_tests --build-jobs 2 → PASS; cargo llvm-cov nextest --features cli --test cli_tests → handler coverage sube"
  evidencia:
    - claim: "cmd_migrate (Run) y cmd_server pasan de 0% a ejecutados"
      evidencia: "cargo llvm-cov test --features cli --test cli_tests: migrate.rs 0%->51.17%, server.rs 0%->61.43%, cli_handlers ponderado ~76.5% (era ~0%); cli_tests 75 passed/0 failed"
      confianza: alta
  artefactos:
    - tests/cli_tests.rs
  invariantes: "no product logic changed; nextest.toml untouched"
  deuda: "cmd_server HTTP real no cubierto (requiere feature server + red)"
  queda_pendiente: "orquestador confirma root coverage gate incluyendo cli_tests"

## Deuda técnica (Regla 6 — MUST)
Saldo neto: Sin deuda (solo se agregan tests; no se introduce deuda nueva).

## Definition of Done (contrato multi-nivel — P2-08)

- **Task:** tests nuevos pasan en nextest; coverage de handlers sube (salto ~+7pts root).
- **Commit:** NO aplica — el orquestador commitea (worker no hace git mutation).
- **Release:** no aplica.

## Herramientas necesarias
- cargo-mcp (nextest, clippy, fmt)
- cargo llvm-cov (coverage)
- codegraph_explore (blast radius)

## Investigation Notes
- `cli_tests` está en `default-filter` de `.config/nextest.toml` como `not binary(cli_tests)` → el gate Fast CI no lo corre. Por eso los handlers reportan 0% en la medición de root coverage (81.40%). La medición de coverage de esta tarea DEBE incluir `cli_tests` explícitamente.
- `cmd_migrate` sin `dry_run` y sin `force` llama `confirm_action` (prompt interactivo) → NO testeable en automated test; se cubre solo el path `dry_run` (sin mutación) y los paths de error.
- `cmd_server` con feature `server` levanta tokio + bind de puerto (bloqueante) → se cubre solo la rama de error (feature ausente).

## Incógnitas (uphill) vs Pendientes (downhill)

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas | 0 |
| Pendientes de ejecución | 4 |
| % completado | 0% |

## Steps

### Step 1: Crear task file + blast radius (Regla 0)
- **Archivos:** `.opencode/skills/campaign-executor/tasks/COV-003.md`
- **Acción:** poblar Impacto mapeado con codegraph_explore + lecturas.
- **Verify:** task file existe con sección Regla 0 completa.
- **Estado:** ✅ COMPLETED

### Step 2: Agregar tests de cmd_migrate (Run) y cmd_server
- **Archivos:** `tests/cli_tests.rs`
- **Acción:** añadir tests: error target inexistente, `dry_run` con db existente (schema + physical), formato desconocido; error de `cmd_server` sin feature server. Reusar `setup_temp_db`.
- **Verify:** `cargo nextest run --profile audit --features cli --test cli_tests --build-jobs 2`
- **Estado:** ✅ COMPLETED

### Step 3: Agregar tests de branches profundos en crud/data (result-printing)
- **Archivos:** `tests/cli_tests.rs`
- **Acción:** `cmd_get` existente, `cmd_query` con resultados, `cmd_list` registros (ya cubiertos parcialmente → reforzar con asserts de resultado).
- **Verify:** nextest pasa.
- **Estado:** ✅ COMPLETED

### Step 4: Medir coverage antes/después y confirmar salto
- **Archivos:** n/a
- **Acción:** `cargo llvm-cov nextest --features cli --test cli_tests --profile audit`; comparar handlers vs baseline (0%).
- **Verify:** handler coverage sube; root coverage proyectado ~+7pts.
- **Estado:** ✅ COMPLETED

## Dependencias
- Ninguna.

## Review (GATE — agente distinto, P2-01)
- **Revisor:** vanta-audit (o doubt-driven-development como fallback)
- **Enfoque:** tests agregados cubren los handlers previamente en 0% sin tocar producto.
- **Cómo se probó:** nextest pasa + llvm-cov muestra salto de coverage de handlers.
- **Checklist anti-hábitos tóxicos:** sin invención de output; contrato verificado mecánicamente.
- **Veredicto:** ⏳ pendiente de ejecución.

## Notas
- Ponytail full: alcance acotado — cubrir funciones en 0% (cmd_migrate Run, cmd_server) + branches de result-printing; no perseguir 100% de 2.500 ln.
- Git sucio: NO commitear; el orquestador lo hace tras verificar.

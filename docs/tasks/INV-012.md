# INV-012: Anti-Locality Disk Layout — re-evaluación

## Metadata
- **Fuente:** docs/Backlog.md línea 202
- **Esfuerzo:** 🟡 1-2d
- **Prioridad:** 🟢
- **Tipo:** Rust (benchmark)
- **Turns estimados:** 15-30
- **Creado:** 2026-08-03
- **Estado:** ⬜ PENDING

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | benches/vfile_search.rs, src/storage/engine/maintenance.rs (compact_layout_bfs) |
| Callees | src/storage/archive.rs (traverse_graph, compact_layout, reindex_nodes) |
| Implicaciones | Sin implementación — benchmark + recomendación. No rompe contratos. |

## Contrato
"Benchmark re-ejecutado en arquitectura actual (LSM + multi-level) con `benches/vfile_search.rs`, comparado contra baseline DRV-130 (in_memory 783ms / with_vfile 2,440ms / compacted 2,221ms, ~9%), y recomendación documentada: re-abrir si mejora >15%, WONTFIX en caso contrario"

## Herramientas necesarias
- cargo-mcp (bench)
- grep / read (src/storage/archive.rs, maintenance.rs)

## Investigation Notes
- **Path del backlog desactualizado:** apunta a `src/index/graph.rs`, pero el BFS relabeling real vive en `src/storage/archive.rs` (`traverse_graph`, `compact_layout`, `reindex_nodes`) y `src/storage/engine/maintenance.rs` (`compact_layout_bfs`, línea 435)
- **Baseline DRV-130 (docs/plans/archive/2026-07-25-p4-drv130-t3-node-reordering.md):** 10K vectors × 128 dims × 200 queries, Cosine, ef_search=100, top_k=10. in_memory 783ms (3.9ms/q, 1x), with_vfile 2,440ms (12.2ms/q, 3.1x), with_vfile_compacted 2,221ms (11.1ms/q, 2.8x). **Mejora ~9%, WONTFIX** (<20% threshold)
- El backlog dice "dataset SIFT 1M" pero el benchmark original usó 10K vectors en `benches/vfile_search.rs` — usar el mismo bench para comparación apples-to-apples
- `compact_layout_bfs` compacts L0 only (nota ponytail en maintenance.rs:452) — multi-level lo maneja `compact_level()`
- DRV-130 T3 concluyó que el acceso del search es distance-guided, no correlacionado con offset order BFS; el overhead es mayormente call overhead, no page misses

## Steps

### Step 1: Re-ejecutar benchmark vfile_search en arquitectura actual
- **Archivos:** `benches/vfile_search.rs`
- **Acción:** `cargo bench --bench vfile_search` (release + cli features) y capturar los 3 grupos: in_memory, with_vfile, with_vfile_compacted
- **Verify:** benchmark completa y produce los 3 tiempos
- **Estado:** ⬜ PENDING

### Step 2: Comparar con baseline DRV-130
- **Acción:** Tabular tiempos actuales vs baseline (783/2,440/2,221 ms); calcular % mejora de compacted vs with_vfile
- **Verify:** tabla de comparación documentada
- **Estado:** ⬜ PENDING

### Step 3: Recomendación final
- **Acción:** Si mejora >15% → recomendar re-apertura (nueva task de implementación). Si <15% → confirmar WONTFIX. Documentar en Backlog
- **Verify:** fila INV-012 actualizada en docs/Backlog.md
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- Solo benchmark + recomendación (alcance del backlog). No implementar cambios.
- Correr en release para números comparables.

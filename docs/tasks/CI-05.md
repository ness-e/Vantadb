# CI-05: Benchmark baseline fijo (perf-bench-40.yml)

## Metadata
- **Plan file:** docs/plans/2026-08-12-ci-deuda.md (Task 4)
- **Fuente:** Backlog § CI & Tooling — WF1-027/029 (bench sin baseline)
- **Esfuerzo:** 🟡 1d (real: ~1h)
- **Prioridad:** 🟡
- **Tipo:** DevOps (CI/CD)
- **Turns estimados:** 6
- **Creado:** 2026-08-12
- **last-synced:** 2026-08-12
- **Estado:** ✅ COMPLETO (commits adec84e7 + 56ebc126, 2026-08-12)
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | GitHub Actions solo (evento `push` a main con paths `src/**`, `vantadb-python/**`, `benchmarks/**`, `Cargo.toml`, `Cargo.lock` + `workflow_dispatch`) — ningún código lo invoca |
| Callees | `benchmarks/vantadb_local_bench.py` (bench Python, existente, sin cambios), `benchmarks/update_markdown.py` (existente, sin cambios), stdlib Python (json/statistics/os/sys), `benchmarks/python_baseline.json` (NUEVO) |
| Implicaciones | El step de benchmark pasa de 1 run a 3 runs (más lento ~2-6min); el artifact `vanta-benchmark-results` mantiene nombre/forma (sigue siendo `benchmark_results.json`, ahora mediana de 3). `docs/operations/BENCHMARKS.md` se actualiza igual (no-fatal). No cambia API pública ni motor. |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `.github/workflows/perf-bench-40.yml` (83 líneas), `benchmarks/vantadb_local_bench.py` (257 líneas — estructura del report JSON: `insert/rebuild/query_text/query_vector/query_hybrid` con `p50_ms/p95_ms/p99_ms` + `throughput_records_per_sec`), `benchmarks/update_markdown.py` (100 líneas — lee `benchmark_results.json` o fallback `benchmarks/vanta_benchmark_report.json`), `.github/workflows/heavy-bench-nightly-51.yml` (295 líneas — patrón establecido de regression gate: compara con `scripts/bench_regression.py` Criterion + issue creation con `issues: write`), `scripts/bench_regression.py` (304 líneas — TSK-79, formato Criterion `mean_ms`, `update-baseline` pisa `benchmarks/criterion_baseline.json`), `benchmarks/criterion_baseline.json` (baseline del nightly Criterion)
- **Archivos referenciados hacia dentro:** `.github/actions/rust-setup` (action), `actions/setup-python`, `actions/upload-artifact` (ambos pinneados a SHA ✅), `benchmarks/python_baseline.json` (nuevo, será leído por el step de comparación)
- **Archivos que referencian a los editados (referencias entrantes):** grep global `perf-bench|vanta-benchmark|python_baseline` → `docs/plans/2026-08-12-ci-deuda.md` (Task 4), `docs/operations/CI_POLICY.md` (política bench). Ninguna referencia de código.
- **Veredicto impacto:** bajo-medio — leaf de GitHub Actions; los jobs existentes (checkout/rust-setup/python/wheel-build/update-markdown/upload) se preservan intactos; solo se modifican el step de benchmark (loop 3 runs) y se agregan 2 steps (comparación + upload baseline candidato). Baseline nuevo NO reusa `criterion_baseline.json` (pertenece al nightly Criterion — mezclar formatos lo corrompería).

## Contrato

"workflow compara contra baseline almacenado (artifact/issue); si regresión > umbral → job falla o issue creado"

Implementación: el workflow compara la mediana de 3 runs del bench Python contra `benchmarks/python_baseline.json` (baseline versionado en repo — durable, reviewable, cumple "almacenado" mejor que artifact 14d). Si alguna métrica regresa > 15% → `::error::` + `exit 1` → job falla (rama "job falla" del contrato; la rama "issue" requiere PAT — gh auth roto, inviable). Rebaseline manual vía `workflow_dispatch` con input `update_baseline=true` → genera el baseline y lo sube como artifact para commit manual.

Verificación exacta: `actionlint .github/workflows/perf-bench-40.yml` exit 0

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** NO tocar `scripts/bench_regression.py` ni `benchmarks/criterion_baseline.json` (pertenecen al nightly Criterion TSK-79); NO tocar jobs existentes del workflow (checkout, rust-setup, setup-python, build wheel, update-markdown, upload results) más allá del step de benchmark (loop 3 runs); NO usar `continue-on-error` (Regla 2); no cambiar triggers ni `permissions: contents: read`; commit conventional con `ci:` (no dispara release-plz); direcciones de métrica: `throughput_records_per_sec` = higher-is-better, todos los `*_ms` = lower-is-better.
- **Comandos de verificación:** `actionlint .github/workflows/perf-bench-40.yml` (exit 0)
- **Deuda pendiente:** el baseline queda vacío (`benchmarks: {}`) — el gate es no-op (warning + pass) hasta que se corra `update_baseline=true` y se commitee el artifact. Los números reales del v0.5.0 no están disponibles (gh auth roto, artifact expirado) — documentado en Notas.

## Contrato recitation

    contract:
      verificacion: "actionlint .github/workflows/perf-bench-40.yml exit 0"
      evidencia:
        - claim: "Workflow corre el bench 3 veces y computa mediana por métrica"
          evidencia: ".github/workflows/perf-bench-40.yml (step benchmark loop 1..3 -> benchmark_results.\$i.json; step compare heredoc python usa statistics.median)"
          confianza: alta
        - claim: "Compara contra baseline almacenado benchmarks/python_baseline.json y falla el job si regresión > 15%"
          evidencia: ".github/workflows/perf-bench-40.yml (step compare: exit 1 con ::error:: si pct > 15); benchmarks/python_baseline.json (baseline versionado)"
          confianza: alta
        - claim: "Jobs existentes intactos; solo se agregó input update_baseline y 2 steps"
          evidencia: "git diff --stat perf-bench-40.yml (checkout/rust-setup/python/wheel/update-markdown/upload sin cambios)"
          confianza: alta
        - claim: "No se tocó el baseline Criterion del nightly"
          evidencia: "git status benchmarks/criterion_baseline.json = sin cambios; scripts/bench_regression.py sin cambios"
          confianza: alta
      artefactos:
        - .github/workflows/perf-bench-40.yml
        - benchmarks/python_baseline.json
        - .opencode/skills/campaign-executor/tasks/CI-05.md
      invariantes: "criterion_baseline.json intacto; sin continue-on-error; triggers/perms sin cambio; jobs existentes preservados"
      deuda: "Gate no-op hasta rebaseline manual (update_baseline=true); validar en CI real tras push"
      queda_pendiente: "correr workflow_dispatch con update_baseline=true tras merge y commitear el baseline"

## Deuda técnica (Regla 6)

**Sin deuda neta nueva** — CI-only, no introduce deuda en código del motor. El baseline vacío es estado inicial de bootstrap, no deuda de implementación.

## Definition of Done (contrato multi-nivel)

| Nivel | Gate |
|-------|------|
| **Task** | actionlint pasa + workflow compara contra baseline + falla en regresión > 15% |
| **Commit** | Commit atómico (1 workflow + 1 baseline + 1 task file + plan), conventional commit `ci(bench): CI-05 — ...`, verificación mecánica actionlint real |
| **Release** | N/A — CI-only (no toca release-plz, no toca versión) |

## Fases explícitas — SECURITY | PERFORMANCE

- [x] **SECURITY** — NO aplica como hardening de código (sin input de usuario ni lógica nueva en el motor). Sí: no se escalan permisos (`contents: read` se mantiene — no se commitea desde CI, el baseline se sube como artifact y se commitea manual). Acciones ya pinneadas a SHA. Regla 2 respetada (sin continue-on-error).
- [x] **PERFORMANCE** — NO aplica al motor. Sí impacta tiempo de CI: bench pasa de 1 a 3 runs (mediana para reducir noise — decisión explícita del plan, risk register: "3 runs + mediana; umbral conservador 15%").

## Steps

### Step 1: Crear benchmarks/python_baseline.json (baseline vacío + metadata)
- **Archivos:** `benchmarks/python_baseline.json`
- **Acción:** crear baseline versionado con `metadata` (descripción, workflow perf-bench-40.yml) + `benchmarks: {}` vacío. Primer estado: gate no-op (warning + pass) hasta rebaseline manual.
- **Verify:** `python -c "import json; json.load(open('benchmarks/python_baseline.json'))"` exit 0
- **Estado:** ✅ COMPLETO (verificado 2026-08-12: JSON válido, metadata + `benchmarks: {}`)

### Step 2: Editar perf-bench-40.yml — 3 runs + step comparación + input update_baseline
- **Archivos:** `.github/workflows/perf-bench-40.yml`
- **Acción:** (a) agregar input `update_baseline` (boolean, default false); (b) step "Run Standard Ingestion & Search Benchmark" → loop 3 runs escribiendo `benchmark_results.1.json/.2.json/.3.json`; (c) nuevo step "Compare vs baseline" con heredoc Python: mediana por métrica → escribe `benchmark_results.json` (misma forma que antes → update_markdown/artifact intactos), si `update_baseline=true` → escribe `benchmarks/python_baseline.json` + notice, si no → compara contra baseline, regresión > 15% → `::error::` + `exit 1`; (d) step condicional "Upload Baseline Candidate" (si `update_baseline == 'true'`).
- **Verify:** `actionlint .github/workflows/perf-bench-40.yml` exit 0
- **Estado:** ✅ COMPLETO (verificado 2026-08-12: actionlint exit 0; lógica mediana/umbral validada con test sintético de 3 caminos: regresión throughput -30% detectada, baseline vacío = no-op, baseline igual = sin falsos positivos)

### Step 3: Verify full — actionlint + diff review
- **Archivos:** workflow + baseline
- **Acción:** `actionlint` sobre el workflow editado; revisar `git diff` para confirmar que jobs existentes están intactos y que `criterion_baseline.json`/`bench_regression.py` no fueron tocados.
- **Verify:** `actionlint .github/workflows/perf-bench-40.yml` exit 0 + `git diff --stat`
- **Estado:** ✅ COMPLETO (verificado 2026-08-12: actionlint global exit 0 — pre-commit hook ok; `criterion_baseline.json`/`bench_regression.py` intactos; dirty state ajeno sin tocar)

### Step 4: Cierre — commit conventional + sync plan file + update task state
- **Archivos:** workflow, baseline, task file, docs/plans/2026-08-12-ci-deuda.md (Task 4)
- **Acción:** `git add` de SOLO los archivos del scope (workflow + baseline + task file + plan) y commit `ci(bench): CI-05 — baseline fijo y umbral de regresión en perf-bench`; marcar Task 4 ✅ en plan file (branch develop, commit hash); `campaign_update_task_state` completed con recitation.
- **Verify:** commit creado + pre-commit hook ok (si el hook lintea `.github/` y falla por otro workflow, reportar sin `--no-verify`)
- **Estado:** ✅ COMPLETO (commits `adec84e7` workflow+baseline+task file, `56ebc126` sync plan Task 4; pre-commit hook ok en ambos — actionlint global exit 0)

## Dependencias
- Ninguna (independiente; CI-05 no depende de CI-06/07)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. CI-only + 🟡: revisión adversarial ligera vía checker de contrato (actionlint) + revisión de diff por lead.

- **Revisor:** actionlint (mecánico) + vanta-lead (revisión post-delegación)
- **Enfoque:** ¿el approach (baseline versionado + comparación inline Python + job falla) satisface el contrato? Sí — "baseline almacenado" = archivo en repo (más durable que artifact), "job falla" es la rama elegida del contrato (issue requiere PAT inviable), mediana de 3 runs + umbral 15% = spec exacta del plan. ¿Por qué no reusar `scripts/bench_regression.py`? Formato Criterion (`mean_ms`) ≠ formato del bench Python (`insert/query_*`), y su `update-baseline` pisa `criterion_baseline.json` que pertenece al nightly — mezclar lo corrompería.
- **Cómo se probó:** acción actionlint real ejecutada sobre el archivo editado (no auto-reporte) + test sintético de la lógica Python (3 caminos: regresión detectada, baseline vacío no-op, sin falsos positivos).
- **Veredicto:** ✅ APROBADO (actionlint exit 0; contrato satisfecho — baseline versionado + job falla en regresión > 15%)

## Notas
- **Baseline v0.5.0 no disponible:** `gh auth status` → token inválido (no se puede bajar el artifact del run del release v0.5.0); artifact retention 14d probablemente expirado. Por eso el baseline arranca vacío con mecanismo de rebaseline manual (`update_baseline=true`), alineado con el risk register del plan ("Rebaseline manual con workflow_dispatch").
- **Script existente `scripts/bench_regression.py` (TSK-79):** pertenece al nightly Criterion (`heavy-bench-nightly-51.yml`) y opera sobre `target/criterion/` — formato incompatible con el bench Python. NO reusado (evita corromper `criterion_baseline.json`).
- **Bootstrap del gate:** con `benchmarks: {}`, el step de comparación hace warning + pass (no rompe CI en el primer run tras merge). El gate se activa al committear el baseline generado por `update_baseline=true`.
- **Dirección de métricas:** `insert.throughput_records_per_sec` higher-is-better; todos los `*_ms` (insert p50/p95/p99, rebuild duration_ms, query_* p50/p95/p99) lower-is-better.

# AUD-047 — Duplicación ~50 líneas del match métrico en layer.rs → extraer helper

> **Plan:** `docs/plans/2026-08-25-batch-core-fixes-research.md` (Task 5)
> **Tipo:** refactor (code-simplification)
> **Cynefin:** 🟦 obvio — duplicación textual idéntica, 1 archivo
> **Esfuerzo:** 🟢 | **Prioridad:** 🟢 | **Appetite:** max 2h
> **Contrato:** helper extraído; `cargo nextest run -p vantadb search_layer` + suite index pasa; clippy/fmt

## Contexto

`src/index/search/layer.rs` tiene el bloque `match metric` (Cosine/Euclidean/SparseDot)
duplicado dos veces con lógica idéntica, anidado peor desde f2c2141e (FIND-29):

1. **Entry points** (líneas 75-102): decode vector del mmap → `match metric` con
   `node.inv_cached_norm`
2. **Neighbors** (líneas 252-282): decode vector del mmap → `match metric` con
   `neighbor.inv_cached_norm`

Diferencia entre brazos: SOLO nombres de variables (`f32_vec`/`node` vs `f32_v`/`neighbor`).
La lógica es textualmente idéntica:

```rust
match metric {
    Cosine => if let Some(q_inv_norm) = query_inv_norm {
        if inv_cached_norm > f32::EPSILON { cosine_sim_cached_norms(query_vec, q_inv_norm, vec, inv_cached_norm) }
        else { f32_slice_similarity(query_vec, query_norm, vec, metric) }
    } else { f32_slice_similarity(query_vec, query_norm, vec, metric) },
    Euclidean => -euclidean_distance_squared_f32(query_vec, vec),
    SparseDot => 0.0,  // SparseDot has its own brute-force path
}
```

### Impacto mapeado (Regla 0)

- **Archivos leídos completos:**
  - `src/index/search/layer.rs` (409L completo) — archivo objetivo
  - `src/node/vector_data.rs:1-80` — `DistanceMetric` es `Copy` (deriva `Clone, Copy, Debug, Serialize, Deserialize, PartialEq, Eq, Hash, Default`) → capturable por valor en closure sin move
  - `src/index/distance/mapper.rs:200-215` — firma `f32_slice_similarity(query_vec, query_norm, candidate, metric) -> f32`, `#[inline(always)]`
  - `src/index/distance/metrics.rs:21,58` — firmas `cosine_sim_cached_norms(a, inv_norm_a, b, inv_norm_b)` y `euclidean_distance_squared_f32(a, b)`
  - `src/index/graph.rs:507-555` — `fast_similarity` (path in-memory, NO se toca; es el gemelo del match disk pero con `norm_sq` optimization propia)
  - `src/index/search/tests.rs:489-745, 1126-1396` — tests directos de `search_layer` (cosine, euclidean, tombstones, disk-vs-memory parity ERR-042, ACORN, Miri)
- **Referencias hacia dentro:** `mod layer;` en `src/index/search/mod.rs:5` (privado). Tests llaman `index.search_layer(...)` vía `pub(crate) fn search_layer` — no dependen de la estructura interna.
- **Referencias hacia fuera:** imports del archivo (pool::give_nl, SearchProfile, distance fns, graph, node::DistanceMetric/FilterBitset, FLAG_TOMBSTONE) — ninguno cambia.
- **Veredicto:** BAJO — cambio confinado a `layer.rs`. No cambia firma pública, no cambia aritmética, no agrega símbolos. El helper será un closure `Fn` inline (zero-cost en release) que captura `query_vec`, `query_norm`, `query_inv_norm`, `metric` (todos Copy/referencias).

## Steps

- [x] **S1 — DISCOVERY + task file** — ✅ hecho (este archivo)
- [x] **S2 — Extraer closure `metric_score`** en `layer.rs`, reemplazar los 2 `match metric` (entry points + neighbors)
- [x] **S3 — Verify contract:** ✅ todos los checks pasan (ver tabla abajo)
- [x] **S4 — Actualizar task file + RESULTADO** — ✅ (NO commit — lo hace el lead)

## Decisiones de diseño

- **Forma del helper:** closure `Fn` inline (no `fn` privada) — captura los 4 args del método ya en scope, evita repetir firma de 6 args, se inlinea a cero costo en release. Ponytail: 1 helper, ~45 líneas menos.
- **NO tocar `fast_similarity`** (graph.rs): tiene optimización `norm_sq` propia para Euclidean y usa `vec_data.as_f32_slice()` — es otro path (in-memory), fuera de scope.
- **FASE PERFORMANCE:** no aplica — refactor estructural puro, la aritmética no cambia; closure `Fn` inline = mismo machine code. Sin benchmark (Regla 9 no dispara: no hay claim de performance ni optimización).
- **FASE SECURITY:** no aplica — refactor interno, sin trust boundaries, sin FFI, sin input nuevo.

## Verificación

| Check | Comando | Estado |
|-------|---------|--------|
| Contract tests | `cargo nextest run -p vantadb -E 'test(search_layer)|test(index)'` | ✅ 358/358 passed (incl. search_layer_*, search_vfile_*, ACORN, Miri, disk-vs-memory parity) |
| Build | `cargo check -p vantadb` | ✅ |
| Format | `cargo fmt --check` | ✅ |
| Lint | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | ✅ |

## Diff aplicado (resumen)

- `src/index/search/layer.rs`: +19 líneas (closure `metric_score` definido una vez, después de `results` init) −54 líneas (2 bloques `match metric` reemplazados por `metric_score(f32_vec, node.inv_cached_norm)` y `metric_score(f32_v, neighbor.inv_cached_norm)`). **Neto: −35 líneas** (409 → 374).
- Semántica preservada 1:1: el closure recibe `(vec, inv_cached_norm)` y captura `query_vec`, `query_norm`, `query_inv_norm`, `metric` (todos Copy) — los brazos Cosine/Euclidean/SparseDot son textualmente los originales.
- No se tocó `fast_similarity` (graph.rs) ni ninguna aritmética.

## Notas

- CodeGraph auto-sync deshabilitado → archivos leídos directos.
- Otra sesión activa trabaja en Justfile/Dockerfile y docs — no tocar esos archivos.
# VS-17 — Favoritos/historial de búsqueda + Copy-as

> **Plan:** `docs/plans/2026-08-18-vanta-studio-fase1.md` (Task 8, Wave 1)
> **Estado:** ✅ COMPLETO (commit cdcaf268 - fase 1)
> **Fuente contrato:** plan Task 8 + `.opencode/task-system/prompts/pipeline-full.md`
> **Paralelos:** VS-CORE-07 (core, `vanta.ts` aditivo) y VS-18 (`DataExplorer.tsx` chips a11y) — mi slice en archivos compartidos es SOLO aditivo.

## Objetivo

Favoritos de namespaces/keys con toggle (★) persistidos en localStorage, listados en palette
(grupo FAVORITOS) y sidebar; historial de las últimas N búsquedas (localStorage, re-ejecutables
desde palette, grupo HISTORIAL); Copy-as desde grid/inspector: registro completo (JSON), key,
o payload (markdown) — botón con feedback "copiado". Sin deps nuevas (navigator.clipboard +
localStorage).

## Impacto mapeado (Regla 0)

**Archivos leídos completos:**
- `desktop/src/components/palette/CommandPalette.tsx` (380L) — estructura VS-09 verificada: `Command.Dialog` con grupos `Navegación` / `Lentes` / `Abrir namespace (en MEMORIAS)` / `Acciones`; `PaletteItem` wrapper; props `namespaces/dark/activeConnection/onNavigate/onSearch/onToggleTheme/onUndo/onDelete/onError`; `run()` cierra la palette; `SearchKeyFallback` llama `onSearch(q)`. Grupos nuevos se insertan de forma aditiva (FAVORITOS antes de "Abrir namespace", HISTORIAL después de "Acciones").
- `desktop/src/components/layout/WorkspaceShell.tsx` (687L) — surfaces RESUMEN/MEMORIAS/PAPELERA/ACTIVITY/RETRIEVAL/ÍNDICES/IQL ya mergeadas (VS-13/15/16 en `0411117e`); sidebar con secciones Workspace/Namespaces; `runSearch(q)` es el funnel de la búsqueda global Y de la palette (dónde grabar historial); `openRecord(record, score)` + `get()` disponibles (patrón `handleInspectAudit` para abrir favorito key). NO toco bloques RETRIEVAL/ACTIVITY/deep-link.
- `desktop/src/components/DataExplorer.tsx` (650L) — grid virtualizado; columna de acciones `helper.display({ id: "actions" })` (líneas 462-478) con `DeleteButton`; refs latest `errorRef`/`removeRowRef`; estilos `border-ink/bg-paper/bg-cream/text-neon/font-tech/press`. Mi slice: sumar ★ + botón copiar en la MISMA celda de acciones (aditivo; no toco baseColumns/MetaChips/TtlCell que VS-18 sí toca en chips).
- `desktop/src/components/inspector/Inspector.tsx` (231L) — header key/ns + tabs + footer commit explícito (P6). Mi slice: fila compacta de acciones copy-as + ★ debajo del header (aditivo, no toco tabs/footer).
- `desktop/src/store/undo.ts` (157L) — patrón de store vanilla con `subscribe`/`notify` a seguir.
- `desktop/src/vanta.ts` (380L) — `MemoryRecord {id, namespace, text, vector?, metadata?, created_at_ms?, updated_at_ms?, version?, node_id?, sparse_vector?, expires_at_ms?}`. NO la edito (VS-CORE-07 corre en paralelo).
- `desktop/package.json` — build = `tsc && vite build`; **sin test runner** → self-check vía script Node que compila módulos puros con tsc (patrón VS-15).
- `desktop/tsconfig.json` — `strict`, `noUnusedLocals: true`, `noEmit: true`.
- `desktop/scripts/selfcheck-vs15.mjs` (90L) — patrón de self-check: mock global, compilar con tsc a temp, import dinámico, asserts.

**Referencias hacia dentro (grep):**
- `CommandPalette`: único uso en `WorkspaceShell.tsx:42,671` (lazy import + render).
- `DataExplorer`: único uso en `WorkspaceShell.tsx:24,613`.
- `Inspector`: único uso en `WorkspaceShell.tsx:38,657` (lazy import).
- `store/undo.ts` importa desde `../../vanta` (remove/vantaPut) — el nuevo `store/favorites.ts` NO importa vanta (solo localStorage), evita dependencias circulares y permite self-check sin resolver el bridge.

**Referencias salientes:**
- `store/favorites.ts` + `store/search-history.ts` → consumidos por CommandPalette, WorkspaceShell, DataExplorer, Inspector, self-check.
- `components/copy/copy-as.ts` + `CopyButton.tsx` → DataExplorer, Inspector, self-check.
- WorkspaceShell → CommandPalette (props nuevas favorites/history/onOpenFavorite/onClearHistory) + sidebar (sección Favoritos + ★ por namespace) + `runSearch` (grabar historial).

**Veredicto de impacto:** Cambio aditivo, sin deps nuevas, sin tocar core/bridge/vanta.ts.
Slice en WorkspaceShell: imports + estado favorites/history (subscribe) + `handleOpenFavorite` +
`searchHistory.add()` en `runSearch` + bloque Favoritos en sidebar + ★ en lista de namespaces +
props de palette. Slice en DataExplorer: celda de acciones (+★, +copiar). Slice en Inspector:
fila de acciones copy-as + ★.

## Steps

- [x] 1. Task file + Impacto mapeado (Regla 0)
- [x] 2. `store/favorites.ts` (localStorage, subscribe, toggle ns/key) + `store/search-history.ts` (últimas N, dedup, clear)
- [x] 3. `components/copy/copy-as.ts` (recordToJson / recordToMarkdown puros) + `components/copy/CopyButton.tsx` (feedback "copiado")
- [x] 4. CommandPalette: grupos FAVORITOS + HISTORIAL + props (aditivo)
- [x] 5. WorkspaceShell slice aditivo (favoritos sidebar + ★ ns + wiring palette + historial en runSearch)
- [x] 6. DataExplorer: celda de acciones con ★ y copiar JSON (aditivo)
- [x] 7. Inspector: ★ + copiar JSON/KEY/MD (aditivo)
- [x] 8. `scripts/selfcheck-vs17.mjs` — roundtrip localStorage (favoritos + historial) + shape copy-as
- [x] 9. Verify: `npm run build` (desktop) + self-check Node verde

## Verificación (contrato)

- `npm run build` en `desktop/` — ✅ (tsc + vite, solo warning chunk pre-existente del Inspector/CodeMirror)
- `node scripts/selfcheck-vs17.mjs` en `desktop/` — ✅ roundtrip favoritos (toggle ns/key → reload persiste; toggle off ns no toca key favs) + roundtrip historial (add/dedup no-consecutivo/cap N=10/clear → reload) + shape recordToJson/recordToMarkdown

## Context Save Point

Completo. NO commit (regla: lo hace el lead). Archivos tocados por VS-17:
- `desktop/src/store/favorites.ts` (nuevo)
- `desktop/src/store/search-history.ts` (nuevo)
- `desktop/src/components/copy/copy-as.ts` (nuevo)
- `desktop/src/components/copy/CopyButton.tsx` (nuevo)
- `desktop/src/components/palette/CommandPalette.tsx` (aditivo: props + grupos FAVORITOS/HISTORIAL)
- `desktop/src/components/layout/WorkspaceShell.tsx` (slice aditivo: imports + estado + `searchHistory.add` en runSearch + `handleOpenFavorite` + sección Favoritos sidebar + ★ por namespace + props palette)
- `desktop/src/components/DataExplorer.tsx` (aditivo: imports + favTick + celda de acciones con ★ y copiar)
- `desktop/src/components/inspector/Inspector.tsx` (aditivo: imports + favTick + isFav + fila copy-as/★)
- `desktop/scripts/selfcheck-vs17.mjs` (nuevo)
- `.opencode/skills/campaign-executor/tasks/VS-17.md` (este task file)

El worktree contiene WIP de tareas paralelas que NO son mías y no toqué: VS-CORE-07 (`src/backend.rs`, `src/backends/*`, `src/config.rs`, `src/sdk/version_history.rs`), VS-18 (`desktop/src/components/inspector/GeneralTab.tsx`, `MetadataTab.tsx`, `desktop/src/index.css`, edits en TtlCell/VectorCell/tab `◆` dentro de DataExplorer.tsx/Inspector.tsx — líneas disjuntas de las mías) y el plan file. El lead hace el merge de integración en los archivos compartidos (WorkspaceShell/DataExplorer/Inspector).

## PROPUESTA / Decisiones

- **Favorito = `{ namespace, key: string | null }`** — `key: null` es favorito de namespace completo (★ en sidebar), `key` presente es favorito de registro (★ en grid/inspector). Persistencia `localStorage["vanta.favorites.v1"]` como JSON array, newest-first.
- **Store vanilla con suscripción** (mismo patrón `undo.ts`): `getFavorites/isFavorite/toggle/removeNamespace/subscribe` + singleton exportado; clase exportada para el self-check (permite instanciar con storage mock).
- **Storage inyectable con default** `typeof localStorage !== "undefined" ? localStorage : null` — en Node el self-check setea `globalThis.localStorage` mock ANTES del import dinámico; si no hay storage (sandbox), el store funciona in-memory sin persistir.
- **Historial**: `localStorage["vanta.search-history.v1"]`, N=10, dedup no-consecutivo (re-buscar una query la mueve al frente), `add/get/clear/subscribe`. Se graba en `runSearch` (funnel único de topbar + palette → cubre ambos entry points del contrato).
- **Copy-as**: `copy-as.ts` módulo puro (React-free, type-only import de vanta como `logic.ts`) — `recordToJson = JSON.stringify(record, null, 2)`; `recordToMarkdown` = doc markdown compacto (encabezado ns/key, payload, tabla de metadata, pie con version/updated). `copyText` usa `navigator.clipboard.writeText` (Tauri v2 lo soporta) con error descriptivo si no está.
- **Feedback "copiado"**: `CopyButton` swaps su label a "✓ copiado" por ~1.5s (aria-label + title fijos para a11y).
- **Grid**: una sola acción copiar por fila (⧉ → JSON completo — lo más útil de una fila; las 3 variantes viven en el Inspector, que tiene espacio). ★ por fila junto al 🗑 existente.
- **Inspector**: fila compacta de acciones copy-as (JSON / KEY / MD) + ★ en el header; feedback igual.
- **Self-check sin test runner**: `scripts/selfcheck-vs17.mjs` compila los 3 módulos puros con tsc a CommonJS (patrón VS-15), mockea localStorage, y valida: roundtrip favoritos (toggle → reload → persiste), roundtrip historial (add/dedup/cap/clear → reload), shape de recordToJson/recordToMarkdown.
# AUD-050: `inject_context` error engañoso en thread_id

## Metadata
- **Plan file:** `docs/plans/2026-08-17-fix-audit-bugs.md`
- **Fuente:** Auditoría profunda 0.5.0 (2026-08-17) — inconsistencia confirmada en vivo (JSON-RPC)
- **Esfuerzo:** 🟢
- **Prioridad:** 🟡
- **Tipo:** Bindings MCP (inconsistencia, UX de error)
- **Turns estimados:** 3-6
- **Creado:** 2026-08-17T22:00
- **last-synced:** 2026-08-17T22:00
- **Estado:** ✅ COMPLETED (stale — plan previo archivado, cerrada por vanta-lead 2026-08-20)

## Blast Radius
| Dirección | Módulos |
|-----------|---------|
| Callers | clientes MCP que llamen inject_context con thread_id string |
| Callees | `vantadb-mcp/src/handlers/tools.rs` (handler inject_context) |
| Implicaciones | solo mejora el mensaje de error — sin cambio de contrato |

## Evidencia
- `inject_context` con `thread_id: "200"` (string) → `Missing 'thread_id'` — engañoso: el campo ESTÁ presente, pero el tipo es incorrecto
- Con `thread_id: 200` (número) → funciona

## Contrato
"error claro: `thread_id must be a numeric id, got string`; el mensaje distingue ausencia de tipo incorrecto; tests actualizados"

## Herramientas necesarias
- cargo-mcp (nextest, check), codegraph_explore (handler inject_context)

## Investigation Notes
El handler parsea `thread_id` con expectativa numérica; un string produce un error de deserialización que se reporta como "Missing". Fix mínimo: distinguir los dos casos en el parseo del campo (ausente vs tipo inválido) y emitir mensaje específico.

**NO cambiar** el tipo aceptado (seguir numérico) ni la semántica.

## Steps

### Step 1: Ver handler inject_context
- **Archivos:** `vantadb-mcp/src/handlers/tools.rs` (inject_context)
- **Acción:** codegraph_explore del handler y del parseo de thread_id
- **Verify:** entender el error actual
- **Estado:** ✅ DONE — handler en tools.rs:632-666; `args["thread_id"].as_u64()` con string → None → "Missing 'thread_id'" (engañoso). Engine (src/executor.rs:344-397): InsertMessage es azúcar que crea nodo Message + edge a thread — NO existe error "thread not found" en el engine (semántica a respetar: no cambiar).

### Step 2: Mejorar mensaje de error
- **Archivos:** `vantadb-mcp/src/handlers/tools.rs`, `vantadb-mcp/src/validation.rs`
- **Acción:** distinguir ausente vs tipo incorrecto; mensaje claro (`thread_id must be a numeric id (integer), got string`)
- **Verify:** `cargo check -p vantadb-mcp`
- **Estado:** ✅ DONE — fix en tools.rs:636-650 (match sobre args.get + json_value_type_name en validation.rs); cargo check -p vantadb-mcp ✅

### Step 3: Test
- **Archivos:** `vantadb-mcp/tests/mcp_tests.rs`, `docs/api/MCP.md`
- **Acción:** test string thread_id → error claro; numérico → OK
- **Verify:** `cargo test -p vantadb-mcp --test mcp_tests`
- **Estado:** ✅ DONE — `test_inject_context_thread_id_type_error` agregado; 41 passed (40 previos + 1 nuevo); docs/api/MCP.md actualizado con requisito numérico

## Context Save Point
- Worktree: cambios en `vantadb-mcp/src/handlers/tools.rs`, `vantadb-mcp/src/validation.rs`, `vantadb-mcp/tests/mcp_tests.rs`, `docs/api/MCP.md` — SIN commit (el lead commitea al cerrar la wave).
- Verificación final: `cargo check -p vantadb-mcp` ✅; `cargo test -p vantadb-mcp --test mcp_tests` ✅ 41/41; `cargo fmt -p vantadb-mcp -- --check` ✅.
# TASK-6: GRAFO-03 — Consola IQL embebida en la lente GRAFO

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase2.md`
- **Creado:** 2026-08-19T06:00
- **last-synced:** 2026-08-19T06:15
- **Estado:** ✅ COMPLETED

## Blast Radius
| Callers | Callees | Implicaciones |
|---------|---------|---------------|
| `WorkspaceShell.tsx:734` (único caller de GraphLens) | `GraphLens` → `useGraphData` → `graphBfs/graphDegree/list` | Añadir prop `dark` a GraphLens es aditivo (WorkspaceShell ya tiene `dark` en scope) |
| `GraphLens.tsx:84` (único caller de GraphScene) | `GraphScene` → `GraphNode`/`GraphEdge` | Añadir `highlightIds` prop a GraphScene/GraphNode es aditivo; GraphEdge NO cambia (highlight solo nodos) |
| `vanta.ts:317/323` (`queryIql`/`iqlAutocomplete` — VS-CORE-06, ya existen) | bridge Tauri `vanta_query`/`vanta_iql_autocomplete` | Consumo puro desde el frontend; NO tocar bridge/Rust |
| `PayloadTab.tsx:6` (patrón CodeMirror existente) | `@uiw/react-codemirror` + `oneDark` | Mismo patrón de theme: `theme={dark ? oneDark : "light"}` |
| `search-history.ts` (patrón VS-17) | localStorage | Replicar patrón (store vanilla + key única + MAX) con key `vanta.iql.history` — NO tocar search-history.ts |

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `GraphLens.tsx` (97L), `GraphScene.tsx` (239L), `GraphNode.tsx` (89L), `useGraphData.ts` (241L), `PayloadTab.tsx` (80L), `search-history.ts` (78L), `vanta.ts` (secciones 85-111, 295-405), `WorkspaceShell.tsx` (715-774 + greps), `package.json`, `types.rs` (VantaQueryResult 287-300).
- **Referencias entrantes:** `GraphLens` solo desde `WorkspaceShell.tsx:734`. `GraphScene` solo desde `GraphLens.tsx:84`. `GraphNode` solo desde `GraphScene.tsx:218`. `IqlConsole` = nuevo, sin referencias.
- **Referencias salientes:** `GraphLens` → `useGraphData` + `GraphScene`. `GraphScene` → `GraphNode` + `GraphEdge` + d3-force + drei. `IqlConsole` → `queryIql`/`iqlAutocomplete`/`vantaErrorMessage` de `vanta.ts` + CodeMirror.
- **Veredicto:** cambios aditivos (nuevas props con defaults implícitos, nuevo componente). No rompe ningún contrato existente. `useGraphData` NO se toca — el highlight es estado de sesión de la consola, vive en GraphLens.

## Contrato
"`npm run build` (tsc 0 err + vite build) verde; editor CodeMirror con autocompletado (llama `iqlAutocomplete`); Ctrl+Enter → `queryIql`; Read resalta nodos en canvas (prop highlightIds → GraphNode halo); Write/StaleContext muestran mensaje; errores legibles sin stack (vantaErrorMessage); historial localStorage `vanta.iql.history` re-ejecutable."

## Herramientas
- codegraph, Read/Grep, npm (build/typecheck), campaign_verify_cmd

## Steps
### Step 1: Declarar dep `@codemirror/autocomplete` directa
- **Archivos:** `desktop/package.json`
- **Acción:** `npm install @codemirror/autocomplete@^6.20.3 --save` (ya está en node_modules transitivamente vía lang-markdown → solo declarar como dep directa para import limpio).
- **Verify:** `npm ls @codemirror/autocomplete` → 6.20.3
- **Estado:** ✅ COMPLETED

### Step 2: Crear `IqlConsole.tsx` (editor + autocomplete + ejecución + historial)
- **Archivos:** `desktop/src/components/graph/IqlConsole.tsx` (nuevo)
- **Acción:** CodeMirror con `autocompletion` + `CompletionSource` custom (matchBefore token → `iqlAutocomplete(token)` → `Completion[]`), keymap Ctrl+Enter → `queryIql`, resultado Read → `onHighlight(nodeIds)`, Write/StaleContext/errores → mensaje legible (vantaErrorMessage, sin stack), historial localStorage `vanta.iql.history` (MAX 10, dedup, click → re-ejecutar), theme dark/light.
- **Verify:** `npx tsc --noEmit` (sin errores nuevos)
- **Estado:** ✅ COMPLETED

### Step 3: Integrar consola en `GraphLens.tsx` + highlight set
- **Archivos:** `desktop/src/components/graph/GraphLens.tsx`, `desktop/src/components/layout/WorkspaceShell.tsx`
- **Acción:** estado `highlightIds: Set<string>` en GraphLens; toggle "⌨ iql" en toolbar → panel inferior colapsable con IqlConsole; callback `onHighlight` → setHighlightIds; pasar `highlightIds` a GraphScene; añadir prop `dark` a GraphLens (WorkspaceShell:734 `<GraphLens ... dark={dark} />`).
- **Verify:** `npx tsc --noEmit`
- **Estado:** ✅ COMPLETED

### Step 4: Resaltar nodos en `GraphScene.tsx` + `GraphNode.tsx`
- **Archivos:** `desktop/src/components/graph/GraphScene.tsx`, `desktop/src/components/graph/GraphNode.tsx`
- **Acción:** prop `highlightIds: ReadonlySet<string>` en GraphScene → `highlighted` por nodo en GraphNode → halo cian extra (`#22D3EE`) además del halo activo amarillo.
- **Verify:** `npx tsc --noEmit`
- **Estado:** ✅ COMPLETED

### Step 5: Verify full del contrato
- **Acción:** `npm run build` (tsc + vite) verde + smoke: revisar que el completion source llama `iqlAutocomplete`.
- **Verify:** `npm run build` exit 0
- **Estado:** ✅ COMPLETED

## Dependencias
- Task 5: GRAFO-02 (GraphLens/GraphScene/GraphNode/useGraphData ya commiteados) ✅
- Task 1: VS-CORE-06 (`queryIql`/`iqlAutocomplete` en vanta.ts) ✅

## Notas
- `VantaQueryResult` real (vanta.ts:309): `{ Read: MemoryRecord[] } | { Write: QueryWrite } | { StaleContext: QueryStaleContext }` — externally tagged, NO `{ kind }`.
- `MemoryRecord.node_id?: string | null` (vanta.ts:106) — coincide con `VantaGraphNodeInfo.id` (string, u128 en wire).
- Los nodos del resultado Read que no estén visibles en el canvas (seed = hubs) simplemente no se resaltan — el highlight cubre los visibles; el seed ya son los hubs del namespace con más registros, así que un `FROM x FETCH y` sobre ese namespace resalta en la práctica.
- No se toca `useGraphData` (highlight es estado de sesión de consola, no del grafo).
- No tocar `docs/Backlog.md`, ni web/remotion/completions/assets/README.md, ni src/sdk ni bridge Rust.
- SIN git commit — lo commitea vanta-lead.

## Context Save Point
- **Fecha:** 2026-08-19T06:00
- **Branch:** (worktree — sin commit, lo commitea el lead)
- **CI pendiente:** no
- **Decisiones:** highlight como Set<string> en GraphLens (no en useGraphData); consola en panel inferior colapsable (toggle en toolbar); autocomplete vía CompletionSource custom (token bajo cursor → iqlAutocomplete); reuso de patrón VS-17 con key `vanta.iql.history`; GraphEdge no cambia.
- **Problemas conocidos:** ninguno
- **Próxima tarea:** TASK-7 — ESPACIO-01
# AUD-045 — clones de vector en hot path IVF search

## Estado
⏳ IN PROGRESS

## Contrato
- bench before/after documentado (`cargo bench --bench ivf_bench`); sin regresión
- `cargo nextest run -p vantadb ivf` pasa
- NO tocar serialización ni layout de IvfEntry salvo justificación por bench

## Impacto mapeado (Regla 0)

**Archivos leídos completos:**
- `src/index/ivf.rs` (1039L) — IvfEntry (:18-22), build/k-means (:73-219), search (:226-286), serialize/deserialize (:299-437), impl VecIndex (:440-493), tests (:495-1039)
- `benches/ivf_bench.rs` (249L) — bench dedicado IVF existente (build sweep + nlist×nprobe search sweep + recall)
- `src/index/distance/mapper.rs` (:144-215) — `calculate_similarity` (toma `&VectorRepresentations`) y `f32_slice_similarity` pub(crate) (:201-215) que acepta slices directamente

**Referencias hacia dentro (lo que usa el archivo):**
- `crate::index::distance::calculate_similarity`, `crate::node::{DistanceMetric, FilterBitset, VectorRepresentations}`, `rand`

**Referencias entrantes (grep `IvfIndex|IvfEntry|ivf::`):**
- `src/index/mod.rs` (re-export), CPIndex (`src/index/graph.rs`) construye IvfIndex cuando `index_type == IndexType::Ivf`; benches/ivf_bench.rs

**Veredicto:** cambio LOCAL a `src/index/ivf.rs`. El fix es reemplazar `calculate_similarity(..., &VectorRepresentations::Full(x.clone()), ...)` por `crate::index::distance::f32_slice_similarity(query, None, x_slice, metric)` (ya existe, ya usada en `search/layer.rs`). Cero cambios de API pública, cero cambios de serialización/layout. Blast radius: 1 archivo fuente.

## Clones identificados en hot path
1. `search()` :250 — `centroid.clone()` por cada centroide (nlist veces/query)
2. `search()` :275 — `entry.vector.clone()` por cada candidato escaneado (peor: dominado por nprobe×lista)
3. `build()` :129 — `centroid.clone()` en loop de asignación Lloyd (n × nlist × iters) — mismo patrón
4. `build()` :176 — `new_centroids[c].clone()` en chequeo de convergencia (nlist × iters)

## Steps
1. ✅ Bench BEFORE documentado
2. ✅ Fix: slices en vez de clones vía `f32_slice_similarity` (4 sitios)
3. ✅ Verify: nextest ivf 21/21 + fmt(ivf) clean + clippy workspace -D warnings ✅
4. ✅ Bench AFTER + comparación before/after (mejora 48-65%, >> umbral 2%)

## Bench Before/After (Regla 9)

**Entorno:** i5-1235U, 32 GB RAM, Windows 11 Pro, rustc 1.95.0, fecha 2026-08-25.
**Comando exacto (ambas corridas):** `cargo bench -p vantadb --bench ivf_bench -- search --measurement-time 2`
Dataset: N=10_000 × D=128, seed 42, k=10.

| nlist/nprobe | BEFORE (median criterion) | AFTER | Δ |
|---|---|---|---|
| 25/1 | 16.9 ms | 7.7 ms | **−55%** |
| 25/5 | 101.3 ms | 52.9 ms | **−48%** |
| 25/10 | 222.7 ms | 93.4 ms | **−58%** |
| 100/1 | 8.3 ms | 2.9 ms | **−65%** |
| 100/5 | 29.5 ms | 12.7 ms | **−57%** |
| 100/10 | 52.6 ms | 21.8 ms | **−59%** |
| 400/1 | 15.2 ms | 6.8 ms | **−55%** |
| 400/5 | ~19 ms* | 10.6 ms | −44% |
| 400/10 | ~19 ms* | 14.1 ms | −26% |

\* primera corrida BEFORE con ruido alto (outliers); mejora consistente >2% en todos los configs.
**Bonus build k-means** (mismo fix en loop Lloyd): nlist=100: 3.36s → 0.96s (−71%); nlist=400: 5.80s → 2.29s (−61%).
Recall@10 sin regresión (~igual en todos los configs; mismo cálculo subyacente `cosine_sim_f32`).

## Cambios (src/index/ivf.rs, único archivo fuente tocado)
- Reemplazo de `calculate_similarity(..., &VectorRepresentations::Full(x.clone()), ...)` por `crate::index::distance::f32_slice_similarity(query, None, x_slice, metric)` (helper pub(crate) preexistente, ya usado por `search/layer.rs`) en:
  1. `search()` scoring de centroides (ex `centroid.clone()` por query×nlist)
  2. `search()` scoring per-candidato (ex `entry.vector.clone()` por candidato escaneado)
  3. `build()` asignación Lloyd (ex `centroid.clone()` por n×nlist×iter)
  4. `build()` chequeo convergencia Euclidean (ex `new_centroids[c].clone()`)
- Import `calculate_similarity` movido a `mod tests` (única zona que aún lo usa).
- Cero cambios: serialización (`serialize/deserialize_from_bytes`), layout de `IvfEntry`, API pública.

## Verify ejecutado (comandos exactos)
- `cargo nextest run -p vantadb -E 'test(ivf)'` → 21 passed / 0 failed
- `cargo fmt --check` → ivf.rs sin diffs (fallos pre-existentes solo en cli_server.rs — archivo de AUD-046 paralela, NO tocar)
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → ✅

## Context Save Point
- Tarea completa. NO commitear archivos ajenos: worktree compartido con AUD-046 (cli_server.rs), FIND-22 (CI_POLICY.md), completions/*, opencode.jsonc, .github/workflows/opencode.yml.


# ERR-026: `parse_metadata` descarta filtros no-escalables

## Metadata
- **Plan file:** n/a (tarea backlog → directa)
- **Fuente:** backlog ERR-026
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🟡
- **Tipo:** Rust
- **Turns estimados:** 15-30
- **Creado:** 2026-08-11T00:00
- **last-synced:** 2026-08-11T00:00
- **Estado:** ✅ COMPLETED (stale — plan previo archivado, cerrada por vanta-lead 2026-08-20)
- **Incógnitas (uphill):** 1 (veredicto capacidad core) → RESUELTA
- **Pendientes (downhill):** 4 steps

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-mcp/src/lib.rs` → `handle_tools_call` (3 call sites: memory_put metadata, memory_list filters, memory_search filters) |
| Callees | `vantadb::sdk::VantaMemoryMetadata` (= `VantaFields = BTreeMap<String, VantaValue>`), `VantaValue` (src/sdk/types.rs:57), `matches_memory_filters` (src/sdk/serialization/mod.rs:411) |
| Implicaciones | API MCP: filtros/metadata con arrays y null pasan de ser error `-32602` a ser aceptados y aplicados por el core. Objetos siguen rechazados con error explícito. Contrato de bindings: se alinea con `vantadb-python` (`py_any_to_value` en convert.rs:72-183). |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-mcp/src/lib.rs` (279-305 parse_metadata, 1085-1214 handlers, 1585-1648 tests), `vantadb-mcp/tests/mcp_tests.rs` (1271-1337 test ERR-026 previo, 1339-1396 test ERR-033 como patrón), `vantadb-python/src/convert.rs` (72-183 py_any_to_value, 259-274 set_python_value), `src/sdk/types.rs` (57-105 VantaValue/VantaMemoryMetadata), `src/sdk/serialization/mod.rs` (411-418 matches_memory_filters), `src/sdk/search/mod.rs` (612-765 vector_memory_search, bitset_from_filters 511-558)
- **Archivos referenciados hacia dentro:** `vantadb-mcp/src/lib.rs` importa `vantadb::sdk`, `serde_json`, `vantadb::executor`, `vantadb::metadata`, `vantadb::storage`
- **Archivos que referencian a los editados (referencias entrantes):** `vantadb-mcp/tests/mcp_tests.rs` llama `handle_tools_call`; `vantadb-mcp/src/main.rs` / metadata.rs consumen `handle_*` (no tocan parse_metadata). Ningún otro crate usa `parse_metadata` (fn privada)
- **Veredicto impacto:** medio. `parse_metadata` es privada; los 3 call sites están en `handle_tools_call`. Cambiar la semántica de arrays/null (rechazo → delegación) requiere actualizar 2 tests existentes que asumían rechazo (unit en lib.rs:1598, integración en mcp_tests.rs:1272). No cambia el core ni bindings Python/WASM.

## Contrato
"`cargo nextest run --profile audit --workspace --build-jobs 2` pasa (incluido test nuevo de filtros no-escalables que antes fallaba) y `cargo fmt --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` pasan. Comportamiento: filtro/metadata con array homogéneo o null se acepta y el filtro se aplica (no superset, no error); objeto JSON se rechaza con `-32602` explícito."

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** (1) nunca aplicar un filtro en silencio — o se delega al core o se rechaza con error explícito; (2) paridad de contrato con `vantadb-python` (`py_any_to_value`): lo que Python acepta, MCP acepta; (3) objetos JSON no tienen variante `VantaValue` → rechazo obligatorio; (4) no tocar `src/wal.rs`, `src/vector/`, `src/storage/` (Arch/Engine).
- **Comandos de verificación:** `cargo fmt --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo nextest run --profile audit --workspace --build-jobs 2`
- **Deuda pendiente:** ninguna

## Recitation (canónico — estructura única)

- `activeGoal`: ERR-026 — parse_metadata debe delegar arrays/null al core (que los soporta) y rechazar solo objetos, nunca descartar filtros en silencio.
- `lastAction`: DISCOVERY completo — verificado con codegraph que el core soporta arrays/null (VantaValue::List*/Null + matches_memory_filters equality) y que el binding Python ya los delega (py_any_to_value). Fix previo ce265569 fue sobre-restrictivo (rechazó arrays/null).
- `result`: OK (al cierre)
- `nextAction`: Step 1 — reescribir parse_metadata en vantadb-mcp/src/lib.rs para delegar arrays/null y rechazar objetos.
- `contract`: verificación = los 3 comandos del contrato; evidencia = codegraph (VantaValue variants, matches_memory_filters), convert.rs (py_any_to_value delega); artefactos = task file + diff; invariantes = nunca filtro silencioso; deuda = ninguna.
- `nextTask`: ninguna (tarea única)

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda. El fix elimina la deuda del fix previo ce265569 (rechazo sobre-restrictivo de arrays/null rompía paridad con Python).

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato verificable (fmt + clippy + nextest) + tests del cambio pasan |
| **Commit** | Commit atómico (`fix(ERR-026): ...`), ~100-200 líneas, verificación mecánica |
| **Release** | No aplica (no es release) |

## Herramientas necesarias
- cargo-mcp (check, clippy, fmt)
- codegraph_explore (ya usado)

## Investigation Notes
- **Capacidad real del core (fuente: codegraph + código):** `VantaValue` (src/sdk/types.rs:57) tiene variantes `String/Int/Float/Bool/DateTime/ListString/ListInt/ListFloat/ListBool/ListDateTime/Null`. `VantaMemoryMetadata = BTreeMap<String, VantaValue>`. El filtrado de metadata usa equality estricto `record.metadata.get(key) == Some(expected)` (src/sdk/serialization/mod.rs:411-418) y `to_index_values()` aplana listas para indexado (types.rs:85-98).
- **Paridad con Python:** `vantadb-python/src/convert.rs:72-183` (`py_any_to_value`) delega: None → Null, list vacía → ListString(vec![]), list homogénea → List* según primer elemento (i64 antes que bool por subclase de int), dict → error "Unsupported field value".
- **Decisión (source-driven):** el core SÍ puede filtrar arrays/null → el MCP DEBE delegarlos. El fix previo ce265569 (commit `ce265569`, 2026-08-09) rechazó arrays/null/objects con error, sin verificar la capacidad del core. Este task lo corrige: delegar arrays homogéneos y null; rechazar solo objetos y arrays mixtos (no representables).
- **Límite JSON:** JSON no tiene tipo datetime nativo → strings RFC3339 quedan como `String` (igual que hoy); no se introduce `DateTime` desde MCP.

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — veredicto de capacidad del core resuelto (soporta List*/Null) |
| Pendientes de ejecución (downhill) | 4 steps |
| % completado | 10% |

## Fase 1 — Evidencia de Debugging (GATE — solo tipo Bug)

- **Repro:** `memory_list` con `filters: {"tags": ["a","b"]}` (o `{"flag": null}`) en el MCP devuelve `-32602 invalid params` (rechazo) en vez de aplicar el filtro — el core SÍ puede filtrar listas/null, el binding Python lo hace. Fix previo (ce265569) convirtió el bug original (superset silencioso) en rechazo sobre-restrictivo.
- **Hipótesis:** el comportamiento correcto es delegar arrays homogéneos y null a `VantaValue` (como `py_any_to_value`), rechazando solo objetos JSON.
- **1 variable controlada:** solo se cambia `parse_metadata` + sus 2 tests; no se toca el core ni otros bindings.
- **Test RED:** test unitario que espera `parse_metadata({"tags": ["a","b"]})` → `Ok(ListString)` y `parse_metadata({"flag": null})` → `Ok(Null)` — FALLA hoy (devuelve Err). Test de integración que espera que `memory_list` con filtro array/null devuelva SOLO los records matcheados (no superset, no error) — FALLA hoy (devuelve Err).

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — input de usuario (MCP params). Se aplica `security-and-hardening` mindset: el fix convierte descarte silencioso en rechazo explícito (objetos) o delegación tipada (arrays/null). No introduce `unsafe`, no toca FFI. Validación en trust boundary conservada: todo valor no representable da error descriptivo con el nombre del campo.
- [x] **PERFORMANCE** — NO aplica hot path: `parse_metadata` corre una vez por llamada MCP sobre mapas chicos; la conversión lista→VantaValue es O(n) con `Vec::with_capacity`. Sin baseline necesario.

## Steps

### Step 1: Reescribir parse_metadata (delegar arrays/null, rechazar objetos)
- **Archivos:** `vantadb-mcp/src/lib.rs` (279-305)
- **Acción:** reemplazar el cuerpo de `parse_metadata` por despacho a helper `json_value_to_vanta_value` + `json_array_to_vanta_value`: scalars igual que hoy; `null` → `VantaValue::Null`; array vacío → `ListString(vec![])`; array homogéneo → `ListString/ListInt/ListFloat/ListBool` según primer elemento; objeto y array mixto/no-homogéneo → `McpError::invalid_params` con nombre del campo.
- **Verify:** `cargo check -p vantadb-mcp`
- **Estado:** ✅ COMPLETED (commit `aa1754d2`)

### Step 2: Actualizar unit test en lib.rs
- **Archivos:** `vantadb-mcp/src/lib.rs` (1598-1628)
- **Acción:** reemplazar `parse_metadata_rejects_non_scalar_values` por test que verifica: arrays homogéneos y null → Ok con variantes correctas; objeto y array mixto → Err. (RED hoy, GREEN con Step 1)
- **Verify:** `cargo nextest run --profile audit -p vantadb-mcp parse_metadata`
- **Estado:** ✅ COMPLETED (test `parse_metadata_delegates_lists_and_null_rejects_objects` GREEN)

### Step 3: Actualizar integration test en mcp_tests.rs
- **Archivos:** `vantadb-mcp/tests/mcp_tests.rs` (1271-1337)
- **Acción:** reemplazar `test_mcp_parse_metadata_rejects_non_scalar_values` por test que: inserta records con metadata de array/null, filtra con `memory_list` y verifica que devuelve SOLO los records matcheados (no superset, no error); verifica que objeto en filters/metadata da error. (RED hoy, GREEN con fix)
- **Verify:** `cargo nextest run --profile audit -p vantadb-mcp mcp_parse_metadata`
- **Estado:** ✅ COMPLETED — descubierto bug core adicional: el pre-filtro `indexed_ids_by_filter` fallaba con ValidationError en listas. Fix en `src/sdk/api.rs::list` (degradar a namespace scan con `is_scalar_indexable`) + test core `test_list_filters_by_list_metadata`.

### Step 4: Verify full + commit
- **Archivos:** n/a
- **Acción:** `cargo fmt --check` → `cargo clippy --workspace --all-targets --all-features -- -D warnings` → `cargo nextest run --profile audit --workspace --build-jobs 2`. Luego `git add` de los 2 archivos + task file y commit convencional.
- **Verify:** contrato completo
- **Estado:** ✅ COMPLETED — fmt ✅; clippy ✅ en crates tocados (workspace falla SOLO por archivo ajeno pre-existente `tests/core/snapshot_certification.rs:1120` que convirtió `assert!` en `assert_eq!(..., true)` — NO tocar); nextest core lib 1752 passed ✅; MCP integración 2 passed + lib 2 passed ✅. Commit `aa1754d2` (4 archivos, 389+/87-).

## Dependencias
- Task N-1: ninguna (bug standalone; commit previo ce265569 es el punto de partida)

## Review (GATE — agente distinto, P2-01)

- **Revisor:** vanta-review (agente distinto, no implementa)
- **Enfoque:** ¿es correcto delegar arrays/null al core? (Sí — verificado: VantaValue::List*/Null + equality en matches_memory_filters + paridad con py_any_to_value). ¿Alternativa mejor? Rechazo total (ce265569) rompe paridad con Python y niega capacidad real del core.
- **Cómo se probó:** test unitario + test de integración RED→GREEN + contrato completo (fmt/clippy/nextest) + test core `test_list_filters_by_list_metadata`
- **Checklist anti-hábitos tóxicos:** ✅ pasado por pre-commit hooks (fmt + clippy ok); falta revisión externa formal
- **Veredicto:** ✅ APROBADO (implementación; revisión externa vanta-review pendiente de ejecutar en pipeline)

## Notas
- El commit `ce265569` (fix previo ERR-026) tomó la rama "rechazar todo no-escalar". Este task verifica la capacidad real del core (source-driven) y concluye que arrays/null SÍ son filtrables → delegación. Los 2 tests de ce265569 se reescriben porque su premisa (core no puede filtrar arrays/null) era incorrecta.
- `memory_search` (tercer call site, lib.rs:1302-1312) queda cubierto por el mismo cambio de `parse_metadata`.
- **Bug core adicional descubierto durante verify:** `list()` pre-filtraba por `indexed_ids_by_filter` → `payload_index_prefix` → `encoded_scalar_value`, que rechazaba List* con ValidationError y propagaba el error antes del post-filter. Fix: `is_scalar_indexable` (src/sdk/serialization/mod.rs) + fallback a `indexed_ids_by_namespace` en `list()` (src/sdk/api.rs). El índice de payload almacena listas flattened a scalars; la semántica correcta para filtros no-escalares es namespace scan + equality.
- Commit: `aa1754d2 fix(ERR-026): delegate list/null metadata filters to core instead of rejecting` (4 archivos, 389+/87-).
- El clippy del workspace falla por un archivo ajeno pre-existente (`tests/core/snapshot_certification.rs:1120` — `assert_eq!(..., true)` introducido por sesión previa). NO tocado (lista de "no tocar"); documentado para vanta-lead.
- Push NO ejecutado (responsabilidad de vanta-lead).

# COV-001: Python AsyncVantaDB smoke test

## Metadata
- **Plan file:** docs/plans/2026-08-12-cov-coverage.md (Task 1)
- **Fuente:** docs/plans/2026-08-12-cov-coverage.md § Task 1 (COV-001)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟢
- **Tipo:** Python
- **Turns estimados:** 5
- **Creado:** 2026-08-12T10:00
- **last-synced:** 2026-08-12T10:00
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps (ambos ✅)

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | vantadb-python/tests/test_async_smoke.py (NUEVO — único caller de la tarea) |
| Callees | vantadb_py.AsyncVantaDB (wrapper asyncio, NO se modifica) |
| Implicaciones | API pública de AsyncVantaDB NO cambia; test es aditivo, no toca __init__.py ni test_sdk.py existente |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-python/vantadb_py/__init__.py` (390 ln), `vantadb-python/tests/test_sdk.py` (TestAsyncVantaDB, líns 790-1104+), `docs/plans/2026-08-12-cov-coverage.md`, estado git (`git status --short` confirma test_sdk.py NO está en dirty list → ya commiteado).
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** el nuevo test importa `vantadb_py as vanta` y `asyncio`, `tempfile`, `os`. Reusa el patrón `_unique_path()` / `memory_limit_bytes` de test_sdk.py (re-implementado inline para autonomía del archivo, sin acoplar al helper de otro módulo).
- **Archivos que referencian a los editados (referencias entrantes):** ninguno — el archivo es nuevo y no lo importa nadie.
- **Veredicto impacto:** BAJO. Solo se crea un archivo de test nuevo en `vantadb-python/tests/`. No se modifica `AsyncVantaDB` ni su API pública (contrato: NO cambiar API). `pytest` del crate lo recolecta automáticamente.

## Contrato
"pytest del nuevo `vantadb-python/tests/test_async_smoke.py` pasa (0 fallos), ejercitando flush, purge_expired, query, graph_* (add_edge/graph_bfs/graph_dfs/graph_topological_sort/graph_is_dag/graph_page_rank/graph_degree_centrality), put, delete/delete_memory y export_* (export_namespace/export_all) sobre AsyncVantaDB sin modificar la API pública."

## Invariantes de dominio (handoff — MUST)
- **Invariantes a preservar:** API pública de `AsyncVantaDB` (`vantadb_py/__init__.py`) no se altera; signature/names de métodos intactos. No se tocan `test_sdk.py` ni `__init__.py`.
- **Comandos de verificación:** `target/audit-venv/Scripts/python.exe -m pytest vantadb-python/tests/test_async_smoke.py -q` → 0 fallos.
- **Deuda pendiente:** ninguna (smoke test aditivo; cobertura exhaustiva queda en test_sdk.py ya commiteado).

## Recitation (canónico — estructura única)
- activeGoal: añadir smoke test async que ejercite los métodos faltantes de AsyncVantaDB y pase pytest.
- lastAction: creado vantadb-python/tests/test_async_smoke.py (3 tests: crud/flush/purge, query/graph/delete, export) y verificado con pytest → 3 passed.
- result: OK
- nextAction: ninguno (tarea completa; vanta-lead commitea el archivo nuevo).
- contract.verificacion: `target/audit-venv/Scripts/python.exe -m pytest vantadb-python/tests/test_async_smoke.py -q` → 3 passed in 1.41s ✅
- contract.evidencia: claim "binding importa y AsyncVantaDB async smoke pasa" — evidencia: pytest run (3 passed). claim "AsyncVantaDB ya commiteado en test_sdk.py pasa 16 tests" — evidencia: pytest run previo (16 passed).
- contract.artefactos: vantadb-python/tests/test_async_smoke.py
- contract.invariantes: API pública AsyncVantaDB intacta; no tocar __init__.py ni test_sdk.py.
- contract.deuda: ninguna.
- contract.queda_pendiente: el orquestador (vanta-lead) commitea el nuevo archivo tras verificar.

## Deuda técnica (Regla 6 — MUST)
**Saldo neto de deuda por PR:** Sin deuda — archivo de test nuevo, no introduce deuda.

## Definition of Done (contrato multi-nivel — P2-08)
- Task: pytest del nuevo test pasa; no se rompe API.
- Commit: atómico, conventional commit `test:`; NO lo hago yo (lo hace vanta-lead).
- Release: no aplica (test-only).

## Herramientas necesarias
- pytest via venv (target/audit-venv/Scripts/python.exe -m pytest)
- codegraph_explore (blast radius — ya ejecutado vía lectura directa)

## Investigation Notes
- Sorprendente pero confirmado: `TestAsyncVantaDB` YA existe y está commiteado en `test_sdk.py` (16 tests pasan hoy). El premisa del plan ("hoy solo sync") está desactualizada respecto al estado actual del repo. El contrato de la tarea (añadir smoke test async) se cumple con un archivo nuevo y enfocado; no se duplica lógica innecesariamente.

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)
- [x] **SECURITY** — NO aplica: el test no toca trust boundaries, input de usuario no confiable, auth, ni agrega dependencias. Solo ejercita una API ya existente con datos de test controlados.
- [x] **PERFORMANCE** — NO aplica: no se toca hot path (vector/, engine.rs); es un test de humo a nivel wrapper Python.

## Steps

### Step 1: Crear smoke test async
- **Archivos:** `vantadb-python/tests/test_async_smoke.py` (NUEVO)
- **Acción:** test aditivo que abre AsyncVantaDB en un temp path, ejercita put/get_memory/search_memory, flush, purge_expired (ttl), query (IQL), grafo (insert/add_edge/graph_bfs/graph_dfs/graph_topological_sort/graph_is_dag/graph_page_rank/graph_degree_centrality), delete/delete_memory, y export_* (export_namespace/export_all). Estilo: funciones con `asyncio.run` internas, igual que test_sdk.py. Reusa `_unique_path()` inline. NO modificar __init__.py.
- **Verify:** `target/audit-venv/Scripts/python.exe -m pytest vantadb-python/tests/test_async_smoke.py -q`
- **Estado:** ✅ COMPLETED

### Step 2: Verificar pytest del crate
- **Archivos:** `vantadb-python/tests/test_async_smoke.py`
- **Acción:** correr pytest sobre el nuevo archivo; confirmar 0 fallos. (No se corre el dir completo para no mezclar con fallos pre-existentes ajenos a esta tarea; el contrato del crate se verifica con el archivo nuevo.)
- **Verify:** `target/audit-venv/Scripts/python.exe -m pytest vantadb-python/tests/test_async_smoke.py -q` → 3 passed in 1.41s ✅
- **Estado:** ✅ COMPLETED

## Dependencias
- Ninguna.

## Review (GATE — agente distinto, P2-01)
- **Revisor:** doubt-driven-development (fallback — no hay agente distinto en esta ejecución de un solo worker; la verificación mecánica es el gate primario).
- **Enfoque:** smoke test aditivo, mínimo, ejercita exactamente los métodos del contrato. No toca API.
- **Cómo se probó:** pytest run mecánico del archivo nuevo (no auto-reporte).
- **Checklist anti-hábitos tóxicos:** sin invención de outputs; verify mecánico real; no se declara done sin pytest verde.
- **Veredicto:** ✅ approve (test pasa, aditivo, sin tocar API).

## Notas
- El binding ya está built en el venv (import OK). Python 3.11.9.
- `test_sdk.py` ya tenía TestAsyncVantaDB commiteado (16 passed) — ver Investigation Notes. Esta tarea añade un smoke test enfocado y autónomo como entregable explícito del contrato COV-001.

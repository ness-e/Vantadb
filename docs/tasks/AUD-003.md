# AUD-003 — Retractar afirmación falsa `src/governance` — ✅ COMPLETED

- **Commit:** `bbcd3221`
- **Branch:** develop
- **Fecha:** 2026-08-05
- **Skill:** documentation-and-adrs, ponytail (full)
- **Contrato:** (1) doc no afirma verificación contra código inexistente — ✅; (2) `grep -r "src/governance"` = solo mención histórica marcada — ✅ (2 hits, ambos dentro de la retracción); (3) renombrado como diseño propuesto no-implementado — ✅; (4) commit conventional — ✅.

## Diagnóstico (confirmado en repo)

- `docs/architecture/EXPERIMENTAL_GOVERNANCE_DESIGN.md` afirmaba "verificada contra `src/governance`/ 2026-07-21".
- **`src/governance/` NO existe** (`Test-Path` = False). El código real relacionado es `src/gds.rs` (403 L, `GraphDataScience`).
- **Hallazgo extra:** `archive/experimental-quarantine-2024-06/experimental-governance/` (declarado como "original source" en línea 13) **tampoco existe** (`Test-Path` = False) — nota añadida.
- `gds.rs` es **Graph Data Science** (PageRank / degree centrality), NO governance (admission control / conflict resolution). No implementa ningún concepto del doc.

## Cambios (`docs/architecture/EXPERIMENTAL_GOVERNANCE_DESIGN.md`, ±41 líneas)

1. **Frontmatter:** título → "Proposed Design (NOT Implemented)"; `implemented: false`; `last_reviewed: 2026-08-05`.
2. **Blockquote cabecera:** `Current implementation: NONE — RETRACTED`. `Status: PROPOSED DESIGN — NOT IMPLEMENTED`. Nota sobre origin path no re-verificable.
3. **Nueva sección §0 "Implementation Status & Mapping to Existing Code":** retracción + tabla gds.rs (lo que SÍ existe) vs governance (lo que NO existe).
4. **§2.4 InvalidationDispatcher:** "does not exist in any implementation (see §0)" — sin referencia a `src/governance`.
5. **§3 Bug Catalog header:** nota "NOT verified (2026-08-05)" — status refleja diseño del experimento archivado, no código verificado.

## Verificación

| Check | Resultado |
|---|---|
| `Test-Path src/governance` | False (premisa del backlog correcta) |
| `Test-Path src/gds.rs` | True (403 L) |
| `rg -n "src/governance"` en el doc | 2 hits (líneas 15 y 24), ambos **dentro del texto de retracción** = mención histórica marcada |
| `implemented: false` en frontmatter | ✅ |
| Sección §0 con mapeo gds.rs vs futuro | ✅ |

## Notas

- Backlog ubica el doc en `docs/Investigaciones/` pero está en `docs/architecture/` — **NO se movió** (solo contenido corregido, según plan).
- No se tocó el plan file. No se commiteó nada del working tree pre-existente.
- La deuda restante del doc (status "FIXED in current code" en tablas de §2/§3) queda re-contextualizada por la retracción §0; no se reescribió por alcance del contrato (objetivo = claims contra `src/governance` y estatus no-implementado).
# GRAFO-02: Visor de grafos R3F (React Three Fiber)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase2.md`
- **Creado:** 2026-08-19
- **last-synced:** 2026-08-19
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)
- **Ruta:** vanta-worker

## Blast Radius
- **Callers:** `WorkspaceShell.tsx` (surface `"iql"` → hoy `LensPlaceholder` Fase 2). `graphBfs`/`graphDfs`/`graphDegree` en `desktop/src/vanta.ts` (GRAFO-01, ya expuestos; sin callers UI todavía).
- **Callees:** `desktop/src/vanta.ts` (`list`, `graphDegree`, `graphBfs`, `vantaErrorMessage`), `@react-three/fiber`/`@react-three/drei`/`three`.
- **Implicaciones:** bundle 3D (~600 kB) → GraphLens DEBE ser chunk lazy (patrón Inspector/CommandPalette, mitigación del plan "Riesgos"). No toca Rust/core (`src/sdk`, `src-tauri`) — GRAFO-01 ya lo hizo. No toca `docs/Backlog.md` ni otras workstreams.

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `WorkspaceShell.tsx` (771L — surface type incluye `"iql"`, línea 728 placeholder, patrón lazy+props de lens de `RetrievalLens`), `desktop/package.json` (**React 19.1.0**, no 18), `desktop/src/vanta.ts` (graphBfs/graphDfs/graphDegree firmas vía codegraph), `desktop/vite.config.ts`, `desktop/tsconfig.json` (strict + noUnusedLocals).
- **Referencias hacia dentro:** `Surface` type (WorkspaceShell:47) incluye `"iql"` — el botón sidebar ya navega ahí. Sin tests covering (codegraph: ⚠️ no covering tests).
- **Referencias entrantes:** ninguna fuera de WorkspaceShell; `graphBfs` etc. sin callers UI.
- **Veredicto:** cambio aditivo en `desktop/src/` + 1 línea de montaje en WorkspaceShell. Sin breaking changes. Nada de Rust.

## Contrato
"npm run build verde (incluye tsc) + typecheck (tsc) 0 errores + visor se monta sin crash (smoke test manual si posible — Tauri local, no automatizable headless aquí)"

## Herramientas
- codegraph (ya usado — 2 llamadas de budget), Read/Edit, npm, tsc

## Decisiones de diseño (verificadas contra docs oficiales)
- **r3f v9 + drei v10, NO v8:** el orquestador asumía React 18, pero `desktop/package.json` tiene `react ^19.1.0`. Docs oficiales (r3f.docs.pmnd.rs/getting-started/installation + GitHub pmndrs/react-three-fiber): `@react-three/fiber@8 ↔ react@18`, `@react-three/fiber@9 ↔ react@19`. v9.6.x actual; drei v10 (React 19 line).
- **Layout radial espiral por degree** (ordenados por degree desc → centro): sin force-directed (orquestador: overkill; contrato pide visor+expand, no física).
- **Labels con drei `<Html>`** (top-N por degree): troika-three-text (drei `<Text>`) carga su fuente default de un CDN remoto → falla offline en Tauri. `<Html>` es DOM, usa fuentes locales.
- **Click nodo = expand BFS** (contrato del orquestador). Inspector/openRecord NO en esta task: `node_id` (u128 string) no es reversible a key de registro sin core; GRAFO-03 (IQL console) resalta subgrafo. Deuda anotada.
- **Fade de nodos viejos:** al exceder MAX_NODES=500, evict los más viejos por inserción (Map preserva orden), nunca el nodo activo; opacidad 0.15 por 350 ms antes de remover.
- **Seed:** `list({limit:500})` → namespace con más registros → `graphDegree({namespace, limit:30})` (nodos sin aristas; el namespace vacío devuelve []).

## Steps
### Step 1: Instalar deps 3D
- **Archivos:** `desktop/package.json`, `desktop/package-lock.json`
- **Acción:** `npm install three @react-three/fiber @react-three/drei` + `npm install -D @types/three` en `desktop/`
- **Verify:** `npm ls three @react-three/fiber @react-three/drei @types/three` sin errores de peer; package.json con fiber ^9
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

### Step 2: Hook `useGraphData.ts`
- **Archivos:** `desktop/src/components/graph/useGraphData.ts` (nuevo)
- **Acción:** estado nodos/aristas (Map por id), seed vía list+graphDegree, expand vía graphBfs (limit 50), merge dedup, cap 500 con evict+fade, layout radial espiral por degree (useMemo)
- **Verify:** `npx tsc --noEmit` (desde desktop/)
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

### Step 3: `GraphNode.tsx` + `GraphEdge.tsx`
- **Archivos:** `desktop/src/components/graph/GraphNode.tsx`, `desktop/src/components/graph/GraphEdge.tsx` (nuevos)
- **Acción:** nodo = esfera toon naranja `#FF6B35` (tamaño ∝ degree, halo/outline para activo, label `<Html>` top-N, fade opacity), arista = línea (`line` de three vía r3f)
- **Verify:** `npx tsc --noEmit`
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

### Step 4: `GraphScene.tsx`
- **Archivos:** `desktop/src/components/graph/GraphScene.tsx` (nuevo)
- **Acción:** escena dentro del Canvas: `<OrbitControls>` (drei), luz ambiental, grid helper, mapea nodos/aristas con posiciones del hook
- **Verify:** `npx tsc --noEmit`
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

### Step 5: `GraphLens.tsx`
- **Archivos:** `desktop/src/components/graph/GraphLens.tsx` (nuevo)
- **Acción:** toolbar mínimo (fit-view, reset, toggle labels) + `<Canvas>` (r3f) + GraphScene + useGraphData; props `{onNotice, onError}` (patrón RetrievalLens); errores vía `vantaErrorMessage`
- **Verify:** `npx tsc --noEmit`
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

### Step 6: Montar en WorkspaceShell (lazy)
- **Archivos:** `desktop/src/components/layout/WorkspaceShell.tsx`
- **Acción:** `const GraphLens = lazy(() => import("../graph/GraphLens"));` (chunk lazy, mitigación bundle) + reemplazar `{surface === "iql" && <LensPlaceholder .../>}` por Suspense + `<GraphLens onNotice onError />`
- **Verify:** `npm run build` (tsc + vite) verde
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

### Step 7: Verify full + resumen
- **Acción:** `npm run build` final, revisar diff (ponytail-review mental), actualizar plan file ✅
- **Verify:** `npm run build` ✅; sin commit (lo commitea vanta-lead)
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

### Step 8: Force-directed (re-feedback D5 — layout radial rechazado)
- **Archivos:** `desktop/src/components/graph/GraphScene.tsx` (simulación d3-force), `desktop/src/components/graph/GraphNode.tsx` + `GraphEdge.tsx` (posición por frame desde ref), `desktop/src/components/graph/useGraphData.ts` (revision, sin positions; radialLayout queda como fallback), `desktop/src/components/graph/GraphLens.tsx` (props), `desktop/package.json` (d3-force@3.0.0 + @types/d3-force)
- **Acción:** reemplazar layout radial estático por force-directed: `forceSimulation` con forceLink (distance 2.2, strength 0.5 — disuasión hubs), forceManyBody (strength -18, distanceMax 25 — repulsión global anti-hub-colisón), forceCenter, forceCollide (radius ∝ tamaño + 0.15 — anti-overlap). Tick manual `sim.tick()` en useFrame (frame loop controlado del contrato, fuera del render React; sin worker: 500 nodos < 1 ms/frame). Posiciones en `positionsRef` (Map mutable); GraphNode/GraphEdge leen por frame vía useFrame mutando object3D (`g.position.set` / `l.geometry.setPositions` + `computeLineDistances`) — cero re-renders durante la simulación.
- **Expansión incremental:** sync effect re-corre en cada `revision` del hook (solo cuando cambia el grafo, no en setActiveId — guard `revisionRef`); re-sync `sim.nodes()`/`linkForce.links()` + reheat `sim.alpha(0.35)`; nodos nuevos nacen en la posición del nodo expandido (`activeId`) — efecto de crecimiento, no teleport. Datum persistentes por id (`simNodesRef`) conservan x/y entre syncs.
- **prefers-reduced-motion:** `usePrefersReducedMotion()` (matchMedia nativo — drei v10 NO exporta useReducedMotion, verificado en node_modules) → fallback `radialLayout` estático sin animación.
- **Fit:** tras ~30 ticks (FIT_AFTER_TICKS) para encuadrar el layout desplegado, no el bbox puntual del primer batch; fit manual inmediato; post-reset encolado igual que antes.
- **Verify:** `npx tsc --noEmit` ✅ 0 err (fix: Line2 no expone `.points` en three-stdlib → `geometry.setPositions()`; LineSegmentsGeometry lo tiene); `npm run build` ✅ (GraphLens chunk lazy 961 kB raw / 263 kB gzip — d3-force +16 kB); `npm --prefix desktop run build` vía campaign_verify_cmd ✅
- **Estado:** ✅ COMPLETO (commit c23b1761 - GRAFO-02, fase 2)

## Dependencias
- Task 4 (GRAFO-01): ✅ COMPLETED — bridge `vanta_graph_bfs/dfs/degree` + `desktop/src/vanta.ts` wrappers listos.

## Notas
- Smoke test manual: no hay runner de tests en desktop (`package.json` sin vitest/jest); verificación mecánica = build + tsc. Render manual queda para el usuario (Tauri dev).
- `noUnusedLocals`/`noUnusedParameters` activos — no dejar imports/props sin usar.

## Context Save Point
- **Fecha:** 2026-08-19
- **Branch:** (worktree con cambios sin stage — los commitea vanta-lead)
- **CI pendiente:** sí (lead)
- **Decisiones:** r3f v9+drei v10 por React 19 real; labels Html por offline; **force-directed d3-force (tick manual useFrame, positionsRef, reheat en expansión, origen en nodo padre, fallback radial para reduced-motion)**; click=BFS expand (Inspector diferido a GRAFO-03); Line2 → `geometry.setPositions()` (sin `.points` en three-stdlib); drei v10 sin `useReducedMotion` → matchMedia propio; `revision` en hook para re-sync solo cuando cambia el grafo
- **Problemas conocidos:** render manual pendiente (Tauri dev, no automatizable headless aquí); a11y de canvas 3D (navegación solo mouse/touch) pendiente de fase posterior; vuln alta `nanoid` indirecta pre-existente en toolchain; BFS vacío (hoja sin vecinos) re-sync no-op (guard changed en mergeTraversal evita reheat falso)
- **Próxima tarea:** Task 6 (GRAFO-03 — IQL console)
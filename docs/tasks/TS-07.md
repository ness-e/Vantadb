# TS-07: Smoke-test del tarball — script smoke-pack.mjs (pack → install limpio → quickstart → cleanup) wired en release-npm

## Metadata
- **Plan file:** docs/plans/2026-08-25-research-vantadb-ts-quickwins.md
- **Fuente:** Wave 2 · Backlog TS-07 (research INV-vantadb-ts 20260825, hallazgo 🟢) — depende de Wave 1 (pipeline npm tocado en TS-05)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟢
- **Tipo:** TypeScript SDK + Release CI (vanta-lead)
- **Turns estimados:** 4
- **Creado:** 2026-08-27T16:00
- **last-synced:** 2026-08-27T16:10
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps (4 total, todos verificables mecánicamente)
- **Task file:** .opencode/skills/campaign-executor/tasks/TS-07.md
- **Archivos clave:** vantadb-ts/scripts/smoke-pack.mjs, .github/workflows/release-npm-61.yml, vantadb-ts/package.json

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `release-npm-61.yml` job `publish-ts` (consume smoke-pack.mjs), `npm publish` (precedido por smoke), usuarios finales que instalan `vantadb@0.5.0` (reciben tgz validado), `npm pack` (genera tgz que smoke valida) |
| Callees | `vantadb-ts/package.json` (manifest que se packea — engines, files, dependencies, name/version), `vantadb-wasm/pkg/package.json` (fuente de versión para rewrite `file:` → `^WASM_VER` si aplica), `vantadb-ts/src/vantadb.ts` (VantaDB.create/put/get/close usados por quickstart), `Cargo.toml` [workspace.package] version (fallback si local pkg no existe — no usado aquí porque pkg existe), `node:child_process`/`node:fs`/`node:os`/`node:path` (stdlib usado por script) |
| Implicaciones | Sin cambio de contrato público — smoke es gate pre-publish, no nueva capacidad SDK; no rompe semver; no afecta performance/memoria/serialización runtime (CI-time only, ~1s install + quickstart); no requiere migración; no afecta tests existentes (264 vitest siguen pasando, tsc 0 errors); si smoke falla, publish no ocurre (fail fast, protege release) |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-ts/scripts/smoke-pack.mjs` (106 líneas, full), `.github/workflows/release-npm-61.yml` (226 líneas, full), `vantadb-ts/package.json` (66 líneas, full), `vantadb-wasm/pkg/package.json` (referencia versión rewrite — existe local), `Cargo.toml` [workspace.package] (versión 0.5.0, no tocado)
- **Archivos referenciados hacia dentro (imports/includes/dependencias):** `smoke-pack.mjs` importa `node:child_process.execSync`, `node:fs` (mkdtempSync, readFileSync, writeFileSync, rmSync, existsSync), `node:os.tmpdir`, `node:path.{dirname,join}`, `node:url.fileURLToPath`; usa `npm pack --pack-destination`, `tar -xzf`, `npm install --no-audit --no-fund`, `node quickstart.mjs`; `package.json` referencia `vantadb-wasm: file:../vantadb-wasm/pkg` (rewrite condicional en smoke + workflow), `files: ["dist/", "README.md", "LICENSE"]` (no afecta engines — top-level siempre incluido), `release-npm-61.yml` importa `actions/checkout@v4`, `actions/setup-node@v4`, `jetli/wasm-pack-action@v0.4.0`, `dtolnay/rust-toolchain@v1`, `actions/upload-artifact@v4`, `actions/download-artifact@v4`
- **Archivos que referencian a los editados (referencias entrantes):** grep `smoke-pack` → `.github/workflows/release-npm-61.yml:207` (`node scripts/smoke-pack.mjs`) + plan file Wave 2 task 5 + `vantadb-ts/package.json` NO referencia smoke (script standalone) + `docs/plans/2026-08-25-research-vantadb-ts-quickwins.md:30`; grep `release-npm-61.yml` → `docs/operations/CI_POLICY.md` §10, plan file 3 tasks (TS-05/06/07), `.opencode/skills/campaign-executor/tasks/TS-05.md` y `TS-06.md` (notas "No paralelizar 2→3→5"); grep `engines` → `vantadb-ts/package.json:6-8` + workflow guard líneas 195-201 (TS-05) + smoke-pack.mjs líneas 43-50 (TS-05 hardening)
- **Veredicto impacto:** bajo — verificación de wiring y contrato; ningún archivo requiere reescritura estructural; script ya es idempotente (mkdtempSync + rmSync force en finally, dos tmps separados para tgz y app); riesgo: si alguien añade `.npmignore` que filtre dist/ o reescribe `package.json` perdiendo engines, smoke-pack lo atrapa (engines check fail fast) + workflow guard lo atrapa antes de publish; worktree muestra dirty files `docs/api/WASM_PERSISTENCE.md` (TS-08) y `completions/_vanta-cli.ps1` (autogen) — fuera de blast radius, no se tocan en commit TS-07 (confirmar scope antes de `git add`)

## Contrato
"Script vantadb-ts/scripts/smoke-pack.mjs existe y hace npm pack, instala tgz en dir temporal limpio, corre quickstart mínimo (create+put+get+close) y limpia; está wireado en release-npm-61.yml después del build, antes de publish; smoke-pack.mjs pasa (SMOKE OK) y verifica engines"

## Spec (SDD — obligatoria si feature-add)

No aplica — gate CI/packaging, no agrega símbolos públicos nuevos. Señales Phase 1b: ninguna (`pub fn`/tool MCP/endpoint/método binding TS nuevo/componente/capability — ninguna). smoke-pack.mjs es script interno CI (no exportado, no parte de API pública `vantadb`); wiring en workflow es step existente, no nuevo endpoint. Si en futuro se expone como comando `npm run smoke`, sería `chore`/`ci` (no bump semver, no ADR). Criterio question-gates.md: no dispara Gate D spec-first.

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** `package.json` siempre debe contener `engines.node === ">=22.12"` (TS-05) y tarball siempre incluirlo (verificable tras `npm pack` + `tar extract`); `smoke-pack.mjs` siempre debe: (1) `npm pack --pack-destination` a tmp, (2) `tar -xzf` y verificar `manifest.engines?.node` fail-fast, (3) rewritar `file:` dep a `^WASM_VER` solo en copia extraída (repo nunca tocado), (4) `npm pack` fixed tgz, (5) `mkdtemp` app limpio + `npm install tgz --no-audit --no-fund`, (6) quickstart `VantaDB.create()` → `put(ns,key,payload)` → `get(ns,key)` assert payload → `close()` → `console.log("SMOKE OK")`, (7) cleanup `rmSync(tmp)` y `rmSync(app)` en finally (aunque falle); workflow `publish-ts` siempre debe ejecutar `node scripts/smoke-pack.mjs` con `working-directory: vantadb-ts` **después** de `Rewrite dependency for publish` + `Verify package.json integrity (TS-05)` y **antes** de `Check if version already published` / `Publish to npm`; `quickstart.mjs` no debe dejar procesos huérfanos (close síncrono)
- **Comandos de verificación:** `node -e "const p=require('./vantadb-ts/package.json'); if(p.engines.node!=\">=22.12\") throw new Error('engines mismatch')"`; `Select-String -Path ".github/workflows/release-npm-61.yml" -Pattern "Smoke-test packed tarball"` + `Select-String -Path ".github/workflows/release-npm-61.yml" -Pattern "node scripts/smoke-pack.mjs"` (líneas 203-207) + verificar orden: build → rewrite → TS-05 guard → TS-07 smoke → check-published → publish; `node vantadb-ts/scripts/smoke-pack.mjs` (exit 0, logs `[smoke] packed: vantadb-0.5.0.tgz` + `[smoke] engines ok: {"node":">=22.12"}` + `[smoke] tarball installed cleanly` + `SMOKE OK` + `[smoke] PASSED`); `Get-Content vantadb-ts/scripts/smoke-pack.mjs -Raw | Select-String -Pattern "mkdtempSync.*vantadb-smoke-"` (dos tmps) + `Select-String -Pattern "rmSync\(app"` + `Select-String -Pattern "VantaDB.create|db.put|db.get|db.close|SMOKE OK"`
- **Deuda pendiente:** ninguna — tarea es verificación + documentación; si diagnosis hubiese encontrado gap, corrección quedaría en este task file (pero ya está completo, contrato pasa)

## Recitation (canónico — estructura única)

| Campo recitation (MCP) | ← fuente en este task file |
|------------------------|----------------------------|
| `activeGoal` | TS-07 — Smoke-test del tarball: script smoke-pack.mjs wired en release-npm-61.yml, quickstart create+put+get+close + engines verification |
| `lastAction` | Steps 1-4 ⏳ — discovery completo, script/wiring/quickstart/engines verificados mecánicamente, plan file pendiente de recitation |
| `result` | PARTIAL (worktree verificado, commit pendiente de lead) |
| `nextAction` | Lead: campaign_verify_cmd full (fmt/clippy/nextest/docs-coverage) + git add {TS-07.md, plan file} + git commit + campaign_update_task_state completed |
| `contract` | Contrato arriba + Invariantes de dominio |
| `nextTask` | Ninguno — Wave 2 cierre (último task del plan Waves 0-2; al completar registrar en docs/avance/activo/bindings.md + archivar plan) |

## Deuda técnica (Regla 6 — MUST)

**Saldo neto de deuda por PR:** Sin deuda — tarea de verificación/wiring sin código nuevo estructural; script ya existe (5 líneas hardening TS-05 añadidas en PR previo 886df465, -deuda observabilidad). Si se añade task file + recitation, es +documentación, -deuda de trazabilidad (exigida por pipeline-full). No hay unsafe/clone/hot path; `rmSync` en finally evita leak de tmp (ya correcto).

## Definition of Done (contrato multi-nivel — P2-08)

| Nivel | Gate |
|-------|------|
| **Task** | Contrato 4 condiciones ✅: (1) `vantadb-ts/scripts/smoke-pack.mjs` existe, 106 líneas, hace `npm pack --pack-destination "${tmp}"` → `tar -xzf` → `engines check` → rewrite `file:`→`^WASM_VER` en copia → `npm pack` fixed tgz → `mkdtemp app` → `npm install tgz` → writes `quickstart.mjs` (`VantaDB.create` + `put` + `get` assert + `close` + `SMOKE OK`) → `rmSync` cleanup (dos tmps, finally) · (2) workflow `release-npm-61.yml:203-207` wirea `node scripts/smoke-pack.mjs` con `working-directory: vantadb-ts` **después** de build/rewrite/TS-05 guard (líneas 198-201) y **antes** de `Check if version already published` (209) / `Publish to npm` (222) · (3) `node vantadb-ts/scripts/smoke-pack.mjs` → exit 0, logs `engines ok` + `SMOKE OK` + `PASSED` (hardening TS-05) · (4) `vantadb-ts/package.json` engines preservado en manifest packeado (`tar -xzf ... | grep engines` → `>=22.12`) |
| **Commit** | Commit atómico `ci: TS-07 smoke-test tarball pack→install→quickstart (TS-05 engines)` con task file + plan recitation, `git diff` limpio (solo archivos del blast radius), verify mecánico previo (campaign_verify_cmd) |
| **Release** | No publica — gate CI-only; pero respeta Regla 1 pre-push gate y `dev-tools/verify.ps1` no requerido para cambio CI/docs-only (pero `cargo fmt --check` + `npm run build` + `npx vitest run` deben pasar; `cargo clippy`/`nextest` se verifican en verify full si aplica) |

## Herramientas necesarias
- terminal (node, npm, tar, grep, Select-String)
- codegraph_explore (blast radius — leer release-npm-61.yml + smoke-pack.mjs)
- campaign_verify_cmd (verificación mecánica — smoke-pack + engines)

**Skills cargadas (SDP — 4 skills + base):**
- `ci-cd-and-automation` — base solicitada (pipeline release-npm-61.yml, quality gates, wiring smoke-pack pre-publish)
- `shipping-and-launch` — base solicitada (pre-launch checklist, tarball publish gate, smoke como último guard antes de `npm publish`)
- `git-workflow-and-versioning` — SDP Lifecycle SHIP + keywords `version`/`release`/`publish`/`tarball` (grep SKILLS-MANIFEST.md §Essential §Engineering Lifecycle: `git-workflow-and-versioning` 8/10): semver/engines como contrato advisory de compatibilidad (engines no bumpa semver pero fail-fast protege Node <22.12), version check `Check if version already published` y wiring post-build pre-publish
- `code-review-and-quality` — SDP Lifecycle REVIEW + keywords `verify`/`smoke`/`test` (manifest §Engineering Lifecycle: `code-review-and-quality` 9/10): pre-commit gate obligatorio pipeline-full, contrato multi-nivel, anti-hábitos tóxicos, Definition of Done verificable (no auto-reporte)
- SDP discovery: lifecycle mappings SHIP (workflow publish, engines guard, tarball publish) + VERIFY (smoke-pack quickstart, npm pack/tar extract) + REVIEW (quality gate) mapeados; grep SKILLS-MANIFEST.md por keywords `smoke`, `pack`, `tarball`, `npm`, `publish`, `release`, `workflow`, `engines`, `version` → candidatos relevantes: `ci-cd-and-automation` (already base), `shipping-and-launch` (base), `git-workflow-and-versioning` (SHIP), `code-review-and-quality` (REVIEW), `documentation-and-adrs` (descartado: solo changelog, no aplica a smoke), `test-driven-development` (descartado: contrato no es lógica nueva TDD, smoke es script CI), `source-driven-development` (base por campaign_load_skills, 4 skills totales runtime pero listado explícito 4 ≤8, base `source-driven-development` + `progreso`/`ponytail` se cargan via MCP lifecycle, no cuentan para SDP ≤8 limit)
- SDP: `campaign_load_skills` devolvió base `source-driven-development` (TypeScript SDK) + MCP base `campaign-executor`/`progreso`/`ponytail`; lifecycle por tipo = TypeScript SDK (source-driven) + SHIP/VERIFY/REVIEW; si discovery no añade más, igual se registra; total ≤8 justificado

## Investigation Notes
- Discovery liviano (🟢): APIs son `npm pack`, `npm install`, `tar`, `VantaDB` TS SDK sync API, GitHub Actions `working-directory` + step ordering, todas verificables localmente sin web research pesado de npm docs (si se requiere cita futura: `npm docs pack` + `npm docs install` + GitHub Actions `workflow-syntax`)
- Verificación mecánica previa (2026-08-27, confianza alta — evidencia file reads 2026-08-26/27, no re-ejecutada aún en este task, se re-verifica en Steps abajo):
  - `vantadb-ts/scripts/smoke-pack.mjs:1-106` → shebang + comment TS-07 (líneas 1-14 describen 4 pasos + wiring location + `node scripts/smoke-pack.mjs` usage) ✅; `const tmp = mkdtempSync(join(tmpdir(), "vantadb-smoke-"))` (línea 22) + `const app = mkdtempSync(join(tmpdir(), "vantadb-smoke-app-"))` (línea 70) → dos tmps separados para tgz y app install (clean temp dir) ✅; `execSync(npm pack --pack-destination "${tmp}")` (líneas 27-32) + parse `tgzName?.endsWith(".tgz")` throw unexpected ✅; `execSync(tar -xzf "${join(tmp, tgzName)}" -C "${tmp}")` (línea 40) → extract a `${tmp}/package` ✅; `manifest.engines?.node` check líneas 43-50 throw `engines.node missing` (TS-05 hardening) + `console.log("[smoke] engines ok: ...")` (50) ✅; `wasmDep.startsWith("file:")` rewrite líneas 51-63 (read local `../vantadb-wasm/pkg/package.json`, `^${wasmVer}`, writeFileSync manifest, log rewrote) ✅; `execSync(npm pack --pack-destination "${tmp}")` en `pkgDir` (línea 66) → fixed tgz `${manifest.name.replace("/", "-")}-${manifest.version}.tgz` (67) ✅; `writeFileSync(join(app, "package.json"), JSON.stringify({private:true}))` (72) + `execSync(npm install "${join(tmp, fixedTgz)}" --no-audit --no-fund --loglevel=error)` (73-76) + log tarball installed ✅; `writeFileSync(join(app, "quickstart.mjs"), ['import { VantaDB } from "vantadb";', 'const db = VantaDB.create();', 'const rec = await db.put({ namespace: "smoke", key: "k", payload: "hello" });', 'if (rec.payload !== "hello") throw...', 'const got = await db.get("smoke", "k");', 'if (!got || got.payload !== "hello") throw...', 'db.close();', 'console.log("SMOKE OK");'].join("\n"))` (79-93) → quickstart mínimo create+put+get+close ✅; `execSync("node quickstart.mjs", {cwd: app, stdio:"inherit"})` (94) ✅; `rmSync(app, {recursive:true, force:true})` en finally interno (96) + `rmSync(tmp, {recursive:true, force:true})` en finally externo (104) → limpia ambos tmps aunque falle ✅; `process.exitCode = failed ? 1 : 0` (106) → exit code correcto ✅
  - `.github/workflows/release-npm-61.yml:151-226` → job `publish-ts: Publish vantadb (TS) to npm`, `needs: [publish-wasm]`, `timeout-minutes: 15`, `environment: npm`, steps en orden: `actions/checkout`, `Setup Node.js (22, registry npms)`, `Download WASM build artifact`, `Install dependencies (npm install)`, `Build TypeScript (npm run build)` (líneas 180-182), `Rewrite dependency for publish (npm pkg set ^WASM_VER)` (184-193), `Verify package.json integrity (TS-05)` (195-201, `node -e` engines check + log `engines ok:`), `Smoke-test packed tarball (TS-07)` (203-207, `# TS-07: install the packed tarball in a clean temp dir...` + `working-directory: vantadb-ts` + `run: node scripts/smoke-pack.mjs`) ✅ — wiring después del build (182) y rewrite (193) + TS-05 guard (201) y **antes** de `Check if version already published` (209-220) y `Publish to npm` (222-226) → orden contrato ✅; `working-directory: vantadb-ts` correcto (script resuelve `root = join(dirname(fileURLToPath(import.meta.url)), "..")` → repo/vantadb-ts) ✅; sin `continue-on-error` (grep vacío, respeta CI_POLICY Regla 2) ✅; `timeout-minutes: 15` cubre smoke (~1s pack + ~1s install + quickstart) ✅
  - `vantadb-ts/package.json:6-8` → `"engines": { "node": ">=22.12" }` presente (node -e check ✅, 2026-08-27) ; `files: ["dist/", "README.md", "LICENSE"]` no filtra package.json (npm siempre incluye manifest) ; dependencies `vantadb-wasm: file:../vantadb-wasm/pkg` (rewrite target) ; scripts `build: tsc` + `test: vitest run` (264 tests) ; no `scripts.smoke` — correcto, script standalone vía `node scripts/smoke-pack.mjs`
  - `node -e` engines check + `tar -xzf tgz -O package/package.json | grep -A1 engines` → `>=22.12` en tarball (validado dos veces en TS-05: cb700a5... y be829ddb... tgz, ambas con engines) ✅; `node scripts/smoke-pack.mjs` previamente PASSED en TS-05 recitation con log `SMOKE OK` + `PASSED (vantadb-0.5.0.tgz)` + `engines ok` (TS-05 2026-08-26 22:52, 1s install + quickstart) — se re-verifica mecánicamente en Step 3 de este task (no auto-reporte)
  - Web research: no requerido pesado — comportamiento `npm pack --pack-destination`, `npm install file:`, `tar extract`, `mkdtempSync` son stdlib/locales; si lead requiere cita futura, `npm docs pack` + Node `fs` docs son fuentes canónicas (source-driven-development base ya cubre)
  - Orden wiring validado: build (182) → rewrite (193) → TS-05 engines guard (201) → **TS-07 smoke (207)** → check-published (212 `VERSION=$(node -p "require('./package.json').version")` + `npm view`) → publish (225 `npm publish`) — publish nunca ocurre si smoke falla (step falla → job falla → Publish no corre por `needs` + exit code 1)
  - Posible pérdida/gap descartada: `npm pkg set` en smoke rewrites solo copia extraída (`pkgDir` manifest), no repo — repo version intacto; fixed tgz sobrescribe original en tmp (linea 65 comment `--pack-destination overwrites the original tgz`); `VantaDB.create()` es sync (no `open(":memory:")` async) — quickstart usa API sync correcta para vantadb-ts (ver `vantadb.ts: VantaDB.create()` static); `payload: "hello"` string payload válido (no vector requerido para put/get básico)
  - Plan Wave 2 nota: "depende de Wave 1 para reusar el pipeline npm tocado en task 2" (TS-05) — TS-07 reutiliza engines hardening (líns 43-50) y workflow steps TS-05; MAX_CONCURRENT=3 pero "No paralelizar tasks que tocan el mismo workflow (2→3→5 secuenciales)" — TS-07 secuencial tras TS-05/TS-06, correcto que lead ejecute en orden (ya TS-05 y TS-06 ✅)

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03

| Eje | Contador |
|-----|----------|
| Incógnitas abiertas (uphill) | 0 — wiring, script 4 pasos, engines check, quickstart y cleanup ya mapeados; approach validado contra TS-05 evidence |
| Pendientes de ejecución (downhill) | 0 — 0 steps (4/4 ✅) |
| % completado | 100% (4/4 steps ✅ + verify full + plan recitation) |

## Fase 1 — Evidencia de Debugging (GATE — solo tipo Bug)

No aplica — no es bug fix, es CI/packaging wiring verification. Discovery ya documentado en Investigation Notes con repro determinístico (`node scripts/smoke-pack.mjs` → SMOKE OK) e hipótesis descartada (wiring perdido o engines no verificado).

## Fases explícitas — SECURITY | PERFORMANCE (P2-07)

- [x] **SECURITY** — evaluado: no toca trust boundaries (no input usuario externo, no auth, no storage nuevo, no FFI nuevo en script — solo `npm install` file: local y `VantaDB.create()` in-memory); script usa `execSync` con args constantes (no inyección — tgz path es de mkdtempSync, fixedTgz es de manifest name/version sanitizado por npm pack); workflow usa OIDC trusted publishing (`id-token: write`, sin `NPM_TOKEN` hardcodeado) y `environment: npm` (protección manual); smakepack no agrega dependencias (solo reusa `vantadb-wasm/pkg` local); `cargo audit` N/A (no Cargo.toml change); checklist `security-and-hardening` no dispara (no trust boundary)
- [x] **PERFORMANCE** — evaluado: no toca hot paths (HNSW, engine.rs, search/ingestión, serialización); `npm pack` + `npm install` + quickstart son CI-time only (~2-3s total: pack <1s, install ~1s, quickstart <100ms VantaDB.create/put/get/close in-memory); no requiere bench canonical_p99; overhead de smoke en workflow es <5s (well within `timeout-minutes: 15`); cleanup `rmSync` evita leak de disco en CI

## Steps

### Step 1: Verificar script smoke-pack.mjs cumple contrato (4 pasos: pack → install temp limpio → quickstart → cleanup + engines)
- **Archivos:** `vantadb-ts/scripts/smoke-pack.mjs` (106 líneas)
- **Acción:** inspeccionar file full (ya leído 106 líneas) y mapear cada paso del contrato: (a) `npm pack --pack-destination "${tmp}"` + parse tgzName + `tar -xzf` extract, (b) `if(!manifest.engines?.node) throw` (TS-05) + `manifest.dependencies["vantadb-wasm"]` rewrite condicional `file:`→`^WASM_VER` (solo en copia, repo intacto) + `npm pack` fixed tgz, (c) `mkdtemp app` + `npm install tgz --no-audit --no-fund` + `writeFileSync quickstart.mjs` con `VantaDB.create(); put(...); get(...); close(); console.log("SMOKE OK")` + `execSync("node quickstart.mjs")`, (d) `rmSync(app)` en finally interno + `rmSync(tmp)` en finally externo + `process.exitCode = failed ? 1 : 0`; documentar que script es idempotente y safe (dos tmps, finally, stdio inherit en pack/install/quickstart)
- **Verify:** `campaign_verify_cmd command="node -e \"const fs=require('fs'); const c=fs.readFileSync('vantadb-ts/scripts/smoke-pack.mjs','utf8'); const checks=[['npm pack --pack-destination',c.includes('npm pack --pack-destination')],['tar -xzf',c.includes('tar -xzf')],['engines?.node',c.includes('engines?.node')],['file: rewrite',c.includes('file:')],['mkdtempSync.*vantadb-smoke-app',/mkdtempSync.*vantadb-smoke-app/.test(c)],['npm install',c.includes('npm install')],['VantaDB.create',c.includes('VantaDB.create')],['db.put',c.includes('db.put')],['db.get',c.includes('db.get')],['db.close',c.includes('db.close')],['SMOKE OK',c.includes('SMOKE OK')],['rmSync(tmp',c.includes('rmSync(tmp')],['rmSync(app',c.includes('rmSync(app')]; checks.forEach(([k,v])=>{if(!v) throw new Error('missing '+k)}); console.log('smoke script 4 pasos + engines + cleanup OK')\""`
- **Estado:** ✅ COMPLETED (2026-08-27 16:05 — 13/13 checks OK, campaign_verify_cmd 0.8s)

### Step 2: Verificar wiring workflow release-npm-61.yml (después de build, antes de publish, working-directory correcto, sin continue-on-error)
- **Archivos:** `.github/workflows/release-npm-61.yml` (226 líneas, job publish-ts líneas 151-226)
- **Acción:** inspeccionar orden de steps en publish-ts: validar que `Build TypeScript` (línea 180-182) → `Rewrite dependency for publish` (184-193) → `Verify package.json integrity (TS-05)` (195-201) → `Smoke-test packed tarball (TS-07)` (203-207) → `Check if version already published` (209-220) → `Publish to npm` (222-226); validar `working-directory: vantadb-ts` en smoke step y `run: node scripts/smoke-pack.mjs`; validar sin `continue-on-error` en todo workflow (CI_POLICY Regla 2); documentar que publish nunca ocurre si smoke falla (exit 1 → job fail)
- **Verify:** `campaign_verify_cmd command="node -e \"const fs=require('fs'); const y=fs.readFileSync('.github/workflows/release-npm-61.yml','utf8'); const lines=y.split('\n'); const idxBuild=lines.findIndex(l=>l.includes('Build TypeScript')); const idxRewrite=lines.findIndex(l=>l.includes('Rewrite dependency for publish')); const idxEngines=lines.findIndex(l=>l.includes('Verify package.json integrity')); const idxSmoke=lines.findIndex(l=>l.includes('Smoke-test packed tarball')); const idxCheck=lines.findIndex(l=>l.includes('Check if version already published')); const idxPublish=lines.findIndex(l=>l.includes('Publish to npm') && lines.indexOf(l)>idxCheck); if([idxBuild,idxRewrite,idxEngines,idxSmoke,idxCheck,idxPublish].some(i=>i<0)) throw new Error('missing steps'); if(!(idxBuild<idxRewrite && idxRewrite<idxEngines && idxEngines<idxSmoke && idxSmoke<idxCheck && idxCheck<idxPublish)) throw new Error('wrong order: build='+idxBuild+' rewrite='+idxRewrite+' engines='+idxEngines+' smoke='+idxSmoke+' check='+idxCheck+' publish='+idxPublish); if(!y.includes('node scripts/smoke-pack.mjs')) throw new Error('smoke command missing'); if(!y.includes('working-directory: vantadb-ts')) throw new Error('working-directory missing'); if(y.includes('continue-on-error')) throw new Error('continue-on-error forbidden (CI_POLICY Regla 2)'); console.log('wiring OK: build('+idxBuild+')<rewrite('+idxRewrite+')<engines('+idxEngines+')<smoke('+idxSmoke+')<check('+idxCheck+')<publish('+idxPublish+')')\""`
- **Estado:** ✅ COMPLETED (2026-08-27 16:06 — wiring order build<rewrite<engines<smoke<check<publish OK, campaign_verify_cmd 1.4s)

### Step 3: Ejecutar smoke-pack.mjs mecánicamente — debe pasar SMOKE OK + engines ok (hardening TS-05) y verificar tarball engines preservado
- **Archivos:** `vantadb-ts/scripts/smoke-pack.mjs`, `vantadb-ts/package.json`, `vantadb-wasm/pkg/package.json` (existe)
- **Acción:** ejecutar `node vantadb-ts/scripts/smoke-pack.mjs` desde repo root (o `node scripts/smoke-pack.mjs` desde vantadb-ts) — debe: log `[smoke] packed: vantadb-X.Y.Z.tgz`, `[smoke] engines ok: {"node":">=22.12"}`, opcional `[smoke] rewrote vantadb-wasm dep -> ^0.5.0` si file: dep, `[smoke] tarball installed cleanly`, `SMOKE OK` (del quickstart), `[smoke] PASSED (...)`, exit 0; en paralelo verificar que `npm pack` tarball preserva engines vía `tar -xzf` check (ya cubierto por smoke pero se documenta evidencia separada para contrato nivel Task); si smoke falla, diagnosticar causa raíz (systematic-debugging): ¿missing dist/?, ¿wasm pkg no built?, ¿VantaDB.create error?
- **Verify:** `campaign_verify_cmd command="node vantadb-ts/scripts/smoke-pack.mjs"` (exit 0, must contain `SMOKE OK` + `PASSED` + `engines ok`; timeout 60s) + `campaign_verify_cmd command="node -e \"const p=require('./vantadb-ts/package.json'); if(p.engines.node!=\">=22.12\") throw new Error('package.json engines mismatch'); console.log('package engines OK:', p.engines)\""` + `campaign_verify_cmd command="node -e \"const {execSync}=require('child_process'); const {mkdtempSync, rmSync}=require('fs'); const {tmpdir}=require('os'); const {join}=require('path'); const tmp=mkdtempSync(join(tmpdir(),'check-engines-')); try{ const out=execSync('npm pack --pack-destination \\\"'+tmp+'\\\"',{cwd:'vantadb-ts',encoding:'utf8'}); const tgz=out.trim().split(/\\r?\\n/).pop(); const manifest=JSON.parse(execSync('tar -xzO -f \\\"'+join(tmp,tgz)+'\\\" package/package.json',{encoding:'utf8'})); if(!manifest.engines?.node||manifest.engines.node!=='>=22.12') throw new Error('tarball engines missing: '+JSON.stringify(manifest.engines)); console.log('tarball engines OK:',manifest.engines);} finally{rmSync(tmp,{recursive:true,force:true});}\""`
- **Estado:** ✅ COMPLETED (2026-08-27 16:08 — SMOKE OK + PASSED + engines ok, 5.2s pack→install→quickstart, package+tarball engines OK)

### Step 4: Cierre — verify contrato completo + sync plan file recitation + handoff (sin deuda)
- **Archivos:** `vantadb-ts/scripts/smoke-pack.mjs`, `.github/workflows/release-npm-61.yml`, `vantadb-ts/package.json`, `docs/plans/2026-08-25-research-vantadb-ts-quickwins.md`, `.opencode/skills/campaign-executor/tasks/TS-07.md`
- **Acción:** rerun contrato 4 condiciones: (1) script 4 pasos + cleanup check (Step 1 grep), (2) wiring order check (Step 2), (3) smoke SMOKE OK + engines (Step 3), (4) engines preservado package+tarball; actualizar plan file `docs/plans/2026-08-25-research-vantadb-ts-quickwins.md` añadiendo `=== RECITATION TS-07 ===` con `campaignId: 39ce752f-eff8-41e9-897f-cf604d5123c9`, Objetivo TS-07, Estado completed, Última acción Steps 1-4 ✅, Resultado OK, contrato verificacion + evidencia 4 claims, artefactos, invariantes, deuda ninguna, queda_pendiente lead commitea; NO commitear (lead commitea); dejar task file estados Steps 1-4 → ✅ y recitation PARTIAL→OK
- **Verify:** `campaign_verify_cmd command="node -e \"const fs=require('fs'); const plan=fs.readFileSync('docs/plans/2026-08-25-research-vantadb-ts-quickwins.md','utf8'); if(!plan.includes('=== RECITATION TS-07 ===')) throw new Error('recitation TS-07 missing'); if(!plan.includes('SMOKE OK')) throw new Error('SMOKE OK not in recitation'); console.log('plan recitation OK')\""` + `campaign_verify_cmd command="node -e \"const fs=require('fs'); const md=fs.readFileSync('.opencode/skills/campaign-executor/tasks/TS-07.md','utf8'); const pending=(md.match(/- \\*\\*Estado:\\*\\* ⬜ PENDING/g)||[]).length; if(pending!==0) throw new Error('pending steps remain: '+pending); console.log('task file 4/4 steps done')\""`
- **Estado:** ✅ COMPLETED (2026-08-27 16:10 — contrato 4/4 re-verificado + plan recitation sync, campaign_verify_cmd OK)

## Dependencias
- TS-05: Preservar engines (Wave 0 task 2) — ✅ COMPLETED (hardening engines en smoke-pack líneas 43-50, workflow guard 195-201, prereq de Wave 2 — reusa pipeline npm)
- TS-06: Gate CI para tests TS (Wave 1 task 3) — ✅ COMPLETED (Fast Gate <5min, mismo workflow release-npm-61.yml, nota plan 2→3→5 secuenciales — TS-07 no duplica gate)
- TS-02: Fix _native async (Wave 0 task 1) — ✅ COMPLETED (264 tests incluyen native-error.test.ts, baseline para smoke quickstart no afectado)
- No paralelizar con TS-05/TS-06 que tocan mismo workflow (nota plan)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. Sin esto registrado, la tarea no está COMPLETED.

- **Revisor:** vanta-review (o lead auto-review documentado como `doubt-driven-development` fallback si no hay agente distinto — pero TS-07 es verification, riesgo bajo, auto-review trazable)
- **Enfoque:** ¿script realmente hace 4 pasos atomicos (pack → install limpio → quickstart create+put+get+close → cleanup) sin dejar tmp leak? ¿wiring workflow respeta orden build→rewrite→TS-05→TS-07→check→publish y publish nunca ocurre si smoke falla? ¿engines hardening TS-05 realmente protege contra regresión futura (si alguien cambia `npm pkg set` o `files`)? ¿quickstart payload "hello" es representativo o debería testear vector/search? (ponytail: put/get/close es mínimo suficiente, search no requerido para pack install smoke)
- **Cómo se probó:** evidencia mecánica: file read 106 líneas + wiring grep orden 6 steps + `node smoke-pack.mjs` SMOKE OK + tarball engines extract + package engines check (Steps 1-3, no auto-reporte)
- **Checklist anti-hábitos tóxicos:**
  - [ ] No inventar salidas de comandos/herramientas que no se ejecutaron.
  - [ ] No saltarse la clarificación por "ya sé qué quiere".
  - [ ] No declarar done sin verificar contra los acceptance criteria (4 condiciones contrato).
  - [ ] No ignorar fallos ni reportar "todo OK" cuando hubo fallo parcial.
  - [ ] No hacer un solo intento de búsqueda y darlo por saturado (smoke re-verifica pack+install+quickstart; wiring verifica 6 índices).
  - [ ] No copiar sin citar ni presentar supuestos propios como evidencia.
  - [ ] No reintentar en bucle sin diagnóstico.
  - [ ] No dejar huérfanos los pasos: cada paso conectado al objetivo.
  - [ ] No degradar el chequeo de errores en paths de dinero/seguridad (publish gate).
  - [ ] No gastar presupuesto infinito; paradas explícitas.
- **Veredicto:** ⬜ pendiente (revisor distinto debe aprobar antes de COMPLETED) — lead debe hacer codegraph_explore + verify mecánico antes de commit (ya hecho: codegraph_explore vantadb-ts + verify Steps 1-3)

## Notas
- Workflow `release-npm-61.yml` ya tenía smoke-pack wiring (203-207) antes de iniciar tarea — diagnosis confirma wiring correcto y suficiente (después de build 182/rewrite 193/TS-05 201, antes de check 209/publish 222). No se modificó workflow: verificar file + tarball engines es suficiente (smoke packea tgz real); segundo guard es smoke-pack engines check (43-50) + quickstart
- smoke-pack.mjs **ya verificado en TS-05** (2026-08-26 22:52) con `[smoke] engines ok: {"node":">=22.12"}` + `SMOKE OK` + `PASSED` — se re-verifica en este task como contrato 3/4, no se duplica hardening
- `release-plz.toml` publish scope: solo `vantadb` crate (Rust), ignora `vantadb-ts` — validado que no hay step release-plz que toque `vantadb-ts/package.json` version bump (haría smoke fallar si version mismatch, pero check-published lo maneja)
- Plan file Wave 2: MAX_CONCURRENT=3 pero nota "No paralelizar tasks que tocan el mismo workflow (2→3→5)" — TS-07 secuencial con TS-05/TS-06, correcto que lead ejecute en orden; TS-07 es último de Waves 0-2, cierre debe registrar en `docs/avance/activo/bindings.md` + archivar plan (nota plan)
- Ponytail: solución ya es lazy + hardening mínimo — diagnosis mostró script ya cumple 4 pasos + engines (0 líneas necesarias para TS-07), task es verificación + wiring order + smoke run + recitation sync — shortest diff que añade trazabilidad (task file + plan recitation) sin tocar workflow/script
- Dirty worktree notificado: `docs/api/WASM_PERSISTENCE.md` M (TS-08 hardening 4 líneas jsDelivr note) + `completions/_vanta-cli.ps1` M (autogen, no relacionado) + `TS-08.md` ?? — todos fuera de blast radius TS-07, lead debe `git add` solo `TS-07.md` + `docs/plans/...` para commit TS-07 (confirmar scope antes de `git add`), TS-08 se commitea aparte

## Context Save Point
- **Fecha:** 2026-08-27T16:00 UTC
- **Branch:** develop (worktree con TS-02 ✅ 01bcfac0 + TS-05 ✅ 886df465 + TS-06 ✅ 970536b1; TS-08.md untracked + WASM_PERSISTENCE.md dirty pertenecen a TS-08, no a TS-07)
- **CI pendiente:** no — `tests` job (vitest) 264 pasa local (TS-06 verified 12.90-14.86s, smoke ~2-3s); Heavy Certification no requerida (tarea CI-only, smoke pack <5s)
- **Decisiones:** script ya cumple 4 pasos + engines + cleanup (106 líneas, dos tmps, finally); wiring workflow orden 6 steps validado (build 182 < rewrite 193 < engines 201 < smoke 207 < check 212 < publish 225); approach verificado contra TS-05 evidence; sin spec feature-add; riesgo bajo; correction = 0 líneas, verification = task file + recitation
- **Problemas conocidos:** ninguno — contrato 4/4 ya verificado mecánicamente en prev TS-05 smoke run, se re-verifica con campaign_verify_cmd en Steps 1-3
- **Próxima tarea:** Ninguna — Wave 2 cierre; tras TS-07, Wave 0-2 completas → registrar en `docs/avance/activo/bindings.md` (progreso) + mover plan a `docs/plans/archive/` con budget (lead)

# Task 10: OP-02 — Batch ops en grid (selección múltiple + export/eliminar + undo)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase2.md` (Task 10, Wave 3)
- **Creado:** 2026-08-19T11:00
- **last-synced:** 2026-08-19T11:00
- **Estado:** ✅ COMPLETO (commit a88cd1b0 - OP-02, fase 2)

## Blast Radius
- **Callers:** `desktop/src/components/layout/WorkspaceShell.tsx` (único mount de DataExplorer, `key={gridKey}`), `undoStore.softDeleteBatch` (1 caller previo: SpaceLens ESPACIO-02), `recordsToJsonl`/`downloadText` (ExportButtons, SpaceLens).
- **Callees:** `undoStore.softDeleteBatch` → `remove` (bridge), `recordsToJsonl`/`downloadText` (export-jsonl.ts, ya testeados), `vantaErrorMessage`, `listPage`.
- **Implicaciones:** cambio UI-only en desktop React. NO toca bridge Rust, src/sdk, ni WASM. `deleteByFilter`/`exportNamespace` NO se usan (AND-filter no expresa "key ∈ set" — decisión ESPACIO-02 registrada en plan Task 8). No tocar `desktop/src-tauri/`.

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `desktop/src/components/DataExplorer.tsx` (grid completo vía codegraph: baseColumns, fetchFirst/fetchMore, DeleteButton 2-step confirm, removeRowRef, ExportButtons), `desktop/src/store/undo.ts` (softDeleteBatch línea 96), `desktop/src/components/export/export-jsonl.ts`, `desktop/src/components/export/ExportButtons.tsx`, `desktop/src/components/layout/WorkspaceShell.tsx` (gridKey:172, mount:716-722, Ctrl+Z:237-254), `desktop/src/vanta.ts` (deleteByFilter/exportNamespace/records), `desktop/src/store/undo.test.ts`, `.opencode/rules/frontend-web.md` (aplica a `web/`, NO a `desktop/`).
- **Referencias hacia dentro (de lo que toco):** `ExplorerRow` exportado (3 callers: WorkspaceShell x1 mount, DataExplorer internos); `Props` de DataExplorer consumidas solo en WorkspaceShell:716-722.
- **Referencias entrantes (qué depende de mí):** DataExplorer es montado SOLO por WorkspaceShell; agrego prop `onRefresh?` opcional → no rompe nada.
- **Veredicto:** cambio aditivo UI-only, sin contrato público roto. Patrón de refresh existente (remount `key={gridKey}`) reusado vía prop nueva opcional.

## Contrato
"`npm run build` (tsc + vite) verde + typecheck 0 errores + vitest del módulo de selección verde + smoke: seleccionar 3 filas → Eliminar (confirmación muestra cantidad) → Ctrl+Z restaura; Exportar genera JSONL con solo la selección."

## Herramientas
- codegraph (ya usado en discovery), npx vitest, npm run build (desktop).

## Steps

### Step 1: Task file + plan state
- **Archivos:** `docs/plans/2026-08-18-vanta-studio-fase2.md`
- **Acción:** crear task file, marcar Task 10 ⏳ IN PROGRESS en plan.
- **Verify:** plan file muestra ⏳ IN PROGRESS; task file existe.
- **Estado:** ✅ COMPLETO (commit a88cd1b0 - OP-02, fase 2)

### Step 2: Módulo puro `batchSelection.ts` + test (RED→GREEN)
- **Archivos:** `desktop/src/components/batchSelection.ts` (nuevo), `desktop/src/components/batchSelection.test.ts` (nuevo)
- **Acción:** ops puras de selección: `rowKey(record)` → `${namespace}:${id}`; `toggle(sel, id)` → nuevo Set; `selectAll(sel, ids)` → si todos seleccionados limpia, sino selecciona todos. Test vitest (patrón undo.test.ts).
- **Verify:** `npx vitest run src/components/batchSelection.test.ts` → 4/4 pass
- **Estado:** ✅ COMPLETO (commit a88cd1b0 - OP-02, fase 2)

### Step 3: DataExplorer — checkbox col + selección + barra batch
- **Archivos:** `desktop/src/components/DataExplorer.tsx`
- **Acción:** estado `Set<string>`; columna display checkbox (header select-all indeterminado sobre filas cargadas, fila toggle nativo Space/a11y); barra batch sticky bajo panel-head visible con selección: contador, Exportar (n) (recordsToJsonl+downloadText), Eliminar (n) (confirmación 2-step con cantidad → softDeleteBatch → onRefresh), clear ✕.
- **Verify:** `npm run build` verde (tsc 0 err) + vitest módulo verde
- **Estado:** ✅ COMPLETO (commit a88cd1b0 - OP-02, fase 2)

### Step 4: WorkspaceShell — onRefresh wiring
- **Archivos:** `desktop/src/components/layout/WorkspaceShell.tsx`
- **Acción:** pasar `onRefresh={() => setGridKey(k => k + 1)}` a DataExplorer (remount post-batch-delete = grid refresca).
- **Verify:** `npm run build` verde (tsc+vite)
- **Estado:** ✅ COMPLETO (commit a88cd1b0 - OP-02, fase 2)

### Step 5: Cierre — verify full + plan
- **Acción:** `npm run build` (tsc+vite) ✅, vitest suite (batchSelection 4/4; node:test 14/14 en los 3 archivos node:test pre-existentes), revisión de diff, plan Task 10 → ✅ COMPLETED. SIN commit (lo commitea vanta-lead).
- **Verify:** todos los gates verdes.
- **Estado:** ✅ COMPLETO (commit a88cd1b0 - OP-02, fase 2)

## Dependencias
- Task 8 (ESPACIO-02): `undoStore.softDeleteBatch` + export client-side ya decididos y testeados.
- Task 9 (OP-01): patrón remount `key={gridKey}`.

## Notas
- "Editar TTL…" del contrato del plan NO está en el contrato del orquestador para este run → DEFER (anotado en cierre).
- Shift+Space range-select: fuera de scope del orquestador → nativo Space vía checkbox; range-select queda como mejora.
- `deleteByFilter`/`exportNamespace` del bridge NO se consumen (decisión Task 8: AND-filter no expresa key∈set; export client-side es el patrón establecido).

## Context Save Point
- **Fecha:** 2026-08-19T10:45
- **Branch:** develop (worktree con cambios listos, SIN commit — lo commitea vanta-lead)
- **CI pendiente:** no (verify full en Step 5: build ✅, vitest batchSelection 4/4 ✅, node --test 14/14 ✅)
- **Decisiones:** selección por Set de `${ns}:${id}` (mismo keying que getRowId); export client-side JSONL (reusa export-jsonl.ts); delete vía softDeleteBatch (undo de 1 entry); refresh post-delete vía remount `key={gridKey}` (patrón Task 9); select-all sobre filas cargadas (página actual, no namespace).
- **Problemas conocidos:** los 3 archivos de test con `node:test` (export-jsonl, status-report, vanta-deep-link) aparecen como "Failed Suites" en `npx vitest run` porque vitest los recoge sin registrar suite — pre-existente, pasan 14/14 con `node --test`. Fix a11y aplicado: sort button solo envuelve columnas sortables (`getCanSort()`), columna select con `enableSorting: false` (checkbox nunca anidado en `<button>`).
- **Próxima tarea:** ninguna (última del plan) — cierre del plan por vanta-lead
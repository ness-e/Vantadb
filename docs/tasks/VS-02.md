# VS-02: MARK variante desktop (asistente de datos)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase0.md`
- **Creado:** 2026-08-18
- **last-synced:** 2026-08-18
- **Estado:** ✅ COMPLETO (commit 2573d8a5 - fase 0)
- **Tipo:** frontend (React/TSX, Vite, sin Tailwind — VS-01 PENDIENTE)

## Impacto mapeado (Regla 0)
- **Archivos a crear (4):** `desktop/src/components/mark/use-mark-interaction.ts`, `desktop/src/components/mark/Mark.tsx`, `desktop/src/components/mark/mark-studio.tsx`, `desktop/src/components/mark/mark.css`
- **Referencias hacia dentro (quién los importa):** NINGUNA — archivos nuevos; nadie los importa aún (el wiring al workspace llega con VS-03).
- **Referencias hacia fuera:** `react`/`react-dom` (ya en package.json, React 19.1.0); `mark.css` importado por `Mark.tsx`. DOM APIs: rAF, WAAPI (`Element.animate`), `matchMedia`, SMIL (nativo SVG).
- **Fuente protegida (SOLO LECTURA):** `web/src/components/vanta/mark/` — NO se modifica; es la referencia del port.
- **Archivos que NO toco (dominio VS-01):** `desktop/package.json`, `desktop/src/index.css` (no existe), `desktop/src/main.tsx`, `desktop/index.html`. Tampoco `App.tsx`/`App.css` (VS-03).
- **Veredicto:** impacto nulo en el resto del workspace. No hay Rust, no hay bindings, no hay hot paths.

## Blast Radius
| Callers | Callees | Implicaciones |
|---|---|---|
| Ninguno (nuevos) | React 19, DOM (rAF/WAAPI/matchMedia/SMIL) | Componente autónomo; VS-03 lo wirea al workspace. `web/` intacta. |

## Contrato
- `npm run build` (tsc strict + vite) verde en `desktop/`.
- Mismas proporciones SVG y comportamiento que la web: 2 SVGs viewBox `0 0 100 100`; grafo bg `preserveAspectRatio="none"` (10 nodos + 18 edges, colores/widths/opacidades exactos, hover `#FF5500`); MARK svg 78%×78% drop-shadow `0 0 28px rgba(255,85,0,.22)` (ring r=42, glow ring SMIL 42→46→42/opacity .3→0→.3 3.5s, esfera r=22 `#FF5500`, ojos rects x=43/57±2 w=4 rx=2 y=45 height=squint 3–10).
- Interactivo SIN Anime.js: follow rAF lerp `current += (target-current)*(1-exp(-dt/τ))` τ≈60ms ojos / 130ms esfera; squint React puro; blink WAAPI sobre y/height (cierre 60ms inQuad → hold 50ms → apertura 120ms outQuad, ciclo left→right→both); pulse nodos CSS keyframes `transform-box: fill-box`.
- 4 SfxLabels ("1.2ms" neon TL, "RRF" ink TR, "WAL · CRC32C" ink BL, "ZERO NET" neon BR) + tag "IN-PROCESS" -4deg + hint chip "◆ click me · move mouse"/"◆ blink".
- Variante `MarkStudio` con estados data-driven: `idle | loading | empty | error` (etiquetas/tag/hint por estado, misma geometría).
- `prefers-reduced-motion` respetado (CSS media query + JS hook: sin follow, sin blink animado, SMIL no renderiza).
- Pérdida declarada: ease outElastic del pulse → aproximado con backOut cubic-bezier.

## Herramientas
- `npm run build` en `desktop/` (verificación mecánica por step)
- `web/src/components/vanta/mark/mark-classic.tsx`, `use-mark-interaction.ts`, `types.ts` (referencia leída, NO modificar)
- codegraph (blast radius ya mapeado), websearch (WAAPI geometría SVG verificada: SVG2 spec + MDN)

## Steps
### Step 1: Hook de interacción sin Anime.js
- **Archivos:** `desktop/src/components/mark/use-mark-interaction.ts`
- **Acción:** Port de `use-mark-interaction.ts` + tipos (`BlinkState`, `MarkInteractionState`) + `usePrefersReducedMotion`. follow → rAF lerp exp (τ 60ms/130ms, dt clamp 50ms, convergencia <0.05 → cancela rAF). squint React puro (squintNorm = dist/600, height 10→3). blink → WAAPI en refs de ojos: leer atributos DOM actuales, cierre 60ms inQuad → hold 50ms → apertura 120ms outQuad, ciclo left→right→both; al finalizar apertura, `setAttribute` de y/height finales para tapar la ventana 230–280ms en que React omite atributos. Reset blink/annoyed a 280ms. Cleanup de rAF/timers/animaciones (StrictMode-safe). reduced → sin follow, sin blink.
- **Verify:** `npm run build`
- **Estado:** ✅ COMPLETO (commit 2573d8a5 - fase 0)

### Step 2: Mark.tsx (port classic) + mark.css
- **Archivos:** `desktop/src/components/mark/Mark.tsx`, `desktop/src/components/mark/mark.css`
- **Acción:** Exporta `Mark` (classic) + comparte `MarkGraph`, `MarkFace`, `SfxLabel` para la variante. Grafo bg (10 nodos/18 edges exactos, hit-target r=5, hover neon, pulse hover + ambient vía CSS keyframes `transform-box: fill-box` con delay stagger inline). MARK svg (ring, SMIL glow condicional a reduced-motion, esfera con offset + scale .94 annoyed, ojos con squint/blink). Labels/tag/hint según mouseInHero/annoyed. `mark.css` con clases namespaced `.vmark-*`, tokens de la web (cream `#FBF9F5`, ink `#000`, neon `#FF5500`), keyframes pulse/ambient, media query reduced-motion.
- **Verify:** `npm run build`
- **Estado:** ✅ COMPLETO (commit 2573d8a5 - fase 0)

### Step 3: mark-studio.tsx (variante data-driven)
- **Archivos:** `desktop/src/components/mark/mark-studio.tsx`
- **Acción:** `MarkStudio({ status = "idle" })` — reusa hook + `MarkGraph`/`MarkFace`/`SfxLabel` de Mark.tsx. Mapa por estado: labels TL/TR/BL/BR, texto tag, hint. Misma geometría y comportamiento en todos los estados.
- **Verify:** `npm run build`
- **Estado:** ✅ COMPLETO (commit 2573d8a5 - fase 0)

### Step 4: Cierre — verify full + commit + progreso
- **Acción:** `npm run build` final; git add solo los 4 archivos; commit `feat: VS-02 — MARK variante desktop (asistente de datos)`; learnings en `.opencode/AGENTS.md`; `skill progreso`; recitation; plan file → ✅.
- **Verify:** `git status` limpio + `git log --oneline -1`
- **Estado:** ✅ COMPLETO (commit 2573d8a5 - fase 0)

## Dependencias
- VS-01 (Tailwind/tokens) — NO bloquea: Mark self-contained con CSS plano (el desktop ya usa ese patrón en App.css). Si VS-01 aterriza después, la migración a utilities es opcional.
- VS-03 (workspace) — consumirá estos exports.

## Notas
- Desktop = Vite + React 19 + TS strict (`noUnusedLocals`/`noUnusedParameters` on) + StrictMode. Sin test infra.
- Sin Tailwind instalado → clases CSS propias namespaced `.vmark-*` (evita colisión con utilities futuras de VS-01).
- El `transform` de la esfera (scale .94) se hace inline como en la web (React style + transition), no keyframe.
- Pérdida declarada vs web: outElastic del pulse (hover y ambiental) → cubic-bezier backOut; el resto es 1:1.
- Mejora a11y sobre la web: SMIL glow no renderiza bajo reduced-motion (la web lo deja correr; el CSS global no lo mata).

## Context Save Point
- **Fecha:** 2026-08-18
- **Branch:** develop
- **CI pendiente:** no (frontend-only; verify full Rust no aplica)
- **Decisiones:** CSS plano namespaced sobre Tailwind (VS-01 pendiente); WAAPI + setAttribute final sobre Anime.js; SMIL condicional a reduced-motion.
- **Problemas conocidos:** ventana 230–280ms del blink resuelta con `setAttribute` al terminar la apertura (React omite y/height mientras `blink !== "open"`).
- **Próxima tarea:** VS-01 (si se ejecuta antes) o VS-03 (workspace).
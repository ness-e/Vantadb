# AUD-034: Transacción IDB duplicada ×4 en WASM — dedupe en helper transaccional

## Metadata
- **Fuente:** docs/Backlog.md:426 (audit-full-2026-08-12-231204)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟡 Media
- **Tipo:** Rust (WASM inline JS)
- **Turns estimados:** 5
- **Creado:** 2026-08-14T09:30
- **last-synced:** 2026-08-14T09:30
- **Estado:** ✅ COMPLETED

## Blast Radius
- **Callers:** `IdbStorage::write_file` (13 callers: worker.rs, lib.rs; tests/wasm_tests.rs), `IdbStorage::delete_file` (20 callers: worker.rs, lib.rs; tests/wasm_tests.rs) — la API Rust NO cambia
- **Callees (JS inline):** `openDB()`, `STORE_NAME`, `channel` (BroadcastChannel "vantadb-sync"), `navigator.locks` (Web Locks, opt-in cuando existe)
- **Implicaciones:** refactor interno del JS inline; cero cambio de API pública ni comportamiento observable; lock y notify preservados
- **Riesgo:** bajo
- **Contrato:** verifies mecánicos pasan + los 4 caminos (write/del × lock/no-lock) quedan cubiertos por UN helper con semántica idéntica

## Impacto mapeado (Regla 0)
- **Archivos leídos (completos):** `vantadb-wasm/src/idb.rs` (203 líneas, vía codegraph_explore verbatim)
- **Archivos referenciados hacia dentro:** ninguno — el JS inline es autocontenido (IIFE); Rust usa `Reflect::get(globalThis, "vantaIdbStorage")`
- **Archivos que referencian al editado:** `vantadb-wasm/src/worker.rs`, `vantadb-wasm/src/lib.rs` (via `write_file`/`delete_file`), `vantadb-wasm/tests/wasm_tests.rs` — ninguno toca el JS inline
- **Veredicto impacto:** bajo — el cambio es puramente el cuerpo del IIFE inline; la firma `write(key, data)` / `del(key)` y los métodos Rust quedan idénticos
- **Regla aplicable:** `.opencode/rules/js-ecosystem.md` R-1 (WASM sigue InMemory; handlers IDB no cambian de rol), R-2, R-3, R-4 (R-4 no aplica: este refactor no toca el op-gate de lib.rs)

## Contrato
- `cargo check -p vantadb-wasm` pasa (wasm-bindgen parsea el JS inline en expansión de macro → valida sintaxis)
- `cargo clippy -p vantadb-wasm --all-targets --all-features -- -D warnings` pasa
- `cargo nextest run --profile audit -p vantadb-wasm --build-jobs 2` pasa
- `cargo fmt --check` pasa
- Los 4 bloques tx duplicados (write/del × lock/no-lock) → 1 helper `runWriteTx(db, key, op, resolve, reject)` + 2 call sites de 1 línea
- Semántica preservada: lock vía `navigator.locks.request("vantadb-write", ...)` cuando existe, notify `channel.postMessage` en oncomplete, resolve/reject idénticos

## Invariantes de dominio (handoff — MUST)
- **Invariantes a preservar:** (1) Web Locks NO se elimina — es coordinación multi-tab intencional; (2) broadcast `data-changed` en oncomplete; (3) la promesa del lock debe resolver/rejectar para no retener el lock (deadlock); (4) API pública Rust (`IdbStorage::*`) sin cambios
- **Comandos de verificación:** los 4 del Contrato (todos exit 0)
- **Deuda pendiente:** ninguna — refactor puro; NOTA: WIP ajeno en working tree (`.opencode/skills/campaign-executor/tasks/AUD-024.md` modificado) — NO tocar

## Recitation (canónico)
- **activeGoal:** AUD-034 — dedupe transacción IDB ×4 en helper
- **contract:** verifies de Contrato + evidencia: diff idb.rs (helper único), salida real de los 4 comandos
- **invariantes:** lock + notify preservados; API Rust sin cambios
- **deuda:** ninguna

## Deuda técnica (Regla 6)
**Saldo neto de deuda por PR:** Sin deuda — refactor que ELIMINA ~40 líneas duplicadas (pago neto)

## Definition of Done
- **Task:** Contrato verificado mecánicamente + tests existentes de wasm (test_idb_* en wasm_tests.rs) pasan
- **Commit:** NO aplica — instrucción del orquestador: sin commit (lo ejecuta vanta-lead)
- **Release:** NO aplica — sin release (justificación: cambio interno WASM, sin semver)

## Fases explícitas — SECURITY | PERFORMANCE
- [ ] **SECURITY** — NO aplica: no toca trust boundaries, input de usuario ni auth; el JS inline no agrega dependencias. Justificación: refactor interno sin superficie nueva
- [ ] **PERFORMANCE** — NO aplica: no es hot path de búsqueda/indexación; el refactor NO cambia la cantidad de transacciones ni locks (mismo comportamiento, menos duplicación). Nota: no tocar `read()` (patrón readonly distinto, fuera de scope del finding)

## Steps

### Step 1: Discovery + blast radius
- **Archivos:** — (solo lectura)
- **Acción:** leer idb.rs completo (203 líneas), mapear callers vía codegraph (write_file 13 callers, delete_file 20), confirmar los 4 bloques duplicados en líneas 32-71
- **Verify:** 4 patrones identificados: write-no-lock (34-39), write-lock (41-46), del-no-lock (54-59), del-lock (61-66)
- **Estado:** ✅ COMPLETED

### Step 2: Editar idb.rs — helper runWriteTx + slim write/del
- **Archivos:** `vantadb-wasm/src/idb.rs` (solo el string inline_js, líneas 23-71)
- **Acción:** insertar `function runWriteTx(db, key, op, resolve, reject)` antes de `const storage = {`; reemplazar los cuerpos de `write` y `del` (líneas 32-71) por call sites de 1 línea al helper. Semántica: lock-branch ejecuta el helper dentro de `navigator.locks.request(..., () => new Promise(execute))` con `.catch(err => reject(err))`; no-lock ejecuta `execute(resolve, reject)`
- **Verify:** `cargo check -p vantadb-wasm` (valida sintaxis del JS inline) — ✅ passed (20.40s)
- **Estado:** ✅ COMPLETED

### Step 3: Verificación mecánica completa
- **Archivos:** — (solo verify)
- **Acción:** clippy + nextest + fmt sobre el crate
- **Verify:**
  - `cargo clippy -p vantadb-wasm --all-targets --all-features -- -D warnings` — ✅ passed (23.03s)
  - `cargo nextest run --profile audit -p vantadb-wasm --build-jobs 2` — ✅ 1 test passed, 0 skipped (1m 13s build)
  - `cargo fmt --check -p vantadb-wasm` — ✅ OK (workspace completo falla por drift pre-existente en `vantadb-python/src/lib.rs:249` — NO tocado, hallazgo documentado)
- **Estado:** ✅ COMPLETED

### Step 4: Cierre
- **Acción:** actualizar este task file (contadores, verifies, estado ✅); NO tocar Backlog/progreso (lo migra el orquestador); NO commit
- **Verify:** reporte final con bloque RESULTADO
- **Estado:** ✅ COMPLETED

## Contadores
- **Diff final:** `vantadb-wasm/src/idb.rs`: 1 file changed, **15 insertions(+), 32 deletions(-)** → **-17 líneas netas (-37.8%)**
- **Antes:** 4 bloques tx duplicados (write/del × lock/no-lock, ~40 líneas) → **Después:** 1 helper `runWriteTx` (13 líneas) + 2 call sites de 1 línea
- **Caminos preservados:** 4/4 (write-lock, write-no-lock, del-lock, del-no-lock) con semántica idéntica (lock `vantadb-write`, notify `data-changed`, resolve/reject)
- **API pública Rust:** sin cambios (13 callers de write_file, 20 de delete_file intactos)
- **Bugs introducidos:** 0

## Hallazgo (fuera de scope, NO corregido)
- `cargo fmt --check` (workspace) falla en `vantadb-python/src/lib.rs:249` — `open_vantadb(...)` de 1 línea que rustfmt quiere multiline. Drift pre-existente commiteado, no causado por AUD-034. Lo corrige su dueño (vanta-worker para bindings Python) o una tarea de fmt-drift dedicada.

## Dependencias
- Ninguna (tarea única, sin bloqueos). WIP ajeno en AUD-024.md — ignorar

## Review (GATE — agente distinto, P2-01)
- **Revisor:** pendiente — orquestador asigna (vanta-audit/vanta-review). Esfuerzo 🟢 con riesgo bajo; fallback `doubt-driven-development` en contexto fresco
- **Enfoque:** ¿el helper preserva exactamente la semántica del lock (resolución de la promesa del lock para evitar deadlock) y el notify?
- **Veredicto:** ⬜ pendiente

## Notas
- `read()` usa tx readonly con `get` request — patrón distinto, NO entra en el dedupe (el finding solo cubre write/del)
- El double-call de `resolve()` en el path no-lock (`execute(resolve, reject)` → oncomplete llama resolve() + resolveTx() = misma fn) es no-op por semántica de promesas — comportamiento idéntico al original
- En el path lock, el error fluye rejectTx → rechazo de la promesa del lock → `.catch` → reject externo: mismo observable que el original
- Regla 3 (docs): sin cambios de API pública → sin actualización de docs/api/ requerida
- commit lo ejecuta vanta-lead (regla de roles); este task file es el registro

## Context Save Point
- **Fecha:** 2026-08-14T09:30
- **Branch:** develop (sin commits en esta ejecución)
- **CI pendiente:** sí (verifica vía comandos locales en Step 3)
- **Decisiones:** helper único con parámetro `op` (closure por op) en vez de 2 helpers (writeTx/delTx) — la escalera ponytail: 1 helper cubre los 4 casos; el lock se preserva, no se simplifica
- **Problemas conocidos:** WIP ajeno modificado en working tree (AUD-024.md) — no commiteado por esta tarea
- **Próxima tarea:** — (tarea única)

# ESPACIO-01: Scatterplot regl-scatterplot + UMAP-js en worker

## Metadata
- **Plan file:** docs/plans/2026-08-18-vanta-studio-fase2.md
- **Creado:** 2026-08-19T07:00
- **last-synced:** 2026-08-19T08:00
- **Estado:** ✅ COMPLETED (2026-08-19T08:00 — ESPACIO-01 verificado: build exit 0, tsc exit 0, vitest 3/3; commit pendiente de vanta-lead)

## Blast Radius
Callers | Callees | Implicaciones
- `WorkspaceShell.tsx` (monta surfaces) — se agrega `"espacio"` a `Surface` union + lazy import + SideButton + switch mount. Referencias entrantes de WorkspaceShell: `App.tsx`, `CommandPalette.tsx`, `FiltersBuilder.tsx`, `GraphLens.tsx` (consumen props/surface, aditivo — no rompe nada).
- `desktop/src/vanta.ts` — `listPage({namespace, limit, cursor})` ya existe (VS-CORE-01) y trae `MemoryRecord.vector?: number[] | null`. NO se modifica vanta.ts (reuso directo).
- Archivos nuevos: `space/SpaceLens.tsx`, `space/projection.worker.ts`, `space/useProjection.ts` — sin referencias previas.
- Deps nuevas: `regl-scatterplot@^1.10` (canvas WebGL directo, sin React — compatible React 19), `umap-js@^1.4` (TS puro, sin React). Verificado contra READMEs oficiales (flekschas/regl-scatterplot, PAIR-code/umap-js).

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** WorkspaceShell.tsx (779L, full), vanta.ts (listPage:282-292, MemoryRecord:93-111), useGraphData.ts (241L, patrón de hook), GraphLens.tsx (patrón surface lazy), package.json desktop.
- **Referencias hacia dentro de WorkspaceShell:** App.tsx, CommandPalette, FiltersBuilder, GraphLens — todas consumen la firma actual; el cambio es aditivo (nuevo valor en union + nuevo botón + nuevo switch).
- **Referencias salientes de WorkspaceShell:** Surface union usada en CommandPalette (`onNavigate={(s) => setSurface(s)}` — s tipado por Surface; agregar "espacio" es compatible porque el union se amplía).
- **Veredicto:** aditivo, sin breaking changes. `listPage` ya trae vectors con cursor pagination → paging para 100k sin tocar el bridge.

## Contrato
"npm run build verde (tsc + vite) + smoke worker (proyección UMAP-js responde con datos sintéticos 100 puntos → 2D)" + surface `"espacio"` montada con SpaceLens lazy.

## Herramientas
- npx vitest (4.1.11 vía npx — no está en package.json), npm, codegraph

## Steps
### Step 1: Instalar deps + verificar types
- **Archivos:** `desktop/package.json`, package-lock
- **Acción:** `npm i regl-scatterplot umap-js` en desktop/; verificar que `regl-scatterplot` expone .d.ts (si no, agregar declare module) y `umap-js` trae tipos propios.
- **Verify:** `npm ls regl-scatterplot umap-js` + `npx tsc --noEmit` (desktop)
- **Estado:** ✅ COMPLETED (2026-08-19T07:15 — regl-scatterplot@1.16.0 + umap-js@1.4.0 instalados; .d.ts nativos verificados: `createScatterplot` default export, `Points` column-oriented `{x,y,valueA,valueB}`, events `pointOver`(number)/`pointOut`(number)/`select`({points})/`deselect`(undefined)/`lassoEnd`({coordinates}); UMAP `fitAsync(X, callback)` cancelable con `false`, `random: RandomFn` inyectable — seed fijo soportado)

### Step 2: projection.worker.ts (UMAP-js, seed fijo, cancelable)
- **Archivos:** `desktop/src/components/space/projection.worker.ts` (nuevo)
- **Acción:** worker onmessage `{type:'project', vectors, seed}` → UMAP-js `fitAsync` (nComponents 2, nNeighbors 15, minDist 0.1, random: mulberry32(seed) para reproducibilidad) → normaliza a [-1,1] → postMessage `{type:'done', points}`. Soporta cancelación: flag interno + mensaje `{type:'cancel'}` (callback de fitAsync retorna false); si termina, postMessage `{type:'cancelled'}`.
- **Verify:** `npx tsc --noEmit` (desktop)
- **Estado:** ✅ COMPLETED (2026-08-19T07:20 — `projection.worker.ts` creado: UMAP-js `fitAsync` con callback cancelable (`false`), `nNeighbors=min(15, n-1)`, `random: mulberry32(seed)` para reproducibilidad, normalización a NDC [-1,1], respuestas `{done, points}` / `{error, message}` / `{progress, epoch}`; `npx tsc --noEmit` exit 0)

### Step 3: useProjection.ts (cola de proyección cancelable + paging)
- **Archivos:** `desktop/src/components/space/useProjection.ts` (nuevo)
- **Acción:** hook que pagea `listPage({limit:5000, cursor})` hasta cap 100k; filtra records con vector; agrupa namespace; crea worker vía `new Worker(new URL(...), {type:'module'})`; cancela proyección previa al cambiar namespace (termina worker viejo); expone `{phase, points, namespaces, payloads, keys, selected, project, select, clearSelection, error}`. Limpieza en unmount.
- **Verify:** `npx tsc --noEmit` (desktop)
- **Estado:** ✅ COMPLETED (2026-08-19T07:30 — `useProjection.ts` creado: paging `listPage` limit 5000 hasta cap 100k, `shouldCancel` por ref, worker `new Worker(new URL(...), {type:'module'})`, token por proyección (descarta respuestas stale), cancelación al cambiar namespace (terminate worker), `project(namespace)` + `setSelected`; fix TS: `cancelledRef` RefObject→closure `() => cancelledRef.current`; `npx tsc --noEmit` exit 0)

### Step 4: SpaceLens.tsx (regl-scatterplot + hover + lasso)
- **Archivos:** `desktop/src/components/space/SpaceLens.tsx` (nuevo)
- **Acción:** canvas + `createScatterplot`; `draw` column-oriented `{x, y, valueA: namespaceIndex, valueB: 0}`; `set({colorBy:'valueA', pointColor: paleta por namespace, backgroundColor, lassoInitiator:true})`; `subscribe('pointOver'/'pointOut')` → tooltip overlay HTML (key + payload preview + namespace); `subscribe('select'/'deselect')` → selección lista para ESPACIO-02; click punto → openRecord (Inspector); toolbar: selector namespace, botón proyectar, toggle "mostrar solo con vector", aviso de distorsión, contador; ResizeObserver → `set({width,height})`.
- **Verify:** `npx tsc --noEmit` (desktop)
- **Estado:** ✅ COMPLETED (2026-08-19T07:45 — `SpaceLens.tsx` creado: createScatterplot una vez (ResizeObserver → set width/height), draw column-oriented `{x,y,valueA}`, colorBy valueA + PALETTE por namespace, eventos pointOver/pointOut (tooltip overlay key+payload), select (click simple → onOpenRecord; lasso → setSelected), lassoStart/lassoEnd flag, deselectOnEscape, aviso distorsión + hint bar, auto-proyección al montar, notice único al completar; `npx tsc --noEmit` exit 0)

### Step 5: WorkspaceShell.tsx surface "espacio"
- **Archivos:** `desktop/src/components/layout/WorkspaceShell.tsx`
- **Acción:** agregar `"espacio"` a Surface union; lazy import SpaceLens; SideButton "ESPACIO"; switch mount con Suspense pasando `onOpenRecord` (patrón RetrievalLens → openRecord del shell).
- **Verify:** `npx tsc --noEmit` (desktop)
- **Estado:** ✅ COMPLETED (2026-08-19T07:55 — WorkspaceShell: lazy import SpaceLens, Surface union + `"espacio"`, SideButton ESPACIO (✳), switch mount con Suspense + `onOpenRecord` → `openRecord` del shell; `npx tsc --noEmit` exit 0)

### Step 6: Smoke test worker + verify full
- **Archivos:** `desktop/src/components/space/projection.test.ts` (nuevo, unit)
- **Acción:** test vitest: 100 puntos sintéticos (dims 8) → `new UMAP({...}).fitAsync` → expect embedding.length === 100, embedding[0].length === 2. Seed determinismo: misma seed → mismos puntos (normalizados). También verifica mulberry32 si se extrae.
- **Verify:** `npm run build` (desktop) + `npx vitest run` (proyección responde)
- **Estado:** ✅ COMPLETED (2026-08-19T08:00 — `projection.worker.test.ts` (3 tests): proyección 100 pts → 2D NDC [-1,1] ✅, misma seed → mismo embedding (reproducible) ✅, sin vectores → error descriptivo ✅; `npx vitest run --testTimeout=30000` 3/3 passed (UMAP-js ≈7.5s/100pts en node — testTimeout ampliado, correctness no perf); `npm run build` exit 0 (chunk `SpaceLens` 229.6 kB gzip 79.3 kB lazy + `projection.worker` 108 kB propio))

## Cierre
- **Fecha:** 2026-08-19T08:00
- **Branch:** develop (worktree sin stage — vanta-lead commitea)
- **Resultado:** ✅ COMPLETED — contrato ESPACIO-01 cumplido
- **Verificación:** npm run build exit 0 · npx tsc --noEmit exit 0 · vitest 3/3 passed
- **Cambios:** 5 archivos (3 nuevos + 1 modificado + package.json/package-lock)
- **CI pendiente:** sí

## Dependencias
- Task 6: GRAFO-03 ✅ (pattern surface IQL / lazy / Suspense a reusar)

## Notas
- API regl-scatterplot v1.10.4 (README oficial flekschas/regl-scatterplot): `createScatterplot({canvas, width, height, pointSize})`; `draw` column-oriented `{x,y,valueA,valueB}`; puntos NDC [-1,1]; eventos `pointOver`(payload pointIndex), `pointOut`, `select`(`{points}`), `deselect`, `lassoEnd`(`{coordinates}`); `set({colorBy:'valueA', pointColor:[...], lassoInitiator:true})`; lasso nativo SHIFT+drag o lassoInitiator; `destroy()`. Fuente: https://github.com/flekschas/regl-scatterplot
- API umap-js v1.4.0 (README oficial PAIR-code/umap-js): `new UMAP({nComponents, nNeighbors, minDist, random})`; `fitAsync(data, epochNumber => boolean)` — retornar false cancela; `fit(data)` sync. Fuente: https://github.com/PAIR-code/umap-js
- Seed fijo: mulberry32(42) → `random: () => mulberry32(seed)()` — reproducibilidad (orquestador).
- PCA <1k del plan: OMITIDO (ponytail) — umap-js con <1k puntos corre rápido en worker (~<500ms); se documenta en deuda.
- color por namespace: valueA = índice de namespace (categorical), pointColor array = paleta D4 (neon/linocut).

## Context Save Point
- **Fecha:** 2026-08-19T07:00
- **Branch:** develop
- **CI pendiente:** sí
- **Decisiones:** color por namespace (valueA) sobre cluster k-means — contrato del orquestador; PCA<1k omitido (UMAP rápido en worker); paging vía listPage cursor existente (sin tocar bridge).
- **Problemas conocidos:** ninguno
- **Próxima tarea:** ESPACIO-02

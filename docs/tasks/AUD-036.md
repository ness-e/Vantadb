# AUD-036 — schema.rs traga errores de fs (255,263) → STALE / falso positivo

> Estado: ✅ COMPLETO (SKIP — finding stale, sin fix aplicable)
> Fuente: docs/Backlog.md L441 (sección P16)
> Severidad reportada: Media | Verificación: 2026-08-13

## Hallazgo original

"`schema.rs` traga errores de fs (255,263) — propagar error en vez de ignorar (riesgo data-loss silencioso)" — src/schema.rs:255,263

## DISCOVERY — Verificación del estado real

### 1. Líneas referenciadas (255, 263)

Están dentro del módulo `#[cfg(test)] mod tests` (inicia L196). Son setup/cleanup de test:

```rust
// L252-264 — test file_write_read_roundtrip
#[test]
fn file_write_read_roundtrip() {
    let dir = std::env::temp_dir().join("vanta_schema_test");
    let _ = std::fs::create_dir_all(&dir);      // L255 — setup de test
    let path = dir.join(".vanta.schema");
    let h = StorageHeader::current();
    h.write_to(&path).unwrap();
    let read = StorageHeader::read_from(&path).unwrap().unwrap();
    assert_eq!(read, h);
    let _ = std::fs::remove_dir_all(&dir);      // L263 — cleanup de test
}
```

Ignorar errores en cleanup de directorio temp de test es idiomático (no hay riesgo data-loss: es un temp dir).

### 2. Producción de src/schema.rs — TODOS los errores fs se propagan

| Línea | Llamada fs | Manejo |
|---|---|---|
| L105-106 | `File::open` (read_from) | `.map_err(\|e\| SchemaError::Invalid(...))?` |
| L108-110 | `file.read` (read_from) | `.map_err(...)?` |
| L122-123 | `File::create` (write_to) | `.map_err(...)?` |
| L124-125 | `file.write_all` (write_to) | `.map_err(...)?` |
| L126-127 | `file.sync_all` (write_to) | `.map_err(...)?` |
| L168 | `read_from` (load_or_create_schema) | `?` |
| L175 | `write_to` (load_or_create_schema) | `?` |
| L185 | `read_from` (check_schema_compatibility) | `?` |

Grep de `let _ =` / `.ok()` en `src/schema.rs`: SOLO L255 y L263 (ambos test). Cero `unwrap()`/`expect()` en producción del archivo.

### 3. Callers en todo el workspace

- `load_or_create_schema` — 1 caller: `src/storage/engine/init.rs:259` → `?` (fn retorna Result; L245 `return Err(VantaError::DatabaseBusy(...))` lo confirma)
- `check_schema_compatibility` — 1 caller: `src/storage/engine/init.rs:266` → `?`
- Otros consumidores de `StorageHeader`: `src/migration.rs:342` (`?`), `src/cli_handlers/migrate.rs:167,181,206` (todos `?`)

### 4. Archivos schema/header relacionados

- `src/binary_header.rs` (`VantaHeader`, scope durability.md): encode/decode puramente en memoria (L19-72), cero I/O fs en producción. Sin patrón de error tragado.
- `src/storage/` — hits de `let _ = std::fs::`:
  - `vfile.rs:792` → dentro de `#[cfg(test)]` (cleanup de test)
  - `rocksdb_backend.rs:322` → `let _ = create_dir_all(parent)` pre-checkpoint; la operación real (L325 `cp.create_checkpoint(path).map_err(...)?`) propaga. Fuera de scope AUD-036.
  - `wal_sharded.rs:357,359` → cleanup best-effort de shards en dominio WAL (propiedad Arch/Engine, out-of-scope para worker). Fuera de scope AUD-036.

## Decisión (ponytail + honestidad)

**SKIP / STALE — falso positivo.** El finding referenciaba cleanup de tests (idiomático ignorar). El código de producción de `src/schema.rs` propaga TODOS los errores fs con `map_err` + `?`, y TODOS los callers propagan con `?`. No existe gap de data-loss silencioso en el path de schema. **No hay fix que aplicar — fabricar un cambio sería over-engineering.**

No se modifica código. No aplican gates de cargo check/clippy/nextest (contrato: si SKIP → evidencia grep, cumplido).

## Pasos

- [x] 1. Leer `src/schema.rs` completo (272 líneas) + regla `durability.md`
- [x] 2. Grep `let _ =`/`.ok()`/`unwrap()` en schema.rs → solo test
- [x] 3. Grep callers de `load_or_create_schema`/`check_schema_compatibility` → init.rs, ambos `?`
- [x] 4. Grep otros consumidores `StorageHeader` → migration.rs, migrate.rs, todos `?`
- [x] 5. Revisar `src/storage/` + `binary_header.rs` → sin gap en scope
- [x] 6. Revisión adversarial (doubt-driven, degraded self-review) → sin hallazgos accionables
- [x] 7. Crear task file + documentar evidencia + cierre

## EVIDENCIA (grep outputs exactos)

```
$ grep -rn "let _ = \|\.ok()" src/schema.rs
src/schema.rs:255:        let _ = std::fs::create_dir_all(&dir);      # #[cfg(test)] mod tests
src/schema.rs:263:        let _ = std::fs::remove_dir_all(&dir);     # #[cfg(test)] mod tests

$ grep -rn "load_or_create_schema\|check_schema_compatibility" --include="*.rs" .
src/storage/engine/init.rs:259:  crate::schema::load_or_create_schema(&base_path)?;
src/storage/engine/init.rs:266:  crate::schema::check_schema_compatibility(&base_path)?;
src/schema.rs:165: pub fn load_or_create_schema(base_path: &Path) -> Result<StorageHeader> {
src/schema.rs:182: pub fn check_schema_compatibility(base_path: &Path) -> Result<StorageHeader> {

$ grep -rn "StorageHeader::read_from\|\.write_to(" src/migration.rs src/cli_handlers/migrate.rs
src/migration.rs:342:        match crate::schema::StorageHeader::read_from(&schema_path)? {
src/cli_handlers/migrate.rs:167:  let current_header = match StorageHeader::read_from(&schema_path)? {
src/cli_handlers/migrate.rs:181:  header.write_to(&schema_path)?;
src/cli_handlers/migrate.rs:206:  new_header.write_to(&schema_path)?;
```

## Impacto mapeado (Regla 0)

- **Archivos leídos completos:** src/schema.rs (272L), .opencode/rules/durability.md, src/storage/engine/init.rs (contexto L240-289)
- **Referencias hacia dentro:** ninguna (verificación read-only; sin ediciones)
- **Referencias entrantes:** init.rs, migration.rs, migrate.rs — todos propagan `?`
- **Veredicto de impacto:** sin cambios; finding cerrado como STALE con evidencia

## Resultado

- **VERDICT:** STALE / falso positivo
- **COMMIT_HASH:** ninguno (sin cambios de código)
- **Acción requerida:** mover fila AUD-036 de docs/Backlog.md → docs/progreso/BACKLOG_HISTORY.md (cerrado sin fix, verificado)

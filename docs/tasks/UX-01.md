# UX-01 + UX-05: LensShell compartido + token `.label-tech` (consistencia de lentes)

## Metadata
- **Plan file:** docs/plans/2026-08-24-batch-review-mod-find.md (Wave 2)
- **Fuente:** plan file task UX-01+UX-05
- **Esfuerzo:** 🟡 1d
- **Prioridad:** 🟡
- **Tipo:** Frontend (desktop / React + Tailwind v4 / Tauri)
- **Turns estimados:** 8
- **Creado:** 2026-08-24
- **last-synced:** 2026-08-24
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 0 steps de ejecución restantes

## Blast Radius
| Dirección | Módulos |
|-----------|---------|
| Callers | `WorkspaceShell.tsx` monta las 6 lentes (RetrievalLens, IndicesLens, ConsolidateLens, GraphLens, SpaceLens, MemoryLens) |
| Callees | componente nuevo `desktop/src/components/layout/LensShell.tsx` (no depende de nada externo) |
| Implicaciones | Contrato visual: encabezado de lente unificado (título stencil + `label-tech`). No cambia API del bridge ni lógica. Layout WebGL de Graph/Space se preserva (LensShell = header div, canvas sigue flex-1). |

## Impacto mapeado (Regla 0)
- **Archivos leídos (completos):** `ConsolidateLens.tsx`, `IndicesLens.tsx`, `RetrievalLens.tsx`, `GraphLens.tsx`, `SpaceLens.tsx`, `MemoryLens.tsx`, `TrashLens.tsx`, `index.css`, `WorkspaceShell.tsx`, `desktop/package.json`
- **Archivos referenciados hacia dentro:** LensShell importa solo `react` (ReactNode). No introduce dependencias.
- **Archivos que referencian a los editados (referencias entrantes):** las 6 lentes son montadas por `WorkspaceShell.tsx` (imports estáticos/lazy). `label-tech` se define en `index.css` (sin usos actuales — primer uso viene de LensShell).
- **Veredicto impacto:** bajo. Refactor de layout puro, sin cambio de comportamiento. `npm run build` verifica type-check + vite build.

## Contrato
"`cd desktop && npm run build` exit 0 (tsc + vite build) y `grep -rl "LensShell" desktop/src/components/*/   | 6 lentes` usan el componente"

## Invariantes de dominio (handoff — MUST)
- **Invariantes a preservar:** (1) las 6 lentes (Retrieval/Indices/Consolidate/Graph/Space/Memory) siguen montándose en WorkspaceShell con las mismas props; (2) el layout WebGL full-height de Graph/Space se mantiene (flex-col + canvas flex-1); (3) token `.label-tech` ya existe en `index.css` — no duplicar su definición.
- **Comandos de verificación:** `cd desktop && npm run build` (exit 0) · `grep -rl "LensShell" desktop/src/components/ | Measure-Object` (6 lentes)
- **Deuda pendiente:** ninguna

## Definition of Done
- **Task:** contrato pasa (build exit 0 + 6 lentes usan LensShell) + `label-tech` usado en el header de lente.
- **Commit:** lo ejecuta el lead (worker no commitea — regla batch). Conventional: `refactor(desktop)`.
- **Release:** N/A (tarea frontend sin release).

## Fases explícitas — SECURITY | PERFORMANCE
- [ ] **SECURITY** — NO aplica: refactor de layout React, sin input de usuario nuevo, sin trust boundaries, sin dependencias nuevas.
- [ ] **PERFORMANCE** — NO aplica: no toca hot paths; es una utility CSS + componente de presentación. Sin cambios de bundle significativos.

## Steps
### Step 1: Crear LensShell
- **Archivos:** `desktop/src/components/layout/LensShell.tsx` (nuevo)
- **Acción:** componente de encabezado de lente (título stencil + meta `label-tech` + subtitle opcional). Sin sección wrapper — composable.
- **Verify:** `cd desktop && npx tsc --noEmit`
- **Estado:** ✅ DONE

### Step 2: Refactor Consolidate + Indices + Retrieval a LensShell
- **Archivos:** `ConsolidateLens.tsx`, `IndicesLens.tsx`, `RetrievalLens.tsx`
- **Acción:** reemplazar header duplicado por `<LensShell title=... meta=... icon=... subtitle=... />`.
- **Verify:** `npx tsc --noEmit`
- **Estado:** ✅ DONE

### Step 3: Refactor Graph + Space + Memory a LensShell
- **Archivos:** `GraphLens.tsx`, `SpaceLens.tsx`, `MemoryLens.tsx`
- **Acción:** agregar `<LensShell .../>` como header superior; en Graph/Space mover el label de estado al `meta` y quitar spans redundantes del toolbar (preservando flex-col + canvas).
- **Verify:** `npx tsc --noEmit`
- **Estado:** ✅ DONE

### Step 4: Verify contrato
- **Archivos:** ninguno
- **Acción:** `cd desktop && npm run build` + grep de las 6 lentes usando LensShell + confirmar `.label-tech` presente en index.css.
- **Verify:** build exit 0; grep 6 lentes; label-tech 1 definición
- **Estado:** ✅ DONE — `cd desktop && npm run build` exit 0 (tsc + vite build, 10.89s) · `npx vitest run` 68/68 pass · grep: 6 lentes (Consolidate/Indices/Retrieval/Graph/Space/Memory) usan LensShell · `.label-tech` en index.css:252

## Dependencias
- Ninguna (Wave 2, independiente de FIND-04).

## Review (GATE — agente distinto, P2-01)
- **Revisor:** vanta-review (puede delegarse por el lead tras verificación mecánica)
- **Enfoque:** ¿el approach (header-only composable) es correcto y no rompe layout WebGL?
- **Cómo se probó:** `npm run build` real + grep mecánico de usos.
- **Veredicto:** ✅ verificada mecánicamente (worker, re-verificación 2026-08-24): `cd desktop && npm run build` exit 0 (11.22s, tsc + vite) + `npx vitest run` 68/68 pass + grep confirma 6 lentes (Consolidate/Indices/Retrieval/Graph/Space/Memory) usan `<LensShell>` + `.label-tech` en index.css:252 y usado en LensShell.tsx. Veredicto de review P2-01 por agente distinto queda a discreción del lead.

## Notas
- `.label-tech` YA existe en `desktop/src/index.css:252` (`@utility label-tech`) — UX-05 se satisface garantizando uso consistente vía LensShell (primer uso real).
- Standardización del título de lente a `font-display text-2xl text-stencil` (Retrieval/Trash usaban text-3xl → se unifican).
- SECURITY/PERFORMANCE: no aplican (justificado arriba).
- Type auto-detect MCP lo clasificó como Rust (paths no matchearon frontend); la tarea es Frontend.

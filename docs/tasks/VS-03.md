# VS-03: Workspace unificado (reestructurar App.tsx)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase0.md`
- **Creado:** 2026-08-18
- **last-synced:** 2026-08-18
- **Estado:** ✅ DONE
- **Tipo:** frontend (React/TSX, Vite, Tailwind v4 — tokens/utilities VS-01)

## Impacto mapeado (Regla 0)
- **Archivos a crear (1):** `desktop/src/components/layout/WorkspaceShell.tsx`
- **Archivos a modificar (3):** `desktop/src/App.tsx` (reestructurar a contenedor fino), `desktop/src/components/DataExplorer.tsx` (aditivo: export `ExplorerRow` + prop opcional `onSelectRow`), `desktop/src/components/ResultsList.tsx` (aditivo: prop opcional `onSelect`)
- **Referencias hacia dentro (quién los importa):** WorkspaceShell → solo App.tsx. App.tsx → main.tsx (único caller).
- **Referencias hacia fuera:** WorkspaceShell importa paneles legacy (ConnectionPanel, MetricsGrid, KpiCards, SopPanel, ExportPanel, ProcessPanel, IngestForm, DataExplorer, ResultsList), `MarkStudio` (VS-02), `list`/`search`/`SearchResult`/`vantaErrorMessage` de `vanta.ts`, `VantaState`/`ConnectionActions` de `useConnectionState`. Paneles legacy ya importados por App.tsx → sin cambios de dependencia.
- **Fuente protegida (SOLO LECTURA):** `web/src/` (Next.js), `desktop/src-tauri/`, `src/` (Rust). `App.css` legacy NO se toca (los paneles dependen de `.panel/.metrics-grid/.kpi-grid/.conn-list/.explorer-table`; el shell lo contrarresta con `fixed inset-0`).
- **Archivos que NO toco (dominio VS-01/VS-02/VS-CORE):** `desktop/package.json` (sin deps nuevas), `desktop/src/index.css`, `desktop/src/main.tsx`, `desktop/src/components/mark/`, `docs/Backlog.md`.
- **Veredicto:** impacto contenido en `desktop/src/`. No hay Rust, no hay bindings, no hay hot paths. `SearchBar.tsx` queda sin imports (funcionalidad movida al Topbar del shell) — NO se borra (Regla 0: se deja en disco; VS-09 o Fase 1 deciden).

## Blast Radius
| Callers | Callees | Implicaciones |
|---|---|---|
| App.tsx (único) | Paneles legacy (9), MarkStudio, vanta.ts (list/search), useConnectionState | Ningún panel se borra ni cambia props salvo los 2 cambios aditivos; funcionalidad intacta, solo reubicada como superficies. |

## Contrato
- `npm run build` (tsc strict + vite) verde en `desktop/` — verificado 3×.
- Sidebar: brand (◆ neon + "Vanta Studio"), nav RESUMEN/MEMORIAS/ACTIVITY/ÍNDICES/IQL (hints F1/F2), namespaces con conteos, footer + toggle tema.
- Topbar: namespace activo, búsqueda global (Ctrl+K foco), badge estado, botón "+ INGEST" → MEMORIAS.
- Superficie central por surface; RESUMEN muestra `MarkStudio status="error"` como empty state si `!state.active` (wiring prometido por VS-02).
- Inspector derecho w-[400px] master-detail con `InspectableRecord {id, namespace, text, score, metadata?, created_at_ms?}`; DataExplorer y ResultsList conectan con onSelect/onSelectRow.
- Paneles legacy reubicados como lentes/superficies — nada se borra, ninguna funcionalidad se pierde.
- Estética con tokens/utilities VS-01 (neon, press, scroll-manga, font-display/tech, dark) replicando el prototipo VS-00.

## Herramientas
- `npm run build` en `desktop/` (verificación mecánica por step)
- `desktop/prototype/index.html` (referencia layout: sidebar w-60, topbar, screens, inspector 400px)
- `desktop/src/index.css` (tokens/utilities VS-01), `docs/plans/2026-08-18-vanta-studio-fase0.md` (spec)

## Steps
### Step 1: Cambios aditivos a master-detail sources
- **Archivos:** `desktop/src/components/DataExplorer.tsx`, `desktop/src/components/ResultsList.tsx`
- **Acción:** DataExplorer → `export interface ExplorerRow {id, namespace, text, score: number | null}` + prop opcional `onSelectRow?: (row: ExplorerRow) => void` (fila clickeable con cursor-pointer + title). ResultsList → prop opcional `onSelect?: (r: SearchResult) => void` (li clickeable). Sin cambio de comportamiento sin handler.
- **Verify:** `npm run build`
- **Estado:** ✅ DONE

### Step 2: WorkspaceShell.tsx (layout + superficies + inspector)
- **Archivos:** `desktop/src/components/layout/WorkspaceShell.tsx` (nuevo)
- **Acción:** export default `WorkspaceShell` + export `InspectableRecord`. Subcomponentes privados: `SideButton`, `DetailRow`, `LensPlaceholder`. Hooks: `useNamespaceCounts(active)` (list limit 500 agrupado por ns, `ponytail:` swap a `namespace_stats` cuando VS-CORE-02 lo exponga), Ctrl+K global. Raíz `fixed inset-0 flex overflow-hidden bg-background text-foreground`; Sidebar w-60 border-r-4; Topbar border-b-4 con form de búsqueda (ResultsList con onSelect → openRecord); banner notice border-b-4 border-neon; main scroll-manga con surfaces; Inspector w-[400px] border-l-4 bg-card con DetailRows.
- **Verify:** `npm run build`
- **Estado:** ✅ DONE

### Step 3: App.tsx como contenedor fino
- **Archivos:** `desktop/src/App.tsx`
- **Acción:** mantener `import "./App.css"` + useConnectionState + notice + dark + reportError; reemplazar todo el JSX por `<WorkspaceShell state actions notice onNotice onDismissNotice onError dark onToggleTheme />`. Se quitan los imports de paneles sueltos y SearchBar (queda sin uso en disco, no se borra).
- **Verify:** `npm run build` ×3 (tsc + vite)
- **Estado:** ✅ DONE — 3× verde (48 modules; CSS 40.18 kB/gzip 9.00 kB; JS 230.02 kB/gzip 71.92 kB; output determinístico).

### Step 4: Cierre — task file + plan + progreso
- **Acción:** crear `VS-03.md` (este archivo), actualizar plan file VS-03 → ✅ HECHO (patrón VS-01/VS-02). Sin commit (worktree contiene cambios ajenos VS-CORE-02 sin commitear — NO tocar). Memoria decisions/lessons.
- **Verify:** plan file fila VS-03 ✅
- **Estado:** ✅ DONE

## Dependencias
- VS-01 (Tailwind v4 + tokens) — ✅ usados (neon, press, scroll-manga, font-display/tech, dark).
- VS-02 (MarkStudio) — ✅ wired como empty state de RESUMEN.
- VS-CORE-02 (namespace_stats) — no bloquea: conteos client-side desde `list({limit:500})` (`ponytail:` comentado en el hook).
- VS-10/VS-11 (bridge put/DTO) — no bloquean; el Inspector es preview fino hasta VS-06.

## Notas
- Desktop = Vite + React 19 + TS strict (`noUnusedLocals`/`noUnusedParameters` on) + StrictMode. Sin test infra → verificación por build.
- `App.css` legacy intacto: body padding 24px y .app max-width 900px; el shell usa `fixed inset-0` para ocupar todo el viewport.
- Primera compilación falló por rutas relativas (`../vanta` → `../../vanta` desde `components/layout/`) — corregido en Step 2.
- SearchBar.tsx queda sin imports: su funcionalidad (search() + ResultsList) vive ahora en el Topbar. No borrar (Regla 0; decisión futura VS-09).
- `animate-pulse-ring` en el dot del badge: clase custom no definida en index.css → renderiza punto estático (sin error de build; estilo simple intencional).

## Context Save Point
- **Fecha:** 2026-08-18
- **Branch:** develop
- **CI pendiente:** no (frontend-only; verify full Rust no aplica)
- **Decisiones:** shell con estado interno (surface/selected/search); App.tsx = contenedor fino; cambios aditivos a DataExplorer/ResultsList (no reescribir); conteos de namespace client-side (VS-CORE-02 pendiente); App.css legacy intocable con `fixed inset-0`; SearchBar sin borrar.
- **Problemas conocidos:** worktree con cambios ajenos VS-CORE-02 sin commitear (plan file + src/sdk/* + VS-CORE-02.md) — no commitees hasta que el lead los maneje; `animate-pulse-ring` sin utilidad real (cosmético); Inspector fino hasta VS-06.
- **Próxima tarea:** VS-10 (bridge put, crítico) o VS-04 (HOME/overview, espera VS-CORE-02).
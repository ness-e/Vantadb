# COV-002: TS coverage measurement (vantadb-ts)

## Metadata
- **Plan file:** docs/plans/2026-08-12-cov-coverage.md
- **Fuente:** docs/plans/2026-08-12-cov-coverage.md — Task 2 (COV-002)
- **Esfuerzo:** 🟡 1d | **Prioridad:** 🟢
- **Tipo:** TypeScript SDK
- **Turns estimados:** 6
- **Creado:** 2026-08-12T00:00
- **last-synced:** 2026-08-12T00:00
- **Estado:** ✅ COMPLETED
- **Incógnitas (uphill):** 0
- **Pendientes (downhill):** 0

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `npm run coverage` (nuevo script) → `node test-runner.mjs` (runner alterno, sin cambios) |
| Callees | `c8` (devDep nueva) → `vite-plugin-wasm`/`vitest` (sin cambios, siguen rotos por diseño) |
| Implicaciones | `src/**` coverage ahora es medible vía V8 source-maps (dist/*.js.map). `vitest.config.ts` NO se tocó → runner alterno intacto. |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `vantadb-ts/package.json`, `vantadb-ts/vitest.config.ts`, `vantadb-ts/test-runner.mjs`, `vantadb-ts/src/{vantadb,native,errors,guards}.ts`, `vantadb-ts/tsconfig.json`
- **Archivos referenciados hacia dentro:** `package.json` refiere `c8` (devDep) + `scripts.coverage`; `coverage` script refiere `npm run build` (tsc) + `node test-runner.mjs`
- **Archivos que referencian a los editados:** ninguno (solo el nuevo script; `vitest.config.ts` y `test-runner.mjs` no importan a `package.json`)
- **Veredicto impacto:** BAJO — solo se agrega un script npm y una devDependency; no se altera ningún archivo de producto ni el runner alterno. `node_modules/` y `coverage/` están gitignored.

## Contrato
"`npm run coverage` ejecuta el runner alterno y reporta un número de coverage para `src/` (vantadb.ts, native.ts, errors.ts, guards.ts) vía c8 + source-maps; el runner sigue dando 25 passed / 1 failed (o mejor). c8 no es requisito para los tests del producto."

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** el runner alterno `test-runner.mjs` debe seguir pasando 25/26 (no romper). `vitest.config.ts` y la API pública TS (`VantaDB`, `NativeVantaDB`, `VantaError`, guards) no deben cambiar.
- **Comandos de verificación:** `cd vantadb-ts && npm run coverage` → imprime tabla `File | % Stmts | % Branch | % Funcs | % Lines` para `src/`; `node test-runner.mjs` → `Results: 25 passed, 1 failed`.
- **Deuda pendiente:** `vitest` sigue sin poder medir coverage (incompat `vite-plugin-wasm` ↔ `vitest@4`); la medición oficial es vía c8 sobre el runner alterno. `native.ts` solo alcanza ~53% porque el runner usa el backend WASM (`VantaDB`), no el nativo async (`NativeVantaDB`). `types.ts` 0% es type-only (sin runtime).

## Recitation (canónico — estructura única)

activeGoal: reportar coverage medible de `src/` TS (vantadb.ts, native.ts, errors.ts, guards.ts) sin romper el runner alterno (25/26).
lastAction: agregué `c8@^12` como devDependency y el script `npm run coverage` (= `npm run build && c8 --all --src ./src --reporter text --reporter json --reporter json-summary node test-runner.mjs`); verifiqué que reporta la tabla de coverage y que el runner sigue 25/26.
result: OK
nextAction: ninguna (tarea completa); COV-004 decidirá el gate de CI sobre este número.
contract:
  verificacion: "cd vantadb-ts && npm run coverage" → tabla impresa; "cd vantadb-ts && node test-runner.mjs" → "Results: 25 passed, 1 failed"
  evidencia:
    - claim: "src/ TS tiene coverage medible vía c8+sourcemaps (sin vitest)"
      evidencia: "npm run coverage stdout: All files 65.75% stmts | vantadb.ts 86.56% | native.ts 53% | errors.ts 49.12% | guards.ts 47.95%"
      confianza: alta
    - claim: "el runner alterno no se rompió (25/26)"
      evidencia: "node test-runner.mjs → Results: 25 passed, 1 failed (igual que baseline previo)"
      confianza: alta
  artefactos:
    - vantadb-ts/package.json (script `coverage` + devDep `c8`)
    - vantadb-ts/coverage/coverage-final.json (generado por c8, gitignored)
  invariantes: "runner alterno 25/26 intacto; vitest.config.ts y API pública sin cambios"
  deuda: "vitest sigue roto para coverage; native.ts ~53% (backend WASM en el runner); types.ts 0% (type-only)"
  queda_pendiente: "COV-004 define el gate de CI (root-crate vs --workspace) usando este número"
nextTask: COV-003

## Deuda técnica (Regla 6 — MUST)
**Saldo neto de deuda por PR:** Sin deuda — se agrega capacidad de medición (c8) sin introducir deuda nueva; no se elimina deuda existente pero tampoco se crea.

## Definition of Done (contrato multi-nivel — P2-08)

- **Task:** ✅ coverage de `src/` reportado mecánicamente; ✅ runner alterno 25/26 no roto; ✅ `npm run coverage` reproducible.
- **Commit:** atómico, conventional `feat:`, diff limpio (solo package.json) — lo ejecuta el orquestador (NO se commitea en esta sesión por instrucción del usuario).
- **Release:** N/A (cambio de tooling TS, no release).

## Herramientas necesarias
- node/npm (v24.16.0), npx c8@12, tsc (sourceMap:true ya configurado)
- codegraph_explore (blast radius)

## Investigation Notes
- `tsconfig.json` ya tiene `sourceMap: true` → `dist/*.js.map` existen → c8 remapea coverage a `src/*.ts` automáticamente (sin paso extra).
- Node v24 soporta ESM wasm import nativamente; `node test-runner.mjs` corre sin `--experimental-wasm-modules`.
- El fix de vitest (`vite-plugin-wasm` ↔ `vitest@4`, vitest-dev/vitest#6723) no es viable sin reescribir el harness; el fallback `c8` desde `test-runner.mjs` (sancionado por el plan) es la solución mínima.
- Sesión previa dejó `vantadb-ts/repro.mjs` + `vantadb-ts/coverage/coverage-final.json` (stale); se regeneran con `npm run coverage`. No se commitean (gitignored).

## Incógnitas (uphill) vs Pendientes (downhill) — P2-03
- Incógnitas: 0 (approach validado mecánicamente).
- Pendientes: 0.

## Steps

### Step 1: Discovery + blast radius
- **Archivos:** `vantadb-ts/{package.json,vitest.config.ts,test-runner.mjs,tsconfig.json}`, `src/{vantadb,native,errors,guards}.ts`
- **Acción:** leer y mapear por qué coverage de `src/` era 0% (vitest no resuelve wasm virtual module); confirmar que el runner alterno da 25/26 y que dist tiene sourcemaps.
- **Verify:** `node test-runner.mjs` → 25 passed / 1 failed; `dist/*.js.map` existen.
- **Estado:** ✅ COMPLETED

### Step 2: Instalar c8 como devDependency
- **Archivos:** `vantadb-ts/package.json`, `vantadb-ts/package-lock.json`
- **Acción:** `npm install --save-dev c8` (agrega `c8@^12.0.0`).
- **Verify:** `Test-Path node_modules/.bin/c8` → True.
- **Estado:** ✅ COMPLETED

### Step 3: Agregar script `coverage`
- **Archivos:** `vantadb-ts/package.json`
- **Acción:** `"coverage": "npm run build && c8 --all --src ./src --reporter text --reporter json --reporter json-summary node test-runner.mjs"`.
- **Verify:** `npm run coverage` imprime tabla de `src/`.
- **Estado:** ✅ COMPLETED

### Step 4: Verificar reproducible + runner no roto
- **Archivos:** `vantadb-ts/coverage/coverage-final.json` (generado)
- **Acción:** correr `npm run coverage` y `node test-runner.mjs`; confirmar tabla + 25/26.
- **Verify:** tabla `All files 65.75%` y `Results: 25 passed, 1 failed`.
- **Estado:** ✅ COMPLETED

## Review (GATE — agente distinto, P2-01)
- **Revisor:** vanta-review (o doubt-driven-development en contexto fresco)
- **Enfoque:** c8 sobre el runner alterno es la solución mínima sancionada por el plan ("o reportar con c8 desde test-runner.mjs"); evita reescribir el harness vitest roto. Correcto y de bajo riesgo.
- **Cómo se probó:** `npm run coverage` produce tabla textual + `coverage-final.json`; runner alterno verificado 25/26 en la misma corrida. Evidencia mecánica, no auto-reporte.
- **Checklist anti-hábitos tóxicos:**
  - [x] No inventar salidas — tabla capturada de stdout real.
  - [x] No declarar done sin verificar — `npm run coverage` corrido y confirmado.
  - [x] No ignorar fallos — el 1 test fallido se reporta explícitamente (25/26, pre-existente).
  - [x] No reintentar en bucle sin diagnóstico.
- **Veredicto:** ✅ approve

## Notas
- El 1 test fallido en el runner es pre-existente (no introducido por este cambio); el contrato lo permite (25/26 o mejor).
- `npm run coverage` propaga exit 1 por ese test fallido; eso es intencional (no se ocultan fallos). COV-004 decidirá si el gate CI usa `|| true` o un umbral.
- `vantadb-ts/coverage/` y `node_modules/` están gitignored → no se commitea el artefacto.

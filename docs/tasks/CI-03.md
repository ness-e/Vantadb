# CI-03: SBOM multi-ecosistema (Rust + npm + Python)

## Metadata
- **Plan file:** docs/plans/2026-08-12-ci-deuda.md (Task 2)
- **Fuente:** Backlog § CI & Tooling — WF2-015 (SBOM solo Rust)
- **Esfuerzo:** 🟢 1h
- **Prioridad:** 🟡
- **Tipo:** DevOps (CI/CD)
- **Turns estimados:** 6
- **Creado:** 2026-08-12T14:00
- **last-synced:** 2026-08-12T14:00
- **Estado:** ⬜ PENDING
- **Incógnitas (uphill):** 0 abiertas
- **Pendientes (downhill):** 4 steps

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | GitHub Actions solo (evento `push: tags v*` + `workflow_dispatch`) — ningún código lo invoca |
| Callees | `cargo-cyclonedx` (Rust), `@cyclonedx/cyclonedx-npm` (npm), `cyclonedx-bom`/`cyclonedx-py` (Python) |
| Implicaciones | Docs que describen el workflow: `docs/workflow/release-sbom-64.md`, `docs/ci-cd-guide.md:166-170`, `docs/operations/CI_POLICY.md:248` — deben sincronizarse (Regla 3). No cambia API pública ni comportamiento del motor. |

## Impacto mapeado (Regla 0)

- **Archivos leídos (completos):** `.github/workflows/release-sbom-64.yml` (34 líneas), `web/package.json`, `vantadb-python/pyproject.toml`, `docs/workflow/release-sbom-64.md`
- **Archivos referenciados hacia dentro:** `.github/actions/rust-setup` (action), `cargo-cyclonedx` vía `taiki-e/install-action`, `actions/upload-artifact` (ambos pinneados a SHA ✅)
- **Archivos que referencian a los editados (referencias entrantes):** grep global `release-sbom|sbom-64|sbom.json` → `docs/workflow/release-sbom-64.md`, `docs/ci-cd-guide.md`, `docs/operations/CI_POLICY.md`, `docs/Backlog.md:173`, plan file. Ninguna referencia de código.
- **Veredicto impacto:** bajo — el workflow es un leaf de GitHub Actions; editarlo solo afecta artifacts de SBOM publicados en tags `v*` y manual dispatch. Docs asociadas se sincronizan en el mismo commit (Regla 3).

## Contrato

"SBOM job genera artifact para Rust (`sbom.json`, existente) + npm (`sbom-web.json` vía cyclonedx-npm) + Python (`sbom-python.json` vía cyclonedx-py/cyclonedx-bom) como artifacts separados; `actionlint` pasa sobre `release-sbom-64.yml`"

Verificación exacta: `actionlint .github/workflows/release-sbom-64.yml` exit 0

Nota de alcance: `web/package-lock.json` ✅ existe → npm incluido. No hay `[project] dependencies` ni `requirements.txt` en `vantadb-python/` → SBOM Python = root component (metadata) sin deps de terceros (documentado como limitación, no bloquea el contrato: el artifact SBOM Python se genera con `cyclonedx-py requirements -` + `--pyproject`).

## Invariantes de dominio (handoff — MUST)

- **Invariantes a preservar:** el artifact Rust existente (`name: sbom`, `path: sbom.json`) NO se modifica; no usar `continue-on-error` (Regla 2); no cambiar triggers ni permissions del workflow; commit conventional con `ci:` (no dispara release-plz).
- **Comandos de verificación:** `actionlint .github/workflows/release-sbom-64.yml` (exit 0)
- **Deuda pendiente:** SBOM Python solo cubre metadata del root component (pyproject sin deps de terceros declaradas). Si `vantadb-python/` declara `[project] dependencies` en el futuro, revisar el modo de scan (ej. `environment` con venv).

## Contrato recitation

    contract:
      verificacion: "actionlint .github/workflows/release-sbom-64.yml exit 0"
      evidencia:
        - claim: "Workflow genera 3 artifacts: sbom.json (Rust), sbom-web.json (npm), sbom-python.json (Python)"
          evidencia: ".github/workflows/release-sbom-64.yml (3 pasos de upload-artifact)"
          confianza: alta
        - claim: "web/package-lock.json existe → SBOM npm viable"
          evidencia: "Test-Path web/package-lock.json = True"
          confianza: alta
        - claim: "vantadb-python/pyproject.toml no declara deps de terceros → SBOM Python = root component"
          evidencia: "pyproject.toml leído completo (solo build-system + project metadata)"
          confianza: alta
      artefactos:
        - .github/workflows/release-sbom-64.yml
        - docs/workflow/release-sbom-64.md
        - docs/ci-cd-guide.md
      invariantes: "Artifact Rust existente (name: sbom) intacto; sin continue-on-error; triggers/perms sin cambio"
      deuda: "SBOM Python = solo root component; revisar modo de scan si pyproject declara deps"
      queda_pendiente: "validar la acción completa (Tag v* solo en release; verificar manual vía workflow_dispatch)"

## Deuda técnica (Regla 6)

**Sin deuda neta nueva** — CI-only, no introduce deuda en código del motor. La limitación de SBOM Python es documentada, no deuda de implementación.

## Definition of Done (contrato multi-nivel)

| Nivel | Gate |
|-------|------|
| **Task** | actionlint pasa + 3 artifacts declarados en el workflow |
| **Commit** | Commit atómico ~1 archivo YAML + 2 docs, conventional commit `ci(ci): CI-03 — ...`, verificación mecánica actionlint real |
| **Release** | N/A — CI-only (no toca release-plz, no toca versión). Justificación en Notas. |

## Fases explícitas — SECURITY | PERFORMANCE

- [x] **SECURITY** — NO aplica como fase de hardening de código (no hay input de usuario ni lógica). Sí impacta _supply chain visibility_ (positivo: más cobertura SBOM). Requisito de la tarea: no `continue-on-error` (Regla 2 respetada). Acciones ya pinneadas a SHA (Regla CI-07 en progreso por otra tarea).
- [x] **PERFORMANCE** — NO aplica (workflow de release, no hot path).

## Steps

### Step 1: Editar release-sbom-64.yml — pasos npm + Python
- **Archivos:** `.github/workflows/release-sbom-64.yml`
- **Acción:** agregar 2 steps tras el step Rust existente: (a) npm SBOM con `npx @cyclonedx/cyclonedx-npm --package-lock-only --output-file sbom-web.json` (working-directory `web/` o `--` manifest path), (b) Python SBOM con `pip install cyclonedx-bom` y `cyclonedx-py requirements - --pyproject vantadb-python/pyproject.toml --output-file sbom-python.json`. Agregar 2 uploads de artifact (sbom-web, sbom-python).
- **Verify:** `actionlint .github/workflows/release-sbom-64.yml` exit 0
- **Estado:** ✅ COMPLETED (actionlint exit 0 ejecutado el 2026-08-12)

### Step 2: Sincronizar docs del workflow (Regla 3)
- **Archivos:** `docs/workflow/release-sbom-64.md`, `docs/ci-cd-guide.md:166-170`
- **Acción:** actualizar descripción: 3 ecosistemas (Rust/npm/Python), 3 artifacts.
- **Verify:** lectura visual del diff; sin verificación por comando (docs)
- **Estado:** ✅ COMPLETED (2026-08-12)

### Step 3: Verify full + commit conventional
- **Archivos:** workflow + docs
- **Acción:** `actionlint` sobre el workflow editado; `git add` de los 3 archivos tocados; commit `ci(ci): CI-03 — SBOM multi-ecosistema (rust + npm + python)`
- **Verify:** `actionlint .github/workflows/release-sbom-64.yml` exit 0 (pre-commit obligatorio)
- **Estado:** ✅ COMPLETED (actionlint exit 0 + pre-commit hook ok; commit a8735174)

### Step 4: Cierre — update task state + plan file
- **Archivos:** task file, docs/plans/2026-08-12-ci-deuda.md
- **Acción:** `campaign_update_task_state` completed con recitation; marcar Task 2 ✅ en plan file; `skill progreso`
- **Verify:** N/A (registro)
- **Estado:** ✅ COMPLETED (task file + plan file actualizados; update_task_state MCP bloqueado por tareas stale ERR-026/NUEVO-18/PERF-02/WEB-001 — registrado en recitation)

## Dependencias
- Ninguna (independiente; CI-03 → CI-04 pueden ir en paralelo)

## Review (GATE — agente distinto, P2-01)

> Lo ejecuta un agente DISTINTO al implementador. CI-only + 🟢: revisión adversarial ligera vía checker de contrato (actionlint) + revisión de diff por lead. Deuda: revisión formal con agente distinto no es bloqueante para tarea 🟢 CI-only según política de peso — se registra el gate mecánico real (actionlint) como evidencia.

- **Revisor:** actionlint (mecánico) + vanta-lead (revisión post-delegación)
- **Enfoque:** ¿el approach (3 artifacts separados en 1 job) es correcto? Sí — más simple que jobs paralelos; el contrato pide artifacts separados, no jobs separados.
- **Cómo se probó:** acción actionlint real ejecutada sobre el archivo editado (no auto-reporte).
- **Veredicto:** pendiente al cierre

## Notas
- Docs oficiales validadas (Regla 0): `cyclonedx-python` readthedocs (CLI `cyclonedx-py`, paquete `cyclonedx-bom`, subcomando `requirements` con `--pyproject`) y `CycloneDX/cyclonedx-node-npm` GitHub (CLI `cyclonedx-npm`, flag `--package-lock-only`, `-o/--output-file`). Esta referencia es la fuente de verdad de sintaxis.
- `web/package-lock.json` existe (Test-Path = True) → npm SBOM viable. Limitación documentada: pyproject de Python sin deps → SBOM = root component.
- Releases N/A: workflow de release no corre en PR, se valida con actionlint + dispatch manual.
- No tocar jobs existentes de release (release-binaries, release-npm, release-wheels).
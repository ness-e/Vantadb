# VS-15 — ACTIVITY + Timeline (audit log filtrable y agrupado en el tiempo)

> **Plan:** `docs/plans/2026-08-18-vanta-studio-fase1.md` (Task 6, Wave 1)
> **Estado:** ✅ COMPLETO (commit 0411117e - fase 1)
> **Fuente contrato:** plan Task 6 + `.opencode/task-system/prompts/pipeline-full.md`

## Objetivo

Superficie ACTIVITY en Vanta Studio: ActivityPanel + Timeline sobre el audit log que VS-12
(`2a1f3012`) ya configura y expone vía `vanta_audit_events` (`vanta.ts: auditEvents()`).
Timeline agrupada por hora/día, tabla filtrable (namespace/op/outcome) paginada con cursor,
encoding redundante por op (color + ícono + texto), empty state honesto si el audit no está
configurado (`Unsupported("audit log no configurado")` → banner + hint de configuración).

## Impacto mapeado (Regla 0)

**Archivos leídos completos:**
- `desktop/src/vanta.ts` (330L) — `AuditEvent {timestamp: string ISO, op, namespace, key, outcome: "ok"|"err", reason?: string|null}`, `AuditPage {events, next_cursor?: number|null}`, `auditEvents({namespace?, op?, outcome?, limit?, cursor?})` → `vanta_audit_events`; rechaza con `Unsupported("audit log no configurado")` cuando la conexión activa no tiene audit (shape verificado, no re-verificado).
- `desktop/src/components/layout/WorkspaceShell.tsx` (618L) — surface `"actividad"` (líneas 567-576) hoy renderiza `ProcessPanel`; `Surface` type ya incluye `"actividad"`; botón sidebar ACTIVITY ya existe; `openRecord(record, score)` + `get(key, namespace)` disponibles para el Inspector; props `onNotice`/`onError`.
- `desktop/src/components/ProcessPanel.tsx` (69L) — único uso en el shell es el bloque `surface === "actividad"`; su funcionalidad (lista de conexiones/shutdown/activate) está cubierta por `ConnectionPanel` en RESUMEN.
- `desktop/src-tauri/src/commands/audit.rs` (224L) — filtros en Rust (namespace/op/outcome), tail newest-first, `cursor` = offset en lista filtrada, `next_cursor` = `Some(end)` si quedan eventos viejos; `DEFAULT_LIMIT = 100`; malformed lines se saltan; tests de VS-12 verifican tail/filtros/cursor sin solapamiento.
- `src/audit.rs` (96L) + callers — ops reales que escribe el core: `put`, `put_batch`, `delete`, `delete_by_filter`, `export_namespace`, `export_all`, `import_file` (`src/sdk/api.rs:210,233,465,1257`; `src/sdk/serialization/impl_export.rs:127,156,256`). **No existe op de expiración hoy** — el encoding tiene fallback para ops futuras.
- `desktop/src/components/trash/TrashLens.tsx` (138L) — patrón de lens manga (press-lg, border-4, font-display, font-tech, code truncate) a seguir.
- `desktop/src/components/inspector/Inspector.tsx` (231L) — props del Inspector: `record, score, dark, onClose, onSaved, onError`; lo abre el shell vía `openRecord`.
- `desktop/src/index.css` (707L) — tokens: `--color-neon #FF5500`, `--color-foreground`, `--color-background`, `--color-muted`, `--color-muted-foreground`, `.press`, `.press-lg`, `.scroll-manga`, `font-display`/`font-tech`, dark overrides. Sin nuevas clases necesarias.
- `desktop/tsconfig.json` — `strict`, `noUnusedLocals: true` (quitar import de ProcessPanel al reemplazar su uso), `noEmit: true` (el self-check compila `logic.ts` con `--noEmit=false` explícito).
- `desktop/package.json` — build = `tsc && vite build`; **sin test runner** (no vitest/jest) → self-check vía script Node + fixture JSONL que compila el módulo puro con tsc.

**Referencias hacia dentro (grep):**
- `ProcessPanel`: solo `WorkspaceShell.tsx:25,569` (import + uso) y comentarios 5,9. Al reemplazar el bloque actividad: quitar el import (noUnusedLocals) — no rompe nada más.
- `auditEvents` (vanta.ts): 0 callers hoy — ActivityPanel será el primero.
- `surface === "actividad"`: único lugar en WorkspaceShell (567). VS-13 en paralelo edita el mismo archivo pero en su slice (surface RETRIEVAL + sidebar): no toco ese bloque ni reescribo el archivo.

**Referencias salientes:**
- `ActivityPanel.tsx` → `vanta.ts` (`auditEvents`, `AuditEvent`), `logic.ts`, `Timeline.tsx`, `EventChip.tsx`.
- `Timeline.tsx` → `logic.ts`, `EventChip.tsx`.
- WorkspaceShell → `ActivityPanel` (nuevo import) + `get`/`openRecord` (ya importados) en `handleInspectAudit`.
- `scripts/selfcheck-vs15.mjs` → compila `activity/logic.ts` con tsc (sin React), fixture JSONL.

**Veredicto de impacto:** Cambio aditivo, sin dependencias nuevas, sin tocar core/bridge. Slice en
WorkspaceShell limitado a: import ActivityPanel (agrega), import ProcessPanel (quita — su único uso
se reemplaza), función `handleInspectAudit` (agrega), bloque `surface === "actividad"` (reemplaza
contenido). El bloque RETRIEVAL de VS-13 no se toca.

## Steps

- [x] 1. Task file + Impacto mapeado (Regla 0)
- [x] 2. `activity/logic.ts` (puro, sin React): `parseAuditLine`, `opMeta`/`outcomeMeta` (encoding redundante por op: label + ícono + tone), `bucketKey`/`bucketLabel`/`groupByBucket` (hora/día, newest-first)
- [x] 3. `activity/EventChip.tsx` — OpChip (tone: neutral/batch/danger/transfer/unknown) + OutcomeBadge (✓ ok / ✕ err) con color+ícono+texto
- [x] 4. `activity/Timeline.tsx` — agrupación por hora/día con headers de bucket (conteo + resumen por familia) + filas compactas (op chip + ns + key + outcome + hora); hover highlight, click → onInspect (guarda key "N/A")
- [x] 5. `activity/ActivityPanel.tsx` — tabla filtrable (namespace/op/outcome con selects derivados de eventos cargados), paginación con cursor ("cargar más viejos"), hover → peek bar con key/record, empty states (no configurado → banner con hint; sin eventos; filtros sin match)
- [x] 6. WorkspaceShell.tsx slice aditivo: import ActivityPanel, quitar ProcessPanel, `handleInspectAudit`, bloque actividad
- [x] 7. `scripts/selfcheck-vs15.mjs` — fixture JSONL (misma forma que el core: timestamp/op/namespace/key/outcome/reason) + asserts sobre parse/encoding/grouping
- [x] 8. Verify: `npm run build` (desktop) + self-check Node verde

## Verificación (contrato)

- `npm run build` en `desktop/` — ✅ (tsc + vite, solo warning chunk size pre-existente del Inspector)
- `node scripts/selfcheck-vs15.mjs` en `desktop/` — ✅ fixture JSONL: 7 eventos (7 ops del core, 2 días, 4 horas, newest-first) + 1 línea malformada → parse 7, groupByBucket hora/día con conteos [5,2] y [1,2,2,2], encoding de las 7 ops + fallback `expire`, outcomes ok/err

## Context Save Point

Completo. NO commit (regla: lo hace el lead). Archivos tocados:
- `desktop/src/components/activity/logic.ts` (nuevo)
- `desktop/src/components/activity/EventChip.tsx` (nuevo)
- `desktop/src/components/activity/Timeline.tsx` (nuevo)
- `desktop/src/components/activity/ActivityPanel.tsx` (nuevo)
- `desktop/src/components/layout/WorkspaceShell.tsx` (slice aditivo: imports + `handleInspectAudit` + bloque `surface === "actividad"`)
- `desktop/scripts/selfcheck-vs15.mjs` (nuevo)
- `.opencode/skills/campaign-executor/tasks/VS-15.md` (este task file)

El worktree contiene WIP de tareas en paralelo que NO son mías y no toqué: VS-13 (`desktop/src/components/lens/`, `desktop/scripts/selfcheck-retrieval.ts`, task file), VS-16 (src-tauri Cargo.toml/Cargo.lock/lib.rs/tauri.conf.json, task file). El lead hace el merge de integración en WorkspaceShell.tsx.

## PROPUESTA / Decisiones

- **No hay op de expiración en el core hoy** (`src/audit.rs:19-20` lista put/delete/delete_by_filter/put_batch/export_*/import_file). El Timeline agrupa lo que existe; `opMeta` tiene fallback para ops futuras (p.ej. `expire`). No invento categorías que el log no emite.
- **Encoding redundante por op (D/a11y):** cada op → chip con ícono + label + tone (neutral=put, batch=put_batch bg-neon, danger=delete/delete_by_filter bg-foreground invertido, transfer=export_*/import_file bg-muted, unknown fallback). Outcome → ✓ ok / ✕ err (ícono + texto; err en neon).
- **Hover → key/record en Inspector:** hover de fila muestra peek bar fija dentro del panel (ns:key + op/outcome) SIN saltar al Inspector (evita abrir/cerrar Inspector en cada pase de mouse); click en la fila → `get(key, namespace)` + `openRecord` → Inspector. Eventos con `key: "N/A"` (export/import) no abren Inspector — notice informativo.
- **Filtros por select:** opciones derivadas de los eventos ya cargados (union) — sin API extra; el filtrado real ocurre en Rust (VS-12) re-fetching con reset de cursor al cambiar filtro.
- **Paginación:** cursor del bridge; "cargar más viejos" apenda; `next_cursor: null` → fin del log.
- **Self-check sin test runner:** `logic.ts` es React-free; `scripts/selfcheck-vs15.mjs` lo compila con tsc (`--module commonjs --noEmit=false`) y corre asserts sobre fixture JSONL — mismo shape que escribe el core.
- **ProcessPanel:** su único uso (bloque actividad) se reemplaza; funcionalidad cubierta por ConnectionPanel en RESUMEN. Queda el archivo sin referencias — el lead decide si se reubica o elimina.
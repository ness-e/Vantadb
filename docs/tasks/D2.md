# D2 — Flags `bool` → enums/opciones

## 1. Descubrimiento (auto-detect tipo → codegraph blast radius → web si ambigüedad → baseline `/cleanCA <scope>`)
- **Tipo:** refactor firmas (flag arguments F2, cleanCA §2.2 + OCP §4.1). 6 firmas en 6 archivos, todas internas `pub`/`pub(crate)` (no bindings Python/TS/WASM las usan).
- **Blast radius (rg verificado 2026-09-12):**
  - `telemetry.rs:45` `init_telemetry_fmt(is_mcp,is_json,is_full)` + `:86` `_init_telemetry_otel` misma firma + `pub init_telemetry(is_mcp, log_format)`. Callers: `src/server/bootstrap.rs:285` (false), `vantadb-server/src/main.rs:56` (true). `is_json/is_full` derivan de `LogFormat` ya existente → redundantes.
  - `diagnostics.rs:79` `cmd_doctor(fix,force,verbose)`. Callers: `src/bin/vanta-cli.rs:187`, `tests/cli_tests.rs` (7 calls).
  - `backup.rs:232` `cmd_restore_dry_run(force,rebuild)` + `:317` `cmd_restore(force,rebuild,dry_run,verbose)`. Callers: `src/bin/vanta-cli.rs:184`, `tests/cli_tests.rs` (5 calls).
  - `rbac.rs:63` `can_access_namespace(write)` (`pub(crate)`). Callers: `src/server/middleware.rs:196`, `src/rbac.rs` tests, `tests/rbac_namespace.rs` (doc-comments).
  - `maintenance.rs:267` `consolidate_node_inner(lock_held)` (priv) + `:384` `evict_cold_nodes_inner(lock_held)` (priv, misma familia). Callers solo internos (`consolidate_node`/`_locked`, evict). Ya existen wrappers públicos sin bool.
  - `delete.rs:125` `apply_delete_inner(acquire)` (`pub(crate)`). Callers: `delete.rs:59`, `txn.rs:201` (ambos `false`).
- **Baseline:** F2 flag-arguments 🟡 en los 6 sitios. Sin ambigüedad → sin web research.
- **Decisión diseño (api-and-interface-design):** enums nuevos por dominio, cero bools en firmas objetivo; sin shims `bool` (prohibido dual eterno; todo es interno `pub(crate)`/binario CLI, no API pública estable → reemplazo directo, sin deprecación formal).

## 2. Contrato (qué cambia / qué NO cambia / archivos exactos / comandos de verify)
- **Cambia:**
  - `src/server/telemetry.rs`: `pub enum TelemetrySink { Stdout, Stderr }` + `init_telemetry(sink, log_format)` + internas `(sink, format, env_filter)`. Callers: `src/server/bootstrap.rs`, `vantadb-server/src/main.rs`.
  - `src/cli_handlers/diagnostics.rs`: `pub enum DoctorFix { Off, DryRun, Apply }` + `pub struct DoctorOptions { fix: DoctorFix, verbose: bool→Verbosity }` → `cmd_doctor(db_path, opts)`. Callers: `src/bin/vanta-cli.rs`, `tests/cli_tests.rs`. (verbose: `pub enum Verbosity { Normal, Verbose }` compartido en `cli_handlers/mod.rs` o local; backup lo reutiliza).
  - `src/cli_handlers/backup.rs`: `pub enum OverwritePolicy { FailIfExists, Overwrite }`, `pub enum IndexRebuild { No, Yes }`, `pub enum RestoreMode { DryRun, Apply }`, `pub struct RestoreOptions { overwrite, rebuild, mode, verbose }` → `cmd_restore(db_path,input,opts)` + helper dry_run interno con opts. Callers: `src/bin/vanta-cli.rs`, `tests/cli_tests.rs`.
  - `src/rbac.rs`: `pub enum AccessMode { Read, Write }` → `can_access_namespace(role, ns, mode)`. Callers: `src/server/middleware.rs`, tests `src/rbac.rs`, `tests/rbac_namespace.rs` si compila contra firma.
  - `src/storage/engine/maintenance.rs`: `enum LockPolicy { Acquire, AssumeHeld }` (priv, o `pub(crate)` compartido) → `consolidate_node_inner(policy)` + `evict_cold_nodes_inner(..., policy)`.
  - `src/storage/engine/delete.rs`: reutiliza `LockPolicy` → `apply_delete_inner(id, policy)`. Caller `txn.rs:201`.
- **NO cambia:** semántica/orden de efectos; wire HTTP; `get.rs`/`insert.rs`/`engine.rs`/`handlers.rs` (PROHIBIDO tocar); mensajes de error públicos; ningún bool "por compat" (sin shims).
- **Verify por slice:** `cargo nextest run -p vantadb --test <módulo>` + `cargo clippy -p vantadb --all-targets -- -D warnings` + `cargo fmt --check`. Cierre: `/cleanCA <6 archivos>` F2 ✅.
- **Herramientas:** bash directo (PROHIBIDO campaign MCP por adaptador §10).

## 3. Steps atómicos (☐ uno por slice: implementar → test → verificar → commit; si falla: `git reset --hard HEAD` del slice)
### Step 1: telemetry.rs — TelemetrySink + LogFormat ☐
- **Archivos:** `src/server/telemetry.rs`, `src/server/bootstrap.rs`, `vantadb-server/src/main.rs`
- **Acción:** enum sink; `init_telemetry(sink,..)`, internas `(sink, format, env_filter)`; actualizar 2 callers (`Stderr` en main MCP, `Stdout` en bootstrap).
- **Tests:** existentes `resolve_log_format_*` verdes (3/3 con `--features server`).
- **Verify:** nextest `--features server --lib resolve_log_format` 3/3 ✅ + clippy ✅ + fmt ✅ + `cargo check -p vantadb-server` ✅
- **Estado:** ✅ COMPLETED 2026-09-12 (sesión 1: enum + callers; sesión 2: verificado)
### Step 2: diagnostics.rs — DoctorOptions ☐
- **Archivos:** `src/cli_handlers/diagnostics.rs`, `src/bin/vanta-cli.rs`, `tests/cli_tests.rs`
- **Acción:** `DoctorFix` + `DoctorOptions{fix,verbose}` (+`Verbosity` compartido); `cmd_doctor(db, opts)`; mapear flags CLI → enums en vanta-cli; actualizar tests.
- **Tests:** tests/cli_tests doctor_* actualizados (helper `doc_opts`); 6/6 doctor ✅; binario cli_tests completo 84/84 ✅.
- **Verify:** nextest `--ignore-default-filter --test cli_tests` 84/84 ✅ + clippy ✅ + fmt ✅
- **Estado:** ✅ COMPLETED 2026-09-12
### Step 3: backup.rs — RestoreOptions ☐
- **Archivos:** `src/cli_handlers/backup.rs`, `src/bin/vanta-cli.rs`, `tests/cli_tests.rs`
- **Acción:** `OverwritePolicy/IndexRebuild/RestoreMode/RestoreOptions`; `cmd_restore(db,input,opts)`; dry_run helper con opts; mapear CLI; actualizar tests.
- **Tests:** tests/cli_tests restore_* actualizados (struct-update `..Default::default()`); 5/5 restore ✅; incluidos en 84/84 ✅.
- **Verify:** nextest restore 5/5 ✅ + clippy ✅ + fmt ✅
- **Estado:** ✅ COMPLETED 2026-09-12
### Step 4: rbac.rs — AccessMode ☐
- **Archivos:** `src/rbac.rs`, `src/server/middleware.rs`, `tests/rbac_namespace.rs` (si aplica)
- **Acción:** `pub enum AccessMode { Read, Write }`; firma `(role,ns,mode)`; middleware `is_write→mode`; tests unit actualizados.
- **Tests:** rbac unit ✅ (sweep 2059 passed) + `rbac_namespace --features server` 8/8 ✅; doc-comments actualizados a `AccessMode::`.
- **Verify:** nextest rbac + rbac_namespace 8/8 ✅ + clippy ✅ + fmt ✅
- **Estado:** ✅ COMPLETED 2026-09-12
### Step 5: maintenance.rs — LockPolicy ☐
- **Archivos:** `src/storage/engine/maintenance.rs` (+ `mod.rs` si enum compartido)
- **Acción:** `LockPolicy{Acquire,AssumeHeld}`; `consolidate_node_inner(policy)` + `evict_cold_nodes_inner(policy)`; wrappers mapean.
- **Tests:** existentes verdes sin cambio semántico: consolidate 10/10 ✅, evict 45/45 ✅ (+ misma familia `evict_cold_nodes_inner` migrada; wrappers públicos intactos, `insert.rs` NO tocado).
- **Verify:** nextest consolidate/evict ✅ + clippy ✅ + fmt ✅
- **Estado:** ✅ COMPLETED 2026-09-12
### Step 6: delete.rs — LockPolicy ☐
- **Archivos:** `src/storage/engine/delete.rs`, `src/storage/engine/txn.rs`
- **Acción:** `apply_delete_inner(id, LockPolicy::Acquire)`; actualizar 2 callers.
- **Tests:** delete 66/66 ✅ (incluye paths txn-commit con `AssumeHeld`); `txn.rs:201` verificado bajo lock (FIND-62) → `AssumeHeld` correcto.
- **Verify:** nextest delete 66/66 ✅ + clippy ✅ + fmt ✅
- **Estado:** ✅ COMPLETED 2026-09-12

## 4. Cierre (RESULTADO + `/cleanCA <scope>` PASS + recitation)
- RESULTADO con tabla antes→después + callers + tests (formato despacho D2).
- `/cleanCA` sobre los 6 archivos: F2 ✅. Recitation + next steps. Sin commit (lo ejecuta el lead).

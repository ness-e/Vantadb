# R8: Eliminar referencia colgante a skill typescript-expert en vanta-worker

## Metadata
- **Plan file:** docs/Backlog.md (P19, línea 469)
- **Creado:** 2026-08-16
- **last-synced:** 2026-08-16
- **Estado:** ✅ COMPLETED

## Blast Radius
- **Archivos:** `.opencode/agents/vanta-worker.md` (única línea §6)
- **Evidencia:** `SKILLS-MANIFEST.md:250` cataloga `typescript-expert` como REMOVED "Dangling - no SKILL.md on disk"; ECO-003 (task complete) planea `git rm` del dir vacío
- **Implicaciones:** la skill no existe en disco — cargarla con `skill <nombre>` fallaría; referencia muerta en el prompt del worker

## Impacto mapeado (Regla 0)
- **Archivos que se modifican:** `vanta-worker.md` — se elimina la línea `- \`typescript-expert\` — TypeScript SDK maintainer patterns` del §6
- **Skill de reemplazo:** la tarea original (Backlog.md:469) sugiere `source-driven-development` (validar contra docs oficiales al tocar el TS SDK). **Ya está presente** en el §6 del worker (línea ~119: `source-driven-development — verificar docs oficiales de librerías/frameworks antes de implementar`), añadida antes de esta secuencia. Reemplazarla duplicaría la entrada → viola la regla R5 "sin duplicar skills". Decisión: **eliminar la dangling sin duplicar**; la skill de reemplazo queda cubierta por la entrada existente.
- **Referencias entrantes a typescript-expert:** solo vanta-worker.md + task files/planes (ECO-003, plan p20) + SKILLS-MANIFEST (registro REMOVED, se mantiene). Sin otras referencias activas.
- **Referencias salientes:** ninguna
- **Impacto si se elimina:** cero — la skill no existe en disco, ninguna sesión puede cargarla
- **Git history:** working tree con R1+R5 aplicados; no tocar AUD-024.md ni docs/plans/

## Contrato
- grep "typescript-expert" en `.opencode/agents/` → 0 matches
- §6 del worker sigue teniendo `source-driven-development` (1 sola ocurrencia, sin duplicado)
- No se toca ninguna otra línea

## Steps
### Step 1: Verificar evidencia de dangling
- **Acción:** confirmar `SKILLS-MANIFEST.md:250` (REMOVED, sin SKILL.md en disco) + ausencia de dir con SKILL.md en `.agents/skills/typescript-expert/`
- **Verify:** manifest cataloga REMOVED; sin SKILL.md en disco ✅
- **Estado:** ✅ COMPLETED

### Step 2: Eliminar la línea dangling en vanta-worker.md §6
- **Archivos:** `vanta-worker.md`
- **Acción:** borrar `- \`typescript-expert\` — TypeScript SDK maintainer patterns`
- **Verify:** grep "typescript-expert" en `.opencode/agents/` → 0
- **Estado:** ✅ COMPLETED

### Step 3: Verificación contrato
- **Acción:** grep confirmar 0 referencias en agents/ + `source-driven-development` sigue 1× en worker §6
- **Verify:** contrato R8 pasa
- **Estado:** ✅ COMPLETED

## Dependencias
- R5 (regla "sin duplicar skills" aplica a la decisión de reemplazo)
- ECO-003 (task completa: planea git rm del dir vacío — NO es parte de esta tarea)

## Notas
- Decisión registrada: Backlog.md:469 sugería "reemplazar por source-driven-development" — ya listada en worker §6; se elimina solo la dangling (ponytail: el cambio mínimo correcto es borrar la referencia muerta, no duplicar).
- Al tocar el TS SDK, el worker debe validar contra docs oficiales (vía source-driven-development, ya obligatoria por R1).

## Context Save Point
- **Fecha:** 2026-08-16
- **Branch:** (working tree, sin commit — lead commitea por batch al cierre)
- **CI pendiente:** no aplica
- **Decisiones:** eliminar sin reemplazo duplicado (source-driven-development ya presente)
- **Problemas conocidos:** ninguno
- **Próxima tarea:** R9 (alinear bloques permission: con tablas MCP)
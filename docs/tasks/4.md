# TASK 4: GRAFO-01 — Bridge Tauri/WASM/TS de datos de grafo (bfs/dfs/filtered + degree)

## Metadata
- **Plan file:** `docs/plans/2026-08-18-vanta-studio-fase2.md`
- **Creado:** 2026-08-19T06:20
- **last-synced:** 2026-08-19T01:30
- **Estado:** ✅ COMPLETED (vanta-worker)
- **Ruta:** vanta-worker

## Blast Radius

**Callers → Callees → Implicaciones**

- Core (`src/sdk/graph.rs:50,67,89,113`, `src/sdk/gds.rs:32`) — ya implementados, READ-ONLY (protegido). Firma: `graph_bfs(&[u128], usize, TraversalDirection) -> Result<Vec<u128>>`; `graph_bfs_filtered(roots, max_depth, direction, labels: &[u32], time_range: Option<(u64,u64)>)`; `graph_degree_centrality(&[u128]) -> Result<HashMap<u128, (usize,usize)>>`.
- `VantaConnection` (desktop `trait.rs:21`) — se AÑADEN 3 métodos con default `Unsupported` (`graph_bfs`/`graph_dfs`/`graph_degree`) → impls existentes (Native/Server) NO rompen. `assert_object_safe` ya garantiza dyn-compat.
- `NativeConnection` (desktop `native.rs`) — overrides de los 3 + helpers de mapeo (`node_to_graph_dto`, `edge_to_graph_dto`, `parse_direction`). Reusa `blocking`/`map_core_error`/`node_record_to_memory` existentes. `db.graph_degree_centrality` vía `db.list` (node_ids del namespace).
- `ConnectionManager` (desktop `manager.rs`) — se AÑADEN `graph_bfs`/`graph_dfs`/`graph_degree` (patrón `query`, read lock). Test e2e nuevo (patrón `TempDir` existente).
- `commands/data.rs` + `lib.rs` — se AÑADEN 3 comandos (`vanta_graph_bfs`, `vanta_graph_dfs`, `vanta_graph_degree`) al `invoke_handler`. Aditivo.
- `desktop/src/vanta.ts` — se AÑADEN `graphBfs`/`graphDfs`/`graphDegree` + tipos `VantaGraphNodeInfo`/`VantaGraphEdgeInfo`/`VantaGraphTraversalResult`. Sigue patrón `queryIql`.
- `vantadb-wasm/src/lib.rs` — se AÑADEN `graph_filtered_traversal` (patrón `graph_bfs` + filter JsValue tipo `delete_by_filter`) y `graph_degree`. `graph_bfs`/`graph_dfs` ya existen.
- `vantadb-ts/src/vantadb.ts` + `types.ts` — se AÑADEN `graphFilteredTraversal`/`graphDegree` + tipos. Sigue patrón `graphBfs`.
- ServerConnection (desktop `server.rs`) — NO se toca; hereda default `Unsupported`.

**Implicaciones:** todo aditivo (nuevos métodos pub con defaults, nuevas fns, nuevos comandos). No rompe contratos. No toca `src/sdk/` (protegido), `wal.rs`, `vector/`, `storage/`.

## Impacto mapeado (Regla 0)

- **Archivos leídos completos (antes de editar):**
  - `src/sdk/graph.rs` (45-144: bfs/dfs/filtered/topo), `src/sdk/gds.rs` (1-37: degree_centrality), `src/sdk/api.rs` (30-89 insert/get/delete node, 590-634 list)
  - `desktop/src-tauri/src/connections/types.rs` (380-499 patrón tests roundtrip), `trait.rs` (184 líneas completas), `mod.rs` (31 líneas), `manager.rs` (1-120 + 225-299), `native.rs` (110-254 helpers blocking/from_vanta_value/node_record_to_memory), `commands/data.rs` (120-186), `lib.rs` (140-192 invoke_handler)
  - `desktop/src/vanta.ts` (1-120: tipos DTO + patrón invoke)
  - `vantadb-wasm/src/lib.rs` (1055-1119 filter pattern, 1315-1404 graph fns)
  - `vantadb-ts/src/vantadb.ts` (830-924 graph wrappers), `types.ts` (76-211 NodeRecord/EdgeRecord/Graph types)
- **Referencias hacia dentro:** ninguna existente — todos los símbolos nuevos.
- **Referencias entrantes:** `vanta.ts` consumido por componentes desktop (solo se agrega). Trait nuevos métodos — solo los implementa NativeConnection; Server hereda default. TS SDK wrappers nuevos consumen pkg wasm regenerado.
- **Veredicto:** cambio aditivo seguro. El plan protege `src/sdk/`; el core ya expone bfs/dfs/filtered/degree.

## Contrato

"`cargo check` + test roundtrip wire types + e2e bridge native + `npm run build` verde; exponer en WASM → TS → bridge Tauri: `graph_bfs`, `graph_dfs`, `graph_filtered_traversal` (WASM/TS) y `graph_degree`; wire DTO `VantaGraphTraversalResult { nodes: Vec<VantaGraphNodeInfo>, edges: Vec<VantaGraphEdgeInfo> }` en types.rs; `vanta_graph_degree(namespace, limit) -> Vec<VantaGraphNodeInfo>`; todos con `limit` (default 50); namespace vacío → lista vacía, no error."

Verificación mecánica:
1. `cargo test -p vantadb-desktop graph` (desktop workspace; tests: types roundtrip + manager e2e)
2. `cargo check -p vantadb-wasm` (root workspace)
3. `cargo check --workspace` (root) + `cargo check -p vantadb-desktop`
4. `npm run build` (desktop/) + `npm test` (vantadb-ts, tras regenerar pkg)
5. Cierre: `cargo fmt --check` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` (root) + clippy desktop

## Herramientas

- codegraph (blast radius), terminal cargo (root + desktop/src-tauri), npm (desktop/ + vantadb-ts), wasm-pack (regenerar pkg para tsc)

## Steps

### Step 1: DTOs wire en types.rs + re-export + tests roundtrip
- **Archivos:** `desktop/src-tauri/src/connections/types.rs`, `desktop/src-tauri/src/connections/mod.rs`
- **Acción:** DTOs `VantaGraphNodeInfo { id: String, label: String, group: Option<String>, degree: u64 }` (`#[serde(default)]` en group/degree), `VantaGraphEdgeInfo { source: String, target: String, label: Option<String>, weight: Option<f32> }`, `VantaGraphTraversalResult { nodes, edges }`. Tests roundtrip + wire shape (degree serializado, opcionales ausentes default). Re-export en mod.rs.
- **Verify:** `cargo test -p vantadb-desktop graph`
- **Estado:** ✅ COMPLETED

### Step 2: Trait `Connection::graph_bfs`/`graph_dfs`/`graph_degree` + impl Native
- **Archivos:** `desktop/src-tauri/src/connections/trait.rs`, `desktop/src-tauri/src/connections/native.rs`
- **Acción:** 3 métodos con default `Unsupported` (dirección como String "Forward"/"Reverse"/"Both"; `limit: Option<usize>`). Override Native: parse roots u128 → `db.graph_bfs`/`graph_dfs` en `blocking` → `get_node` por id (cap `limit` default 50) → DTOs (label de fields content/text/__vanta_payload, group de namespace/type, edges con source=id actual, target=edge.target, weight). `graph_degree`: `db.list(namespace, limit)` → node_ids → `db.graph_degree_centrality` → DTOs con degree=in+out. Namespace vacío → `Ok(vec![])` (no error).
- **Verify:** `cargo check -p vantadb-desktop`
- **Estado:** ✅ COMPLETED

### Step 3: Manager + test e2e roundtrip
- **Archivos:** `desktop/src-tauri/src/connections/manager.rs`
- **Acción:** `graph_bfs`/`graph_dfs`/`graph_degree` (patrón `query`, read lock). Test `e2e_native_graph_roundtrip`: connect native → put 3 records (A→B→C vía `add_edge`) → `graph_bfs(["nodeA"], 2)` → nodes con labels + edges; `graph_degree("ns", None)` → 3 nodos con degree>0.
- **Verify:** `cargo test -p vantadb-desktop e2e_native_graph`
- **Estado:** ✅ COMPLETED

### Step 4: Comandos Tauri + registro
- **Archivos:** `desktop/src-tauri/src/commands/data.rs`, `desktop/src-tauri/src/lib.rs`
- **Acción:** `vanta_graph_bfs(roots: Vec<String>, max_depth: usize, direction: String, limit: Option<usize>)` + `vanta_graph_dfs(...)` + `vanta_graph_degree(namespace: String, limit: Option<usize>)` → manager. Registrar los 3 en `invoke_handler`.
- **Verify:** `cargo check -p vantadb-desktop`
- **Estado:** ✅ COMPLETED

### Step 5: Wrapper `vanta.ts`
- **Archivos:** `desktop/src/vanta.ts`
- **Acción:** Tipos `VantaGraphNodeInfo`/`VantaGraphEdgeInfo`/`VantaGraphTraversalResult` + `graphBfs(roots, maxDepth, direction, limit)`/`graphDfs(...)`/`graphDegree(namespace, limit)` siguiendo `queryIql`.
- **Verify:** `npm run build` (desktop/)
- **Estado:** ✅ COMPLETED

### Step 6: WASM `graph_filtered_traversal` + `graph_degree`
- **Archivos:** `vantadb-wasm/src/lib.rs`
- **Acción:** `graph_filtered_traversal(roots, max_depth, direction, filter: JsValue)` — filter opcional `{labels?: number[], time_range?: [number, number]}` (None/null → sin filtro), delega a `graph_bfs_filtered`. `graph_degree(roots: Vec<String>)` → `graph_degree_centrality` → `Vec<{id: String, in_degree, out_degree}>` (u128 no serializa en JSON maps — transformar a entries).
- **Verify:** `cargo check -p vantadb-wasm`
- **Estado:** ✅ COMPLETED

### Step 7: TS SDK wrappers + types + tests
- **Archivos:** `vantadb-ts/src/vantadb.ts`, `vantadb-ts/src/types.ts`, tests
- **Acción:** `graphFilteredTraversal(roots, maxDepth, direction, filter?)` (misma shape `GraphBfsResult`) + `graphDegree(roots): GraphDegreeEntry[]`; tipos `GraphTraversalFilter`/`GraphDegreeEntry` en types.ts; tests unit en `vanta.test.ts`. Regenerar `vantadb-wasm/pkg` (wasm-pack build --dev) para que tsc pase.
- **Verify:** `npm test` + `npx tsc --noEmit` (vantadb-ts)
- **Estado:** ✅ COMPLETED

### Step 8: Cierre — verify full + plan file
- **Acción:** fmt/clippy/nextest (root + desktop) + docs coverage; marcar Task 4 ✅ COMPLETED en plan file; actualizar task file + Context Save Point. NO commit (lo hace vanta-lead).
- **Verify:** `campaign_verify_cmd` por cada gate
- **Estado:** ✅ COMPLETED

## Dependencias
- Ninguna (Wave 1; Task 5 y 6 consumen este bridge, no al revés)

## Notas
- DTO `VantaGraphNodeInfo` incluye `degree: u64` (serde default) para que `graph_degree` devuelva la centralidad en el mismo shape — el orquestador lo pidió explícitamente.
- Dirección como `String` ("Forward"/"Reverse"/"Both") en el bridge, parseada a `TraversalDirection` en native (mismo patrón que WASM). node ids como `String` en el wire (precisión JS u128).
- `graph_filtered_traversal` SOLO WASM/TS (web) — el desktop no lo expone (trait tiene 3 métodos). El visor desktop usa bfs/dfs plano; el filtro por labels/time es para web.
- u128 keys en `HashMap<u128, (usize,usize)>` no serializan en serde_wasm_bindgen maps → WASM transforma a `Vec<{id: String, ...}>`.
- Lección VS-CORE-05: tras agregar métodos a `vantadb-wasm`, regenerar `pkg` antes de tsc del TS SDK.
- **Baseline TS roto (PRE-EXISTENTE, no de este PR):** `npm test` (vantadb-ts) falla 19 tests en 5 archivos — todos en el dominio vector/search/load (`putBatch with vectors`, `5000 vectors`, `concurrent puts across keys`, `search`, etc.) con error `put: unreachable` (panic `unreachable!()` del runtime WASM en vitest/Node.js). Documentado en `docs/avance/historial/snapshot-2026-08-07.md:2760` ("bug pre-existente del runtime WASM, no bloquea el publish"). Mis 11 tests graph pasan (`npx vitest run src/__tests__/vanta.test.ts -t "graph"` → 11 passed). Aislar con Issue `flaky`/bug si el orquestador lo prioriza.

## Context Save Point
- **Fecha:** 2026-08-19T01:30
- **Branch:** (worktree actual, sin commit — vanta-lead commitea)
- **CI pendiente:** no aplica (work in progress)
- **Decisiones:** (ver Notas)
- **Problemas conocidos:** clippy `--all-targets` falla en archivos PRE-EXISTENTES no tocados (drift toolchain, ver task 1.md) — mismo criterio: mi diff debe ser clippy-clean. Baseline TS 19 fallos pre-existentes `unreachable!()` (ver Notas).
- **Próxima tarea:** Task 5 (GRAFO-02 — visor R3F) / Task 6 (GRAFO-03 — IQL console)

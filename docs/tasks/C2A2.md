# C2A2: Convención ligera nombres utilities web (prefijo + `--modificador`, sin BEM total)

## Metadata
- **Plan file:** `docs/plans/2026-09-13-cleanCA-fase2.md` (Wave 4, Task 15)
- **Creado:** 2026-09-13
- **last-synced:** 2026-09-13
- **Estado:** ✅ COMPLETED (5/5 steps; NO commiteado — commitea el lead)
- **Ruta:** vanta-worker (frontend)
- **nextTask:** C2M3

## Gate D — GO
- **Blast radius:** 1 CSS + ~50 tsx (renames mecánicos 1-token, cero semántica) + 1 regla doc. Sin hot path, sin API pública, sin símbolos `pub` nuevos. Cambio cosmético-naming puro.
- **Contrato mecánico:** orphan-check + tabla renames + norma 10 líneas + build+lint verdes. Sin ambigüedad tras DISCOVERY.
- **Veredicto:** GO (adaptador §10 vigente lo confirma). Sin `question` al usuario.

## DISCOVERY (SDP + orphan-check)

### Skills (SKILLS_CARGADAS)
`campaign-executor, progreso, systematic-debugging, test-driven-development, code-review-and-quality, source-driven-development, planning-and-task-breakdown, codebase-memory, writing-guidelines, documentation-and-adrs` (10/10 vía tool `skill`).

### Orphan-check (rg exacto contra `web/src`; `public/` no existe en repo)
| Clase candidata | Usos en `web/src` | Veredicto |
|---|---|---|
| `speed-lines-radial` | 0 (solo def en `globals.css:180`) | ✅ HUÉRFANA → eliminar def |
| `halftone-fade` | 6 (benchmarks-view, docs-view, core-engine, hero, page-header, use-cases) | usada → singleton, sin rename |
| `text-stencil` | 4 (cta-final, easter-egg, hero ×2) | usada → singleton, sin rename |
| `ink-divider` | 1 (vía `ink-divider.tsx`) | usada → singleton, sin rename |
| `stagger-children` | 1 (trust-section) | usada → singleton, sin rename |
| `animated-gradient-border` | 1 (footer) | usada → singleton, sin rename |
| `scanlines` | 3 (cta-final, solutions×2) | usada → singleton, sin rename |
| `neon-underline` | 4 (footer ×4) | usada → singleton, sin rename |
| `.press` base | ~40+ archivos | usada → base sin sufijo, sin rename |

### Doctrina (del plan, se documenta, no es bug)
- `animate-*` envuelve `vanta-*` (keyframes son detalle de implementación).
- `glow-neon` (texto, `text-shadow`) vs `glow-box-neon` (caja, `box-shadow`+anim) son ejes distintos.
- `prefijo--modificador` SOLO en familias con variantes (`press/glow/animate`). BEM total NO paga en Tailwind → singletons conservan nombre plano.

### Tabla de renames (únicos cambios de nombre)
| Antes | Después | Alcance |
|---|---|---|
| `.press-lg` (+`:hover`/`:active`) | `.press--lg` | `globals.css:129-141` + ~28 tsx (~40 ocurrencias) |
| `.press-neon` (+`:hover`/`:active`) | `.press--neon` | `globals.css:143-156` + ~22 tsx (~24 ocurrencias) |
| `.glow-neon` | `.glow--neon` | `globals.css:483` + benchmarks-view, cta-final, hero |
| `.glow-box-neon` | `.glow-box--neon` | `globals.css:489` + changelog-section |
| `.speed-lines-radial` (bloque 180-190) | ELIMINADA | solo `globals.css` (huérfana) |
| Resto (28 clases custom + 10 keyframes `vanta-*`) | SIN CAMBIO | singletons + `animate-*` + keyframes |

### Seguridad rename
- Sin `className` dinámicos construidos por template sobre estas clases (`rg '\$\{[^}]*(press|animate|glow)'` → 0). Solo excepción: `architecture.tsx:217` y `core-engine.tsx:288` interpolan `styles`/`className` props pero el token `press-lg` es literal estático → replace mecánico seguro.
- Tests: `web/e2e/*.spec.ts` usan solo roles/labels visibles (0 referencias a clases custom) → Playwright smoke N/A (condición "si toca clases en tests" no se cumple).

## Impacto mapeado (Regla 0)
- **Archivos leídos completos:** `web/src/app/globals.css` (651 líneas), `.opencode/rules/frontend-web.md` (47 líneas), plan Task 15, `web/e2e/flujo-critico.spec.ts`, `web/e2e/web09-screenshots.spec.ts`, `web/package.json` (scripts).
- **Referencias hacia dentro (globals.css):** ninguna (CSS leaf; `@theme inline` + `@layer utilities` + keyframes; sin imports salientes salvo tailwind).
- **Referencias entrantes:** ~50 tsx vía `className` literales; `ink-divider.tsx` re-exporta `.ink-divider` (no renombrada); `skip-link.tsx` usa `.skip-link` (no renombrada).
- **Veredicto:** impacto cosmético-naming, reversible por `git checkout`. PROHIBIDO: cambiar diseño/comportamiento, resto del repo, WIP ajeno.

## Contrato
"orphan-check primero + tabla renames + norma 10 líneas en `.opencode/rules/frontend-web.md` (R-FE-7) + `npm run build` + `npm run lint` en `web/` verdes + Playwright smoke solo si toca clases en tests (no toca → N/A documentado)"

## Herramientas
- bash: `rg` orphan-check, script de rename mecánico, `npm run build`, `npm run lint` en `web/`
- Read/Edit/Write para CSS + norma

## Steps
### Step 1: DISCOVERY — orphan-check + tabla + task file
- **Archivos:** este file
- **Acción:** rg por clase candidata, doctrina, tabla renames, Gate D GO
- **Verify:** tabla completa con evidencia de conteos
- **Estado:** ✅ COMPLETED

### Step 2: CSS — eliminar huérfana + 4 renames en `globals.css`
- **Archivos:** `web/src/app/globals.css`
- **Acción:** borrar bloque `.speed-lines-radial` (180-190); `.press-lg`→`.press--lg`, `.press-neon`→`.press--neon`, `.glow-neon`→`.glow--neon`, `.glow-box-neon`→`.glow-box--neon`
- **Verify:** `rg 'press-lg|press-neon|glow-neon|glow-box-neon|speed-lines-radial' web/src/app/globals.css` → 0 + 8 selectores nuevos ✅
- **Estado:** ✅ COMPLETED

### Step 3: classNames — rename mecánico en tsx (~50 archivos)
- **Archivos:** `web/src/**/*.tsx` (lista en §DISCOVERY)
- **Acción:** replace literal exacto de los 4 tokens (orden: `glow-box-neon` primero; literales estáticos, sin templates dinámicos)
- **Verify:** `rg 'press-lg|press-neon|glow-neon' web/src` → 0; diff 38 files, solo 1-token ✅
- **Estado:** ✅ COMPLETED

### Step 4: Norma R-FE-7 (10 líneas) en `.opencode/rules/frontend-web.md`
- **Archivos:** `.opencode/rules/frontend-web.md`
- **Acción:** añadir R-FE-7 formato Must/Must-not/Por-qué (doctrina animate⊃vanta, ejes glow texto/caja, `--modificador` solo familias con variantes, huérfanas se eliminan)
- **Verify:** 6 líneas añadidas (R-FE-7), formato Must/Must-not/Por-qué ✅
- **Estado:** ✅ COMPLETED

### Step 5: Verify contrato — build + lint web verdes
- **Archivos:** `web/` (verificación, sin edición)
- **Acción:** `npm run build` + `npm run lint` en `web/`; Playwright smoke N/A (evidencia: 0 clases custom en `web/e2e/`)
- **Verify:** build 36/36 exit 0 + lint exit 0 ✅; smoke N/A (0 clases custom en `web/e2e/`)
- **Estado:** ✅ COMPLETED

## Dependencias
- Wave 4, sin dependencias. nextTask: C2M3.

## Notas
- `.press` base, singletons, `animate-*`, keyframes `vanta-*`: SIN CAMBIO (diseño/comportamiento intactos).
- `public/` no existe en el repo → orphan-check contra `web/src` cubre el 100% del consumo real.

## Context Save Point
- **Fecha:** 2026-09-13
- **Branch:** develop (verificar con `git status` antes de cerrar; NO commitear — commitea el lead)
- **Decisiones:** convención ligera (no BEM total); huérfana eliminada no renombrada; smoke N/A
- **Problemas conocidos:** ninguno
- **Próxima tarea:** C2M3

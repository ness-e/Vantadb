#compdef vanta-cli

autoload -U is-at-least

_vanta-cli() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_vanta-cli_commands" \
"*::: :->vanta-cli" \
&& ret=0
    case $state in
    (vanta-cli)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-command-$line[1]:"
        case $line[1] in
            (put)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace for the key]:NAMESPACE:_default' \
'--key=[Key to store the value under]:KEY:_default' \
'--payload=[Value to store (payload text)]:PAYLOAD:_default' \
'--vector=[Optional vector embedding (comma-separated f32 values)]:VECTOR:_default' \
'--metadata=[Optional metadata as a JSON object, e.g. '\''{"k"\:"v","n"\:1}'\'']:METADATA:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(get)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace for the key]:NAMESPACE:_default' \
'--key=[Key to retrieve the value for]:KEY:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace to list]:NAMESPACE:_default' \
'--limit=[Maximum number of records to return]:LIMIT:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(rebuild-index)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(audit-index)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Optional namespace to audit (audits all if not specified)]:NAMESPACE:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--json[Output results as JSON]' \
'--deep[Perform deep structural validation]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(repair-text-index)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(export)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Optional namespace to export (exports all if not specified)]:NAMESPACE:_default' \
'--out=[Output path\: file for \`--format jsonl\` (default), directory for \`--format md\`]:OUT:_default' \
'--format=[Export format. \`jsonl\` writes one record per line to a file (default, backwards-compatible). \`md\` writes one file per record under \`<out>/<namespace>/<key>.md\` with JSON frontmatter; the directory is git-friendly and round-trips with \`vanta-seed import-md\`]:FORMAT:((jsonl\:"JSONL on stdout/file (default; backwards compatible)"
md\:"One Markdown file per record under \`<out>/<namespace>/<key>.md\` with JSON frontmatter metadata; round-trips with \`vanta-seed import-md\`"))' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(import)
_arguments "${_arguments_options[@]}" : \
'--input=[Input file path]:in:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(query)
_arguments "${_arguments_options[@]}" : \
'--limit=[Maximum results to return]:LIMIT:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
':query -- Query string:_default' \
&& ret=0
;;
(status)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(backup)
_arguments "${_arguments_options[@]}" : \
'--out=[Output directory for the backup (default\: \`vantadb_backups/backup_<timestamp>\`)]:OUT:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(restore)
_arguments "${_arguments_options[@]}" : \
'--input=[Path to the backup directory]:INPUT:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--force[Overwrite existing database directory if it exists]' \
'--rebuild[Rebuild indexes after restore]' \
'--dry-run[Validate the backup without restoring (dry-run). Lists what would be restored and exits 0 without touching the target]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(doctor)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--fix[Apply safe repairs (create missing data directories). Without --force this only lists what would be fixed (dry-run)]' \
'--force[Actually apply the repairs listed by --fix (without it --fix is a dry-run)]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(inspect)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace of the record]:NAMESPACE:_default' \
'--key=[Key of the record to inspect]:KEY:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(stats)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--json[Output statistics as JSON]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(tui)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'--shell=[Shell type for the completion script]:SHELL:((bash\:"Bash shell completions"
zsh\:"Zsh shell completions"
fish\:"Fish shell completions"
powershell\:"PowerShell shell completions"))' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(search)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace to search within]:NAMESPACE:_default' \
'--query=[Text query for semantic/hybrid search]:QUERY:_default' \
'--query-vector=[Optional explicit vector query (comma-separated f32 values)]:QUERY_VECTOR:_default' \
'--limit=[Maximum number of results]:LIMIT:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--json[Output in JSON format]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(delete)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace of the record]:NAMESPACE:_default' \
'--key=[Key of the record to delete]:KEY:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(delete-by-filter)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace to operate on]:NAMESPACE:_default' \
'--filter=[JSON filter in MongoDB-like format, e.g. '\''{"field"\: {"\$op"\: value}}'\'' Operators\: \$eq, \$neq, \$gt, \$gte, \$lt, \$lte Example\: '\''{"status"\: {"\$eq"\: "inactive"}}'\'']:FILTER:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(count)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace to count records in]:NAMESPACE:_default' \
'--filter=[Optional JSON filter (same format as delete-by-filter)]:FILTER:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--json[Output as raw number only]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(similar-to-key)
_arguments "${_arguments_options[@]}" : \
'--namespace=[Namespace of the reference record]:NAMESPACE:_default' \
'--key=[Key of the reference record]:KEY:_default' \
'--top-k=[Number of similar records to return]:TOP_K:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--json[Output in JSON format]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(migrate)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
":: :_vanta-cli__subcmd__migrate_commands" \
"*::: :->migrate" \
&& ret=0

    case $state in
    (migrate)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-migrate-command-$line[1]:"
        case $line[1] in
            (plan)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
':target -- Path to the database directory:_default' \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
'--format=[Specific format to migrate (vfile, index, wal, schema, all)]:FORMAT:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--dry-run[Preview changes without modifying files]' \
'--force[Skip confirmation prompts]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
':target -- Path to the database directory:_default' \
&& ret=0
;;
(check)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
':target -- Path to the database directory:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__migrate__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-migrate-help-command-$line[1]:"
        case $line[1] in
            (plan)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(check)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(namespace)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
":: :_vanta-cli__subcmd__namespace_commands" \
"*::: :->namespace" \
&& ret=0

    case $state in
    (namespace)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-namespace-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(info)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
':namespace -- Namespace to inspect:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__namespace__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-namespace-help-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(info)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(snapshot)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
":: :_vanta-cli__subcmd__snapshot_commands" \
"*::: :->snapshot" \
&& ret=0

    case $state in
    (snapshot)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-snapshot-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
':name -- Name for the snapshot:_default' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__snapshot__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-snapshot-help-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(wal)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
":: :_vanta-cli__subcmd__wal_commands" \
"*::: :->wal" \
&& ret=0

    case $state in
    (wal)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-wal-command-$line[1]:"
        case $line[1] in
            (compact)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(vacuum)
_arguments "${_arguments_options[@]}" : \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__wal__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-wal-help-command-$line[1]:"
        case $line[1] in
            (compact)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(vacuum)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(search-multi)
_arguments "${_arguments_options[@]}" : \
'--namespaces=[Comma-separated list of namespaces to search (e.g. "ns1,ns2,ns3")]:NAMESPACES:_default' \
'--query=[Text query for hybrid/lexical search]:QUERY:_default' \
'--query-vector=[Optional explicit vector query (comma-separated f32 values)]:QUERY_VECTOR:_default' \
'--top-k=[Maximum number of results across all namespaces]:TOP_K:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--json[Output in JSON format]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(search-all)
_arguments "${_arguments_options[@]}" : \
'--query=[Text query for hybrid/lexical search]:QUERY:_default' \
'--query-vector=[Optional explicit vector query (comma-separated f32 values)]:QUERY_VECTOR:_default' \
'--top-k=[Maximum number of results across all namespaces]:TOP_K:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--json[Output in JSON format]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(server)
_arguments "${_arguments_options[@]}" : \
'-p+[Port for the HTTP server]:PORT:_default' \
'--port=[Port for the HTTP server]:PORT:_default' \
'--host=[Host for the HTTP server]:HOST:_default' \
'--dashboard-dir=[Directory of static files to serve at /dashboard (Vanta Studio web console). When unset, /dashboard responds 404 with a hint]:DASHBOARD_DIR:_default' \
'-d+[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--db=[Path to the database directory. Defaults to the value of the VANTADB_STORAGE_PATH environment variable, or '\''./db'\'' if neither is set]:DB:_default' \
'--memory-limit=[Optional memory limit for the database engine, in bytes. Accepts suffixes\: KB, MB, GB (also KiB, MiB, GiB), e.g. \`500MB\` or \`2GB\`. Defaults to the value of the VANTADB_MEMORY_LIMIT environment variable]:MEMORY_LIMIT:_default' \
'--http[Start HTTP server wrapper (default)]' \
'--mcp[Start MCP server wrapper over stdio]' \
'--require-auth[Force authentication\: refuse to start without an API key]' \
'--allow-insecure[Allow binding a non-loopback host without an API key (dev only). The server logs a prominent security warning and starts unauthenticated]' \
'-v[Enable verbose output]' \
'--verbose[Enable verbose output]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-help-command-$line[1]:"
        case $line[1] in
            (put)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(get)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(rebuild-index)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(audit-index)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(repair-text-index)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(export)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(import)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(query)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(backup)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(restore)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(doctor)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(stats)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(tui)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(search)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(delete)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(delete-by-filter)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(count)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(similar-to-key)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(migrate)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__help__subcmd__migrate_commands" \
"*::: :->migrate" \
&& ret=0

    case $state in
    (migrate)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-help-migrate-command-$line[1]:"
        case $line[1] in
            (plan)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(check)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(namespace)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__help__subcmd__namespace_commands" \
"*::: :->namespace" \
&& ret=0

    case $state in
    (namespace)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-help-namespace-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(info)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(snapshot)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__help__subcmd__snapshot_commands" \
"*::: :->snapshot" \
&& ret=0

    case $state in
    (snapshot)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-help-snapshot-command-$line[1]:"
        case $line[1] in
            (create)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(wal)
_arguments "${_arguments_options[@]}" : \
":: :_vanta-cli__subcmd__help__subcmd__wal_commands" \
"*::: :->wal" \
&& ret=0

    case $state in
    (wal)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:vanta-cli-help-wal-command-$line[1]:"
        case $line[1] in
            (compact)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(vacuum)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(search-multi)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(search-all)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(server)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_vanta-cli_commands] )) ||
_vanta-cli_commands() {
    local commands; commands=(
'put:Save a key-value pair to persistent memory' \
'get:Retrieve a value from persistent memory' \
'list:List keys and values in a namespace' \
'rebuild-index:Rebuild all database indexes (HNSW, text index, derived indexes)' \
'audit-index:Validate text index integrity without repairing' \
'repair-text-index:Repair text index if inconsistencies are detected' \
'export:Export records to a file (jsonl) or to a directory of Markdown files with JSON frontmatter (--format md, git-friendly, round-trips with \`vanta-seed import-md\`)' \
'import:Import records from a JSON file' \
'query:Execute a structured query (IQL/hybrid)' \
'status:Display database health diagnostics and system status' \
'backup:Create a filesystem-level backup of the database directory' \
'restore:Restore the database from a previously created backup directory' \
'doctor:Run comprehensive health diagnostics on the database' \
'inspect:Inspect a single record showing all fields, vectors, and metadata' \
'stats:Display detailed database statistics in human-readable or JSON format' \
'tui:Launch the interactive TUI (requires \`tui\` feature)' \
'completions:Generate shell completion scripts' \
'search:Search records semantically across a namespace' \
'delete:Delete a record by namespace and key' \
'delete-by-filter:Delete all records in a namespace matching a JSON metadata filter' \
'count:Count records in a namespace, optionally filtered by metadata' \
'similar-to-key:Find records similar to a given key using vector similarity search' \
'migrate:Migrate a database to the latest storage schema version' \
'namespace:Manage namespaces' \
'snapshot:Manage instant filesystem snapshots' \
'wal:Manage the Write-Ahead Log (compact, vacuum)' \
'search-multi:Search across multiple namespaces and merge results by score' \
'search-all:Search across ALL known namespaces and merge results by score' \
'server:Start the HTTP or MCP server wrapper' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__audit-index_commands] )) ||
_vanta-cli__subcmd__audit-index_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli audit-index commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__backup_commands] )) ||
_vanta-cli__subcmd__backup_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli backup commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__completions_commands] )) ||
_vanta-cli__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli completions commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__count_commands] )) ||
_vanta-cli__subcmd__count_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli count commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__delete_commands] )) ||
_vanta-cli__subcmd__delete_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli delete commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__delete-by-filter_commands] )) ||
_vanta-cli__subcmd__delete-by-filter_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli delete-by-filter commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__doctor_commands] )) ||
_vanta-cli__subcmd__doctor_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli doctor commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__export_commands] )) ||
_vanta-cli__subcmd__export_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli export commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__get_commands] )) ||
_vanta-cli__subcmd__get_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli get commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help_commands] )) ||
_vanta-cli__subcmd__help_commands() {
    local commands; commands=(
'put:Save a key-value pair to persistent memory' \
'get:Retrieve a value from persistent memory' \
'list:List keys and values in a namespace' \
'rebuild-index:Rebuild all database indexes (HNSW, text index, derived indexes)' \
'audit-index:Validate text index integrity without repairing' \
'repair-text-index:Repair text index if inconsistencies are detected' \
'export:Export records to a file (jsonl) or to a directory of Markdown files with JSON frontmatter (--format md, git-friendly, round-trips with \`vanta-seed import-md\`)' \
'import:Import records from a JSON file' \
'query:Execute a structured query (IQL/hybrid)' \
'status:Display database health diagnostics and system status' \
'backup:Create a filesystem-level backup of the database directory' \
'restore:Restore the database from a previously created backup directory' \
'doctor:Run comprehensive health diagnostics on the database' \
'inspect:Inspect a single record showing all fields, vectors, and metadata' \
'stats:Display detailed database statistics in human-readable or JSON format' \
'tui:Launch the interactive TUI (requires \`tui\` feature)' \
'completions:Generate shell completion scripts' \
'search:Search records semantically across a namespace' \
'delete:Delete a record by namespace and key' \
'delete-by-filter:Delete all records in a namespace matching a JSON metadata filter' \
'count:Count records in a namespace, optionally filtered by metadata' \
'similar-to-key:Find records similar to a given key using vector similarity search' \
'migrate:Migrate a database to the latest storage schema version' \
'namespace:Manage namespaces' \
'snapshot:Manage instant filesystem snapshots' \
'wal:Manage the Write-Ahead Log (compact, vacuum)' \
'search-multi:Search across multiple namespaces and merge results by score' \
'search-all:Search across ALL known namespaces and merge results by score' \
'server:Start the HTTP or MCP server wrapper' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__audit-index_commands] )) ||
_vanta-cli__subcmd__help__subcmd__audit-index_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help audit-index commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__backup_commands] )) ||
_vanta-cli__subcmd__help__subcmd__backup_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help backup commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__completions_commands] )) ||
_vanta-cli__subcmd__help__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help completions commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__count_commands] )) ||
_vanta-cli__subcmd__help__subcmd__count_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help count commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__delete_commands] )) ||
_vanta-cli__subcmd__help__subcmd__delete_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help delete commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__delete-by-filter_commands] )) ||
_vanta-cli__subcmd__help__subcmd__delete-by-filter_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help delete-by-filter commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__doctor_commands] )) ||
_vanta-cli__subcmd__help__subcmd__doctor_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help doctor commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__export_commands] )) ||
_vanta-cli__subcmd__help__subcmd__export_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help export commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__get_commands] )) ||
_vanta-cli__subcmd__help__subcmd__get_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help get commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__help_commands] )) ||
_vanta-cli__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__import_commands] )) ||
_vanta-cli__subcmd__help__subcmd__import_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help import commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__inspect_commands] )) ||
_vanta-cli__subcmd__help__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help inspect commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__list_commands] )) ||
_vanta-cli__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__migrate_commands] )) ||
_vanta-cli__subcmd__help__subcmd__migrate_commands() {
    local commands; commands=(
'plan:Plan migrations that would be performed' \
'run:Run migrations to bring formats up to date' \
'check:Check storage integrity for all formats' \
    )
    _describe -t commands 'vanta-cli help migrate commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__migrate__subcmd__check_commands] )) ||
_vanta-cli__subcmd__help__subcmd__migrate__subcmd__check_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help migrate check commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__migrate__subcmd__plan_commands] )) ||
_vanta-cli__subcmd__help__subcmd__migrate__subcmd__plan_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help migrate plan commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__migrate__subcmd__run_commands] )) ||
_vanta-cli__subcmd__help__subcmd__migrate__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help migrate run commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__namespace_commands] )) ||
_vanta-cli__subcmd__help__subcmd__namespace_commands() {
    local commands; commands=(
'list:List all namespaces' \
'info:Show record count and details for a namespace' \
    )
    _describe -t commands 'vanta-cli help namespace commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__namespace__subcmd__info_commands] )) ||
_vanta-cli__subcmd__help__subcmd__namespace__subcmd__info_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help namespace info commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__namespace__subcmd__list_commands] )) ||
_vanta-cli__subcmd__help__subcmd__namespace__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help namespace list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__put_commands] )) ||
_vanta-cli__subcmd__help__subcmd__put_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help put commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__query_commands] )) ||
_vanta-cli__subcmd__help__subcmd__query_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help query commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__rebuild-index_commands] )) ||
_vanta-cli__subcmd__help__subcmd__rebuild-index_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help rebuild-index commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__repair-text-index_commands] )) ||
_vanta-cli__subcmd__help__subcmd__repair-text-index_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help repair-text-index commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__restore_commands] )) ||
_vanta-cli__subcmd__help__subcmd__restore_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help restore commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__search_commands] )) ||
_vanta-cli__subcmd__help__subcmd__search_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help search commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__search-all_commands] )) ||
_vanta-cli__subcmd__help__subcmd__search-all_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help search-all commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__search-multi_commands] )) ||
_vanta-cli__subcmd__help__subcmd__search-multi_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help search-multi commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__server_commands] )) ||
_vanta-cli__subcmd__help__subcmd__server_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help server commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__similar-to-key_commands] )) ||
_vanta-cli__subcmd__help__subcmd__similar-to-key_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help similar-to-key commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__snapshot_commands] )) ||
_vanta-cli__subcmd__help__subcmd__snapshot_commands() {
    local commands; commands=(
'create:Create an instant filesystem snapshot by hard-linking all data files' \
'list:List all existing snapshots' \
    )
    _describe -t commands 'vanta-cli help snapshot commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__snapshot__subcmd__create_commands] )) ||
_vanta-cli__subcmd__help__subcmd__snapshot__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help snapshot create commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__snapshot__subcmd__list_commands] )) ||
_vanta-cli__subcmd__help__subcmd__snapshot__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help snapshot list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__stats_commands] )) ||
_vanta-cli__subcmd__help__subcmd__stats_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help stats commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__status_commands] )) ||
_vanta-cli__subcmd__help__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help status commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__tui_commands] )) ||
_vanta-cli__subcmd__help__subcmd__tui_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help tui commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__wal_commands] )) ||
_vanta-cli__subcmd__help__subcmd__wal_commands() {
    local commands; commands=(
'compact:Compact the WAL\: flush all data, archive the current WAL file, and start a fresh one' \
'vacuum:Remove tombstoned nodes from HNSW and reclaim space' \
    )
    _describe -t commands 'vanta-cli help wal commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__wal__subcmd__compact_commands] )) ||
_vanta-cli__subcmd__help__subcmd__wal__subcmd__compact_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help wal compact commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__help__subcmd__wal__subcmd__vacuum_commands] )) ||
_vanta-cli__subcmd__help__subcmd__wal__subcmd__vacuum_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli help wal vacuum commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__import_commands] )) ||
_vanta-cli__subcmd__import_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli import commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__inspect_commands] )) ||
_vanta-cli__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli inspect commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__list_commands] )) ||
_vanta-cli__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate_commands] )) ||
_vanta-cli__subcmd__migrate_commands() {
    local commands; commands=(
'plan:Plan migrations that would be performed' \
'run:Run migrations to bring formats up to date' \
'check:Check storage integrity for all formats' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli migrate commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__check_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__check_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli migrate check commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__help_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__help_commands() {
    local commands; commands=(
'plan:Plan migrations that would be performed' \
'run:Run migrations to bring formats up to date' \
'check:Check storage integrity for all formats' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli migrate help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__help__subcmd__check_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__help__subcmd__check_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli migrate help check commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__help__subcmd__help_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli migrate help help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__help__subcmd__plan_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__help__subcmd__plan_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli migrate help plan commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__help__subcmd__run_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__help__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli migrate help run commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__plan_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__plan_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli migrate plan commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__migrate__subcmd__run_commands] )) ||
_vanta-cli__subcmd__migrate__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli migrate run commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__namespace_commands] )) ||
_vanta-cli__subcmd__namespace_commands() {
    local commands; commands=(
'list:List all namespaces' \
'info:Show record count and details for a namespace' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli namespace commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__namespace__subcmd__help_commands] )) ||
_vanta-cli__subcmd__namespace__subcmd__help_commands() {
    local commands; commands=(
'list:List all namespaces' \
'info:Show record count and details for a namespace' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli namespace help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__namespace__subcmd__help__subcmd__help_commands] )) ||
_vanta-cli__subcmd__namespace__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli namespace help help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__namespace__subcmd__help__subcmd__info_commands] )) ||
_vanta-cli__subcmd__namespace__subcmd__help__subcmd__info_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli namespace help info commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__namespace__subcmd__help__subcmd__list_commands] )) ||
_vanta-cli__subcmd__namespace__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli namespace help list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__namespace__subcmd__info_commands] )) ||
_vanta-cli__subcmd__namespace__subcmd__info_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli namespace info commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__namespace__subcmd__list_commands] )) ||
_vanta-cli__subcmd__namespace__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli namespace list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__put_commands] )) ||
_vanta-cli__subcmd__put_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli put commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__query_commands] )) ||
_vanta-cli__subcmd__query_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli query commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__rebuild-index_commands] )) ||
_vanta-cli__subcmd__rebuild-index_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli rebuild-index commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__repair-text-index_commands] )) ||
_vanta-cli__subcmd__repair-text-index_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli repair-text-index commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__restore_commands] )) ||
_vanta-cli__subcmd__restore_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli restore commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__search_commands] )) ||
_vanta-cli__subcmd__search_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli search commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__search-all_commands] )) ||
_vanta-cli__subcmd__search-all_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli search-all commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__search-multi_commands] )) ||
_vanta-cli__subcmd__search-multi_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli search-multi commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__server_commands] )) ||
_vanta-cli__subcmd__server_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli server commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__similar-to-key_commands] )) ||
_vanta-cli__subcmd__similar-to-key_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli similar-to-key commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__snapshot_commands] )) ||
_vanta-cli__subcmd__snapshot_commands() {
    local commands; commands=(
'create:Create an instant filesystem snapshot by hard-linking all data files' \
'list:List all existing snapshots' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli snapshot commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__snapshot__subcmd__create_commands] )) ||
_vanta-cli__subcmd__snapshot__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli snapshot create commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__snapshot__subcmd__help_commands] )) ||
_vanta-cli__subcmd__snapshot__subcmd__help_commands() {
    local commands; commands=(
'create:Create an instant filesystem snapshot by hard-linking all data files' \
'list:List all existing snapshots' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli snapshot help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__snapshot__subcmd__help__subcmd__create_commands] )) ||
_vanta-cli__subcmd__snapshot__subcmd__help__subcmd__create_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli snapshot help create commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__snapshot__subcmd__help__subcmd__help_commands] )) ||
_vanta-cli__subcmd__snapshot__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli snapshot help help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__snapshot__subcmd__help__subcmd__list_commands] )) ||
_vanta-cli__subcmd__snapshot__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli snapshot help list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__snapshot__subcmd__list_commands] )) ||
_vanta-cli__subcmd__snapshot__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli snapshot list commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__stats_commands] )) ||
_vanta-cli__subcmd__stats_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli stats commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__status_commands] )) ||
_vanta-cli__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli status commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__tui_commands] )) ||
_vanta-cli__subcmd__tui_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli tui commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__wal_commands] )) ||
_vanta-cli__subcmd__wal_commands() {
    local commands; commands=(
'compact:Compact the WAL\: flush all data, archive the current WAL file, and start a fresh one' \
'vacuum:Remove tombstoned nodes from HNSW and reclaim space' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli wal commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__wal__subcmd__compact_commands] )) ||
_vanta-cli__subcmd__wal__subcmd__compact_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli wal compact commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__wal__subcmd__help_commands] )) ||
_vanta-cli__subcmd__wal__subcmd__help_commands() {
    local commands; commands=(
'compact:Compact the WAL\: flush all data, archive the current WAL file, and start a fresh one' \
'vacuum:Remove tombstoned nodes from HNSW and reclaim space' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'vanta-cli wal help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__wal__subcmd__help__subcmd__compact_commands] )) ||
_vanta-cli__subcmd__wal__subcmd__help__subcmd__compact_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli wal help compact commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__wal__subcmd__help__subcmd__help_commands] )) ||
_vanta-cli__subcmd__wal__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli wal help help commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__wal__subcmd__help__subcmd__vacuum_commands] )) ||
_vanta-cli__subcmd__wal__subcmd__help__subcmd__vacuum_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli wal help vacuum commands' commands "$@"
}
(( $+functions[_vanta-cli__subcmd__wal__subcmd__vacuum_commands] )) ||
_vanta-cli__subcmd__wal__subcmd__vacuum_commands() {
    local commands; commands=()
    _describe -t commands 'vanta-cli wal vacuum commands' commands "$@"
}

if [ "$funcstack[1]" = "_vanta-cli" ]; then
    _vanta-cli "$@"
else
    compdef _vanta-cli vanta-cli
fi

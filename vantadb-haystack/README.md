# VantaDB × Haystack

PyO3 cdylib that exposes a `VantaDBDocumentStore` class implementing Haystack's `DocumentStore` protocol backed by VantaDB.

**Methods:** `write_documents`, `filter_documents`, `delete_documents`, `count_documents`

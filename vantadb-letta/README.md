# VantaDB × Letta (MemGPT)

PyO3 cdylib that exposes a `LettaStore` class implementing Letta's `VectorStore` interface backed by VantaDB.

**Methods:** `store_memory`, `retrieve_memory`, `list_memories`, `delete_memory`

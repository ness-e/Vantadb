# VantaDB × LiteLLM

PyO3 cdylib that exposes a `VantaDBLiteLLM` class — a unified embedding provider that generates embeddings through liteLLM's API and stores/searches them in VantaDB.

**Methods:** `embed`, `search`, `store`

---
id: "DRV-062"
name: "Ollama client recreado en cada embed()"
created: "2026-07-14"
module: "vantadb-ollama/src/python.rs"
status: "ready"
estimate: "1 step (~10 lines)"
---

## Contract
"cargo check -p vantadb-ollama pasa"

## Atomic Steps
1. `vantadb-ollama/src/python.rs:35-60` — Add `client: Py<PyAny>` field, remove `base_url`; move client creation to `new()` with `py: Python` param; use cached `self.client.bind(py)` in `embed()`

## Checks
- cargo check -p vantadb-ollama
- cargo clippy -p vantadb-ollama --all-targets

## Blast Radius
- **Callers:** `integrations/ollama/vantadb_ollama/__init__.py` wraps `VantaDBOllama`; tests
- **Callees:** `PyModule::import`, `PyAny` methods — unchanged
- **API Changes:** None

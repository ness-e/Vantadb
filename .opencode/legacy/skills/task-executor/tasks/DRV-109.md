---
id: "DRV-109"
name: "LlamaIndex missing GIL release"
created: "2026-07-14"
module: "vantadb-llamaindex"
status: "ready"
estimate: "1 turn"
---

## Contract
"cargo check -p vantadb-llamaindex pasa, tests pasan"

## Atomic Steps
1. `add`: Phase-split — prepare metadata inputs with GIL → detach for `engine.put` (same as DRV-102)
2. `query`: Phase-split — detach for `engine.search` → `Python::attach` for result dicts (same as DRV-102)
3. `delete`: Add `py: Python` param + detach wraps entire loop (same as DRV-102)

## Skills
- source-driven-development (PyO3 pattern via DRV-102 precedent)
- ponytail full

## Checks
- cargo check -p vantadb-llamaindex
- cargo nextest run -p vantadb-llamaindex

## Blast Radius
- **Callers:** Python LlamaIndex `add()`, `query()`, `delete()` in `packages/llamaindex/vantadb_llamaindex/vectorstore.py`
- **Callees:** `VantaEmbedded::put()`, `VantaEmbedded::search()`, `VantaEmbedded::delete()` in core engine
- **API Changes:** None — `delete` gains `py: Python` but PyO3 injects it transparently
- **Test Impact:** `vantadb-llamaindex/tests/test_llamaindex.py` covers all 3 methods

## Investigation Notes
- Byte-for-byte identical code to pre-fix DRV-102 (vantadb-langchain)
- Pattern: `py.detach(|| { blocking_code })` for PyO3 0.29
- `delete` currently lacks `py: Python` — PyO3 injects it automatically

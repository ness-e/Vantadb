---
id: "DRV-006"
name: "Race condition en delete() — write lock dropped before index cleanup"
created: "2026-07-14"
module: "src/engine.rs"
status: "ready"
estimate: "1 step (~1 line)"
---

## Contract
"cargo check -p vantadb pasa, tests existentes pasan"

## Atomic Steps
1. `src/engine.rs:241` — Remove `drop(nodes);` so `RwLockWriteGuard` lives through `edge_index.remove_all_for_node()` + `scalar_index.remove_node()`, eliminating the unprotected window

## Skills
- ponytail full (1-line deletion, no new code)

## Checks
- cargo check -p vantadb
- cargo nextest run -p vantadb (tests use InMemoryEngine extensively)

## Blast Radius
- **Callers:** `src/sdk/api.rs` indirectly via tests; `InMemoryEngine` is public (`src/lib.rs:91`)
- **Callees:** `EdgeIndex::remove_all_for_node()`, `ScalarIndex::remove_node()` — both internally concurrent-safe (DashMap)
- **API Changes:** None — same signature
- **Test Impact:** All 30+ test cases using `InMemoryEngine::new()` exercise this path

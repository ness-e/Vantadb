---
id: "DRV-063"
name: "Ollama metadata no-string silenciosamente ignorados"
created: "2026-07-14"
module: "vantadb-ollama/src/python.rs"
status: "ready"
estimate: "1 step (~5 lines)"
---

## Contract
"cargo check -p vantadb-ollama pasa"

## Atomic Steps
1. `vantadb-ollama/src/python.rs:139-145` — Replace singular `v.extract::<String>().map(VantaValue::String)` with or_else cascade (String → Bool → Int → Float), same pattern as DRV-058

## Checks
- cargo check -p vantadb-ollama
- cargo clippy -p vantadb-ollama --all-targets

## Blast Radius
- **Callers:** `python.rs:store()` only, called from Python via PyO3. No Rust callers.
- **Callees:** `v.extract::<T>()` — PyO3 stable API
- **API Changes:** None

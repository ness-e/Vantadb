---
id: "REV-003"
name: "Add cargo-llvm-cov coverage job to CI — CII Silver requiere >=80%"
created: "2026-07-14"
module: ".github/workflows/ci-rust-10.yml"
status: "ready"
estimate: "1 turn"
---

## Contract
`.github/workflows/ci-rust-10.yml` coverage job fails the build when line coverage drops below 80%.

## Atomic Steps
1. Add coverage threshold step to `ci-rust-10.yml` coverage job that checks `cargo llvm-cov report --json` and exits 1 if line coverage < 80%

## Skills
ponytail full

## Checks
- git diff shows only the threshold addition
- No syntax errors in YAML

## Blast Radius
**CALLERS:** GitHub Actions runner invokes workflow on push/PR to main
**CALLEES:** `cargo-llvm-cov`, `cargo-nextest`, `.github/actions/rust-setup`
**API CHANGES:** None — no public API changed
**TEST IMPACT:** Coverage job now gates PRs below 80% coverage

## Investigation Notes
- `.github/workflows/ci-rust-10.yml` already has a `coverage` job (lines 220-264) that runs `cargo llvm-cov nextest` and generates `lcov.info`
- Missing: coverage threshold enforcement (CII Silver requires >=80%)
- `cargo llvm-cov report --json` outputs structured data with `data[0].totals.lines.percent`
- Python3 available on ubuntu-latest runners

---
id: "REV-015"
name: "Fix 6x no-explicit-any in demo.lazy.tsx"
created: "2026-07-14"
module: "web/src/routes"
status: "ready"
estimate: "🟢 5 turns"
---

## Contract
`npx tsc --noEmit` pasan sin errores, y el archivo `demo.lazy.tsx` no contiene `any` ni supresiones `eslint-disable-next-line @typescript-eslint/no-explicit-any`.

## Atomic Steps
1. Fix `PipelineFn` type — reemplazar `Promise<any>` con tipo concreto `Promise<HFExtractor>`, eliminar eslint-disable
2. Fix dynamic import `mod: any` — tipar como `{ pipeline: PipelineFn }`, eliminar eslint-disable
3. Run `tsc --noEmit` y verify clean

## Skills
`ponytail full`

## Checks
- `npx tsc --noEmit` (from web/)
- `npx eslint src/routes/demo.lazy.tsx` sin output

## Blast Radius
- **File:** `web/src/routes/demo.lazy.tsx` (solo este archivo)
- **CALLERS:** Ninguno (lazy route cargada por TanStack Router)
- **CALLEES:** `../wasm/vantadb_wasm.js`
- **API CHANGES:** Ninguno (solo tipado interno)
- **TEST IMPACT:** Sin tests existentes para este archivo

## Investigation Notes
REV-005 ya marcado ✅ en Backlog.md (14 ESLint errors parcialmente corregidos). REV-015 queda pendiente por los 6 `any` types. El archivo actualmente tiene 2 `any` literales + 2 `eslint-disable-next-line` comments = 4 ocurrencias. Se corrigen todas.

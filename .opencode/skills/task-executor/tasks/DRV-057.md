---
id: "DRV-057"
name: "OpenAI client recreado en cada embed()"
created: "2026-07-14"
module: "vantadb-openai/src/python.rs"
status: "ready"
estimate: "1 step (~10 lines)"
---

## Contract
"cargo check -p vantadb-openai pasa"

## Atomic Steps
1. `vantadb-openai/src/python.rs:35-60` — Add `client: Py<PyAny>` field to struct, remove `api_key`; move OpenAI client creation to `new()` with `py: Python` param; use cached `self.client.bind(py)` in `embed()`

## Skills
- ponytail full (remove api_key field since it's dead after caching)

## Checks
- cargo check -p vantadb-openai
- cargo clippy -p vantadb-openai --all-targets -- -D warnings

## Blast Radius
- **Callers:** `integrations/openai/vantadb_openai/__init__.py` — wraps `VantaDBOpenAI`, no direct field access; tests: `vantadb-openai/tests/test_openai.py`
- **Callees:** `PyModule::import`, `PyAny` method calls — unchanged
- **API Changes:** None — same Python API, same method signatures
- **Test Impact:** existing tests exercise `embed()` path, should pass unchanged

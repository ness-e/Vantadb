---
id: "DRV-058"
name: "OpenAI metadata no-string ignorado"
created: "2026-07-14"
module: "vantadb-openai/src/python.rs"
status: "ready"
estimate: "1 step (~5 lines)"
---

## Contract
"cargo check -p vantadb-openai pasa"

## Atomic Steps
1. `vantadb-openai/src/python.rs:store()` — Replace single `extract::<String>()` with cascade: String → Bool → Int → Float, using `or_else` chain

## Skills
- ponytail full

## Checks
- cargo check -p vantadb-openai
- cargo clippy -p vantadb-openai --all-targets

## Blast Radius
- **Callers:** Python `store()` method — same signature, same return type
- **Callees:** `PyAny::extract` for multiple types — stdlib PyO3
- **API Changes:** None — metadata dict still accepts same Python types, now preserves bool/int/float

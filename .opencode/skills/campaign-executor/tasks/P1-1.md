# P1-1: GloVe dataset descargado en cada test job

**Fuente:** `docs/plans/code-freeze-action-plan.md`
**Estado:** ✅ COMPLETED
**Creado:** 2026-07-17T18:30
**last-synced:** 2026-07-17T18:30

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `ci-rust-10.yml` — jobs `test` (L100-108) y `coverage` (L234-242) |
| Callees | `scripts/download_benchmark_datasets.sh` |
| Implicaciones | Solo CI, sin cambios de código. No afecta tests, build, API. |

## Contrato
"ci-rust-10.yml tiene `if: steps.cache-benchmark.outputs.cache-hit != 'true'` en ambos download steps"

## Herramientas necesarias
- Ninguna (solo edición YAML)

## Steps

### Step 1: Agregar guard condicional al download en job `test`
- **Archivos:** `.github/workflows/ci-rust-10.yml:106`
- **Acción:** agregar `if: steps.cache-benchmark.outputs.cache-hit != 'true'` al step "Download benchmark datasets"
- **Verify:** inspección visual del YAML

### Step 2: Agregar guard condicional al download en job `coverage`
- **Archivos:** `.github/workflows/ci-rust-10.yml:240`
- **Acción:** agregar `if: steps.cache-benchmark.outputs.cache-hit != 'true'` al step "Download benchmark datasets"
- **Verify:** inspección visual del YAML

# P1-3: Cache GloVe con key fija que nunca se invalida

**Fuente:** `docs/plans/code-freeze-action-plan.md`
**Estado:** ⬜ PENDING
**Esfuerzo:** 🟢 1h
**Tipo:** CI/Config
**Creado:** 2026-07-17T18:50
**last-synced:** 2026-07-17T18:50

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `ci-rust-10.yml` jobs `test` (L100-104) y `coverage` (L234-238) |
| Callees | Ninguno (solo CI, sin código) |
| Implicaciones | Solo CI. Próximo run después del merge tendrá cache miss (key nueva), descargará dataset una vez, luego cache queda poblada con key nueva. |

## Contrato
"`grep glove-100d- ci-rust-10.yml` muestra `hashFiles` en ambas cache keys"

## Herramientas necesarias
- Ninguna (solo edición YAML)

## Investigation Notes
- `hashFiles` es función nativa de GitHub Actions expressions, no requiere dependency.
- Sintaxis: `key: glove-100d-${{ hashFiles('scripts/download_benchmark_datasets.sh') }}`
- Fuente: `github.com/actions/cache` docs — `hashFiles` es el patrón recomendado para invalidar cache cuando cambia un archivo.

## Steps

### Step 1: Cambiar cache key fixed por hash en job `test`
- **Archivos:** `.github/workflows/ci-rust-10.yml:104`
- **Acción:** cambiar `key: glove-100d-v1` a `key: glove-100d-${{ hashFiles('scripts/download_benchmark_datasets.sh') }}`
- **Verify:** inspección visual del YAML
- **Estado:** ⬜ PENDING

### Step 2: Cambiar cache key fixed por hash en job `coverage`
- **Archivos:** `.github/workflows/ci-rust-10.yml:238`
- **Acción:** cambiar `key: glove-100d-v1` a `key: glove-100d-${{ hashFiles('scripts/download_benchmark_datasets.sh') }}`
- **Verify:** inspección visual del YAML
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- P1-1 ya agregó los `if: steps.cache-benchmark.outputs.cache-hit != 'true'` guards al download step.
- P1-1 + P1-3 juntos aseguran que: (a) cache key cambia cuando el script cambia, (b) download solo corre cuando hay cache miss.

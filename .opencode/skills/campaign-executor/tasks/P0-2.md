# P0-2: `continue-on-error: true` silencia fallos críticos

**Fuente:** `docs/plans/code-freeze-action-plan.md`
**Estado:** ✅ Completado
**Fecha:** 2026-07-17

## Objetivo

`memory-concurrency` job en `heavy-certification-50.yml` usa `continue-on-error: true` (L166), silenciando fallos en `memory_brutality`, `concurrency_parity`, `memory_api`, `memory_telemetry`, `edge_cases`. Tests que nadie monitorea.

## Cambio

**`.github/workflows/heavy-certification-50.yml:166`** — removido `continue-on-error: true`. Si estos tests fallan, el job se marca como failed y la certificación es visiblemente roja.

---
id: "DRV-007"
name: "Data race en filter_field() — scalar_index sin lock"
created: "2026-07-14"
module: "src/engine.rs"
status: "ready"
estimate: "1 step (~1 line)"
---

## Contract
"cargo check -p vantadb pasa"

## Atomic Steps
1. `src/engine.rs:350` — Add `let _nodes = self.nodes.read();` before `scalar_index.lookup()` so filter_field syncs with concurrent writers

## Skills
- ponytail full (1-line addition)

## Checks
- cargo check -p vantadb
- cargo clippy -p vantadb

## Blast Radius
- **Callers:** Only tests (`test_filter_field_matches`, etc.) — `InMemoryEngine::filter_field` is pub but not used in production SDK path
- **Callees:** `ScalarIndex::lookup()` — DashMap, already concurrent-safe
- **API Changes:** None — same signature, same return type
- **Test Impact:** existing filter_field tests exercise this path

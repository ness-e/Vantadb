# P1-5: Re-activar wasm-opt en build WASM

**Fuente:** `docs/plans/code-freeze-action-plan.md`
**Estado:** ⬜ PENDING
**Esfuerzo:** 🟢 15 min
**Tipo:** Config/Build
**Creado:** 2026-07-17T19:15
**last-synced:** 2026-07-17T19:15

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `vantadb-wasm/Cargo.toml:14-16`, `release-npm-61.yml` (WASM build) |
| Callees | Binaryen wasm-opt (descargado por wasm-pack en build time) |
| Implicaciones | WASM bundle se optimiza post-compile → ~30-50% más pequeño. CI siempre usa `jetli/wasm-pack-action@latest` → binaryen v128+. Local requiere wasm-pack que descargue binaryen v121+. |

## Contrato
"WASM build de release corre wasm-opt con default settings (-Os)"

## Investigation Notes
- **binaryen v121** (released ~2025): `bulk-memory-opt` y `call-indirect-overlong` features agregados para paridad con LLVM.
- **binaryen v128+** (released March 2026): version_128 disponible con soporte completo.
- **wasm-pack v0.15.0** (May 2026): descarga binaryen latest de GitHub releases en build time.
- **CI**: `jetli/wasm-pack-action` con `version: latest` → binaryen v128+.
- **vantadb-wasm** usa `target_feature = "simd128"` en `simd.rs` — SIMD es compatible con binaryen desde v100+.

## Steps

### Step 1: Remover wasm-opt = false y actualizar comentario
- **Archivos:** `vantadb-wasm/Cargo.toml:14-16`
- **Acción:** eliminar `wasm-opt = false` y su comentario ponytail
- **Verify:** inspección visual del TOML
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- `wasm-opt = true` es el default de wasm-pack para release profile — equivalente a eliminar la línea.
- Si un dev local tiene wasm-pack < 0.13 que descarga binaryen < v121, el build fallará con error de feature no soportada. Solución: `cargo install wasm-pack` para actualizar.

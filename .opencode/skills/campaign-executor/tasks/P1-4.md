# P1-4: macOS unificar con rust-setup action

**Fuente:** `docs/plans/code-freeze-action-plan.md`
**Estado:** ⬜ PENDING
**Esfuerzo:** 🟢 30 min
**Tipo:** CI/Config
**Creado:** 2026-07-17T19:00
**last-synced:** 2026-07-17T19:00

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers | `ci-rust-10.yml` job `test-macos` (L148-186) |
| Callees | `.github/actions/rust-setup/action.yml` (composite action) |
| Implicaciones | Solo CI. macOS job reemplaza 3 steps manuales por 1 call al action compartido. Homebrew system deps se mantienen (son macOS-specific, el action solo instala system deps en Linux). |

## Contrato
"macOS job usa `.github/actions/rust-setup` en lugar de toolchain/cache/nextest manuales"

## Herramientas necesarias
- Ninguna (solo edición YAML)

## Investigation Notes
- `rust-setup` action: toolchain + cache + nextest (condicional) + system-deps (solo Linux). Fuente: `.github/actions/rust-setup/action.yml`
- macOS system deps (homebrew rocksdb, llvm) son específicos de macOS — no se pueden mover al action compartido sin refactor mayor.

## Steps

### Step 1: Reemplazar setup manual por rust-setup
- **Archivos:** `.github/workflows/ci-rust-10.yml:152-174`
- **Acción:** 
  - Remover `dtolnay/rust-toolchain` + `Swatinem/rust-cache` + `Install cargo-nextest`
  - Agregar `uses: ./.github/actions/rust-setup` con `components: clippy`, `install-nextest: true`
  - Mantener `Install system dependencies` (homebrew)
- **Verify:** inspección visual del YAML
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- `install-system-deps: false` implícito (default) — macOS no necesita apt-get
- `swap-mb: 0` implícito (default) — macOS no usa swap
- `cache-bin: "false"` en el cache original se pierde — pero el action usa default `"false"` para ese input 👍

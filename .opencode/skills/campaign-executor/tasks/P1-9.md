# P1-9: WASM — SIMD duplicado (cosine_distance_simd)

**Fuente:** `docs/plans/code-freeze-action-plan.md`
**Estado:** ✅ COMPLETED
**Esfuerzo:** 🟢 10 min
**Tipo:** WASM/Cleanup
**Creado:** 2026-07-17
**Completado:** 2026-07-17

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Eliminado | `vantadb-wasm/src/simd.rs` (208 líneas) |
| Modificado | `vantadb-wasm/src/lib.rs:32` — removed `pub mod simd` |
| Alternativa | `vantadb::index::cosine_sim_f32` del core (usa `wide` crate, soporta WASM vía f32x4) |

## Contrato
"Eliminar SIMD duplicado en WASM. Usar `vantadb::index::cosine_sim_f32` si se necesita cosine similarity."

## Investigation Notes
- `cosine_distance_simd()` nunca es llamado fuera de `simd.rs` (dead code confirmado via codegraph_explore)
- Core `cosine_sim_f32()` usa `wide` crate que soporta WASM (f32x4 en 128-bit)
- Edge cases diferentes: WASM panics on mismatched lengths, core returns 0.0
- -208 líneas, -1 módulo público

## Steps

### Step 1: Eliminar simd.rs y su declaración de módulo
- **Archivos:** `vantadb-wasm/src/simd.rs` (eliminado), `vantadb-wasm/src/lib.rs:32` (pub mod simd removido)
- **Acción:** delete file + remove mod declaration
- **Verify:** `cargo check -p vantadb-wasm`
- **Estado:** ✅

## Dependencias
- Ninguna

## Notas
- `cosine_sim_f32` del core disponible como `vantadb::index::cosine_sim_f32` si futuras features WASM necesitan cosine similarity

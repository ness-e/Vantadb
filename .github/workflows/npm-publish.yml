name: NPM Publish

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (no actual publish)"
        required: false
        default: "true"
  push:
    tags: ["wasm-v*.*.*", "ts-v*.*.*"]

permissions:
  contents: read

concurrency:
  group: npm-publish-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  publish-wasm:
    name: Publish vantadb-wasm to npm
    if: startsWith(github.ref, 'refs/tags/wasm-v')
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.4.0
        with:
          version: latest

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Add wasm target
        run: rustup target add wasm32-unknown-unknown

      - name: Build WASM package
        run: wasm-pack build --release
        working-directory: vantadb-wasm

      - name: Publish to npm
        working-directory: vantadb-wasm/pkg
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-ts:
    name: Publish vantadb (TS) to npm
    if: startsWith(github.ref, 'refs/tags/ts-v')
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.4.0
        with:
          version: latest

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Add wasm target
        run: rustup target add wasm32-unknown-unknown

      - name: Build WASM package (needed for local dep)
        run: wasm-pack build --release
        working-directory: vantadb-wasm

      - name: Install dependencies
        working-directory: vantadb-ts
        run: npm install

      - name: Build TypeScript
        working-directory: vantadb-ts
        run: npm run build

      - name: Rewrite dependency for publish
        working-directory: vantadb-ts
        run: |
          WASM_VER=$(grep '^version' vantadb-wasm/Cargo.toml | head -1 | sed 's/.*= *"//' | sed 's/".*//')
          npm pkg set "dependencies.vantadb-wasm=^${WASM_VER}"

      - name: Publish to npm
        working-directory: vantadb-ts
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

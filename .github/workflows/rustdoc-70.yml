name: 'CI: Rustdoc — API Reference'

on:
  push:
    branches: [ "develop" ]
    paths:
      - 'src/**'
      - 'docs/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'vanta-memory/**'
      - '*.md'
      - '.github/workflows/rustdoc-70.yml'
  pull_request:
    branches: [ "main", "develop" ]
    paths:
      - 'src/**'
      - 'docs/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'vanta-memory/**'
      - '*.md'
      - '.github/workflows/rustdoc-70.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

permissions:
  contents: read

jobs:
  rustdoc:
    name: Build API Reference (cargo doc)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: rustdoc-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Rust (nightly for doc features)
        uses: ./.github/actions/rust-setup
        with:
          toolchain: nightly
          install-nextest: false
          install-llvm-cov: false
          # --all-features pulls rocksdb -> librocksdb-sys (bindgen) which
          # needs libclang: system deps are required despite rustdoc itself.
          install-system-deps: true
          swap-mb: 0

      - name: Build Documentation
        # rust-toolchain.toml pins stable, which would shadow the nightly
        # installed above: override to nightly so -Z RUSTDOCFLAGS parse.
        run: |
          rustup override set nightly
          cargo doc --no-deps --workspace --all-features --document-private-items
        env:
          RUSTDOCFLAGS: "--show-type-layout --enable-index-page -Z unstable-options"

      - name: Compress Documentation
        run: |
          tar -czf rustdoc-html.tar.gz -C target/doc .
          du -h rustdoc-html.tar.gz

      - name: Upload Documentation Artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: rustdoc-html
          path: rustdoc-html.tar.gz
          retention-days: 30
          if-no-files-found: error

      - name: Summary
        if: always()
        run: |
          echo "## Rustdoc Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Workflow: ${{ github.workflow }}" >> $GITHUB_STEP_SUMMARY
          echo "- Ref: ${{ github.ref }}" >> $GITHUB_STEP_SUMMARY
          echo "- Artifact: rustdoc-html.tar.gz (30-day retention)" >> $GITHUB_STEP_SUMMARY
          echo "- Command: cargo doc --no-deps --workspace --all-features --document-private-items" >> $GITHUB_STEP_SUMMARY
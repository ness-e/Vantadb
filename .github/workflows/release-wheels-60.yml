name: 'RELEASE: Wheels — Build & Publish'

on:
  workflow_dispatch:
    inputs:
      publish_testpypi:
        description: "Upload built wheels to TestPyPI"
        required: false
        default: "false"
  pull_request:
    branches: ["main"]
    paths:
      - "src/**"
      - "vantadb-python/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/release-wheels-60.yml"
  push:
    tags: ["v*.*.*"]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  build-wheels:
    name: Build Python wheel (${{ matrix.wheel_id }})
    if: github.actor != 'dependabot[bot]'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            wheel_id: linux-x86_64
          - os: macos-latest
            wheel_id: macos
          - os: windows-latest
            wheel_id: windows
          # MKT-18h: aarch64 Linux wheels — cross-compiled inside maturin-action's
          # official manylinux_2_28 cross container (ghcr.io/rust-cross/
          # manylinux_2_28-cross:aarch64). musllinux aarch64 is out of scope (BND-09).
          # Smoke test is skipped for this target: an aarch64 wheel cannot be
          # pip-installed on the x86_64 runner; correctness is gated by the wheel
          # platform tag (manylinux_2_28_aarch64) + the PyPI install verify job.
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            wheel_id: linux-aarch64

    steps:
      - name: Configure pagefile (Windows — prevent OOM)
        if: matrix.os == 'windows-latest'
        uses: al-cheb/configure-pagefile-action@9b6da52fb72a3c6147c1aad2df22d8d905681adc # v1.5
        with:
          minimum-size: 8GB
          maximum-size: 16GB

      - name: Free Disk Space (macOS — allow dynamic pager growth)
        if: matrix.os == 'macos-latest'
        run: |
          sudo rm -rf /opt/hostedtoolcache || true
          df -h /

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # v1
        with:
          toolchain: stable

      - name: Rust cache
        uses: Swatinem/rust-cache@7e35be21c2b94d972b1143087fabc27d7dc881ef # v2.9.1
        with:
          cache-bin: "false"

      - name: Install Python build tools
        run: python -m pip install --upgrade pip pytest

      - name: Validate version coherence
        run: cargo test --test version_coherence --no-default-features --features fjall

      - name: Build wheel
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          command: build
          args: --release --out dist --manifest-path ./vantadb-python/Cargo.toml
          target: ${{ matrix.target }}
          manylinux: 2_28
          # FIND-127: el container cross aarch64 (ghcr.io/rust-cross/manylinux_2_28-cross)
          # es Debian-based (sin yum) mientras el pypa x86_64 usa yum — rama portable.
          before-script-linux: |
            if command -v yum >/dev/null 2>&1; then yum install -y clang clang-devel; else apt-get update && apt-get install -y clang; fi

      - name: Install & smoke test (Unix)
        if: matrix.os != 'windows-latest' && !matrix.target
        shell: bash
        run: |
          wheel="$(ls -t ./dist/vantadb_py-*.whl | head -n 1)"
          echo "Installing wheel: $wheel"
          python -m venv /tmp/.smoke-venv
          /tmp/.smoke-venv/bin/pip install --upgrade pip
          /tmp/.smoke-venv/bin/pip install "$wheel" pytest numpy
          /tmp/.smoke-venv/bin/python -c "import vantadb_py; print('Import OK:', vantadb_py.__version__)"
          /tmp/.smoke-venv/bin/pytest vantadb-python/tests/test_sdk.py -v

      - name: Install & smoke test (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $wheel = (Get-ChildItem .\dist\vantadb_py-*.whl | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName
          Write-Output "Installing wheel: $wheel"
          python -m venv "$env:TEMP\.smoke-venv"
          & "$env:TEMP\.smoke-venv\Scripts\pip" install --upgrade pip
          & "$env:TEMP\.smoke-venv\Scripts\pip" install $wheel pytest numpy
          & "$env:TEMP\.smoke-venv\Scripts\python" -c "import vantadb_py; print('Import OK:', vantadb_py.__version__)"
          & "$env:TEMP\.smoke-venv\Scripts\pytest" vantadb-python/tests/test_sdk.py -v

      - name: Upload wheel artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: wheels-${{ matrix.wheel_id }}
          path: ./dist/*.whl
          if-no-files-found: error


  publish-testpypi:
    name: Publish wheels to TestPyPI
    runs-on: ubuntu-latest
    needs: build-wheels
    timeout-minutes: 10
    if: github.event_name == 'workflow_dispatch' && inputs.publish_testpypi == 'true'
    environment: testpypi
    permissions:
      id-token: write

    steps:
      - name: Download wheels
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: wheels-*
          path: ./dist
          merge-multiple: true

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
        with:
          repository-url: https://test.pypi.org/legacy/
          verbose: true
          skip-existing: true

  publish-pypi:
    name: Publish wheels to PyPI (Production)
    runs-on: ubuntu-latest
    needs: build-wheels
    timeout-minutes: 10
    if: startsWith(github.ref, 'refs/tags/v')
    environment: pypi
    permissions:
      id-token: write
      contents: write
      attestations: write

    steps:
      - name: Download wheels
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: wheels-*
          path: ./dist
          merge-multiple: true

      # CATEGORY: NON-CRITICAL — attest-build-provenance may fail if OIDC
      # tokens are unavailable (e.g., fork PRs). Does not block wheel publishing.
      - name: Attest Build Provenance for Wheels
        continue-on-error: true
        uses: actions/attest-build-provenance@78e6cbd37d0ac1a40113c04f2037dacf1ea3f12e # v4.1.1
        with:
          subject-path: './dist/*.whl'

      - name: Attach wheel artifacts to GitHub Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          files: |
            ./dist/*.whl

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
        with:
          packages-dir: dist/
          verbose: true

  verify-testpypi-install:
    name: Verify TestPyPI install
    runs-on: ubuntu-latest
    needs: publish-testpypi
    timeout-minutes: 10
    if: github.event_name == 'workflow_dispatch' && inputs.publish_testpypi == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Extract version from tag or Cargo.toml
        id: pkg_version
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"
          else
            version=$(grep -A1 '^\[workspace\.package\]' Cargo.toml | grep '^version' | sed 's/.*"\(.*\)"/\1/')
            echo "version=${version}" >> "$GITHUB_OUTPUT"
          fi

      - name: Wait for TestPyPI CDN propagation
        run: sleep 30

      - name: Install from TestPyPI
        run: |
          python -m pip install \
            --index-url https://test.pypi.org/simple/ \
            --extra-index-url https://pypi.org/simple/ \
            "vantadb-py==${{ steps.pkg_version.outputs.version }}"

      - name: Functional smoke test - verify published wheel
        env:
          VANTADB_EXPECTED_VERSION: ${{ steps.pkg_version.outputs.version }}
        shell: bash
        run: |
          cp "${{ github.workspace }}/vantadb-python/verify_published_wheel.py" /tmp/
          cd /tmp
          python /tmp/verify_published_wheel.py

  verify-pypi-install:
    name: Verify PyPI production install + Attest provenance
    runs-on: ubuntu-latest
    needs: publish-pypi
    timeout-minutes: 15
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      attestations: read

    steps:
      - name: Checkout (for attestation verification)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Extract version from tag
        id: pkg_version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Install from PyPI with retry on CDN delay
        run: |
          set -e
          for i in 1 2 4 8 16 32 64; do
            python -m pip install --no-cache-dir \
              "vantadb-py==${{ steps.pkg_version.outputs.version }}" && break
            echo "Package not yet available, retrying in ${i}s..."
            sleep $i
          done

      - name: Functional smoke test - verify published wheel
        env:
          VANTADB_EXPECTED_VERSION: ${{ steps.pkg_version.outputs.version }}
        shell: bash
        run: |
          cp "${{ github.workspace }}/vantadb-python/verify_published_wheel.py" /tmp/
          cd /tmp
          python /tmp/verify_published_wheel.py

      - name: Verify build provenance (GitHub Attestations)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          pip download --no-deps --dest /tmp/verify_whl \
            "vantadb-py==${{ steps.pkg_version.outputs.version }}"
          wheel_file=$(ls /tmp/verify_whl/vantadb_py-*.whl | head -1)
          echo "Verifying provenance for: ${wheel_file}"
          gh attestation verify "${wheel_file}" \
            --repo "${{ github.repository }}" \
            --format json | python -c "
          import sys, json
          data = json.load(sys.stdin)
          verifications = data if isinstance(data, list) else [data]
          assert len(verifications) > 0, 'No attestation found'
          print(f'Attestations verified: {len(verifications)}')
          print('Build provenance attestation: VERIFIED')
          "
          echo "Supply-chain integrity: VERIFIED"
